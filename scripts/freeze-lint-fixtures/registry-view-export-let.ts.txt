// Fixture: SHOULD-2 (round-5, opus reviewer's own probe) — `export let alternateView = {...}`
// was fully invisible to the pre-fix census: extractTopLevelConstExports' own name regex only
// matched `export const`, never `export let`/`export var`.
// (.ts.txt so the repo's eslint/tsc sweeps never parse fixture code — DATA.)
import { closeExampleV1 } from "./closeExample.v1.js";

export const workflows = {
  closeExample: closeExampleV1,
} as const;

export const workflowsByName: Readonly<Record<string, (input: any) => Promise<unknown>>> = Object.freeze(workflows);

export let alternateView = { evilThing: (x: unknown) => Promise.resolve(x) };
