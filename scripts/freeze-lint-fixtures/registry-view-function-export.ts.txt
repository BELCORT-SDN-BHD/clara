// Fixture: #11 (round-4 review, REOPENED) — registry.ts declaring and exporting a function of
// its own. Even a genuinely-shaped `(input) => Promise<unknown>` callable is REJECTED on sight:
// registry.ts's own closed world is imports + `workflows`/`workflowsByName` + safe derivations +
// bare re-exports, never a fresh declaration of its own.
// (.ts.txt so the repo's eslint/tsc sweeps never parse fixture code — DATA.)
import { closeExampleV1 } from "./closeExample.v1.js";

export const workflows = {
  closeExample: closeExampleV1,
} as const;

export const workflowsByName: Readonly<Record<string, (input: any) => Promise<unknown>>> = Object.freeze(workflows);

export async function alternateView(input: unknown): Promise<unknown> {
  return input;
}
