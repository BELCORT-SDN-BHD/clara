// Fixture: canonical registry, closeExample -> V1.
// (.ts.txt so the repo's eslint/tsc sweeps never parse fixture code — this is
// DATA for check-frozen-workflows.selftest.mjs, not compiled source.)
import { closeExampleV1 } from "./closeExample.v1.js";

export const workflows = {
  closeExample: closeExampleV1,
} as const;

export const workflowNames: string[] = Object.keys(workflows);
