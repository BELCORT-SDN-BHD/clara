// Fixture (REJECT: ENQUEUE-UNTRACEABLE) — S4-FX6 sibling: re-aliasing the
// enqueue namespace (`const alias = api`) would let every later alias.start(...)
// escape the per-namespace scans. Refused at the alias site (fail-closed).
import * as api from "workflow/api";
import { chatTurnV1 } from "../workflows/chatTurn.v1.js";

const alias = api;

export async function kick(): Promise<void> {
  await alias.start(chatTurnV1, []);
}
