{
  "version": 1,
  "evaluators": {
    "clara.evaluate_agreement_contract_state_v1": {
      "sha256": "bbddec2f7028bcf8d07d0cd1942495a29a327b28f125a81a09a8b0536f509543",
      "migration": "packages/db/migrations/0299_agreement_contract_acquisition.sql",
      "deployed": false,
      "note": "#948, the agreement contract's deterministic evaluator: two channel envelopes in, one fact state out — which KIND of agreement the page calls itself (a closed ordered keyword roster over the printed rendering, so the model quotes and never classifies), each scheduled instalment's own principal-plus-interest identity, the three schedule column sums cross-checked against the printed totals, deposit plus amount financed against the cash price, the instalments reconciled to financed plus charges, and the text-vs-vision comparison. It is named clara.evaluate_* DELIBERATELY: this lint discovers only that spelling, so an underscore-named entrypoint would be catalog-frozen at apply and invisible to review, which is the half-freeze this manifest exists to close. Its clara.evaluator_versions closure is ONE member and that is structural, not stylistic — the body reads no table and calls no other clara function (asserted live by packages/db/tests/agreement-contract-acquisition.test.mjs's closure cell, both structurally and by a call-shape scan of prosrc), so the rendering-to-cents normalization is written inline rather than reaching for clara._normalize_invoice_cents or for 0296's own copy. UNDEPLOYED at birth: 0060's _tf_evaluator_deploy_once makes the flip a one-way ceremony act. The entry is hand-inserted rather than --update re-baselined so this change blesses ONE body and re-hashes nobody else's (0111's own recorded reason, 0296's own use)."
    },
    "clara.evaluate_fs_pack_agent_v1": {
      "sha256": "d94696991276094afc02bd202ec1c0aa6c97479cdd5565c35042e5c3f9490ae5",
      "migration": "packages/db/migrations/0111_f_a5_reporting_agency_pr1.sql",
      "deployed": true,
      "note": "F-A5 PR-1, the agent lane's pack entrypoint. DEPLOYED — deploy-lock stamped by the ceremony (`deployed: true`); born UNDEPLOYED because 0060's _tf_evaluator_deploy_once makes the flip a one-way ceremony act, and the core itself refused `evaluator_undeployed` until the owner ran it from merged main (design SS3.2 / F5-D28). It is named clara.evaluate_* DELIBERATELY -- this lint discovers only that spelling, so an underscore-named entrypoint would be catalog-frozen at apply and invisible to review, which is the half-freeze this manifest exists to close. The eight OTHER members of its closure are delta's already-frozen bodies (evaluate_metric v1's ordinals 1-8, clara._hash included) and are NOT renamed: renaming a frozen member IS moving it. They stay DB-frozen and source-invisible, the same posture clara.assess_metric_cell_independent_v1 already holds. The entry is hand-inserted rather than --update re-baselined so this PR blesses ONE body and re-hashes nobody else's. (Authored as UNNUMBERED_f_a5_reporting_agency_pr1.sql; re-pointed to 0111 in the commit that claimed the numbers, per hard constraint 10.)"
    },
    "clara.evaluate_fs_pack_v1": {
      "sha256": "69086e62a2c9f52989f9bedb821343dca26e4dcf74cb242f05dc56ef30b8308b",
      "migration": "packages/db/migrations/0059_wave_e_delta_metrics_behavior.sql",
      "deployed": true,
      "note": "Wave E lane delta, the FS pack driver. DEPLOYED: the 0058-0063 ceremony ran 2026-08-14 from merged main and verify_evaluator_freeze reports 2/2 closures deployed and frozen on the live project. This entry said UNDEPLOYED because it was written before that ceremony and never trued afterwards (codex M6) -- which would have left the live body outside the append-only hash lock the moment the checker was wired, so a source rebaseline of a LIVE evaluator would have passed."
    },
    "clara.evaluate_metric_v1": {
      "sha256": "2de7665d627875c1e575970b32363b57f4ef376f99c0525aade798f54ce1f2d2",
      "migration": "packages/db/migrations/0059_wave_e_delta_metrics_behavior.sql",
      "deployed": true,
      "note": "Wave E lane delta, the typed metric evaluator. DEPLOYED -- see the fs_pack note. The DB half (clara.verify_evaluator_freeze, run by migrate.mjs between every migration body and its commit) compares only rows flagged deployed, and both delta closures now are; this manifest must agree with that, because a deployed-vs-undeployed disagreement is exactly the gap an append-only lock cannot see across."
    },
    "clara.evaluate_payroll_run_state_v1": {
      "sha256": "0efbe1fb33afedb68bbd142feecab5ac20cff07938225eb62da58557e98507eb",
      "migration": "packages/db/migrations/0296_payroll_summary_typed_facts.sql",
      "deployed": false,
      "note": "#945, the payroll run's deterministic evaluator: two channel envelopes in, one fact state out — the column sums over the quoted employee rows, each row's own gross-minus-deductions identity, the cross-check against a printed totals row where one exists, and the text-vs-vision comparison. It is named clara.evaluate_* DELIBERATELY: this lint discovers only that spelling, so an underscore-named entrypoint would be catalog-frozen at apply and invisible to review, which is the half-freeze this manifest exists to close. Its clara.evaluator_versions closure is ONE member and that is structural, not stylistic — the body reads no table and calls no other clara function (asserted live by packages/db/tests/payroll-summary-facts.test.mjs's closure cell, both structurally and by a call-shape scan of prosrc), so the rendering-to-cents normalization is written inline rather than reaching for clara._normalize_invoice_cents, which would have dragged the whole F-A1 witness freeze in with it. UNDEPLOYED at birth: 0060's _tf_evaluator_deploy_once makes the flip a one-way ceremony act, and #945 ships the READING half only — no posting verb consults this state yet. The entry is hand-inserted rather than --update re-baselined so this change blesses ONE body and re-hashes nobody else's (0111's own recorded reason)."
    },
    "clara.evaluate_payroll_run_state_v2": {
      "sha256": "5bc6cb6cf0926b4ded8958a32b8037e9e3855382c3a7fc8ae664215506fbb0f5",
      "migration": "packages/db/migrations/0343_payroll_completeness_witness.sql",
      "deployed": false,
      "note": "#1048, the payroll run's deterministic evaluator, SECOND version -- v1's eleven run-level questions, row census, row identity, column sums, printed-total cross-check and text-vs-vision comparison byte for byte, PLUS the two COMPLETENESS WITNESSES a page may print about itself (an employee headcount and a page count) and the completeness verdict read off them against the row census. A NEW _vN BESIDE THE FROZEN v1, never a recut of it: 0296 registered v1's single-member closure and clara.verify_evaluator_freeze() re-derives it LIVE from the catalog between every migration body and its commit, so an in-place edit fails at APPLY, not merely at review -- the same posture clara.evaluate_metric_v2 took beside evaluate_metric_v1 (0135). The two new keys (`witness`, `completeness`) are deliberately OUTSIDE `facts`: clara._payroll_posting_verdict folds an unanswered fact into its arithmetic rung, so a witness question the frozen payrollFacts_v1 prompts do not ask yet would have blocked every payroll run in the estate on the grounds that the page does not add up. A count is parsed as a whole number by this body, never through v1's money normalization, because \"2\" is two employees and not two ringgit. It is named clara.evaluate_* DELIBERATELY: this lint discovers only that spelling, so an underscore-named entrypoint would be catalog-frozen at apply and invisible to review. Its clara.evaluator_versions closure is ONE member and that is structural, not stylistic -- the body reads no table and calls no other clara function (asserted live by packages/db/tests/payroll-completeness-witness.test.mjs's closure cell), so both normalizations are written inline rather than reaching for clara._normalize_invoice_cents or for v1's own copy. UNDEPLOYED at birth: 0060's _tf_evaluator_deploy_once makes the flip a one-way ceremony act, and the freeze binds regardless -- the flag is about traffic, not about immutability. The entry is hand-inserted rather than --update re-baselined so this change blesses ONE body and re-hashes nobody else's (0111's own recorded reason, 0296's and 0299's own use; measured here -- a --update run re-serialized all twelve entries and moved four `deployed` keys, which is exactly the blast radius this practice exists to avoid)."
    },
    "clara.evaluate_metric_v2": {
      "sha256": "ba2caa43c5eb5eaa3cbaa364fb4f7e69121e0c47aab6f59c89f0843e35c41dcc",
      "migration": "packages/db/migrations/0135_card1_substitution_seam.sql",
      "note": "Wave F Track-A, F-A5b CARD 1: the substitution seam's stage-(b) evaluator -- v1's grammar plus ONE closed leaf kind, `cell`, which resolves an already-minted, definition-backed metric_cells row. A NEW _vN beside the frozen v1, never a recut of it: v1's four bodies are members of a deployed closure and clara.verify_evaluator_freeze() re-derives their hashes LIVE from the catalog between every migration body and its commit, so an in-place edit fails at APPLY, not merely at review. DEPLOYED — deploy-lock stamped by the ceremony (`deployed: true`): 0060's _tf_evaluator_deploy_once made the flip a one-way ceremony act under the bare migration principal, and _eta_compose_metric_preview_core_v2 refused `evaluator_undeployed` once it ran -- so stage (b) shipped DARK, which is the expected post-merge state and not a defect. It is named clara.evaluate_* DELIBERATELY: this lint discovers only that spelling, so a vocabulary reachable only through underscore-named cores would be catalog-frozen at apply and INVISIBLE to review. Its DB-side closure is NINE members -- the four v2 bodies plus the five v1 helpers reused verbatim (_metric_selector_account_ids, _metric_input_dataset_v1, _metric_context_sha256_v1, _metric_resolved_inputs_sha256_v1, _hash). v1's tenth member, clara.evaluate_fs_pack_v1, is DELIBERATELY not a member of v2's: it is pinned to v1's entrypoint and is no part of v2's determinism, so freezing it here would assert a closure v2 does not have. The entry is hand-inserted rather than --update re-baselined so this PR blesses ONE body and re-hashes nobody else's.",
      "deployed": true
    },
    "clara.evaluate_sst_watch": {
      "sha256": "57e644849be142435c0331a953aaa98cfa7282d0dfdbb6fc818382cd0b80440c",
      "migration": "packages/db/migrations/0016_a21_compliance_watch.sql",
      "deployed": true,
      "note": "Wave A2.1 SST compliance-watch evaluator. LIVE on the project (0016 is applied), so its body is immutable vs origin/main forever. It predates clara.evaluator_versions and has no registry row; the DB-side verifier therefore says nothing about it and THIS manifest is its only freeze. That asymmetry is recorded rather than papered over: adding a retrospective registry row would be a claim about a deploy nobody made."
    },
    "clara.evaluate_sst_watches_all": {
      "sha256": "2aead0a6e62e9cfbfcbb50e173d61721356be0da602db0cc40f83372e743f569",
      "migration": "packages/db/migrations/0016_a21_compliance_watch.sql",
      "deployed": true,
      "note": "The all-clients driver beside evaluate_sst_watch; same LIVE status and the same recorded asymmetry."
    },
    "clara.evaluate_witness_fact_state_v1": {
      "sha256": "177f788971c60b9d471f6b756711f7e8ad7b73b8f8392c8a97bea053d27b2ea5",
      "migration": "packages/db/migrations/0092_f_a1_predicate.sql",
      "note": "F-A1 PR-1, the LLM witness-pair corroboration predicate. DEPLOYED — deploy-lock stamped by the ceremony (`deployed: true`), granted by --lock-deployed after the PR-1 ceremony ran from merged main, never by --update. (Authored as UNNUMBERED_f_a1_predicate.sql; re-pointed to 0092 in the commit that claimed the numbers, per hard constraint 10.) Registry closure is FOUR members, one more than the design's three: the file-size split gave the identity geometry its own body (clara.evaluate_witness_identity_v1), and a called leaf outside the closure is exactly the half-freeze this manifest exists to prevent.",
      "deployed": true
    },
    "clara.evaluate_witness_fact_state_v2": {
      "sha256": "fbe0e56dacb844fd051f558263df3cb372a6ef4a77875bcc1f5803d0ec3de2e9",
      "migration": "packages/db/migrations/0100_f_a2_nil_tax_arm_part2.sql",
      "note": "F-A2 opener 1, the three-locks nil-tax arm. A NEW body beside the FROZEN v1, never a recut of it: v1's closure is frozen with four members and its body can never be replaced, which is exactly why a behavioural change ships as a _vN. v1 stays in the catalog, stays registered and stays deployed; it merely becomes unreachable once the resolver repoint lands. The closure is the SAME four members with the v2 entrypoint at ordinal 0. DEPLOYED 2026-08-21 at the combined Window A+B ceremony (live 97/0102); the deploy-lock below was granted by --lock-deployed from merged main at that ceremony's close, never by --update -- which is the shape this note previously described in the future tense. (Authored as UNNUMBERED_f_a2_nil_tax_arm_part2.sql; re-pointed to 0100 in the commit that claimed the numbers, per hard constraint 10, against a repo frontier of 0098 -- this entry and the battery both gate on the STABLE SUFFIX, never the number.) This entry is an APPEND: v1's recorded hash is byte-untouched above, which is the only lawful shape the append-only check admits.",
      "deployed": true
    },
    "clara.evaluate_witness_identity_v1": {
      "sha256": "859b5728dfe064af2443303ffef9dfe38079bb374c7344445846bf4c0add03f9",
      "migration": "packages/db/migrations/0091_f_a1_identity_helper.sql",
      "note": "F-A1 PR-1, the identity verdict leaf -- closure leaf of clara.evaluate_witness_fact_state_v1, carrying its OWN entry so check-frozen-evaluators' clara.evaluate_* discovery covers it AT REVIEW TIME. It was authored as clara._witness_identity_v1 and renamed at the adjudicated review (M7): under the underscore name it was catalog-frozen (a closure member checked at apply time by verify_evaluator_freeze) but SOURCE-unfrozen, the exact half-freeze this manifest's own note warns about. clara._fact_hash and clara._normalize_invoice_cents remain apply-time-covered ONLY -- they are pre-existing 0009 leaves with ~30 live call sites and renaming them is not this PR's change; stated per the review's M7 rather than left implicit. DEPLOYED — deploy-lock stamped by the ceremony (`deployed: true`), same ceremony rule as the entrypoint: the lock was granted by --lock-deployed after the PR-1 ceremony, never by --update.",
      "deployed": true
    }
  }
}
