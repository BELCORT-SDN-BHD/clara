# Keep the dependency graph honest across the workspace.
# - strict-peer-dependencies off: the WDK beta stack (nitro/workflow) ships
#   loose peers; we pin exact versions instead (see each package.json).
# - shared-workspace-lockfile: one lockfile at the root.
strict-peer-dependencies=false
shared-workspace-lockfile=true
auto-install-peers=true
