# dependencies
node_modules/
.pnpm-store/

# env / secrets (only *.example is tracked)
.env
.env.*
!.env.example
*.pem

# build output
dist/
.output/
.nitro/
.next/
out/
*.tsbuildinfo

# test / coverage
coverage/
.workflow-data/
.workflow-data-dryrun/

# DR / backups (may contain data — never commit)
backups/
*.dump
*.sql.gz

# logs / os
*.log
.DS_Store
Thumbs.db

# spike (frozen; keeps its own env)
spike/.env

# Local Claude Code state — except repo-authored shared skills.
.claude/*
!.claude/skills/
.claude/skills/*
!.claude/skills/orchestrator-fable/
