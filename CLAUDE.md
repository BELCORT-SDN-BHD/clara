@AGENTS.md

<!-- INVERTED (harness refactor, 2026-08-12): AGENTS.md is now the single agent entry point —
     it is the cross-tool convention (Codex, Cursor and others read it natively), so one file
     serves every agent and none of them can drift from another. This file exists only to
     import it. Put nothing else here; every edit goes to AGENTS.md. -->
