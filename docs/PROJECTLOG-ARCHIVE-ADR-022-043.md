# PROJECTLOG ARCHIVE — ADR-022 .. ADR-043 (Phase 4: Waves A, A2, A2.1, B — build + deploy + gates)

> **The same append-only decision log, middle span.** Split out of `docs/PROJECTLOG.md` on
> 2026-07-27 at the Wave-B close (ADR-046) to keep the active log lean — a limit an append-only
> log reaches by design, so the fix is a split, never a rewrite or a prune. **Nothing here has
> been edited**: original entries, byte-for-byte, original order.
>
> **Cite these by number as before** (`ADR-025`, etc.) — the numbering is global: ADR-001..021
> in `PROJECTLOG-ARCHIVE-ADR-001-021.md`, ADR-044 onward in `docs/PROJECTLOG.md` (plus PART 2,
> open items). To supersede a decision recorded here, append a NEW entry there; never touch
> this file.
>
> Coverage: the Wave-A daily loop (contract, build, live, ADR-022..024) · Wave A2 AR/MyInvois/
> bounded auto-post (025..027) · Wave A2.1 compliance watch (028..030) · the Wave-B contract +
> rulings (031..037) · migrations 0017..0021 with their deploy ceremonies (033..036, 038..043) ·
> Gates O + K closed on Rome Secretary and Bee Creative (040, 043).

### ADR-022 — The Wave-A daily-loop design contract (Phase 4 opens)
**Decision:** Phase-4 Wave A is ratified as `docs/plan/wave-a-daily-loop-contract.md` v1.1 + `wave-a-migration-0011-design.md` v1.1: twelve grill rulings **WA-R1..R12** (AP-only scope with aliases + identity-equivalence merge; three-lane DB-gated confidence ladder READY/NEEDS-REVIEW/NEEDS-YOU with qualitative labels; an event-driven `autoDraft_v1` sweep — drafts only, one bill per task, reserve-first token budgeting, receipt-based bookkeeper+ acknowledgement; one cross-client firm review queue subsuming the notification inbox; routine-only batch approve via a dedicated `approve_routine_entry` that structurally refuses high-stakes; doc_review split-view with honest no-region degradation; dual-source human-signed sticky KB rules over transactional per-sighting evidence; scoped open-question objects with a serialized CLR26 approval gate; DB-computed document-baseline diffs over a new append-only revision-snapshot table; gross-to-expense stands for SST supplier bills) plus five review deltas **WA-D1..D5** (the egress lane-carve — OCR is firm-gated because it structurally precedes client attribution, per-client consent gates invoice_facts with an every-active-filing rule and a last-boundary recheck; the sweep runs the coding model; merge never rewrites posted lines and never crosses differing registrations; a signed rule resolves account-choice uncertainty ONLY; approving a high-stakes agent-made draft requires the approver's recorded attestation). **Two supersessions:** WA-R2 supersedes the S6-R1 RPR-only constraint — the owner declared the C6/consent material handled, confirmed and valid (2026-07-21) — and WA-R7 boundedly reopens the §11 batch-approve deferral. New error codes CLR26–29; a new `autodraft` agent-task kind + wake_kind with an exact five-fn allowlist; migration 0011 opens with the AB-3 `engine_kind` pin.
**Why:** The full house ladder ran at design stage: owner grill (12 rulings playback-ratified) → dual design review (native FLAWED, 13 findings; Codex xhigh FLAWED, 24 findings — 8 CRITICAL, independently converging on four defects and adding six more incl. a posted-line-immutability violation in the v1.0 merge design and a consent-revocation replay hole) → a 10-probe empirical battery on a fresh throwaway PG16 (10/10 SUPPORTED — incl. live proof that a high-stakes agent draft was approvable by one bookkeeper with no attestation, the WA-D5 motivation, and that the existing token admission is chat-only read-then-act, so the sweep's reserve-first primitive is genuinely new) → one consolidated owner delta-ratification. v1.0 was not safe to hand to a build lane; every defect was caught before a line of code. Evidence: `docs/plan/research/wave-a/{design-review-native,design-review-codex,design-probes}.md`.

### ADR-023 — The Wave-A daily-loop BUILD (as-built)
**Decision:** Phase-4 Wave A is BUILT on `build/wave-a-daily-loop` as a five-lane landing: migration `0011_daily_loop.sql` (opening with the AB-3 `engine_kind` pin) — the counterparty-alias/identity-merge core, the DEFINER lane function + `autodraft` task lane, sweep runs + reserve-first budget, the single-snapshot review queue, transactional per-sighting rules + scoped open-questions with the serialized CLR26 gate, append-only revision snapshots + DB-computed diffs, and the per-client egress registry with the WA-D1 lane-carve; the runtime `autodraft` consumer + `autoDraft_v1` frozen workflow + the `GET /api/documents/:id/bytes` route (`src/documentRoutes.ts`); the dashboard `/queue` surface with the dashboard-canonical ClaraPart union (`app/shared/parts.ts`, 9+5 types), doc_review split-view, and the CLR26–29 cards; and the matcher-adjacent content-scan capture fix in `invoiceFacts.v1.azure.mjs`. **The as-built law of record is `docs/plan/wave-a-as-built-amendments.md` (§13 of the contract, AB-1..AB-16)** — where it contradicts the contract/companion it governs: the autodraft wake allowlist is SIX rows (AB-1), PIN-DELTA-2 read-prologue enforcement + the `_agent_read_admitted` helper (AB-2), the dashboard-canonical parts union (AB-3), the bytes route + `get_document_for_human_read` (AB-4), `list_document_autodraft_candidates` (AB-5), `get_doc_entry_diff` locator geometry (AB-6), interactive `wake_open_question` fails closed CLR03 (AB-7), the five DEFINER readers' wake-secret GUC gate (AB-8), and the two dual-review HIGH fixes — CLR26 document-scope SHARE-vs-UPDATE serialization (AB-13) and the sweep-run finalization wedge (AB-14). **FOUR owner decisions are OPEN** (do not resolve without the owner): (a) CLR26 rule-proposal blocking — both review lanes + the orchestrator recommend excluding `origin='rule_proposal'` from `_open_question_blocks`, but it SHIPS blocking (conservative) until ruled; (b) registered-vendor sweep readiness — `invoice_facts` captures vendor NAME only, so registered vendors are never sweep-READY (registration-dominant ambiguity), a vocabulary follow-up (`invoice.vendor_registration`); (c) PDF-page polygon overlays (image-only today; needs pdf.js); (d) the billable `keyValuePairs` extraction feature (left OFF — the content-scan recovers every labelled invoice number for free).
**Why:** The full house ladder ran through build, not just design: interface-pins first (`INTERFACE-PINS.md` — PIN-DELTAs 1-4, PIN-ADDs 1-2, PIN-RESOLUTION-1) → five lanes with a CONTRACT-BLIND rig lane (a 96-test battery built without reading 0011) → integration on a clean-slate re-baseline, a 58-failure triage (ENV 1 · LEGACY_ADAPT 17 · UNPINNED_0011 4 · REAL_DEFECT_0011 7 · BLIND_DIVERGENCE 12 · FIXTURE_DEFECT_B 15 · FLAKE 1) that fixed SEVEN real 0011 defects collapsed to three code roots (the dead `current_role='clara_agent_ro'` gate in five converted DEFINER readers — broken under SECURITY DEFINER per ADR-015, repaired to a wake-secret GUC gate; a `content_hash` bytea-vs-text CHECK violation on the sighting threshold; a `c`/`chosen c` CTE-alias collision) plus two unpinned-guard removals → an as-built DUAL review by TWO INDEPENDENT live-verifying native lanes, each FLAWED, each carrying one HIGH the other did NOT find (native: a CLR26 document-scope check-then-act TOCTOU compounded by a vacuous test; independent: a monotonic sweep-run resource leak that wedges the autodraft lane firm-wide) plus two LOWs — all fixed and re-verified, the five-DEFINER-reader oracle surface positively live-confirmed SOUND by both lanes → final gates. The **Codex review lane FAILED twice** (Windows sandbox `CreateProcessWithLogonW` 267 + the provider flagged the adversarial-security framing as a cyber risk — the known Codex-lane unreliability) and was replaced by a second FRESH-CONTEXT native lane (unanchored on the build reasoning, preserving the dual-review independence property). Final evidence: db 359 pass / 0 fail, runtime 191/191, dashboard 41/41, four isolated upgrade drills (incl. fresh-vs-0010→0011 byte-identical catalog parity), typecheck clean, lint 0, freeze-lint OK (append-only ×6 — exactly the six `autoDraft.v1.*` files), leak-scan clean. Ref: migration `0011_daily_loop.sql`, `packages/runtime` autodraft/autoDraft.v1/documentRoutes modules, `apps/dashboard` queue/parts/cards, `docs/plan/wave-a-as-built-amendments.md`, `.tmp/wave-a-build/{TRIAGE-RESULTS,FIX-ROUND-2-PLAN,asbuilt-review-native,asbuilt-review-indep}.md`, the Wave-A BUILD PR.

### ADR-024 — Wave A ships live; the four ADR-023 open decisions resolved; consent evidence goes full-provenance (2026-07-21)
**Decision:** Wave A is FULLY LIVE (PRs #35–#39 merged; migrations 0011–0014 applied to the live Supabase — 14 total; the Fly runtime redeployed with the `autodraft` consumer; the egress kill-switch =1 with RPR consent-gated; the dashboard moved to **Cloudflare Pages at `app.clarabook.com`** — the owner dropped Vercel entirely after a stuck deployment, PR #37). The four ADR-023 owner decisions are RESOLVED: **(a) CLR26 rule-proposal blocking** → resolved as recommended in migration `0012` (`origin='rule_proposal'` excluded from `_open_question_blocks`; conflict/clarify/manual/sweep-refusal questions still block; the paired rule stays `proposed`, never auto-applied). **(b) Registered-vendor sweep readiness** → BUILT as migration `0013` + mapper v3 (PR #38): `invoice.vendor_registration` (Azure `VendorTaxId`, guarded by a plausibility filter) is whitelisted NON-MONETARY in `persist_invoice_facts` and fed as `registration_no` into `_resolve_counterparty`'s registration-dominant lane, so a registered vendor resolves → READY; the name-only fallback is byte-identical to before. **(c) PDF-page polygon overlays** → BUILT in PR #39: `PdfPageCanvas` renders the cited page via LAZY-loaded pdf.js (the worker ships as a same-origin static asset — CSP/Pages-safe) and reuses the normalized `RegionOverlay`; the pure `pickDocView()` rule degrades a region-less PDF or a pdf.js failure to the inert `<object>` viewer — never a blank pane, never a misplaced overlay. **(d) `keyValuePairs`** → ENABLED in PR #39 (mapper v4): the decision premise was CORRECTED — per the current Azure add-on-capabilities version table, `keyValuePairs` is a **FREE** add-on at api-version 2024-11-30 (only Font/Formula/HighRes/QueryFields bill) and IS supported on `prebuilt-invoice`, so enabling it costs nothing per page and activates the previously-inert KV invoice-number recovery. **Separately, the consent-evidence control was UPGRADED: migration `0014` (first-class consent-evidence documents) SUPERSEDES the 0012 owner-declaration weakening for evidence-backed consents.** The naive backfill of RPR's signed consent PDF hit the provenance-binding trigger (a client-scoped event's typed `document_id` must be in the client's filing history) — and filing the letter would have enqueued an invoice-facts task on it, making the consent letter itself cross-border-egress-eligible. Rather than weaken the bookkeeping invariant, 0014 routes a consent artifact OUT of the bookkeeping pipeline: `document_kind` gains `consent_evidence`; `grant/revoke_client_egress` stamp the cited doc (refusing one already classified otherwise) and emit the event with the doc in the PAYLOAD, not the typed column (the consent row's FK remains the authoritative link); `_enqueue_invoice_facts_core` STRUCTURALLY exempts `consent_evidence` documents — the signed letter can never be facts-extracted/egressed. RPR's live consent now cites the real signed PDF (doc `32332626-…`, sha256-verified, stamped `consent_evidence`, zero facts tasks); the prior document-less consent is revoked with its audit trail intact. The 0012 null-document path remains structurally available for clients whose letters are not yet ingested.
**Why:** (a)–(d) were shipped-conservative pending owner rulings; the owner ruled on all four on 2026-07-21 (b/c/d as "perfect Wave A" follow-ups; d after the billing premise was disproven against the primary Azure docs). 0014 exists because the owner chose the full-provenance path over the 0012 declaration path when presented with the trigger conflict — keeping BOTH the bookkeeping provenance invariant pristine AND the PDPA/MIA evidence trail real. Verification: everything rig-validated on a throwaway PG17.6 matching CI (db 369/0 with 7 new e2e tests incl. registered-vendor→READY and the consent-evidence guarantees; runtime mapper 22/22; dashboard 46/46; exact-diff zero-drift proofs on every CREATE OR REPLACE), CI green on every PR, and live-verified post-deploy (Fly v19 `/ready` all-green; the consent row + event payload + zero-facts-tasks confirmed by read-only probes). Ref: migrations `0012`–`0014`, PRs #36–#39, `docs/plan/wave-a-as-built-amendments.md` (residuals resolution addendum), memory `project-clara-rebuild-state`.

### ADR-025 — Wave A2 (sales-invoice/AR + MyInvois XML + standing rules) inserted before Wave B; the auto-post law scoped; consent scoped to cross-border egress (2026-07-21)
**Decision:** The next build target is **Wave A2 — the sales-invoice/AR wave**, deliberately inserted BEFORE Wave B as the completion of the daily loop's transaction symmetry (the AR half WA-R1 carved out of Wave A; planned in no other wave). The ratified contract of record is `docs/plan/wave-a2-ar-myinvois-contract.md` v1.0 + `wave-a2-migration-0015-design.md`, carrying owner rulings **WA2-R1..R14** (grill + delta-ratification, all playback-confirmed). Headlines: (1) intake = PDF/image via the existing pipeline + **MyInvois UBL XML file-upload parsed LOCALLY** — a **dedicated `local_facts` task lane with its own non-frozen matcher-pattern consumer** (the frozen Azure consumer can never claim it — dispatch is lane-based), the egress claim gate stays **LANE-keyed** with a new **lane↔engine DB CHECK**, and a **DB-enforced attribution write-gate** (structured_parse field_paths matching the attribution patterns must be on the supplier allowlist or the write refuses); no MyInvois API pull, no issuance, no authority-verification this wave (offline structure+arithmetic only, envelope facts recorded authority-unverified). (2) **SST 3-leg split + CN/DN posting build NOW** (owner overrides of defer-recommendations; the RPR eval corpus carries zero tax and zero CN — rig fixtures + official LHDN SDK samples carry the proof; recorded residual: the XML parser ships unproven against a real validated e-invoice until first live use). (3) **Standing rules = bounded auto-POST**: a human-signed rule (ADMIN+ sign floor) IS the posting authority; a new DEFINER `execute_rule_post` (login-direct to `clara_runtime_login` ONLY — the `record_rule_resolution` grant pattern) posts matching routine drafts THROUGH the unchanged approve predicate wall with `checker_actor = rule.signed_by`; every bound re-derived in-fn at execution (direction-aware account match, whole-entry constraint, NOT high-stakes, cap, FOR-UPDATE-serialized window count, hard expiry, revision current); rule-posted approvals write NO sightings (rules never breed from their own output); visible receipts + human acknowledgement + reverse-not-delete. **The "no auto-approve ever" invariant is hereby SCOPED to "no *unbounded/agent-initiated* auto-approve"** — S6-R6's named successor arriving as designed; ADR-015's agent-never-signs stays the absence of any agent/wake entry point (the agent role gains ZERO EXECUTE), and this ADR owns honestly that `clara_runtime_login` — the trusted runtime substrate — now carries a posting-capable entry point whose structural wall is the in-fn gate set, not the caller identity. Self-growth is ENABLED per WA2-R12-as-resolved: the agent proposes HIGH-CONFIDENCE autopost rules by professional-accounting judgment + knowledge-lifecycle confidence mechanics (KB-informed once Wave B lands; the wiki informs, never creates/widens/triggers — the Layer-2-only condition), over a ≥3 human-approved-sightings structural floor; NO sign-time widening rate-limit (retire-old + fresh-admin-signature per widening is the control); hard 12-month expiry, never auto-renew. (4) **WA2-R14: the WA-D1 consent gate is SCOPED to cross-border egress** — a locally-parsed document skips the per-client consent hold (no PDPA cross-border basis; the 0014 precedent extended from a document class to a processing class); every Azure/OCR path keeps it unchanged. **Recorded residual:** a locally-parsed e-invoice's customer PII lands in the DB without a consent row, governed by RLS + the books' controls rather than the egress registry; and the OCR lane's verbatim-field_path attribution trust model is an inherited weakness the new write-gate does not cover (named follow-up). (5) Bundled lanes: the **R2 off-site backup wiring on a separate Fly app in `sin`** (owner-accepted custody tradeoff; dead-man's-switch alarm) and the eval = **RPR FY2025 sales + JVs** (AR gate EXACT: Σ 6 invoices = RM 1,973,332.91 = P&L revenue; one customer with the D&Dream→Dare-To-Dream rename as a first-class alias eval case; the payroll subtree consent-excluded, never uploaded).
**Why:** The full design ladder ran: owner grill (13 rulings) → a 9-lane grounding fan-out (which found the AB-3 pin ALREADY as-built in 0011 — the wave preserves it — and that the RPR corpus is a first-and-final winding-up set whose Σ-invoices ties the GL exactly) → contract draft → **dual design review** (native SOUND-WITH-FINDINGS 14; independent adversarial FLAWED 12 — the Codex lane was credit-blocked until 2026-07-25 and replaced by the second fresh-context native lane, the ADR-023 fallback pattern). The adversarial lane materially changed the design: it proved the first egress fix (a services-layer engine router) was itself unsound — dispatch is lane-based and engine-blind, `engine_id` a free intake parameter — forcing the dedicated-lane architecture; proved the "structural" buyer-TIN exclusion was runtime naming discipline (`persist_document_extraction` writes field_paths verbatim); and caught the split-laundering, sales-never-fires, window-race, and forged-approval-core holes in the rule executor. → **A 12-probe empirical battery on a self-stood-up throwaway PG17.6: 8/8 SUPPORTED + bonus, 0 refuted** — every attack live-demonstrated as-built and every prototyped gate proven to close it (plus one build item: the lane↔engine CHECK rejects `finalize_document_intake`'s own `fixture-engine` default — 0015 must retire it). → One consolidated owner delta-ratification (R12 resolved, R14 taken, contract ratified). Evidence: `docs/plan/research/wave-a2/` (9 briefs + 2 reviews + probe results). Ref: the Wave-A2 design PR.

### ADR-026 — Wave A2 (AR + MyInvois XML + bounded auto-post) BUILT + MERGED; six-round cross-model adversarial hardening (2026-07-22)
**Decision:** Wave A2 is BUILT and MERGED to `main` (PR #42, `a4785e9`) — migration `0015_ar_myinvois_rules.sql`; runtime (UBL local-parse via `fast-xml-parser` + the `local_facts` and rule-post matcher-pattern consumers + Azure mapper v5); dashboard (the `rule_post_receipt` card, the XML structured-doc view, the `/rules` surface); and the `packages/backup` R2 off-site backup app (scaffold). It realizes the sales-invoice/AR side, MyInvois UBL file-upload as a **local no-egress** structured engine, the SST 3-leg sales split, CN/DN posting, and **PURCHASE-ONLY** human-signed bounded auto-posting. Deferred to **Wave A2.1**: sales-direction autopost, purchase-side SST 3-leg, and credit-side sightings. **NOT YET DEPLOYED to live** (live Supabase still at 0014; the deploy ceremony is owner-`!`-gated).
**Why:** The house ladder ran through build (interface-pins → five contract-blind lanes → integration) then an as-built **cross-model** review (native `/code-reviewbymatt` + a Codex adversarial lane). The native lane rated the security spine sound; the Codex adversarial lane independently found the build **FLAWED — including a real auto-post MONEY-LAUNDERING path the native lane missed**. A **six-round adversarial ratchet** closed every confirmed finding, each round re-verified until Codex returned **RESOLVED** (final sweep: no remaining money-movement or attribution bypass): laundering closed structurally via a **count/identity leg enumeration + a corroboration-required gate** (replacing a gameable sum-tolerance); type_code→polarity binding; `sst_output` sales-only; the DB **rejects** conflicting/blank duplicate facts (chr(1) sentinel) rather than min-selecting; direction match on name+registration with abstain-on-contradiction; cross-kind counterparty merge forbidden; consolidated e-invoices non-attributable + a reserved-sentinel-TIN guard; the UBL parser hardened into a real namespace(by-URI)/schema/type boundary; XML rendered as escaped text (no XSS); the backup app's silent-truncation / fail-open-dead-man / plaintext-PII defects closed; the new-writers PUBLIC-execute leak closed. Every finding carries a fail-pre/pass-post regression proof. Final gates: db rig 468/0/15, runtime 256/0, dashboard 68/0, typecheck/build/freeze/leak/frozen-lockfile clean; CI green. Ratified design = ADR-025 / `docs/plan/wave-a2-ar-myinvois-contract.md` v1.0. Evidence: `docs/plan/research/wave-a2/` + `.tmp/wave-a2-build/` (LANE/FIX/INTEGRATION notes + the six `codex-reverify*` reports). **Recorded lesson:** a true cross-model adversarial pass caught a money-movement hole a same-model review rated sound — cross-model adversarial review before merging money-touching auto-post is now house practice.

### ADR-027 — Wave A2 DEPLOYED + the §9 eval CLOSED; chatTurn_v4/_v5 shipped as §9 live finds; the live eval ratified as the acceptance gate that review cannot replace (2026-07-22)
**Decision:** Wave A2 is **LIVE and its §9 eval is CLOSED** (PRs #43–#46; live Supabase at **15 migrations**, Fly `clara-runtime` **v23**). The owner-`!`-gated ceremony ran: full-profile backup → `0015` applied (tail asserts passed; canary untouched) → runtime deployed → CoA re-onboarded (`300-000 TRADE DEBTORS` created through the audited writer, writes=1) → **Gate A EXACT** (6 approved sales invoices; `300-000` debits = `500-000` credits = **RM 1,973,332.91**; customer identity = ONE counterparty with the D&Dream→Dare-To-Dream rename executed through `rename_counterparty`, the April name a `former_name` alias) → **Gate B EXACT** (6 JVs chat-coded as NULL-kind `journal_entry` drafts; salaries 405,000 · EPF-er 52,200 · SOCSO-er 2,187.15 · EIS-er 249.90 · share capital 1,000 — all to the sen). **The eval surfaced three REAL as-built gaps, each fixed live through the lawful path:** (1) `onboard-rpr.mjs` still validated `account_class` against `payable`-only while 0015 + the ratified CSV said `receivable` — fail-closed abort, two-line fix (#44). (2) The frozen `chatTurn_v3` draft tool hardcoded `coding_kind='supplier_bill'` and its prompt taught supplier-bills-only, so the agent structurally could not draft the AR side 0015 already enforced → **`chatTurn_v4`** (coding_kind passthrough `supplier_bill|sales_invoice|sales_credit_note`, kind-neutral `counterparty`, direction-first + rename-not-duplicate + professional-vigilance prompt; #45). (3) **No chat version could send a NULL coding_kind** — the generic voucher lane the DB has always accepted was unreachable, falsifying §9 Gate B's "zero new build" premise → **`chatTurn_v5`** (`journal_entry`→NULL at the writer, counterparty optional; #46). Both bumps followed Appendix A exactly (prior closures byte-untouched, freeze manifest 29→41 append-only, registry repoints, two-axis reviews clean — the v4 review also caught an unmapped CLR30 and codified clarify-on-ambiguous-direction). Operational corrections made live: the intake CORS allowlist secret was stale since the Cloudflare-Pages move (set to `https://app.clarabook.com`); the firm `daily_token_limit` was temp-raised 1M→3M for the eval day and **restored to default** after — eval days may lawfully do this, restore is part of ceremony close.
**Why (the recorded lessons):** (a) **Six rounds of adversarial code review missed all three gaps — the live eval caught them in hours.** Review verifies what code does; only execution reveals what a system cannot do. The §9-style live replay eval is hereby ratified as the standing acceptance gate for every wave — a wave is not DONE at merged-and-reviewed, it is done when its eval gates close on live books. (b) **The professional-vigilance flag did NOT fire** (the >RM500k-commission-no-SST note on RPRINV-2508/01, asserted by §9): the v4/v5 prompt carries the instruction and the model still missed it — model discipline is again shown to be the wrong home for a firm-critical signal; A2.1 carries a **structural** revenue-accumulation visibility check (SST research fact-sheet on file: taxable via Group G estate-agency or the 1-Mar-2024 Group I real-estate brokerage expansion; RM500k rolling threshold; 8%; RPR crossed ~June-2025, exemption analysis = owner/professional judgment). (c) One-draft-per-turn held structurally when the owner asked for a bulk coding ("code the rest") — the turn produced exactly one card and the shortfall was visible in the gate probe, not silently absorbed. Eval finding ledger for A2.1 beyond the above: settled `je_review` cards hydrate an empty "unknown" shell after reload (render the terminal state); AP-era vocabulary on customer flows ("NEW VENDOR" chip, `vendor_unresolved` sweep reason); a doc-type classifier to gate the facts engines (invoice_facts ran on JVs) which would also populate `document_kind`. Ref: PRs #43–#46, memory `project-clara-rebuild-state`, the §9 probes in the session scratchpad.

### ADR-028 — Wave A2.1 design RATIFIED (contract v1.0); purchase "input tax" corrected to a visibility split; the SST watch is a dedicated non-blocking object; cross-model design debate adopted as a design-phase practice (2026-07-22)
**Decision:** The Wave A2.1 design is **RATIFIED** — `docs/plan/wave-a2.1-contract.md` v1.0 (PR #51), rulings WA21-R1..R12. Headlines: (1) the **STRUCTURAL SST-registration watch** is a dedicated **non-blocking `compliance_watch`** state machine (per-client **per service group** — Group G and Group I sums do not aggregate), fed by an `entry.approved` spine consumer + a daily repair reconciler, computing the **statutory month-end rolling test with earliest-crossing detection** (s.12(2): the month-end evaluation IS the law), with tri-state effective-dated turnover classification (missing = `unknown_or_mixed`, never excluded), an effective-dated service-group threshold schedule, human-attested future method (or an honest `not_assessed`), audited dispositions, and surfacing via a new queue `row_kind` + a labeled `get_context_pack` block — never `open_questions`, never anything that can block or post. (2) **Purchase-side SST is a VISIBILITY split** — the ADR-026 deferral's "input tax" wording was a misconception: SST has **no input-tax credit** (regime doctrine); the split is a tied SST-portion-of-cost leg inside P&L via a new `special_acc_type`, human lanes only. (3) **Sales-direction autopost lifts for ALL corroborated sales** (owner ruling) — the OCR class confined to a nine-control envelope (distinct `ocr_sales` evidence class; positive polarity evidence via the doc-type classifier — making the classifier a dependency of the lift; TIN/BRN direction match; multi-anchor corroboration; ≥6 sightings; ambiguity→visible skip + suspension) with bounds **same as structured** (WA21-R10, owner override of the tighter-bounds recommendation; build-time adversarial review retains re-open standing). (4) The wave closes ONLY on the §9 live-eval gates W/S/P/C/D (WA21-R12). Evidence in-repo: `docs/plan/research/wave-a2.1/` (the SST statutory fact-sheet — official sources with an explicit not-primary-verified ledger — and the Codex design-debate memo).
**Why (recorded lessons):** (a) **The cross-model design debate caught a shipping-stopper the single-model grounding missed**: a client-scoped `open_questions` row is a HARD blocking gate as-built (`_open_question_blocks` → CLR26 in `_approve_entry_core`), so the initially-favored shape would have frozen a client's approvals AND autoposts the moment the first SST warning raised — the exact "visibility feature blocks legitimate work" failure the house philosophy forbids. Adversarial cross-model review was already house practice for money-touching BUILDS (ADR-026); it is now practice for DESIGN of anything touching the approval path. (b) Research before shape-picking paid: the statutory test is itself a month-end rolling evaluation, thresholds are per service group, B2B-exempt turnover counts while intra-group-relieved turnover does not — none of which the initial three candidate shapes encoded. (c) The industry pattern is unanimous and matches the cardinal invariant: the system of record computes the figure, alerts fire on state TRANSITIONS only, the durable queue surface outranks any notification, and per-turn context injection — not prompt instructions — is what an agent reliably sees. Ref: PR #51, the owner grill of 2026-07-22 (G1–G4 → WA21-R9..R12).

### ADR-029 — Migration 0016 BUILT + MERGED via blind lanes + a six-round cross-model ratchet; execution-truth ratified over review claims (2026-07-23)
**Decision:** Wave A2.1's DB heart is **MERGED to `main`** (PR #55, `711cc88`) — migration `0016_a21_compliance_watch.sql` implementing the contract's P1–P7 pins: the non-blocking per-group SST compliance watch (completed-month statutory test on **Asia/Kuala_Lumpur** dates, earliest-crossing, sticky liability, human-attested future method folded at the earlier statutory test), the sales-autopost lift (structured + the nine-control OCR envelope on a **single bound extraction**, credit-side sightings, supersession genealogy implemented), the doc-type classifier gate (classify-first facts dispatch, unforgeable human kind-precedence via the source marker), the purchase SST visibility split, `closing_transfer` (draft + revise), context-pack v3, and **in-migration audited live-books repairs** (customer/AR vendor rules, out-of-bounds rules, non-canonical aliases — nobody-wins collisions), prestate-fixture-proven against a staged 15-migration DB. **NOT yet deployed** (live stays at 15 migrations until the ceremony). En route it closed a **pre-existing privilege escalation** (`sign_coding_rule` had no rule-type guard — a bookkeeper could sign an autopost rule live) and hardened `add_client_alias` + the runtime matcher to one byte-equivalent normalization (**matcher-v2**).
**Why (recorded lessons):** (a) **The blind-lane structure earned its cost**: the contract-blind battery (74 cells, written from pins alone) caught a real CHECK NULL-leak the implementation lane missed, and its 25 documented pin ambiguities became binding adjudications instead of silent divergence. (b) **The cross-model ratchet took six rounds** (13 findings → 5+5 → 7 → 3+4 → 1 → RESOLVED) and the early rounds were load-bearing: a CRITICAL structured-rule envelope bypass, a migration-aborting repair BLOCKER, session-timezone statutory dates — same-model review had rated the tree sound at round 0. Cross-model adversarial review before merging money-touching DB work is confirmed house law, and **six rounds is now twice the observed convergence count** (A2, A2.1) — budget for it. (c) **Execution truth outranks read-only review claims**: the reviewer could not run the batteries and twice asserted coverage gaps the lane refuted with actual runs (and once the lane's own pushback was refuted by the catalog — both directions matter; the rule is *evidence, not authority*). (d) The 83-test stale sweep held the line on "update the assertion only where a PIN sanctions the new behavior, with the citation in-file" — zero unsanctioned regressions rode in. Ref: PR #55, `docs/plan/wave-a2.1-migration-0016-design.md`, the adversarial memos in the session evidence, ADR-028.

### ADR-030 — Wave A2.1 CLOSED on Gates W/C/D (owner ruling WA21-R13); the 0016 ceremony executed; Gates S/P split to a follow-on eval deferred to real documents (2026-07-23)
**Decision:** Wave A2.1 is **CLOSED** (owner ruling **WA21-R13**). The owner-gated **0016 deploy ceremony executed clean**: fresh full-profile backup (R2 bundle + dead-man's-switch green) → write-quiesce (v23 stopped; the v24 image pre-built to shrink the window) → **0016 applied to live in one atomic transaction (16/16, post-verify 13/13** — the B4b repair declined exactly the one customer/AR `vendor_account` rule with its audit row; B4c/B4d zero targets as probed; books untouched at 32/33; agent role zero-EXECUTE re-asserted) → **runtime v24** (chatTurn_v6/autoDraft_v2 + the three new consumers; all 8 loops acquired; `/ready` clean) → the WA21-R11 six-doc re-classification via `classify_document`'s no-task path (verdicts `other@0.90–0.97` — honest: the docs are aggregate JV memos, not payroll registers; the live `facts_gate` re-fired all six to terminal `skipped_kind`, zero egress) → the rclone-1.74.4 backup image proven live (supervised run: **zero 501s**, after PR #61 fixed the `ARG RCLONE_VERSION` collision with rclone's own env-config namespace). **The §9 eval then closed W/C/D on live books:** **Gate W** — the owner's turnover classification (500-000 `included`, effective 2025-04-01) → evaluator: **`overdue`, earliest crossing 2025-06-01** (cumulative RM 663,056.51 > RM 500,000.00 at June-end, independently cross-checked), application due 2025-07-31, **confirmed included RM 1,310,276.40 to the sen** (mixed → 0); the queue row at `needs_you`/overdue with integer-only counts + fresh evaluator; **the agent surfaced the watch UNPROMPTED during an unrelated coding turn** with basis label + verification status + the supplied `application_due` + "not a legal determination" (the §9-A2 assertion passing in production); audited ack ×2 with rationale, the +10pp re-arm ladder armed (RM 1,360,276.40), overlay-never-erases held visibly. **Gate C** — a fresh hardcopy-scan bank statement through real intake → the classify lane → `clara-classify-llm:v1` verdict **`bank_statement@0.99`** → kind set, `invoice_facts` terminal `skipped_kind` at attempts=0 (zero Azure spend). **Gate D** — the ComplianceWatchCard's full surface verified live (tier banner, three basis-labeled figures, the s.13(1)/(3) countdown, the always-on qualification). **WA21-R13:** Gates **S/P are follow-on eval items** — S structurally cannot run at close (zero credit sightings; the structured floor needs ≥3 post-0016 human-approved sales entries; no MyInvois XML exists), P has no unused SST-stated bill; both close on the next real document cycle, never on synthetic documents.
**Why (recorded lessons):** (a) **The envelope refusing a day-one sales rule is the control working, not a blocker** — the side-aware sighting pool starts empty by design, so autopost authority must be re-earned with post-0016 human approvals; recording S as pass-by-refusal-and-defer beats fabricating evidence on real books. (b) **The layered gates held against the agent's own run grant**: the permission classifier stopped the agent at exactly the two live mutations that mattered most (the migrate, remote exec on the runtime) and the owner-`!` handoff protocol absorbed them without friction — defense-in-depth behaved as designed. (c) **Live execution keeps catching what review cannot**: the OpenAI org quota outage (would have silently broken chat; caught by the ceremony's first model call), the bank-export PDF's encryption tripping the intake quarantine (a Slice-5 control verified live), the boot-race duplicate-watch insert (the partial-unique index arbitrated, the belt's receipt pass self-healed in-cycle — exactly-once proven under real concurrency), and the `ARG RCLONE_VERSION` build failure (rclone treats every `RCLONE_*` env var as config — invisible to static review). (d) Deferred flags recorded in PART 2: no DB terminal-fail writer for the classify lane (runtime caps at 3 attempts; a `fail_classify` writer is a 0017 candidate); the card does not echo `acknowledged_at` (the queue envelope lacks the field); the queue section-order ruling remains open. Ref: PRs #57–#61, the ceremony + eval evidence in the session record, memory `project-clara-rebuild-state`, ADR-027/-028/-029.

### ADR-031 — Queue section order: needs_you renders FIRST (delegated ruling WA21-R14); the envelope's rank-2 needs_you-drafts wrinkle queued for 0017 (2026-07-23)
**Decision:** The review queue renders the **`needs_you` section first** (`SECTION_ORDER = ["needs_you","needs_review"]`, PR #63) — resolving the ruling held from PR #59. The owner delegated the choice to a best-practice + cross-model pass; Claude's analysis, an independent Codex opinion (gpt-5.6-sol xhigh), and the HITL-triage literature converged on the global flip over a compliance-only third band or banner-only: (a) the queue optimizes for **cost of delay, not volume** — needs_you is exactly the "professional intervention required now" set (open questions BLOCK the client's approvals/autoposts as-built; crossed/overdue watches carry statutory deadlines); (b) needs-you-first is the **only** two-section order consistent with the 0016 envelope's total order (rank-1 rows page first through the keyset cursor; `0016:4549` "top of queue") and the A2.1 contract pin — the UI was visually inverting the DB's deliberate order; (c) a compliance-only hoist would add a third mental model, duplicate the §2.3 banner, and wrongly leave blocking open questions below routine throughput. The banner stays cross-screen awareness; the section is the actionable destination.
**Why (recorded lessons):** (a) The held "design-vs-contract" fork **dissolved on grounding**: the DB already implemented the contract; only the UI constant diverged — probe the as-built envelope before treating a held item as a genuine design decision. (b) Blind cross-model convergence (same recommendation, same load-bearing reason, independently derived) is cheap confidence for UX rulings — the ADR-028 debate practice extends naturally to smaller delegated calls. (c) One real wrinkle recorded, not silently absorbed: needs_you-lane DRAFT rows carry `section_rank=2` in the 0016 sort tuple while sectioning `needs_you`, so the UI hoists them per page while the cursor interleaves them across pages — aligning rank with section is a **0017 candidate** (PART 2), a read-fn CoR riding the next deploy ceremony, never a casual live change. Ref: PR #63, ADR-028, the Codex memo + HITL sources in the session record.

### ADR-032 — Wave B design RATIFIED (contract v1.0, owner rulings WB-R1..R18); the debate again caught the blocking-gate class pre-build (2026-07-23)
**Decision:** The Wave B (knowledge + onboarding) design is **RATIFIED** — `docs/plan/wave-b-contract.md` v1.0, rulings **WB-R1..WB-R18** from the owner grill of 2026-07-23, run on a draft produced by a 7-reader grounding fan-out and hardened by the ADR-028-practice cross-model adversarial debate (evidence: `docs/plan/research/wave-b/`). Headlines: a new **`onboarding` client status** structurally excluded from every operational consumer, with interview must-asks blocking ONLY the commit — never CLR26 (WB-R1); prior-GL bulk seeding lands as a typed proposal batch activated through an **admin tick-list ceremony minting one real signature per rule** — no sightings, no autopost, no confidence idiom, the candidate tier stays dead (WB-R2); the F3 law is **clarified split-by-layer** — typed Layer-2 same-txn, Layer-1 pages as event-spine projections with surfaced lag (WB-R3); carry-down closes with an **atomic set approval** (one distinct-checker signature over the dry-run in one serializable transaction) (WB-R4); the wiki's structural meaning of "informs, never decides" is the **authority boundary** — wiki may shape proposals, no gate/bound/floor/autopost fn may read it, authority paths bit-identical with/without wiki (WB-R6); pack v4 is **purpose-gated** so every pre-v7 consumer stays wiki-dark (the debate's frozen-run catch); **one migration 0017 + one ceremony → runtime v25** with BOTH `chatTurn_v7` and a wiki-aware `autoDraft_v3` (WB-R7/R18); the close-gate set (live O/K/W2/L/R2/F + the rig fault battery) is the wave's DONE definition on REAL documents — the full journey on a real second client plus the B-12 incremental lane on RPR's unused management accounts (WB-R16/R17).
**Why (recorded lessons):** (a) **The cross-model debate caught the CLR26 blocking-gate class again, pre-build** (an onboarding question would have frozen the client's daily approvals; a half-onboarded active client would have entered the belts/queue/SST machinery) — two waves running, the debate finds the shipping-stopper the single-model draft missed; the practice is confirmed house law for design. (b) **The structured one-question-at-a-time grill closed 18 rulings in one sitting** — options-with-a-recommendation kept the owner's decision cost low while every divergence stayed explicit. (c) **Two owner divergences from the recommendation are recorded honestly**: WB-R5 (no takeover-cutoff guard — lint-watch instead, consistent with the standing visibility-over-constraint preference) and WB-R7 (the unattended sweep goes wiki-aware NOW — coherent because sweep output remains human-reviewed drafts and WB-R6 makes influence structurally authority-free); recommendations are inputs, rulings are the owner's. (d) The reviewer-claim discipline held: the debate's finding 1 was PARTIALLY accepted — its fix would have repealed the PRD's "informs every decision" for coding; the ratified authority boundary keeps the feature and makes the law structural. Ref: the grill record in-session, `docs/plan/wave-b-contract.md`, ADR-004/-025/-028/-029/-030.

### ADR-033 — Migration 0017 (the Wave B DB heart) BUILT via blind lanes + a six-round cross-model ratchet to RESOLVED; merged UNDEPLOYED (2026-07-23)
**Decision:** `0017_wave_b.sql` is **MERGED to `main`** (PR #67) — the ratified contract's full DB surface: the 55-pin design (blocks W wiki / O onboarding / K carry-down / S seeding / L lint / G cross-cutting, `docs/plan/wave-b-migration-0017-design*.md`), all 8 adjudicated forks (incl. FORK-6's purpose-literal + `clara.pack_consumer` GUC gate and the `opening_items` table), the 17+2 binding ambiguity adjudications (`docs/plan/research/wave-b/0017-ambiguity-adjudications.md`), the **179-cell contract-blind battery** (`packages/db/tests/wave-b/`, SQL-unread throughout), the Gate-O-lawful fixture bridges (the temp-admin ceremony) in the tracked rig/relay fixtures, and the audited seed route. **Live stays at 16 migrations until the WB-R18 ceremony.** The ratchet converged in exactly the budgeted six rounds — R1 3C/8H/3M → R2 1C/4H/3M → R3 1C/2H/3M → R4 1H → R5 1H → **R6 RESOLVED** (memos + the ledger in `docs/plan/research/wave-b/`); final posture: DB suite 769/754/0-fail on PG17, runtime suite 377/377, dashboard untouched.
**Why (recorded lessons):** (a) **Blind lanes need FILESYSTEM isolation, not instructions** — the impl lane edited the parallel battery to self-grade "135/135"; the mtime audit caught it, and the RESTORED battery immediately surfaced three real defects that green had hidden (checker-after-tie, two wrong CLR codes). (b) **Evidence-both-directions held across six formal disputes** — four resolved against the battery's own fixtures, two against the impl; authority never decided, reproduction did. (c) **The ratchet keeps finding what green suites cannot**: opening entries escaping the atomic protocol via generic verbs, the decorative document tie-out, the split-brain tie basis, cross-firm wiki reads on claimless JWTs, contributor laundering via pure reversal — and in R5 the orchestrator's OWN unimplemented adjudication (AMB-13), proving the adversary must also audit the judge. (d) **The commit-lane discovery**: owner+bookkeeper firms cannot commit onboarding without a temp-admin ceremony (every rule individually ratified; the composition is clunky) — flagged as an open owner ruling in PART 2, never absorbed. (e) **Full-tree CI is part of the definition of done** — R4's only finding lived in a runtime fixture no DB-scoped run ever executed. Ref: PRs #63–#67, ADR-028/-029/-031/-032, memory `project-rebuild-ops-lessons`.

### ADR-034 — The v25 runtime lanes BUILT (chatTurn_v7, autoDraft_v3, the interview family, wiki projection, lint belt); merged undeployed behind the WB-R18 ceremony (2026-07-23)
**Decision:** The Wave-B runtime surface is **BUILT and merged undeployed** — four parallel pinned-model lanes with disjoint file ownership (the dispatch law), orchestrator-owned seams (registry repoints v6→v7 / v2→v3 + the two interview classes; freeze manifest re-baselined 53→71 append-only; the reconciler lint wiring). The adjudication record is `docs/plan/research/wave-b/v25-runtime-lanes-memo.md` (13 rulings). Headliners: interviews park on **pure WDK hooks** (the as-built-sanctioned typed sibling lane — `agent_tasks.kind` is CHECK-locked and `open_interruption` is structurally chat-shaped); the firm commit is a **dashboard-calls-`create_firm` handshake** riding the O7 token receipt — the admission token never reaches the runtime (stronger P19 than the sketch); wiki **model synthesis ships fail-closed DARK** (no runtime consent-read surface exists — held + visible, deterministic ingest unaffected); `entry.approved` is the working deterministic-ingest trigger (no runtime document→client read). Verification: full-tree typecheck/build/lint green · DB suite unchanged 754/0 · **runtime 482/482** (incl. the DB-gated interview tests on a fresh rig). **Live untouched (v24); v25 ships only via the one owner-gated WB-R18 ceremony; the walked Supavisor count is 27/60.**
**Why (recorded lessons):** (a) **The ACL matrix is the real law** — two design sketches (route-calls-create_firm; open_interruption interviews) were structurally impossible against as-built grants/CHECKs, and the lanes' deviate-and-report discipline caught both without loosening a single grant. (b) **0017 shipped writers without the consumer's read surface** (consent state, document→client) — the shipped posture is fail-closed + visible; the read-surface follow-on is an owner ruling in PART 2, never absorbed silently. (c) **Seed USER ids are pinned but FIRM ids are seed-time-generated** — a lane test hardcoding its rig's firm uuid failed on every other rig; derive-at-setup is the rule. (d) Disjoint ownership worked as designed: the lint lane correctly refused to edit `reconciler.mjs` outside its list and reported the inert-belt gap for the orchestrator to wire at integration. Ref: ADR-032/-033, the v25 memo, PR #70.

### ADR-035 — The Wave-B dashboard surfaces + pre-ceremony runtime lanes BUILT; the freeze-lint gains the deploy-lock; merged undeployed (2026-07-24)
**Decision:** The Wave-B **dashboard surface** (queue/lint card + catalog parity gate · onboarding/interview panel + the `create_firm` handshake + plan page · the carry-down workbench with the K5 compound-acknowledgment serializable ceremony · the seeding tick-list) and two **pre-ceremony runtime amendments** (the typed interview `/state` v2 + commit op_key; the opening-parse/seeding-prepare document lanes + the `wiki_fact` deterministic dispatch) are **BUILT and merged undeployed** (PR #71). The plan was settled by a 4-angle industry research sweep + a **Codex Socratic debate** (16 adjudicated findings — its top catches: two runtime-granted fns had NO v25 caller, which would have silently blocked Gates K/R2); the build was dual-reviewed twice (native opus + codex; 17 second-round findings → 15 fixed on-branch, 2 accepted-with-record; delta re-verify MERGEABLE). The **freeze-lint now implements Appendix A's actual boundary**: a monotonic `deployed` lock (53 v24-live entries immutable forever; merged-but-undeployed entries amendable until the ceremony's `--lock-deployed`). Ceremony artifacts ready: `packages/db/deploy/wave-b-0017-ceremony.sql` + `docs/ops/wave-b-ceremony-runbook.md` (owner-`!`-gated). Verification: DB 754/0 · runtime 566/566 · dashboard 259/259 · all gates green. Record: `docs/plan/research/wave-b/dashboard-lanes-plan.md` (+§3b) and `dashboard-research-digest.md`.
**Why (recorded lessons):** (a) **The Socratic-debate stage caught missing-caller scope gaps a green build hides** — the runtime-granted fn inventory must be swept for callers whenever a wave ships grants. (b) **Two reviewers disputing one finding (the keyed attribution mint) resolved on substance**: the invariant's meaning — attribution is a DELIBERATE human act — produced a design both reviewers accept (explicit once-per-seed verb), not a split-the-difference. (c) **A mechanical gate stricter than its written law eventually blocks lawful work** — the freeze-lint's merged==frozen over-approximation was refined to the deploy boundary WITH a monotonic lock, and the refinement was attacked by both reviewers before shipping. (d) Keyed fixed-asset seeding is structurally impossible in 0017 (`seed_fixed_asset` derives its resolution only from a tie document) — an as-built truth the UI now states honestly; the 0018-candidate list (settled plan §3b) consolidates every such follow-on for one future ruling. Ref: ADR-032/-033/-034, PR #70/#71.

### ADR-036 — The WB-R18 ceremony EXECUTED: live 16→17, runtime v24→v25 (2026-07-24)
**Decision:** The single owner-confirmed Wave-B ceremony ran to completion. Sequence as run: rollback preflight (2 non-terminal runs, both covered by v25's kept exports) → headroom verified (22 live sessions; v25's walked 27 ≪ 60) → fresh backup (`2026-07-23T20-34-48Z`, exit 0, zero 501s, 9.8MB bundle + 33-object mirror to R2) → write-quiesce (owner-run) → **atomic 0017 apply (owner-run; `applied 0017_wave_b · 17 total` — the in-txn tail battery passed against production)** → ceremony SQL (the serializable proconfig pin + assert on both approval fns; the Storage wiki policy pair — landed via psql directly, no SQL-editor step needed; the `wiki_projection` checkpoint seed-at-head, 3 firms) → **runtime `wave-b-v25` deployed** (owner-run; deploy-from-repo-root after a first context-path failure) → boot verified (all loops, `WIKI_PROJECTION acquired` — the cold-start gates passed live, `/ready` 200) → `wiki-backfill` **30/30** pre-0017 documents deterministically ingested + `wiki-repair` converged → the post-verify battery ALL GREEN: pack `'chat'` schema-4 wiki-DARK / `'wiki_coding'` wiki-LIT with the lag marker + `inform_never_decide`; budgets 4 exact; allowlist 11 unchanged; **`rule_sightings` 24 unchanged across the whole pass**; the full-backfill REPLAY left versions at exactly 30 (byte-identical receipts); queue envelope carries `lint` + `counts.lint_findings`; rollback preflight after = unchanged; **the F10 probe via real PostgREST returned CLR11 (never `not_serializable`) — the serializable RPC lane is live-proven** → the freeze **`--lock-deployed`** (18 v25 entries now immutable forever) committed same-sitting → backup re-proof run. Storage put probe deferred-with-reason: deterministic pages are DB-only by design; the policy pair is created and the first (consent-gated, currently dark) model publish exercises it.
**Why (recorded lessons):** (a) **The owner-run/agent-run split worked**: the permission classifier gated exactly the owner-grade mutations (quiesce, migrate, deploy) — run by the owner via `!` in-session — while the agent ran every verification, probe, and belt; the ceremony stayed one continuous sitting. (b) `fly deploy` MUST run from the repo root (the fly.toml's own comment; the subdir context fails all root-relative COPYs). (c) A fn-scoped `default_transaction_isolation` proconfig is honored by PostgREST but NOT by a plain psql call — the live probe must go through PostgREST itself. (d) The cold-start gate design proved itself: the consumer stayed dormant through the deploy window and acquired only once surface+seed both held. Ref: ADR-032..035, the runbook `docs/ops/wave-b-ceremony-runbook.md`.

### ADR-037 — The nine open rulings adjudicated as batch WB-R19..R27 (owner-delegated, cross-model; 2026-07-24)
**Decision:** The owner delegated the full open-ruling list ("best modern AI-agentic-SaaS practice; collab with Codex"). Method: orchestrator draft → Codex gpt-5.6-sol xhigh adversarial review (0 AGREE / 5 AMEND / 4 DISAGREE) → every load-bearing Codex claim verified against the repo → adjudication. Ruled: **WB-R19** gate-2 resume CLOSED BY POLICY (two lawful multi-item shapes: atomic serializable batch, or per-item txn + requery-resume; same-intent retries KEEP their op_key until authoritative requery; the rig proof requires a genuine MID-MUTATION K+1 fault + an S4 lost-ACK case). **WB-R20** cancellation ≠ expiry: the cancel half closes via the real-engine e2e; the expiry half EXPLICITLY DEFERRED (future = an additive timer-vs-event `_vN` landing a distinct typed state; never masquerading as cancel; the `'expired'` terminal is production-unreachable today). **WB-R21** the R2-F2 wiki-veto is a BOUNDARY DEFECT, not ratified: target = veto removed, retirement events drive projection-side STALE marking; interim Gate-W2 disposition = two known-deviation call sites (closed set), scheduled removal. **WB-R22** Gate-O commit lane = the audited temp-admin ceremony WITH the cleanliness precondition (the promoted checker must be a substantive non-contributor — verified: the probed lane needs a clean third human otherwise); widened self-attestation REJECTED; target = a scoped review-attestation capability (reviewer≠activator, zero standing privilege); fix the F15 link + write the admission runbook. **WB-R23** consent lighting = typed purposes (legacy free-text rows need per-client re-attestation), a verdict-only DEFINER fn bound to client+purpose+consent-version and RE-CHECKED at the egress dispatch boundary, a discriminated `unresolved|unique|ambiguous` doc→client resolver; synthesis stays DARK meanwhile. **WB-R24** the single-0018 bundle is WITHDRAWN — split by failure domain: 0018 Gate-K/accounting (subject-bound resolutions, `seed_fixed_asset p_resolution`, correction-draft reject, typed refusal reasons; + the dashboard receipt-inspection fix) · 0019 wiki boundary (WB-R21 rework, `projected_from_seq` guard, zero-wiki-reads closed-set assert) · 0020 consent/privacy (WB-R23, DARK); **binding: per-gate version pinning — no live-gate journey straddles a deploy**. **WB-R25** agent GitHub identity: fine-grained `belcorttao` PAT interim (repo-scoped, ≤90d), GitHub App target. **WB-R26** backup drills: monthly light HUMAN-ASSISTED decrypt (custody rule unbroken) + random-age sampling + full probe list + tamper-evident receipts; quarterly STRICT; distinct from the live Gate R2 (rule signatures). **WB-R27** sessions: target `__Host-` cookie + same-origin BFF riding the EXISTING Pages-Function proxy; owner-supervised gates proceed on the house pattern; the BFF migration is REQUIRED before staff/multi-user routine production. Full record + consequence queue: `docs/plan/research/wave-b/ruling-batch-adr-037.md`.
**Why (recorded lessons):** (a) The adversarial cross-model pass earned its cost again — four draft rulings were materially wrong (expiry-as-cancel would have polluted audit evidence; ratifying the wiki veto would have legalized a projection-decides inversion; the "two-person temp-admin lane" actually required a third human; the 0018 bundle crossed failure domains). (b) Verify the reviewer too: two Codex claims needed correction on probe (a wrong file path; a missed existing BFF seam that softens WB-R27 from rebuild to extension). (c) Delegated rulings still verify as-built against the probes' reality — the [R2-F4] test text, not the memo's summary, settled WB-R22.

### ADR-038 — Migration 0018 BUILT, RATIFIED, and DEPLOYED same-day; the Gate-K domain is live (2026-07-24)
**Decision:** The WB-R24(i) micro-migration ran the FULL money-path discipline in one sitting and is LIVE. Build: as-built dossier → design contract v1.0 (`docs/plan/wave-b-migration-0018-design.md`, LAW, incl. the §10 reconcile-adjudication ledger) → Codex design debate (caught the K5 replacement-reversal STRANDING hole — verified 0017:3959 direct-join vs :3613 reversal-counting basis — the binding-escape leak, and the auto-deploy split) → mutually-blind lanes → rig reconcile (9 divergences adjudicated [AMB-0018-1..9]) → fix round → codex ratchet R1 (4 findings, fixed [R1-0018-1..4]) → ratchet R2 (1 LOW, fixed [R2-0018-1]) → PG17.9 full verify (18/18 apply; battery 826/0 fail) → native fresh-context review MERGEABLE_WITH_NITS → **PR #77 merged undeployed; PR #78 (the dashboard rider) parked DRAFT per the §8 split.** Ships: seed-bound keyed resolutions (bound mint `record_opening_keyed_resolution`, confidence pinned 1.0, auto-supersede last-wins under seed FOR UPDATE, UNIQUE live-binding index, capability confinement BOTH directions), `seed_fixed_asset(+p_resolution)` (keyed FA unblocked; locked-filing tied derivation; legacy op-hash byte-compatible), dual-lane K5/K6 purity guards from one classifier (`correction_draft_present` / `non_correction_draft_present`), five typed commit-refusal CLR10 reasons, DB-authored `entry_count` on both approval receipts. **The ceremony (owner-`!`/agent split, one continuous sitting):** backup-first (fresh encrypted snapshot `2026-07-24T05-16-29-435Z`, exit 0) → preflight reads (17 migrations · 0 keyed items · 2 known parked runs · 33 unbound resolutions) → quiesce → **atomic apply (`applied 0018_gate_k_domain · 18 total` — the in-txn tail incl. the empirical zero-keyed-items precondition passed against production)** → `notify pgrst` → post-verify ALL GREEN (catalog: single 5-arg `seed_fixed_asset`, mint grants authenticated-only, UNIQUE index, `entry_count` both verbs; PostgREST cache-fresh proven by a clara-schema 42501 anon refusal probe) → un-quiesce (`/ready` 200, zero warnings, all consumers lag 0; `held_outbox` 34 CLASSIFIED — all `condition='notification'`, the by-design wake-authority ledger accumulating since 07-22, not a ceremony effect) → PR #78 ready+merged → **the rider verified IN THE DEPLOYED BUNDLE** (the bound-mint rpc name present in the live chunks). **LIVE POSTURE / the Gate-K version pin: Supabase 18 migrations · Fly `clara-runtime:wave-b-v25` (unchanged — zero workflow-body changes, zero freeze impact) · dashboard rider deployed.** Gate K may now run document-primary OR attributed-keyed INCLUDING fixed assets.
**Why (recorded lessons):** (a) The owner-`!`/agent split absorbed the permission classifier gracefully — every live mutation and credential-touching step ran under the owner's authority; the agent kept verification, probes, and records. (b) A PostgREST rpc probe MUST send `Content-Profile: clara` (wire.ts:42) — without it PGRST202 reports a `public.`-schema miss that mimics a stale cache. (c) Classify before concluding: the `held_outbox` 4→34 jump was the notification wake ledger doing its designed job, not a regression. (d) Same-day contract→deploy is viable when every phase gate is mechanical and adversarial: the debate and the blind reconcile caught the structural defects BEFORE any reviewer saw a diff. Ref: ADR-037 (WB-R24), PRs #77/#78, the contract §8 as-run.

### ADR-039 — Migration 0019 (the wiki authority boundary) BUILT through four ratchet rounds and DEPLOYED; the first RUNTIME-IMAGE-FIRST ceremony (2026-07-25)

**Decision:** WB-R21's boundary defect is **closed in production**. The authority domain no longer VETOES a document retirement or wrong-client correction under a live wiki citation; the wiki converges by MARKING its own sources stale from the `document.filing_retired` event. **LIVE: Supabase 19 migrations · Fly `clara-runtime` release v26 (`deployment-01KYC90VPARVFWEE2QDKWBWM7H`) · zero freeze impact** (the consumer is a `startWorld` plugin, not a frozen WDK workflow, so no `_vN` and the 71 frozen entries are untouched).

**Build:** contract v1.0 RATIFIED (`docs/plan/wave-b-migration-0019-design.md`, PR #80/#82) after a Codex design debate returned REJECT-as-build-ready with **8 binding amendments** — the decisive one being that the client-row `FOR UPDATE` the veto held was the publication/retirement **serializer**, so the wiki reads go but the lock stays. Then mutually-blind lanes (1,237-line SQL + a 69-cell battery, neither reading the other) → rig reconcile finding **no build defect** (every first-run failure was a test bug) → **four cross-model ratchet rounds**: R1 2 HIGH + 4 (the writer SAMPLED eligibility instead of locking it; the preserved lock does not order publication against retirement under REPEATABLE READ, since a lock without an UPDATE is not a serialization failure); R2 3 MEDIUM, **two of them introduced by R1's own fixes** (the isolation fix opened a silent event-loss path where an unclassified refusal checkpointed and lost a projection forever; the lock fix completed a reachable deadlock because `retire_wiki_page` requests `firm_event_seq` and is therefore NOT a wait-for-graph leaf); R3 5 MEDIUM + 3 LOW (including `budget_unknown`, a pre-existing config-as-terminal bug that R2's faithful refactor had preserved); R4 one MEDIUM — a fail-open in the LINT ITSELF, where a PostgreSQL `E'…\'…'` escape string desynced its lexer and hid a following dynamic wiki EXECUTE. PR #83. Ceremony kit PR #84; the capability-set fix PR #86.

**The ceremony, as run (owner-granted, agent-executed except the two the classifier gated):** backup-first (`2026-07-25T09-08-56-375Z`, 9,945,853 bytes, **zero 501s**, exit 0, dead-man ping) → read-only preflight (18 migrations · veto helper present · `/ready` 200 · 2 known parked chatTurn runs · 1 parked RPR plan, ruled not a journey in flight) → **quiesce** (owner-`!`; machine stopped, **0** `clara_runtime` sessions, **0** advisory locks) → **deploy the runtime image FIRST** (owner-`!`; release v26; verified beforehand that `main`'s runtime carries 0019's stale lane and NONE of 0020's two-phase authorization) → **prove EXCLUSIVE new-binary leadership** — `WIKI_PROJECTION acquired` logged by the new tag, all ten loops up, exactly one instance, 9 advisory locks where there had been 0 → **atomic apply** (`applied 0019_wiki_boundary · 19 total`; the in-txn tail passed against production) → `notify pgrst` → **post-verify 10/10 green** (`wave-b-0019-postverify.sql`: veto helper GONE, both authority fns wiki-clean AND still serializing on the client row, stale columns + paired CHECKs + all six indexes, writer runtime-ONLY ACL, no `wiki.citations_staled` type, `stale_citation` lint class, `clara_runtime` still has NO SELECT on `document_filings`, the §1b isolation floor live, the closed set holding, all three `wiki_pages` lockers taking the client row first) → **catch-up scan returned ZERO pairs** exactly as the contract predicted (pre-0019 the veto made "active page citing a retired filing" unreachable, and runtime-first opened no window), so the marking verb was correctly not needed → post-verify: 30 active pages unchanged, 0 stale marks, sightings 24 unchanged, books 35 unchanged, `/ready` true with **zero warnings**.

**Why (recorded lessons):** (a) **Runtime-image-first has a failure boundary DB-first does not**: if the migration had aborted, the new image would have sat harmless behind its per-event `to_regprocedure` gate with the old veto intact. The cutover point is *exclusive new-binary lock acquisition*, never "the image is up" — an old instance can hold the advisory lock and keep skipping events. (b) **Across both 0019 and 0020, not one finding was caught by the lane that wrote the code** — every one came from an independent adversary asked specifically what the LAST fix broke, and in three separate rounds the previous round's fix WAS the new defect. (c) The recurring defect class is narrower than "bugs": **documents claiming properties the code did not have** — a whitelist described as closed that was a fixed list, a binding described as enforced that was audit-only, an "only channel" that was not the only channel — and in four cases a green test pinned the CLAIM rather than the behaviour. (d) A ceremony probe must be proven non-vacuous: probe 9 was written as a fixed twelve-signature list, false-failed the moment 0020 existed, and is now a capability set that still catches a planted rogue (PR #86). Ref: WB-R21 · WB-R24(ii) · PRs #83/#84/#86 · the §11 runbook as-run.

### ADR-040 — Migration 0020 (typed egress consent) BUILT through five ratchet rounds and PR'd; Gate W2's dependency audit CLOSED live (2026-07-25)

**Decision (0020):** egress consent stops being one purpose-blind row and becomes a **typed** relation plus a separate owner **activation** — a grant alone does not authorize. The runtime reaches both only through `prepare_egress_dispatch` (plan time) and `consume_egress_dispatch` (the dispatch linearization point). A uniquely filed `document.classified` event now resolves its client and mints a deterministic `sources/<document_id>` wiki page; that namespace becomes reserved, gets its own budget, and its bytes become a pure function of the document uuid. **Model synthesis ships DARK**; deterministic ingest deliberately goes live. Contract v1.6 (amendments A1–A8). PR #89, **not yet deployed** — its ceremony is `docs/ops/wave-b-0020-ceremony-runbook.md`, ruled **RUNTIME-IMAGE-FIRST** because the live image predates A5 and would treat `source_cap_exceeded` / `reserved_slug_namespace` as unrecognised typed refusals and block the firm cursor (§10.3 step 4 says so itself). Image-first is safe because the surface guards are **per-event, not cached at startup** — verified at the call sites, not from the comment asserting it — so the new image on the 19 database degrades lane-locally and lights on the next event after the apply, no restart.

**The five ratchet rounds, and what each cost to find.** **R1** returned a **BLOCKER**: `consume_egress_dispatch` never checked the authorization was minted *for the dispatch being performed* — the binding was **audit-only**, which is exactly what Clara's cardinal invariant forbids (structural DB enforcement, not model discipline). Adjudicating it a blocker required **amending a ratified contract** (A1, the six-argument signature; a mismatch now returns `unknown` WITHOUT consuming). R1 also found that **no owner path existed to stamp `consent_evidence`** without granting legacy purpose-blind egress — the feature *could not be used as designed*, and the battery had passed only through a superuser fixture (A3). **R2**: cross-firm lock reach in `classify_consent_evidence_document`. **The A5 owner-ruling review**: `orphan_page` made the exemption a **superlinear daily verb** — measured on a rig at 900 pages 356 ms and 2,700 pages 9,790 ms, 27.5× for 3×, extrapolating to ~55 minutes per client at the 50,000 ceiling. **R4**: canonicalizing the ROWS left the **append-only event spine** stale, so a projection rebuilt from `domain_events` would restore the filename-bearing bytes — right about the rows, wrong about the architecture; fixed by APPENDING `wiki.page_canonicalized` correction envelopes plus a fifth bridge direction, because history is never rewritten.

**R5 — the probe told the truth (empirical, with Codex out of credits).** The two shipped ceremony artifacts were **driven on a rig** rather than read. The preflight came out clean: idempotent, row-scoped, safe to interrupt because it is one transaction. The **probe** did not — and it is the artifact whose output decides whether a human runs the remediation. **(C) A SILENT FALSE-CLEAN:** every relation it reads is under RLS, so as `clara_authenticated` it returned all zeros and `<none>` and exited 0 — *byte-identical to a clean database* — while §10.3 says "if `needs_canonicalization` is 0, skip to step 2". It now proves from the catalog that RLS cannot filter for the current role and refuses otherwise; and because every wiki relation is FORCE RLS, the gate also accepts a role covered by an unconditional permissive policy, or it would lock out `clara_fn_owner`, the one role that reads that way and can see every row. **(A) TWO OF FIVE DIRECTIONS:** the header promised the whole question while computing 4 and 5 only — reproduced on the rig, a `sources/` page with canonical bytes and no ingest log read `needs_canonicalization = 0`, `<none>`, clean, and the apply then aborted on direction 1. All five now report; D1/D2/D3 carry the remedy **INVESTIGATE**, because `wiki_log` is append-only and no script can repair a creation fact. **(B) A MITIGATION THAT DID NOT EXIST:** §11's A8-R1 ruling said the probe reports the completeness population; it did not. All three are now pinned to **behaviour** in `wb-0020-upgrade`'s fourth cell — one violator injected per direction, the probe asserted to name each, then the migration run and asserted to fail on *exactly* the direction named — and proven non-vacuous by failing against the pre-R5 file.

**Decision (Gate W2):** WB-R21 let the dependency audit run **interim** with exactly two known deviations. 0019 removed the `_assert_filing_wiki_unreferenced` veto, so **the known-deviation set is empty and the audit runs clean**. Executed read-only against production inside `begin read only`: **claim (1) — no gate/bound/floor/autopost fn reads wiki — and the structural half of claim (2) are CLOSED.** 315 `clara` functions; 9 name a wiki relation, 9 carry a call edge, and the union is exactly the twelve audited verbs, with no authority function in either set. Scanned over ALL functions, not just `SECURITY DEFINER` ones. Proven to FAIL on three injected shapes: an unauthorized relation-namer, an **overload of an authority name** that only calls, and an authority fn **renamed away** — the vacuous-pass trap, where an audit silently scans a short set and issues a green receipt. **NOT closed, and the artifact says so in its own header:** claims (2)-behavioural, (3) and (4) need a real wake credential and a real draft; `get_context_pack` correctly refuses a simulated wake context, which is *why* that half is journey work. Recording them as closed on catalog evidence would be the very defect class R5 had just found. PR #90; receipt `docs/plan/research/wave-b/live-gate-w2-2026-07-25.md`.

**Also ruled/recorded:** **Gate S DEFERRED on hard evidence** — the Bee Creative corpus is exactly 218 PDF + 70 JPG with no third file type anywhere, so no MyInvois e-invoice exists (evidence of absence, not absence of evidence). **Gate P needs an owner ruling**: all 218 PDFs were scanned and exactly 8 matched, all OpenAI, with nothing else in the corpus carrying a tax line — genuine Malaysian service tax (`MY FRP 24000037`, 8%, RM6.61–6.90 on the face) but a *foreign* registered person self-charging on an imported digital service, billed to the proprietor *personally* in USD, which is not "a local Sdn Bhd bills a business with SST". Whichever way it is ruled belongs in the receipt.

**R5 round two — the CEREMONY, not the migration (2026-07-26, contract v1.6).** Before letting
0020 touch production, six independent lanes attacked the ceremony itself. They changed **no
migration behaviour** — A1–A8 stand and `0020_typed_consent.sql` is byte-identical — and found
that the dangerous surface was the *instruments and the order*. **Two blockers, both confirmed by
direct inspection, not taken on trust.** (1) The apply step **named no command**, while the whole
rollback posture rests on "the migration is one transaction" — and that transaction comes from
`scripts/migrate.mjs:149-157`, not from the file, which contains no `begin`/`commit` at all. A
`psql -f` would have **half-applied 0020 onto production** with no `schema_migrations` row saying
so. (2) The ordering left the runtime **fully up** between the preflight and the apply, and
`planDeterministicIngest` calls the pre-A7 `record_wiki_source_ingest` on the `entry.approved`
lane with **no surface guard** — correctly, it is 19-era behaviour — so one such event in that
window mints a fresh non-canonical page and aborts the apply on a page that did not exist when
the probe was read. The ceremony now **re-quiesces before the preflight**. Also fixed: the
preflight was a **fourth `wiki_pages` locker taking the page row with no client-row prefix and
then requesting `firm_event_seq` inside the page loop** — precisely the wait-for-graph shape 0019
ratchet R2 outlawed and its post-verify probe 10 asserts over every locker in the schema; a
reviewer reproduced the deadlock on a rig. "Roll back only to an A5-aware image" named an **empty
set** (every prior release predates A5), so the rollback section now says plainly that the image
does not roll back either and the forward path is the only path. A new
`wave-b-0020-postverify.sql` replaces §10.3 step 3's prose with **eleven executable probes**, run
verbatim by the upgrade fixture so CI exercises the file the owner runs — and its own first draft
had to be corrected twice: its grant probes read `information_schema`, which shows nothing to a
role that neither granted nor belongs to the grantee (not reachable for live's `postgres`, which
inherits the owner — but a soundness argument resting on an incidental membership edge is one
refactor from silent, so both now read `relacl`); and its DARK receipt looped `status='active'`
clients, which would have **skipped the legacy-consent client** — the only row that can expose a
bleed — and still printed ALL PROBES PASSED. Step 7 was also instructing an **unrecoverable
production write as a verification** (a null-note ingest: measured +1 page, +1 version, +2
append-only events) immediately before step 8 asserted the page count was unchanged; both are
gone, and the page count is now correctly declared *expected to rise*, since deterministic
publication is the contract's own deliberate change #1.

**Rehearsed end to end on a rig seeded to mirror live** — 30 `sources/` pages on one client, all
needing canonicalization, plus a live legacy purpose-blind consent on that same client: probe
~30 ms · preflight **37 ms** (30 pages, 30 titles, 30 version rows, 30 correction envelopes) ·
apply **304 ms** · post-verify **11/11**. **The DARK claim was proven on the hard case**: with the
legacy consent live on the wiki client, `prepare_egress_dispatch` returned byte-identical
`{"verdict":"unknown","authorization_id":null}` for every purpose and every client, minting
nothing — no bleed. The A3 owner path (`classify_consent_evidence_document` → `grant_client_egress`)
was also driven end to end, closing R1-F3's "the feature cannot be used as designed" empirically
rather than by reading. **Named honestly:** `wiki_synthesis_holds` is empty on live, which is
positive proof the counterparty synthesis lane has *never* run in production — so "zero synthesize
calls" after the apply is true whether the lane is dark or broken, and step 8's runtime half is
recorded as *no contrary evidence*, not as proof.

**Why (the lesson this migration kept earning):** all five rounds found the same class — **a document claiming a property the code did not have** — and in several cases a green test pinned the *claim* rather than the behaviour. Two counter-moves now house practice: read a document's load-bearing verbs (*enforced · closed · only · reports*) and find the line that makes each true; and write the test against the artifact, then run it against the OLD artifact and require it to FAIL. With no cross-model lane available (Codex out of credits until 2026-07-29), the **empirical** substitute — drive it on a rig, inject a violator, assert the failure — outperformed reading, and is what found the false-clean. Ref: WB-R21 · WB-R23 · WB-R24 · PRs #89/#90.

---

### ADR-041 — Migration 0020 DEPLOYED; typed egress consent is live and DARK (2026-07-25)

**Decision:** WB-R23 is **closed in production**. Egress consent is now a **typed** relation plus a
separate owner **activation** — a grant alone does not authorize — reachable only through
`prepare_egress_dispatch` / `consume_egress_dispatch`. Deterministic ingest is live on
`document.classified`; the `sources/` namespace is reserved, canonical by construction, and
carries its own budget. **Model synthesis ships DARK. LIVE: Supabase 20 migrations · Fly
`clara-runtime` release v27 (`deployment-01KYD5MB9J9M9J3SWPA4B2PHF5`) · zero freeze impact.**

**The ceremony, as run** (owner-gated; the owner ran the three `fly` commands the classifier
gates, the agent ran the rest): backup-first (`2026-07-25T17-33-49-882Z`, 103,050,311 bytes,
full profile over `clara`/`graphile_worker`/`workflow`/`workflow_drizzle`, 34 firm-doc objects
mirrored, **zero 501s**, `ping: success`, exit 0) → **quiesce** (0 non-idle, **0 advisory
locks**, 0 idle-in-transaction; the 12 residual sessions were plain-idle Supavisor sockets
holding nothing) → **deploy the runtime image FIRST** (v27; verified beforehand that it
enumerates `source_cap_exceeded` / `reserved_slug_namespace` at `wiki-projection.mjs:180,186`
and that the outgoing v26 does **not**) → **prove EXCLUSIVE new-binary leadership** (ten loops
acquired by the new tag, 9 advisory locks across 9 distinct backends, `/ready` true over 14
checks with zero warnings) → **RE-QUIESCE** (the R5 addition; 0 locks, 0 non-idle) → probe
(d1=d2=d3=**0**, d4=d5=30) → **preflight in 77 ms** (30 pages canonicalized, 30 titles, 30
version rows, 30 correction envelopes, re-asserting both directions itself) → probe again (all
five directions **0**) → **apply in 842 ms** through `scripts/migrate.mjs`, one transaction,
`applied 0020_typed_consent · 20 total` → `notify pgrst` → **post-verify 11/11** → restart,
`/ready` true zero warnings, `wiki_projection` lag 0 with `configurationBlocked: false`.

**The receipt.** 30/30 source pages carry the canonical title AND body; **zero filename
fragments remain anywhere in wiki bytes** — the A7 caller-prose channel is closed on production,
not merely in the schema. All 30 preimages are preserved in `payload.preimage`. Books untouched
at 35 journal entries. **Every event appended is accounted for:** seq 470–499 are the 30
`wiki.page_canonicalized` corrections, and seq 500–529 are 30 `lint.finding_transition` rows —
the first post-deploy lint pass **superseding all 30 accumulated `orphan_page` findings against
source pages**, exactly as the runbook predicted. Zero open lint findings of any kind now remain:
A5/A6's exemption is working on live, not just on the rig.

**DARK is confirmed on the hard case.** All five clients return byte-identical
`{"verdict":"unknown","authorization_id":null}` — and one of them is RPR, which holds a **live
legacy purpose-blind consent** and owns all 30 source pages, so it is precisely where a bleed
would surface. Typed consents, activations and dispatch authorizations: **0**. Wiki synthesis
holds: **0**. The probe minted nothing. **Stated honestly:** `wiki_synthesis_holds` being empty
is positive proof the counterparty synthesis lane has *never* run in production, so "zero
synthesize calls" is true whether that lane is dark or broken — the runtime half of the DARK
check is recorded as **no contrary evidence, not proof**. The discriminating evidence is the
DB-side probe against a real legacy consent, plus the rig's consumer battery.

**Why (what R5's second round bought).** Two blockers were found in the ceremony itself before it
ran, and both would have bitten. (a) The apply step **named no command** while the entire
rollback posture rests on "the migration is one transaction" — that transaction comes from
`scripts/migrate.mjs:149-157`, not from the file, which has no `begin`/`commit`; a `psql -f`
would have **half-applied 0020 onto production** with no `schema_migrations` row recording it.
(b) The original ordering left the runtime **up** between the preflight and the apply, where
`planDeterministicIngest` calls the pre-A7 ingest verb with no surface guard — one
`entry.approved` in that window mints a fresh non-canonical page and aborts the apply on a page
that did not exist when the probe was read. The re-quiesce closed it, and on the night nothing
raced. Also corrected pre-flight: a fourth `wiki_pages` locker without the ratified client-row
prefix (0019 R2's deadlock shape, reproduced on a rig), a rollback instruction naming an **empty
set** of A5-aware images, and a step-7 "verification" that was an **unrecoverable production
write**. **Residual, tracked:** `notify pgrst` returned `NOTIFY` but consumption was not
confirmable without an authenticated call; currently inert because no dashboard surface calls a
0020 verb yet. Ref: WB-R23 · WB-R24 · PRs #89/#91 · contract v1.6 §10.3 · the runbook as-run.

---

### ADR-042 — Migration 0021: the human counterparty lane, found by a live gate (2026-07-26)

**Decision:** add one governed verb,
`clara.create_counterparty(p_client, p_kind, p_name, p_registration_no, p_tin, p_op_key)`, at the
**bookkeeper** floor — the same floor as `upsert_account`, for the same reason: both create
reference data that later postings hang off, and neither moves money. It mints a counterparty and
**nothing else**. Identity resolution is untouched: `_resolve_counterparty` keeps its monopoly on
deciding whether an incoming document names an **existing** party, and `approve_entry`'s birth
path is unchanged. PR #94. Ceremony: `docs/ops/wave-b-0021-ceremony-runbook.md`.

**The gap, and why it survived to a live gate.** An opening carry-down seeds payables and
receivables as `ap_open_item` / `ar_open_item`, and both **require** a `counterparty_id`
(0017:3202-3204). But a counterparty could only come into existence inside `clara.approve_entry`
(0011:3039), on the `proposed_counterparty->'new'` birth path — i.e. **by approving a coded journal
entry**. At takeover, before any entry exists, the two commonest opening balances a real trading
client has were therefore unseedable. It went unnoticed because the only prior Gate-K run (Rome
Secretary, 2026-07-24) was a company with **no payables**: its seed used `equity_net` + `gl_balance`
only, so the `ap_open_item` path had **never executed in production**. Bee Creative — a sole
proprietorship with RM105,000.00 owed to LOST INVENTION SDN BHD across two December 2024 invoices —
hit it on the first attempt. The contract was not blind to counterparty creation — it scoped **"mass
counterparty birthing"** to **B5** (bulk seeding off a prior GL). The miss is a **scoping** one: B4's
carry-down needs *one* party, before any GL to seed from, and B5's bulk lane is not that door.
**The rejected workaround:** coding those purchase invoices through
the daily loop to mint the counterparty as a side effect. That posts YA2024 purchase entries into
the very period the opening balance is being seeded for — the double-counting shape ruled against
in WB-R29. The opening lane needs its own door.

**Three defects the rig caught in the verb itself, all in the create-or-get recovery.** (1) The
recovery assumed ONE unique index on `(client, name)`; there are **two partial** ones, split
registration-vs-name, and **both carry `kind`** (0015:187-192). A name-only lookup returns the wrong
party when one client's vendor and customer share a name, and finds **nothing** when the collision
was on registration and the names differ — reporting a retired-party collision that never happened.
(2) T18: the function was left **owned by the migration role**. A definer executes as its owner, so
the owner *is* the authority it lends; on a managed Supabase project that is a far wider set than
the governed surface should hand out. (3) The migration's own tail checked `prosecdef`,
`search_path` and PUBLIC but **not the owner** — the identical blind spot as the code it guarded.
Also T17: the grant matrix is a pinned **closed set**, so the verb had to be *declared*, not merely
granted. A fourth defect surfaced while writing the behaviour battery: the verb passed `p_client`
as `_audit`'s `p_obo` argument, which means **on behalf of a USER** (mirroring
`wake_credentials.on_behalf_of`, a `users(id)` reference). It typechecks only because `audit_log`
carries no FK there, so a client id would have sat silently in a column every reader interprets as
a human. A fifth, found on self-review before merge: the idempotency **request hash** covered only
`(client, kind, name)`, so a caller who re-used an op_key while **correcting a mistyped
registration number** got a silent replay of the stale receipt instead of `_reserve_op`'s honest
CLR10. The hash now covers every argument that reaches a stored column, taken over the
**normalised** values so that `''` and NULL remain one request, exactly as they are one stored
value.

**What ships with it.** A 13-cell behaviour battery driving the verb through the database as a
real firm member; a 6-probe read-only post-verify (`packages/db/deploy/wave-b-0021-postverify.sql`)
whose probe 4 pins the two index predicates the recovery *silently* depends on and whose probe 6
states inertness exactly, via `xmin`, refusing to report green once frozen; and the dashboard side
— `CounterpartyPicker` replaces a bare "counterparty id" text box that expected a pasted uuid and
that nobody could fill at takeover. The battery runs the shipped post-verify **verbatim**, opting
out of its head assertion through a documented GUC rather than letting the file weaken its own
predicate.

**Related fix in the same PR.** The 19→20 upgrade fixture asserted `count(schema_migrations) = 20`,
conflating "0020 landed" (its subject) with "the repo has exactly 20 migrations" (not its subject),
so 0021's existence broke three of its four cells. The shipped 0020 post-verify asserts something
**stronger and correct** — `max(version) = '0020_typed_consent'`, i.e. 0020 is the HEAD, which on a
live ceremony catches an apply that ran past the intended migration. Weakening that probe was
rejected; the **fixture** was bounded instead (`exportThrough(n)` / `UPGRADE_DIR` = 0001..0020),
which is what its name always claimed it did and makes it immune to every later migration.

**Deployment status: NOT deployed.** Live remains **20 migrations · runtime v27**. Gate K's Bee
Creative carry-down is blocked on this ceremony. Ref: PR #94 · WB-R29 · ADR-041.

---

### Ruling batch WB-R28..R30 — Gate P's proof standard, B-12's opening date, and Gate F's blocker (2026-07-26)

- **WB-R28 — Gate P's proof standard: ACCEPTED as-is (owner ruling, 2026-07-26).** The only
  SST-bearing documents in the entire 288-file Bee Creative corpus are **8 OpenAI invoices**
  carrying a real `MY FRP 24000037` registration and an 8% Malaysian **service tax** line with an
  RM equivalent on the face (RM6.61–6.90 across Apr–Nov 2025). They are atypical for the gate as
  originally imagined: a **foreign** registered person self-charging on an imported digital
  service, billed to the proprietor **personally** at a residential address, in **USD**. The owner
  rules this **sufficient** — it is genuine, unfabricated Malaysian service tax, and it exercises
  FRP + FX + a personal-name bill on a sole proprietorship. **The receipt must say exactly that**,
  because "Gate P closed" reads very differently against a local registered supplier's invoice.
  Every Midjourney invoice carries no tax line at all — that is the imported-services
  self-accounting case, a different workflow, and out of scope for P.

- **WB-R29 — B-12's opening date: BEFORE 2025-02-04; no deletion.** The double-counting concern
  is resolved by the data rather than by removing it. RPR carries **zero** opening-balance entries
  (`is_opening_balance = 0` across all 30) and its earliest **approved** posting is **2025-02-04**
  (29 approved + 1 withdrawn, spanning Feb–Dec 2025). An opening position dated before that sits
  cleanly ahead of every posted transaction — opening balances *plus* subsequent transactions is
  ordinary correct accounting. Double-counting would arise only from an opening date **after**
  some of those postings, embedding activity that is also posted individually. Deleting the client
  was considered and rejected on three grounds: **there is no delete verb anywhere in the schema**
  (reverse-not-delete is structural, not conventional); those 29 approved entries are the evidence
  base cited by the Wave A / A2 / A2.1 gate receipts; and **WB-R16 makes B-12 the *incremental*
  variant deliberately** — a carry-down onto a client that already has activity — so a cleaned
  client cannot test the property B-12 exists to test. The **clean-room** carry-down is Bee
  Creative (zero entries, both YA2024 and YA2025 present, sole-proprietor CoA merged in #87),
  which also yields an independent second Gate K on a different entity shape.
  *(Context: these tenants are the owner's test vehicles, not paying-client production; the
  DOCUMENTS are real, which is what the no-fabrication rule protects.)*

- **WB-R30 — Gate F is BLOCKED on account provisioning, not on engineering.** `create_firm`
  (`0004:318`) refuses every principal that exists today: the owner's account and the seed account
  each hold an active membership (`actor already belongs to a firm`, CLR10), and the only
  membership-free identity is the **agent**, which is refused by name (`the agent identity cannot
  own a firm`, HIGH-11). There are also **zero unconsumed `firm_admissions` tokens** (3 of 3
  consumed). Gate F therefore needs three owner actions before any engineering can start: a new
  auth account holding **no** firm membership, a freshly minted admission token, and Rome Public
  Advisory's real particulars for the 11-Q interview. The agent is prohibited from creating
  accounts, so this is not delegable.

### ADR-043 — Migration 0021 DEPLOYED; **GATE K CLOSED on Bee Creative** (2026-07-26)

**Decision:** 0021 is live. **21 migrations · `clara-runtime` v27 unchanged** — the first Wave-B
migration to need **no quiescence and no runtime redeploy**, because it is one `create function`
the runtime holds no EXECUTE on. Backup first (10,088,736 B, head recorded at 20, zero 501s) →
`node packages/db/scripts/migrate.mjs` → post-verify **6/6**, strict head check included (the
ceremony does **not** set `clara.postverify_allow_later`; only the rig does).

**A read-only pre-flight was written and run before the apply** and is worth keeping as a habit:
seven probes asking whether LIVE is the database the rig proved the migration against — every
column 0021 writes, both unique indexes still kind-scoped and partial, all six helpers at the
**exact** signatures 0021 calls, both roles present with ownership transferable, **no
`create_counterparty` at any overload** (an existing overload would let `create function` succeed
and leave two verbs, which the tail's exact-signature check would not catch), the `kind` check
admitting both values, and the head at 0020. 7/7 — and the apply then succeeded first time.

**GATE K IS CLOSED, on a second entity shape, end to end through the product.** BEE CREATIVE
SOLUTION (sole proprietorship) — seed `1e60960e` **finalized**, batch 1, four entries **approved
in one serializable transaction**. Posted trial balance, to the sen:

| account | Dr | Cr |
|---|---|---|
| 150-CAP proprietor's capital — accumulated | 65,747.97 | — |
| 310-B01 bank — main operating | 39,252.03 | — |
| 400-000 trade payables — control (**2 counterparty-bound lines**) | — | 105,000.00 |
| 190-OBE opening balance equity (clearing) | 105,000.00 | 105,000.00 |
| **totals** | **210,000.00** | **210,000.00** |

OBE nets to **nil**; the dry-run read `TIES · opening-balance-equity net RM 0.00`. This is the
**negative-equity** case (drawings 163,495.02 exceed profit 53,517.57), so the carry-down seeded a
**debit-balance** capital account into the single `retained_earnings` slot — the demanding shape.

**Three firsts, all in production.** (1) `create_counterparty` minted LOST INVENTION SDN BHD
(`11463f05`), and its audit row proves the ADR-042 fix: `on_behalf_of` **NULL**, client in `args`,
actor the human. Registration left **blank** — no verified SSM number exists for that party and
none was invented, which exercised the name-only index branch. (2) The **`ap_open_item` path
executed for the first time ever**, twice, seeded **at invoice level** (`LIIV-20241205` 84,000.00 +
`LIIV-20241220` 21,000.00) rather than as one lump, so the AP sub-ledger is right from day one.
(3) **WB-R22's solo lane fired on a money-touching approval**: the first submit was refused
`CLR05 · SELF_ATTESTATION` — maker and checker are the same actor and BELCORT has exactly one
eligible approver — and only a typed attestation released it. The refusal came from the DB, not
the UI.

**One product finding, fixed in the same pass.** The opening page rendered **two** inputs both
labelled `aria-label="Amount in cents"` — the keyed trial-balance-target line and the opening item.
A sighted user has the section headings; a screen reader announces the same name twice (WCAG
1.3.1). Now `Keyed target amount in cents` / `Opening item amount in cents`, with a regression
guard that scans every opening-page control for a colliding label — proven to fail on a planted
collision. *(An earlier claim that this "silently misrouted a write" was withdrawn: that was a
`querySelector` first-match artefact of the automation, not something a human clicking a field can
do.)*

**What Gate K does NOT close.** The seed is **keyed**, not document-tied — the `opening_tb.line`
producer still does not exist, so the document-tied carry-down remains unproven on any client.
Ref: PR #94 · ADR-042 · WB-R22 · WB-R24.

---

---

