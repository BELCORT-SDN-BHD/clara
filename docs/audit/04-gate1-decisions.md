# Gate 1 — Owner Decision Record (BINDING)

**Approved by:** Tao (tools@belcort.com) · **Date:** 2026-07-17 · **Method:** structured grilling interview (one decision at a time, recommendation + alternatives presented, owner ruled each) · **Status:** Gate 1 APPROVED — findings report accepted, salvage manifest accepted, rulings below binding on Phases 2–5.

| # | Decision | Ruling |
|---|---|---|
| C3 | **Autonomy-constraint boundary** | **Four firm-killing invariants become structural DB guarantees:** (1) client attribution — the ≥0.95 gate DB-enforced; (2) provenance binding — document/sha validated at insert; (3) wake write-authority — allowlist per wake kind; (4) write authorization — plan→approve + role floors enforced in the DB. Accounting-judgement calls (coding choices, materiality, close-readiness g3/g4) stay visibility-first. |
| C4 | **Maker-checker** | Maker identity always modeled (structural). Distinct-approver is a HARD gate on the high-stakes lane only (tax-affecting, closed-period, large-amount, year-end close, opening balances). Routine entries keep the one-person flow. The agent can NEVER satisfy a human sign-off (sweep acknowledgements get a bookkeeper+ floor). Solo firms: DB-recorded self-approval attestation instead of a hard block. |
| C1 | **Memory notes** | **MERGE-INTO-KB.** Free-text notes table retired. Three needs become typed, lifecycle-managed objects: observation/profile → typed client-profile facts with provenance (injected into context packs); must_ask → first-class open-question object with resolution state surfaced at client-work start; rule_hint → low-evidence KB-proposal state. |
| B | **KB design (supersedes the old Desktop KB notes)** | **Two-layer Karpathy design** (per [karpathy LLM-wiki gist](https://gist.github.com/karpathy/442a6bf555914893e9891c11519de94f)): **Layer 1 —** a per-client, Clara-maintained WIKI (interlinked markdown pages: profile, counterparties, treatments, recurring patterns, open questions, period context) over immutable raw sources, with the three operations **ingest / query / lint**, cross-referenced, provenance-cited, versioned, and injected into every context pack. **Layer 2 —** a THIN typed authority layer holding the posting gates (user-gated account-mapping rules, the DB-enforced 0.95 gate per C3, must-ask objects that block workflows). **The wiki informs every decision but never decides an account or lowers a gate; wiki content is inert data on read.** Lint runs scheduled, surfacing contradictions/staleness to the owner. The two Desktop notes (`KB ITERATION SYSTEM.md`, `PER CLIENT KB MECHANISM.md`) are formally SUPERSEDED — stale, do not bind. |
| C2 | **Contacts / counterparties** | **SPLIT + INTRINSIC SUBLEDGER.** (1) PORT the proven name-intelligence (alias map, narration normalizer, human-gated recon-hint loop). (2) BUILD a first-class id-keyed counterparty ENTITY per client (typed, alias children) FK'd from rules, evidence, hints, AR/AP open items. (3) Counterparty narrative lives as wiki pages cross-linked to the entity id. (4) Subledger maintenance is INTRINSIC to coding/receipt executions — the audited write path creates/links the open item in the SAME transaction (kills the F3 dead-chain class structurally). |
| C5 | **Tax / compliance v1 scope** | **COMPLIANCE-CORRECT CORE:** (1) SST fully right — taxable-period model (registration date, assigned cycles, DG variations), true payment-basis service tax on real AR anchors, s.11(2) 12-month rule, dual-registrant separation surviving export, maintained rate/sector schedule; (2) AR/AP subledger + FA register + depreciation + disposal wired into live workflows; (3) honest FS — add SOCE + basic notes, or the pack stops claiming MPERS compliance; (4) ADR-044 draft tax computation stays v1 but as the LAST slice — first candidate to slip to v1.1; (5) payroll = coding + the deadline calendar actually built (no engine); (6) inventory = periodic closing-stock adjustment at close with completeness check. |
| C6 | **Client-data egress (OWNER OVERRIDE of the recommendation)** | **Full-content vendor tracing under DPA + firm disclosure.** Platform tracing keeps sensitive content for debugging value. Consequences carried as GATE-2 CHECKLIST ITEMS: the DPA must actually be executed; the disclosure must appear in firm-facing terms; PDPA cross-border transfer rules must be checked. Recorded: the recommendation (self-hosted / redacted tracing) was declined twice — this is a deliberate owner posture. |
| E1 | **Workspace** | Rebuild lives at `C:\Users\zhant\Desktop\clara-rebuild\` (git); seeds the Phase-3 fresh repo. Old repo + old Supabase stay frozen until Phase-5 decommission sign-off. |
| E2 | **Harness signal notes** | `harness bugs.txt` + the improvement report target the separate dev-toolchain/an earlier prototype; their lessons (user-journey simulation gate, verification-before-completion, state-transition ACs) feed the **Phase-5 verification design**, not Phase-1 findings. |
| — | **Live sweep** | A live READ-ONLY Supabase sweep (live schema/fns/RLS/storage vs `db/v2` SQL) runs **BEFORE Phase 2 starts** — owner authenticates the Supabase MCP; findings annotated into the audit evidence. |
| D | **Runtime** | OPEN — decided at Gate 2 with the architecture packet. The incumbent OpenAI Agents SDK is NOT presumptive. C6 adds a runtime requirement: vendor-tracing must be DPA-coverable; durable execution + resumable HITL remain the decisive rows. |

**Next:** live read-only sweep → Phase 2 (refreshed vision/PRD · target architecture · runtime recommendation · design direction · rebuild plan) → **GATE 2**.
