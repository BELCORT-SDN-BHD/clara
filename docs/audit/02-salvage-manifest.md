# Clara Phase 1 Audit — Salvage Manifest

*Every significant asset of the frozen product, classified **PORT** (proven — take it largely as-is), **REBUILD** (right idea, wrong implementation — rebuild to the stated standard), or **DROP** (do not carry forward). Consistent with the verified findings: an asset with a confirmed critical defect is not PORT unless the defect is trivially fixable in place. This is the supersession backbone — nothing here is deleted from the frozen repo; this records where each thing's replacement lives.*

## Summary

| Asset class | Assets | PORT | REBUILD | DROP |
|---|--:|--:|--:|--:|
| Database — schema objects, functions, triggers, storage, isolation tests (`db/v2/`) | 134 | 89 | 43 | 2 |
| Agent runtime & tool registry (`agent/`) | 37 | 17 | 19 | 1 |
| Doctrine, skills & product docs (`belcort/`, `docs/`, `PRD.md`) | 56 | 23 | 29 | 4 |
| Dashboard — surfaces, libs, components (`dashboard/`) | 55 | 27 | 24 | 4 |
| **TOTAL** | **282** | **156** | **115** | **11** |

---

## Database — schema objects, functions, triggers, storage, isolation tests (`db/v2/`)

| Asset | Verdict | Reason |
|---|:--:|---|
| `90-isolation-tests.sql (21 test groups, 1844 lines)` | **PORT** | Proves the tenancy invariants that matter (RLS+FORCE on every base table, cross-firm BLC02 fires first on 30+ write fns, per-user chat isolation, wake-credential shape, column-sealed webhook_secret) — the gate to keep and extend. Hardening notes (real coverage holes): no test for the SELECT-wrapped-DEFINER bypass (Ggr-1), no intra-firm role denial for the close/tax/opening families (F12-8/B-2), and no post-close tie-out/close-cross tests (F12-1). Extend, don't discard. |
| `_aging_core -> ar_aging/ap_aging, _statement_core -> customer/supplier_statement (read fns, 23-fns-subledger)` | **PORT** | As-of-aware aging and running-balance statements keyed on the items' own dates, never on GL sums, so they stay correct across a close. Sound. |
| `_trial_balance_core -> client_trial_balance / trial_balance_range (fns, 25-fns-ops)` | **PORT** | Segment-aware (all-time excludes closing/opening), raises trial_balance_unbalanced — the balance-read model every other GL reader should emulate. Sound. |
| `adjustments_status / amortisation_schedule_detail (read fns, 23c-fns-adjustments)` | **PORT** | Due-occurrence/catch-up/anchor-drift visibility. Hardening note: it omits a depreciation-through-period_end completeness component (F12-13) — add it; the read is sound. |
| `amortisation_schedules / amortisation_postings (tables, 19c)` | **PORT** | GL-bounded schedule with cumulative-target release and per-period idempotency. Sound. |
| `app.assert_can_review / assert_can_manage_kb / assert_can_manage_firm_settings (fns, 00-foundation)` | **PORT** | Correct RBAC floors (bookkeeper+/admin+, NULL denied) that also fence the wake credential. Sound; the gap is which fns call them (see the Track-1 engines), not these helpers. |
| `app.audit_actor (fn, 00-foundation)` | **PORT** | Anti-spoof actor constraint on the curated review/KB/triage/profile family. Hardening note: it lets any caller stamp the literal 'agent' (A-11/N3), which also silences the human-verb learn wakes — make the agent a real distinguishable principal in the rebuild rather than a self-assertable string. |
| `app.check_entry_balance + trg_je_balance/trg_jl_balance (trigger fn, 15-triggers)` | **PORT** | THE balance invariant: deferred SECURITY DEFINER constraint triggers on entries AND lines enforcing Σdr=Σcr and non-zero at commit, correctly skipping 'drafting', a moved line leaving both entries balanced. Spot-read and confirmed; rig-tested. Preserve verbatim. |
| `app.control_account / entry_account_net / assert_postable_entry (internal fns, 23-fns-subledger)` | **PORT** | Marker-resolved control-account helpers (DC/CC/OBE) and postable-entry guard — the re-keyable-COA-safe pattern the SST engine should have used. Sound. |
| `app.current_firm_id / app.role_rank / app.caller_rank / app.assert_firm_owns_client (fns, 00-foundation)` | **PORT** | The cross-firm isolation core: current_firm_id() reads the JWT firm claim, assert_firm_owns_client() raises BLC01/BLC02 before any other error, and every write fn re-asserts it. Proven and rig-tested — the gem to preserve. |
| `app.decay_rule_on_override (internal fn, 21-fns-kb)` | **PORT** | The sole (revoked) rule-decay writer, in-transaction, auto-retire at 3 overrides with evidence/confidence preserved — the one deterministic learn direction, rig-tested. Hardening note: it decays human-pinned 1.000 rules identically (C-13) — add a pinned tier decision in the rebuild. |
| `app.fye_period_start / fye_period_end (fns, 15-triggers)` | **PORT** | Correct computed FY-window helpers (leap-Feb clamp verified). Spot-read. Architectural note: the 'no periods table, windows computed' design (D18) is what forces every reader to hand-roll its own segment logic (root of F12-1) — the helpers are sound; a first-class periods/state-machine is the rebuild's job. |
| `app.generate_firm_slug (fn, 00-foundation)` | **PORT** | Global-uniqueness slug minter for bootstrap. Sound. |
| `app.guard_last_owner + trigger (15-triggers)` | **PORT** | Enforces >=1 active owner per firm, raising check_violation the member fns map to 'last_owner'. Sound, tested. |
| `app.normalize_counterparty / normalize_bank_narration (fns, 00-foundation)` | **PORT** | IMMUTABLE frozen normalizers backing functional unique indexes. Sound and deliberately frozen; a rebuild counterparty entity would build on them. |
| `app.set_firm_id / set_firm_id_self / set_jl_client_firm (trigger fns, 00-foundation)` | **PORT** | Firm-id (and journal-line client+firm) trigger-stamped from the parent/JWT, never caller-supplied (BLC03 on unresolvable firm). The isolation stamping the whole model rests on. |
| `app.set_updated_at + trg_entries_updated/trg_kb_rules_updated (trigger, 15-triggers)` | **PORT** | Standard updated_at stamping. Sound. |
| `app.sst_service_target / sst_receipt_effective (internal fns, 23e-fns-sst)` | **PORT** | The payment-basis statutory model (s.11(2) 12-month trigger, CN-cap, posted-and-never-reversed receipt effectiveness) verified against the owner's primary-source Act text. Sound internal engine to preserve. |
| `ar_invoices / ar_receipts / ar_allocations / ap_bills / ap_payments / ap_allocations (tables, 19-tables-subledger)` | **PORT** | Open-item subledger anchored 1:1 to the GL entry that moved the control account (entry_id NOT NULL unique), append-only allocations, closed status sets (no 'void' deliberate). Structurally sound and rig-tie-out-proven. Hardening notes: add a void/supersede verb (B-8) and note the gross snapshot can stale off a mutable auto_draft anchor (N11) — the tables are correct. |
| `assert_signup_admitted / signup_admission_status (fns, 28b-fns-signup-admission)` | **PORT** | Fail-closed admission gate (signups_closed/org_cap_reached) + dashboard pre-gate read. The interim launch guardrail to keep (note: it is a firm-COUNT gate, NOT usage metering — GAP4-5 requires real per-firm budgets before pilot). |
| `auto_draft_review_batch (read fn, 25-fns-ops)` | **PORT** | Watermark-keyed unsupervised-lane sweep (keyed on auto_posted_at, clamped), rig-tested. Sound. |
| `auto_draft_sweep_log (table, 14-tables-ops)` | **PORT** | Append-only per-firm auto-post sign-off watermark (ADR-034), proven by the sweep isolation tests. Table is sound; the acknowledge fn needs a role floor (see acknowledge_auto_draft_sweep). |
| `bank_match_audit (table, 11-tables-recon)` | **PORT** | Append-only match/unmatch trail with plain-bigint (no-FK) ids so it survives deletes, and it correctly drives the human-verb learn wake. Sound audit design. |
| `chat_sessions (table, 16-tables-session)` | **PORT** | Per-(firm,user) private sessions with one-active-per-scope partial uniques and client_id ON DELETE RESTRICT. Hardening note: active_run_id must bind to durable run records (a dangling pointer today, Grt-8) and set_active_run needs a CAS lease (GAP4-2) — both are the durable-runtime rebuild, the session table shape is fine. |
| `client_bank_accounts (table, 11-tables-recon)` | **PORT** | Correct client-bank->COA leg mapping with composite FK. update_client_profile's delete-if-missing churn (N10/C-16) is a fn defect, not a table one. |
| `client_directors / client_email_domains / client_aliases (tables, 13-tables-client)` | **PORT** | Client-identity registers genuinely read by accounting executions (director_payee must-ask, doc->client matching). The delete-all+reinsert churn is in update_client_profile (rebuild that fn), not these tables. |
| `client_financial_position (table, 13-tables-client)` | **PORT** | Sound close/opening snapshot (unique(client,as_of), in_balance). Hardening note: reverse_year_end_close hard-DELETEs the close-generated snapshot (GAP5-2) and generated_by_close_id has no FK — make it supersede-not-delete in the rebuild; the table shape is fine. |
| `client_fy_close (table, 14-tables-ops)` | **PORT** | Year-end close registry (uq_fy_close_live one-live-row, reversed_at reverse-not-delete, RE-code composite FK) — the design other families should mirror. Hardening note: persist a readiness/acknowledgment payload at close time so the g3/g4 surface-and-acknowledge decisions are auditable (GAP5-1); the record fn is the REBUILD, the registry table is sound. |
| `client_kb_audit (table, 12-tables-kb)` | **PORT** | Append-only KB governance trail (promote/reject/edit/retire/create/decay/flag/alias). Sound. |
| `client_kb_rules (table, 12-tables-kb)` | **PORT** | Sound governance spine (status/confidence/override_count decay, composite FK, unique triple). Hardening notes (owner design decisions, not wrong-impl): the 'candidate' tier is unreachable in production (C-11) and there is no pinned/locked tier for human-authored 1.000 rules (C-13) — resolve as an explicit ladder decision in the rebuild. |
| `client_partners (table, 19c)` | **PORT** | Partnership PSR register (psr CHECK, unique(client,name), 3 composite FKs), enforced Σ psr = 1. Sound; onboarding just never captures it (B-14, external). |
| `client_profile_audit / document_audit / coa_audit (tables, 13-tables-client)` | **PORT** | Append-only audit sidecars with correct action CHECKs; document_audit correctly drives the human-triage wake. Sound. |
| `client_recon_hints (table, 11-tables-recon)` | **PORT** | Vote-model narration->counterparty matcher, human-fenced, never auto-matches; confirmed/retired never silently flip. Proven, tested learn loop. |
| `client_trial_balance_comparative / client_overview / firm_needs_attention / firm_digest / mark_digest_seen (read fns, 28-fns-reads)` | **PORT** | Comparative TB (minor post-close asymmetry F12-15), overview dot contract, needs-attention/digest reads with per-user watermark. Sound. |
| `clients (table, 10-tables-core)` | **PORT** | Sound per-firm client row (slug unique, regime/framework/fye CHECKs). Hardening notes (additive, not wrong-impl): currency_default is a label with no FX ledger behind it (Ggr-12), msic_code is stored-but-unread by coding/tax logic (A-8), and SST registration facts (effective date, assigned taxable-period cycle, DG variation) are unmodeled (GAP3-1) — all new columns/consumers, the table shape is correct. |
| `coa_accounts (table, 10-tables-core)` | **PORT** | The composite-FK anchor (client_id,acc_code) and special_acc_type marker system are proven cross-client-safety gems. Data-level note: SST payable 460/461 are seeded with NULL markers so the SST engine keys on literals (F12-11) — fix in the seed, not the table. |
| `create_recurring_journal / end_recurring_journal / run_recurring_journals / create_amortisation_schedule / cancel_amortisation_schedule / run_amortisation / set_client_partners (fns, 23c-fns-adjustments)` | **PORT** | Sound adjustments engine: templates must balance, per-client advisory close lock, idempotent runs, closed-period occurrences skipped+surfaced, cumulative-target amortisation release, PSR=1 partner enforcement. Hardening note: add role floors (N2). Preserve. |
| `dashboard_seen (table, 14-tables-ops)` | **PORT** | Per-user digest watermark UI state. Sound. |
| `ensure_chat_session / append_chat_message / set_active_run / reset_chat_session (fns, 26-fns-session)` | **PORT** | Per-scope session lifecycle with unique-violation race handling, 64KiB cap, scope-stamp guards, reset-archive. Hardening notes: append_chat_message must persist tool_calls/artifact/clarify (Grt-9, tied to the chat_messages rebuild) and set_active_run needs a CAS lease so two tabs can't mint overlapping runs (GAP4-2). Session shape is sound. |
| `export_artifacts (table, 14-tables-ops)` | **PORT** | Write-once, versioned, firm-scoped 7-yr artifact index with key-shape CHECKs and version-under-advisory-lock — the artifact taxonomy to keep. Hardening note (real trust seam): add a stored/verified status and reconcile the caller-supplied sha256 against the stored object so a failed upload can't leave a phantom row and model-authored bytes can't masquerade as an audited artifact (H-1/H-7). |
| `export_receipts (table, 14-tables-ops)` | **PORT** | Handoff receipt row. Hardening notes: capture the requesting human (not the literal 'agent', H-9) and the full request spec/data-version for reproducibility (H-8) — added columns, not a shape rebuild. |
| `fa_depreciation (table, 19b-tables-fixed-assets)` | **PORT** | Depreciation postings with unique(asset,period_start,period_end) idempotency backstop; the carry-down baseline row makes run_depreciation continue from NBV. Sound. |
| `fa_register / fa_depreciation_schedule (read fns, 23b-fns-fixed-assets)` | **PORT** | Lead-schedule/as-of register reads anchored on acquisition/depreciation entries. Sound. |
| `firm-stamp triggers: trg_<t>_set_firm across ~30 tables + trg_clients_set_firm + trg_jl_set_client_firm (15-triggers/per-file)` | **PORT** | Firm-id (and journal-line client+firm) stamped from parent/JWT on every client-scoped table, never caller-supplied — the isolation stamping the whole tenancy model rests on. Proven, rig-tested. |
| `firm_invites (table, 00-foundation)` | **PORT** | Correct pending-invite model with admin+ RLS read gate and case-insensitive pending-dedup. token_hash is generated-but-unused dead capability (N9) — drop the column in the rebuild. |
| `firm_profile_audit (table, 00-foundation)` | **PORT** | Append-only firm-settings edit trail with before/after jsonb, sole writer update_firm_profile. Sound audit sidecar. |
| `firm_users (table, 00-foundation)` | **PORT** | Sound membership+RBAC shape (one-firm-per-user unique, role/status CHECK, guard_last_owner). Hardening note: add a session-version/epoch column to enable revocation, and a maker/drafted-by identity model at the application layer — the stale-JWT-authority hole (GAP1-4) and no-maker-checker (GAP3-7) live in the auth hook / role helper, not this table. |
| `firms (table, 00-foundation)` | **PORT** | Proven multi-firm root: RLS+FORCE, id=current_firm_id() policy, global-unique slug, entity/turnover/tier CHECKs. No confirmed defect in the table itself. |
| `fixed_assets (table, 19b-tables-fixed-assets)` | **PORT** | PPE register with GL-anchored cost (acquisition_entry_id NOT NULL), 3 composite FKs to coa, conservative CA-basis descriptors (ca_class, is_commercial_vehicle, is_new default FALSE). Sound. |
| `je_cursor/gl_cursor helpers + journal_entries_page / journal_entries_band_counts / journal_entry_detail (read fns, 28-fns-reads)` | **PORT** | Relay-parity picker with durable authorship (rule/matched/auto/human), keyset paging, per-row balanced flag, typed entry detail — proven, rig-tested read surface. Sound. |
| `journal_entry_history (table, 10-tables-core)` | **PORT** | Append-only before/after jsonb entry audit with a full action set (drafted/approved/edited/rejected/reassigned/recoded/reversed/reversal) — the audit spine to keep. Note: the close family writes no rows here (GAP5-2) — a caller-fn gap, not a table gap. |
| `journal_lines (table, 10-tables-core)` | **PORT** | The composite FK (client_id,account_code)->coa_accounts, bigint-cents, dr/cr domain CHECKs and firm/client denormalization-via-trigger are proven. Hardening note: add a posted-line immutability trigger — the GAP0-4 posted-line mutability is a missing trigger on this table, not a shape defect. |
| `member fns: create_invite / revoke_invite / accept_invite / my_firm / my_pending_invite / list_firm_members / set_member_role / remove_member (24b-fns-members)` | **PORT** | Rig-tested member/invite lifecycle (rank>=3 gates, one-firm-per-user, guard_last_owner, case-insensitive dedup). Hardening note: set_member_role/remove_member do no session revocation, so a removed/demoted member keeps authority until token refresh (GAP1-4) — fix in the auth-hook/role-helper rebuild, the member fns themselves are sound. |
| `proactive_notifications (table, 14-tables-ops)` | **PORT** | Durable dedup-keyed agent-notice rows (open-only partial unique, evidence/intent bands) — the right substrate. Hardening note: add a clarify/needs-you kind so cross-scope pending clarifies project into the inbox (J-28), and let routing be risk/materiality-keyed (A-4) — additive kinds, not a rebuild of the table. |
| `public.add_coa_account / set_coa_account_active / set_coa_account_type (fns, 20-fns-journal)` | **PORT** | Row-add/retire-not-delete/presentation-reclassify with review floor, control-account protection, and coa_audit rows. Sound. |
| `public.add_counterparty_alias (fn, 21-fns-kb)` | **PORT** | Review floor + audit_actor, single-level guard, canonical collapse, kb_audit 'alias'. Sound; the missing canonical re-key sweep (C-9) is the counterparty-entity rebuild, not this fn. |
| `public.assign_document / mark_document_sample / request_document_coding (fns, 22-fns-documents-recon)` | **PORT** | Review floor + audit_actor, unassigned/status guards, document_audit rows that atomically fire the human-triage wake. Sound. |
| `public.client_financial_statements (read fn, 28-fns-reads)` | **PORT** | Segment-floored SoCI (standard-only) + SoFP with presentation-completeness honesty. Hardening notes: it stamps 'Prepared in accordance with MPERS/MFRS' while producing only SoCI+SoFP (no SOCE/cash-flow/notes, GAP2-5) — relabel to management accounts OR build the statutory pack, and add a spans-close guard (F12-7). The read shape is sound. |
| `public.client_general_ledger (read fn, 28-fns-reads)` | **PORT** | Correctly SEGMENTED by live year-end closes (opening floors at the latest close boundary; raises gl_window_spans_close) — the segmentation model the tie-outs should copy. Sound. |
| `public.compute_sst_leg (fn, 20-fns-journal)` | **PORT** | DB owns the SST number: STABLE, effective-dated rate lookup from tax_rates, gross/net handling, raises unknown_sst_treatment/no_sst_rate_for_date/amount_negative. Spot-read and clean — a keep-verbatim compute primitive. |
| `public.create_firm (fn, 24-fns-onboard)` | **PORT** | Bootstrap: requires a logged-in sub, one-firm-per-user, fail-closed signup-admission gate, mints slug, inserts firm+owner. The one write fn deliberately without assert_firm_owns_client. Sound. |
| `public.entry_snapshot (internal fn, 20-fns-journal)` | **PORT** | Internal header+lines jsonb builder for audit rows. Sound helper. |
| `public.get_sst_return (fn, 23e-fns-sst)` | **PORT** | Fail-loud read of the persisted SST draft. Sound. |
| `public.get_tax_computation (fn, 23d-fns-tax)` | **PORT** | Fail-loud read of the persisted draft. Sound. |
| `public.insert_bank_lines (fn, 22-fns-documents-recon)` | **PORT** | Doc firm re-check, per-line/statement-level bank account inference, validates every line's account belongs to the client. Sound. |
| `public.onboard_client (fn, 24-fns-onboard)` | **PORT** | Validated client insert + COA seed + bank/directors/domains/aliases load with COA-code validation. Hardening notes (additive): add an admin+ role floor (B-2 high — a viewer can currently onboard) and move SSM/TIN/MSIC format validation into the DB (B-11). Core flow is sound. |
| `public.open_reconciliation (fn, 22-fns-documents-recon)` | **PORT** | All required fields guarded, bank account and statement document verified against client+firm (cross-client statement docs rejected). Sound. |
| `public.promote_proposal / reject_proposal / create_kb_rule / edit_kb_rule / retire_kb_rule / confirm_kb_rule (fns, 21-fns-kb)` | **PORT** | Admin+ governance verbs: no-downgrade promote (greatest(confidence,0.95)), open-only reject, confirmed-1.000 create, reverse-not-delete retire, candidate\|retired->confirmed confirm. Sound, rig-tested. Note the candidate tier they support is unreachable in production (C-11) — resolve as a ladder decision, the fns are correct. |
| `public.record_recon_hint / suggest_recon_counterparty / promote_recon_hint / retire_recon_hint (fns, 22-fns-documents-recon)` | **PORT** | Vote-tally hint writer (confirmed/retired never silently flip), advisory read, admin-gated promote/retire. Proven, human-fenced learn loop. |
| `public.reject_entry (fn, 20-fns-journal)` | **PORT** | Sound: review floor + audit_actor, reason-required, un-reconciles the entry/lines/recon, and decays the cited rule in-transaction. Hardening note: shares the no-revision-token gap but reject is non-posting. |
| `public.resolve_counterparty / kb_pnl_side (fns, 21-fns-kb)` | **PORT** | Deterministic alias/canonical resolution (alias beats canonical, then lowest id, passthrough on no hit). Sound; note coding-time lookup doesn't call it (C-10, a doctrine gap). |
| `public.reverse_entry (fn, 22-fns-documents-recon)` | **PORT** | Well-built reversal: review floor + audit_actor, verbatim guards (cannot_reverse_close_entry, approved-only, cannot_reverse_a_reversal, already_reversed, entry_anchors_active_schedule), never touches the original, current-dated mirror. Hardening note: it leans on tie-outs for anchor-reversal visibility which break post-close (F3-6) — fix the tie-outs, not this fn. |
| `public.seed_client_coa (fn, 24-fns-onboard)` | **PORT** | Copies the ~90-account LHDN-reconciled master COA idempotently (marker-seeded controls). Hardening notes: add a role floor (B-2, trivially in-place) and seed SST markers on 460/461 (F12-11 data fix). The COA provenance is a genuine asset to keep. |
| `public.seed_client_knowledge (fn, 24-fns-onboard)` | **PORT** | Dual-seed (confirmed rules via create_kb_rule @1.000 + advisory notes) — atomic, idempotent, cross-firm-denied, and the admin+ floor transits the nested create_kb_rule. Sound; a rebuilt Path-B GL-import lane could reuse this batch primitive. |
| `public.set_document_storage_path (fn, 22-fns-documents-recon)` | **PORT** | Strict key grammar (own firm prefix, own slug, folder whitelist, key ends in this doc's sha). Sound as a validator; note the agent has no storage move capability to pair with it (E-3, a runtime gap). |
| `public.update_firm_profile / add_bank_account (fns, 24-fns-onboard)` | **PORT** | update_firm_profile is admin+ and operates only on current_firm_id() (zero cross-firm surface); add_bank_account requires an active COA leg with a dup guard. Sound. |
| `record_accrual (fn, 23c-fns-adjustments)` | **PORT** | Atomic one-shot accrual + dated auto-reversal pair with advisory close lock and balance/domain guards. Hardening note: it takes no idempotency key, so a retry-after-lost-response posts a SECOND approved accrual pair with no review gate (GAP4-1) — add an idempotency key; the accrual logic is sound. |
| `record_ar_invoice / record_ar_receipt / record_ap_bill / record_ap_payment / allocate_ar_receipt / allocate_ap_payment / write_off_ar_invoice (fns, 23-fns-subledger)` | **PORT** | Internally sound accounting: gross READ from the anchoring GL control leg (never agent-supplied), counterparty canonicalised, over-allocation and counterparty-mismatch guards, write-off bounded by outstanding — rig-tie-out-proven to the cent. Hardening notes: add an intra-firm role floor (N2, trivially in-place) and note the integration gap (no workflow ever calls them post-onboarding, F3-1/F3-2/F3-3) plus stored-string allocation stranding (C-9) are external to the fns. |
| `record_fixed_asset / run_depreciation / dispose_fixed_asset (fns, 23b-fns-fixed-assets)` | **PORT** | Exemplary DB-owned math: cost from the anchor minus prior claims (race-locked), cumulative-target idempotent depreciation, DB-owned NBV+gain/loss on disposal, account-role/method-driver validation. Hardening note: add a role floor (N2) — the logic is the reference implementation to keep. |
| `record_proactive_notification / mark_notifications_read / resolve_notification (fns, 25-fns-ops)` | **PORT** | Fail-closed validation, open-dedup, cap-5-per-day digest rollup, review floor on resolve. Sound. Hardening note: add risk/materiality/clarify kinds (A-4/J-28) — additive. |
| `recurring_journals / recurring_journal_lines / recurring_journal_runs (tables, 19c)` | **PORT** | Template + idempotent runs (unique(template_id,occurrence_date)); templates must balance (fn-enforced). Sound. |
| `seed_opening_ar_invoice / seed_opening_ap_bill / seed_opening_fixed_asset (fns, 25b-fns-opening)` | **PORT** | Per-item opening seeders — Dr/Cr control<->OBE by marker, reuse of record_ar_invoice/record_ap_bill/record_fixed_asset, FA baseline row so run_depreciation continues from NBV — rig-proven to the cent (BEE golden test). Hardening notes: add a role floor + idempotency key and replace raw 23502/23503 with named raises (B-13). The accounting model is a genuine asset to keep. |
| `signup_admission (table, 28b)` | **PORT** | Fail-CLOSED admission singleton with deny-all RLS, read only via signup_admission_status(). Sound gate. |
| `sst_returns (table, 19e)` | **PORT** | SST-02 draft worksheet with overlap-supersede (uq_sst_return_live) — statutory mechanics verified against the owner's primary sources. Hardening notes: it inherits the no-reviewed/finalized-lifecycle gap (like tax_computations) and depends on SST registration facts that are unmodeled on clients (GAP3-1); the compute fn keys on literal 460/461 (F12-11). The draft table itself is sound to carry forward. |
| `storage: firm-docs bucket + RLS (firm_docs_read/insert, NO update/delete) (storage-setup.sql)` | **PORT** | Private, write-once, firm-prefixed bucket (no UPDATE preserves the sha256<->bytes bond, no DELETE gives reverse-not-delete + LHDN 7-yr retention), service-role contained to one upload closure. A sound substrate. Hardening notes (external to the policies): the agent has no storage read/copy/move capability so the mandated post-assign 'Storage move' is impossible (E-3), no evidence-region bytes are captured (J-18), and legal_hold is a dead control with no governed place/release fn (GAP3-5). |
| `tax_ca_rates (table, 19d)` | **PORT** | Global effective-dated capital-allowance rate authority (read-all, no writes). Data note: the YA2024+ ICT split (IA40/AA20) is self-flagged as disputed and could not be verified against the gazette (F12-14) — owner must verify P.U.(A) 328/2024 before live reliance; the table structure is sound. |
| `tax_corp_rates (table, 19d)` | **PORT** | Global effective-dated corp-band authority (MSME 15/17/24, human-confirmed conditions). Sound. |
| `tax_rates (table, 18-tables-reference)` | **PORT** | Global, effective-dated SST rate schedule (PK treatment+valid_from), read-all policy, migration-only writes — the DB-owns-the-rate design to keep. Data note: re-seed for the 2025-07-01 service-tax expansion and split 6% vs 8% treatments distinctly (GAP3-2) — a seed/vocabulary refresh, not a shape rebuild. |
| `tests/ functional suite (62 files + PG-rig runner + net stub)` | **PORT** | Fresh-PG initdb rig with Supabase/auth stubs, pg_net stub with failure-injection, completeness tripwire, and business-logic coverage per fn family incl. the BEE-CREATIVE real-FY golden test — genuine regression value to carry forward. Hardening notes: it runs as the migration role (never 'authenticated', so grants/RLS are proven only by the isolation suite, N14) and the close x tie-out interaction is disjoint/untested (F12-1). Keep and close the gaps; the CI must actually run it (GAP1-5). |
| `30-grants.sql posture (grant/revoke model)` | **REBUILD** | The per-file revoke-DML + grant-EXECUTE posture is right, but it is not future-proof: it strips PUBLIC EXECUTE from EXISTING fns only with no ALTER DEFAULT PRIVILEGES, so any fn applied live after 30-grants silently gets PUBLIC EXECUTE (N4), and the belcort_definer non-BYPASSRLS re-ownership is documented but NOT applied — so today every DEFINER fn is owned by a BYPASSRLS role and the firm boundary inside them rests solely on assert_firm_owns_client (N4/30:24-43). Rebuild with default-privilege revocation and the non-BYPASSRLS definer ownership applied so RLS is a real second line, not a single code closure. |
| `_tie_out_core -> ar_control_tie_out/ap_control_tie_out (read fns, 23-fns-subledger)` | **REBUILD** | Sums the GL control account all-time with no entry_kind/segment filter, so from the first year-end close it double-counts the close's opening restatement entry and reports permanent phantom drift equal to the whole control balance — burying the real drift it exists to surface (F12-1 critical, F3-7). Rebuild to force every GL-consuming read through one segment-aware balance primitive (as _trial_balance_core already excludes closing/opening). |
| `app.current_user_role (fn, 00-foundation)` | **REBUILD** | Prefers the JWT firm_role claim and only falls back to the live membership row, so a demoted member's stale 'admin' claim outranks their live 'viewer' row for every RBAC floor until token refresh (GAP1-4). Rebuild to resolve role from live membership (or verify a session-epoch) so revocation bites immediately. |
| `app.je_closed_period_guard + trg_je_closed_period (trigger, 25-fns-ops)` | **REBUILD** | One-directional: it inspects only NEW.posting_date, so an entry can be re-dated OUT of a closed period draining swept history (F12-4), and its via_fn+kind exemption is broad enough that a close reversal posts mirrors into LATER locked periods (F12-5/GAP5-3). Rebuild as a bidirectional lock (reject when OLD.posting_date is in a live close too) with the exemption scoped to the exact period being closed/reversed, plus a journal_lines-level guard. |
| `bank_reconciliations (table, 11-tables-recon)` | **REBUILD** | Missing the structure a recon chain needs: no uniqueness on (bank_account_id, period), no opening=prior-closing continuity, a dead 'balanced' status the code never sets, and the close arithmetic drops brought-forward outstanding items (GAP1-3/F12-1). Rebuild as a period-chained recon (one live recon per account-period, opening continuity enforced, carry-forward outstanding). |
| `bank_statement_lines (table, 11-tables-recon)` | **REBUILD** | No uniqueness on matched_entry_id and no first-class match table, enabling ghost 'reconciled' entries and one-entry-claimed-by-many-lines with last-writer-wins (GAP1-2). Rebuild the match model as a match table with UNIQUE(line_id)/UNIQUE(entry_id) (or explicit split semantics) so reconciled state is derivable, never a free-floating flag. |
| `belcort_books_sync_emit + trg_bs_* triggers (25-fns-ops)` | **REBUILD** | Same fire-and-forget webhook transport as the proactive emit. The agentless books-changed SSE-nudge CONCEPT is good and should be preserved (D fine_claim), but the delivery mechanism belongs in the outbox rebuild; note chat_messages has no such trigger so a persisted reply never nudges the transcript (D-11). |
| `belcort_proactive_emit + trg_pn_* triggers (25-fns-ops)` | **REBUILD** | Fire-and-forget webhook emit wrapped in EXCEPTION-WHEN-OTHERS (enqueue failure swallowed, nothing consumes net._http_response, no retry) driving a static 8-condition trigger map with zero risk/materiality/period keying, missing whole human-verb families (reversals, COA/tax-status changes, closes, KB governance), and firing self-caused wakes (A-2/A-4/A-13). Rebuild as a transactional domain-event outbox with a DB-owned trigger policy (classify every event, route by risk/materiality/period, origin-aware) and durable consumers. |
| `belcort_webhook_config (table, 14-tables-ops)` | **REBUILD** | Part of the fire-and-forget webhook wake transport being replaced (A-2), and it stores a Bearer-grade webhook_secret in plaintext at rest (N15, column-sealed from the request plane only). Rebuild event distribution as a transactional outbox + durable consumers; if per-firm webhook config survives at all, move the secret to a managed secret store. |
| `chat_messages (table, 16-tables-session)` | **REBUILD** | The reserved durable tool_calls and artifact jsonb columns are NEVER written, so reasoning trail and native cards are lost, cards travel as fenced prose re-extracted at read, and clarify Q&A is never persisted as typed rows (Grt-9, D-8). Rebuild the transcript store with typed tool-call/artifact/clarify/interruption rows. |
| `client_counterparty_aliases (table, 13-tables-client)` | **REBUILD** | Counterparty identity is a string convention, not data: there is no id-keyed counterparty entity, so alias rows are a flat map, a rename splits history (old rules/evidence/open items keep the old canonical) and can strand settlements (C-9). Rebuild as alias children of a first-class per-client counterparty record FK'd from KB rules, evidence, subledger items, and recon hints, so a rename/merge atomically re-keys. |
| `client_kb_rules_history (table, 12-tables-kb)` | **REBUILD** | An aggregate integer tally with zero provenance — no entry_id/source_doc link, so 'which transactions taught this rule?' is unanswerable and repeated wakes double-count over the >=3 proposal threshold (A-9). Rebuild as per-sighting evidence events carrying entry/document provenance with DB idempotency, aggregates derived. |
| `documents (table, 10-tables-core)` | **REBUILD** | Wrong implementation of the extraction/evidence index: OCR is one jsonb column with two competing producer schemas (A-15), no per-field evidence regions are captured anywhere (J-18 critical), the kind CHECK is too narrow to hold knowledge artefacts the onboarding skill already tries to file (F3-10/GAP2-2), retain_until is anchored at row-creation not period-end (GAP3-4), and unique(firm,sha256) makes a genuinely shared source doc un-fileable to two clients (E-13). Rebuild to a typed, indexed extraction store with captured evidence regions, a period-anchored retention clock, and a document-kind taxonomy that includes knowledge artefacts. |
| `fa_control_tie_out (read fn, 23b-fns-fixed-assets)` | **REBUILD** | Same defect as _tie_out_core: raw all-time GL sums for FA cost and accum-dep with no segment floor double-count the close's opening entry after the first close (F12-1/F3-7). Rebuild through the shared segment-aware balance primitive. |
| `human-verb learn-wake triggers: trg_pn_doc_triage / trg_pn_workbench / trg_pn_bank_match (25-fns-ops)` | **REBUILD** | The actor-is-distinct-from-'agent' fencing and one-hop-bounded self-wake prevention are a genuinely good design (A/F3 fine_claims), but they ride the fire-and-forget emit transport being replaced and are bypassable by a spoofed 'agent' label (A-11), and human reversals emit nothing (A-4). Rebuild the fences on top of the transactional outbox with a non-spoofable agent principal. |
| `jobs (table, 14-tables-ops)` | **REBUILD** | A thin async-job lane superseded by the greenfield durable-runtime target (DB-backed tasks/runs/parts/checkpoints/interruptions). Restart re-drive miscounts already-approved ids as failures and can flip a successful bulk job to FAILED, and it only re-drives on a user surface (Grt-11). Rebuild as the durable run/task substrate with idempotent restart re-drive. |
| `jobs lane fns: create_job/advance_job/finish_job/fail_job/request_pause/resume/cancel/get_job/list_jobs (25-fns-ops)` | **REBUILD** | The async-job lane superseded by the durable-runtime target; get_job redaction and terminal-idempotence are fine, but restart re-drive miscounts and there is no durable run/checkpoint substrate (Grt-6/Grt-11). Rebuild as DB-backed tasks/runs. |
| `journal_entries (table, 10-tables-core)` | **REBUILD** | Header shape is good but provenance/authorship/concurrency are structurally missing: document_id+source_doc_sha256 are unconstrained/nullable with no typed-origin contract (GAP0-1 critical), there is no maker/drafted_by column (GAP3-7), no revision token for exact-approval concurrency (GAP0-5), and kb_rule_id is a single mutable pointer that corrections erase (C-14). Rebuild with a typed-origin contract per entry, structural maker identity, an optimistic-revision token, and immutable draft-time knowledge snapshots. |
| `kb_proposals (table, 12-tables-kb)` | **REBUILD** | proposal_type CHECK IN ('rule_promotion') is structurally single-type, so new-account candidates, conflict resolutions, and document-extracted rules are impossible rows (C-12). Rebuild typed for multiple proposal kinds from day one; keep the open-only partial-unique idempotency. |
| `public.acknowledge_auto_draft_sweep (fn, 25-fns-ops)` | **REBUILD** | The compensating control for unsupervised posting is self-satisfiable: NO role floor, NO audit_actor constraint, blank actor coalesces to 'agent' — so a viewer can sign off the firm's oversight sweep and the same agent principal that auto-posted can acknowledge its own sweep (N2/GAP3-7). Rebuild with a real role floor and a forbidden-'agent'-identity sign-off. |
| `public.agent_select (fn, 26-fns-session)` | **REBUILD** | CRITICAL: the agent's only freeform read is lexically read-only only — `select approve_entry(...)` (any SECURITY DEFINER write wrapped in a SELECT) passes both the TS guard and the DB verb-scan and commits, defeating the tool-layer active-scope write-gate and per-wake speak-never-act policy (SDT-001/SEC-001, confirmed at Ggr-1/I-1/C-15/H-6); reads are also unlogged (Ggr-4) and firm-scoped-but-not-client-scoped (GAP5-5). Rebuild the read path structurally read-only: a typed/whitelisted read layer or a role with NO EXECUTE on write fns under default_transaction_read_only, client-scoped by default, with every query logged. |
| `public.approve_entry (fn, 20-fns-journal)` | **REBUILD** | Carries no expected-revision/snapshot token, so an approval after an intervening edit posts legs the approver never saw (GAP0-5), and there is no maker!=approver duty separation or human-approval envelope (GAP0-2/GAP3-7). Its RBAC floor + audit_actor are sound and should be kept; rebuild the signature to carry an exact-payload approval token and enforce duty separation. |
| `public.belcort_access_token_hook (fn, 00-foundation)` | **REBUILD** | The sole revocation point fires only at token issuance and its exception handler FAILS OPEN — on any error it returns the event with inbound caller-supplied firm claims intact (GAP1-4 caveat). Rebuild the authority model with a real revocation mechanism (short TTL + admin sign-out, or a session-epoch checked in the RLS helper) and a fail-closed hook. |
| `public.close_reconciliation (fn, 22-fns-documents-recon)` | **REBUILD** | Ignores statement opening-continuity, drops prior-period brought-forward outstanding items from the tie-out (correct only for the first period), computes book balance all-time with no segment floor so it double-counts the close's opening entry (F12-1 critical/GAP1-3), and reaches 'completed' with zero statement lines matched. Rebuild with period chaining, carry-forward outstanding, segment-aware book balance, and a matched-lines completion requirement. |
| `public.compute_sst_return (fn, 23e-fns-sst)` | **REBUILD** | Statutory mechanics (form arithmetic, due dates, CN handling, bad-debt-relief candidates) are verified crown jewels to PRESERVE — but it hardcodes literal 460-000/461-000 (so SST tagged to any other account silently vanishes from both the return AND its uncounted-movement safety net, F12-11), takes caller-supplied period bounds with no registration-fact validation (GAP3-1), and its service-tax payment basis silently degrades to accrual because no workflow records the AR anchors (C-8/F3-8). Rebuild to resolve controls by marker, validate the assigned taxable period, and gate on proven feedstock. |
| `public.compute_tax_draft (fn, 23d-fns-tax)` | **REBUILD** | Its statutory chargeable-income bridge, CA schedule (first-use-YA rate fixing, MV/SVA caps), band selection, and honesty layer are crown-jewel logic to PRESERVE — but the fn has no entry_kind filter, so run after a close it computes PBT=0 and supersedes the correct draft with garbage (F12-3), and its add-back detection is coding-flag-only with no account fallback for donations/entertainment (F12-12). Rebuild to filter entry_kind='standard', add account-sweep add-back candidates, and feed the typed tax_computations lifecycle. |
| `public.draft_entry (fn, 20-fns-journal)` | **REBUILD** | Inserts caller-supplied document_id + source_doc_sha256 verbatim with the ONLY provenance check being the kb_rule confirmed-rule test — no validation that the document exists in-firm/for-the-client or that the sha matches documents.sha256, and primary leg amounts are model-transcribed with no source tie (GAP0-1 critical, Ggr-9). Spot-read and confirmed. Rebuild with a typed-origin contract that derives-and-asserts the (document->firm,client,sha) tuple and binds amounts to extracted fields. |
| `public.edit_entry (fn, 20-fns-journal)` | **REBUILD** | Physically deletes+reinserts posted legs under the same id (edits APPROVED with a reason and auto_draft with NO reason/no demotion), no trigger blocks posted-line mutation, and it accepts a new posting_date that can re-date an entry OUT of a closed period draining swept history (GAP0-4, F12-4). Rebuild so posted means immutable (amend only via reversal/supersede-with-lineage) and closed-period dates are bidirectionally locked. |
| `public.finalize_coding (fn, 20-fns-journal)` | **REBUILD** | Structurally enforces only the auto_draft<->risk='auto' label bind; the substantive auto-post gates (a confirmed cited rule, stored confidence >=0.95, empty must_ask_flags, no tax leg) are model-asserted labels the DB stores but never checks (A-5, Ggr-13). Rebuild as a DB-enforced autonomy/materiality kernel that refuses the unsupervised lane unless the conditions actually hold. |
| `public.firm_activity_feed (fn, 25-fns-ops)` | **REBUILD** | Unions only 6 of 11 audit families (COA changes, firm-profile edits, year-end closes, sweep sign-offs, memory notes are invisible) so the one governance surface humans use is partial (A-12). Rebuild as a complete, uniformly-cursored feed generated from a single domain-event log rather than hand-unioned sidecars. |
| `public.ingest_document (fn, 22-fns-documents-recon)` | **REBUILD** | No storage-key grammar validation (only the table's firm-prefix CHECK, E-4), accepts a caller-supplied status that creates an invisible-forever document state (E-5), ocr_cache is write-once fill-forward so a thin/failed first OCR can never be corrected (E-6), and it persists client_match_conf verbatim with no gate (A-16/E-9). Rebuild as a deterministic server-side pipeline that persists bytes+row before any model involvement, validates keys, gates identity, and allows OCR re-derivation. |
| `public.match_bank_line (fn, 22-fns-documents-recon)` | **REBUILD** | Enforces zero structural parity — never compares the line's bank_account_id/statement/period/amount to the recon or the entry, matches into a COMPLETED recon, and no unmatch-before-rematch leaves ghost 'reconciled' entries (GAP1-1/GAP1-2 critical); it also trips the closed-period guard for cross-FY cheque clearing (F12-2). Rebuild the match model with FK-bound, CHECK-constrained membership and amount validation against the entry's net movement on the recon's bank account. |
| `public.reassign_document (fn, 22-fns-documents-recon)` | **REBUILD** | Moves the document row alone — journal entries citing it, parsed bank_statement_lines, and the storage_path all stay with the old client (GAP0-6). Rebuild as an atomic transfer that re-validates/refuses when citations/statement lines exist and transactionally re-keys the storage object. |
| `public.reassign_entry (fn, 20-fns-journal)` | **REBUILD** | Moves the GL entry cross-client via a bare two-table UPDATE while every dependent stays behind — subledger anchors, bank match/recon fields, FA/amortisation anchors, and document provenance — manufacturing the exact wrong-client state it exists to fix (GAP0-3 critical). Rebuild as an atomic domain-aware transfer that moves/re-derives every dependent or refuses when dependents exist (mirror reverse_entry's anchor guard). |
| `public.record_export / record_export_artifact (fns, 25-fns-ops)` | **REBUILD** | Reserve-the-row-before-bytes with caller-supplied sha256/byte_size/row_count that are NEVER reconciled against the stored object, no status column, and the same tool surface lets a model file model-authored bytes as a durable 'audited' artifact (H-1/H-7), attributed to the literal 'agent' not the requesting human (H-9). Rebuild so the receipt->artifact->bytes pipeline is the ONLY way bytes enter the tree, with a stored/verified status, sha reconciliation, and captured caller identity. |
| `public.record_kb_evidence (fn, 21-fns-kb)` | **REBUILD** | Alias-aware and correctly suppresses retired-rule re-proposal, but it upserts an unattributable aggregate counter (no entry/document provenance) and auto-files at >=3 off that inflatable tally (A-9); coding-time it is invoked as a SELECT-wrapped write (C-15). Rebuild to consume per-sighting provenance-carrying evidence events with DB idempotency, aggregates derived. |
| `public.record_opening_balances (fn, 25-fns-ops)` | **REBUILD** | Unguarded like the carry-forward orchestrator (no pre-existing-opening check, no role floor, writes no history rows, B-7) and coexists with seed_opening_carry_forward with only prose to prevent running both. Rebuild with a seeded-once registry guard, role floor, and attributed history. |
| `public.record_year_end_close (fn, 25-fns-ops)` | **REBUILD** | Its sweep/RE-roll/partner-mode/exhaustiveness logic is a crown jewel to PRESERVE, but the surrounding controls fail: no RBAC floor (a viewer can close a year, F12-8), it races every journal writer so entries commit into the just-locked FY unswept — a 'silent escape from RE' the code itself documents (GAP2-1 critical), g1 counts only needs_review/needs_decision so drafting entries strand (F12-6), and g3/g4 (uncoded docs, open recons) plus AR/AP/FA tie-outs gate nothing at the DB (GAP5-1). Rebuild as a role-floored, per-client-serialized period-state machine with a DB-owned readiness checklist evaluated inside the close transaction. |
| `public.reverse_year_end_close (fn, 25-fns-ops)` | **REBUILD** | No RBAC floor and no reversal-ordering guard, so an earlier FY can be reversed while a later FY's close is live — posting mirrors into a later locked period and orphaning its opening carry (GAP5-3 critical); it hard-DELETEs the signed-off financial-position snapshot and writes zero journal_entry_history receipts (GAP5-2). Rebuild to supersede-not-delete snapshots, write typed history/receipt rows, require a reason + role floor, and enforce reverse-in-reverse-order. |
| `public.seed_opening_carry_forward (fn, 25b-fns-opening)` | **REBUILD** | The atomic orchestrator has NO idempotency/already-seeded guard, so a re-run (a normal outcome of the non-durable interview dying and reloading) silently DOUBLES the entire opening position with the FP snapshot still reporting in_balance (B-1 critical), plus no role floor (B-2), no history/audit rows (B-7), no marker-control rejection in gl_lines and net-only tie-out (B-6), and no correction path (opening entries can't be reversed, B-8). Rebuild with a per-client seeded-once registry (mirror uq_fy_close_live), per-item idempotency keys, a supersede verb, and seed-time composition tie-out — PRESERVING the per-item + OBE-plug + baseline model. |
| `public.unmatch_bank_line (fn, 22-fns-documents-recon)` | **REBUILD** | Clears the entry's reconciliation fields unconditionally from the line's matched_entry_id, so after a cross-recon double-match, unmatching the stale line strips fields now owned by another recon (GAP1-2); same closed-period block as match (F12-2). Rebuild alongside the first-class match table. |
| `public.update_client_profile (fn, 24-fns-onboard)` | **REBUILD** | Hard-deletes+reinserts the contact registers (directors/domains/aliases delete-all, bank accounts delete-if-missing) violating the repo's own reverse-not-delete law, churning ids and raising raw 23503 FK errors when an in-use bank account is dropped (C-16/N10); it also lets fye_month change after closes exist, silently corrupting comparatives (F12-7). Rebuild with audited, reverse-not-delete per-row treatment and a fye-change guard. |
| `tax_computations (table, 19d)` | **REBUILD** | status CHECK is only ('draft','superseded') — no reviewed/finalized lifecycle, no typed confirmation/adjustment write-back so every professional judgment (WHT disallowance, fines split, doubtful debts, b/f relief) stays a display-only string, and there is no persistent brought-forward relief ledger (GAP5-4). Rebuild with a draft->reviewed->finalized state machine, typed per-item confirmation records that feed the recompute, and a YA-to-YA relief ledger. |
| `client_memory_notes (table, 17-tables-memory)` | **DROP** | Write-only dead layer: nothing structural ever reads a note (no skill, wake, read fn, or dashboard surface), the promised storage markdown index was never built, and it is the weakest-audited writer in the schema (C-1/C-2/C-5). Do not port; carry its three real needs (observation/profile facts -> typed KB profile fields with provenance; must_ask -> first-class open-question/interruption objects; rule_hint -> low-evidence KB-proposal state) into the rebuilt KB/task layer. |
| `public.record_memory_note (fn, 27-fns-memory)` | **DROP** | Sole writer of the dead client_memory_notes layer and the weakest-audited writer in the schema (no via_fn, unconstrained free-text actor, empty note accepted, kind checked only by the table CHECK, no consumer, C-1/C-5). Drop with the table; fold its needs into typed KB objects. |

---

## Agent runtime & tool registry (`agent/`)

| Asset | Verdict | Reason |
|---|:--:|---|
| `agent/src/http/auth.ts (JWT verify + caller identity)` | **PORT** | Proven: signature verification (HS256 shared-secret or JWKS) with issuer/audience checks, firm-claim extraction, route-level IDOR guard, and decode-only helpers explicitly marked not-auth — no confirmed defect is in this module itself. Caveats to carry: stale-JWT-claims authority (GAP1-4) is a claims/DB-architecture gap fixed in the target authorization kernel, and the firm-less bootstrap sentinel (firmId=userId, synthesized owner role) needs a first-class pre-firm principal in the rebuild. |
| `agent/src/http/cors.ts` | **PORT** | Exact-origin allowlist that echoes only listed origins, never '*', deny-by-default on empty list, Vary: Origin set before every writeHead. Small, correct, zero findings against it. |
| `agent/src/http/documents.ts (attachment validation)` | **PORT** | Clean defence-in-depth boundary: <=5 files, <=20MB decoded (computed without allocating), MIME allowlist, bytes never logged. Whole-batch first-failure rejection (E-10, low) is a UX polish item, not a defect that blocks porting. |
| `agent/src/http/hub.ts (SSE fan-out + books coalescer)` | **PORT** | Small, correct in-memory fan-out with ready-greet, trailing-window coalescing (drops-not-resets) and clean shutdown flush — acceptable as-is because in the target it remains a lossy UI-refresh nudge layer over durable DB state (all recovery lives in read surfaces, per Grt-12's durable-wake requirement). Multi-machine topology would need a pub/sub backplane, but that is a deployment change, not a defect in this module. |
| `agent/src/ocr/azureDocai.ts (Azure Document Intelligence adapter)` | **PORT** | Clean, proven REST adapter (prebuilt-layout v4.0, poll-with-deadline, TSV table reconstruction with span fill, key never reaches model or DB, honest not-configured degradation) with one confirmed medium defect that is trivially fixable in place: the 429-throttle branch skips the 120s poll-deadline check, making the loop unbounded under sustained throttling (E-8 ADJUSTED — apply the deadline check in the 429 branch). One required extension for the rebuild: capture and return boundingRegions (Azure already provides them) so per-field evidence regions can be persisted and surfaced (J-18). |
| `agent/src/runtime/openai/params.ts (zod schema builder)` | **PORT** | Proven pattern: strict zod schemas for ordinary tools (nullable-optional convention, int-cents money, enums) with fail-before-DB parse errors; no confirmed finding against it. Carry with one tightening: the non-strict raw-JSON escape hatch for jsonb-bearing tools should become fully typed payload schemas in the rebuilt catalog. |
| `agent/src/runtime/sse.ts (SSE wire framing)` | **PORT** | Trivial, proven relay of RuntimeEvent to the exact wire names the dashboard consumes, with keepalive; the event-name-duplicated-in-payload quirk is a known dashboard contract, not a defect. The dead artifact/directive frames ride along until the rebuilt event contract lands (Grt-10 is a runtime-side gap, not an sse.ts one). |
| `agent/src/tools/exportFormat.ts (CSV formatter)` | **PORT** | Proven byte-identical formatter: BigInt-safe integer-cents to 2dp, RFC-4180 escaping, formula-injection neutralization, BOM/CRLF discipline, boolean/NaN guards — read in full, no finding against it anywhere in the audit. |
| `agent/src/tools/pdfReport.ts (pdfkit renderer)` | **PORT** | Mechanical renderer of the ReportDoc model; the confirmed defects on the PDF path (float-recomputed seal H-3, MPERS/MFRS over-stamping GAP2-5) originate in reportModel/DB composition, not in the rendering code. Port the renderer; assurance wording and statement-pack completeness are fixed upstream. |
| `agent/src/tools/registry.ts core (+ registry/enums.ts, tools/types.ts): declarative ToolSpec registry, EXCLUDED_FNS, BANNED_TOOL_TOKENS, 1:1 name==fn, locked by registry.test.ts` | **PORT** | Proven pattern worth keeping: a declarative, test-locked, 1:1 fn-backed tool surface with structural name bans and an explicit exclusion list — the registry mechanism has no confirmed defect. Carry with one process fix: the surface drifted three ways from the DB fn catalog and doctrine (F3-12), so the rebuilt registry must be generated from / CI-verified against the DB's audited-fn catalog rather than hand-maintained (and the stale 'Google Document AI' comment in tools/types.ts goes). |
| `agent/src/tools/registry/documents.ts (14 document/recon tools)` | **PORT** | The wrappers port (idempotent ingest, sole storage_path writer, DB-computed recon tie-out, advisory hint store), with two named additions from confirmed findings: ingest_document needs key-grammar validation beyond the firm-prefix CHECK (E-4), and the surface is missing storage read/copy/move/delete plus a re-OCR-from-storage capability, which makes the doctrine-mandated post-assign Storage move structurally impossible (E-3 high, E-6) — those are new tools, not defects in the existing ones. |
| `agent/src/tools/registry/fixedAssets.ts (3 FA tools)` | **PORT** | The tools are right (cost read from the posted GL leg, DB-computed MPERS depreciation idempotent per period, DB-owned NBV/disposal) — the confirmed F3-4/5/6 highs are that NOTHING in the product wires them into a workflow: record_fixed_asset is doctrine-orphaned and nothing ever runs or gates depreciation. Port the tools; the rebuild must add the missing workflow/close-gate wiring around them. |
| `agent/src/tools/registry/journal.ts (10 journal tools)` | **PORT** | Thin, correct wrappers over the audited journal fns with sensible scope rules (match_client, the single 'present' cross-client reassign) and the estimated-risk write confined to finalize_coding. The confirmed defects live below them in the DB fns (draft_entry lacks an idempotency key GAP4-1; no maker-checker GAP3-7; no SST-leg constraint F3-9) — signatures will grow idempotency keys and approval envelopes, but the wrapper group ports. |
| `agent/src/tools/registry/kb.ts (9 KB tools)` | **PORT** | Wrappers are correct; the confirmed problems are ecosystem-level, not in these tools: memory notes have no structural consumer (C-1), the candidate tier is unreachable because every sanctioned writer mints 'confirmed' (C-11), and KB/books sync is LLM-mediated (A-3) — those are DB/workflow-design fixes. The rebuilt KB layer keeps these verbs and adds the structural learn-event emission the findings demand. |
| `agent/src/tools/registry/onboard.ts (6 onboarding tools)` | **PORT** | Wrappers are correct and the scope-rule exceptions (create_firm/onboard_client as 'none') are justified. The B-1 CRITICAL (seed_opening_carry_forward re-run silently doubles the opening position) is a defect of the DB fn in db/v2/25b, not of this wrapper — the fix (one-shot seeded-registry guard + idempotency keys) lands DB-side and the wrapper re-points unchanged, which is why PORT stands despite the linked critical. B-12's missing incremental carry-down tool is an addition to this group, not a defect in it. |
| `agent/src/tools/registry/ops.ts (9 ops tools)` | **PORT** | Correct wrappers (write-once artifact receipts under advisory lock, dedup'd proactive notifications, close/reverse pair). Carry with the H-9 caveat: the export receipt fns accept free-text actor and everything is attributed to literal 'agent' — the rebuilt fn signatures must capture the requesting human principal, a DB+injection change the wrappers inherit. |
| `agent/src/tools/xlsxReport.ts (exceljs renderer)` | **PORT** | Same posture as pdfReport: a mechanical ReportDoc renderer with no independent confirmed defect; the sst_return form_map omission (H-13) is a reportModel composition gap it inherits. |
| `agent/src/doctrine/loader.ts + frontmatter.ts (doctrine load + progressive disclosure)` | **REBUILD** | Right idea proven — load-once residency budget (~22KB resident, ~200KB skill bodies on demand via read_skill/read_reference, no filesystem tool) — but the implementation is ungoverned: verbatim filesystem doctrine baked into the image with no versioning/reload/governance, a skill-routing menu scraped by heuristic from prose first-lines (year-end-close lacks its 'When this fires' header so routing silently degrades, I-11), hardcoded wake-prefix routing, and the content it serves verbatim is confirmed drifted/contradictory (I-4..I-10: wrong OCR vendor resident every run, Hermes/Telegram residue, tools described that don't exist). Rebuilt loader must serve versioned, DB-governed doctrine with structural routing metadata and content-freshness checks, and skill-first execution should become a gate, not a convention (Ggr-5). |
| `agent/src/http/server.ts (HTTP shell + routes)` | **REBUILD** | Wrong: a POSTed chat run executes ONLY inside a subsequent SSE attach, so fire-and-forget callers (Documents tab) silently never run (D-1/E-1 critical); no per-scope run lease (GAP4-2), no cancel verb and Stop never aborts the 40-turn write-capable loop (GAP4-4 high); 150MB bodies fully buffered on a 1GB VM (E-7); bootstrap routes silently drop attachments (E-11). Rebuilt shell must enqueue durable runs executed independently of any SSE consumer, CAS a per-scope run lease with typed 409, expose a real cancel that aborts before the next tool call, and stream/bound request bodies — while keeping the proven verify-then-IDOR auth order, exact error taxonomy, and honest webhook 202. |
| `agent/src/http/sessions.ts (transcript store client)` | **REBUILD** | Wrong: the durable tool_calls/artifact columns are never written and cards are re-derived by fence-regex at read (Grt-3/Grt-9; multi-card turns structurally broken per D-4); assistant persistence is best-effort after settle and silently drops when the caller JWT expires mid-clarify (Grt-15); cross-turn memory is cold transcript replay so stale figures re-enter later runs (A-7, Grt-2). Rebuilt store must persist typed turns, tool calls, and artifacts natively at emit time under a run-owned credential (no fence-regex, no JWT-expiry transcript gaps) — while keeping the proven caller-JWT-rides-every-call and persist-user-turn-before-run disciplines. |
| `agent/src/http/wakeGate.ts (batching/rate governor)` | **REBUILD** | Wrong: the whole governor is unref'd in-memory timers — rate-cap excess is dropped, shutdown clearPending drops open windows without firing, and a restart loses every pending batch (Grt-12; A-2's five loss points include this stage). Rebuilt version must implement the same good ideas (per-(firm,condition,client) trailing-window batching, caps, dedup, per-firm rate limits) as durable DB queue state with at-least-once delivery and idempotent consumers instead of at-most-once process-local hints. |
| `agent/src/http/wakes.ts (webhook ingress + classifier)` | **REBUILD** | Wrong: the trigger taxonomy is a static condition-to-lane map with no risk/materiality/period keying and whole event families emit nothing (A-4 high); ingress is best-effort with no persistence, and the BOOKS_TABLES allowlist must move in lockstep with DB triggers but is untested against them (agent-map note 13). Rebuilt ingress must persist-then-ack into a durable wake/outbox queue keyed by a DB-owned event taxonomy — while keeping the proven per-firm derived-HMAC bearer with constant-time compare, strict shape validation, allowlisted record extras, and default-to-safest-lane routing. |
| `agent/src/jobs/bulkApproveRunner.ts (durable bulk-approve job)` | **REBUILD** | Right idea — the ONLY durable workflow in the runtime (DB job rows, resumable remaining-array, partial-failure honesty, caller-JWT per-id approval never service_role) — but confirmed wrong in three ways: restart re-drive counts an already-approved id as a failure and can settle a genuinely successful job FAILED (Grt-11), resume fires only when some user reconnects the notifications SSE so orphaned jobs sleep indefinitely (Grt-11), and a resumed job audits the ORIGINAL actor string while running on the resumer's JWT — audit rows name a person whose credential did not authorize the write (agent-map note 9). Rebuilt version: idempotent re-drive (already-approved = success), worker-lease-driven resume from durable job state independent of any UI connection, and attribution bound to the authorizing credential. |
| `agent/src/main.ts (composition root)` | **REBUILD** | The DI discipline is right and must carry (env read only at the root, injected lambdas, service-role confined to one storeBytes closure) — but the wiring it composes is the single-process world the audit condemned: shutdown clearPending drops pending wakes without firing (Grt-12), the settle path has a named JWT-expiry transcript gap (Grt-15), liveness-only /health with no dependency/readiness checks on a machine whose restarts are silent data-loss events (GAP1-7), and no tracing-policy, metering, or budget wiring exists (GAP1-8, GAP4-5). The rebuilt root wires the durable runtime, run-owned credentials, dependency-aware health/readiness, and fail-closed per-firm budgets. |
| `agent/src/runtime/inject.ts (fencing/sanitize + wake-note builder)` | **REBUILD** | The untrusted-data fencing discipline (guillemet fences, control-char strip, 4000 cap) is proven and must carry, but the wake-note content is the wrong context mechanism: hardcoded per-wake prose hints that leak hallucinated legacy table names (Ggr-3 ADJUSTED) and constitute the run's ENTIRE context — no client context pack exists (A-1 high, Ggr-6). Rebuilt injection must assemble governed, fresh per-client context packs (profile, COA, FY/period+lock status, tax regime, banks, open items, KB rules) instead of framing prose plus starter-SELECT hints. |
| `agent/src/runtime/openai/db.ts (rpc + agent_select query client)` | **REBUILD** | The ctx.rpc half (caller-JWT Supabase client, DB RAISE surfaced verbatim) is sound, but ctx.query is the transport for the confirmed critical mutation-bypass (SELECT-wrapped SECURITY DEFINER writes pass both the TS and DB lexical guards — Ggr-1/I-1/H-6) and its own comments misstate the DB backstop as READ ONLY. Rebuilt data access must drop the freeform-SQL channel entirely or run it under a role with no EXECUTE on volatile writers and default_transaction_read_only, keeping only the rpc path. |
| `agent/src/runtime/openai/events.ts (SDK stream event mapper)` | **REBUILD** | Wrong: maps only text deltas and tool breadcrumbs; never emits the artifact/directive kinds the union promises, leaving tool activity effectively invisible and cards traveling as fenced prose (Grt-10, D-6). The rebuilt mapper must emit native typed tool-call/tool-result/artifact events that are persisted and rendered, retiring the fenced-block regex — it is a thin SDK-specific adapter and will be rewritten with the new event contract anyway. |
| `agent/src/runtime/openai/runtime.ts (run loop, clarify, wakes, eviction)` | **REBUILD** | Wrong at the core: 100% process-local run state (every in-flight run, parked clarify, interruption dies on restart — Grt-1/6/7/8 high), runs execute only when an SSE consumer attaches (D-1/E-1 critical), unknown needsApproval interruptions silently auto-approved (fail-open, Ggr-11/GAP0-2), clarify answers uncorrelated to clarifyId or asking user (GAP4-3 high), clarify uploads REPLACE the parked attachment batch causing evidence aliasing (GAP5-6 high), wake deadline races but never cancels the loop so late writes land after 'failed' (agent-map note 6), and SDK platform tracing ships every firm's data to OpenAI's trace store by default (GAP1-8 high). Rebuilt runtime must be DB-durable (tasks/runs/checkpoints/interruptions/tool-calls), execute independently of stream attach, deny-by-default on unknown interruptions, correlate + persist clarify Q&A, use immutable sha-addressed attachment refs, propagate real cancellation into the SDK loop, and make tracing an explicit owner-approved policy defaulting off. Worth carrying: the deterministic je_needs_human short-circuit pattern (Grt-14) and the firm-binding of runIds. |
| `agent/src/runtime/openai/tools.ts (SDK tool adapter + clarify_tool)` | **REBUILD** | Right idea — clarify as a needsApproval interruption that structurally pauses the SDK run — but the loop's survival depends on a hard-coded [flow-control] directive injected into every clarify result to prompt-coerce the model into not ending the run (I-14, Grt-5), and the JSON-string back-compat parse papers over weak jsonb schemas. Rebuilt clarify must be a checkpointed, resumable protocol (persisted question/choices/answer, clarifyId-validated per GAP4-3) whose continuation is structural, not prompt-begged. |
| `agent/src/runtime/types.ts (AgentRuntime seam + RuntimeEvent union)` | **REBUILD** | Right idea — a neutral swap-seam runtime contract (ADR-031) that survived one adapter swap — but the contract itself encodes the broken model: startRun/streamRun coupling (execution requires a stream consumer), no cancel verb, no durable task identity, and artifact/directive event kinds that are dead code nothing ever emits (Grt-10). The rebuilt seam must be cut around durable tasks (enqueue/attach/cancel/resume), typed tool-result and artifact events that are actually produced, and checkpointed interruptions. |
| `agent/src/runtime/wakeCredential.ts (wake JWT minter)` | **REBUILD** | Right ideas proven (30-min TTL, RLS plane never service_role, deterministic synthetic sub, distinct aud, firm-id re-validation) but wrong trust anchor: signed with the project-wide legacy shared secret so holding it mints ANY firm's token, PostgREST does not enforce aud so the token is a full DB credential, and firm_role:'bookkeeper' gives wake runs human-decision fn eligibility fenced only by tool policy (agent-map notes 5; enables Ggr-7). Rebuilt credential must be a first-class agent principal: dedicated signing key or DB-issued short-lived token, a distinct DB role whose grants ARE the wake-lane allowlist, and enforced audience. |
| `agent/src/tools/analysisTool.ts (build_analysis_report)` | **REBUILD** | Right idea (the North-Star ad-hoc analytical reporting lane: structured sections, charts/KPIs, branded PDF, provenance page, honest 'not an audited statement' stamp) but the trust foundation is confirmed broken: model-authored SQL literals and prose are laundered as 'computed by the database' (H-4 high), money magnitude rests entirely on model money_columns labeling with 100x error modes (H-5), the section SQL rides the same lexical guard so an analysis request is a mutation vector (H-6), and the defining queries are unqueryable afterward — irreproducible artifacts (H-8). Rebuilt analysis must compose from typed/parameterized reads or a governed semantic layer (no freeform SQL), derive units from schema not model labels, label model commentary as commentary, and persist the full spec for reproducibility. |
| `agent/src/tools/buildTools.ts (enforcement core: policy matrix, scope gate, actor injection, upload_document, extract_document)` | **REBUILD** | The per-tool enforcement pipeline (policyAllows -> enforceScope -> injectActor anti-spoof; firm-scoped storage key grammar; banned-token structural guard) is the right shape and must carry as a pattern — but the policy matrix is confirmed unsafe: the [documents] wake lane is a write BLOCKLIST that leaves approve/reverse/depreciate/close reachable unsupervised (Ggr-7/I-2 high), query_books is pushed under EVERY policy including all wake lanes (carrier of the Ggr-1 critical), interactive allows ALL writes with no structural human authorization (GAP0-2 critical — UI-convention gate only), and upload_document lets the model file model-authored bytes as durable branded 'audited' artifacts (H-1). Rebuilt tool layer must enforce DB-owned per-mutation-class ALLOWLIST policies (default fail-to-draft), verify exact-payload human-approval envelopes before consequential writes, carry no freeform SQL tool on any lane, and reconcile artifact claims (sha256/size) against stored objects. |
| `agent/src/tools/exportTool.ts (build_export pipeline)` | **REBUILD** | The architecture is the right answer (DB -> formatter -> bytes server-side; a figure never passes through model text; structural receipt + write-once artifact + firm-prefix belt-and-braces) but its trust claims are confirmed false: balanced:true is hardcoded for every scope so unbalanced books export under a green 'In balance' chip (H-2 high), artifact rows are reserved before bytes and never reconciled so phantom artifacts persist (H-7), the full request spec is not persisted so filed artifacts are unreproducible (H-8), the MANDATORY whole-ledger client-confirm is prompt-only and the tool also runs on the [documents] wake lane where clarify does not exist (H-11), GL silently truncates at 200 pages (H-12), and format parity is false for sst_return (H-13). Rebuilt export must pass DB-owned assurance flags through verbatim, reconcile artifacts to a stored/verified status, persist full parameters + data-version for reproducibility, policy-gate whole-ledger egress, and fail or annotate loudly on truncation. |
| `agent/src/tools/registry/reads.ts (10 read tools)` | **REBUILD** | Wrong: the curated read surface is confirmed insufficient — whole workflow read families (AR/AP aging, statements, GL/FS, FA register, tie-outs, tax/SST, KB rules, period/lock status, client_overview) exist as granted DB fns but are not tools, forcing the model onto freeform SQL (Ggr-2 high); and reads are firm-wide with no scope inheritance or client-provenance tagging, so cross-client read contamination becomes a scope-valid wrong posting (GAP5-5 high). The rebuilt read layer must be complete (a curated/typed read for every workflow's data — treat 'workflow with no curated read path' as a build gate), client-scoped by default with named cross-client reads as deliberate typed operations, and rich enough to retire freeform SQL entirely. |
| `agent/src/tools/reportModel.ts (ReportDoc composition)` | **REBUILD** | Wrong: it recomputes the 'IN BALANCE' assurance seal in the agent with JS float arithmetic, ignoring the DB's own in_balance — a direct 'the agent never computes a figure' violation with a demonstrated false-negative on real cent combinations (H-3 CONFIRMED) — and its PDF/XLSX document model drops the SST-02 form_map rows the CSV carries while still counting them in rowCount (H-13). Rebuilt composition must pass every assurance/derived figure through from the DB verbatim (money stays bigint-cents until final render) and guarantee full content parity across formats. |
| `agent/src/tools/sqlGuard.ts + the query_books freeform-SQL tool` | **DROP** | Confirmed critical at both layers: the lexical verb filter cannot stop a SELECT-wrapped SECURITY DEFINER write (Ggr-1 CONFIRMED — the DB-side agent_select guard is equally lexical), it bypasses the scope gate and actor injection, is reachable under every policy including wakes, is doctrine-endorsed as 'read-only' in all 11 skills (I-1), and its own header misstates the Postgres backstop (H-6). A verb-based string filter is not a security boundary and no rebuilt version of THIS mechanism should exist: the replacement is a structurally read-only read layer (complete typed/curated read tools per Ggr-2, or a role with no EXECUTE on volatile writers + default_transaction_read_only) — i.e. a different component, not this one rebuilt. |

---

## Doctrine, skills & product docs (`belcort/`, `docs/`, `PRD.md`)

| Asset | Verdict | Reason |
|---|:--:|---|
| `belcort/AGENTS.md §10 — the document is truth / source_doc_sha256 (L61)` | **PORT** | Proven provenance law (raw scan = truth, OCR = claims with confidence, every entry traces to sha256). Carry as-is; the rebuild extends it with typed provenance for non-document entries but this core stands. |
| `belcort/AGENTS.md §11 — refuse nonexistent COA account (L63)` | **PORT** | Correct and structurally backed (journal_lines composite FK makes coding to a nonexistent account impossible; the doctrine adds the graceful offer-to-add path). Carry as-is. |
| `belcort/AGENTS.md §12 — know the current date (L65)` | **PORT** | Trivial, correct, and load-bearing for effective-dated SST rates and period math. Carry as-is. |
| `belcort/AGENTS.md §13 — recon/close preconditions (L67)` | **PORT** | Right preconditions (no close over unbalanced recon / pending entries / unassigned docs) and consistent with the owner-ratified g3/g4 visibility split. Carry; note the rebuild separately hardens the close DB-side (GAP5-1 is a DB gap, not a defect of this mandate). |
| `belcort/AGENTS.md §14 — integer cents + finalised set + KB tables (L69)` | **PORT** | Proven facts (bigint cents; status IN ('auto_draft','approved'); the three KB tables) that every skill keys on. Carry, regenerating table names against the rebuilt schema. |
| `belcort/AGENTS.md §16 — global injection defence (L86)` | **PORT** | Verified consistent and thorough across the doctrine (inert-DATA framing, wake handlers restate second-order injection with concrete examples, runtime prepends the dataRule to every wake note). Carry verbatim as a fine claim that held under adversarial verification. |
| `belcort/AGENTS.md §2 — database is the only state mutator (L31-33)` | **PORT** | Proven cardinal invariant, verified structurally for the fn-tools (every write via ctx.rpc to audited SECURITY DEFINER fns; agent never computes a figure; reverse-never-delete). Carry verbatim as the first law of the rebuilt doctrine. |
| `belcort/AGENTS.md §5 — never invent identifiers (L48)` | **PORT** | Clean, proven anti-hallucination law with the important record-user-given-values carve-out that fixed a real refusal bug. Carry as-is. |
| `belcort/AGENTS.md §8 — progressive disclosure / read_skill (L57)` | **PORT** | Proven mechanism with real code support (loader + read_skill/read_reference); it kept resident prompt small while serving full procedure bodies on demand. Carry the mandate; the rebuild may additionally make skill-before-write a structural gate (Ggr-5) but the doctrine itself is sound. |
| `belcort/AGENTS.md §9 — schema frozen to skills (L59)` | **PORT** | Trivially correct separation (no skill touches DDL) and structurally true (EXECUTE-only role). Carry as one line. |
| `belcort/SOUL.md` | **PORT** | Clean, residue-free persona (zero Hermes/Telegram/vendor/code_execution hits) whose voice rules (I do not invent; I reverse, never delete; honest about doubt; BM/Mandarin) all align with the invariants. Port verbatim per I-13. |
| `belcort/_shared/client-row.md` | **PORT** | Live, fail-closed chip contract (both IDs required, snapshot semantics, firm-thread-only taste rule). Port the content into the rebuilt artifact lane; note D-4 means multi-chip turns are broken today at the transport layer, which the rebuilt native lane fixes. |
| `belcort/_shared/myinvois-reference.md` | **PORT** | Verified regime reference: RM1M exemption threshold, the cancelled RM500k-1M band explicitly recorded (ADR-013 never-re-add), the 55-field model, tier mapping. Port per I-13; re-confirm code lists against the current SDK at build time as the file itself instructs. |
| `belcort/_shared/suggestion-chips.md` | **PORT** | One of the 4 genuinely live card channels; the fail-closed schema and the taste rules (zero chips usually right, never mid-clarify, never nag) are proven prompt-craft. Port the contract content; the transport itself should become a native typed artifact lane (I-14/D-4), which changes the envelope, not these rules. |
| `belcort/_shared/validators.md` | **PORT** | Precise, well-sourced validator contracts (SSM 4 shapes, LHDN TIN prefix table with the exact-literal EI rule, MSIC format) that are honest about being app-layer-only. Port as-is; the rebuild should additionally mirror the shapes as DB CHECKs (B-11), which this file already flags. |
| `belcort/doc-ingest/references/extracted-fields-schemas.md` | **PORT** | Per-doc-kind extracted-field schemas with no adverse finding; classified PORT-pending-review consistent with I.json's unverified note (not exhaustively cross-checked field-by-field against the MyInvois 55-field model). Verify against the 55-field model during the rebuild's contract pass. |
| `belcort/period-entries/SKILL.md` | **PORT** | Clean, small, correct: finalised-set reads with a same-scope control total, the honest approved/auto_draft split disclosure, and the export handoff that protects the export audit path. Port per I-13 with one light edit (drop the 'Plain send_message' phrase). |
| `belcort/rule-edit/SKILL.md` | **PORT** | Sound user-proactive rule-mutation flow over the audited SP-6.2 fns (create/confirm/retire, 0/1/2+ resolution, no auto-recode) — port per I-13 with light edits: specify the referenced-but-missing Steps 1-2 client picker, drop the 'Telegram phrases' trigger wording, and reword the query_books safety framing (the I-1 fix itself lives in the runtime, a one-line prose edit here). |
| `dashboard/docs/contracts/je-balance-tax-leg.md` | **PORT** | A model contract doc: the two-truth gate-vs-indicator split (≤5¢-tolerant approve gate vs strict Balanced indicator), rounding-at-approve/exact-at-commit reconciliation, and the owner-decided sub-cent policy are all verified against fns + executable tests, with honest documentation of the accepted Bulk-Approve gap. Port the invariant wholesale, re-anchoring file paths to the new build; the rebuild separately adds the DB-side taxable-revenue-requires-leg check (F3-9), which this contract never claimed to provide. |
| `docs/coa-reconciliation-findings.md` | **PORT** | LHDN/RMCD-sourced provenance for the 95-account seed: verified regime facts (GST repeal Act 805, NO input-tax-recoverable asset, numeric MyInvois codes), the 906-000 and installment-receivable fixes, and the must-preserve hardcoded-code list (980-100, 100-950, 150-000, coding defaults) the rebuild's seed must honor. Port as reference evidence; honor ADR-014's own TODO to re-verify against official sources before re-seeding. |
| `docs/design/01-foundations.md (ch.01)` | **PORT** | Proven floors that survived adversarial audit as LAW even where the code violates them: WCAG 2.2 AA specifics, the 60fps/10k-row transform-opacity-only perf floor, zero backdrop-filter on product surfaces (J-22 is a code violation, not a law defect), OKLCH 3-tier tokens, the reserved agent-violet, money-color rules. Carry as the rebuilt product's floor; the rebuild must add the CI enforcement the old build never wired (J-26). |
| `docs/design/02-interaction.md (ch.02)` | **PORT** | Sound, battle-tested interaction law: motion registers with reduced-motion MUSTs, the grid keyboard model with the binding suppression rule ('typing a in a reason field MUST never approve'), five mandatory screen states, the feedback ladder (toast never for errors), fail-loud reads. J findings fault the implementation (J-15 undiscoverable shortcuts, J-17 empty states), not this law. Carry as-is. |
| `docs/track-c-myinvois-inbound-contract.md` | **PORT** | The parser-hardening mandate is timeless and correct (no DTD/XXE/network, bounded expansion, never render XML as HTML, don't-trust-totals with 5-sen reconcile, decimal→cents half-up, self-billed→review) and the v2 Track-C build remains unbuilt so this stays its SoT. Port as a deferred contract with two edits: strip the DEAD v1 deploy-runbook section and re-verify all regime facts (thresholds, code lists) against official sources at build time — it is deferred reference, not current build authority. |
| `PRD.md` | **REBUILD** | Wrong: the §6 invariants core is proven and must carry (DB owns numbers, plan→approve, ≥0.95, sha256 provenance, cents, injection defence, SST output-only), but the PRD codifies the coding-loop product rather than the Accounting-OS lifecycle, asserts falsified status claims as law ('proven by the isolation rig', Track B 'built', gated beta live), and is silent on invariants the audit proved missing — maker-checker/SoD (GAP3-7), evidence regions (J-18 verifier: not required by this PRD), structural enforcement of its own gates (GAP0-2), payroll/inventory (GAP2-6/2-7 vs ADR-044 scope). Rebuilt: a lifecycle-scoped PRD carrying the surviving invariants verbatim, adding the missing ones, with zero as-built status claims inside law text. |
| `belcort/AGENTS.md §1 — firm scoping + data-access canon (L24-29)` | **REBUILD** | Wrong: presents query_books as 'a single read-only, RLS-scoped SELECT' — the confirmed-critical SDT-001/SEC-001 mutation-bypass path (I-1: agent_select's lexical guard passes SELECT-wrapped SECURITY DEFINER writers under every wake policy) — and overstates wake-note self-sufficiency (N18). Rebuilt: the read surface must be structurally read-only before doctrine may call it safe; probe/session rules re-grounded on the durable-runtime model. |
| `belcort/AGENTS.md §15 — audited-fn tool inventory + estimated_risk contract (L71-84)` | **REBUILD** | Wrong: the inventory claiming completeness has drifted BOTH directions (omits set_coa_account_type, add_bank_account, the 3 FA tools, seed_opening_carry_forward, journal_entry_detail and the 6 non-registry tools; advertises unmatch_bank_line which has no ToolSpec; carries record_opening_balances with no precedence over its successor — I-4/F3-12), and the agent-emitted estimated_risk materiality call is superseded by the target's DB-owned autonomy/materiality policy. Rebuilt: generate the resident tool list from the registry at load time (single source of truth) and move the risk/lane bind into DB-enforced policy. |
| `belcort/AGENTS.md §17-19 — dashboard affordances (suggestion / filter_journals / client_row) (L88-92)` | **REBUILD** | Wrong: §18 instructs emitting a filter_journals directive whose channel is dead at every station — following the doctrine renders raw JSON in the live chat bubble (D-3) — and the multi-card transport drops everything after the first fence (D-4). Rebuilt: doctrine may only advertise channels that exist; the agent→UI directive lane must be native, typed, and wider than one verb (J-8) before it re-enters doctrine. |
| `belcort/AGENTS.md §20 — Storage CREED (L94)` | **REBUILD** | Wrong: the firm-scoped key grammar + three-layer isolation design is proven and must carry, but the CREED over-claims ('all writers reject keys outside my firm + slug' — ingest_document does no key-grammar validation, E-4), the 7-yr retention clock is anchored at row-creation instead of period-end+filing (GAP3-4), and legal_hold has no place/release verb (GAP3-5). Rebuilt: same CREED with every writer actually validating key grammar, statutory retention anchored per ITA s.82/82A, and an audited legal-hold function. |
| `belcort/AGENTS.md §3 — client identification protocol (L35-43)` | **REBUILD** | Wrong: the protocol is right (≥0.95, never guess, one client at a time) but the text is garbled ('MUST well recognized client per client, CANNOT POLLUTE'), mixes UI affordances into law, and the gate is prompt-only — client_match_conf is model-self-reported and never enforced by any fn (A-16). Rebuilt: clean statement paired with a DB-owned identity gate (fns refuse/park below the floor, persist match basis). |
| `belcort/AGENTS.md §4 — clarify_tool loop mandate (L45-46)` | **REBUILD** | Wrong: it is shim-law compensating for a fragile process-local runtime (a plain reply ends the run — the observed onboarding-kill bug, B-4/I-14) and it references the nonexistent messaging.send_message. Rebuilt: multi-step interviews must be durable runtime-owned checkpointed flows; the doctrine then states the interaction contract once, without triple-stated loop discipline or ghost tools. |
| `belcort/AGENTS.md §6 — deterministic OCR engine (L50)` | **REBUILD** | Wrong: the law (deterministic OCR, never LLM vision, extraction is inert data) is right but it names the wrong vendor — 'Google Document AI' where the as-built engine is Azure Document Intelligence (I-5; three OCR identities exist across the tree). Rebuilt: vendor-agnostic law with the engine pinned once in the deploy doc, plus the evidence-region capture requirement (J-18). |
| `belcort/AGENTS.md §7 — wake-note authority (L52-55)` | **REBUILD** | Wrong: the four-lane taxonomy is prose-only — the [documents] code policy is a permissive blocklist under which approve/reverse/close remain reachable from a non-interactive wake (I-2/Ggr-7), and backend.md:92 contradicts the lane grammar outright (N2). Rebuilt: per-lane ALLOWLISTS enforced in the tool layer/DB, with doctrine text generated from (or verified against) the enforced policy. |
| `belcort/AGENTS.md — header + Project Objectives (L1-19)` | **REBUILD** | Wrong: scopes Clara to the document-coding loop ('cut coding labour 99%+', 'no automated import/post') — not the Accounting-OS super-UI North Star — and carries v1 residue ('disposable v1 firms run a frozen pre-migration copy'). Rebuilt version must state the full-lifecycle mandate (onboarding→close→tax→continuity) and drop all v1/plane references. |
| `belcort/_shared/filter-directive.md` | **REBUILD** | Wrong: it specifies a channel that is dead at all four stations — no agent emitter path, no SSE branch, no dispatch call, no grid listener — so doctrine-compliant emission renders raw JSON in the live bubble (D-3 HIGH). Rebuilt: a real, end-to-end, typed agent→UI directive lane with more than one verb (open view / focus entity / carry period per J-8), or do not carry the contract at all. |
| `belcort/_shared/ocr-cache-schema.md` | **REBUILD** | Wrong: documents a nested canonical envelope the producer never emits while the live agent writes a flat record — permanent 'aspirational' debt forcing every consumer to coalesce two shapes (I-12) — and its engine enum is v1-era pymupdf/marker-pdf (I-5's third OCR identity). Rebuilt: pick ONE shape the producer actually emits, name the real engine, and add per-field evidence regions (J-18) to the contract. |
| `belcort/bank-recon/SKILL.md` | **REBUILD** | DIVERGENCE from I-13 (light-PORT), justified: F3-3 is a confirmed CRITICAL and this skill IS the defective route — Step 4 sends customer receipts into plain coa-coding Dr/Cr so ar_receipts is never written or allocated, which porting largely-as-is would carry forward; plus the mid-sentence paste error (L94-96) and the match_bank_line signature drift. Rebuilt: keep the proven DB-owns-the-tie-out procedure and hint-learning verbatim, add the subledger allocation branch (allocate_ar_receipt/allocate_ap_payment) as a first-class step, fix the paste error and tool signatures. |
| `belcort/client-onboarding/SKILL.md` | **REBUILD** | Wrong: structurally defective as executable spec (no Phase C, a [[step:14/14]] marker in a 13-Q interview, superseded record_opening_balances text left in place — I-11) and it orchestrates a non-idempotent seed_opening_carry_forward whose re-run silently doubles the opening position (B-1 CRITICAL), a broken ingest kind step (F3-10), and an attachment-replacement hazard (GAP5-6). Rebuilt: preserve the domain gold (the BEE negative-equity carry-down worked example L280-294, TIN deferred-return, closing-net-never-movements rule) as tested fixtures inside a clean, contiguous, durable-flow body over idempotent fns. |
| `belcort/coa-coding/SKILL.md` | **REBUILD** | Wrong: the highest-throughput skill carries the stale 'Python stays fine for OCR / code_execution' line contradicting six siblings (I-8), instructs the KB-evidence write as a SELECT-wrapped fn call — doctrine-endorsed use of the agent_select mutation bypass (C-15) — and its six-AND auto lane plus SST-leg presence are prompt-only (A-5, F3-9). Rebuilt: extract the crown jewels (the SST hybrid ladder L174-262, CN/DN polarity both directions, must-ask flag classes, Dr-as-sum-of-credits) into a drift-free body whose gates are DB-enforced, with all writes as registered tools and subledger integration (F3-1/F3-2) added. |
| `belcort/doc-ingest/SKILL.md` | **REBUILD** | Wrong: teaches a Telegram inflow that does not exist, mandates persist-after-OCR and storage moves the runtime cannot perform (E-2/E-3 — attachments are process-local; no storage read/copy/move tool), and embeds a do-not-execute UBL block that is a prompt hazard (I-13). Rebuilt: the ordered ingest pipeline + dedup + client-ID gates carry, re-grounded on real capabilities (durable attachment persistence, a storage-move primitive), with the UBL target mapping moved to a non-loadable design doc. |
| `belcort/export/SKILL.md` | **REBUILD** | Wrong: a ghost Step 6 cited by Step 7, a bundled reference contradicting the skill on what is wired (I-9), a whole-ledger client-confirm that is prompt-only and absent on the [documents] wake lane (H-11), no role floor on the lane (H-10), and the skill's claim that the balanced chip is 'labelled non-authoritative at render' is false while build_export hardcodes balanced:true (H-2). Rebuilt: the scope/period/format resolution and ONE-build_export orchestration carry; the confirm gate, role floor, and balance truthfulness must become structural, and steps renumbered. |
| `belcort/export/references/presentation-mapping.md` | **REBUILD** | Wrong: still instructs flagging 906-000 as mis-typed OI (seed fixed to EP 2026-06-27 — generates false escalations, I-10) and sanctions model-side statement composition/netting (NBV = cost − acc.dep.) — the exact model-computes-money class the export lane exists to prevent (H-15). Rebuilt: a read-only presentation map scoped to legacy data where needed, with all figure composition delegated to the DB statement fns. |
| `belcort/firm-bootstrap/SKILL.md` | **REBUILD** | Wrong: the 11-Q interview content, validators, synonym table, and [[dryrun]] commit gate are proven, but the body is the worst Hermes carrier — a DEPLOY.md pointer that resolves to nothing, a 'persist to Hermes session memory' step the runtime cannot honor (I-6), stale /setup route names, and the whole interview rides one non-durable run where any prose sentence kills setup (B-4). Rebuilt: same interview content on a durable checkpointed flow, all v1 residue purged, transport references matching the real web/SSE surface. |
| `belcort/kb-evolve/SKILL.md` | **REBUILD** | Wrong: the three-door learning design is sound (never blind-writes rules, user-gated promotion, human-only self-reconcile fence — verified fine claims), but learning is LLM-mediated and never transactional (chat-mediated decisions can silently never learn, A-3), evidence has zero provenance and tolerated double-counting (A-9), and the body jumps Step 1→3a (I-11). Rebuilt: same learning contract with the learn event emitted transactionally by the DB fns themselves, evidence rows carrying entry/document provenance, and contiguous steps. |
| `belcort/review-queue/SKILL.md` | **REBUILD** | Wrong: three roles in one 531-line body (interactive queue + [documents] handler + [proactive] handler), a missing Step 2, a header still teaching Telegram inline_keyboard/send_message mechanics, and its [documents] lane is narrowed only by prose over a permissive code blocklist (I-2/Ggr-7). Rebuilt: split into three skills; the wake handlers' excellent read-back/provenance/injection discipline and the wake carve-out logic carry; the lane restriction moves into an enforced allowlist so the skill stops being the only safety layer. |
| `belcort/review-queue/references/clarify-tool-card-patterns.md` | **REBUILD** | Wrong: describes Telegram inline_keyboard card mechanics and 'plain send_message' for a runtime that has neither (I-7/N14). Rebuilt: card-pattern guidance written for the real surface (web chat + SSE + typed artifact cards). |
| `belcort/year-end-close/SKILL.md` | **REBUILD** | Wrong: mandates a 7-arg tax-provision close path the registered tool cannot call (only 4 params — Clara following her own doctrine dead-ends, F12-9), tells the agent the wired full pack is 'still unwired' (I-9), and has no '## When this fires' so its routing trigger renders empty (I-11). Rebuilt: preserve the rich close content (pre-close gate incl. TX-vs-410-081 provision logic, partnership per-partner close, sign-aware card, reverse-then-recluse flow) against a synchronized tool schema, and pair with the DB fixes (GAP5-3 reverse-order guard, F12-6 stranded drafting entries). |
| `docs/architecture/backend.md` | **REBUILD** | Wrong: an as-built map whose headline claims are falsified — 'durable wakes' vs at-most-once pg_net (N1), 'Every wake is speak-never-act' vs the MAY-act [documents] lane (N2), 'go-live-ready' (N4), SDK tracing shipping firm data to OpenAI unmentioned (GAP1-8) — and its binding §2A SST policy is frozen at the 2024-03-01 sector list with the 8% default hazard (GAP3-2). Rebuilt: a fresh pointer-map of the new architecture; carry §2A's policy SKELETON (output-only, effective-dated single rate authority, hybrid ladder, not_registered never auto-legs) only after official-source RMCD re-verification, and never restate runtime durability claims the deploy doc contradicts. |
| `docs/design/03-architecture.md (ch.03)` | **REBUILD** | Wrong for the target: its IA fixes Clara into 3 seats with a ≤1/3 rail, 'Clara never drives a write surface', and a scope model with no period/entity dimension — the audited result is a scope-follower, not the conversational super-UI (Ggr-8, J-8, J-13 URL-not-truth), and the thread model contradicts ADR-032 (N13). Rebuilt: an agent-led IA where Clara can drive navigation/views/period scope through a real directive lane, deep-link contract retained, with the thread/scope model decided once and stated once. |
| `docs/design/04-agentic.md (ch.04)` | **REBUILD** | Wrong: the right agentic patterns (7-state FSM, plan→approve tiers, static fail-closed gen-UI catalog, permanent attribution) sit under falsified as-built claims — 4 of 8 'live' catalog cards can never render (D-2), the durable-authorship badge is claimed closed while PROJECTLOG says the emit-seam never populates it (N9), rehydrated cards are stale-actionable (D-7), Edit/Dismiss are dead (D-12/J-1). Rebuilt: a native typed artifact lane with lifecycle state (supersede/expire cards), a catalog sized for the OS (multi-step plans, tables, doc-review per J-2), and honest as-built annotations kept out of the spec. |
| `docs/design/05-surfaces.md (ch.05)` | **REBUILD** | Wrong: the per-surface domain rules are valuable (SST-02 present-never-hidden/never-submits, period-close agent-only owner ruling, verbatim-DB-figures, reverse binds to the fn's reversible flag) but the chapter is saturated with falsified 'As built' annotations and a jobs lane spec'd against the decommissioned relay (N10 — dead dependency), plus grid/inbox specs the build never met (J-14, J-19). Rebuilt: re-spec every surface for the new IA, carrying the domain rules and owner rulings forward and stripping all build-status narrative. |
| `docs/design/HANDBOOK.md (governing law + refresh log)` | **REBUILD** | Wrong: the governing-law pillars (grid is truth/chat is intent, DB owns every number/UI renders, receipts, drift protocol) are proven and carry, but the 2026-07-08 refresh embeds an unreconciled 0.97 auto-post threshold against the 0.95 canon (N8) and a stale F0→S0 OCR claim (N7), and the ratified-at-slice-2b framing binds it to the old product shape. Rebuilt: restate the pillars for the workbench-led dual Agentic-Accounting-OS target, resolve the 0.95/0.97 owner decision explicitly, and keep status/history out of the law text. |
| `docs/reference/confidence-ladder.md` | **REBUILD** | Wrong: the ladder's design (deterministic persisted inputs, fail-closed gates, owner-tunable constants) is right, but its central claim — 'the ladder's verdict cannot be faked at write time' — is false in substance: only the status↔risk label bind is DB-enforced while confirmed-rule/≥0.95/empty-flags/no-tax-leg are model-asserted (A-5/Ggr-13), Gate 0's client_match_conf is model-self-reported and never gated (A-16), and the candidate tier it describes is unreachable in production (C-11). Rebuilt: each rung structurally enforced in the DB fns, the candidate-tier and 0.95-vs-0.97 owner decisions resolved, and the doc regenerated from the enforced policy (the target autonomy model supersedes its posting threshold). |
| `belcort/__init__.py` | **DROP** | 0-byte v1 Python-package residue, meaningless in the TS runtime (I-12). Do not carry. |
| `belcort/coa-coding/GL-Account-TaxNIndustriesCode.pdf + review-queue/ + rule-edit/ (3 byte-identical copies)` | **DROP** | The loader serves only .md references, so these 39KB PDFs (SHA256-identical in three dirs) can never reach the agent — pure dead weight (I-12). If the COA/tax-code mapping inside is needed, convert once to a single loadable .md; the binary copies do not carry. |
| `belcort/export/references/journals-csv-execution-pattern.md` | **DROP** | Kept 'only as a pointer' yet it now actively misinforms — tells the agent the wired full pack and analysis report are 'not yet wired' and to refuse them (I-9). Its residual correct content is already in export/SKILL.md; drop rather than maintain a second drifting copy. |
| `docs/design/06-implementation.md (ch.06)` | **DROP** | A build log for the superseded dashboard build: stack pins, slice checklists all marked ✅, a '2088 tests ported' migration narrative, and validation gates that were never enforced in CI (J-26). The greenfield build makes its own stack and gate decisions; keep the file only as frozen historical evidence, carry nothing forward as authority. |

---

## Dashboard — surfaces, libs, components (`dashboard/`)

| Asset | Verdict | Reason |
|---|:--:|---|
| `A11y/interaction machinery (lib/useGridKeys, components/a11y/useFocusTrap, DensityBoot + lib/uiPrefs, shadcn-lite ui/* primitives)` | **PORT** | useGridKeys faithfully implements the full grid keyboard model (roving tabindex, ranges, edit-mode suppression) and the density/prefs plumbing works — this is exactly the layer the map warns is at risk of being LOST in a rebuild, not added. Additive: the '?' shortcut sheet and per-surface density defaults. |
| `Auth perimeter pages (login, signup + pause gating, auth/confirm, forgot/set-password, welcome ceremony shell)` | **PORT** | Straightforward, working perimeter with the pending-invite/admission/bootstrap sequencing and a verified false-success guard on the firm commit; no findings against the pages themselves. The firm-bootstrap INTERVIEW inherits the onboarding-flow rebuild; the session/claims model beneath is the separate auth-plumbing rebuild. |
| `CommandCenterV2 + InboxLanes + digest (firm home: state strip, since-you-were-here, proactive lanes)` | **PORT** | Verified workable lane surface, not a bell: decision/review/proposal/FYI lanes off machine-readable intent, optimistic resolve with the projection rule, Answer/Discuss seeding the user's composer and never auto-sending. Two bounded fixes while porting: render the reason body visibly (it is sr-only today, J-19) and harden the render-time digest watermark idiom. |
| `CommandPalette (⌘K) + lib/commandIndex + lib/commandRouter` | **PORT** | Verified dispatch-not-conversation that never executes a write, degrades Ask to a seeded navigation, and structurally strips forbidden rows. One mandatory fix in place: restyle the pane opaque (it is the last live backdrop-filter surface, violating the product's own MUST floor — trivially a CSS change once the glass ladder is dropped). |
| `Design tokens v2 (app/tokens-v2.css) + contrast test harness (lib/__tests__/tokens-v2.test.ts)` | **PORT** | A ~273-prop OKLCH token system whose WCAG pairs are pinned by a real computed contrast harness in both themes ('change a value => change the test in the same commit') — verified and rare craft; port system and harness together. Add the missing CI enforcement (grep gates, bundle budget, axe) the handbook promised, since discipline alone already failed once. |
| `EntryDrawer + EntryReview + ApproveGate/RejectGate + EditEntrySheet` | **PORT** | Verified full-context, correctly tiered write gates: alertdialog approve with legs/balance-cue/SST/evidence/KB citation, reject-with-required-reason, typed-REVERSE destructive tier, reverse affordance bound to the fn-provided flag. Port and extend: render the before/after legs diff the DB already stores (J-3 — history blobs are deliberately opaque today), show a before/after preview in EditEntrySheet, and adopt the approval-envelope RPC params from the rebuilt approval contract. |
| `Presence/state machinery (lib/chat/useAgentState, lib/agentState FSM, PresenceDot, lib/connectivity offline latch)` | **PORT** | The idle/thinking/working/parked/clarify/offline FSM and the durable relay-offline latch feeding an honest 'your books are safe and editable' banner are verified working trust plumbing. Extend, don't rewrite: cross-scope needs-you count + jump list (J-28) and rendering the tool verb it already computes. |
| `Reports surfaces (TrialBalance, GeneralLedger + GlSegment/AccountPicker, FinancialStatements + FsStatement)` | **PORT** | Verified to keep the DB-owns-every-number law: TB renders the envelope's own totals and never sums client-side, FS interleaves DB subtotals with zero client arithmetic, presentation_complete=false is a first-class banner with an actionable COA drill, GL raises honestly when a window spans a close. Upstream post-close asymmetries (F12-15 prior-year TB) are DB-read rebuilds this surface then inherits. |
| `Session UI (SessionMenu, sessionsApi, keyset 'Show earlier' pagination, archived sessions read-only, reset guard handling)` | **PORT** | DB-backed per-scope sessions over audited fns with id-keyset pagination, inert lead pages, and read-only archives — verified solid persistence UX. The endpoints it calls move to the durable-runtime session model, but the UI mechanics carry as-is. |
| `Status/evidence/authorship primitives (StatusPill, EvidenceGlyph, AuthorshipBadge, MoneyCell)` | **PORT** | Durable machine-authorship (RULE/AUTO/MATCHED from durable fields, never mutable status) rendered identically across grid, drawer, and chat card, plus the shaped four-band evidence glyph and tabular-nums money cell — verified consistent trust vocabulary. Migrate EvidenceGlyph off inline styles into the token system and apply the band everywhere (the one raw-% deviation is in the triage lane). |
| `Streaming markdown + transcript a11y core (ClaraMarkdown, StreamdownView, useSentenceFlush, MessageScroller, ClaraTranscript render primitives)` | **PORT** | Verified genuinely Claude-class where it works: Streamdown with incomplete-markdown parsing, per-word fade honoring reduced motion, an accounting-safe sentence-flush screen-reader mirror that never splits decimals, stick-to-bottom scroll physics with pin-user-turn-to-top, role=log semantics. Re-enable code-block copy controls (controls={false} today) and add the missing per-message affordances while porting. |
| `Typed data-lib layer over rpcBooks (workbenchActions, firmBooks, trialBalance, generalLedger, financialStatements, journalEntryDetail, activity, recon, arap, fa, kbActions, documentActions, digest, clientOverview, periodClose reads, booksErrors vocabulary)` | **PORT** | Thin per-surface wrappers with a consistent `${status}: ${body}` error vocabulary and no client-side arithmetic — the pattern and most files carry over as-is even as the fn catalog changes. Fix the handful of `selectBooks<any[]>` untyped seams (contactsRegister, documents) while porting. |
| `components/activity/ActivityFeedLive.tsx + ReverseDialog + ReceiptItem` | **PORT** | The firm-wide receipts feed with the typed-REVERSE reverse-not-delete dialog is proven trust UX; feed incompleteness (6 of 11 audit families) is the DB-side unified-event-log rebuild it will inherit. Restyle ReceiptItem off legacy tokens/inline styles into the v2 system while porting. |
| `components/clara/ClaraComposer.tsx + lib/attachments.ts (three-ingress attachment pipeline, caps, drafts, Esc ladder)` | **PORT** | One cumulative validation pipeline behind picker/drag/paste with caps mirrored to the service and inline first-failure errors — verified solid at three layers. Port with named fixes: de-dupe pasted images by content not filename (D-10 silently drops the second screenshot), close the .xml empty-MIME bypass, add the fixed scope chip and @-mention popover (J-9/D-15), and extend drop targets thread-wide (D-13); the b64-in-JSON wire itself is replaced by the rebuilt ingest transport. |
| `components/clara/ClaraRail.tsx dock shell (drag/keyboard resize, collapse-to-edge, overlay <1280px, canvas variant)` | **PORT** | The persistent docked-rail + full-canvas dual mounting is proven interaction design with real a11y craft (SC 2.5.8 resize grab zone, per-scope clean remount preventing cross-client leakage). Add the missing expand affordance and shared thread state across seats (J-11) as additive work on the ported shell. |
| `components/documents/DocumentsTable.tsx grid + triage verbs (rows, keyboard model, reassign/mark-sample/request-coding, signed-URL view, docStatus)` | **PORT** | The listing/triage half is verified good: role=grid keyboard model, honest fetch-error-vs-empty states, audited triage verbs with the atomic in-DB wake, mint-on-click 600s signed URLs, sourced-entry deep links. Type the `documents: any[]` seam while porting; the upload half is the separate REBUILD above. |
| `components/files/FilesShelf.tsx + lib/filesShelf.ts + lib/exportArtifacts.ts` | **PORT** | Read-only artifact shelf with current-version-per-coordinate folding, superseded history visible, artifactId-only cards, and mint-on-click 10-minute signed downloads — verified honest and scoped. Upstream artifact-integrity fixes (sha verification, balanced honesty) are H-workstream agent/DB rebuilds, not this surface. |
| `components/kb/KbWorkbench.tsx (proposal lane + Confirmed/Candidate/Retired rulebook)` | **PORT** | The KB governance loop it fronts (evidence -> open proposal -> human promote/reject, admin-gated, no-downgrade) is verified wired end-to-end across DB, tools, and UI. Port it, and remove or populate the candidate lane per the owner's C-11 decision (the tier is currently unreachable in production, so the lane renders a population of zero). |
| `components/onboarding/members surfaces (MembersLive, InviteDialog, RolePicker, RemoveMemberAlert) + SettingsLive` | **PORT** | Functionally sound roster/invite/role management over audited fns; port with the v2 restyle (MembersLive still wears the legacy glass-set skin and inline styles). Note remove/demote must trigger the rebuilt revocation flow (GAP1-4) — a contract addition, not a UI rewrite. |
| `components/rail/ReadOnlyCards.tsx + ReviewSummaryCard.tsx (read-only card renderers)` | **PORT** | The renderers themselves are sound — per-card verbs wired only when a real route resolves, ReviewSummaryCard enforces the no-prior-evidence review-only carve-out with a single in-flight latch and live-id re-read. Their unreachability (4 of 8 catalog types have no emitter) is the emission pipeline's defect, fixed there, not here. |
| `components/records/CoaBrowser.tsx` | **PORT** | Modest, correct surface over audited fns (add account, toggle active, set type) with no findings against it. COA-change visibility gaps (no wake, absent from the activity feed) are DB-side event-taxonomy rebuilds, not this component. |
| `components/workbench/JournalsGrid.tsx + virtualization + row primitives (FilterBar, row selection funnel)` | **PORT** | Verified strong skeleton: TanStack virtualization with measured rows, honest aria-rowcount, a selection funnel that structurally prevents bulk-approving non-flagged rows, SSR-seeded react-query with keyset load-more and SSE nudges. Port with the named ergonomic additions (Dr\|Cr columns, DB-owned footer totals, multi-status + month windows, bulk reject/reassign, filtered-empty state, sweep banner — J-14/J-17) and settle the auto_draft queue-membership contradiction (J-16) as one deliberate semantic decision. |
| `dashboard/lib/artifacts.ts — parseArtifact card gate` | **PORT** | Verified genuine security asset: fail-closed discriminated parsing, bounded strings/rows/files/chips, safe-integer cents, strict slug charset, unknown types render nothing — keep the 'text-to-hydration, never text-to-code' LAW verbatim. Additive changes only: allow genuinely firm-scoped cards (today every card requires a positive clientId) and register the planned card types. |
| `dashboard/lib/booksApi.ts — dashboard-direct data plane (rpcBooks / selectBooks / signBooksUrl / actorFromToken)` | **PORT** | An 89-line proven seam: browser -> audited SECURITY DEFINER fns / RLS reads / short-TTL signed URLs on the session JWT, no relay, no service role — the architectural keeper of the dashboard. Carry the caveat that actorFromToken is a display hint only (the DB must constrain actor on EVERY fn — A-11/H-9 show the ops engines don't today) and that the upstream auth model changes (GAP1-4). |
| `lib/booksSync.ts (belcort:books-changed debounced refetch gate + BooksRouterRefresh)` | **PORT** | Verified freshness spine: one debounced gate that suppresses refetch while an editor is dirty and flushes once on close, bridged to RSC surfaces without resetting the persistent rail. Carries over regardless of what emits the nudge. |
| `lib/notifications.ts (notifications SSE subscriber + nav badge + mark/resolve fns)` | **PORT** | Thin fetch-based SSE subscriber over durable notification rows with audited mark/resolve — sound consumption pattern that carries over onto the rebuilt event backbone. Strip the NEXT_PUBLIC_RELAY_URL fallback (retired-plane residue) while porting — a one-line fix. |
| `shell-v2 (FirmShell, TopBarV2, NavRailV2, DesktopGate, persistent-rail architecture)` | **PORT** | The persistent firm shell surviving intra-firm navigation with the docked Clara rail and an a11y-correct desktop gate (inert tree below 1024px) is proven architecture. Additive work: cross-scope needs-you badging on the presence dot, a breadcrumb client switcher, and per-segment loading.tsx skeletons. |
| `Approval flow model (grid instant 'a'-approve vs drawer ApproveGate vs ungated bulk approve; approve RPC binding)` | **REBUILD** | Wrong: three different frictions for the product's most consequential verb (single-keystroke posts with zero context; drawer demands a full gate; bulk fires a job with no summary card), and no approval anywhere is bound to the content reviewed (no expected-revision/snapshot token — an approval after an intervening edit posts legs the approver never saw, GAP0-5). Rebuilt flow must be one coherent model: every approve carries an exact-payload/revision envelope the DB verifies, bulk approve gets the spec'd one-summary-card with per-row opt-out, and any one-tap fast path is a deliberate, stated policy for balanced strong-evidence rows only. |
| `Artifact fence/emission pipeline (lib/chat/artifactFence.ts, runEventChunks.ts fence suppression, hydrateTranscript single-artifact shape, dead native artifact/directive SSE vocabulary)` | **REBUILD** | Wrong: cards travel as fenced JSON in prose with two divergent extractor allowlists, only the FIRST fence per turn is lifted, suppression never resets (post-fence prose silently withheld live), half the catalog would flash raw JSON, and the native artifact/directive SSE frames are dead code on both ends. Rebuilt version must be a native typed artifact lane (one authoritative emit path per card type, N cards per turn, identical live and reload rendering, parity-tested across agent and dashboard) — retire fenced-text extraction entirely. |
| `Auth/session plumbing (middleware.ts + lib/supabase/*, requireVerifiedUser/loadMemberships per-page guards, notifySessionExpired singleton, authActions)` | **REBUILD** | Wrong: authority is stale-JWT-claims-only with no revocation (a removed/demoted member keeps full read+write power until token expiry — confirmed critical, and the claims hook fails open), per-route auth is unenforced convention with no central guard, and the session-expiry signal is a Node module-level singleton whose server-side use is a documented cross-user leak hazard. Rebuilt plumbing must implement the chosen revocation model (session-epoch claim or live-membership resolution + admin sign-out on remove/demote/suspend), an enforced route-guard layer, and per-request session-expiry signaling. Keep the session-JWT-to-PostgREST data-plane shape and the defense-in-depth RSC-guard habit. |
| `Background-work lane (components/jobs/JobLane.tsx, FirmJobsLane, lib/jobs.ts, lib/bulkApprove.ts)` | **REBUILD** | Wrong: the lane is unported relay-era UI (glass-set skin, legacy tokens, all inline styles) with no staleness rule — a dead runner shows a live pulsing bar forever against the handbook's own 10-minute heartbeat rule — and the bulk-approve job it fronts miscounts restart re-drives as failures and only resumes on user-triggered surfaces. Rebuilt lane must bind to the durable-runtime job model with heartbeat/'state unknown' staleness, v2 styling, idempotent resume semantics, and a pre-dispatch summary gate; keep the pause/resume/cancel verb surface and the RELAY fallback goes with the residue drop. |
| `Chat transport (lib/chat/makeHermesRunner.ts, hermesChatTransport.ts, relayStream.ts, useHermesChat.ts, hydrateTranscript.ts)` | **REBUILD** | Wrong: the stream is single-consumer and non-replaying with lossy-by-design drop recovery that even discards the final output the poll endpoint already returns, reconnectToStream is null (no reattach — a reload mid-run shows nothing while the reply lands in the DB), tool_calls are dropped at hydrate, and the whole stack is named and commented for the retired Hermes/relay contract. Rebuilt transport must speak a durable-run protocol: reattach/resume by run id, reconcile terminal polls to the authoritative output, carry typed tool/attachment/clarify parts, and shed all Hermes/relay vocabulary. Salvage the proven internals as reference: line-buffered dual-dialect SSE decode, heartbeats, early-close-on-done, keyset shape-at-read hydration. |
| `Client onboarding cinematic (ClientOnboardLive, useOnboardingRun, stageReducer, ClientOnboardCinematic, CarryDownReview, DryRunReview, AnswerRail, chapter machinery)` | **REBUILD** | Wrong: the whole interview rides one non-durable process-local run (reload = fresh interview; two production abort modes documented in the skill itself), 'done' maps to success unconditionally so a failed/never-reached carry-down or KB seed still shows an unqualified success card (only the client row is verified), and cross-clarify uploads clobber the held attachment batch. Rebuilt flow must be a durable, resumable, step-tracked workflow (identity -> carry-down -> KB with persisted completion state and guided resume), verify the full committed outcome per path before rendering success, and use immutable attachment references. Keep the verified gold: the [[carrydown]] plan->approve payload card, the dry-run review, the false-success guard on the identity commit, chip choice cards, and the cinematic presentation layer. |
| `Composer seeding buses + chat<->workbench bridge (lib/claraAsk.ts, lib/railChips.ts, lib/directives.ts, batch-bar context passing)` | **REBUILD** | Wrong: chips drop the entry id and serialize to lossy prose ('Re 14 Apr · Director loan' — Clara must guess which entry), multi-row selections can't be attached at all, and the agent->UI directive channel is dead at all four stations (no emitter, no SSE branch, no dispatcher caller, no grid listener) with exactly one verb defined. Rebuilt bridge must pass structured entity references (entry/document/filter descriptors) both ways, add 'Ask Clara about these N' to selections, and ship the directive union (open_view/focus_entity/apply_filter with period) end-to-end with the attributed one-click-undo chip pattern. Keep the never-auto-send seeding discipline. |
| `Document detail / verification surface (components/documents/DocumentExpand.tsx + EntryDrawer source-document section)` | **REBUILD** | Confirmed CRITICAL gap: document 'detail' is a guillemet-fenced raw OCR JSON dump plus a new-tab file link — the core professional verification loop (source page beside the proposed entry with extracted fields highlighted) does not exist, and evidence regions are not captured anywhere in the pipeline. Rebuilt version must be an in-drawer/side-by-side document viewer with per-field evidence-region overlays (requires pipeline capture + storage upstream), linked from every je_review card, drawer, and inbox review card. Keep the inert-data rendering discipline (OCR as data, never markup). |
| `components/books/ArApSubledger.tsx (AR/AP aging, statements, tie-outs)` | **REBUILD** | Wrong: an honest read surface over a dead register — the subledger freezes at the onboarding seed (no workflow ever records invoices/receipts/allocations, so aging/statements/credit-balances read a corpse) and the tie-out it renders reports false drift after the first year-end close; the North Star also needs AR/AP write flows (raise invoice, record/allocate settlement, write-off) that have no UI at all. Rebuilt surface must sit on the maintained (co-committed or derived) subledger, add the settlement/allocation work surfaces and an open-items workflow, and keep the never-hidden tie-out badge pattern on segment-aware arithmetic. |
| `components/books/FaRegister.tsx (fixed-asset register + depreciation schedule + disposal)` | **REBUILD** | Wrong: nothing in the product ever runs or gates depreciation (no run action here, no close guard, no completeness signal), acquisition never reaches the register from coding, and the control tie-out it displays is structurally false after the first close — the register UI is a viewer over a disconnected chain. Rebuilt surface must expose the depreciation run/completeness state (through-period_end signal wired to close readiness), register-from-coding linkage, and disposal tied to proceeds evidence, on segment-aware tie-outs. Keep the register + schedule presentation and the agent-routed-with-deterministic-fallback disposal pattern. |
| `components/books/PeriodClose.tsx + lib/periodClose.ts (FY-close readiness checklist)` | **REBUILD** | Wrong: the checklist FAILS OPEN — every failed read degrades to a non-blocking 'warn' and ready flips true, AR/AP/FA tie-outs are warn-only, and the whole checklist gates only a chat-intent button while the close fn itself is callable by any member with no floor; the readiness state is never persisted. Rebuilt version must render the DB-owned typed readiness checklist evaluated inside the close transaction (each condition hard-gating or requiring an attributed, persisted acknowledgment receipt), fail closed on read errors, and treat the UI as a projection of that contract rather than the only gate. The checklist composition itself (statements balance, pending entries, recons, docs, tie-outs, adjustments) is the right idea and materially better than the DB fn today. |
| `components/books/ReconWorkbench.tsx + ReconForms.tsx (bank reconciliation surface)` | **REBUILD** | Wrong at both layers: the surface sits on a match model with confirmed criticals (zero parity checks — any line matches any entry into any recon including completed ones; ghost matches on re-match; opening continuity ignored; every post-close recon structurally cannot tie), and the UI itself opens a recon on a hand-typed numeric document id with no picker on a control-critical flow. Rebuilt workbench must bind to the rebuilt first-class match model (structural line<->entry<->recon membership, chained periods, brought-forward outstanding items), replace the raw-id input with a statement-document picker, and surface match/unmatch as audited reversible verbs. Keep the two-plane statement<->ledger matching concept and DB-owned tie-out arithmetic display. |
| `components/calendar/ComplianceCalendarLive.tsx + lib/complianceCalendar.ts (client-side statutory deadline engine)` | **REBUILD** | Wrong home: 18.9KB of Malaysian statutory deadline rules live in the frontend as duplicated knowledge the DB/agent can't audit and that drifts against agent-side rules, and the ruleset omits whole families (payroll statutory deadlines the PRD claims). Rebuilt version must move the deadline ruleset to a DB/shared authority (effective-dated, citation-carrying) with the calendar as a pure renderer. Keep the verified honesty pattern: dates-not-money carve-out, per-deadline primary-source citations, 'assumed/verify' caveats, explicit do-not-guess deferrals. |
| `components/clara/ClaraThread.tsx orchestration (agent FSM wiring, clarify lifecycle, optimistic plan ledger, error banner)` | **REBUILD** | Wrong: the clarify lifecycle is process-local and lossy (never persisted, orphaned by reload, answered fire-and-forget, card stays clickable after a free-text answer) and plan/tool state is thread-local optimism instead of projection of DB truth; tool activity is computed then rendered nowhere. Rebuilt thread must render persisted typed interruptions (clarify Q&A as transcript rows that survive reload), project card state from DB-owned status, show in-thread tool chips and an honest pre-first-token status line, and reattach to parked runs on mount. Keep the verified pieces: guardedApprove's fail-closed active-scope write gate, expired-session mutation blocking, honest offline banner. |
| `components/documents/DocumentsTable.tsx upload path (drop-zone/picker -> defaultSend fire-and-forget chat POST)` | **REBUILD** | Confirmed CRITICAL: the self-declared ONLY ingest path POSTs a chat run that nothing ever executes (no SSE attach; runs only execute inside streamRun) — files are never uploaded/OCR'd/ingested while the UI toasts success and wedges session reset; not trivially fixable in place because the honest fix is the durable ingest architecture, not attaching a stream to a doctrine-dependent chat run. Rebuilt version must be a deterministic server-side ingest pipeline (bytes + row persisted BEFORE any model involvement), with real per-file progress/status and no success signal until the document row exists. |
| `components/documents/FirmDocumentsTriage.tsx (firm unassigned lane + Assign-all)` | **REBUILD** | Wrong: 'Assign all' mass-files the ENTIRE mixed multi-client lane to one client in one click, ignoring the per-row match-confidence evidence it renders, with no grouped preview, no outlier exclusion, no subset selection — and mis-files harden once coded; it also renders raw confidence percentages against the shaped-band law. Rebuilt lane must group by suggested client before committing, exclude low/other-client-confidence outliers by default with per-row opt-in, offer subset selection with a named-counts confirm step, and bucket confidence through the shaped evidence band. Keep the verified FIFO lane semantics, loop-of-audited-singles mechanics, and the zero-client escape hatch. |
| `components/onboarding/ClientOnboardFallback.tsx + lib/clientWizard.ts (deterministic onboarding fallback)` | **REBUILD** | Wrong: the sanctioned degradation path silently onboards a stripped client (no banks, directors, aliases, carry-down, or KB) with no warning and no follow-up surface anywhere — against the product's own 'cold start is NOT acceptable' bar — and it is a COMMON path because the live lane dies easily. Rebuilt fallback must end with an explicit 'still to capture' checklist deep-linking into a resumable completion flow and mark the client visibly onboarding-incomplete. Keep its real code validators (ironically stronger than the AI lane's prose validators). |
| `components/rail/PlanCard.tsx (je_review approval card) + planStateMap thread-local plan ledger` | **REBUILD** | Wrong: rehydrated cards are stale-actionable (an already-posted entry re-renders 'needs approval' with a live Approve — violates the catalog's own inertness law), Edit/'Not now' exist but are never wired so chat is Approve-only, plan state is per-mount React state that disagrees with the grid, and the card is inline-styled outside the verified token system. Rebuilt card must derive state from the DB-owned entry status (projected live, self-resolving across surfaces), ship Approve/Edit/Reject-with-reason/Not-now, bind Approve to an exact-payload approval envelope (GAP0-5), and live in the token system. Keep its verified anatomy: balance cue vs posting gate, always-visible SST legs, authorship badge, provenance chip, fail-closed scope gate. |
| `components/records/ContactsRegister.tsx + lib/contactsRegister.ts` | **REBUILD** | Wrong: 'contacts' is a display-side merge over free-text snapshot strings from four sources with a second, drift-prone copy of the DB normalizer, sitting on a counterparty model that has no entity at all (alias repoints split history and strand settlements) and an AR/AP population that cannot grow post-onboarding. Rebuilt register must front the first-class id-keyed counterparty record the C-workstream specifies (typed roles, alias children, FK'd rules/evidence/subledger/hints) with governed rename/merge — never a string-folding projection. The honest display-only posture (no client-side figures) carries over. |
| `components/reports/ReportsPicker.tsx + lib/reportsPicker.ts` | **REBUILD** | Wrong: the 'deterministic' picker routes through the model by client NAME (wrong-client egress is one look-alike name away), still exposes CSV-only despite all scopes rendering pdf/xlsx, and the tax 'Export forms' intent targets an export scope that does not exist. Rebuilt picker must pass structured parameters (client id, scope, fy, format) to a typed export capability — not a free-text chat intent — expose the real format set, and only offer scopes that exist; keep the fixed-menu, RBAC-aware, deterministic-intent shape. |
| `components/tax/SstReturn.tsx + lib/sstReturn.ts (SST-02 draft surface)` | **REBUILD** | Wrong: the period selector hard-codes six calendar-aligned bi-monthly periods, so an RMCD-assigned odd-cycle, DG-varied, or irregular-first-period registrant cannot even draft the right period (the DB fn accepts varied bounds but no surface can supply them), and the dual-registrant per-tax split must survive to the surface/export. Rebuilt surface must derive period candidates from modeled SST registration facts (assigned cycle, effective dates, variations), validate bounds, and render per-tax declarations for 'both' registrants. Keep the present-never-hidden posture, the honesty-layer rendering, and human-driven compute. |
| `components/tax/TaxComputation.tsx + lib/taxComp.ts (Form C/PT + CP204/CP500 worksheet)` | **REBUILD** | Wrong: the honesty layer is display-only — no per-item confirmation can be persisted as an audited decision (the only lever is recompute with two inputs), there is no reviewed/finalized lifecycle, and 'Export forms' is enabled while confirmations remain open and routes to a nonexistent export scope. Rebuilt surface must capture typed per-item confirmations (decision, actor, reason, evidence) that feed the recompute, drive a draft->reviewed->finalized state machine, surface the b/f relief ledger, and gate export on open confirmations. Keep the worksheet presentation and verbatim rendering of DB assumptions/confirmations. |
| `components/workbench/ClientWorkspace.tsx + WorkspaceHeader + workspaceTabs (17-tab workspace shell, deep-link targets)` | **REBUILD** | Wrong: the URL-as-source-of-truth contract is parse-only — tab switches and all journal filters (period/status/band/flagged) never write to the URL, so reload/share/back are broken on the daily driver, and the tab row fakes tablist semantics without the ARIA keyboard pattern. Rebuilt shell must mirror tab/filter/band state into the querystring (as ?fy= already does), implement or honestly drop the tabs pattern, and keep the verified one-shot deep-link focus machinery (?entry/?focus with flash guards) and the 5-group tab IA. |
| `lib/rbac.ts (client-side role/capability map)` | **REBUILD** | Wrong: the UI capability map is the ONLY floor for whole write families (onboarding/opening seeders, close family, exports have no DB role floor — a viewer can call them directly), and code comments falsely claim 'the fn re-enforces via RLS'; UI gates and DB floors have already diverged (documents.export at viewer vs doctrine's bookkeeper+ claim). Rebuilt version must be generated from or contract-tested against the DB-owned per-mutation authorization policy so the UI map can never diverge from (or substitute for) real enforcement. |
| `Glass system (app/glass.css, components/glass/GlassSurface.tsx, LiquidGlassEngine.tsx SDF refraction lens)` | **DROP** | The product went opaque-first as a MUST floor ('zero backdrop-filter in product surfaces — a lint rule, not a convention') yet the live-glass ladder still ships, is mounted at root, and powers the most-used overlay in violation of the floor. Do not carry it forward: restyle the two remaining consumers (⌘K palette, ReverseDialog overlay) opaque and add the backdrop-filter grep gate to CI so it cannot return. |
| `Hermes/relay-era residue (hermes* module naming across the live chat stack, NEXT_PUBLIC_RELAY_URL fallbacks in notifications.ts/bulkApprove.ts, stale relay/src/transcript.ts parity citations)` | **DROP** | The retired v1 framework's name saturates the live chat stack and two env fallbacks still point at the decommissioned relay — a verified cognitive-drift hazard whose stale citations already mislead (the parity contract's other half moved to the agent service). Purge wholesale in the rebuild: no Hermes naming, no relay env vars, no dead parity comments. |
| `Legacy shell v1 (components/shell/*) + app/tokens.css legacy palette + inert data-ui="v2" scope attributes` | **DROP** | Superseded by shell-v2 and tokens-v2 but still shipped as dead-ish weight, creating the two-shells/two-palettes coexistence the map flags; the legacy-token aliases keep legacy-skinned surfaces (Members/Jobs/Activity) alive instead of forcing their conversion. Do not port; finish the v2 conversion of the three legacy-skinned surfaces instead. |
| `lib/chatThreads.ts (retired active_run_id chat-threads dialect)` | **DROP** | Consumer-less dead code — imported only by its own test — from the retired thread-pointer dialect; its existence helped mask the D-1/E-1 never-executed-run defect by suggesting a reattach path that nothing uses. The rebuilt durable-run protocol supersedes it entirely. |

---

