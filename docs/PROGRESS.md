# Clara — Project Progress

## Current State

- Updated: 2026-09-11 (MYT), after the fourth `/implement` session: [#629](https://github.com/BELCORT-SDN-BHD/clara/issues/629) (shared Work questions), [#632](https://github.com/BELCORT-SDN-BHD/clara/issues/632) (attributable Activity feed) and [#634](https://github.com/BELCORT-SDN-BHD/clara/issues/634) (manual JV as the expert path: optional and late evidence, refusal-to-field mapping, journal links) implemented in three parallel worktree lanes, reviewed from three angles each, fixed, and merged in order as PRs #717 (main 8e6f8113), #723 (59c3e738) and #724 (b3d10441); the #629 follow-up PR #725 (the durable `work_accepted` card hosts the question panel after a reload) is the last web-only change of the train. `main` requires the `ci` check, so every push went branch → PR → green → fast-forward of the same SHA.
- Migrations on `main`: **0180** work questions, **0181** activity feed, **0182** journal work evidence (frontier 177 / 0182 once applied live). Runtime: `claraWork` repointed to the frozen `claraWork_v2` closure (v1 retained for in-flight runs; manifest additions-only, 7 entries). Web: B3/B4/B6 share one question record and one form; `/activity` is a real feed; the Journals compose ceremony is gone.
- Hosted (2026-09-11 13:05–13:20 MYT, main e8340fe2, the documented order): live DB preflight through an ephemeral probe machine (frontier 174 / 0179, zero in-flight tasks, zero pending clarifies); runtime image `refresh-e8340fe2` built and pushed first; machine 48ee715b763048 stopped (0182 recuts two live writer bodies); **0180 → 0181 → 0182** applied from this checkout with the DSN piped through `scripts/ops/dsn-pipe.mjs` (every prestate/tail notice clean; frontier **177 / 0182**); `clara-runtime` release **v80** (`/health` ok, `/ready` true with every lane ok; boot log shows bundles `clara-work/v1` afb2038a… and `clara-work/v2` c8fd7670…, `CONTROL listening`, `LEADER acquired`); `clara-web` version **8560d72f** (tag `refresh-e8340fe2`, six secret names, built in a clean Linux clone) promoted at 100% — `/login` 200, icons 200, `/activity` and the Journals `?entry=` route 307 to login, legacy `/needs-you` 307 intact. Rollback points: web `a0e6eb53`, runtime `refresh-72b9b85b` (v79), DB 0179. Probe machine destroyed. #629/#632/#634 **closed** with local, CI and hosted evidence in separate comments; the owner's signed-in walk of the three journeys is still to do.
- Local evidence per lane (Windows 11, Node 22.23.2, PG17 disposable clusters by deploy-onto-existing): #629 db suite 4208 / 4114 pass / 0 fail / 94 skip, runtime 2233 / 2226 / 1 (Windows-only EICAR) / 6, world + version-cutover + work-journal + work-question e2es all pass (the last: 7 legs incl. two workers on one expired lease, crash before resume, crash after commit before checkpoint, expiry → Retry → version 2, role loss), web unit 3003 / 3003, browser 186 passed / 1 timing cell (1/1 alone); #632 db suite 4173 / 4076 / 3 (migration-runner lock/timeout races under 3-lane load) / 94, web unit 3001 / 3002 (known flake), browser 183 / 3 untouched specs; #634 db suite 4193 / 4099 / 0 / 94, runtime 2222 / 2215 / 1 (EICAR) / 6, work-journal e2e leg 8 (evidence end to end), web unit 2995 / 2995, browser 182 / 1 (5/5 alone); after the rebases the full browser suite ran 208 passed / 0 failed / 7 skips on #634's branch and 208 / 1 (the #614 overlay-stack timing cell) on #725's. CI: `db-estate` and `db-live-gates` green on every PR (one re-run of #723's estate leg for a single load-sensitive readiness cell, recorded on #706).
- Reviews: native standards/spec at high effort plus Codex per branch; blockers found and fixed — a comma-stripping money parser (100x, #629), a real 401 leaving stale rows on screen (#632), a reversed entry's document stranded forever (#634); ~60 further findings fixed and pinned by tests; three deferred with reasons as issues #718–#722.

## Completed

- [x] Product decisions, research and prototype direction accepted; [formal spec #612](https://github.com/BELCORT-SDN-BHD/clara/issues/612) and 71 tickets published (#606, #614–#683).
- [x] [#606](https://github.com/BELCORT-SDN-BHD/clara/issues/606) classify after extraction; [#616](https://github.com/BELCORT-SDN-BHD/clara/issues/616) Node 22 host; [#617](https://github.com/BELCORT-SDN-BHD/clara/issues/617) readiness observability; [#618](https://github.com/BELCORT-SDN-BHD/clara/issues/618) operation-contract census (first session).
- [x] [#614](https://github.com/BELCORT-SDN-BHD/clara/issues/614) scoped shell: one navigation registry driving the Sidebar, scope switcher, Breadcrumb and ⌘K; `/work`, `/settings/*`, `/clients/:id/work|accounting`; legacy 307s (second session).
- [x] [#623](https://github.com/BELCORT-SDN-BHD/clara/issues/623) first persistent Clara successor (0178, `claraWork_v1` + `chatTurn_v18`, `/api/work/*`, C3 composer, B3 detail, B6 cards; released 0178 / runtime v79 / clara-web 2d94f5fb); [#626](https://github.com/BELCORT-SDN-BHD/clara/issues/626) personal settings (0179; clara-web a0e6eb53); [#627](https://github.com/BELCORT-SDN-BHD/clara/issues/627) tax boundary (third session).
- [x] [#629](https://github.com/BELCORT-SDN-BHD/clara/issues/629) shared Work questions — 0180 (`open_work_question` / `answer_work_question` first-answer gate / read doors / `expire_due_interruptions` / `work_question` inbox row), claimant+lease-conditioned delivery with real hook/run reconciliation, frozen `claraWork_v2` with its own error table, one record/one form on B3/B4/B6, `work-question-e2e.mjs` in CI; PR #717 (+ #725).
- [x] [#632](https://github.com/BELCORT-SDN-BHD/clara/issues/632) attributable Activity feed — 0181 `list_activity` / `get_activity_event` (invoker union of domain events, agent receipts, operation receipts; keyset cursor; no oracle), `/activity` with URL-state filters, event Sheet, eight distinct states, corrections linked both ways; CB-AE2E-018 discharged; PR #723.
- [x] [#634](https://github.com/BELCORT-SDN-BHD/clara/issues/634) manual JV — 0182 `entry_evidence_links` (one live binding per document firm-wide, released on reversal), recut `admit_journal_work` / `_record_journal_entry_core` with element-level source-ref validation, `attach_entry_evidence`, `list_entry_links`; composer evidence chooser, late-attachment dialog, journals filters/links/`?entry=`; compose ceremony retired; PR #724.

## In Progress

- The owner's signed-in hosted walk of the three journeys (a question asked and answered from B3/B4/the rail; the Activity feed with real events; a JV with evidence and a late attachment) — the agent never types passwords, so this rides the owner's browser session.

## Known Issues

GitHub owns these as plain issues (`needs-triage`, no milestone): #689 (p6-1 cells single-use on a reused DB — a second such cell recorded this session), #690, #691, #692, #693 (Windows-only EICAR), #698 (auth wall drops `?next=` query), #705–#709, #714 (CI cross-suite race), #715 (motion preference applies on next load), and from this session: #718 (the document-coding lane can still post on a document that holds a live evidence link), #719 (Journals `?entry=` past the 1000-row read cap; Documents/Reports have no item parameter, so Activity links land on the tab), #720 (chat-lane clarifies: no 14-day enforcer, HookNotFound still assumed delivery), #721 (a Work question's answer cannot complete an incomplete basis — owner decision), #722 (e2e mock lanes read a request stream once — unify body caching; document `pg_dump` + `CLARA_ALLOW_DESTRUCTIVE=1` for the runtime suite). Findings an existing ticket will fix stay as comments there: Work-run parts are live-stream-only and a route-started Work never reaches the rail (#641), the Activity `report` kind has no producer (#672), the growing list of load-sensitive cells incl. a CI occurrence (#706). Residuals noted on #629: `client_inactive` after the resume recheck is `recoverable:false`; the shadcn AI Questionnaire is not installable in this Base UI project (`components.json` has no registries), so the bounded stepper is built on the project's own Field. Signed-in hosted journeys need the owner's browser session; the agent never types passwords.

## Next Steps

1. Finish the hosted release (0180 → 0181 → 0182 with the runtime stopped for the two recut writer bodies → runtime image → web), smoke, deploy-lock the manifest, record hosted evidence on #629/#632/#634 and close them; the owner's signed-in walk of the three journeys.
2. Next frontier: #630 (cancel/revoke/lock ordering — builds on `claraWork_v2`), #631 (egress authority), #641 (B-style Work list/detail — needs #629/#630), #633 (needs #620/#624); still-open frontier #615, #619, #620, #621, #622, #624, #635 (needs #625/#628).
3. Owner: rule on #721 (basis-completing answers) and triage #718–#722 with #689–#693, #705–#709, #714, #715.
4. [Final acceptance #683](https://github.com/BELCORT-SDN-BHD/clara/issues/683) owns integrated delivery, blueprint synchronization and explicit closure of #612/#597.
