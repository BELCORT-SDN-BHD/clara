# Clara — Project Progress

## Current State

- Updated: 2026-09-12 (MYT), after the fifth `/implement` session: [#727](https://github.com/BELCORT-SDN-BHD/clara/issues/727) (PR #730, main cebe9706), [#728](https://github.com/BELCORT-SDN-BHD/clara/issues/728) (PR #731, main 5f5add5e, migration 0183) and [#630](https://github.com/BELCORT-SDN-BHD/clara/issues/630) (PR #747, migration 0184; two runtime routes; `work-cancel-e2e` in CI) implemented in three parallel worktree lanes and merged in that order as fast-forwards of their reviewed SHAs (`main` requires the `ci` check). Review method: per lane a multi-agent workflow (six/seven finder lenses, then two refuters and one reproducer per finding, all with explicit model pins) plus Codex `gpt-5.6-sol` until its quota ran out mid-session; every fix round got its own adversarial delta review until a round found no blocker — #727 ×2, #728 ×5 (two of them one-item rounds), #630 ×4 plus the rebase round. Blockers found and fixed this way, none visible to a unit suite or CI: the Activity feed's per-row SECURITY DEFINER call (4.8 s per page at 21 days of sweeps) → one set read per call; the plpgsql generic-plan flip (a shared optional-parameter statement, then the firm-id parameter, then the door itself: 130 ms → 32–48 s from the sixth call per pooled connection) → `plan_cache_mode = force_custom_plan` pinned at function level on all five Activity/spoken-for functions with a composite partial index and a LATERAL fence bounding the cost by the kept set (1.6–1.9 ms at 1k/6k/30k receipts); a queued Work cancelled through the older task-level door stranded forever; the lock order not being global (two older doors locking task-first, a 40P01 reaching humans as a 500); "Stop reply" invisible during the streamed reply; the replay arm converging a superseded run's Work (a regression a fix round introduced); a membership share lock that inverted the estate's firms-then-membership order (found by the new two-session pin). Hosted: 0183 applied 2026-09-12 (frontier 178 / 0183) and clara-web `ebe812bb` (tag refresh-5f5add5e) at 100% with the runtime unchanged at v80; the 0184 step is `<HOSTED-PENDING>` (frontier 179 / 0184, runtime v81 `refresh-<sha8>`, clara-web `<version>`); rollback points before it: DB 0183, runtime `refresh-e8340fe2` (v80), clara-web `ebe812bb`.
- Migrations on `main`: **0183** Activity sweep attribution + `list_spoken_for_documents` (#728), **0184** Work cancel ordering — the `accounting_work` row lock as the single boundary, `initiated_by`/`initiator` split, `stopping` written, `cancel_accounting_work`/`take_over_accounting_work`, `settle_work_run`'s cancel translation, `_converge_work_terminal`, the recut `cancel_agent_task`/`open_work_question`/`mint_wake_credential`/`list_entry_links`/`list_activity`/`get_activity_event` (#630) — frontier 179 / 0184 once applied live. Runtime: `workRoutes.ts` gains `POST /api/work/:workId/cancel` (200) and `/take-over` (202) with typed 4xx mapping incl. 40P01/40001 → 409 `transient`; the reconciler's §A2 stranded-pair sweep; `storage.mjs` opens a local object eagerly (a missing test-storage file is a typed `storage_error`, not a supervisor-killing stream error); `claraWork.v1/v2` stay deploy-locked and classify CLR13 `work_cancelled` under `state_changed` (#737 — a `claraWork_v3` owns it, with #631). Web: `TurnProgress` reads its clock through a ref (one timer per turn); `ActivityActorLine` ("Clara"/"Clara (system)"), the spoken-for pickers, two focus returns; B3 Cancel Work / Take responsibility, B6 cards polling to terminal, B7 Stop reply's explicit `idle → pending → stopped | failed{denied|finished|refused|transport}` machine with a bounded "lost sight" state.
- Local evidence (Windows 11, Node 22.23.2, PG17 disposable clusters built deploy-onto-existing): #727 web unit 3104/3104, full browser suite (on the #728 branch that carries it) 215 passed / 0 failed / 7 skipped, browser timer census 536 → 1; #728 db suite fresh chain 4276 / 4182 pass / 0 fail / 94 skip, web unit 3147/3147 (serialised), full browser suite on the final tree 215 / 0 / 7, the plan-cache pins proven by ten-call series before/after and red-on-old-definition runs; #630 db suite fresh chain (main@0183 → 0184) 4319 / 4225 / 0 / 94 with the boundary pin wc.28 red without the row lock, runtime 2271 / 2264 / 1 (Windows-only EICAR) / 6, the standalone e2es version-cutover, work-journal, work-cancel (9 legs) pass and work-question passes standalone (its leg 3 timed out once in the battery — measured cause on #745, #629's file), web unit 3213/3213, full browser suite 225 / 0 / 7. CI: every job green on every PR; `db-live-gates` (the four-e2e battery) green on #747.
- Reviews and fix counts: #727 — native three-axis (0 blockers, 6 should-fix), Codex (none), delta 7; #728 — 19 (six lenses) + Codex 3, then 13 → 6 → 11 → 4; #630 — 40 (seven lenses, 5 blockers) + Codex 12, then 30 → 12 → 8 → final. Every confirmed item fixed and pinned; the rejected ones are recorded in the session's workflow journals.

## Completed

- [x] Product decisions, research and prototype direction accepted; [formal spec #612](https://github.com/BELCORT-SDN-BHD/clara/issues/612) and 71 tickets published (#606, #614–#683).
- [x] [#606](https://github.com/BELCORT-SDN-BHD/clara/issues/606) classify after extraction; [#616](https://github.com/BELCORT-SDN-BHD/clara/issues/616) Node 22 host; [#617](https://github.com/BELCORT-SDN-BHD/clara/issues/617) readiness observability; [#618](https://github.com/BELCORT-SDN-BHD/clara/issues/618) operation-contract census (first session).
- [x] [#614](https://github.com/BELCORT-SDN-BHD/clara/issues/614) scoped shell: one navigation registry driving the Sidebar, scope switcher, Breadcrumb and ⌘K; `/work`, `/settings/*`, `/clients/:id/work|accounting`; legacy 307s (second session).
- [x] [#623](https://github.com/BELCORT-SDN-BHD/clara/issues/623) first persistent Clara successor (0178, `claraWork_v1` + `chatTurn_v18`, `/api/work/*`, C3 composer, B3 detail, B6 cards; released 0178 / runtime v79 / clara-web 2d94f5fb); [#626](https://github.com/BELCORT-SDN-BHD/clara/issues/626) personal settings (0179; clara-web a0e6eb53); [#627](https://github.com/BELCORT-SDN-BHD/clara/issues/627) tax boundary (third session).
- [x] [#629](https://github.com/BELCORT-SDN-BHD/clara/issues/629) shared Work questions — 0180 (`open_work_question` / `answer_work_question` first-answer gate / read doors / `expire_due_interruptions` / `work_question` inbox row), claimant+lease-conditioned delivery with real hook/run reconciliation, frozen `claraWork_v2` with its own error table, one record/one form on B3/B4/B6, `work-question-e2e.mjs` in CI; PR #717 (+ #725).
- [x] [#632](https://github.com/BELCORT-SDN-BHD/clara/issues/632) attributable Activity feed — 0181 `list_activity` / `get_activity_event` (invoker union of domain events, agent receipts, operation receipts; keyset cursor; no oracle), `/activity` with URL-state filters, event Sheet, eight distinct states, corrections linked both ways; CB-AE2E-018 discharged; PR #723.
- [x] [#634](https://github.com/BELCORT-SDN-BHD/clara/issues/634) manual JV — 0182 `entry_evidence_links` (one live binding per document firm-wide, released on reversal), recut `admit_journal_work` / `_record_journal_entry_core` with element-level source-ref validation, `attach_entry_evidence`, `list_entry_links`; composer evidence chooser, late-attachment dialog, journals filters/links/`?entry=`; compose ceremony retired; PR #724.
- [x] [#727](https://github.com/BELCORT-SDN-BHD/clara/issues/727) live turn clock — `TurnProgress` reads its clock through a ref instead of a default parameter, so one turn arms exactly one timer; pinned by `thread-live-stream-stability.test.tsx` (unit) and `chat-parity-walk.spec.ts`'s browser timer census (536 armed pre-fix → 1 post-fix); Work detail hydration pinned by `journal-work-walk.spec.ts`'s console/pageerror collector (empty storage and prior-visit-state faces). The hosted React #418 stays unreproduced locally and is its own issue (#732). PR #730 (main cebe9706).
- [x] [#728](https://github.com/BELCORT-SDN-BHD/clara/issues/728) Activity sweep attribution — 0183 excludes a zero-effect `sweep.run_completed` heartbeat (`drafted_count + posted_count = 0`) and labels a kept one "Clara (system)" under kind `agent` through one set read per page (`clara._sweep_events_with_effect()` / `_sweep_event_has_effect(uuid)`, driven by the kept set through a composite partial index and a LATERAL fence); all five Activity/spoken-for functions pin `plan_cache_mode = force_custom_plan` (the generic-plan flip house rule); `clara.list_spoken_for_documents(p_client)` (advisory, firm-wide claims, claimant named) disables claimed documents in both evidence pickers; `ActivityActorLine`; two focus returns on Attach evidence; Activity Sheet Back returns focus to the row or the feed heading. PR #731 (main 5f5add5e). Hosted: frontier 178 / 0183, clara-web `ebe812bb`; the live feed's first page under the owner's claims went from 20+ sweep rows of 25 to 0.
- [x] [#630](https://github.com/BELCORT-SDN-BHD/clara/issues/630) Work cancel ordering — 0184 makes the `clara.accounting_work` row lock the global ordering boundary (`accounting_work → agent_tasks → agent_interruptions`) shared by `cancel_accounting_work`, `take_over_accounting_work`, `settle_work_run`, `claim_work_run`, `_record_journal_entry_core` and the recut `cancel_agent_task` (0133) / `open_work_question` (0180); the posting core holds `firms` (key share) then the responsible human's membership (share) so revocation and posting serialise at commit; `stopping` written; `_converge_work_terminal` (task-scoped receipt law; a stale settle answers `stale_task`); `accounting_work.initiator` split into immutable `initiated_by` and movable `initiator`; `take_over_accounting_work` (story 29: `basis_confirmation_required`, `not_takeable`, `work.taken_over` under kind `work`, `get_activity_event` projects `initiated_by`/`responsible`); `cancel_agent_task` answers `changed`/`transition`; 40P01/40001 typed as transient 409; `POST /api/work/:workId/cancel` / `/take-over`; B3 Cancel Work dialog in one stable slot, Take responsibility with its basis rendered, B6 cards polling to terminal, B7 Stop reply's explicit machine (a refused stop re-attaches the read; the clock retires with the turn; a lost run is labelled after three misses); `work-cancel-e2e` (9 legs) in CI. PR #747.

## In Progress

- Nothing in flight. The 0184 hosted step (runtime STOPPED during the apply, runtime image, web) and the owner's signed-in walk of #728 and #630 are `<HOSTED-PENDING>`.

## Known Issues

GitHub owns these as plain issues (`needs-triage`, no milestone): #689 (p6-1 cells single-use on a reused DB), #690, #691, #692, #693 (Windows-only EICAR), #698 (auth wall drops `?next=` query), #705–#709, #714 (CI cross-suite race), #715 (motion preference applies on next load), #718 (the document-coding lane can still post on a document that holds a live evidence link), #719 (Journals `?entry=` past the 1000-row read cap; Documents/Reports have no item parameter), #720 (chat-lane clarifies: no 14-day enforcer, HookNotFound still assumed delivery), #721 (a Work question's answer cannot complete an incomplete basis — owner decision), #722 (e2e mock lanes read a request stream once), and from this session: #732 (hosted React #418 on Work detail — unreproduced locally, pin standing), #733 (`SIDEBAR_COOKIE_NAME` resolves `undefined` in the Server layout — the collapse cookie is inert), #734 (a render fault in a transcript subscriber is reported as "Could not send that message"), #735 (browser cells with no headroom under host load), #736 (the Clara rail overlay intercepts clicks at 320 px), #737 (the frozen roster classifies `work_cancelled` as `state_changed` — `claraWork_v3`, with #631), #738 (`work_status` part carries no client id), #739 (clock-sensitive unit cells under load), #740 (`journal-work-mock`'s control leg drains the body before deciding ownership), #741 (`i18n/request.ts` sets no `timeZone`), #742 (document-pipeline timeline events carry no actor — 7 of the live feed's first 25 rows), #743 (`SidebarMenuSkeleton` `Math.random()` in a lazy initialiser), #744 (the kept-sweep set is read whole per page and grows with kept history), #745 (`work-question-e2e` leg 3's 90 s budget under load — measured causes recorded), #746 (`links = []` default-parameter identity in two journals components). Findings an existing ticket will fix stay as comments there: #641, #672, #706. Signed-in hosted journeys need the owner's browser session; the agent never types passwords.

## Next Steps

1. Hosted step 2: 0184 with the runtime machine stopped (it recuts live writer bodies), runtime image v81, clara-web; then the owner's signed-in walk covering #728 (Activity attribution, actor line, spoken-for pickers, the two focus returns) and #630 (Cancel Work → stopping → cancelled/completed per receipt, Stop reply leaving the Work running, Take responsibility with basis confirmation).
2. Next frontier: [#631](https://github.com/BELCORT-SDN-BHD/clara/issues/631) (egress authority — also where `claraWork_v3` folds in the cancel/authority roster entries, #737, and the `work_status` client id, #738), [#641](https://github.com/BELCORT-SDN-BHD/clara/issues/641) (B-style Work list/detail — now has the stopping/cancelled vocabulary), #633 (needs #620/#624); still-open frontier #615, #619–#622, #624, #635 (needs #625/#628). Parallel lanes on separate machines are viable given pre-assigned migration numbers, appended namespaces and disjoint SQL-function ownership (this session's `list_activity` collision was the counter-example).
3. Owner: rule on #721 (basis-completing answers) and on #728's "effect" definition (refused/skipped sweeps excluded); triage #732–#746 with #718–#722, #689–#693, #705–#709, #714, #715.
4. [Final acceptance #683](https://github.com/BELCORT-SDN-BHD/clara/issues/683) owns integrated delivery, blueprint synchronization and explicit closure of #612/#597.
