# Clara — Project Progress

Minimal session state. Everything durable lives on GitHub (issues, PRs, the #597 wayfinder map) and in git (`docs/plan/active/refresh-wave-2026-09-18/` for the current wave — DECISIONS, reports, drafts, the triage audit, the release runbook with its as-run results; `docs/plan/active/refresh-wave-2026-09-15/` and `refresh-wave-2026-09-14/` for the two released before it). Clock in: read this, confirm the repo is clean (`git status`, `git log -1`, local `main` = `origin/main`), then ask the owner which ticket.

## Current State

- `main`: wave 2026-09-18 **LANDED 2026-09-19 21:59 MYT as PR #954 → `ede1df83`** (`integration/wave-2026-09-18`; ten tickets #635 #636 #642 #651 #655 #656 #657 #658 #659 #660; migrations **0225–0233**, 228 files; successor cut `chatTurn_v21` / `claraWork_v5` carrying riders #847 and #882 (a); `ci` all green incl. db-estate and db-live-gates). The as-run docs PR after it adds only the deploy-locked `frozen-workflows.json`, the runbook's § RESULTS, the triage audit and this file, so `main`'s tip differs from the deployed image's `git_sha` by that one docs/manifest commit.
- Hosted: **RELEASED 2026-09-19 14:43–14:47Z** (`refresh-wave-2026-09-18/RELEASE-RUNBOOK-0225-0233.md` § RESULTS) — DB frontier **228 / `0233_firm_commercial_settings`** (`9 new migration(s) applied · 228 total`, one run, 42 s; 75 of 75 prestate pins matched on hosted before the window); `clara-runtime` image **`refresh-ede1df83`** (`sha256:2c6e7b4acf3b5126365ebb892b47f5c7af6d2e06d76dc1fa49975a3b5f0898ff`, machine `48ee715b763048`, boot `bodies=55`, `stranded bodies n=0`, pins v21 / v5 / `clientOnboarding_v5`, five `clara-work` banners); `clara-web` **`c550d944-352c-46de-b6f8-cc44b1e52733`** (tag `refresh-ede1df83`, 100% since 14:46:50Z). Runtime outage 2 min 49 s. Restore point: full dump `packages/db/backups/…2026-09-19T14-37-11-128Z.sql` (216,868,575 bytes).
- Rollback points: runtime previous `refresh-a296765c` = `sha256:b67163f3…` (rollback-preflight ALLOWED at 14:48:51Z — a snapshot that degrades with the first `chatTurn_v21` / `claraWork_v5` run); web one command: `pnpm --dir apps/web exec wrangler versions deploy 095073c9-ecba-47ca-99de-ab3c8781deb2@100% --yes`. **The asymmetry stands:** the runtime and web roll back by one command, the database does not — nothing in this wave drafts a below-frontier migration, so below 0225 the only route is the dump (no Storage bytes, no managed Auth config, no engine state).
- Wave evidence before the release is LOCAL (`refresh-wave-2026-09-18/reports/`): two from-scratch chains 0001→0233 on virgin clusters; db estate 5151 / 5056 pass / 0 fail / 95 expected skips; web unit 4628 / 4626; World legs incl. the two-build cutover green; `frozen-workflows.json` additions only (296 → 312), now all 312 deploy-locked.
- Owner rulings this wave: DECISIONS §0 (D1–D19), §6.1 brief rulings, §6.2 nineteen ratifications, §6.3–§6.4 integration rulings. Owner go for the release, verbatim, 2026-09-19 ~22:05 MYT: "如果合适and 一切都通过的话, 就直接去吧,不用问我." The owner also chose to close the small riders before the next mainline wave (option B).

## In Progress

- **Wave 2026-09-18 — DONE** (session `2d0e3faa…`): landed (PR #954), delivery comments posted, hosted release executed and recorded, hosted-evidence comments posted and the ten tickets closed.
- **Owed to the owner's browser:** the signed-in read-only walk of the ten released surfaces (runbook step 8's table). The agent session had no operator browser session and does not sign in.
- **Triage audit 2026-09-19 — CONFIRMED BY THE OWNER AND APPLIED 2026-09-20** (`refresh-wave-2026-09-18/reports/TRIAGE-AUDIT-2026-09-19.md`; the record of what was posted is `TRIAGE-APPLIED-2026-09-20.md` beside it): 139 items (85 open non-mainline tickets + this wave's 54 unfiled follow-up drafts) checked against the mainline's delivered, planned and closed work; 0 superseded, 0 in conflict with a ruling. Applied: **48 new issues #956–#1003** (23 `ready-for-agent` with an agent brief, 25 `ready-for-human` naming one decision with a 大白话 paragraph), 25 comments on existing tickets (six narrowed to their remaining scope, #665 and #663 told what this wave left them, the rest coordination notes), #847 closed as completed, #693's auto-close given its record and `wontfix`. Every text was written fresh, re-verified against `main` by a second worker and posted by script.
- **One triage question still with the owner:** who stops a trade invoice recorded twice under one document number. Recommendation: a new follow-up that adds a warn-and-confirm probe at #655's admission door (the earlier step), not at #662. Held, not filed, until the owner answers. Ruled 2026-09-19: `lib/firm/timeline.ts` / `clara.list_firm_timeline` retire (#998) and #843 builds on `list_activity`.
- **One release question with the owner** (runbook step 10.6): does anything outside this repository call `clara.get_llm_usage_summary` with a non-admin identity? 0233 put an admin floor on it; the database cannot see a PostgREST caller.

## Known Issues

- Windows-host reds that are not code: no `pg_dump` on PATH (four runtime files), #693 Defender/EICAR (also the intake spool `EPERM` rename), host-contention flakes in the sign-in helpers under many concurrent lanes, and `next build` panicking `0xc0000142` in the PostCSS loader under heavy contention (a retry clears it, #869). CI (Linux) does not see them.
- Released, and correct rather than broken: hosted has **zero published cash account sets**, so every client home shows the cash arm's empty state until a human publishes one (D19.a: a human, never the code, decides which accounts are cash).
- Carried on hosted, not a release defect: one `clara.document_processing_tasks` row reads `statement_facts · running` with no live workflow run behind it (present before the window).
- `scripts/ops/dsn-pipe.mjs` still pins the CA with its Windows spelling, so the backup runs under a small WSL wrapper; the `--child-os wsl` follow-up is unfiled. The release reads script needed two as-run fixes (savepoint per read; a wrong column name), both recorded in the runbook.
- Blueprint drift recorded for the #683 sync, not edited: the 2026-09-15 list (DECISIONS §3.2 there) plus this wave's (`refresh-wave-2026-09-18/reports/WAVE-DIGEST.md` §2); `ARCHITECTURE.md`'s pins line now lags at v20 / v4 (serving: v21 / v5 / v5).
- The wave-3 two-build blocker (ARCHITECTURE §10: a Work parked on `claraWork_v1`/`_v2` cannot post once 0195 is live) is untouched.

## Next Steps

1. The board at 2026-09-20: 157 open, `ready-for-agent` 131, `ready-for-human` 25, `needs-triage` 0. Run one grill for the 25 `ready-for-human` follow-ups (#958 #960 #961 #962 #965 #968 #970 #971 #974 #975 #977 #979 #982 #983 #984 #988 #990 #991 #992 #995 #997 #999 #1001 #1002 #1003) plus the held duplicate-number question, then the lighter riders wave (option B) before the next mainline wave. The next successor cut is `chatTurn_v22` / `claraWork_v6`, owed jointly by #915, the conversation halves of #931 #933 #937 #941 #942 and #656's `read_opening_source`: one assignee, one ceremony. Dependency chains from the idea round: #927→#928→#929, #930→#931, #932→#933, #934→#935, #936→#937 and #936→#942, #919→#939/#940→#941. From #926: #945→#946→#947 and #945→#948→#949 (#944 is independent).
2. The #597 map: the mainline's remaining children (#645, #661–#683), ending with **#682** (verify the real accounting journey and runtime recovery on the released combination; #847's trace census read 0 rows at this release) → **#683** (final acceptance + blueprint sync, then close #612 and #597).
3. Housekeeping: the ten `clara-wt/<n>` worktrees and `clara-wt/int`, and the WSL clusters `rig635…rig660`, `rigint`, `rigint3…6`, `rigrt`, `rigreh`, `rigreh2`, can go once the owner has no further use for them.
4. When the lawyer-reviewed Terms/DPA wording arrives: publish v2 through `clara.publish_legal_document` (BELCORT owner) or a seed migration.
5. When the admission beta should stop taking firms: `set_admission_capacity` (BELCORT owner).
