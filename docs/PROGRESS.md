# Clara — Project Progress

## Current State

- Updated: 2026-09-12 (MYT), after the fifth `/implement` session: [#727](https://github.com/BELCORT-SDN-BHD/clara/issues/727) (merged PR #730, main cebe9706), [#728](https://github.com/BELCORT-SDN-BHD/clara/issues/728) (PR #731, migration 0183) and [#630](https://github.com/BELCORT-SDN-BHD/clara/issues/630) (PR #<PR-630>, migration 0184; runtime routes; `work-cancel-e2e` in CI) implemented in three worktree lanes; reviewed by multi-agent workflows (six/seven finder lenses plus three verifiers per finding) and Codex until its quota ran out; fix rounds: #727 ×2, #728 ×3, #630 ×3. Blockers found and fixed: the Activity feed's per-row SECURITY DEFINER call turned into a one-read-per-call set, `clara._sweep_events_with_effect()`/`_sweep_event_has_effect(uuid)` (#728); a single plpgsql statement with an `x is null or …` predicate shared by two callers whose generic-plan flip cost 18 ms → 13.5–16.4 s past the fifth call on one session, fixed by splitting into two fixed-shape functions rather than pinning a plan mode (#728); a stranded queued Work with no run left to abort (#630); the two older doors `cancel_agent_task` (0133) and `open_work_question` (0180) locking the task row before the Work row — the inverse of the lane's global order, reachable as a raw 40P01/500 (#630); Stop reply's state machine not distinguishing an in-flight intent from an outcome (#630); the replay arm converging a *superseded* run's late settle instead of refusing it (#630). Hosted: `<HOSTED-PENDING>` (frontier 0184, runtime v81, clara-web `<HOSTED-PENDING>`), rollback points DB 0182, runtime `refresh-e8340fe2` (v80), clara-web `8560d72f`.
- Migrations on `main`: **0183** Activity sweep attribution + spoken-for documents (#728), **0184** Work cancel ordering, `initiated_by`/`initiator` split, `stopping` status (#630) — frontier 177 / 0184 once applied live. Runtime: `workRoutes.ts` gains `/api/work/:workId/cancel` (200) and `/api/work/:workId/take-over` (202) with typed 400/409 mapping including transient 40P01/40001; `claraWork.v1/v2.errors.ts` remain deploy-locked and classify the new CLR13 `work_cancelled` under their existing `state_changed` default (`#737` — teaching it its own pair needs a `claraWork_v3`, tracked as part of #631). Web: `TurnProgress` reads its clock through a ref (one timer per turn); B3 Cancel Work / Take responsibility, B7 Stop reply's explicit `idle → pending → stopped|failed` machine, `ActivityActorLine`'s "Clara (system)" label and two focus-return fixes on Attach evidence.
- Local evidence per lane: `<EVIDENCE-PENDING>` — the orchestrator has not yet pasted DB/runtime/web/browser suite counts into `<scratch>/EVIDENCE.md` for this session; fill in from that file once it exists.
- Reviews: native standards/spec at high effort plus Codex per branch (Codex's quota ran out partway through the round and the remaining passes were native-only — note which lane if this matters for #630's fix-round ×3). Findings and fix counts per lane: `<EVIDENCE-PENDING>`.

## Completed

- [x] Product decisions, research and prototype direction accepted; [formal spec #612](https://github.com/BELCORT-SDN-BHD/clara/issues/612) and 71 tickets published (#606, #614–#683).
- [x] [#606](https://github.com/BELCORT-SDN-BHD/clara/issues/606) classify after extraction; [#616](https://github.com/BELCORT-SDN-BHD/clara/issues/616) Node 22 host; [#617](https://github.com/BELCORT-SDN-BHD/clara/issues/617) readiness observability; [#618](https://github.com/BELCORT-SDN-BHD/clara/issues/618) operation-contract census (first session).
- [x] [#614](https://github.com/BELCORT-SDN-BHD/clara/issues/614) scoped shell: one navigation registry driving the Sidebar, scope switcher, Breadcrumb and ⌘K; `/work`, `/settings/*`, `/clients/:id/work|accounting`; legacy 307s (second session).
- [x] [#623](https://github.com/BELCORT-SDN-BHD/clara/issues/623) first persistent Clara successor (0178, `claraWork_v1` + `chatTurn_v18`, `/api/work/*`, C3 composer, B3 detail, B6 cards; released 0178 / runtime v79 / clara-web 2d94f5fb); [#626](https://github.com/BELCORT-SDN-BHD/clara/issues/626) personal settings (0179; clara-web a0e6eb53); [#627](https://github.com/BELCORT-SDN-BHD/clara/issues/627) tax boundary (third session).
- [x] [#629](https://github.com/BELCORT-SDN-BHD/clara/issues/629) shared Work questions — 0180 (`open_work_question` / `answer_work_question` first-answer gate / read doors / `expire_due_interruptions` / `work_question` inbox row), claimant+lease-conditioned delivery with real hook/run reconciliation, frozen `claraWork_v2` with its own error table, one record/one form on B3/B4/B6, `work-question-e2e.mjs` in CI; PR #717 (+ #725).
- [x] [#632](https://github.com/BELCORT-SDN-BHD/clara/issues/632) attributable Activity feed — 0181 `list_activity` / `get_activity_event` (invoker union of domain events, agent receipts, operation receipts; keyset cursor; no oracle), `/activity` with URL-state filters, event Sheet, eight distinct states, corrections linked both ways; CB-AE2E-018 discharged; PR #723.
- [x] [#634](https://github.com/BELCORT-SDN-BHD/clara/issues/634) manual JV — 0182 `entry_evidence_links` (one live binding per document firm-wide, released on reversal), recut `admit_journal_work` / `_record_journal_entry_core` with element-level source-ref validation, `attach_entry_evidence`, `list_entry_links`; composer evidence chooser, late-attachment dialog, journals filters/links/`?entry=`; compose ceremony retired; PR #724.
- [x] [#727](https://github.com/BELCORT-SDN-BHD/clara/issues/727) live turn clock — `TurnProgress` reads its clock through a ref instead of a default parameter, so one turn arms exactly one timer; pinned by `thread-live-stream-stability.test.tsx` (unit) and `chat-parity-walk.spec.ts`'s browser timer census (536 armed pre-fix → ≤1 post-fix); Work detail route hydration pinned by `journal-work-walk.spec.ts`'s console/pageerror collector (empty storage and prior-visit-state faces). Hosted's three `React #418` hydration errors not reproduced locally; ticket stays open for that one item. PR #730 (main cebe9706).
- [x] [#728](https://github.com/BELCORT-SDN-BHD/clara/issues/728) Activity sweep attribution — 0183 `clara._sweep_events_with_effect()` / `_sweep_event_has_effect(uuid)` exclude a zero-effect `sweep.run_completed` heartbeat from the feed and label a kept one "Clara (system)" (two fixed-shape SECURITY DEFINER functions, not a shared optional-parameter statement, after the generic-plan flip cost 18 ms → 13.5–16.4 s past the fifth call); `clara.list_spoken_for_documents(p_client)` advisory bookkeeper+ door disables an already-claimed document in the evidence pickers and names its claimant; `ActivityActorLine` shared row/Sheet component; two focus-return fixes on Attach evidence (success → "What was recorded", refusal → the chooser); Activity Sheet Back honours page-click vs deep-link origin. PR #731.
- [x] [#630](https://github.com/BELCORT-SDN-BHD/clara/issues/630) Work cancel ordering — 0184 makes the `clara.accounting_work` row lock the lane's global ordering boundary (`accounting_work → agent_tasks → agent_interruptions`), shared by `cancel_accounting_work`, `take_over_accounting_work`, `settle_work_run`, `claim_work_run`, `_record_journal_entry_core` and the recut `cancel_agent_task` (0133) / `open_work_question` (0180); new `stopping` status; `accounting_work.initiator` split into immutable `initiated_by` and movable `initiator`; `clara.take_over_accounting_work` (story 29, CLR10 `basis_confirmation_required`, CLR13 `not_takeable`); `settle_work_run`'s cancel translation (`error.superseded`); 40P01/40001 typed as transient 409; `POST /api/work/:workId/cancel` (200) / `/take-over` (202); B3 Cancel Work dialog, Take responsibility, B7 Stop reply's `idle → pending → stopped|failed` machine (distinct `agent_tasks` rows from Cancel Work, no cascade); `work-cancel-e2e` in CI. PR #<PR-630>.

## In Progress

- Nothing in flight. Hosted release (frontier 0184, runtime v81, clara-web) and the owner's signed-in walk of #728 and #630 are `<HOSTED-PENDING>`.

## Known Issues

GitHub owns these as plain issues (`needs-triage`, no milestone): #689 (p6-1 cells single-use on a reused DB — a second such cell recorded this session), #690, #691, #692, #693 (Windows-only EICAR), #698 (auth wall drops `?next=` query), #705–#709, #714 (CI cross-suite race), #715 (motion preference applies on next load), and from this session: #718 (the document-coding lane can still post on a document that holds a live evidence link), #719 (Journals `?entry=` past the 1000-row read cap; Documents/Reports have no item parameter, so Activity links land on the tab), #720 (chat-lane clarifies: no 14-day enforcer, HookNotFound still assumed delivery), #721 (a Work question's answer cannot complete an incomplete basis — owner decision), #722 (e2e mock lanes read a request stream once — unify body caching; document `pg_dump` + `CLARA_ALLOW_DESTRUCTIVE=1` for the runtime suite), #727 (hosted: the Clara rail's live clarify stream dies with React #185 — maximum update depth — and the answer control vanishes until a reload; React #418 hydration errors on the Work detail page), #728 (hosted: the autodraft sweep heartbeat floods Activity as unattributed "Documents" rows; "00000000on behalf of" actor spacing; focus after a successful late attach and after a history Back out of the event Sheet; the evidence picker offers documents already spoken for). Findings an existing ticket will fix stay as comments there: Work-run parts are live-stream-only and a route-started Work never reaches the rail (#641), the Activity `report` kind has no producer (#672), the growing list of load-sensitive cells incl. a CI occurrence (#706). Residuals noted on #629: `client_inactive` after the resume recheck is `recoverable:false`; the shadcn AI Questionnaire is not installable in this Base UI project (`components.json` has no registries), so the bounded stepper is built on the project's own Field. Signed-in hosted journeys need the owner's browser session; the agent never types passwords.

From the fifth session (#727/#728/#630 fix rounds), filed or pending filing as plain issues: #733 (the collapsed-sidebar cookie is inert on the server: `app/(firm)/layout.tsx` imports `SIDEBAR_COOKIE_NAME` — a bare string constant — from a `"use client"` module, so the RSC bundler has no client-reference for it and the server-side import resolves to `undefined`; `cookies().get(undefined)` finds nothing regardless of the request's actual cookie, measured by sending the raw SSR request with an explicit header both ways); #734 (the last step of #727's misreport chain: a pure rendering fault surfacing through the stream's rejection still reads to a person as "Could not send that message," and needs its own distinct treatment from an actual send failure); #735 (journal-work B3-recovery and accessibility multi-face cells need more timeout headroom under load); #736 (at the 320px floor the Clara rail is an overlay that intercepts clicks on the page behind it); #737 (the frozen `claraWork.v1/v2.errors.ts` roster has no entry for CLR13 `work_cancelled` and classifies it under the unrecognised-pair default `state_changed`; a correct classification needs a `claraWork_v3` closure — tracked as part of #631); #738 (`WorkStatusLine` does not carry a client id); #739 (thread-rehydrate and live-clarify clock cells are sensitive to load); #740 (the `journal-work-mock` control leg's request-stream drain shape needs unifying, related to #722).

## Next Steps

1. Hosted release of 0184 / runtime v81 / clara-web, then the owner's signed-in walk covering #728 (Activity attribution, spoken-for documents) and #630 (Cancel Work, Take responsibility, Stop reply) if not already done.
2. Next frontier: [#631](https://github.com/BELCORT-SDN-BHD/clara/issues/631) (egress authority — also where `claraWork_v3` folds in the cancel/authority roster entries, `#737`), [#641](https://github.com/BELCORT-SDN-BHD/clara/issues/641) (B-style Work list/detail — now has the stopping/cancelled vocabulary from #630), #633 (needs #620/#624); still-open frontier #615, #619–#622, #624, #635 (needs #625/#628).
3. Owner: rule on #721 (basis-completing answers) and triage #718–#722, #733, #734, #735, #736, #738, #739, #740 with #689–#693, #705–#709, #714, #715.
4. [Final acceptance #683](https://github.com/BELCORT-SDN-BHD/clara/issues/683) owns integrated delivery, blueprint synchronization and explicit closure of #612/#597.
