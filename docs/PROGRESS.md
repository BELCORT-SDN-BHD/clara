# Clara — Project Progress

Minimal session state. Everything durable lives on GitHub (issues, PRs, the #597 wayfinder map) and in git (`docs/plan/active/refresh-wave-2026-09-15/` for the current wave — DECISIONS, reports, drafts, the release runbook; `docs/plan/active/refresh-wave-2026-09-14/` for the released one). Clock in: read this, confirm the repo is clean (`git status`, `git log -1`, local `main` = `origin/main`), then ask the owner which ticket.

## Current State

- `main`: PR #838 (riders batch 2026-09-15, `7e40e3be`: 15 migrations 0199–0213, `chatTurn_v1` retired, 17 tickets `awaiting-release`) **plus this wave's PR #860, LANDED 2026-09-17 22:25 MYT by fast-forward: `main` = `a296765c`** (`integration/wave-2026-09-15` (twelve tickets #625 #633 #638 #639 #646 #647 #648 #649 #650 #652 #653 #654; migrations **0214–0224**; successor cut `chatTurn_v20` / `claraWork_v4` / `clientOnboarding_v5`; 156+ commits, +85k lines). `ci` was green on the landed sha (lint incl. full-history gitleaks, build, db-estate, db-live-gates).
- Hosted: **RELEASED 2026-09-17 15:02–15:15Z** (`RELEASE-RUNBOOK-0199-0224.md` § RESULTS) — DB frontier **219 / `0224_preview_invite`** (`26 new migration(s) applied · 219 total`, one run, 99 s); `clara-runtime` image **`refresh-a296765c`** (`sha256:b67163f3…`, machine `48ee715b763048`, boot `bodies=53`, `stranded bodies n=0`, pins v20 / v4 / v5); `clara-web` **`095073c9-…`** (tag `refresh-a296765c`) at 100%. Restore point: full dump 2026-09-17T15:00:26Z. Previous image `refresh-3486b6c2` was rollback-preflight ALLOWED at 15:16Z only (snapshot). Frozen manifest deploy-locked (`--lock-deployed`, 18 entries).
- Wave evidence is entirely LOCAL (`refresh-wave-2026-09-15/reports/`): from-scratch chain 0001→0224 (219 files) on a virgin cluster; db estate 4934 / 4838 pass / 95 expected skips; runtime unit 2658 / 2638 (four environmental reds); 18 World e2e legs green incl. the three new bodies and the two-build cutover; browser 41 specs green; frozen law `--compare-base origin/main` additions only. Two contracts deliberately not cut at grant walls (#653's chat tool + `read_prepayment_source`, #647's alias writer) — follow-up issues.
- Owner rulings this wave: DECISIONS §0 (D1–D13), §3.1 fourteen post-review ratifications, §3.4 the re-base onto the moved main, **§3.5 the agent runs the hosted release itself, carrying PR #838's batch, and closes both batches' tickets** (`awaiting-release` is not a house label; each assignee ships their own, the next releaser carries what sits ahead on `main`).

## In Progress

- **Wave 2026-09-15 — DONE** (session `11d28f2d…`): landed (PR #860), delivery comments posted, 55 follow-ups filed (#861–#915), hosted release executed and recorded, hosted-evidence comments posted and the 29 tickets closed (twelve wave + seventeen of PR #838's riders batch incl. #764 and the already-closed #787; `awaiting-release` removed everywhere). One more follow-up filed from the ceremony itself: `dsn-pipe.mjs` pins the CA with a Windows path a WSL child cannot open (backup ran under a scratchpad wrapper). Every `rig*` cluster is dropped; `clara-wt\int` is the one worktree directory still on disk (file-locked by another process — delete by hand).
- **Follow-up triage 2026-09-18 — DONE** (session `01Ngd3X4…`, verified at `65fde7f3`): 74 `needs-triage` follow-ups (#839–#858, #861–#915, #917) dispositioned with a codebase claim check each. 54 `ready-for-agent` carrying an agent brief; 13 `ready-for-human`, each comment naming the one decision only the owner can make; 6 closed (#870 #873 already fixed on `main`; #855 premature by its own rule; #856 #893 ratified as implemented; #888 duplicate of #861); #869 closed by the Known Issues line above; #919 split from #910 so its two small items stop waiting behind the product question. Two routed to the mainline: #886 → #683's historic-ID ledger, #901 → #682's hosted session. Six orchestrator calls flagged as overridable in their comments (#841 delete the dead rung · #843 route A domain events · #856 keep the wider scope · #887 client-scoped face · #897 mock lane · #900 field-shaped parts only). The tickets carry the durable record; the verification reports live only in the session scratchpad.
- **Owner review of the triage — DONE 2026-09-18** (same session, grill): all 13 `ready-for-human` questions answered and the six calls reviewed. Overturned: #841 and #887 closed, because the vendor-binding proposal lane is a retired workflow (O37 / D6) and 0215's AC4 says identity conflicts are stated, never resolved (`.out-of-scope/identity-conflict-resolution-face.md`); a new ticket (#921) makes the LEGACY vendor-bindings panel read-only. Confirmed: #843 #856 #897 #900. Flipped to `ready-for-agent` with the ruling on the ticket: #861 (five kinds: people / assets / counterparties / clients / firm), #871, #872 (R1 superseded: read-time `issuer_lapsed`, still acceptable), #874 (mail-endpoint seam only), #882 (pin the refusal, no trigger change), #885 (a correction cancels and re-runs the parked Work), #912 (audit-row role column). Parked as ideas: #881 #883 #892 #907 #910 #911. Constraint that survives: #847, #882 (a) and #915 share the one `chatTurn_v21` / `claraWork_v5` cut and go to one assignee.
- **Idea round — DONE 2026-09-18** (same session, grill, one question at a time): the eight parked ideas are ruled and published as `ready-for-agent` tickets with native blocking edges. #782 narrowed to wording (invoice line items are an accepted limitation, not `planned`; its wider half → #656 and idea #926). #788 → #927 → #928 → #929 (full retirement of the 0045 template lane; a read-only hosted census found zero templates, so no data migration). #881 → #930 → #931 (explicit allocation list, date-ordered suggestion; WD-R10 kept). #883 → #932 → #933 (a depreciation policy per enrolled asset account; Clara proposes when none, six defaults on the ticket). #892 → #934 → #935 (accountant-readable notes; optional education tips outside the required count). #907 → #936 (bug: a revised accrual leaves its detail at r1) → #937 (person-stated amount per period) + #938 (a bill inside an accrued period is surfaced with skip / reverse-now). #910 → #939 (person-stated term via `prepayment_schedule_v2`, six defaults; blocked by #919). #911 → #940 (per-client prepayment-account roster with a purpose column; blocked by #919). The round surfaced the revenue-side gap (no deferred-revenue or accrued-revenue lane; no such accounts in the standard chart) and the owner grilled it to ready in the session: #941 deferred revenue (mirror of the prepayment schedule; blocked by #939 #940 #919) and #942 accrued revenue (a side on the accrual lane; blocked by #936). Defaults taken are written on each ticket for the owner to flip on review.
- **#926 ruled — DONE 2026-09-18** (same session, grill of seven questions). The last parked idea is decided and closed as split. The measurement that settled it: the blueprint forbids *deriving* a statutory rate and nowhere forbids *reading* a figure the page prints, and the invoice lane already posts model-read figures unattended, gated on arithmetic the document checks against itself rather than on trusting the model. So a payroll summary and a contract go down the same lane as any other accounting document (owner chose the full lane over the narrow read-only and chat-only options, both of which were measured and offered). Published as #944 (blueprint wording, incl. the architecture row #782 still owed) · #945 (payroll typed facts: a new frozen questionnaire family of its own, a registered deterministic evaluator that sums the model's per-row quotes, the vocabulary and field-path widening, the capability registry re-derived) → #946 (draft and post unattended; a new `Salaries Payable` chart row) → #947 (find and settle the net-pay payment) · #945 → #948 (hire-purchase agreements into the fixed-asset lane) → #949 (tenancy terms and a confirmed recurring rent plan). Standing rulings from this round: per-employee detail is never persisted and the entry carries no employee dimension; the net-pay and rent legs credit a payable, never a bank account, so the real payment settles rather than double-counts; #938, #947 and #949 share ONE settlement shape (derived row, self-clearing, candidates offered never chosen) and whichever lands first defines it.
- **Triage board empty at clock-out 2026-09-18**: `needs-triage` 0, `needs-info` 0, `ready-for-human` 0, `idea` 0, `ready-for-agent` 120. The seven parent idea tickets (#788 #881 #883 #892 #907 #910 #911) and #926 are closed as split, each pointing at its children.
- **Signed-in smoke walk DONE** 2026-09-17 15:42–15:50Z through the owner's Chrome (runbook § RESULTS step 8): build-info `git_sha` pairs, `/operator`, a Work's Activity tab, `/clients/<id>` work-pack tiles, accruals, prepayments, `/settings/knowledge`, `/settings/setup` all render. Watch the first hosted reconciler sweeps for #764's legacy arm (never met a real backlog row).

## Known Issues

- Windows-host reds that are not code: no `pg_dump` on PATH (four runtime files), #693 Defender/EICAR (also the intake spool `EPERM` rename), host-contention flakes in the sign-in helpers under many concurrent lanes, and `next build` panicking `0xc0000142` in the PostCSS loader under heavy contention (measured at twelve lanes, ~594 processes, 5 GB free of 32; a retry clears it, #869). CI (Linux) does not see them.
- Blueprint drift recorded for the #683 sync, not edited (DECISIONS §3.2): `ARCHITECTURE.md:171` pins (now v20 / v4 / v5), §3.5 fixed-asset "intrinsic" claim, `PRD.md:69/114/122/123`.
- The wave-3 two-build blocker (ARCHITECTURE §10: a Work parked on `claraWork_v1`/`_v2` cannot post once 0195 is live) is untouched; #810's retirement makes a parked `chatTurn_v1` run a hard STOP in the runbook.
- Ideas: all nine parked ideas were decided 2026-09-18 (In Progress above) and none remains open; the `idea` label carries nothing. #836 closed 2026-09-18: the owner's second real-provider run is filed (`refresh-wave-2026-09-14/reports/631-provider-eval-2026-09-18.md`, `gpt-4.1`, 3/3 · 2/3 · 3/3 · 3/3; the one `refuse_to_invent` miss is noted for #682's provider evaluation). #792 closed 2026-09-18: filed upstream as vercel/workflow#4231; hosted exposure is zero (boot census `stranded bodies n=0`, the pre-release cleanup rule and the #637 gate in front).

## Next Steps

1. The rider queue is `ready-for-agent` (120 open at clock-out 2026-09-18 incl. the mainline's #663–#683, a brief on each; `ready-for-human`, `needs-info`, `needs-triage` and `idea` are all empty). One `chatTurn_v21` / `claraWork_v5` cut is owed jointly by #847, #882 (a), #915 and the conversation halves of #931 #933 #937 #941 #942: one assignee, one ceremony. Dependency chains from the idea round: #927→#928→#929, #930→#931, #932→#933, #934→#935, #936→#937 and #936→#942, #919→#939/#940→#941. From #926: #945→#946→#947 and #945→#948→#949 (#944 is independent).
2. The owner returns to the #597 map: **#682** (verify the real accounting journey and runtime recovery on the released combination, incl. #820's cleanup, #810's census, #813's provider eval; carry #847's trace census; #901's hosted count is done: 0, closed, and the one stale `clientOnboarding_v4` run is cancelled, both recorded on #682) → **#683** (final acceptance + blueprint sync incl. #886's C88.16 row, whose discovery is posted on the ticket and whose one-line acceptance for ARCHITECTURE §6 the owner deferred to this sync, and the PRD:69 prepayment-via-chat gap #915 records, then close #612 and #597).
3. When the lawyer-reviewed Terms/DPA wording arrives: publish v2 through `clara.publish_legal_document` (BELCORT owner) or a seed migration.
4. When the admission beta should stop taking firms: `set_admission_capacity` (BELCORT owner).
