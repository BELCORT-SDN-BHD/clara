# The 2026-09-03 night / 2026-09-04 rulings — the 23-decision ceremony grill, the cutover, the reset, the product walk, and GO (裁-151…185)

> **The TENTH ledger**, continuing [`mohe-grill-rulings-2026-09-03.md`](mohe-grill-rulings-2026-09-03.md)
> (裁-146…150) at that file's ceiling, as each earlier file continued the one before it. **The chain:**
> `-08-31` → `-09-01` → `-09-01-pm` → `-09-02` → `-09-02-pm` → `-09-03` → **this file, the newest.**
>
> **Thirty-five rulings across one night:** the **23-decision ceremony grill** (裁-151…173, opened
> ≈19:4x on 2026-09-03 at the owner's ask 「把該問的用grillin 問清楚」, one question per turn through
> `AskUserQuestion` from OD-4 on, the recommendation first and the cost stated); the two the P6-X
> classification forced (裁-175/176); the ones the ceremonies produced as they ran (裁-174, 177…184);
> the launch decision (裁-185). **EIGHT went AGAINST the recommendation** — 156 · 158 · 161 · 163 ·
> 174 · 175 · 176 · 177 — each with its dissent, filed once and not relitigated.
>
> **Each ruling's TEXT OF RECORD for 151…177 is filed verbatim** as one ruling-NNN.md file under
> [`docs/plan/completed/owner-rulings-151-177-2026-09-03/`](../completed/owner-rulings-151-177-2026-09-03/),
> **and those files govern on any divergence with the entries below**. 裁-178…185 have no separate
> file: this ledger IS their text of record, written from the lead's as-run notes. **Resume map:**
> `PROGRESS.md`'s posture plus [`beta-handover-2026-09-04.md`](beta-handover-2026-09-04.md).

---

## The 23-decision ceremony grill (裁-151 … 裁-173)

### 裁-151 — OD-1: chat and SSE go through the same-origin proxy, and the standing steer for the whole grill (owner, 2026-09-03 ≈19:4x MYT)

**Owner's words, verbatim:** 「按照最流暢和標準的做法辦，我要architwcture 是有效率的的」.
**Ruled:** option (b) — repoint `apps/web/lib/clara/api.ts`, `apps/web/lib/clara/stream.ts` and
`apps/web/lib/clara/useClaraThread.ts` at the same-origin `/api/runtime/*` proxy every other runtime caller
already uses; **the runtime's CORS surface is NOT widened.** Plus a **standing steer for the rest of
the grill**: where two lawful shapes exist, pick the smoothest standard one. The proxy adds one
browser → Worker → Fly hop (the standard BFF shape), keeps the Supabase credential off the
browser→Fly wire and removes the build-time `NEXT_PUBLIC_*` freeze; streaming through the Worker was
the one unknown, with FS-10 S14 as the instrument and option (a) as the fallback **only** if the
preview did not stream. **It streamed** (S14: TTFB 1.83 s against a 6.05 s total). Shipped as #539
`9d5d844e`. Text: `ruling-151.md`.

### 裁-152 — OD-3: the owner mints the pepper and the auth-wall token ONCE, at FS-10 S8 (owner, ≈19:5x, 「照建議」)

`CLARA_RATE_WALL_PEPPER` and `CLARA_AUTH_WALL_SERVICE_TOKEN` are generated by the owner locally
(≥32 random bytes each), kept in his password manager, set on `clara-web` env-to-env at FS-10 S8, and
**the same bytes** go to `clara-runtime` at FS-11 step 12 — the first moment two operands exist for a
hash comparison. Never pasted in chat; the lead never sees them.
**As run:** minted 2026-09-03 ≈22:38 MYT at S8. The comparison could not run as written — **the
clara-web side is not readable back** — so the record carries the two sha256 digests of the bytes
sent to Fly and the FUNCTIONAL proof at FS-11 step 13, where the confirmation answered through the
C-5 route. Text: `ruling-152.md`.

### 裁-153 — OD-4: the four security-pass cutover lines are EXPLICITLY deferred, each to its FS-11 step (owner, ≈20:0x, 「照建議」 after one 大白话 re-explanation)

`security-pass-2026-09-02.md` lines 3 · 4 · 5 · 7 are not ticked at FS-10; the as-run writes "deferred
to FS-11 step N" beside each — line 3 → step 12 (the C-5 secrets) · line 4 → step 11 (the two
NOLOGIN→LOGIN flips) · line 5 → step 6 (`acl-baseline.sql` on the live project) · line 7's walk half
→ step 13. The other seven are ticked at FS-10. Ground: nobody signs up between the two ceremonies.
**All four were ticked in the field** — line 5 at 01:59:19, line 4 at 02:30:37, line 3 (functional)
and line 7's walk half at FS-11 step 13. A truing rides: items 4 and 5 cite `0161` where the
auth-wall role pair is minted by `0163`. Text: `ruling-153.md`.

### 裁-154 — OD-5: the preview walk is password-login only; the redirect allowlist is NOT widened (owner, ≈20:0x, 「照建議」)

Supabase Auth's redirect allowlist stays exactly the two real-origin entries at every point of FS-10
— no wildcard, no `workers.dev`. The signup-confirm and password-recover arms are proven on the REAL
origin at S21, where a rollback is still a repoint. **As run:** both arms walked at S21; the 01:15
read found the allowlist holding **FOUR** entries (wider than ruled, no wildcard) and the owner
narrowed it to the two at ≈01:18. Text: `ruling-154.md`.

### 裁-155 — OD-6: the invite link's `?ct=` edge-log redaction — look first, then configure or defer explicitly (owner, ≈20:1x, 「照建議」)

Read Cloudflare's dashboard for a query-string redaction control on THIS plan; if present, configure
and prove it with one live invite link; if absent, an explicit dated deferral naming the exposure —
never a silent skip. Stated on the record: the link carries **two** bearer factors (path `token_hash`
plus query `ct`), and the checklist covers the query one only.
**As run (S16, ≈00:35):** Workers Logs **OFF**, no Logpush job, no redaction control on this plan —
so the exposure has **no log to land in today**. Recorded as a deferral-by-absence with a standing
rule: *if Workers Logs are ever enabled, `?ct=` lands in invocation logs and the decision is
re-taken.* A Known-issues row carries it. Text: `ruling-155.md`.

### 裁-156 — OD-7: NO soak. Switch the domain and DELETE the Pages project in the same sitting (owner, ≈20:2x — AGAINST the recommendation)

**Owner's words, verbatim:** 「不觀察 ，舊的forntend 完全不能用，直接換and 删」. His ground, recorded as
stated: the legacy `apps/dashboard` on Pages is not a product anyone may use, so a soak that protects
a rollback to it protects nothing. **Recommendation declined (dissent filed, not relitigated):** 24 h
with three named observations. **Consequence stated once:** after the delete there is no repoint
rollback; a broken Worker on the real origin is fixed FORWARD through `wrangler versions` — the
Worker's own version history is the rollback surface.
**The sharpened execution:** the real-origin re-walk (S21) is the GATE, with the three observations
folded into it as its own reads; all clean ⇒ the Pages delete follows in the same sitting; any read
not clean ⇒ no delete, fix forward, and the Pages project stays as an inert leftover, never a
fallback. **As run:** S21 clean (32 PASS anonymous, the recovery arm end to end, Worker Errors 0), so
S25 unlocked and the project `clara` was deleted and read back ABSENT at 02:39. Text: `ruling-156.md`.

### 裁-157 — OD-8: NO maintenance page during FS-11; the as-run records the window (owner, ≈20:2x, 「照建議」)

Every route on `app.clarabook.com` errors during the reset, and no holding page is configured; the
as-run records the window's start and end and states the errors were expected. Ground: the first
invited firm signs up AFTER FS-11, and a holding page is an unordered, unproven surface added at the
worst hour. **As run: the window opened at the machine stop, 01:30:46 MYT, and closed at the healthy
restart, 02:09:03 MYT — ≈38 minutes.** 裁-156 + 裁-157 together answer truing line T-G. Text:
`ruling-157.md`.

### 裁-158 — OD-9: the `apps/dashboard` SOURCE delete lands BEFORE beta live (owner, ≈20:3x — AGAINST the recommendation)

**Owner's words, verbatim:** 「上線前就刪：現在開這條代碼 lane」. **Recommendation declined (dissent
filed):** defer to a dated Backlog row. **Consequence stated once:** one more full-ladder code PR
before the sitting. **The sharpened execution:** the lane opens now with a DO-NOT-MERGE-BEFORE-S21
banner; **merge gate = FS-10 S21 passed** (the sequencing law: repoint first, prove the Workers build
serves every route, then delete — never the same commit as the repoint); a hand sweep after merge;
FS-11 does not depend on it; 裁-150 is intact because this is BEFORE the e2e, not a next lane.
**As run:** PR **#540** opened at `b98197df`, merged **`ba8e7d35`** at ≈01:00 after S21 ✓, one fresh
opus review CLEAR ✓ and 裁-176's twelve dispositions ✓; the hand sweep **33781966143** came back
**SUCCESS 13/13** at ≈02:14. 59 suites classified 19①/18②/10③/12④ (the commit message's 18/20/9 was
corrected in the PR body by the review). Text: `ruling-158.md`.

### 裁-159 — OD-10: BELCORT is re-minted through the product's own self-serve door (owner, ≈20:3x, 「照建議」)

Route (a): the owner walks `/signup` as an applicant — account, the six-digit code, the DPA
signature, one sandbox MYR 0 checkout — which mints BELCORT; `is_operator` follows at FS-11 step 14,
which refuses on zero BELCORT rows and therefore must come after. Route (b) (a hand-made `clara.users`
row plus an admission token plus a manual Supabase auth step) was offered and not taken.
**As run:** BELCORT born **`04daf86c-3aaf-4c59-9442-cce93f3582af`** at 19:07:27.385Z, the registration
approved with `decided_by` **null** — the self-serve door, no operator. Text: `ruling-159.md`.

### 裁-160 — OD-11: the parked canary's clara-side rows die with the schema (owner, ≈20:4x, 「照建議」)

Accepted, and written as an as-run line at step 4 rather than later. The `workflow.workflow_runs` row
survives under constraint 15 as an orphaned durable run. **Constraint 11 is untouched** — deleting
test rows is neither answering the canary nor approving the witness — and no preserve attempt was
made. The DR STRICT `4.9` probe loses its subject; OD-22 names the replacement. **As run:** executed
at step 4 with that line written; see 裁-172. Text: `ruling-160.md`.

### 裁-161 — OD-12: PURGE BOTH — every test auth user AND every Storage object (owner, ≈20:4x — AGAINST the Storage half)

**Owner's words, verbatim:** 「都清，沒有real user now ， all test user and 資料now」. **Recommendation
partly declined (dissent filed on the Storage half):** purge the auth users, leave the Storage
objects — because the Storage delete is an irreversible act on a vendor surface with no repo runbook.
**The sharpened execution:** a NEW step **4b**, after the reset and never before it; delete the
OBJECTS in every bucket but **never a bucket and never a policy** (bucket RLS is a mechanism under
test — constraint 14's operative clause); record the counts before and `0` after; Storage bytes are
not in the Postgres dump, so the loss is accepted on the record.
**As run:** BEFORE 3 / 156; AFTER, read back 01:36:54, **0 and 0**, bucket `firm-docs` present, its **6 policies untouched**. Text: `ruling-161.md`.

### 裁-162 — OD-13: for FS-11 only, constraint 14 supersedes DR.md's owner-run classifier, and the supersession EXPIRES at beta live (owner, ≈20:5x)

**Owner's words, verbatim:** 「什麼意思？ beta live launch 後還有嗎？ 沒有的話就按照你的建議做」 — answered:
none after beta live, because constraint 14's data authority is DATA-scoped and expires at beta. The
lead therefore runs FS-11 steps 2, 4 and 7 as the owner's delegate through the real audited doors,
DSN via `scripts/ops/dsn-pipe.mjs` only, receipted. **Stays owner-run:** any live secret, the R2
token, the `age` identity, `gh pr merge`, and the secret-bearing steps 11 and 12.
**The expiry is the operative half: from beta live, no agent-run destructive command against the live
project.** Text: `ruling-162.md`.

### 裁-163 — OD-14: the restore-proof uses a FRESH LOCAL dump restored by the lead (owner, ≈21:0x — AGAINST route A)

**Owner's words, verbatim:** 「(B) 本地新備份，我自己做」. **Recommendation declined (dissent filed):**
route A — decrypt the latest R2 bundle with the owner's `age` identity, discharging the pre-reset gate
AND the overdue monthly-light cadence in one act, proving the OFF-SITE copy. **Consequence stated
once:** only the local dump is proven; the off-site bundle's decryptability stays unproven since
2026-07-22. **A Known-issues row was minted by the ruling itself** (owner · run `DR.md:376-381` /
`:431-436` with the `age` identity on a date he picks · 裁-163).
**Superseded the same night by 裁-177**, which waived the dump and the restore-proof entirely. **The
Known-issues row about the overdue R2 drill STANDS** — it was never about the pre-reset gate. Text:
`ruling-163.md`.

### 裁-164 — OD-15: the walk's denominator is the ELEVEN enumerated milestones, AND the walk must exercise the agentic paths (owner, ≈21:0x, 照建議 + an ADDITION)

**Owner's words, verbatim:** 「照建議， and agentic 能auto 的東西like autopost and bank reconcialition due
uplaod bankstatembt and using chat for all accoubting execution 也是要test ，確保其我們的backend 沒有大問題
in product vision/objectives and agentic ambitious」.
**Part 1:** "sixteen steps" is re-cut to the enumerated list wherever it appears; **nothing is
invented to reach sixteen.** **Part 2 (the owner's addition):** the product walk gains an explicit
AGENTIC section — (a) bank statement upload → intake → reconciliation → the belt's drafts → the human
disposes · (b) chat as the accounting execution surface (onboarding, a coding/journal proposal,
close-prep, a report render) · (c) auto-post / automatic wake, whose disposition became OD-15b ·
(d) witness and FA/adjustment authorities, recorded as DARK IN THE UI rather than walked through SQL.
**The verdict weight, in the ruling's own words: a backend defect found here is a launch blocker if
it breaks (a) or (b); gaps in (c)/(d) are Backlog rows.**
**As run: the walk broke (a) outright and half of (b)** — which is why the launch question was put as
a real go/no-go. Honest count: **11 enumerated milestones · 6 PASS · 1 PARTIAL · 2 FAIL · 2 NOT
REACHED.** Text: `ruling-164.md`.

### 裁-165 — OD-15b: beta ships autonomy LAYER 1 only; the G1 cadence wake sources stay OFF (owner, ≈21:1x, 「照建議」)

Grounded on a measurement, not a guess: the G1 engine and both wake bodies are MERGED but not
deployed, both switches OFF, **and the 裁-40 flip is INERT until G1 PR-2 builds the two producers**
plus eight deferred DB items. **Beta has:** statement upload → the matcher and auto-draft belt on the
leader's cadence → drafts in the Journals queue → the human posts or refuses; and chat as the
execution surface. **Beta lacks:** unattended per-client cadence wakes. **Backlog row:** G1 PR-2 —
the two producers, the eight deferred DB items and the `bank_agent_due_claims` retention path — a
DB+runtime train under the full ladder, before 上市. The walk records layer 2 as "OFF by ruling",
never as a failure. Text: `ruling-165.md`.

### 裁-166 — OD-16: beta launches on the DPA signature ONLY; the beta terms are a dated Backlog row (owner, ≈21:2x, 「照建議」 + a question answered)

**Owner's words, verbatim:** 「照建議，不過通常正式user term and conditions 簽一次就好right？ 究竟是要多少
文件？ 給我了解下」. **The document count, answered from the repo:** per FIRM, at signup, once per text
version — the DPA (live now) and, from 上市, the Terms of Service (a separate document kind, 裁-125/129,
NOT seeded and NOT in force at beta); per CLIENT — the authorization letter, signed between the firm
and its client outside the app; per INVITED member — nothing separate in beta.
**The exact re-cut owed to `docs/product/PRD.md`, measured after the ruling:** 裁-145's note said
"four of the five are live (DPA e-sign · Beta terms · rate wall · Stripe checkout success)" — **the
Beta terms are NOT live**, so it re-cuts to **THREE live** with the terms as the not-yet-live fourth,
and every text that copied "four live" re-cuts with it. Text: `ruling-166.md`.

### 裁-167 — OD-17 (DS-07): follow `clarabook-frontend`, and what it SHIPS is the authority (owner, ≈20:4x)

**Owner's words, verbatim:** 「跟著clarabook-frontend就對了」. The design repo's shipped button
component (24/28/32/36 px) is byte-identical to `apps/web/components/ui/button.tsx`'s size block
(md5 `6f29955ea9f9f080f7e602149d6a4aa6`); its token contract §5.2 (32/36/40) exists in **neither**
repo's code and is recorded as NEVER IMPLEMENTED. Option B in substance, with the owner's framing —
the authority is what the design repo ships, not what a contract paragraph says. 裁-13's 24 px gate
stays GREEN; the 13 `size="xs"` buttons sit on the SC 2.5.8 floor with zero headroom (lawful,
recorded). FS-9's owner-owed DS-07 line CLOSES with this. Text: `ruling-167.md`.

### 裁-168 — OD-18: the two `clarabook-frontend` recut PRs are DEFERRED to the owner (owner, 20:40, 「照建議」)

Both PRs — 裁-64②'s `--input` token value and R3 §9's focus-ring founder amendment — are the owner's
own, in the design-authority repo, outside every lane's write boundary, and change no shipped
behaviour here. **Backlog row (owner · next step · ruling):** open them on a date he picks; until then
the design law drifts from the shipped app and any future port re-imports the drift. 裁-167's note
rides the same row. Text: `ruling-168.md`.

### 裁-169 — OD-19: 裁-102's wall is the two platform rate limits, accepted BY THEIR NUMBERS (owner, ≈20:4x, 「照建議」)

Number 1 = the Resend plan's cap. Number 2 = the Supabase Auth email rate limit as raised by the
owner on 2026-09-03 — a value **never stated anywhere** — read back by Management API at the walk,
never from memory or a screenshot. Security-pass line 6 is ticked against those two READ values.
**裁-102 closes as SUBSTITUTED, not repealed.** **As run: number 2 = `rate_limit_email_sent`
100/hour, written down for the first time. NUMBER 1 WAS NEVER READ** — carried as not measured.
Text: `ruling-169.md`.

### 裁-170 — OD-20: BELCORT is NOT SST-registered; Stripe Tax stays OFF for the beta (owner, ≈20:4x)

**Owner's words, verbatim:** 「BELCORT 目前未註冊 SST」. The checklist's rule now rests on a stated fact
rather than an omission. Beta is sandbox at MYR 0, so no tax amount exists to compute. The real-money
switch ceremony carries a line: re-confirm the status, and if registered by then, enable Stripe Tax
and verify the service-tax rate and invoice format before the first priced checkout. Text:
`ruling-170.md`.

### 裁-171 — OD-21: the twenty knowingly-open items are ACCEPTED in principle and read aloud at the sitting; 裁-133 and 裁-111 stay SUSPENDED (owner, ≈20:5x, 「照建議」)

The two beta-shape items named on the record: checkout's `livemode` is stored and never read
(裁-120 A-M5), and a paid applicant who then joins another firm strands their payment (A-M4) — both
harmless at MYR 0 in a sandbox, both owed before the real-money switch. The other eighteen are ruled
deferrals with rows. 裁-133 (no Codex lane) and 裁-111 (the cross-family review leg) are time-boxed
suspensions, resumed or repealed only by the owner at the next session's opening.
**As run: the item-by-item READING did not happen** — the 裁-184 walk consumed the sitting's hours.
The list survives in full as Backlog and Known-issues rows, but **the reading is owed and is a
next-session item, not a discharged one.** Text: `ruling-171.md`.

### 裁-172 — OD-22: name a replacement subject for the DR STRICT `4.9` probe at the final truing, or record it UNPROVEN (owner, ≈20:5x, 「照建議」)

Candidates after FS-11: any durable run with both a `workflow.workflow_runs` row and its clara-side
projection. If the verify script HARD-CODES the canary ids, that is a code change and therefore a
Backlog row naming the file — never a hand edit on launch night. **As run:
`packages/db/scripts/dr-verify-checks.mjs` DOES hard-code them, so `4.9` is recorded UNPROVEN IN THE
FIELD from 2026-09-04** with a Backlog row naming the file and the two line pairs, and the candidates named — the `clientOnboarding_v4` run of 19:25:15.760Z, the `witnessFacts_v3` run of 20:33:50Z, and the walk's `autoDraft_v9` / `chatTurn_v17` runs. Never a silent skip. Text: `ruling-172.md`.

### 裁-173 — OD-23: the close housekeeping — the PNGs now, the elevated-shell acts at the pause window (owner, ≈20:5x, 「照建議」)

The two untracked repo-root PNGs (FS-2-era login screenshots superseded by the walk's own receipts,
in a PUBLIC repo) were **removed at the ruling, 20:55**. At the pause window the OWNER runs, in an
elevated PowerShell: the removal of the locked worktree shells (enumerated by a WALK of the .claude/worktrees directory plus `git worktree list --porcelain`, **reparse points unlinked FIRST**) and `Optimize-VHD` on the WSL distro's `.vhdx`. **The teardown census is a WALK, never a list.** Text: `ruling-173.md`.

---

## The rulings the ceremonies themselves produced (裁-174 … 裁-185)

### 裁-174 — FS-10 opens TONIGHT (owner, 2026-09-03 20:58 — AGAINST "tomorrow morning")

**Owner's words, verbatim:** 「今晚就開：sweep 綠 + 走查腳本就位就開 FS-10」. **Recommendation declined
(dissent filed):** open in the morning — the irreversible acts (the Pages delete, `DROP SCHEMA clara
CASCADE`, the purges) not at night, the lanes finished, one more full-green sweep first. **Consequence
stated once:** a 4–6 h owner-present block starting ≈21:45 with irreversible acts in it, whose only
lawful pause point is after FS-10's as-run and before FS-11 opens.
**Preconditions, all measured before S1:** sweep **33757365379** SUCCESS 13/13 read from
`gh run view --json jobs` (concluded 22:02:49 MYT) · the remote-walk instrument present and
smoke-run against a live HTTPS origin · the as-run template written. **FS-10 opened 22:10:21 MYT.**
Text: `ruling-174.md`.

### 裁-175 — OD-24: the XML blob-open exposure is a Known-issues row, fixed AFTER beta (owner, 21:40 — AGAINST "fix the minimal version before beta")

**Owner's words, verbatim:** 「先記 Known issue，beta 後修」. **The finding, measured at `9d5d844e` by
the P6-X classification:** `apps/web/lib/documents/bytes.ts` admits the MIME type application/xml,
`fetchDocumentBytes` wraps the bytes in a `blob:` URL, `apps/web/lib/documents/open-in-new-tab.ts` navigates a
new tab to it, and `apps/web/components/documents/document-metadata.tsx` calls it with **no MIME gate**; there
is **no `Content-Security-Policy` anywhere in `apps/web`**. A `blob:` URL inherits the creating page's
origin, so an XML carrying an inline `<?xml-stylesheet?>` executes in `apps/web`'s origin under the
opening user's session — **and MyInvois e-invoices ARE XML, an uploadable artifact.**
**Recommendation declined (dissent filed):** a minimal MIME allowlist before beta. **Consequence
stated once:** during beta, any malicious XML a firm member opens through "open in new tab" runs in
the app's origin as that member. **The sharpened record:** a Known-issues row (fix = MIME allowlist on
open-in-new-tab + XML as attachment or the structured view; CSP as its own row; the first post-beta
code item), the walk records the exposure and **does NOT open an XML through that door** (a walk must
not exercise a known hole), and it is listed among the knowingly-open items. Text: `ruling-175.md`.

### 裁-176 — OD-25: each of the 12 ④ suites is ruled BEFORE #540 merges (owner, 21:45 — AGAINST Backlog rows)

**Owner's words, verbatim:** 「先每一個裁完再合併 #540」. **Recommendation declined (dissent filed):**
merge after S21 with the twelve as dated Backlog rows. **Consequence stated once:** a 12-question
grill inside the night's sitting, and #540's merge waits on it in addition to S21, Gate 1 and the
review. The grill ran one suite per turn, interleaved with FS-10's owner-idle stretches, each with
the path, what it pinned, why the web has no equivalent, and a disposition among **(a) re-pin in web ·
(b) port the behaviour · (c) rule dead**. None of the three re-opens the delete.

**All twelve ruled by ≈00:58 MYT 2026-09-04 — 1 retire · 10 port/fix/rebuild · 1 = 裁-175:**
(a) `CommitGate.test.tsx` (F-M15) **(c) RETIRED** — the web's deliberate client-side lock is accepted
as the design 「开账是什么? … 这个不是只有一次吗? onboarding的时候? 我倾向c」 · (b) `AdjustmentTemplatePanel`
**(b) PORT after beta** 「(b) 三項都搬回新頁面 + 修零對平那個錯，beta 後做」 · (c) `adjustmentModel`
**(b) PORT + FIX** the all-zero "balanced" defect · (d) `CounterpartyPicker` **(b) PORT** 「(b) 搬回：
依應收／應付分流文案…」 · (e) `OpeningCeremony` **(b) PORT, step 1 = read `0018`'s door for the
mixed-batch rule** 「(b) Backlog：先查資料庫規則，再補提醒（若需要）和收據；beta 後」 · (f) `QueueRowView`
**(b) PORT** the direction-aware noun + the severity chip as colour AND shape 「(b) 搬回：依方向寫客戶／
供應商 + 嚴重度顏色＋圖形；beta 後」 · (g) `SeedingProposalRow` **(b) PORT** 「(b) 搬回：證據卡＋拒絕理由
＋發布狀態；beta 後」 · (h) `adjustmentApi` **(b) PORT** (one row with b and c) · (i) `advancesApi`
**(b) FIX — a live defect**: `staff-advances-register.tsx:45` sends `businessToday()` where the DB
must decide "today" 「(b) 修錯：改回傳空值讓資料庫決定今天；beta 後的第一批小修」 · (j) `agingApi`
**(b) PORT** an envelope-shape guard 「(b) 補防護：缺欄位時顯示「無法讀取」而非空清單；beta 後」 ·
(k) `xmlStructuredView` **= 裁-175** · (l) `dbSeamCensus` **(b) REBUILD, flagged INFRASTRUCTURE,
before 上市** — apps/web's first rig-gated CI leg plus a two-direction seam census over the DEPLOYED
function bodies 「(b) 重建：進 Backlog 標「基礎建設」，上市前做」. Text: `ruling-176.md`.

### 裁-177 — FS-11 opens WITHOUT the pre-reset dump and WITHOUT the restore-proof (owner, 2026-09-04 ≈01:30 — AGAINST "(b) a 5-minute forensic dump")

**Owner's words, verbatim:** 「今晚接着做,备份不用了, 直接factory reset and sync all newest db migration?」
→ after the explanation, 「(a) 兩個都不做，直接重置」. **His ground:** everything in the live `clara`
schema is test data and the repo re-creates the estate. **Recommendation declined (dissent filed):**
(b) a `--profile full` dump kept outside the tree as forensic insurance, the restore-proof waived.
**Consequence stated once:** the old database's contents survive only in the encrypted nightly R2
bundle, unproven since 2026-07-22, and a failure mid-reset is fixed forward.
**Effect:** 裁-163's route B is SUPERSEDED for tonight (its Known-issues row about the overdue drill
stands); the Wave-G checklist's "confirm the backup is restorable before the reset" is re-cut to *"for
a TEST-DATA reset before beta live the gate is waived by ruling (裁-177); for any reset after beta
live it binds in full"*; the throwaway PG17 was torn down unused; **steps 2 and 2b are recorded as
WAIVED, not silently skipped.** **A consequence the owner should see: ROME SECRETARY's 3,396,500
books — built through the product at Slice 6 and the standing books pin — died with the schema, so
that pin is UNPROVEN POST-RESET and a new one was minted from what the walk posted.** Text:
`ruling-177.md`.

### 裁-178 — the two leaked role passwords are NOT rotated (owner, 2026-09-04 ≈02:2x)

**Owner's words, verbatim:** 「不用rotate le, I DONT CARE」.
**What happened:** the lead's step-11 v1 script put `\set ON_ERROR_STOP on` INSIDE a psql `-c`
string; psql parsed the whole payload as that meta-command's argument, refused, and **echoed the
argument — with the two role passwords already interpolated** — to the terminal and into this
session's transcript. **The ALTERs never executed** (the roles were still NOLOGIN with no password
four minutes later), so nothing on the database ever carried the leaked bytes; the two Fly DSNs
imported at step 12 DID. **Ruled:** no rotation. Step 11 v2 sets the SAME values step 12 already put
in the Fly DSNs, and the planned step 12b re-import is withdrawn (its script deleted). **The lead's
dissent is on the record and is carried as a Known-issues row:** the values sit in this session's
chat transcript and log, and both should be rotated before the first REAL client's data lands — the
owner's call on timing. **Lesson minted:** a secret-prompt script is dry-run against its own ERROR
path, through the real pipe, with a dummy value, before an owner types a real one. No separate
ruling file — this entry is the text of record.

### 裁-179 — the six lane DSNs go to `uselibpqcompat=true&sslmode=require` tonight; verify-full is the first post-beta code PR (owner, 2026-09-04 02:37)

**The finding, MEASURED on the machine** with a `pg` Client probe printing only the stream class:
the four existing lanes (`CLARA_READ/RUNTIME/WRITE/FREEFORM_DATABASE_URL`) carried **no `sslmode` at
all** and connected over a plain `Socket`, `encrypted=false` — **production credentials and data were
crossing Fly (sin) → AWS (ap-southeast-1) in PLAINTEXT.** Separately, the two NEW C-5 DSNs carried
`?sslmode=require`, which `pg-connection-string` 2.14 in non-libpq mode treats as *verify against
Node's bundled roots*; the pooler chain roots in "Supabase Root 2021 CA", so every C-5 pool connection
failed and the applier logged `self-signed certificate in certificate chain` every ~2 s.
**Options put:** (a) the verify-full PR now, ≈45–60 min (a Dockerfile CA COPY is required — the image
ships none) · (b) two lanes only · (c) an env-to-env re-import of all six tonight with
`uselibpqcompat=true&sslmode=require` — encrypted, certificate unverified — and verify-full right
after beta. **Ruled (c).** **As run 02:38:** all six rewritten env-to-env in one `fly secrets import`
release, machine rolled, healthy 2/2; the on-machine probe read every reachable lane as `TLSSocket
encrypted=true authorized=false` and the self-signed log storm stopped. **The verify-full Dockerfile
PR is a Known-issues/Backlog row, owner, the first post-beta code item.** A truing rides: the runtime
README and the checklist carry NO documented TLS posture for the lane DSNs today, and none records
`pg-connection-string` 2.14's semantics. **Amends** the FS-11 template's "match the existing TLS
posture" instruction, which would have matched plaintext. No separate ruling file.

### 裁-180 — the desktop corpus (the real Rome/BEE documents) is the walk's data (owner, 2026-09-04 03:20)

**Owner's question, verbatim:** 「为什么放rome的资料进去, 不是factory reset 吗? 我们自己e2e 产品的core
features? 还是我们误会了什么?」 Answered from the repo: constraint 13, 裁-63 and FS-11 step 16A.7 all
say *reset, then re-run the fixtures through the real doors and walk the product on the desktop
corpus*; the Rome/BEE files ARE the test data by ruling (ADR-0075). The synthetic alternative was
offered. **Ruled: 照計畫** — the real documents, statement **series C** (`clara-rpr-decrypted`, the
only Apr–Jul series that is complete AND readable, picked by measurement), and the **four 资料缺失
marks stand** (BEE GL/TB either FY · RPR Feb-2025 · RPR Mar-2025 · a named producer/certifier for RS
and RPR). Inventory: [`corpus-manifest-2026-09-04.md`](../completed/corpus-manifest-2026-09-04.md).
No separate ruling file — this entry is the text of record.

### 裁-181 — the onboarding interview should PROPOSE and still ask the human to confirm (owner, 2026-09-04 03:39)

**Owner's question, verbatim:** 「这个是agentic chat right? 如果user 不懂或者回答有一些瑕疵, agentic是可以
自己输入对的, 标准答案in database right? … 如果我回 lesser than 1M 这种会怎样?」 Answered from the code:
the interview is a **deterministic validator per step**, not a free LLM — a fixed synonym map plus
exact band strings, so "lesser than 1M" is REJECTED and re-asked; every capture is echoed "I
recorded … Is that correct?" and the human confirms; nothing reaches the DB until Commit through the
door. That is PRD §6 invariant 1 by design.
**Ruled, verbatim 「是的, 我要的东西是这样的」: an LLM normaliser that PROPOSES the value and still asks
the human to confirm is WANTED** — the agent proposes, the human disposes. A Backlog row: owner,
after beta, 裁-181. No separate ruling file — this entry is the text of record.

### 裁-182 — the client consent letter is produced tonight and the GRANT is walked through the audited door (owner, 2026-09-04 04:09)

**Owner's words, verbatim:** 「为甚么firm cereate 那个dpa的阶段就不要一次过同意全部东西??? uiux这样很乱,
今天先1吧, 我要test core features.」
**What forced it:** the coding lane refused with `no_consent` — no live `client_egress_consents` row
for ROME SECRETARY — and the path to one runs (1) upload the client authorization letter, (2)
`classify_consent_evidence_document` (**has a web button**), (3) `grant_client_egress` (**NO WEB
SURFACE**), (4) `activate_client_egress_purpose` per typed purpose (**also none**).
**Launch-relevant finding: a real beta firm cannot enable AI processing for a client through the
product today.** **Ruled option 1:** the letter is produced, uploaded and classified through the UI,
and the GRANT is walked by the lead as the owner's delegate through the audited DB door. **Rider —
the owner's product view:** consent should be collected ONCE at the firm's DPA stage, not per client
through a dark door. **The lead's note:** the MIA By-Law R114.3(b) obligation is per-CLIENT, so a
single firm-level consent cannot replace it — but the product CAN collect the client letter inside
the onboarding interview and grant on commit, which removes both the dark door and the extra clicks.
A design Backlog row: owner, before 上市, 裁-182. No separate ruling file.

### 裁-183 — produce the consent evidence and the grant NOW, without waiting for the letter's fills (owner, 2026-09-04 04:19)

**Owner's words, verbatim:** 「bro beta test only , just make it happen. 我们要test 功能啊啊. do the sql
work in supabase and unlock it.」
**Executed as:** the repo template §4 (EN) filled for ROME SECRETARY with the known facts and MARKED
beta-walk placeholders for BELCORT's company number, address, telephone and the retention period;
signatory lines reading "electronically signed — approved in writing by Tao in the Clara beta walk";
all five purposes ticked; rendered to PDF with the workspace's own headless Chromium (no network);
**uploaded through the product's Documents tab, classified through the UI button, and the GRANT
through the audited door `clara.grant_client_egress` as the owner's delegate — JWT human-ctx = the
owner's principal, every door check intact. NOT a raw insert; constraint 14 honoured.**
**This ruling became the night's standing precedent:** where a mechanism is live but its FACE is
missing or defective, the lead walks the DOOR as the owner's delegate and records both — used again
for `activate_client_egress_purpose` (five purposes), `add_client_identifier`,
`set_sales_lane_activation`, `enter_bank_statement`, `begin_close` (run 2) and
`fail_statement_facts`. The letter is a test instrument on test data; the lawyer pass (裁-166)
replaces it before any external client. No separate ruling file.

### 裁-184 — walk the rest of the product tonight (owner, 2026-09-04 05:02)

**Owner's words, verbatim:** 「全部走完（銀行+結帳+報表，約 60 分）再 sitting」. Bank subject chosen by
measurement: **not** the RPR April statement (RPR has no letter and no consent) but ROME SECRETARY's
own **June 2025 Maybank statement**, because consent was live, the customer was registered and the
RM2,800 receipt of the already-posted invoice is in it.
**What the extra hour bought — the most valuable hour of the sprint:** the two statement walls, the
classifier's blindness to bank statements, the stranded `statement_facts` task, the "Enter a
statement" web↔door gap, the payer-identifier refusal, the `CLR19` period wall, the missing "Begin
close" button after an abandon, the two false close gates, the close-prep chat refusal and its
misleading sentence, and the empty report-template roster. **None of it would have been found by a
mock e2e.** Every finding is a row in
[`beta-handover-2026-09-04.md`](beta-handover-2026-09-04.md). No separate ruling file.

### 裁-185 — GO: beta is LIVE, CLOSED (owner, 2026-09-04 ≈06:15 MYT, by `AskUserQuestion`, option 1 of 3)

**The three options as they were put:** **(1) GO — 封閉 beta（建議）** — beta live for BELCORT and
owner-invited testers only, with open applicants waiting for v72 + the statement-lane fixes + the
"Enter a statement" form fix + DPA v2 *(the option was put in those words at 06:15; "v72" was
CORRECTED at 06:21 — `fly releases --json` shows v72–v74 are the night's secrets-import releases on
the v71 image, so a NEW image is owed and the next release is v75)* · **(2) NO-GO — fix first** · **(3) GO — open beta**.
**The lead's recommendation was option 1**, on this ground: everything that gates a REAL outside
applicant is either a wall that held or a defect that only bites on paths a closed tester group can
be warned about, while the two genuine outsider blockers — the DPA v1 placeholder and the runtime↔DB
skew — are both fixable without touching a mechanism.

**The owner chose option 1. His words, verbatim:**

> GO, 这个session 算结束了吗?  我要你整理下所有backlog 和 known issue, 包括目前e2e 得到的. 然后给我一个完整的清单
> and report, 我要下个session 维修和完善谢谢. included 所有backend and frontend. 所有的backlog 和knownissue and
> all harness 的需要注意的东西哦.

**The ruling.**
1. **Beta is LIVE and CLOSED** — BELCORT plus owner-invited testers. Sign-ups are not disabled at the
   platform (`disable_signup: false`, read by Management API), so the closure is an OPERATING
   posture, not a technical wall: the owner controls who is given the address.
2. **Before the first EXTERNAL applicant:** **a NEW runtime image from `ba8e7d35` or later — the next
   release is v75** (v72–v74 are secrets-import releases on the v71 image) for the skew;
   the statement-lane fixes, the "Enter a statement" institution/account pair, and **DPA v2**.
3. **This session CLOSES when the final truing PR merges** (裁-150). The repo is the handover; there
   are **no next lanes**; the next session starts on the owner's ask.
4. **The handover is [`beta-handover-2026-09-04.md`](beta-handover-2026-09-04.md)** — the posture, the
   milestone tally, the COMPLETE Backlog + Known-issues list (backend, frontend, harness, ops, legal)
   with an owner, a next step, a size and a priority tier on every row, the harness notes for the next
   session, and an ordered pick-list. `PROGRESS.md` carries the short rows and points at it.

**Amends:** it discharges 裁-150's exit condition and sets the beta's operating posture. No separate
ruling file — this entry is the text of record.
