# Wave 3 · lane 11 · ticket #1022 — a local seam for the invite flow's identity-provisioning call

**`lib/members/invite-mail.ts` gained a second, `#874`-shaped test-only seam
(`CLARA_E2E_INVITE_IDENTITY_ENDPOINT`) for the Supabase admin calls `canMintFor`/
`mintSupabaseTokenHash` make, and `members-invite-walk.spec.ts` now drives a real invite
submission all the way to a settled success banner and a new pending row, with both outbound
legs intercepted locally.** Status: **done**.

| | |
|---|---|
| Worktree / branch | `C:\Users\zhant\Desktop\clara-wt\int` · `riders/w3-lane11` |
| Base | `ffe63a0dd084e99b84c1368119845be273c421ce` |
| HEAD at report | `3fc1bb17a` |
| Database | none used — lane 11 is web-only, and this ticket needed none (confirmed: no `packages/db` file read or written) |
| Migration | **none**, as the prompt expected. No schema or function change, no prestate pins, no rig-meta cohort, no gate-chain entry. |
| Playwright triple | `https://127.0.0.1:3600` / `3601` / `3602` — exercised twice (below) |

## Commits (`git log --oneline ffe63a0d..HEAD`, this ticket's own two commits)

```
3fc1bb17a feat(web): #1022 the invite-to-pending-row browser walk, both admin legs local
3696bde28 test(web): #1022 a local seam for the invite flow's identity-provisioning call
```

(Earlier commits on the branch — `44bf4a07d` … `3fbdb294c` — are #1021, #897 and #1024, already
landed by earlier implementers in this lane; unchanged by this ticket.)

Working tree clean after the commits. Nothing pushed, no PR, no GitHub write, no other worktree
touched.

## The ticket was live on this branch — verified before building

`gh issue view 1022 --json title,body,comments,labels,state`: **0 comments**, `state: OPEN`,
labels `enhancement, ready-for-agent`. The newest (and only) Agent Brief is the issue body; there
is no 2026-09-20 ruling comment to obey or be overruled by. The body itself records that it was
"generated by AI during triage" from `wave1-lane08-final.md`'s own "Follow-ups worth filing" item 2
and its "Anything unverified" note.

Verified NOT already satisfied: at `44bf4a07d` (this ticket's own starting point),
`lib/members/invite-mail.ts` had exactly one test-only override (`INVITE_MAIL_ENDPOINT_ENV_NAME`,
`#874`); no `identityEndpoint` field, no `INVITE_IDENTITY_ENDPOINT_ENV_NAME`, and no
`GoTrueAdminApi` handler anywhere under `apps/web/e2e/` (`grep -rn "IDENTITY_ENDPOINT\|
admin/generate_link\|admin/users"` returned nothing). `members-invite-walk.spec.ts`'s own first
cell asserted `mail_not_configured`, exactly the "invite leg terminates before any admin call"
state the brief and `wave1-lane08-final.md` both describe.

## Seams I tested at (written before the first change, per work-order rule 4)

The brief names two, explicitly by analogy to the existing mail-transport seam:

1. **`inviteMailCapability`'s resolution of a new, optional, loopback-fenced environment
   variable into `InviteMailConfig`** — the exact shape `#874`'s `mailEndpoint` already has.
   Public interface: `inviteMailCapability(env)`'s returned `config.identityEndpoint`.
2. **`productionInviteMailer`'s `admin()` — the ONE place that builds the Supabase admin client
   `canMintFor` (`listUsers`) and `mintSupabaseTokenHash` (`generateLink`) both use** — observed
   through the file's own `recordingClient` fake (injected via `InviteMailTransportDeps.
   createClient`, already an existing, documented seam), asserting the exact URL argument the
   client constructor received.
3. **The browser walk's own public interface** — the invite dialog's submit button, the settled
   banner text, and the invite-list re-read — driven through `/settings/members`, never a mocked
   component or an internal React state read.

No test was written at a seam the brief does not give me (`send()`'s own behaviour, the mail seam's
interface, and the door's authority walls are all explicitly out of scope or already proven
elsewhere — see "Out of scope" in the brief and `packages/db/tests/p4t1-invite.test.mjs`).

## What changed, vertical slice by vertical slice

**Slice 1 — the unit-level seam (`3696bde28`).** `apps/web/lib/members/invite-mail.ts`:
- `INVITE_IDENTITY_ENDPOINT_ENV_NAME = "CLARA_E2E_INVITE_IDENTITY_ENDPOINT"`, read by
  `inviteMailCapability` the same way as `INVITE_MAIL_ENDPOINT_ENV_NAME` — optional, read last,
  never contributing to `missing`, fenced to a loopback http(s) URL.
- `isLoopbackMailEndpoint` renamed to `isLoopbackEndpointOverride` (now shared by both seams) — a
  rename only, same body, same three loopback hostnames.
- `InviteMailConfig.identityEndpoint?: string`, resolved alongside `mailEndpoint`.
- `productionInviteMailer`'s `admin()` now builds from `config.identityEndpoint ??
  config.supabaseUrl` instead of `config.supabaseUrl` alone — the service-role key and the
  separate mail-endpoint seam are untouched.
- `apps/web/tests/invite-mail-transport.test.ts`: a new `describe("#1022 the identity-provisioning
  seam: …")` block, 9 cells (see "Acceptance criteria" below), directly mirroring `#874`'s own
  suite shape (production-pinned default, blank-is-absence, never-in-`missing`, the ADV-1
  loopback/IPv6/non-loopback fences, whitespace trimming, the env-var-name pin, plus one cell
  proving the TWO seams are independent).
- **Red confirmed for the right reason**: `git stash` reverted `invite-mail.ts` alone; the test
  file failed to even import (`SyntaxError: … does not provide an export named
  'INVITE_IDENTITY_ENDPOINT_ENV_NAME'`); `git stash pop` restored the implementation byte for
  byte (verified via `git diff --stat` showing only the intended file).

**Slice 2 — the browser walk (`3fc1bb17a`).** Three files, plus docs:
- `apps/web/e2e/run.mjs`: three harness-only placeholder values for the four required
  `inviteMailCapability` variables (`SUPABASE_SERVICE_ROLE_KEY`, `RESEND_API_KEY`,
  `INVITE_MAIL_FROM` — `NEXT_PUBLIC_SUPABASE_URL` was already set), plus
  `CLARA_E2E_INVITE_MAIL_ENDPOINT` and `CLARA_E2E_INVITE_IDENTITY_ENDPOINT`, both loopback URLs
  under the same `/e2e-supabase` prefix `handleSupabase` already dispatches on.
- `apps/web/e2e/members-lifecycle-mock.mjs`: `invite_member` added to the RPC allow-list; new
  handlers for `GET /auth/v1/admin/users` (an empty directory, so `canMintFor` answers `{ok:true}`
  on the first page), `POST /auth/v1/admin/generate_link` (a fixed hashed token — verified against
  the installed `@supabase/auth-js@2.112.4`'s own `_generateLinkResponse`, which reads
  `hashed_token` off the response body's TOP level), `POST /e2e-invite-mail-capture` (records what
  `send()` posted), the real `POST /rest/v1/rpc/invite_member` verb (a `0147`-shaped receipt:
  `invite_id`/`token_hash`/`expires_at` plus the plaintext `token`, and the one duplicate-email
  refusal a single-session walk can reach), and a new read-only control,
  `e2e_members_lifecycle_invite_trace`, exposing call counts and the captured mail. Every new
  handler scoped by `ours`, matching this file's own standing rule.
- `apps/web/e2e/members-invite-walk.spec.ts`: the first cell's INVITE step now asserts the settled
  `"The invitation to another-hire@larkin.test was sent."` banner, a new PENDING row, and — via the
  new trace control — that `listUsers` fired at least once, `generateLink` fired exactly once, and
  the captured mail's `to`/`subject`/`html` match what a real deployment would have produced. The
  other three cells in the file are untouched.
- `apps/web/README.md`'s Membership section: the `#874`-era "AC2 remains unmet" and "third
  confirmation" paragraphs are replaced with what `#1022` built (same section, overwritten rather
  than appended, per the maintenance-frame convention).

## Acceptance criteria, each with its evidence

**AC1 — "A test can point the identity-provisioning call at a local substitute endpoint and
observe what would have been sent, mirroring how the mail-transport seam already works." DONE.**

`tests/invite-mail-transport.test.ts`, the new `#1022` `describe` block, 9/9 green:
- `PRODUCTION PINNED: … the capability resolves no identityEndpoint, and admin() constructs with
  config.supabaseUrl` — absence is unpinned by construction, not by discipline.
- `the override variable NEVER contributes to missing`.
- `a BLANK override variable is treated exactly like absence`.
- `SET: canMintFor AND mintSupabaseTokenHash both construct the admin client with the override
  URL, not supabaseUrl` — the "observe what would have been sent" claim, via the injected
  `recordingClient` fake asserting the exact constructor argument for BOTH calls, one client per
  call (never a module-scope singleton), the same service-role key travelling either way.
- `the mail-endpoint seam and the identity seam are independent`.
- Two ADV-1 fence cells (non-loopback rejected, IPv6 loopback honoured) and one whitespace-trim
  cell, mirroring `#874`'s own three.
- `the env var's NAME itself carries the CLARA_E2E_ harness prefix`.

**AC2 — "A browser walk exercises invite submission through to a pending member row rendering,
with both the identity-provisioning call and the mail transport intercepted locally, and no real
outbound call made during the walk." DONE.**

`members-invite-walk.spec.ts`'s first cell (`#625: invite, revoke, change a role and remove — each
act reaches a settled, persistent result`), run twice on the lane's own triple
(`CLARA_E2E_APP_ORIGIN=https://127.0.0.1:3600 CLARA_E2E_NEXT_PORT=3601
CLARA_E2E_RUNTIME_PORT=3602`):
- Run 1 (`pnpm --filter @clara/web e2e -- members-invite-walk`, full build): **5 passed (34.2s)**,
  the invite cell **9.9s**. The server log shows the exact real sequence: `GET
  /auth/v1/admin/users` → `POST /rest/v1/rpc/invite_member` → `POST
  /auth/v1/admin/generate_link` → `POST /e2e-invite-mail-capture` → `POST
  /rest/v1/rpc/e2e_members_lifecycle_invite_trace`.
- Run 2 (`--no-build`, same triple): **5 passed (49.4s)**, the invite cell **10.5s**.
- The cell's own in-browser assertions: the settled banner, the new pending row, AND (positive
  evidence, not inference) `identityCalls.listUsers >= 1`, `identityCalls.generateLink === 1`, and
  the captured mail's exact `to`/`subject`/`html` — read back through the new trace control.
- No outbound call left the process: both `CLARA_E2E_INVITE_MAIL_ENDPOINT` and
  `CLARA_E2E_INVITE_IDENTITY_ENDPOINT` are loopback URLs under `/e2e-supabase`, and
  `isLoopbackEndpointOverride` — proven by AC1's own ADV-1 cells — is what makes that the only
  shape either variable is ever honoured for.
- No lingering process after either run: `netstat` after run 2 showed only `TIME_WAIT` sockets on
  ports 3600–3602, no live listener.

**AC3 — "The existing mail-transport seam's own behavior and tests are unaffected." DONE.**

- All 38 pre-existing cells in `invite-mail-transport.test.ts` (including the full original `#874`
  `describe` block) pass byte-for-byte unchanged, alongside the 9 new ones: **47 tests, 47 pass**.
- `tests/invite-courier.test.ts`, `tests/invite-courier-authority.test.ts`,
  `tests/invite-courier-egress.test.ts` (the three suites that drive the courier through
  `mailerFor`/`InviteMailer`, never touching `productionInviteMailer` directly): **107 tests, 107
  pass** — the courier's own ordering and refusal contract is unaffected by a change entirely
  inside `productionInviteMailer`'s `admin()`.
- The other four cells in `members-invite-walk.spec.ts` (REFUSED-act, axe-scan/reduced-motion,
  the two acceptance-leg preview cells) are unchanged and green in both walk runs above.

## Gates, with counts (all at HEAD `3fc1bb17a`)

| gate | command | result |
|---|---|---|
| touched unit test file (isolated) | `node --import ./test/bootstrap.mjs --import tsx --test tests/invite-mail-transport.test.ts` | **47 tests, 47 pass, 0 fail** |
| regression: sibling courier suites | `node … --test tests/invite-courier.test.ts tests/invite-courier-authority.test.ts tests/invite-courier-egress.test.ts` | **107 tests, 107 pass, 0 fail** |
| e2e mock ownership census (touched `members-lifecycle-mock.mjs`) | `node … --test e2e/e2e-fixture-ownership.test.ts` | **44 tests, 44 pass, 0 fail** — every new handler censused as `scoped`, `invite_member` has no other claimant |
| whole web unit suite | `node scripts/run-tests.mjs` (from `apps/web`) | **4870 tests, 140 suites, 4867 pass, 1 fail (pre-existing, unrelated — see below), 2 skipped**, 127.6 s |
| typecheck | `pnpm typecheck` | exit 0 (`apps/web` + `packages/runtime`) |
| lint | `pnpm lint` | exit 0 |
| lint, as the runner sees it | `CI=true GITHUB_ACTIONS=true pnpm lint` | exit 0 (run twice, both exit 0) |
| leak scan | `node scripts/check-leaks.mjs` | OK — the three new placeholder env values in `run.mjs` (`e2e-service-role`, `e2e-resend-key`, and the `INVITE_MAIL_FROM` address) do not match any rule, including the generic key-assignment pattern (checked: too short / wrong key name to match) |
| browser walk touched | `pnpm --filter @clara/web e2e -- members-invite-walk` on the lane triple | **5/5 passed**, run twice (34.2s, 49.4s) |

Not run, and why: any `packages/db` gate chain (no migration, no SQL function, this lane has no
database for this ticket); `check-parts-parity.mjs` (no `packages/runtime` file touched).

**One pre-existing, unrelated failure in the whole-suite run — NOT fixed, reported as found.**
`documents-workbench-refresh.test.tsx`'s `"[633]: an UNSETTLED receipt keeps a bounded watch and
says so; the poll's budget is finite"` failed once (`grew > 0` false — the poll never ticked
before `settleUntilQuiet`'s 300ms quiet window elapsed). This is the SAME cell `#1021` (the
ticket immediately before mine in this lane) just changed from a fixed-count loop to a
deadline/quiet-window wait, and that ticket's own report already flagged exactly this gap under
"Anything unverified": *"I could not run 'repeated' (N>1) whole-suite passes inside this session's
time budget… a single pass is ~2.3 minutes."* My run is effectively that second pass, and it shows
the new mechanism is still sensitive to host contention (a 300ms quiet window can itself be
starved under load, the same class of flake the fixed-count shape had). Isolated re-run of the
file confirms it is not a real regression: `node --import ./test/bootstrap.mjs --import tsx --test
components/documents/documents-workbench-refresh.test.tsx` → **9 tests, 9 pass, 0 fail**,
including this exact cell. Nothing in this ticket's diff touches `apps/web/components/documents/`
or anything in its import graph (`invite-mail.ts`, `members-lifecycle-mock.mjs`, `run.mjs`,
`members-invite-walk.spec.ts` — none reachable from the documents workbench). Filed below as a
follow-up rather than attempted (out of this ticket's scope, and #1021's own report already names
the right next step).

## Docs

- `apps/web/README.md` — Membership section: the `#874` paragraph gained one clarifying sentence
  (the admin-calls-stay-real ruling was about `config.supabaseUrl`, not overturned), a new `#1022`
  paragraph documenting `identityEndpoint`, and the old `#874`-era "AC2 remains unmet" / "third
  confirmation" paragraphs are replaced with what this ticket built — overwritten, not appended,
  so the section describes current state rather than accumulating a ticket history. The
  `fix-round ADV-1` paragraph's reference to `isLoopbackMailEndpoint` was updated to the renamed
  `isLoopbackEndpointOverride`. In the same commit as the code and walk changes.
- `CONTEXT.md` — not touched. No new domain vocabulary: `identityEndpoint`/`CLARA_E2E_INVITE_
  IDENTITY_ENDPOINT` are test-harness mechanism, not a product or accounting concept.
- `apps/web/messages/en.json` and `apps/web/test/manifest.txt` — not touched: no new copy, and
  every touched test file already existed (no new file to register).

## Successor contract

**None is owed.** No door, zod input, argument order, refusal mapping, part kind or prompt stanza
changed. This ticket never touches a frozen chat body, a frozen Work tool, or any module in a
frozen closure — `node scripts/check-frozen-workflows.mjs` (run as part of the root `pnpm lint`)
showed no manifest diff.

## Follow-ups worth filing

1. **`documents-workbench-refresh.test.tsx`'s `[633]` unsettled-receipt cell is still
   host-contention-sensitive after `#1021`'s deadline/quiet-window fix** (evidence above). `#1021`'s
   own report already named the honest gap ("I could not run repeated whole-suite passes"); this
   run is empirical confirmation that the gap is real, not merely theoretical. A next fix-round
   might widen `QUIET_MS` past 300ms, or key "the poll started" off a more direct signal than "no
   new reads for N ms" — but that decision belongs to whoever owns `#1021`'s follow-up, not this
   ticket (unrelated file, unrelated import graph).
2. **The `e2e_members_lifecycle_invite_trace` control and `newInvite*` fixtures this ticket added
   are scoped to ONE happy path and ONE duplicate-email refusal.** If a future ticket wants the
   invite leg's OTHER courier codes (`recipient_has_account` via a confirmed-user directory row,
   `mail_failed` via a broken capture endpoint, `no_token_returned` via an empty `generate_link`
   response) walked in a browser, the admin-API handlers this ticket built are the right place to
   extend — each already keys off the request body, so a second scripted response per handler is a
   small, additive change, not a redesign.

## Anything unverified

- **A THIRD walk run, or a run under simultaneous host load (matching how the whole-suite gate
  above happened to overlap a `pnpm typecheck` run in the background).** Two consecutive runs green
  is the evidence in hand; I did not deliberately run the walk WHILE the unit suite or typecheck
  was also executing, the way the `[633]` flake above suggests host contention can matter for a
  timing-sensitive cell. The walk's own assertions are not timing-sensitive in the same way (no
  fixed-count poll loop is involved — every wait is `expect(...).toBeVisible()`'s own
  auto-retrying assertion), so I judge this a lower risk than the `[633]` cell's, but it is not
  independently measured under contention here.
- **Whether a FUTURE lane's spec ever calls `/api/invite`.** Today `members-invite-walk.spec.ts` is
  the only spec that does (confirmed by a repo-wide `grep` for `mail_not_configured`/
  `RESEND_API_KEY`/`invitation was not sent` across `apps/web` before starting), so enabling mail
  capability globally in `run.mjs` changes no other spec's observed behaviour — verified by the
  whole-suite run showing no OTHER failure. A later ticket that adds a second `/api/invite`-driving
  spec would land on the SAME now-configured capability and the SAME `members-lifecycle-mock.mjs`
  handlers (scoped by `ours`, so a different persona's admin calls would simply fall through
  unanswered) — worth a one-line check by whoever writes that spec, not a gap in this one.
