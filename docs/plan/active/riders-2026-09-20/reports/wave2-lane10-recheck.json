{
  "lens": "RECHECK",
  "wave": 2,
  "lane": 10,
  "branch": "riders/w2-lane10",
  "worktree": "C:\\Users\\zhant\\Desktop\\clara-wt\\660",
  "fixed_point": "23cfad947b5598214168ba9c43d391b4e16aa745",
  "head_at_recheck": "ad1c2ce33",
  "fix_report": "docs/plan/active/riders-2026-09-20/reports/wave2-lane10-fix.md",
  "verdict": "accept",
  "verification_runs": [
    "CLARA_MIGRATION_REDO=0270_firm_document_limits_writer node scripts/migrate.mjs (packages/db) -> refused verbatim as quoted in the fix report and in packages/db/README.md's 0270 section",
    "packages/db: node --test --test-concurrency=1 $GATES tests/firm-document-limits-writer.test.mjs -> 11/11 pass (cells 10 and 11 both present and green)",
    "packages/db: node --test --test-concurrency=1 $GATES tests/operation-census.test.mjs tests/rig-isolation.test.mjs tests/client-financial-pack.test.mjs tests/preview-invite.test.mjs -> 84 tests, 81 pass, 0 fail, 3 skip (documented T10b/T10b-AC2/T19 rig conditions)",
    "packages/db: node --test --test-concurrency=1 $GATES tests/delta-contract.test.mjs -> 60 tests, 58 pass, 2 fail, twice -- reproduces the fix report's own disclosed, non-lane, fresh-DB-only figure (delta-catalog-phase.mjs stale literal, file untouched by this diff)",
    "apps/web: node scripts/run-tests.mjs (whole unit suite) -> 4784 tests, 4782 pass, 0 fail, 2 skip -- matches the fix report exactly",
    "apps/web: node --import ./test/bootstrap.mjs --import tsx --test for lib/firm/capacity-doors.test.ts + components/firm-admin/processing-capacity-card.test.tsx + components/firm-admin/firm-settings-panel.test.tsx -> 16/16 pass",
    "apps/web: node --import ./test/bootstrap.mjs --import tsx --test components/firm-admin/compliance-register-panel.test.tsx -> 5/5 pass, including the new five-row roster-read-count=1 cell",
    "apps/web: node --import ./test/bootstrap.mjs --import tsx --test components/invite-accept-form.test.tsx -> 44/44 pass, including p872.web.issuer_lapsed_removed",
    "pnpm typecheck (worktree root) -> clean, apps/web and packages/runtime both Done",
    "pnpm lint (worktree root) -> exit 0, green",
    "CI=true GITHUB_ACTIONS=true pnpm lint (worktree root) -> ONE failure, freeze-lint selftest '(L05-STD-02 fix round) --ruling followed by another flag', reproduced; re-confirmed non-lane via git merge-base --is-ancestor e8eb79ba2 23cfad947b (true, predates base) and an empty git diff --stat for packages/runtime",
    "pnpm --filter @clara/web e2e firm-commercial-walk on the lane's own Playwright triple (3590/3591/3592) -> 10/10 pass, including the processing-cap cell",
    "git diff 23cfad947b...HEAD --stat -- docs/ARCHITECTURE.md docs/PRD.md -> empty (rule 5 respected)",
    "git log 23cfad947b..HEAD --oneline | grep 871 -> 0 (no #871 commits on the branch, confirming the stop)",
    "git show 23cfad947b:apps/web/components/documents/document-kind-dialog.tsx -> DOCUMENT_KINDS reference present at the lane base, confirming S-LANE-1's defect predates this lane",
    "code inspection: apps/web/lib/firm/capacity-doors.ts (newProcessingCapsOpKey, no caller id), apps/web/components/firm/compliance-watch-affordance.tsx + compliance-register-panel.tsx (resolver prop, hook hoisted), apps/web/messages/en.json (issuerLapsedNote reworded, capacityFieldInvalid names 2,147,483,647), packages/db/tests/preview-invite.test.mjs (stem-based readiness), packages/db/tests/retire-create-account-set-preintegration-gate.mjs + packages/db/tests/rig-meta.mjs RETIRED_0271_HUMAN_FNS + packages/db/package.json gate-chain entry, packages/db/README.md 0270/0271 sections, apps/web/lib/firm/capacity-doors.ts readCaps (key param removed)"
  ],
  "disposition_by_finding": [
    { "finding": "S-960-1 / ADV-L10-01", "ticket": 960, "status": "fixed", "note": "newProcessingCapsOpKey() mints a fresh crypto.randomUUID() per submission, no caller id folded in; db cell 10 drives set(8)->set(2)->set(8) under fresh keys (row moves, 3 audit rows) and under a repeated key (silent replay, vacuity control) -- both green live." },
    { "finding": "S-960-2 / ADV-L10-09", "ticket": 960, "status": "fixed", "note": "the op key is a bare uuid; there is no caller-id argument left to collapse to empty." },
    { "finding": "S-996-1 / L10-STD-01", "ticket": 996, "status": "fixed", "note": "useMemberNames hoisted to ComplianceRegisterPanel (and to WatchDispositionReceipt for the needs-you/tax mounts); WatchDispositionLine takes a resolver prop. New cell pins the roster-read count at 1 for a five-row register, green." },
    { "finding": "S-996-2", "ticket": 996, "status": "fixed", "note": "WatchDispositionReceipt holds its own useMemberNames call above its early returns again, restoring the needs-you/tax mount's pre-#996 network behaviour byte for byte." },
    { "finding": "ADV-L10-02", "ticket": 872, "status": "fixed", "note": "en.json's issuerLapsedNote no longer claims acceptance is unaffected; it states only what is true for every issuer_lapsed invite. New cell p872.web.issuer_lapsed_removed drives the removed-issuer case end to end and asserts the surface makes no such promise." },
    { "finding": "ADV-L10-03 / L10-STD-02 / S-1003-1", "ticket": 1003, "status": "fixed", "note": "retire-create-account-set-preintegration-gate.mjs, RETIRED_0271_HUMAN_FNS as an unconditional (frontier-bimodal) member of ALLOWED[clara_authenticated] with no cohort entry (by design, since above the frontier the function is truly absent), and the --import token in package.json's gate chain in migration order. operation-census/rig-isolation/client-financial-pack/preview-invite all green." },
    { "finding": "ADV-L10-04", "ticket": 872, "status": "fixed", "note": "preview-invite.test.mjs's two probes both read version ~ 'invite_issuer_lapsed_status$', not '^0269_'." },
    { "finding": "ADV-L10-05", "ticket": 960, "status": "fixed", "note": "card-side bound (DOOR_ARGUMENT_MAX) plus capacityFieldInvalid now names '1 to 2,147,483,647'; the raw-RPC residual is named and correctly deferred as a follow-up (redo refusal re-verified live)." },
    { "finding": "ADV-L10-06", "ticket": 960, "status": "no_change_needed", "note": "kept, with cell 11 proving the CLR13 arm reachable from an arranged op_receipts prestate row and citing the 18 other migrations sharing clara._reserve_op's contract; a defensible judgement call, not an open defect." },
    { "finding": "ADV-L10-07", "ticket": 960, "status": "fixed", "note": "recorded in packages/db/README.md's 0270 section (the applied migration's header cannot be edited); nothing in #960's AC was broken, and the estate-wide backstop is filed as a follow-up." },
    { "finding": "L10-STD-03", "ticket": 872, "status": "fixed", "note": "isNonBlockingPreviewStatus(status) extracted beside INVITE_PREVIEW_NON_BLOCKING_STATUSES; all three call sites in invite-accept-form.tsx use it." },
    { "finding": "L10-STD-04", "ticket": 960, "status": "fixed", "note": "readCaps's unused key parameter removed; the three-reader duplication itself is kept with a recorded reason, matching the reviewer's own 'minor judgement call, not required' scoring." },
    { "finding": "S-871-1", "ticket": 871, "status": "no_change_needed", "note": "#871 remains correctly out of this lane (confirmed: zero #871 commits on the branch); the stop's load-bearing claims re-verified (0 service_role hits in packages/db/migrations, no pg dependency in apps/web). Needs an orchestrator/owner decision, not an implementer -- exactly as the fix report and both review reports agree." },
    { "finding": "S-872-1", "ticket": 872, "status": "no_change_needed", "note": "ARCHITECTURE.md is untouched by design (WORK-ORDER rule 5 forbids editing it); confirmed via empty diff --stat. Recorded in CONTEXT.md and packages/db/README.md instead; correctly left as an orchestrator/Wayfinder call, not closed as met." },
    { "finding": "S-1003-2 / ADV-L10-08", "ticket": 1003, "status": "no_change_needed", "note": "re-reproduced: 58 pass / 2 fail, twice, on clara_l10; the single leaf is delta-catalog-phase.mjs's own stale deployed-count literal, a file untouched by this diff, and the suite's own top-level cell documents the fresh-DB-only contract. Correctly a note for the integrator, not a lane defect." },
    { "finding": "S-LANE-1", "ticket": null, "status": "no_change_needed", "note": "confirmed the DOCUMENT_KINDS defect predates the lane's base (git show at 23cfad947b); an integrator de-duplication call across wave-2 branches, not a lane-10 defect." },
    { "finding": "S-960-3", "ticket": 960, "status": "no_change_needed", "note": "both behaviours (no_cap_named refusal, per-firm advisory lock) remain disclosed in the migration header and README, and neither widens the door's reach." },
    { "finding": "S-872-2", "ticket": 872, "status": "no_change_needed", "note": "isKnownInviteStatus's dead-export status is deliberate and documented in lib/members/reads.ts's header; no change owed." }
  ],
  "newly_broken": [],
  "still_open": [],
  "notes_for_integrator": [
    "The CI-form lint failure (freeze-lint selftest, '(L05-STD-02 fix round) --ruling followed by another flag') reproduces on this head under CI=true GITHUB_ACTIONS=true and is re-confirmed pre-existing: it predates the lane's base (git merge-base --is-ancestor e8eb79ba2 23cfad947b -> true) and the lane's diff touches zero packages/runtime files. It will be red on the runner for every wave-2 lane and needs an owner, as both the fix report and the standards review already say.",
    "#1003's delta-contract.test.mjs AC5 cannot be re-proved on clara_l10 (58/2, reproduced twice); it needs a fresh, never-ceremonied database, and delta-catalog-phase.mjs's stale deployed-count literal is worth its own ticket, as already filed in the fix report's follow-ups.",
    "The five follow-ups the fix report lists (widen the four cap parameters to bigint; an estate-wide concurrency backstop; the delta-catalog-phase.mjs stale literal; the docs/ARCHITECTURE.md invites subsection; the CI-form freeze-lint selftest fix) remain accurate and are not superseded by anything found in this recheck.",
    "#871 and the S-872-1 ARCHITECTURE.md deviation both still need an orchestrator or owner decision, not an implementer; nothing in this recheck changes that."
  ],
  "summary": "Every finding across the three review reports (11 spec, 4 standards, 9 adversarial -- 12 distinct defects after de-duplication) is either fixed and independently re-verified on the current head (ad1c2ce33), or was already correctly a non-defect record needing an orchestrator/owner call rather than lane code. The op-key replay blocker (S-960-1/ADV-L10-01) is closed with a fresh-uuid-per-submission door and a db cell that drives set(8)->set(2)->set(8) under both fresh and repeated keys, verified green. The per-row roster-read fan-out (S-996-1/L10-STD-01) is closed with a hoisted useMemberNames and a five-row count=1 cell, verified green. The false-reassurance invite notice (ADV-L10-02), the migration-number-keyed gate (ADV-L10-04), and 0271's missing gate/cohort/chain-entry triad (ADV-L10-03/L10-STD-02/S-1003-1) are all closed and independently re-verified, including a live re-run of the #957 redo-refusal claim and the full db gate chain (operation-census, rig-isolation, client-financial-pack, preview-invite: 81/84 pass, 3 documented skips). The two named residuals (raw-22003 above int4 max, per-firm vs estate-wide ceiling) are correctly left as disclosed follow-ups, not defects. Nothing new is broken: typecheck is clean, pnpm lint is green, the whole apps/web unit suite reproduces the reported 4782/4784 figure exactly, the e2e firm-commercial-walk reproduces 10/10, and the one CI-form lint red is re-confirmed to predate this lane. Accept."
}
