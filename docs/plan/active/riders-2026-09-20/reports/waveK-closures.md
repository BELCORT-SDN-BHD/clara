# Riders closing wave K, the closures

Written 2026-09-26 by the closures worker. **Eight tickets closed, two filed, nothing else written
to GitHub.** Nothing was pushed, no pull request was opened, no code was edited, no worktree but the
main checkout was touched, no subagent was spawned and no process this worker did not start was
killed. The only file written in the repository is this one; everything else lives in
`scratchpad/release-wK/closures/`.

Every comment opens with `> *This was generated by AI during triage.*`, carries the identical hosted
evidence block, contains **no em dash** and **no closing keyword**. Each close was done by hand with
`gh issue close --reason completed` inside the idempotent poster, never by GitHub's own keyword
handling. The poster is `scratchpad/release-wK/closures/post.cjs` and its state file is
`posted.json`; it refuses to send a body that fails any of those checks.

---

## 1 · The eight

| ticket | lane | migration | comment | state now | how the comment reads |
|---|---|---|---|---|---|
| **#1144** | LC | none, the version cut | [5843876383](https://github.com/BELCORT-SDN-BHD/clara/issues/1144#issuecomment-5843876383) | CLOSED / completed | DONE. The seven tools in `chatTurn_v23` and the `claraWork_v7` step, with the **three departures accepted explicitly** and the six successor contracts re-parented to #1155 |
| **#1145** | L3 | none | [5843876732](https://github.com/BELCORT-SDN-BHD/clara/issues/1145#issuecomment-5843876732) | CLOSED / completed | DONE. The bootstrap command's durable home in the root README, with no ephemeral path inside the paragraph |
| **#1147** | L1 | `0362` | [5843877129](https://github.com/BELCORT-SDN-BHD/clara/issues/1147#issuecomment-5843877129) | CLOSED / completed | DONE for the two halves built, the third held by `p1147.asymmetry.census`, and the **owner ruling recorded** |
| **#1148** | L1 | `0363` | [5843877462](https://github.com/BELCORT-SDN-BHD/clara/issues/1148#issuecomment-5843877462) | CLOSED / completed | DONE. Already closed at merge by a keyword in its own commit; this comment is the evidence record |
| **#1149** | L2 | none | [5843877658](https://github.com/BELCORT-SDN-BHD/clara/issues/1149#issuecomment-5843877658) | CLOSED / completed | DONE. One `CLR` map, 46 codes, every meaning read off its allocating migration |
| **#1150** | L2 | `0364` | [5843878078](https://github.com/BELCORT-SDN-BHD/clara/issues/1150#issuecomment-5843878078) | CLOSED / completed | DONE. Four namespaces, the third on-behalf-of plan body folded, two within-lane pairs watched rather than dropped |
| **#1151** | L3 | none | [5843878387](https://github.com/BELCORT-SDN-BHD/clara/issues/1151#issuecomment-5843878387) | CLOSED / completed | AC1 to AC3 PASS, **AC4 PARTIAL and disclosed**, with the CI measurement that narrows it |
| **#1152** | L3 | `0365` | [5843878658](https://github.com/BELCORT-SDN-BHD/clara/issues/1152#issuecomment-5843878658) | CLOSED / completed | DONE. The register pages, the side filter reaches the database, #1075's second criterion is true |

**#1148 was already CLOSED before this run**, at 06:04:52Z on 2026-09-26, `state_reason: completed`,
by a closing keyword in its own commit `e210b1db3` when PR #1154 merged. The poster detected it
(`gh issue view --json state`), commented, and did **not** issue a second close. It is recorded in
`posted.json` as `"closed": "already closed before this run"` rather than as this worker's act.

**The leave-open set is empty, deliberately**, which matches wave 4's, the cut's and the sweep's
final convention: PARTIAL is wording in the comment, not a reason to leave the issue open. #1151's
AC4 gap and #1147's deliberately unbuilt third half are both named in full in their own comments,
and each has somewhere to live.

### The three closures that must not read as plain DONE, and how each reads

- **#1147** carries the owner ruling as its own headed section, quoted as the ruling was given and
  attributed to the owner's delegation of 2026-09-23: withdrawing a firm standing instruction does
  NOT pause the plans it authorised, each plan is a separate already-authorised commitment a person
  pauses or ends per plan (Client to Plans), which is how #940 ruled a retired roster enrolment, the
  withdraw door now answers `plans_still_posting` so the firm sees the count, **recorded as built**.
  The comment also carries the two rulings the same ticket records and does not take, still open.
- **#1151** states AC4 as PARTIAL before it states anything that narrows it, names the residual
  (`intake-e2e.mjs` not driven end to end with the stranger-row contamination the fix exists for),
  and only then adds the release's own measurement.
- **#1144** lists the three departures as a numbered block headed ACCEPTED EXPLICITLY, which is what
  `waveK-lane04-fix.md` section 6 asked the integration record to do rather than let them pass as
  discharged.

---

## 2 · The two filed

| ticket | labels | what |
|---|---|---|
| **#1155** [Successor contracts owed to the cut after the riders closing wave](https://github.com/BELCORT-SDN-BHD/clara/issues/1155) | `enhancement`, `ready-for-agent` | The six contracts of `waveK-merge.md` section 6, each with its report path: the two chat tools over `0362` and `0363`, the `for key share` lock in the prepayment schedule core's wake arm, the `CLR44` comment correction, the **two tenancy confirmation cores' replay marker** with the note that `0364` rewrote both in this same wave without adding it, and `clara.get_document_state`'s missing date, memo and total. It records that L2 and L3 owe none, with their evidence. It ends by saying the work belongs to the #597 mainline's next version cut and is re-parented there. |
| **#1156** [`collation-pin-scan.mjs` is blind to a bare `order by 1` over a text expression](https://github.com/BELCORT-SDN-BHD/clara/issues/1156) | `bug`, `ready-for-agent` | From `waveK-ci-reds.md`: the scanner's integer-key regex classifies a lone digit as free, which is right for an integer key and wrong for a positional reference to a text column. That is how the `p1148.acl.census` red shipped to the runner. Carries the fix the report proposes (a bare digit is `unresolved`, the answer the module already gives an alias), the second site at `standing-instruction-agent-read.test.mjs:272` which is not a red today, and four acceptance criteria that loosen nothing. |

One line on **#597** points at #1155:
[comment 5843829972](https://github.com/BELCORT-SDN-BHD/clara/issues/597#issuecomment-5843829972).

**Nothing else was filed.** Three candidates were deliberately left as text in the closure comments
rather than turned into tickets, because the brief says to file exactly two: the five `chatTurn_v23`
tools with no World leg of their own, `STD-2`'s cosmetic env block, and the `CONTEXT.md` entry the
firm standing instruction still lacks.

---

## 3 · Anything unverified

- **The main-branch CI run at the merge commit was still in progress** when these closures were
  posted. Run `36222675119` at `111196753` had `lint`, `changes`, `build`, `render-drill`,
  `db-split-partition-total` and `storage-policy-battery` green, with `db-estate` and
  `db-live-gates` still running. What IS complete and green in every job is PR #1154's final CI run
  **`36221238555`** at `fb0392d7b`, the post-fix branch head, and that is the run the #1144, #1148
  and #1151 comments cite.
- **The hosted evidence block is carried, not re-measured.** This worker holds no hosted access and
  ran no `fly` command, no hosted read and no database command of any kind. Every figure in the
  block comes from the orchestrator's release record and the runbook; the one thing that was read
  first-hand is the rollback preflight verdict, out of
  `scratchpad/release-wK/rollback-preflight.log`, which reads ALLOWED over 0 non-terminal workflow
  runs and 0 live tasks bound to no run.
- **The CI measurement quoted in #1151's comment is from the PR's own log**, `scratchpad/wK/
  ci-db-live-gates.log`, at the pre-fix head `11b82ea57`: `INTAKE E2E: PASS` at line 2189,
  `INTAKE ADMISSION E2E: PASS` at 2340, and `intake-batch-e2e: ALL LEGS PASSED` with its receipt
  census at 6387 and 6503. All three intake legs sit ahead of the v22 leg in the action, so none of
  them was affected by the abort. Neither CI fix touches any of those three files, so the green
  carries to the released tree; that is an argument from an empty diff, not a second run.
- **No lane claim was re-verified.** This worker read the lane reports, the fix and recheck reports,
  the merge report, the release preparation, the gate reports and the CI-reds report, and wrote what
  they record. The only independent measurements taken here are GitHub state, the two CI run
  listings, the CI log greps above, the preflight log and the `#1148` timeline that shows who closed
  it and when.
- **No mid-task status request arrived.** Had one, it would have been noted and left to the
  orchestrator.
