# Brief: #643 — periodic stock adjustment and supplied payroll/statutory-obligation bookkeeping

## Orchestrator decisions (binding)
- Migration number: **0194** (`0194_periodic_adjustments.sql`). 0188–0193 belong to other in-flight tickets and will be absent locally — expected. Note that #631 (0195, later) will recut `_record_journal_entry_core` again on top of YOUR recut: keep your recut a clean full-body copy with clearly delimited, commented insertion points, and keep the pre-state sha pin idiom so the next recut can pin against your body.
- THIS PR = DB + web + the direct-Accounting and upload/reference entry points (`workRoutes.ts` sibling route). **No `chatTurn_v19` in this PR**: the chat tool `start_periodic_adjustment_work` is delivered later in ONE shared chatTurn_v19 (it also carries another ticket's tool). To make that trivial, ship the non-frozen pieces now: the discriminated-union zod schema for the two typed bases and the `basisFromInput`-style builder in a non-frozen module (`packages/runtime/lib/periodic-adjustment-basis.mjs` or .ts), unit-tested, and document in your report exactly what v19 must wire.
- No claraWork v3 here; the frozen run stays byte-identical; typed particulars live in the new frozen column `accounting_work.adjustment_basis`, never inside `basis`.
- Salaries Payable / staff-advance accounts: NO chart template change. The liability/advance/payment accounts are supplied particulars (`liability_account_code` etc.); the form defaults `liability_account_code` to the template's statutory account for `epf/socso/eis/pcb_mtd/hrdf` kinds (2100–2140) and to `2020 Accruals` for `salary`/`other_supplied`; every default is editable and shown.
- #721: the honest shape — the form and (later) the chat lane ask BEFORE admission; do not claim a post-admission Work question can complete the basis. State it.
- Naming: `clara.periodic_adjustments` (never `adjustment_*` — that is the 0045 lane / #640's territory). New web leaf `/clients/:id/accounting/adjustments/new` + an `ACCOUNTING_ITEMS` history row `periodicAdjustments`; do NOT reuse `registers?tab=adjustments`.
- Never invent rates or thresholds; every number is a supplied particular.

## 1. Current state
AC1: nothing exists. Close gate `closing_stock_present` (drawer 2, attestable, `applies_when='goods_trading'`, `0056_wave_e_close_model.sql:400`), evaluator `_close_gate_closing_stock` (:1277) measures `je.flags ? 'closing_stock'` on a live approved entry inside the FY (:1300-1303) and returns `'no_producer_verb', true` (:1308-1312, "Drop this key when the producer verb ships"); no writer of that flag exists; `journal_entries.flags` added 0009:851; `_record_journal_entry_core` never sets flags. Chart: `1200 Inventory / Stock`, `5000 Purchases`, `5020 Opening Stock`, `5030 Closing Stock`, `5040 Cost of Sales` (`0150_coa_template_pr_a.sql:1476,1551-1555`, `account_class = null`). `client_facts` key `trade_nature` drives applicability (0056:1284-1291).
AC2: no operation. Accounts shipped: `2100 EPF`, `2110 SOCSO`, `2120 EIS`, `2130 PCB (MTD)`, `2140 HRDF` (0150:1505-1509), expenses `6000 Salaries and Wages`, `6010-6040` (0150:1564-1568), `statutory` annotation column (0150:1617-1625). No Salaries Payable, no staff-advance account in the starter template (nearest `2010 Other Payables`, `2020 Accruals`, `1110 Other Receivables`). Staff-advance subledger exists: `0043_wave_d_b1_staff_advances.sql` (`enrol_staff_advance_account:1946`, `complete_staff_advance_particulars:2188`, `book_staff_advance_application:2513`, `staff_advance_tie:3366`; agent door 0129:563).
Control-leg refusal: `_record_journal_entry_core` step 7 (live body `0184_work_cancel_ordering.sql:1066-1078`; origin 0178:1367-1376) refuses `account_class in ('payable','receivable')` with CLR10 `generic_control_leg`; the CHECK admits only those two or null (0015:199-200) — so inventory and statutory payables do NOT trip it. Belts that bite: `t_period_wall` (CLR19 `write_into_closed_period`), `t_je_subledger_belt` (0037:1447), FA/advance belts (`_adv_on_approve`). Cells `w623.post.control-leg`, `w623.post.closed-period` in `packages/db/tests/work-journal-post.test.mjs:515,543`.
`purpose`: two CHECKs `in ('journal_entry')` on `accounting_work` (0178:305) and `operation_receipts` (0178:416); `purpose` is in the frozen array of `_tf_accounting_work_immutable` (0184:449); logical identity literal `'work:' || id || ':journal_entry:1'` (0178:914, 0182:726); the core filters `aw.purpose = 'journal_entry'` under `for update` (0184:879-882).
Entry points: direct Accounting `/clients/:id/accounting/journal/new` (`tree.ts:348,362-364`), composer `components/accounting/journal-composer.tsx`, `POST /api/work/journal` (`packages/runtime/src/workRoutes.ts:465`) → `clara.admit_journal_work` (:505); chat frozen tool `start_journal_work` (`chatTurn.v18.tools.ts:52`, `.strict()` four keys :63-71, `basisFromInput` :129-142, 7-arg admit :163); upload/reference via `source_refs` `{kind:'document'}` validated by `_assert_journal_source_refs` (0182:496), re-read at commit (0184:1082-1101), `entry_evidence_links` (0182:301), late attach `attach_entry_evidence` (0182:1055). All converge on `claraWork_v2` reading the Work row (`claraWork.v1.impl.ts:137-162`) and echoing `basis` into the frozen strict `record_journal_entry` tool (`claraWork.v1.tools.ts:95-105`) → `wake_record_journal_entry`.
Tests: DB `work-journal-admission.test.mjs`, `work-journal-post.test.mjs`, `journal-work-evidence.test.mjs`, `work-cancel.test.mjs`, `work-question.test.mjs`, `spoken-for-documents.test.mjs`; `rig-meta.mjs:1519` cohort. Runtime `work-journal-e2e.mjs` (8 legs), `work-journal-db.test.mjs`, `work-routes-unit.test.mjs`, `work-bundle.test.mjs`. Web `journal-composer.test.tsx`, `work-detail.test.tsx`, e2e `manual-journal-walk`, `journal-work-walk`, `journals-table-walk`.
AC4 today: `work.purpose` renders raw at `work-detail.tsx:579`; Journals link row maps only `journal_entry` (:497); Activity label map has one key (`en.json:4114`). Close readiness re-measured per run (`get_close_readiness` 0056:2618; `_measure_one_gate` recut 0161:378). One-effect under duplicate/lost-ack/restart is structural (`uq_operation_receipts_committed` 0178:448; `_reserve_op`; `for update` boundary 0184:876).

## 2. Gaps / rows
AC1/AC2 whole operation missing. AC3 direct + upload reachable via a new non-frozen admission verb + route; chat needs v19 (later PR); missing-basis question blocked by #721 (ask before admission). AC4 purpose labels; close readiness via gate measuring pass + dropping `no_producer_verb`. AC5 no representative cases.
C-29 to build then verify (producer must carry the marker + typed basis, not an anonymous balancing JE). C-49 split: connected filing / payroll-document ingestion / full payroll processing out of scope; supplied-obligation bookkeeping to build (AC2).

## 3. Slice (one PR)
Migration `0194_periodic_adjustments.sql`: (1) widen both purpose CHECKs to `('journal_entry','periodic_stock_adjustment','payroll_obligation')`. (2) frozen column `clara.accounting_work.adjustment_basis jsonb` (CHECK null iff `purpose='journal_entry'`), added to `_tf_accounting_work_immutable`'s frozen array; `_journal_basis_digest` formula unchanged; add the typed half to the intent-payload comparison via `clara._adjustment_basis_canonical` exactly as 0182 added the source-refs half (0182:676-683). (3) `clara._assert_adjustment_basis(p_purpose, p_adjustment)` field-scoped like `_assert_journal_basis`: `periodic_stock_adjustment` = `{period_start, period_end, method: 'opening_closing_count'|'explicit_adjustment', opening_cents?, closing_cents?, counted_at?, count_reference?, inventory_account_code, cost_account_code, adjustment_cents, currency:'MYR', instruction}`; `payroll_obligation` = `{period_start, period_end, obligation_kind: 'epf'|'socso'|'eis'|'pcb_mtd'|'hrdf'|'salary'|'other_supplied', expense_account_code, liability_account_code, advance_account_code?, payment_account_code?, amount_cents, currency:'MYR', particulars_source, instruction}`. (4) `clara.periodic_adjustments` (`id, firm_id, client_id, work_id, logical_op_id, purpose, period_start, period_end, basis jsonb, amount_cents, currency, entry_id, receipt_id, source_document_id, corrects_adjustment_id, corrected_by_adjustment_id, recorded_by, on_behalf_of, created_at`; FORCE RLS owner + `firm_id = clara.jwt_firm()` read; zero app-role DML; append-only + no-truncate — the `operation_receipts` idiom 0178:457-468); written only inside the commit transaction. (5) Admission: extract `clara._admit_accounting_work_core` from `admit_journal_work` (0119 extraction idiom; prove the public verb's prosrc re-derives) and add `clara.admit_periodic_adjustment_work(p_client, p_author, p_intent_key, p_purpose, p_basis, p_adjustment, p_basis_origin, p_source_refs, p_model)` granted to `clara_runtime`; `admit_journal_work`'s 7-arg signature must not move; `logical_op_id` = `'work:'||id||':'||purpose||':1'`. (6) Commit: recut `_record_journal_entry_core` (third full copy) to widen the purpose filter, call `clara._assert_adjustment_relationships(p_client, purpose, adjustment, canon_lines)` (payload-shaped arms before `_reserve_op`, world-shaped after — 0182's rule), stamp `flags` on the draft INSERT (`closing_stock` / `payroll_obligation`), insert the `periodic_adjustments` row, add `adjustment_id` to `effects`. (7) Refusals with typed `detail.reason`: inventory leg active asset + cost leg active expense, client-owned; payroll expense/liability legs exist and liability is a liability; `advance_account_code` must name a live staff-advance enrolment (`clara.staff_advance_accounts`) else `advance_not_enrolled`; `adjustment_all_zero`; `scope_overbroad` (period spans more than one FY or ends after the client's FY end); `stale_basis` (`counted_at` outside the period, or cited document no longer filed — reuse `_journal_document_filed`); `authority_lost`/`insufficient_role` inherit CLR04 arms; locked period inherits `t_period_wall` CLR19 plus a typed pre-check. (8) Recut `_close_gate_closing_stock` to drop `no_producer_verb` and name the producing row; test that `measured_digest` moves (invalidating interim attestations — intended, user-visible). (9) Correction: linked new Work (`corrects_adjustment_id`), reversal via `clara.reverse_entry` (0004:560); `t_entry_evidence_release` frees the document. (10) Reads: `clara.list_periodic_adjustments(p_client, p_from, p_to)`; extend `list_entry_links`' purpose projection. Tail assertions, preintegration gate, `rig-meta.mjs` cohort entries.
Runtime: `workRoutes.ts` sibling route `POST /api/work/periodic-adjustment` → `admit_periodic_adjustment_work`; the non-frozen basis schema/builder module; unit tests; a runtime e2e leg (sibling `periodic-adjustment-e2e.mjs`, standalone, skip-clean when 0194 absent, wired after `work-cancel-e2e` in db-live-gates): admit via the route, SIGKILL after commit before checkpoint, resume, exactly one entry/receipt/`periodic_adjustments` row.
Web: leaf `periodicAdjustment` (bookkeeper floor) at `/clients/:id/accounting/adjustments/new` + `ACCOUNTING_ITEMS` row `periodicAdjustments` (history table with exact fields, sources, Work/JE/receipt links); the form reuses `JournalBasisFields`, the evidence chooser, `lib/work/journal-draft.ts`'s intent key and the composer's lost-response arm, with a named type switch and typed particular fields; purpose labels at `work-detail.tsx:579`, `:497`, `en.json` (Activity + Work + Journals). e2e `periodic-adjustment-walk.spec.ts` + mock (one-line hook in serve-built.mjs).
Docs: ARCHITECTURE §4 (ticket log paragraph; the `#623 形状` sentence extended to three purposes), §8 (closing-stock producer exists; `no_producer_verb` retired), §11 row 会计能力; CONTEXT.md **Periodic adjustment**, **Supplied obligation particulars**; PRD scope note.
Excluded: full payroll processing, payroll-document ingestion, connected filing, perpetual inventory, invented rates.

## 4. TDD seams (red first)
1. `packages/db/tests/periodic-adjustment.test.mjs` pa.stock.happy — admit `opening_closing_count`, claim, post under a real `interactive_client` credential OBO the initiator; one approved entry with `flags ? 'closing_stock'`, one receipt `purpose='periodic_stock_adjustment'` with `effects->>'adjustment_id'`, one `periodic_adjustments` row with exact cents, `accounting_work.result.entry_id` matching.
2. pa.payroll.relationships — `advance_account_code` with no live enrolment refuses CLR10 `advance_not_enrolled`; entry and committed-receipt counts unmoved.
3. pa.refusals — table-driven: `adjustment_all_zero`, `scope_overbroad`, `stale_basis`, CLR19 on a seeded closed FY (copy `work-journal-post.test.mjs:517-527`), CLR04 `obo_not_initiator`.
4. pa.correction — post, reverse, admit corrected Work; two rows linked by `corrects_adjustment_id`/`corrected_by_adjustment_id`; document released; two committed receipts under two logical identities.
5. `close-closing-stock-producer.test.mjs` cs.gate.flips — goods-trading client without marker measures `fail` with no `no_producer_verb`; after a committed adjustment measures `pass` naming it; prior attestation no longer effective (`measured_digest` moved).
6. Runtime e2e leg (kill/resume) + `apps/web/components/accounting/periodic-adjustment-form.test.tsx` (server field errors map to focusable controls via `fieldForServerPath`; all-zero and stale render as `StateBanner`) + `apps/web/e2e/periodic-adjustment-walk.spec.ts` (320 px, 200% zoom, keyboard/focus return, stable URL/Back, preserved draft).

## 5. Risks
Digest/echo fidelity (never move particulars into `basis`). Attestation invalidation when `no_producer_verb` drops. Coordinate purpose labels with #641 (it is restructuring `work-detail.tsx`; keep your edits to the label maps).

## 6. Effort: L. Rig: PG cluster (assigned) + real Postgres World for the e2e leg, web unit + Playwright.
