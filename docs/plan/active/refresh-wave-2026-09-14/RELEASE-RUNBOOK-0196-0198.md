# Hosted release ceremony — 0196 · 0197 · 0198 (#692 #718 #720 Half 1) + runtime image `refresh-3486b6c2`

Second ceremony of 2026-09-14/15, owner-authorised in session ("都做完"). Follows `RELEASE-RUNBOOK.md` (the 0188–0195 ceremony) in shape; everything below was re-derived from the three files, the three tickets and PR #822 by five readers, then corrected by a three-critic panel (locks / obligations / ceremony) before the window.

RELEASE_SHA = `3486b6c2` (`main` after PR #823; carries PR #822 = #692 #718 #720). The DEPLOYED image is `refresh-70c731ef` (v83, digest `sha256:28a2f13d…`). Between the two SHAs, besides docs and tests: `packages/db/migrations/0196…0198`, a doc-comment + log-wording edit in `packages/runtime/lib/control.mjs` (`past-due work question(s)` → `past-due clarification(s)`), `frozen-workflows.json` (87f6d5ff stamps `deployed: true` on the 17 v3/v19 entries — flags only, no `sha256` moved; the file is not COPYed into the image) and `packages/db/package.json` (one added preintegration-gate import). No `FRONTIER_BODY_RULES` entry, no `apps/web` source. `check-frozen-workflows.mjs` at 3486b6c2: OK, 281/281 already deploy-locked, so `--lock-deployed` is a NO-OP this ceremony and is dropped deliberately, not forgotten. **#692 and #718 are DB-only. #720's acceptance criteria include the runtime wrapper's re-worded doc comment and log line, which ship with the image — the runtime release is #720's, not a courtesy.** No web release (version stays `0290977b…`).

**Secrets rule** (unchanged from `RELEASE-RUNBOOK.md`): the fly token and the DSN are substituted INLINE inside one pipeline only — never assigned to a shell variable, never echoed, never in argv. Every `flyctl` below is shorthand for `FLY_API_TOKEN="$(grep -E '^access_token:' ~/.fly/config.yml | awk '{print $2}')" flyctl <cmd>`; every `<dsn pipe>` is `flyctl ssh console -a clara-runtime --machine <id> -q -C "sh -c 'printf %s \"\$WORKFLOW_POSTGRES_URL\"'" </dev/null 2>/dev/null | tr -d '\r\n' | node scripts/ops/dsn-pipe.mjs -- <cmd>`. From Git Bash, a `wsl` argument that is a `/mnt/c` path needs `MSYS_NO_PATHCONV=1`. The DSN login is `postgres` (Supabase's, `rolsuper=false`, reads every firm's `audit_log` — verified below).

## Rollback points BEFORE (from the ledger)
DB **190 / 0195** (all 190 ledger checksums equal their files on disk; 0187 `5fc28e38…` MATCH as the carried-forward spot check). Runtime **v83** = `registry.fly.io/clara-runtime@sha256:28a2f13d6819517fcaab98aaa7850e07bdd9b3c149f50f96ec1d96432b12bdba`. Web `0290977b-74a4-4c4a-849f-ee60efd631bb` (unchanged by this ceremony).

## What the three files do (readers' findings, verified against source)
- **0196** (#692): recuts the live BEFORE-INSERT trigger body `clara._tf_firm_document_limits_upsert()` (0007 body, pinned sha `718eaecd…`) to column-preserving `coalesce(new.<col>, old.<col>)`, and drops the four column DEFAULTs (100/1000/2/2) under **ACCESS EXCLUSIVE** on `clara.firm_document_limits` (`set local lock_timeout='5s'`, `statement_timeout='5min'`, both transaction-scoped). Writers: only owner-level hands (no application role holds INSERT/UPDATE/DELETE; verified by grep of runtime + web). **Readers the header does not name:** `claim_document_processing_task` and the ingest-reservation definer functions LEFT JOIN this table (ACCESS SHARE) every ~2 s from the runtime's document reconciler — runtime-only callers; `clara_authenticated` also holds a direct SELECT grant (relacl) though nothing in `apps/web` names the table. A waiting ALTER aborts at 5 s (`55P03`), rolled back, ledger unchanged, re-runnable. Prestate pins: 7 columns at exact nullability/default, the 0007 four triggers, `tgtype=7`, proacl `{clara_fn_owner=X/clara_fn_owner}`, relacl `{clara_fn_owner=arwdDxtm/clara_fn_owner,clara_authenticated=r/clara_fn_owner}`, zero `control_witnesses` rows naming the body (D2).
- **0197** (#718): recuts NO body. Adds `_lock_document_binding(uuid)`, `_tf_source_binding_wall()`, `_tf_evidence_link_binding_wall()` and three triggers (`t_source_binding_wall_ins/_upd` on `clara.journal_entries`, `t_entry_evidence_links_binding_wall` on `clara.entry_evidence_links`); CREATE TRIGGER takes **SHARE ROW EXCLUSIVE**, which conflicts with the ROW EXCLUSIVE of every INSERT/UPDATE on those tables (`lock_timeout='5s'`). Header: "owes NO consumer-first obligation and NO writer-quiescence window … Its rollback is a successor migration dropping the three triggers." Prestate pins `_document_posting_entry(uuid,uuid)` at `8ba5e67f…` and refuses if either coding core already reads `entry_evidence_links`. **Behaviour after:** a second coded posting on a document that already holds a live link / approved entry is refused (`CLR13 source_already_posted`); legacy double claims are left as they are — counted BEFORE (PR #822's probe) and recorded on #718.
- **0198** (#720 Half 1): `create or replace` of `clara.expire_due_interruptions(int,uuid)` removing the one arm `and work_id is not null` — the chat lane's 14-day clarifications now expire too. No table lock; **no `lock_timeout` of its own** (only `statement_timeout='5min'`): a lock wait there runs to 5 min and aborts `57014`, not `55P03`. **Deploy-order-agnostic:** the deployed v83 image is lane-blind (`control.mjs` calls the one verb every 2 s control tick, batch 50, firm null) and would sweep chat rows the moment 0198 commits — the behaviour change ships with the MIGRATION, not with an image. **§R release note** (queries verbatim in `ceremony/reads-0196-0198.mjs`): BEFORE the first sweep, in autocommit with the sweep's own `clock_timestamp()` cutoff —
  ```sql
  select count(*)::int as chat_clarifies_past_due,
         min(expires_at) as oldest_deadline,
         count(distinct firm_id)::int as firms_touched
    from clara.agent_interruptions
   where status = 'pending' and work_id is null and expires_at < clock_timestamp();
  ```
  AFTER the first sweep, the durable witness, with `<the deploy instant>` = **0198's own `applied_at` from `clara.schema_migrations`** (DB clock; the machine is stopped for the whole migration, so it is a safe lower bound — never a client wall clock read after `machine start`, which can post-date the first 2 s sweep) —
  ```sql
  select count(*)::int as chat_rows_expired_by_the_first_sweep
    from clara.audit_log
   where fn = 'expire_due_interruptions'
     and args->>'work' is null
     and at >= '<the deploy instant>'::timestamptz;
  ```
  and the one number #764 needs, counted right after the first cycle —
  ```sql
  select count(*)::int as chat_rows_delivered_onto_a_still_parked_turn
    from clara.agent_interruptions i
    join clara.agent_tasks t on t.id = i.task_id
   where i.work_id is null
     and i.status = 'expired'
     and i.delivery_state = 'delivered'
     and t.status = 'awaiting_input';
  ```
  **What a zero backlog does NOT prove:** audit rows are one per SWEPT row, the NOTIFY fires per NON-EMPTY sweep, and `control.mjs` logs only when `expired > 0` — with 0 rows the sweep leaves no trace, so zeros AFTER witness nothing about the loop. The positive witness that 0198 is live is its own `#720 tail: OK` notice (`work_id is not null` GONE) plus `packages/db/tests/chat-clarify-expiry.test.mjs`; say so on #720, and on #764 that its number is 0 because the backlog was 0, not because the asymmetry was exercised.

## Gates before the window
- **Build tree** = RELEASE_SHA `3486b6c2`, `git status --porcelain` EMPTY at image-build time (2026-09-14T17:21:57Z) — verified. This runbook and `ceremony/reads-0196-0198.mjs` are committed to `main` by the landing PR after the window (they do not enter the image).
- **Rehearsal** on `rig195` (pristine chain to 0195 from a worktree at `70c731ef`, WSL port 55461, db `clara_195`): `migrate.mjs` from the `main` checkout → `3 new migration(s) applied · 193 total`, every prestate/tail notice printed, **wall < 1 s** (T). §R readings 0 before and after.
- **Probe machine** `7817642f059718` (image `refresh-70c731ef`, `CLARA_START_WORLD=0`, `sleep infinity`; the control loop does not run there) is the DSN source for steps 2–4; destroyed at the end of step 4, before the deploy (a deploy would try to roll it). The LIVE machine's own pipe (`48ee715b763048`) was rehearsed pre-window with the same identity/ledger/audit read and works — it is the DSN source for steps 6–7.
- **Phase B reads** — `node docs/plan/active/refresh-wave-2026-09-14/ceremony/reads-0196-0198.mjs --prod` through the probe (the script is tracked; tested CLEAN on `clara_w3`@0195, 10-STOP on `rig195`@0198, and `--prod` STOPs on any rig). **BINDING IDENTITY:** the `== server ==` line must print `db=postgres`, port 5432, a non-loopback pooler address, `usr=postgres`; a `127.0.0.1` / `clara_195` / `clara_w3` line is a RIG and the run is discarded. Production → **CLEAN** (2026-09-14 ~17:55Z): ledger 190/0195; **all 190 ledger checksums equal their files** (migrate.mjs aborts the whole run on any one mismatch); exactly 0196/0197/0198 on disk beyond the ledger; all six bodies at their reference sha; `firm_document_limits` 7 columns at 100/1000/2/2, the 0007 four triggers, ACLs pinned, 0 rows; **`clara.control_witnesses` EMPTY table-wide** (covers `expire_due_interruptions`, which 0198 never checks); 0197 walls/triggers absent, `is_opening_balance` NOT NULL, `t_je_immutable` + `t_period_wall` present; exactly one `expire_due_interruptions` body with 0180's arms; **§R read-access probe:** `audit_log` 1,857 rows across 5 firms readable as `postgres`, 0 `expire_due_interruptions` rows yet, the #764 join readable; quiescence: `accounting_work` non-terminal none, `agent_tasks` only the ten held `wake` orphans (#820), one stale `clientOnboarding_v4` run, no lock on the three tables, no holder of the F10 advisory lock.
- **#718 legacy double claims** (PR #822's probe, verbatim, read pre-window through the probe): **0 rows** (denominators 11 documents / 4 approved entries with a document / 2 live evidence links) → recorded on #718's evidence comment.
- **0198 §R backlog** (pre-window, informational; the BINDING reading is step 2): `chat_clarifies_past_due = 0`, `oldest_deadline = NULL`, `firms_touched = 0`; `agent_interruptions` = 7 answered + 1 cancelled chat rows, no `pending` row in either lane. Expected: the first sweep expires **0** rows; #764's number **0**.
- **Image** — PUSHED 2026-09-14T17:24:06Z: `flyctl deploy --config packages/runtime/fly.toml --build-only --push --image-label refresh-3486b6c2 --build-arg CLARA_BUILD_SHA=3486b6c24caea94f3847313184b92b112525dccb` → `registry.fly.io/clara-runtime:refresh-3486b6c2` = manifest digest **`sha256:b6dd2fa56525e04aa6fab2c27235ca419aa40f32dfb61af518b53ed42970b79e`** (265 MB); release by `registry.fly.io/clara-runtime@sha256:b6dd2fa5…` only.
- **DB RESTORE POINT — TAKEN 2026-09-14T17:24:27Z** (before any write; NOT a read-only step): `backup.mjs --profile full` through the probe's DSN into WSL (the WSLENV form recorded at `RELEASE-RUNBOOK.md` step 3f) → `clara-clara-graphile-worker-workflow-workflow-drizzle-2026-09-14T17-2*.sql` (214,145,856 bytes; schemas clara, graphile_worker, workflow, workflow_drizzle) + `clara-globals-2026-09-14T17-27-04-723Z.sql`, 159 s wall, under the session scratchpad `backup/` (outside the repo). If the window opens more than ~2 h after this stamp, RE-TAKE it. A restore does NOT return Storage bytes, managed Auth config or engine state (`packages/db/README.md`).

## The window
**Decision: STOP the runtime.** It removes 0196's ACCESS EXCLUSIVE contention with the runtime's 2 s reconciler reads (runtime-only callers) and makes the §R BEFORE reading exact (no sweep can run between the reading and the commit). **It does NOT quiesce 0197's tables:** the web app stays up and writes them directly through PostgREST as `clara_authenticated` — `approve_entry` / `draft_entry` (`clara.journal_entries`) and `attach_entry_evidence` (`clara.entry_evidence_links`) — so a signed-in bookkeeper can hold ROW EXCLUSIVE and time out 0197's CREATE TRIGGER at 5 s (`55P03`; safe, re-runnable). Mitigation: the owner is asked in-session to stay out of the journals workbench for the ~3-minute window, and step 2's census reads `pg_locks` on the three tables immediately before step 3. Chat/Clara return 502 `runtime_unreachable` during the window; the web app stays up otherwise.
1. `flyctl machine stop 48ee715b763048` → `flyctl machine status` shows `stopped`.
2. Re-run `reads-0196-0198.mjs --prod` through the probe → must print CLEAN again (the post-stop census binds; the `backends by role` and `locks on the tables` blocks must show no `clara_authenticated` writer on the three tables). Then `reads-0196-0198.mjs --r` → the BINDING §R BEFORE reading — all three columns (`chat_clarifies_past_due`, `oldest_deadline` recorded as NULL when the count is 0, `firms_touched`) — record.
3. `(cd packages/db && <probe dsn pipe> | node ../../scripts/ops/dsn-pipe.mjs -- node scripts/migrate.mjs)` from the `main` checkout at `3486b6c2` → expect `3 new migration(s) applied · 193 total` with `#692 prestate: clean` / `#692 tail: OK`, `#718 prestate: clean` / `#718 tail: OK`, `#720 prestate: clean` / `#720 tail: OK`. Expected wall ≈ T + pooler latency.
4. `reads-0196-0198.mjs --applied-at` → ledger 193 / `0198_chat_clarify_expiry` and the three `applied_at` stamps; **0198's `applied_at` is `<the deploy instant>` for step 6.** Destroy the probe (`flyctl machine destroy 7817642f059718 --force`); `flyctl machine list` → exactly one machine.
5. `flyctl deploy --config packages/runtime/fly.toml --image registry.fly.io/clara-runtime@sha256:b6dd2fa56525e04aa6fab2c27235ca419aa40f32dfb61af518b53ed42970b79e` → `flyctl machine start 48ee715b763048` (narrative start instant in RESULTS) → `/ready` 200 with `bodies {measured:true, stranded:0, world_start_refused:false}` → `flyctl logs`: `serving git_sha=3486b6c24caea94f3847313184b92b112525dccb frontier=0198_chat_clarify_expiry(193) bodies=51 pins closeExample=closeExampleV1 chatTurn=chatTurn_v19 claraWork=claraWork_v3`, three bundle banners, `stranded bodies n=0` BEFORE `durable world started`, `CONTROL listening`, `LEADER acquired`. Record the wrapper's log wording as #720 evidence: the v84 image must never print `past-due work question(s)`; with a 0-row backlog the line does not fire, so cite the released digest and `control.mjs`'s shipped text. Signed-in `GET /api/build-info` (`git_sha` = the full SHA, `frontier` 0198(193), 51 bodies, the pins) must agree with the log line — no signed-in session is available to the agent, so this is the OWNER's step, listed in PROGRESS with the 2026-09-14 walk.
6. §R AFTER through the LIVE machine's pipe: `reads-0196-0198.mjs --after <0198 applied_at>` → `chat_rows_expired_by_the_first_sweep`, audit rows of the verb since the cutoff, the #764 number, remaining past-due chat rows — expect **0 / 0 / 0 / 0** and say what that does not prove (above).
7. Post-release ticket checks through the live pipe: #692 `select column_name, column_default from information_schema.columns where table_schema='clara' and table_name='firm_document_limits'` → the four limits NULL, `updated_at` still `now()`, the recut body's sha; #718 the three functions + three triggers present; #720 the recut verb's sha, `work_id` scoping gone.

**FAILURE BRANCH — decide from the LEDGER** (each file is its own transaction and the runner STOPS at the first failure, `migrate.mjs`): a `55P03 lock_not_available` abort (0196's ALTER or 0197's CREATE TRIGGER) leaves that file's ledger row unwritten → re-run step 3; a SECOND timeout is most likely a LIVE WEB WRITER, not a foreign session — read `pg_locks join pg_stat_activity` for a `clara_authenticated` backend on `journal_entries` / `entry_evidence_links` / `firm_document_limits` first. **0198 carries no `lock_timeout`** — a wait there runs to the 5-minute `statement_timeout` and aborts `57014`. **SILENT HANG with no output at all** (not even `#692 prestate`) = the F10 advisory lock, which `migrate.mjs` takes UNBOUNDED by design: diagnose with `select a.pid, a.state, a.query, a.backend_start from pg_stat_activity a join pg_locks l on l.pid = a.pid where l.locktype = 'advisory' and l.classid = 439041101 and l.objid = 794746;` — a stale holder is released by `select pg_terminate_backend(<pid>)` on THAT backend; do not wait past 10 min for this case. A `CLR10` prestate refusal → STOP, report the notice text, start v83. A partial ledger (0196 and/or 0197 in, 0198 out) → starting either image is legal; finish forward on the next attempt. **Starting v83 is legal at any of 0195/0196/0197/0198:** the ONE frontier rule (`rollback-preflight.mjs` `FRONTIER_BODY_RULES`, keyed at 0195, requiring `claraWork_v3`) IS in force at every one of those frontiers and is SATISFIED because v83's roster carries `claraWork_v3`; the boot gate itself checks only the stranded-body census, never the frontier; no consumer-first obligation. **IF THE DEPLOY OR THE START FAILS** (deploy error, machine will not start, `/ready` 503 including `world_start_refused`), the probe is already gone and there is no DSN source — re-create one before diagnosing: `flyctl machine run registry.fly.io/clara-runtime@sha256:<digest> --app clara-runtime --name probe-recover --region sin --vm-memory 512 --env CLARA_START_WORLD=0 --env PORT=3200 --command "sleep infinity"`, read through it, destroy it again before retrying the deploy.

## Rollback points AFTER (from the ledger)
DB **193 / 0198** — below it only via an append-only successor, none drafted: 0197 rolls back by dropping its three triggers; the two recut pre-images are recoverable from the immutable chain (0007 §… for `_tf_firm_document_limits_upsert` plus the four DEFAULTs 100/1000/2/2; 0180 §G for `expire_due_interruptions`). Runtime **v84** = `registry.fly.io/clara-runtime@sha256:b6dd2fa5…`; previous **v83** `sha256:28a2f13d…` — one command: `flyctl deploy --config packages/runtime/fly.toml --image registry.fly.io/clara-runtime@sha256:28a2f13d6819517fcaab98aaa7850e07bdd9b3c149f50f96ec1d96432b12bdba` then `flyctl machine start 48ee715b763048`. Web `0290977b-74a4-4c4a-849f-ee60efd631bb`, unchanged (previous `742b09e9…`, one command as in `RELEASE-RUNBOOK.md`). **What an image rollback does and does not undo:** v83 is a legal boot target after 0198 (same 51-body roster, the 0195 rule satisfied, `stranded bodies n=0`), and it would keep sweeping chat clarifications — the sweep lives in SQL, not in the image; the only thing an image rollback undoes is the log wording. **The below-0195 restore draft** (`drafts/restore_pre_0195_bodies.DRAFT.sql`) now takes number 0199, needs all 193 release files beside it, and expects `1 new migration(s) applied · 194 total` — its README carries the correction.

## After
Evidence comment on #692, #718, #720 in the #621/#628 shape ("Hosted release evidence — <migration> (<date>, release session)" with a `Rollback points` row, ending "Closing per the awaiting-release rule: local and CI evidence in the lane comment above, hosted evidence here."): #692 the quiescence statement + the two notices + the `information_schema.columns` rows; #718 the quiescence statement + the two notices + a `Legacy double claims` row (PR #822's probe, its result and the three denominators, pre-window UTC, read as `postgres`); #720 the three §R readings BEFORE (three columns), AFTER (audit witness, #764 number) with the zero-backlog caveat, the recut verb's sha and the shipped wrapper wording. Remove `awaiting-release`, close all three; post the #764 number on #764 (stays open). Fill the RESULTS table below; PROGRESS Current State (deployed = `main` tip again except for the landing docs commit; the runtime/database asymmetry paragraph stays; the below-0195 restore counts); HANDOFF (removed after the walk — PROGRESS is the entry point now); memory. **Landing:** branch → PR → `ci` SUCCESS → fast-forward (`main` is protected; never a direct push); the commit carries this runbook + RESULTS, `ceremony/reads-0196-0198.mjs`, the restore-draft README correction, PROGRESS and HANDOFF. `flyctl machine list` final. Rig `rig195` and worktree `clara-wt/base195` are droppable.

## RESULTS — executed 2026-09-14 17:44–17:48 UTC (2026-09-15 01:44–01:48 MYT), one session, owner-authorised
| Step | Reading |
|---|---|
| gates | rehearsal 3 new · 193 total, < 1 s; production Phase B `--prod` CLEAN through probe `7817642f059718` (identity `postgres@postgres:5432` on the pooler; 190/190 checksums; six bodies at reference; control_witnesses empty; audit_log 1,857 rows / 5 firms readable; #718 double claims 0 with denominators 11/4/2; §R backlog 0/NULL/0); live-machine pipe rehearsed pre-window |
| image | pushed 17:24:06Z, `refresh-3486b6c2` = `sha256:b6dd2fa56525e04aa6fab2c27235ca419aa40f32dfb61af518b53ed42970b79e` |
| restore point | 17:24:27Z, 214,145,856 bytes + globals, 159 s |
| 1 stop | `48ee715b763048` stopped 17:44:52Z |
| 2 re-census + §R BEFORE | CLEAN again (backends: only the idle `authenticator`; no lock on the three tables; no F10 holder). §R BEFORE 17:45:05Z: `chat_clarifies_past_due 0`, `oldest_deadline NULL`, `firms_touched 0` (work questions past due 0) |
| 3 migrate | 17:45:11Z → **`3 new migration(s) applied · 193 total`** in 19 s; `#692 prestate: clean` / `tail: OK`, `#718 prestate: clean` / `tail: OK`, `#720 prestate: clean` / `tail: OK` |
| 4 ledger | 193 / `0198_chat_clarify_expiry`; `applied_at` 0196 17:45:18Z · 0197 17:45:22Z · **0198 17:45:27Z (the §R cutoff)**; probe destroyed 17:45:41Z; machines: 1 |
| 5 release | deploy by digest 17:45:56Z → **v84** → start 17:46:40Z → `/ready` 200 at 17:46:59Z with `bodies {measured:true, stranded:0, world_start_refused:false}`, `leader.held:true`, `world.ok:true`; logs: `serving git_sha=3486b6c24caea94f3847313184b92b112525dccb frontier=0198_chat_clarify_expiry(193) bodies=51 pins closeExample=closeExampleV1 chatTurn=chatTurn_v19 claraWork=claraWork_v3`, `stranded bodies n=0` BEFORE `durable world started`, banners v1/v2/v3, `LEADER acquired`, `CONTROL listening` (the two Fly health-check errors at 17:46:40Z are the pre-HTTP startup seconds). No `expired` log line (zero backlog). `/api/build-info` pairing: OWNER's step (no signed-in session) |
| 6 §R AFTER | read 17:47:32Z through the live pipe, cutoff 17:45:27Z: `chat_rows_expired_by_the_first_sweep 0`; audit rows of the verb since the cutoff 0; **#764 number 0**; remaining past-due chat rows 0 — zeros witness nothing about the loop (stated on #720/#764); the positive witness is `#720 tail: OK` |
| 7 ticket checks | #692: four limits `NULL`, `updated_at` `now()`, body sha `e07fabd4…`, the four triggers unchanged. #718: three functions + three triggers present, `_document_posting_entry` still `8ba5e67f…`. #720: verb sha `34b879c8…`, `work_id is not null` GONE, owner/SECDEF/ACL as pinned |
| evidence | posted on #692, #718, #720 (closed, `awaiting-release` removed) and the #764 number on #764 |
| web | untouched; `/login` 200 throughout |
