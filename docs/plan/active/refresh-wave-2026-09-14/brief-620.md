# Brief: #620 — least-privilege source custody, preview and download

## Orchestrator decisions (binding)
- Migration number: **0190** (`0190_document_byte_door_v2.sql`). v1 door stays byte-identical.
- Cross-client denial = active-filing scope (`document_filings … retired_at is null`); no per-user client ACL (a different ticket).
- No rank floor change on source reads (any active member, as today); record the decision.
- Credential decision: keep the custom-role JWT (`clara_storage_docs`) for custody writes/reads and the server-side proxy for human reads; no signed URLs, no S3 keys, no service_role in the runtime. Cite the vendor pages listed below in ARCHITECTURE §10.
- Storage grant battery: build it to run against ANY Supabase stack given env (`CLARA_STORAGE_BATTERY_DB_URL`, `CLARA_STORAGE_BATTERY_API_URL`, `CLARA_STORAGE_BATTERY_JWT_SECRET`, `CLARA_STORAGE_BATTERY_ANON_KEY`), skipping with a NAMED reason otherwise. Add a CI job `storage-battery` gated on a new `changes` output `storage` (paths `packages/db/deploy/storage-*.sql`, `packages/runtime/lib/storage*.mjs`, the battery file) that uses `supabase/setup-cli` + `supabase start` (pin versions) — keep it in the `ci` meta-gate with the same lawful-skip polarity as `db-split-partition-total`. If `supabase start` proves unworkable on the hosted runner within your budget, land the battery + local instructions and leave the CI job out with a written reason; do not leave a red job.
- PRIOR WORK EXISTS AND IS SUBSTANTIAL: branch `refresh/620-source-custody` (a stopped 2026-09-12 lane; ~7,900 lines over 59 files; 17 review-round fixes F1–F17 already applied) carries `0185_document_download_door.sql`, `packages/db/storage-battery/{run.mjs,verdicts.mjs,hosted-probe.sql,README.md,supabase/config.toml}`, `rig-docs-download-door.test.mjs`, `documents-byte-route`-style `document-route-e2e.test.mjs`, `storage-read-contract.test.mjs`, `documentRoutes.ts`/`storage.mjs` changes, `apps/web/tests/documents-download.test.ts`, ARCHITECTURE edits and a CI storage classifier. `refresh/620-fix-battery` and `refresh/620-fix-web` are earlier/partial variants of the same lane (the first equals `refresh/620-source-custody`). START by rebasing `refresh/620-source-custody` onto your branch (`git rebase --onto impl/620-source-custody main refresh/620-source-custody` into your worktree, or cherry-pick its range), resolve conflicts against current main (0185–0187 landed since), RENUMBER its migration to `0190_document_byte_door_v2.sql` (keep its stable suffix idea if the branch's tests key on the suffix), then reconcile with the decisions above and the current ticket text; keep its battery design if it satisfies AC4, and discard only what conflicts. Read its commit messages: they record measured decisions. Do not delete the old worktrees or branches (the orchestrator cleans up).
- #624 shares the document surface: #620 owns the byte door, its error taxonomy, `document-metadata.tsx`'s state ladder, `lib/documents/{bytes,download}.ts`, and the `?document=<uuid>` workbench param; #624 owns facts/overlay/extract panels. Use message-key prefix `ClientDocuments.download*`/`source*`; #624 uses `facts*`.
- Add a negative DB cell that the operator lane (#615) gains no document byte access.

## 1. Current state
Upload → bytes: browser PUTs `/api/runtime/intake/documents/:id/bytes` (`apps/web/lib/documents/intake.ts:80`) through the proxy (`app/api/runtime/[...path]/route.ts:150-156`, `firmScopeGuard()` first; bytes/finalize legs carry the runtime's short-lived capability, never the session bearer, `route.ts:55-64`). Runtime spools (`packages/runtime/lib/intake.mjs:203-227`).
Bytes → locator: `finalizeDocumentIntake` (`intake.mjs:252-310`): `detectDocument` → MIME wall (415 `bad_type`, :276) → `scanFile` → key `firms/{firm}/docs/{sha256}.{ext}` (:277) → `putCanonical` (:278) → `verifyCanonical` (:279) → then `claim_document_intake_upload`/`mark_document_intake_received` (:288-310); `clara.finalize_document_intake` (:354) inserts `clara.documents` with `storage_path` + `bytes_verified_at` in one txn (`0007_document_pipeline.sql:2010-2016`). AC3 already holds — untested.
Storage credential: `realConfig()` (`packages/runtime/lib/storage.mjs:102-124`) requires `CLARA_STORAGE_URL` + `CLARA_STORAGE_ROLE_JWT` + `CLARA_STORAGE_ROLE`; refuses `anon`/`authenticated`/`service_role` as the role and as the JWT `role` claim; refuses expired/malformed (30 s skew). Writes `POST` with `x-upsert:false` (:152); duplicates read from the body (:156-168).
Preview: `apps/web/lib/documents/bytes.ts:80-103` → `/api/runtime/documents/:id/bytes` → `packages/runtime/src/documentRoutes.ts:50-120`: JWT first (401), uuid (404), one `clara_runtime` txn with `resolvePrincipal` + `clara.get_document_for_human_read($1,$2)` (:78) — definer at `0011_daily_loop.sql:2401-2414`, granted to `clara_runtime` only (:4221); authorises on live active firm membership only: no client scope, no audit line. `downloadCanonical` hashes en route (:95); `Content-Disposition: inline` (:100).
Download: none for documents; precedent `apps/web/lib/reports/download.ts` + `packages/runtime/src/reportRoutes.ts:98-101` (RFC 5987) + `clara.get_artifact_for_human_read` (`0162_fs7_e2_artifact_download_door.sql:334-368`) which writes an egress audit line (:356) omitting the storage key.
Storage policies: deploy artifacts, not migrations — `packages/db/deploy/storage-provision.sql` (role NOLOGIN NOINHERIT + escalation abort :39-55; `grant clara_storage_docs to authenticator` :60; `grant select, insert on storage.objects` :64; insert/select policies on `bucket_id='firm-docs'` + `firms/<uuid>/docs/<sha256>.<ext>` regex :66-80) and `wave-b-storage-update-amendment.sql` (UPDATE with mirrored predicate + four assertions). DELETE/TRUNCATE never granted; `storage.buckets` SELECT asserted absent. `chain-minted-roles-drift-guard.test.mjs:68`.
/ready storage lane: `{pending:true}` / `{skipped:true, reason:'storage_not_configured'}` / `{ok,reason,pending:false}` — `storage-probe.mjs:198-270`, `health.mjs:406-417`; probe = write + read-back on a daily key under sentinel firm `00000000-0000-4000-8000-000000000000` (:92,126-132); no DELETE.
Tests: `storage-probe.test.mjs` (11), `storage-missing-object.test.mjs` (10), `wave-a-document-route.test.mjs` (4), `rig-docs-isolation-grants.test.mjs`, `fs7-e2-artifact-download.test.mjs` + `fs7-e2-artifact-route.test.mjs` (the HTTP-battery house shape: `app.listen(0,"127.0.0.1")` :235, skip-gated :216), web `bytes.test.ts`, `open-in-new-tab.test.ts`, `document-metadata-viewer-gate.test.tsx`, e2e `documents-viewer-walk.spec.ts` (9; bytes mocked at `documents-viewer-mock.mjs:398`). `storage.objects` appears in zero tests.
Vendor docs (Context7): `supabase.com/docs/guides/storage/schema/custom-roles` (custom role JWT + policies — ours is a strict subset); `.../getting-started/api-keys` and `.../database/postgres/row-level-security` §Bypassing RLS (service role disqualified); `.../self-hosting/self-hosted-s3` §Session token (no privilege reduction; rejected); `.../auth/signout` (revoked tokens valid until exp ⇒ proxy beats minted URL); createSignedUrl reference (needs objects SELECT).

## 2. Gaps / rows
AC1 verify-only + record. AC2 to build (byte-door behaviour tests; filing-scope predicate). AC3 satisfied, unpinned — add cells. AC4 to build (the battery). AC5 partly (download absent; one red banner for all failures; no retry/denied/unavailable/expired distinction). AC6 partly (`?document=` param absent, #719). AC7 to build.
Rows: H-53 to build (one intake cell); C-12 verify-only (hard-gate half stays an accepted target); C-57 to build; C13.6 verify-only; C83.27 verify-only re-scoped (DELETE asserted denied); C83.28 to build; C83.29 verify-only + record; C88.9 to build (needed by C83.28). Renderer/backup credential minting out of scope; record the risk acceptance in ARCHITECTURE §10.

## 3. Slice (one PR)
DB `0190_document_byte_door_v2.sql`: `clara.get_document_for_human_read_v2(p_document uuid, p_user uuid, p_client uuid, p_purpose text)` on the `get_artifact_for_human_read` shape: live active membership; when `p_client` non-null require a matching active filing; one CLR11 `document_not_found` for absent/foreign-firm/wrong-client; `clara._audit` line before return (document, actor, sha256, byte_size, purpose; NOT the storage path); granted to `clara_runtime` only. v1 untouched.
Battery `packages/db/tests/storage-grant-battery.test.mjs`: SQL layer (privilege matrix SELECT/INSERT/UPDATE present, DELETE/TRUNCATE/REFERENCES/TRIGGER absent; NOINHERIT; no anon/authenticated/service_role membership; buckets SELECT absent; insert/update predicates identical; non-vacuity via `set local role` UPDATE matching 0 rows) + HTTP layer (JWT minted with `{role:'clara_storage_docs'}` against the stack's secret; allowed POST to a canonical docs key + GET read-back with matching sha; denied non-sha key, other bucket, `reports/`/`wiki/` prefixes, DELETE (assert the vendor's body-wrapped `{"statusCode":"403"}`), anon-key request). Cleanup: disposable stack; per-run UUID keys under the sentinel firm; tail leak-check that only expected objects exist under the run prefix. Temporary-admin-grant detection: snapshot `pg_roles` attributes + `has_table_privilege` matrix + `pg_policy` predicate text before/after, assert byte-equality; `rolbypassrls=false` at each denial; reuse `storage.mjs:118-122`'s role check.
Hosted: read-only `packages/db/deploy/storage-hosted-probe.sql` (catalog reads only) for the release session, labelled hosted evidence.
Runtime `documentRoutes.ts`: `?client=`, `?disposition=attachment`, cut over to v2, typed 401/403/404/409/400/502/503, RFC 5987 filename via a shared `contentDisposition` module lifted from `reportRoutes.ts:98-101`; pin proxy header forwarding (`route.ts:113-114`) with a cell.
Web: `lib/documents/download.ts` (on the reports mechanism); `document-metadata.tsx` six-outcome ladder — preview / downloading / denied (403 with the door's reason, no Retry) / unavailable (502/503 with Retry) / expired-link (401 mid-read → re-authenticate) / not-viewable; keys beside `messages/en.json:1368-1374`; `?document=<uuid>` on the workbench (`documents-workbench.tsx:28`); focus returns to the invoking control.
Docs: ARCHITECTURE §7 + §10 (credential decision with citations; backup/renderer risk acceptance; policies-outside-migrations drift and the battery as cure); §11 row 文件; CONTEXT terms if new.

## 4. TDD seams (red first)
1. `packages/db/tests/rig-docs-download-door.test.mjs` D1 — v2 returns one identical CLR11 `document_not_found` for random uuid / foreign-firm document / own-firm document whose active filing is client B while `p_client`=A; returns `storage_path` for the matching client.
2. D2 — a successful v2 read adds exactly one `clara._audit` row naming actor + document with `args.storage_path === undefined`.
3. D3 — `clara_authenticated`, agent role and `anon` get 42501 on v2; only `clara_runtime` executes; the operator lane (#615) gains nothing.
4. `packages/runtime/tests/documents-byte-route.test.mjs` (express on 127.0.0.1, PG-skip-gated) R1 `?disposition=attachment` yields RFC 5987 disposition + byte-exact content; R2 revoked member's still-valid JWT → 404, zero bytes; R3 `?client=<other>` → same 404; R4 missing object → 502, never a truncated 200.
5. `packages/db/tests/storage-grant-battery.test.mjs` S1 allowed POST + read-back; denied non-sha / foreign bucket / `reports/` prefix / DELETE; S2 role snapshot byte-identical before/after; `rolbypassrls=false` at each denial.
6. `apps/web/components/documents/document-source-states.test.tsx` — six distinct accessible outcomes; 502 renders Retry that re-invokes; 403 renders the reason with no Retry; focus returns to the Download control.

## 5. Risks
`storage.mjs` writes `wiki/`, `reports/`, `sandbox/` prefixes with the same role but policies admit only `docs/` (:245,336,432; `storage.mjs:325-331`) — hosted sealed-report/sandbox writes may fail today; the hosted probe must answer; file a follow-up issue if confirmed. `grant clara_storage_docs to authenticator` makes the JWT secret the boundary; rotation unrehearsed. Backup holds a service_role key (`packages/backup/scripts/lib/storage-mirror.mjs:32`); renderer holds the custody role — written risk acceptance only.

## 6. Effort: L. Rig: PG cluster (assigned); a Supabase stack is absent from the repo (skip-gated battery; CI job attempt); hosted probe is the owner's.
