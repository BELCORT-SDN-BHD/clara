# Brief: #637 — two-build cutover: successor admission, parked legacy resume, rollback preflight, provenance

## Orchestrator decisions (binding)
- NO migration, NO new frozen closure in this PR. Build the cutover mechanism generically; the drill derives its claraWork pair from `registry.ts` (today v1→v2). When #631 lands `claraWork_v3`, the orchestrator will ask you (or a successor worker) to re-run the drill v2→v3 — design for that with no edit beyond the registry.
- (a) Accept the asymmetric pair for the drill: v1 parks a bare clarify (`claraWork.v1.ts:141-142`, `openInterruptionStep`, answered through `clara.answer_interruption`), v2 parks a Work question (`claraWork.v2.ts:176-183`, `openWorkQuestionStep`, answered through `clara.answer_work_question`). State it in the e2e header.
- (b) Boot-time stranded-body check is WARNING-ONLY (fail-open); `/ready` gains `checks.bodies` at warning level following the `checks.leader` precedent (`held:false` is a warning).
- (c) The `chatTurn_v1` census-waiver retirement belongs to #631 — not here.
- Fold in the #708 fix (scoped inventory) and resolve it in this PR (write "Resolves the defect filed as #708"; the orchestrator closes it).
- Two-build e2e: try the scratch nitro build first (copy the runtime package sources into a scratch dir inside the workspace so bare imports resolve, rewrite the copy's `registry.ts` to pin `claraWork: claraWork_v1` and drop the v2 import/export, `nitro build` there); never mutate the repo's registry; fallback = `git worktree` at the pre-#629 commit. Measure the extra build time and report it; wire the e2e into `.github/actions/db-live-gates/action.yml` after `work-cancel-e2e` only if the measured cost is acceptable (< ~6 min); otherwise wire it behind `workflow_dispatch`/schedule in the sweep job and say so.
- Provenance: `registry.ts` gains `export const workflowBodies` (exported `_vN` identifiers) and `export const workflowPins` (`{class: identifier}`), both frozen under the view-integrity law; `lib/build-info.mjs` gains `bodies` and `pins` as copies (existing fields untouched); `src/buildInfoRoutes.ts` passes them; `plugins/startWorld.ts` adds ONE boot line naming git sha, frontier, pins and body count — the two existing bundle banners stay byte-identical (`work-bundle.test.mjs:124` pins v1's exact string). The web `/api/build-info` route (`apps/web/app/api/build-info/route.ts`) may pass `bodies`/`pins` through if trivial.
- Rollback preflight: `packages/runtime/lib/rollback-preflight.mjs` + CLI `packages/runtime/scripts/rollback-preflight.mjs` — censuses non-terminal `workflow.workflow_runs` by `name` AND `clara.agent_tasks` of kind `accounting_work` in `queued`/`running` with `workflow_run_id IS NULL` (uncounted today; `packages/runtime/README.md:124-128`), compares against a supported-body set taken from the TARGET image (its built bundle scanned for WDK directives, reusing `scripts/check-workflow-bundle.mjs`'s `includesToken` + directive derivation `:68-77,122-140`, or the target's `/api/build-info` `bodies`); explicit `--scope` (run ids / name filter) fixes #708; non-zero exit with the offending names is the gate; the README release ceremony makes it a required step before `fly deploy --image <previous>`.
- C-38/C54.4/C86.3: fix the EDITABLE citations of the non-existent "ARCHITECTURE Appendix A" (`registry.ts:3`, `scripts/check-frozen-workflows.mjs:56,381,486`, `scripts/check-workflow-bundle.mjs:2,123`) and add an anchor + supersession pointer in ARCHITECTURE §10 that the frozen files' citations (`claraWork.v2.ts:6` — never edit) now resolve to.
- C77.2: `check-frozen-workflows.mjs` must REFUSE a manifest key under `packages/runtime/tests/` (today `isTestPath` is consulted only at :476 for enqueue sites); positive-control it in `scripts/check-frozen-workflows.selftest.mjs`.
- Refresh stale pins: the e2e header's dated expectations and README `:286-290` ("ceremony is still OWED" — all entries are now `deployed:true`).
- Hosted half (two releases + one deliberate rollback on Fly) is a production ceremony the owner schedules; write the exact steps into docs/PROGRESS.md's release ceremony and the runtime README; do not claim it.

## 1. Current state
AC1 file-level half built: `frozen-workflows.json` 264 entries all `deployed: true`; `scripts/check-frozen-workflows.mjs` enforces append-only vs origin/main (:14-24), deploy-lock monotonicity (:56-62), registry version monotonicity + enqueue provenance (:31-44), `--lock-deployed` (:50); `scripts/check-workflow-bundle.mjs:122-140` UNRESUMABLE rule; selftests (`check-frozen-workflows.selftest.mjs`, `.registration.selftest.mjs:18-26`). Manifest half partial: `claraWork.v2.bundle.ts:45-58` hashes `{id, instructions, skills, tools{id,names}, budgets}` — not tool JSON schemas/dependencies (:19-23 admits it; `ask_question`'s schema changed v1→v2 with only a hand-bumped `tools.id`); pin cell for v1 only (`tests/work-bundle.test.mjs:32-40`); no literal pin for `CLARA_WORK_BUNDLE_V2_DIGEST`.
AC2: `tests/version-cutover-e2e.mjs` (372 lines; CI `db-live-gates/action.yml:80`) is one build, one process, chat lane only (parked `chatTurn_v7` via `start({workflowId})` from the nitro manifest :154-161; second turn via `POST /api/chat/:s/turns` binding the registry pin :171-176; name invariance :301). Multi-process precedent: `work-question-e2e.mjs:112-113` spawns five `serve.mjs` children.
AC3: inventory exists only inside that e2e (`rollbackPreflight` :180-186, `rollbackPreflightInventory` :188-197 — global, #708 at :193); unbound `accounting_work` tasks uncounted.
AC4: no refusal anywhere (README:259-263 prose; two `fly deploy` commands :246-249).
AC5: boot `plugins/startWorld.ts:156,162,168` (world started; two C88.8 bundle banners), `lib/leader.mjs:195`; no git sha/frontier/pin at boot. `/api/build-info` (`src/buildInfoRoutes.ts:42,57`; `lib/build-info.mjs:100-119`) serves service, git_sha (null never placeholder :105), image_ref, machine_version, machine_id, `workflows` (CLASS names only — `registry.ts` `workflowNames = Object.keys(workflows)`), bundles, frontier, ts. DB: `claim_work_run` refuses a claim without `{id,digest}` (0178:1045-1050; 0184:581-633), `accounting_work.bundle`, `operation_receipts.bundle_digest` NOT NULL (0178:422). Web `work-detail.tsx:558-559,652-657` renders "Run version"; `apps/web/app/api/build-info/route.ts:33-47`. Tests: `l9-build-info.test.mjs`, `work-bundle.test.mjs:123-148`, `work-journal-e2e.mjs:366`, `work-question-e2e.mjs:408-409`, `work-detail.test.tsx:275-285`.
D6: no surface tells a reader a Work is parked on a body the current image no longer pins.
Rows: C-38 verify-only + small build (citations); C-61 satisfied (#616; Dockerfile node:22); C-70 to build (spine); C-75 verify-only (refresh pins); C13.5 verify-only + two-build re-proof; C48.2 out of scope; C54.2 to build (literal AC2+AC3); C54.4 to build small (anchor/pointer); C77.2 half to build; C86.3 resolves into C-38/C54.4; C88.8 partially satisfied; extend (registry-version half).

## 2. Slice (one PR)
- `lib/rollback-preflight.mjs` + `scripts/rollback-preflight.mjs` (see decisions).
- `plugins/startWorld.ts` warning line `[clara-runtime] stranded bodies n=… names=…` after world start (non-fatal); `/ready` `checks.bodies` `{stranded, names}` in `warnings`, `ready` unchanged.
- Provenance exports/copies/boot line (see decisions).
- `tests/two-build-cutover-e2e.mjs` standalone: build A (scratch nitro output pinned to v1) admits W1 → binds `clara-work/v1`, parks; A stops; preflight refuses a v1-less set; B (current build) starts, `/api/build-info` reports `pins.claraWork = claraWork_v2` and both bodies; B admits W2 → binds v2, asks a Work question; W1 answered → resumes on its ORIGINAL body (run `name` invariant) → own receipt with the v1 digest; W2 answered → own receipt with the v2 digest; preflight refuses a v2-less set while W2 is live and allows once both settle; an unbound queued `accounting_work` task refuses on its own. Lengthen `CLARA_RECONCILE_GRACE` as `version-cutover-e2e.mjs:92` does; stop A before B starts (leader contention).
- C77.2 refusal + selftest; citation fixes + ARCHITECTURE §10 anchor; README/PROGRESS ceremony updates (preflight command, the two admissible proofs — retain every nonterminal bundle OR verified drain, warning-level boot check, rollback points as input).
- Docs: ARCHITECTURE §5 (generic mechanism), §10 (executable rollback paragraph), §11 row "Agent 与宿主" (two-build evidence, local/CI); `packages/runtime/README.md`; docs/PROGRESS.md release ceremony.

## 3. TDD seams (red first)
1. `packages/runtime/tests/rollback-preflight.test.mjs` (node --test, real PG rig, skip when 0178 absent): one non-terminal `claraWork.v2` run with `supported:["claraWork_v1"]` → `verdict === "refused"` and `outside` names it; a queued `accounting_work` task with `workflow_run_id IS NULL` → refused `reason === "unbound_accounting_work"` even with empty `workflow_runs`; `scope` set to the staged run ids with 20 unrelated parked runs present → `"allowed"` (#708).
2. `tests/l9-build-info.test.mjs` (extend): `buildInfo({bodies, pins})` carries both; `out.bodies.push` leaves the input untouched; a source cell that `src/buildInfoRoutes.ts` passes `bodies: workflowBodies` / `pins: workflowPins`.
3. `tests/registry-view.test.mjs` (extend): every `workflowBodies` id is an own export of `registry.ts` resolving to a function; every `workflowPins[class]` ∈ `workflowBodies`; both frozen, mutation throws.
4. `tests/two-build-cutover-e2e.mjs` (new standalone): assertions `accounting_work.bundle.id` per Work, run `name` invariance across W1's resume, two distinct `operation_receipts.bundle_digest` values with one entry each.
5. `scripts/check-frozen-workflows.selftest.mjs` (extend): a manifest key under `packages/runtime/tests/foo.test.mjs` is REFUSED with a named violation; the valid production registration passes.
6. `tests/ready.test.mjs` (extend): `checks.bodies` reports `{stranded, names}` in `warnings` and `ready` stays true when `stranded > 0`.

## 4. Risks
Second nitro build cost in CI; the WDK manifest path (`../node_modules/.nitro/workflow/manifest.json`, `version-cutover-e2e.mjs:155`) must exist for the scratch build — spike this first and report; leader contention; do not assert a "correct" `(CLR13, work_cancelled)` classification in a drill that cancels (frozen v1/v2 tables map it to `state_changed`).

## 5. Effort: L (XL if the fallback worktree build is needed). Rig: PG cluster (assigned) with the real Postgres World bootstrapped; a built runtime plus the scratch build; two spawned `serve.mjs` children on ephemeral ports. No web rig unless a D6 affordance is added (keep D6 to the Run-version row + a "body not pinned by this image" note if cheap).
