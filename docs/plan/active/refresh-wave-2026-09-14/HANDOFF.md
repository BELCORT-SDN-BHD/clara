# Handoff — refresh implementation wave (#612 children), 2026-09-14

Written at the owner's request when the weekly usage limit was nearly exhausted. A fresh agent continues from here. Everything below is machine-local state on `C:\Users\zhant\Desktop` unless a GitHub URL is given. Read `docs/PROGRESS.md` first, then this file, then the per-ticket briefs beside it.

## What the session was doing

`/implement` on 11 `ready-for-agent` tickets from the refresh spec (#612): **#615 #619 #620 #622 #624 #631 #637 #640 #641 #643 #644** (all assigned to the owner on GitHub). The orchestrator (this session, Fable) ran `orchestrator-fable`: one discovery brief per ticket, then one implementation worker per ticket in its own git worktree + its own PostgreSQL 17 cluster, then three review agents per finished branch (standards / spec / adversarial migration-safety), then fix rounds, then integration per wave onto one branch, one CI run per wave, fast-forward `main`.

The binding per-ticket scope lives in `brief-6NN.md` beside this file ("Orchestrator decisions" sections). Common worker rules: `WORK-ORDER.md`. Machine rig: `RIG.md`. Scripts: `integrate.sh` (rebase flavour — superseded by merges, see below), `mkwt.sh` (worktree + install), `mkrig.sh` (WSL cluster), `mkrig-migrate.ps1` / `mkrig-migrate-wt.ps1` (cluster + migrate + seed from a checkout).

## Decisions taken (binding unless the owner overrides)

- Migration numbers: 0188 #615 · 0189 #641 · 0190 #620 · 0191 #624 · 0192 #644 · 0193 #640 · 0194 #643 · 0195 #631. #619, #622, #637 carry none. Merge in this order; renumber only if a later one must land first.
- ONE new frozen agent successor `claraWork_v3`, owned by #631 (egress purpose consume before the model call, execution trace rows, #737 error-roster pairs, #738 `client_id` on parts, `get_journal_entry_for` so the `chatTurn_v1` census waiver retires). #637 built the cutover mechanism generically (drill v1→v2 now; re-run v2→v3 once v3 lands — registry-derived, no edit needed).
- ONE shared `chatTurn_v19` to be built AFTER #643 and #644 DB halves are final: carries #643's `start_periodic_adjustment_work` tool (non-frozen basis schema/builder already shipped by #643) and #644's `remember_client_information` tool + the honest knowledge-pack context step (`packages/runtime/lib/knowledge.mjs` `readKnowledgePack` / `captureKnowledgeFor`; wiring notes are in the #644 worker's report, summarised in `docs/ARCHITECTURE.md` §7 on its branch). Not started.
- #631 activation ASSUMPTION (owner must confirm): model egress authority for `accounting_work` = firm's current accepted Terms+DPA (0185/0187 legal acceptance) + client active; revocation = newer published legal version not yet accepted, client inactive, or initiator membership lost. No per-client "AI on" switch.
- #640 floors: create/revise/catch-up at `bookkeeper` per the brief ("sufficiently scoped instruction") while the live 0045 template lane needs admin twice — flag for the owner.
- #637 runtime guard (changes the brief's warning-only ruling; flag for the owner): the stranded-body census runs BEFORE `getWorld().start()` and refuses to start the World (HTTP stays up, `/ready` names the stranded bodies) unless `CLARA_ALLOW_STRANDED_BODIES=1`. Reason: the worker measured that a rollback with a parked run of a missing body crash-loops the process (`ReplayDivergenceError` → exit 1).
- #624's Work-surface half of its AC4 ("Documents AND Work show the four states") is an orchestrator integration task after #641's Work-detail Tabs merge: render `DocumentStatePanel` / `getDocumentState` inside Work detail's Sources tab. Not done yet.
- #644 explicit deferrals (record on the ticket at close): reassessment consumer for `knowledge.corrected/withdrawn` (needs #631 trace + #658/#663); conflict → shared Work question is the runtime lane's (v19/v3); firm-scope A6 promotion has a door but no caller until #648; "resume setup from missing applicable facts" is #648/#649's surface.
- #640: the "observed direct debit is bookable but labelled separately" AC clause belongs to #667, not this slice — say so at close.
- #619 resolves #722 and #740; #637 resolves #708. Close those with the wave (write "Resolves the defect filed as …" style; main is protected, so no auto-close).
- Integration uses MERGES (not rebases) into the integration branch: a 48-commit branch conflicted repeatedly under rebase on `docs/ARCHITECTURE.md` §11. Conflicts so far were only §11 table rows (each side edits a different row, or both append to the "财务界面与输出" row) — keep both rows / union the row.

## State per branch (HEAD at handoff; worktree `C:\Users\zhant\Desktop\clara-wt\<ticket>`)

| Ticket | Branch | HEAD | Commits ahead of main | Dirty | Status |
|---|---|---|---|---|---|
| #620 | `impl/620-source-custody` | 549b73b2 | 48 | 0 | DONE, 2-axis review clean, MERGED into `integration/wave-1` |
| #622 | `impl/622-auth-signin-recovery` | 8bb50fc1 | 6 | 0 | DONE. The census fix round LANDED (8bb50fc1: roster entry for `next-cookie.ts`; 裁-117 accepts the computed `?next=` href + two forwarding assertions); full web unit suite 3438/3442 twice with only load flakes. `integration/wave-1` still holds the OLD 5-commit tip (de3c2dad) — MERGE the branch again to pick up 8bb50fc1. |
| #619 | `impl/619-e2e-harness-parallel` | be378bc5 | 4 | 0 | DONE, 2-axis review clean, MERGED |
| #615 | `impl/615-operator-support` | 667e9426 | 7 | 3 | DONE, 3-axis review: boundary holds; fix round IN FLIGHT (p_id text → single not-found; header vs `supportedActionFor` for `metadata_missing`; stale citations in `lib/registration/doors.ts`; arm-1 intent ordering; door ORDER BY; exact `proacl`; firms snapshot; `uq_frp_registration` prestate; `capacityOpKey` collision). Then merge into wave-1. |
| #641 | `impl/641-work-list-detail` | 68cf6651 | 6 | 0 | DONE per its report: 0189 `clara.list_accounting_work` (keyset cursor, clamped limit, no totals) + firm/client Data Table surfaces with URL-stable filters/cursor, saved views, Sheet filters at narrow widths, detail Tabs Results/Sources/Activity with the question above them; AC2 emits only canonical words (no blocked/partial/runnable); AC3 child counts + AC4 batch DESCOPED to #636; Activity tab filters `list_activity` client-side by work_id (follow-up: a `p_work` door filter). Evidence: `work-list.test.mjs` 19/19, web unit 122/122 on 8 touched files, `work-list-walk.spec.ts` 16/16, typecheck/lint green; `journal-work-walk` was 12/15 before its fix commit 68cf6651 and its own green re-run is UNVERIFIED (host load). shadcn provenance: `pagination` + `empty` added via `shadcn@4.19.0` (base-nova); the CLI tried to overwrite `components/ui/button.tsx` (reverting owner-ruled a11y fixes) and to add `cn` — both reverted; only the two new UI files remain. NOT reviewed (run the 3 axes); not merged. Follow-ups it names: `p_work` filter for `list_activity`; shadcn PaginationLink role=button on an `<a>`; guard the pagination install; `rig-isolation` T19. |
| #640 | `impl/640-accounting-plans` | 2fa5ad4e | 7 | 5 | DONE; reviews found 3 BLOCKERS (revision double-posts current period; naked reversal before its accrual; `effective_from` can move backwards) + S4–S8; fix round IN FLIGHT. |
| #644 | `impl/644-knowledge-records` | 223374e3 | 4 | 2 | DONE; reviews found 2 BLOCKERS (claims-less session takes the machine lane in `promote_plan_answers_to_knowledge` → cross-firm; legacy five keys get lower floors + shadowing hides the armed legacy row) + shadow-by-applicability bug + S1 (`jsonb_pretty(...)::bytea` throws) S2 (watermark backwards on withdrawal) S3 (version as JSON number); fix round IN FLIGHT. |
| #624 | `impl/624-document-capability` | 148da420 | 9 | 2 | DONE; reviews found 1 BLOCKER (XLSX `r=` ref unconstrained → retry-loop under the new grammar; clamp at `structured-worker.mjs:63`) + S2–S5 + lines/regions invariant pin; fix round IN FLIGHT. |
| #643 | `impl/643-periodic-adjustments` | fae608e6 | 5 | 4 | Worker finishing (0194, runtime e2e, form, docs committed); FINAL REPORT NOT YET READ; not reviewed. |
| #637 | `impl/637-two-build-cutover` | 2a7d908f | 6 | 0 | DONE; reviews found 3 BLOCKERS in the preflight (run/name scope disables the unbound census + false zero; name scope hides other classes' parked runs; unbound leg covers one kind of five) + S4 (CI dirty-inventory gate) + S5 (World guard, see decisions) + S6/S7 + notes; fix round IN FLIGHT. |
| #631 | `impl/631-work-egress-trace` | fae608e6 (= #643 tip) | 5 (#643's) | 0 | STARTED ~15 min before handoff; based on #643's tip so 0195 pins against 0194; must rebase onto #643's final tip and re-pin if 0194 changes. Cluster `rig631` (55440, `clara_631`, main chain + 0194) is provisioned and seeded. |
| integration | `integration/wave-1` (worktree `clara-wt\integration`) | d6a64207 | — | 0 | = main + #622 (5) + #620 (merge c90d6538) + #619 (merge d6a64207). typecheck + lint GREEN. Web unit: 3500/3506 (2 real reds are #622's census suites above; `onboarding-checklist` CB-AE2E-023 and `checkout-faces-a11y` bounded-wait are load flakes). Runtime suite against rig620 (`node --test --test-concurrency=1 "tests/**/*.test.mjs"` with PG env, `CLARA_RIG_DB=1`): 2326 pass / 6 fail / 3 skip in 34 min — the failing names were not captured (log kept only the tail); expected members: #707 `x56-rest-c` (grep), #693 EICAR, `leader-state.test.mjs` (needs `pg_dump`), `ready.test.mjs` MAJOR-1 wall-clock flakes. Re-run with full logging before the wave-1 PR to confirm none is a merge regression. |

Workers were background subagents of the ended session; they may have been cut off mid-fix. ALWAYS inspect a worktree (`git log main..HEAD`, `git status`, `git diff`) before assuming a fix landed. Uncommitted work in a worktree is the worker's partial fix round.

## Rig (this machine)

- Node 22.23.2 for Windows lives at `C:\Users\zhant\AppData\Local\pnpm\node.exe` (installed via `pnpm env use --global 22.23.2`); prefix `export PATH="/c/Users/zhant/AppData/Local/pnpm:$PATH"` (Bash) or `$env:PATH = "C:\Users\zhant\AppData\Local\pnpm;" + $env:PATH` (PowerShell). Default `node` is v20 — never use it.
- WSL PostgreSQL 17 clusters (all data intact; start with `wsl -u root -- pg_ctlcluster 17 <name> start` after any WSL restart): `rig` 55432 (`clara_dev` at 0187), `rig615` 55433 (`clara_615`), `rig641` 55434 (`clara_641`), `rig620` 55435 (`clara_620`), `rig624` 55436 (`clara_624`), `rig644` 55437 (`clara_644`), `rig640` 55438 (`clara_640`), `rig643` 55439 (`clara_643`), `rig631` 55440 (`clara_631`, main chain + 0194), `rig637` 55441 (`clara_637`, plus `clara_rt_test` template copy). One from-scratch chain per cluster (0154 role-count pin). NEVER set `CLARA_RIG_ALLOW_RESET=1`/`CLARA_RIG_ALLOW_ROLE_SWEEP=1` on a rig you need (`rig-isolation.test.mjs` T19 resets it).
- Playwright port triples per lane are in each worker's prompt: #615 3110-2, #619 3120-2, #620 3130-2, #622 3140-2, #641 3150-2, #640 3160-2, #644 3170-2, #624 3180-2, #643 3190-2, #631 3200-2, #637 3210-2.
- The WSL clone `/home/runner/clara-deploy` (installed, at main 8f0de590) is only needed for the Cloudflare web bundle upload at release.
- CI: a code PR takes ~22 min; `main` is protected (`ci` check) — branch → PR → `git push origin <sha>:main` fast-forward. #620 adds a `storage-policy-battery` job (Supabase CLI + Docker on the runner) that has NEVER run on GitHub yet; the first wave-1 CI run is its first real execution.

## Next steps, in order

1. Read the #641 and #643 final reports (their transcripts are gone with the session; read the worktrees and `git log` instead) and run the 3-axis review on each (prompts: copy the pattern used for #615/#640 — standards + spec + adversarial migration-safety, opus for the migration axis).
2. Collect every fix round (#622, #615, #624, #640, #644, #637): verify the listed findings are closed in the worktree, run touched tests + `pnpm typecheck` + `pnpm lint` + the whole `apps/web` unit suite (`node scripts/run-tests.mjs`).
3. Wave 1: merge #622 (re-merge its fix commits), #615, #641 into `integration/wave-1` (merges; resolve §11 rows), run typecheck/lint/web unit/runtime suite locally, push the branch, open the PR ("wave 1: #619 #622 #615 #641 #620" — body lists tickets and resolved bugs), wait for `ci`, `git push origin <sha>:main`, delete the branch. Post a closing comment on each ticket with local evidence, the deferrals above, and "hosted evidence pending"; close #722 #740.
4. Wave 2: same for #624 #644 #640 #643 #637 onto `integration/wave-2` (from the new main). Add the orchestrator integration commit: `DocumentStatePanel` in Work detail's Sources tab (#624 AC4). Close #708 with #637.
5. #631: wait for its worker (or resume from its worktree), rebase onto the final #643 tip, review (3 axes), merge as wave 3 together with the shared `chatTurn_v19` (build it as its own worker on a branch from wave-2's main: both tools + the knowledge context step; parts-parity exemptions; frozen manifest entries; registry repoint; #637's drill re-run v2→v3 and v18→v19).
6. Update `docs/PROGRESS.md` (current state, hosted frontier still 182/0187 until a release), file the follow-up issues listed in the workers' reports (OFX/TSV/XLSX intake honesty, invoice line items, Work four-state surface, 0045↔0193 convergence, `rig-isolation` T19 guard, WDK crash-loop on unrunnable parked run, manifest half of #637 AC1, operator audit row for `resolve_stripe_event_problem`, applicant-name read, per-purpose egress tokens, runtime knowledge withdrawal door, `signIn` cold-server flake, `personal-settings` keyboard flake), and note the owner confirmations needed (#631 activation basis; #640 bookkeeper floor; #637 World guard; #644 `item_key → knowledge_key` mapping table in the 0192 migration header).
7. Hosted release of the merged migrations is a separate ceremony (`docs/PROGRESS.md` + memory `awaiting-release-ceremony`); #637 added a required rollback-preflight step to it.

## Suggested skills for the next agent

`orchestrator-fable` (run it first — same lanes, models: Sonnet 5 for mechanical/web workers and standards/spec reviewers, Opus 5 for DB/runtime workers and migration-safety reviewers, never Fable as a worker), `code-review` (the two-axis pattern; the third adversarial axis was added by hand), `tdd`, `resolving-merge-conflicts` (for the §11 rows), `grilling` only if the owner confirmations above change scope.

## Open owner confirmations

1. #631 activation basis (DPA-accepted + active client, no per-client switch).
2. #640 `bookkeeper` floor for creating standing posting authority.
3. #637 World-start refusal on stranded bodies (with `CLARA_ALLOW_STRANDED_BODIES=1` override).
4. #644 `item_key → knowledge_key` mapping (in the 0192 header / worker report).
5. Hosted release timing for 0188–0195 (writer quiescence needed: 0191 and 0194/0195 recut live bodies; 0191 also needs the release-time field-path census `packages/db/deploy/0191-field-path-census.sql`).
