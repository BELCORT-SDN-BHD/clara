# Brief: #631 — model/OCR/other egress obeys current purpose authorisation; server-owned capability registry; redacted execution trace

## Orchestrator decisions (binding)
- Migration number: **0195** (`0195_work_egress_purpose_and_execution_trace.sql`). Your branch is based on main AFTER #643's migration 0194 (which recuts `_record_journal_entry_core` and widens `accounting_work.purpose`); your recut of the core pins against THAT body (read 0194 first). 0188–0193 are on your base too.
- You own the new frozen successor `claraWork_v3` (bundle `clara-work/v3`). #637 (two-build cutover) will re-run its drill v2→v3 once your closure lands — do not build cutover/preflight/provenance mechanics; do not claim old-run resume proof.
- Purpose activation for `accounting_work` — ASSUMPTION the owner must confirm (state it in the migration header, the PR body and your report): there is NO manual per-client "AI on" switch (UI-28/UI-29). Authority evidence for model egress on a client's books = (a) the firm's current accepted Terms + DPA under the published legal versions (0185/0187 `clara.legal_acceptances` / published documents) and (b) the client being active. Revocation = a newer legal version published and not yet accepted by the firm, or the client inactive/archived, or the firm membership of the initiator lost. Implement the `accounting_work` arm of `prepare_egress_dispatch` on this derived basis (synthesise the consent/activation references from the legal acceptance id and the client status, or add a derived helper `clara._accounting_work_egress_live(firm, client)`), keeping the consume step's single-use + TTL semantics and re-binding intact. Do not require manual `grant_client_egress_purpose`/`activate_client_egress_purpose` calls for this purpose.
- "Exhausted" ≡ consumed or TTL-expired authorization; no quota in this slice (record it).
- One purpose token: `accounting_work` (coarse). Record the alternative (per-operation tokens) as a follow-up.
- Trace rows carry NO payload column (structural leak prevention). Trace export stays disabled (no route). Retention rides the existing prune lane.
- Deadlock discipline: write trace rows outside the posting transaction except the settle row; respect lock order accounting_work → agent_tasks → agent_interruptions (ARCHITECTURE §6, 0184).
- v3 also carries: the #737 error-roster pairs (`(CLR13, work_cancelled) → cancelled`, `(CLR13, work_settled) → refusal`, `(CLR13, egress_not_authorized) → refusal`), #738 (`client_id` on `work_status` parts), and `get_journal_entry_for` in place of the bare read so the `chatTurn_v1` READ-pool census waiver (`packages/db/tests/fixtures/operation-census-waivers.mjs:58-63`) can retire — retire it.
- Web: a "Diagnostics" section on B3 Work detail reading `get_work_execution_trace` (#641 has restructured the detail into Tabs; place it inside the Activity tab or beneath the identity block — smallest merge surface), plus the `egress_not_authorized` refusal face with no provider disclosure. C13 Knowledge untouched (#644 owns it).
- C-20 finding (`clara.consume_firm_egress_dispatch` does not exist — the firm-narrow family can mint but never consume): record it in ARCHITECTURE §10 and your follow-ups; do not build it.
- Provider evaluation lane: `packages/runtime/tests/provider-eval/work-journal-eval.mjs` gated on `CLARA_PROVIDER_EVAL=1` + a real key; never in CI; emits a separately labelled report. You will not have a key; write it, run it in "unconfigured → skip with named reason" mode, and say so.

## 1. Current state
Frontier at your base: 0194. Serving closures `claraWork_v2` + `chatTurn_v18` (`packages/runtime/workflows/registry.ts:114,146`); all `claraWork.v1.*`, `claraWork.v2.*`, `chatTurn.v18.*` entries in `frozen-workflows.json` are `deployed: true` (hash-immutable vs origin/main; `scripts/check-frozen-workflows.mjs:436-438`); `registry.ts` is editable.
AC1: tool roster is a fixed literal built server-side — `buildClaraWorkToolsV2(ctx, ledger, budgets)` (`claraWork.v2.tools.ts:271-300`; `list_accounts`, `record_journal_entry`, `ask_question`), used at `claraWork.v2.impl.ts:191`, `new ToolLoopAgent({ tools })` :197-204. No path reads basis/document/wiki/OKF to add tools. Bundle `CLARA_WORK_BUNDLE_V2` (`claraWork.v2.bundle.ts:44-51`, digest :61) stamped by `claimWorkRunStepV2` (`claraWork.v2.impl.ts:107-126`). No registry mapping capability → purpose/scope/data class (`docs/ARCHITECTURE.md:151`: "统一能力目录尚待实现").
AC2: DB typed-egress family — `clara.client_egress_purpose_consents` / `_activations` / `clara.egress_dispatch_authorizations` (`0020_typed_consent.sql:149,194,246`), owner verbs, `clara.prepare_egress_dispatch` (latest `0123_f_a7_gamma_egress.sql:576-626`), `clara.consume_egress_dispatch` (latest `0038:6004-6050`; re-binds client/purpose/event_seq/event_type/document_sha256, re-checks consent + activation live, single-use, 120 s TTL). Firm-narrow family (`0123:642-731`, verbs :799-1024, grants :1079-1083) has no consume verb. Work and chat lanes call none of it (grep hits only wiki-projection, statementFacts, witnessFacts). Work segment calls `resolveModel(work.model)` → `openai(modelId)` (`chatTurn.v13.infra.ts:69-72`) with no purpose check. Purposes today: `wiki_synthesis | statement_extraction | witness_extraction | bank_matching | document_processing` (+ firm `firm_narrow_intake`). Only wiki consumes before the model call (`lib/wiki-projection.mjs:43-45,484-505`; ARCHITECTURE:317-318).
Accounting-write check is strong but purpose-blind: `_record_journal_entry_core` (now the 0194 body) locks the Work, refuses `work_cancelled`/`work_settled`, re-reads membership/role, client status, period lock, accounts, control rule, exact cents.
Refusal shape: `claraWork.v1.errors.ts:141-150,300-310` classifies `(errcode, detail.reason)`; unrecognised CLR13 → `state_changed` (#737: a refused turn is spent up to `budgets.replans`). UI renders `error.code · error.reason` + DB message (`work-detail.tsx:753-790`).
AC3: `accounting_work` carries purpose, initiator + role snapshot, intent/logical-op ids, basis + digest, basis_origin, source_refs, current_task_id, `bundle` manifest, result/error (0178:301-340); `operation_receipts` carries bundle_digest, run_id, task_id, acting_actor, on_behalf_of, via_wake_kind, payload_digest, outcome/effects/refusal (0178:411-445). Missing: per-step records, tool/registry/instruction/skill ids at call granularity, model timing, observed Knowledge/books/source revisions, authorization id. `clara.llm_usage_events` (0094:53-70) is document-bound. `clara.trace_spans` (0006:268-280) has no production writer (`recordSpan` `lib/tracing.mjs:66` has zero callers) and no human SELECT grant.
Leak surfaces: `basis` jsonb on `accounting_work`; the run's `ModelMessage[]` transcript in `workflow.workflow_steps.input/output` (+ cbor); live stream parts via `writePartsV2` (`claraWork.v2.impl.ts:266-280`).
AC4: `redact()` (`lib/tracing.mjs:32-54`) recursive, array-aware, cycle-guarded, depth 8, key denylist + four value patterns, "best-effort". Tests `tests/unit.test.mjs:70-96` — one single-level nested object; no PII fixtures, no arrays-of-objects, no depth>2, no projection/log/UI boundary assertion. Positive-control idiom: `world-e2e.mjs:224-239` regex scan over `workflow_steps` jsonb + cbor and `trace_spans.attributes`. No export route (`lib/tracing.mjs:6-8`).
AC5: `resolveModel` honours `globalThis.__claraModelForTest` (`chatTurn.v13.infra.ts:69-72`); `tests/work-scripted-model.mjs` scripts `MockLanguageModelV4.doGenerate`; every Work cell is scripted; no real-provider harness; no label convention in the tree.

## 2. Gaps / rows
UI-28/UI-29 verify-only (no AI-on switch; add a negative test). H-17 verify-only. C-20 record the finding. C-36 to build (v3 versioned instructions/skill/tools + trace). C55.19/C83.21 out of scope (#644) — only the negative cell (Knowledge/OKF text cannot register a tool). C55.20/C83.23 partly (per-client authorisation half = AC2). C77.7 to build (nested fixtures). C82.2/C82.3 to build reframed (consent/activation live windows + 120 s TTL: revoke→reactivate sequencing; before/inside/after TTL; assert selected ids; retain refusal classification). C88.11 to build small (temporary admin grant positive control + teardown detection; waiver idiom exists). C88.12 discovery closes: minimum durable diagnostic event = one execution-trace row per step. C88.18 to build (human read verb + bounded retention).

## 3. Slice (one PR)
DB `0195_…sql`: (1) sixth client purpose `accounting_work` on the three purpose CHECKs and the four typed verbs' in-body allowlists (0123 header §2), not document-tied (the `wiki_synthesis`/`bank_matching` shape); the derived activation basis per the decision above. (2) recut `prepare_egress_dispatch` with the `accounting_work` arm (`v_sha` must be NULL), prestate/postcheck prosrc-sha pinned as 0122/0123 do; `consume_egress_dispatch` unchanged. (3) `clara.work_execution_traces` (id, firm_id, client_id, work_id, task_id, run_id, seq, phase ∈ (dispatch, model_call, tool_call, settle), capability_id, registry_version, bundle_id, bundle_digest, instructions_id, skills jsonb, tools_id, model_id, purpose, authorization_id, consent_ref, activation_ref, input_digest, observed_revisions jsonb (closed key vocabulary incl. `knowledge_version`, `books_version`, source shas), started_at, ended_at, duration_ms, outcome, refusal jsonb, receipt_id) — no payload column; append-only + no-truncate; RLS force; owner policy + `clara_authenticated` firm-scoped SELECT; no runtime DML grant. (4) `clara.record_work_execution_trace(...)` SECURITY DEFINER → `clara_runtime` only; binds task→work→firm/client positively (0094:105-112 idiom); `input_digest ~ '^[0-9a-f]{64}$'`; rejects out-of-vocabulary `observed_revisions` keys. (5) `clara.get_work_execution_trace(p_work uuid)` → `clara_authenticated`, firm-scoped, by seq. (6) recut `_record_journal_entry_core` additively on the 0194 body: before `_reserve_op`, require a consumed, non-invalidated `accounting_work` authorization bound to this run, else CLR13 `egress_not_authorized`. (7) retention via the existing prune lane. (8) retire the census waiver. Tail assertions, preintegration gate, `rig-meta.mjs` cohorts.
Runtime: `packages/runtime/lib/capability-registry.mjs` (capability id → purpose, data class, model-bound?, required scope; successor to `GOVERNED_EGRESS_PURPOSES` `lib/egress.mjs:232-300`), `lib/work-trace.mjs` (trace writer + hardened `redact` with PII patterns: NRIC, bank account, email, phone, JWT, api keys; arrays of objects; depth>2 → `[truncated]`); frozen `claraWork.v3.*` closure (bundle `clara-work/v3`): prepare→consume immediately before `agent.generate` (wiki precedent), trace rows at dispatch/model/tool/settle, the roster additions, parts with `client_id`, `get_journal_entry_for`; `registry.ts` repoints `claraWork: claraWork_v3`, v1/v2 stay exported; parts-parity exemptions updated (`packages/runtime/scripts/parts-parity-exemptions.mjs`); `frozen-workflows.json` gains the v3 entries (not deploy-locked). Digest pinned in `tests/work-bundle.test.mjs` and written to the boot log.
Web: Work detail Diagnostics section + `egress_not_authorized` face; `messages/en.json` keys.
Docs: ARCHITECTURE §5 (capability registry exists for the Work lane; :151,155), §10 (Work lane joins wiki as consume-before-model; export still off; C-20 finding; the activation assumption), §11 rows "Agent 与宿主" + "准入与运行保障"; CONTEXT.md *Capability registry*, *Purpose authorisation*, *Execution trace*.

## 4. TDD seams (red first)
1. `packages/db/tests/work-egress-authority.test.mjs` — `prepare_egress_dispatch(firm, client, 'accounting_work', seq, 'work.segment', NULL)` returns `granted` under a live legal acceptance + active client; returns `unknown` uniformly when the acceptance is superseded by a newer published version, when the client is inactive, when a doc-sha is supplied, on a second consume, and after the 120 s TTL (no existence oracle); `_record_journal_entry_core` without a consumed authorization raises CLR13 `egress_not_authorized`, writes no entry and no receipt, leaves `logical_op_id` unspent.
2. `packages/runtime/tests/work-trace-redaction.test.mjs` — nested NRIC/bank account/email/phone/api_key/JWT at depths 1–7 inside arrays of objects: the persisted trace row contains none of the literals; `record_work_execution_trace` rejects an out-of-vocabulary key; depth cap emits `[truncated]`.
3. `packages/runtime/tests/work-bundle.test.mjs` (extend) — pin `clara-work/v3` digest; `buildClaraWorkToolsV3` returns exactly the server-owned roster; a basis/source_ref/wiki string containing a tool-shaped JSON declaration adds no tool and widens no schema.
4. `packages/runtime/tests/work-egress-e2e.mjs` (real Postgres World, scripted model; standalone, skip-clean when 0195 absent; wired in db-live-gates after the existing legs) — leg A authorised run → one entry, one receipt, trace rows dispatch→model_call→tool_call→settle, authorization consumed exactly once; leg B revoke after prepare → `refused/egress_not_authorized`, no entry, Retry mints a new run that refuses again without a model call; leg C the `world-e2e.mjs:224-239` leak probe widened to `work_execution_traces` with the PII regex set.
5. `apps/web/e2e/work-trace-walk.spec.ts` + `apps/web/tests/journal-refusal-roster.test.ts` (extend) — loading / empty-trace / partial / denied faces; exact money and dates; 320 px; 200% zoom; keyboard focus return; SR names; reduced motion; stable URL/Back.
6. `packages/runtime/tests/provider-eval/work-journal-eval.mjs` — gated on `CLARA_PROVIDER_EVAL=1` + key; skip with named reason.

## 5. Risks
Activation assumption (owner confirmation); deadlock discipline; #637 owns cutover proof; v3 manifest entries merge not deploy-locked (`--lock-deployed` after the hosted release).

## 6. Effort: L–XL. Rig: PG cluster (assigned) + real Postgres World, runtime unit, web unit + Playwright.
