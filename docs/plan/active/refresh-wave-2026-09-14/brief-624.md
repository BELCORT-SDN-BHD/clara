# Brief: #624 — per-kind extraction → typed facts with source regions; capability registry; honest document states

## Orchestrator decisions (binding)
- Migration number: **0191** (`0191_document_capability_registry.sql`). 0188–0190 belong to other in-flight tickets and will be absent locally — expected.
- Invoice LINE ITEMS are deferred: the registry records `business_operation='planned'` / the facts level honestly for lines; no new `document_invoice_lines` table in this PR. Say so in the PR body.
- The registry is GLOBAL (the 0165 precedent), one monotone `registry_version` integer.
- OFX: read `packages/runtime/lib/statement-parse.mjs` first; if it cannot read OFX, seed `ofx×bank_statement` as `typed_facts='unsupported'` and make the intake lane honest (no promise from a filename). Record the finding.
- `persist_document_extraction` recut with the field_path validator uses the 0177 ceremony (pre-state sha pin, single-anchor splice, owner/ACL post-check). Seed the grammar from every path the in-repo writers and fixtures emit (census the runtime's witness/statement/local-facts writers and test fixtures; make the tail probe assert every allowlisted path passes). The hosted census of live `document_regions.field_path` values is a RELEASE-TIME check — write it as a read-only probe SQL file for the release session and say so.
- #620 owns the byte door, `document-metadata.tsx`'s state ladder, `lib/documents/{bytes,download}.ts`, and the `?document=<uuid>` workbench param (it is adding that param; if it is not on main when you rebase, add the minimal param yourself in a way that merges trivially — one search-param read in `documents-workbench.tsx`). #624 owns facts/overlay/extract/state panels. Message-key prefix `ClientDocuments.facts*` / `capability*`; #620 uses `download*`/`source*`.
- #644 (Knowledge) LINKS to extraction rows (FK to `document_extractions`/`document_regions` + `field_path`); do not duplicate facts into Knowledge and do not imply a document fact is a confirmed client fact. Agree the boundary sentence in CONTEXT.md.
- #631: `typed_facts='supported'` never overrides an inactive egress purpose — a note in the registry header, no code.
- No new OCR/model provider; no real-model evaluation in this PR (C83.X2 acceptance definition only, labelled).

## 1. Current state
No capability table. Per-kind boolean only: `clara.document_kind_codeability(kind, codeable, basis, recorded_at)` + `clara._is_codeable_kind(text)` (`0165_document_kind_codeability.sql:152-157, 204-248, 265-289`; 20 kinds; RLS-forced; SELECT to `clara_authenticated`; unknown kind coalesces to true; roster derived from the live `documents_document_kind_check` with a tail guard 250-345).
Capability facts live in function bodies: intake lanes `packages/runtime/lib/intake-lanes.mjs:54-60` (`ofx → 'none'` store-only; `xlsx|docx|csv|tsv|xml → structured_parse`; else `ocr`); admission allowlist `intake.mjs:33-49` (14 spellings → 11 canonical mimes); facts routing `clara._enqueue_invoice_facts_core` live tip `0123_f_a7_gamma_egress.sql:1095-1244` spliced by `0177:44-82` (pdf/image NULL kind → classify after done extraction else `awaiting_extraction`; invoice/credit_note/debit_note/receipt → llm_witness; bank_statement → statement_facts; other kinds → terminal `failed/skipped_kind` 1155-1174; xml → local_facts unless bank_statement → skipped_type; csv/ofx → statement_parse only if bank_statement). Structured parser `structured-worker.mjs:122-130` (xlsx, docx, xml, else CSV/TSV); no OFX reader found.
Typed facts persistence: invoice facts are `clara.document_extractions` + `clara.document_regions` rows keyed by `field_path` (`0007_document_pipeline.sql:183-221`): source version = `version_n` + `superseded_by` + `documents.sha256` (unique per firm, 0003:68); page/region = `locator_kind ∈ {page_polygon, sheet_cell_range, row_col, paragraph_run}` + `locator jsonb`; `field_path text` nullable, no CHECK (:210); engine = `document_extractions.engine_id` + `document_processing_tasks.engine_id/engine_config/version_n`. Bank statements have typed tables `clara.bank_statements` / `bank_statement_lines` (`0038_wave_c_b_bank.sql:369-418, 537-575`). No invoice line-item table. Arithmetic validation is a read-time verdict: six-term identity in `_invoice_fact_state` v2 (`0100:500-530`); statements refuse `totals_unreadable`/`chain_broken` at persist (0038:1244,1253,1497). Nothing persists a per-fact validity flag.
field_path writers: `clara.persist_document_extraction` (`0123:1718-1725`) inserts verbatim, unvalidated (C33.4); `clara.persist_invoice_facts` (`0009:2063-2092`) closed 7-path allowlist, CLR10; `clara.persist_witness_facts` (`0095:329-332, 548, 572-574, 587-614`) closed arrays. Second `field_path` column on `entry_evidence` (0009:891, 451-453). No shared grammar.
Lineage: `documents.sha256`; `document_intakes.status verified|duplicate → adopted` (0007:110, 577-579, 1939, 2093); refile via `filing_corrections` + items (0007:310-363), `document_filings.retired_*`, `uq_document_filing_active (document_id, client_id) where retired_at is null` (0007:93-94); supersede `document_extractions.superseded_by` kind-scoped by `_tf_set_authoritative_extraction_0017` (0089:277-282); `bank_statements.superseded_by`; `request_reextraction` (0026:994).
C-19/C13.2: constraint `uq_document_filing_active`; idiom `get stacked diagnostics v_constraint = constraint_name` in `exception when unique_violation` (37 sites; 0148:432,504 self-check); no TypeScript reads `constraint_name`; `clara.file_document` live tip 0124:160 over `_file_document_write` (0009:2319-2326, CLR01).
UI: `apps/web/app/(firm)/clients/[clientId]/documents/page.tsx` → `components/documents/documents-workbench.tsx` (local selection state); `document-detail.tsx` composes `DocumentMetadata` (badges incl. `extraction: {status}`, kind, retention; per-task rows), `DocumentFilingsHistory`, `DocumentEvidence` (regions), `DocumentEntries`, `DocumentExtractPanel` (tiered via `get_document_extract`, 0090:1558), `DocumentAdmin`, `CorrectionWizard`; `document-facts-table.tsx:41-53` labels 7 invoice paths; `document-page-overlay.tsx` + `lib/documents/region-geometry.ts` highlight regions (UI-17/18 satisfied). Reads are PostgREST GETs (`lib/documents/reads.ts`, `loaders.ts:110-133`). No facts-state, Work link, capability statement, or unsupported-kind explanation. `upload-panel.tsx:34-45` has no `accept`; hint implies any file.
Tests: DB `rig-docs-*.test.mjs` family, `x54-region-ordinal`, `x1-reextraction`, `x27-filings-lock-order`, `f-a1-statements*`, `f-a2-witness-readers`, `f-a3-pr1a-extraction`, `spoken-for-documents`; 0177 evidence in `rig-docs-fixtures.mjs`, `x-receipt-routing.test.mjs`, `wave-b/wb-0020-legacy.test.mjs`, `f-a7-gamma-egress.test.mjs` (commit 2d87f8ec). Runtime `facts-gate-consumer.test.mjs`, `facts-gate-unit.test.mjs`, `f-a1-witness-*`, `f-a2-statement-*`, `intake-*`, `document-ingest-v2*`, `l9-classify-recall.test.mjs`. Web `document-facts-table.test.tsx`, `documents-a11y.test.tsx`, `document-metadata-viewer-gate.test.tsx`, `lib/documents/*.test.ts`, `tests/sql-oracle.test.ts` (SQL census), e2e `documents-viewer-walk.spec.ts`. No in-repo fixture corpus (`packages/runtime/tests/fixtures/classify/README.md`).

## 2. Gaps / rows
(a) no registry; (b) no persisted validity; (c) unvalidated field_path at one boundary, no grammar; (d) lineage not read together; (e) detail cannot show facts/operation/Work/capability; (f) no corpus/acceptance bar.
UI-17/UI-18 satisfied (re-verify under new states); H-02/H-03 out of scope (Bank contract); H-23 to build (named populations + freshness on the documents surface); H-53 satisfied verify-only (consent_evidence exempt, 0165:235); C-07 verify-only; C-19/C13.2 to build small (pin same-key replay + one distinct conflict; keep handlers narrow; C77.9 second-unique guard); C-37 to build as registry rows; C-85 verify-only; classifier recall out of scope (follow-up kept); C13.1 verify-only (record as registry rows); C33.3 out of scope; C33.4 to build (core); C83.X2 partial (acceptance for the two kinds from evidence).

## 3. Slice (one PR)
Migration `0191_document_capability_registry.sql` (+ preintegration gate): (1) `clara.document_capabilities(format, document_kind, custody, byte_extraction, typed_facts, business_operation, engine_id, registry_version int not null, basis text not null, recorded_at)` PK `(format, document_kind)`, each level ∈ `('supported','stored_only','unsupported','planned')`, forced RLS, owner policy + select to `clara_authenticated`, `clara_agent_ro`; seeded DERIVED from `documents_document_kind_check` × canonical intake mimes with a tail guard refusing roster disagreement (0165:250-345 pattern). (2) `clara._document_capability(p_format, p_kind) returns jsonb` STABLE SECURITY DEFINER, unknown pair ⇒ `typed_facts='unsupported'` (honest default). (3) `clara._assert_field_path(p_path text)` — grammar `^[a-z][a-z0-9_]*(\.[a-z][a-z0-9_]*)*(\[[0-9]+\])?$`, ≤128 chars, first segment ∈ registered namespaces (`invoice|statement|claim|document`); called from an append-only 0177-ceremony recut of `persist_document_extraction` wrapping `elem->>'field_path'`; leave `persist_invoice_facts`/`persist_witness_facts` allowlists untouched; tail probe: every allowlisted path passes. (4) `clara.document_fact_validations(firm_id, document_id, extraction_id, check_name, outcome ∈ (pass,fail,not_applicable,unmeasured), detail jsonb, evaluated_at)` with same-firm composite FKs, append-only, RLS; one row per persist for the invoice six-term identity and the statement chain/totals checks, computed from values the persist already has. (5) `clara.get_document_state(p_document uuid, p_client uuid) returns jsonb` → `{custody, byte_extraction, facts, operation, capability, lineage}` granted to `clara_authenticated` + `clara_agent_ro`; register in `apps/web/tests/sql-oracle.test.ts` and `rig-meta.mjs` READS.
Runtime: no new lane/engine. Record engine/model version on validation rows from the task's `engine_id`/`engine_config`; `intake-lanes.mjs` drift cell asserting every `laneSnapshot` branch matches a registry row; the OFX finding applied.
Web: `document-detail.tsx` gains four independent labelled states from `get_document_state` — Custody (stored / bytes verified / held), Extraction (pending/running/done/failed/stored_unparsed per task with engine id), Facts (none / partial / validated / invalid with the failing check named), Operation (uncoded / coded / posted entry link / not applicable for this kind); an honest capability line for `stored_only`/`unsupported`; fact → region link preserved; source version beside each fact block; copy in `messages/en.json` only. Update `upload-panel.tsx`'s hint to name the admitted formats from the registry (no `accept` promise beyond what the server admits).
Docs: ARCHITECTURE §7 (implemented registry + limits) and §11 row 文件; CONTEXT.md **Document capability**, **Typed fact**, **Field path** + the Knowledge/document-fact boundary sentence; PRD only if the supported-kind promise changes.
NOT in this slice: source-correction action (#646); Bank surface (H-02/H-03); Storage credentials (#620); real-model recall / corpus purchase; invoice line items; C33.3; #718; chat attachments.

## 4. TDD seams (red first)
1. `packages/db/tests/document-capability-registry.test.mjs` — every kind × every canonical mime has exactly one row; `ofx×bank_statement` reads `typed_facts <> 'supported'` unless a reader is proven.
2. `packages/db/tests/field-path-grammar.test.mjs` — `persist_document_extraction` with `'invoice..total'`, `'Invoice.Total'`, `'a'*200` each raise CLR10; `'invoice.total'` and `'statement.closing_balance'` persist; every allowlisted path passes `_assert_field_path`.
3. `packages/db/tests/document-filing-conflict.test.mjs` — same-key `file_document` replay returns the first receipt; a second distinct unique violation on the same path is not reported as a filing conflict.
4. `packages/runtime/tests/document-facts-validation-db.test.mjs` — a witness persist whose six-term identity fails writes a `fail` validation row naming the check; facts remain readable; no coding task is admitted.
5. `apps/web/components/documents/document-state-panel.test.tsx` — a `payroll_summary` PDF renders custody=stored, extraction=done, facts=unsupported kind with the registry sentence, operation=not applicable; never a success badge for facts.
6. `apps/web/e2e/documents-viewer-walk.spec.ts` (new cell) — failed arithmetic check shows the four states independently; the failing fact still links to its region; `?document=<id>` deep-links and Back restores the list; 320 px and 200% zoom; states announced by name.

## 5. Risks
Recutting `persist_document_extraction` is a live-body replacement on the hot ingest path (needs the 0177 ceremony; a writer-quiesce window at release — note in the migration header). A validator can turn silent acceptance into refusal for a non-conforming live engine path — the census + release-time probe mitigate.

## 6. Effort: L. Rig: PG cluster (assigned), runtime DB tests, web unit + Playwright. No Workflow world required (the facts gate is covered by `facts-gate-consumer.test.mjs`).
