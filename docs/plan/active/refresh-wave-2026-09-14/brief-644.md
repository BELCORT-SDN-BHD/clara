# Brief: #644 — one governed Knowledge record (typed assertions, extracted facts, preferences, policies)

## Orchestrator decisions (binding)
- Migration number: **0192** (`0192_client_knowledge_records.sql`). Migrations 0188–0191 belong to other in-flight tickets; your local DB will not have them.
- THIS PR = DB + promotion door + C13 web + the onboarding-commit hook + a non-frozen runtime helper module. **No `chatTurn_v19` in this PR**: the chat capture tool (`remember_client_information`) and the honest knowledge-pack context step are delivered later in ONE shared chatTurn_v19 (it also carries another ticket's tool). To make that step trivial, ship and unit-test `packages/runtime/lib/knowledge.mjs` now: `readKnowledgePack(sql, {clientId, purpose})` → `{status:'ok'|'unavailable', reason?, knowledge_version, records}` (never null; never throws into the caller) and `captureKnowledgeFor(sql, {...})` wrapping `capture_knowledge_for`. Document in your report exactly what v19 must wire.
- (a) `policy` is descriptive-only in this slice (nothing changes posting behaviour). (b) Legacy `clara.client_facts` stays byte-untouched and is UNIONed read-only into `list_client_knowledge` as `source_kind='legacy_client_fact'`; no dual-write. (c) Derive the A6 `onboarding_plan_items.item_key → knowledge_key` mapping from the item keys that exist in code today; list the mapping in your final report for owner ratification.
- Extracted facts are LINKED, not copied (agreed with #624): `knowledge_records` carries FK columns `source_document_id`, `source_extraction_id`, `source_region_id`, `source_field_path` onto `clara.document_extractions`/`document_regions` (0007:183-215). No extraction payload duplication.
- The watermark field is named `knowledge_version` (per-client max revision); #631's trace will record it.
- Trust rule enforced three ways (CHECK, trigger on `knowledge_keys.authority_bearing`, reads label trust and never emit non-`asserted` rows in a policy section). Restate in the migration header that tool registration comes only from the server-owned registry and that source text is supplied data.
- Do not touch counterparties/aliases/0176 (#647), projections/wiki (#663), retrieval (#658), `set_turnover_classification` (C79.3 follow-up), onboarding routes themselves (#648/#649) beyond the single commit hook.

## 1. Current state
Catalog: `0055_client_facts_trio.sql` — `clara.client_fact_keys` (347-368; global, code-populated, append-only; five keys: `entity_type`, `msic` (0055:370-381), `trade_nature` (0056:1233-1239), `customer_identity_policy` (0062:172-186), `banking_arrangement` (0121:4827-4833)); `clara.client_facts` (386-424; `fact_value` JSON string; `basis` + `basis_kind ∈ (owner_instruction|document|registry_lookup|interview_carryover)`; `source_document_id` only with document basis; supersede-by-insert, `uq_client_fact_live`; `_tf_client_facts_supersede_only` 428-455; delete/truncate refused); RLS humans select in-firm only (465-467); no runtime/agent grant. Door `clara.record_client_fact` (499-669): admin floor (510), `_reserve_op` (525), fail-closed catalog dispatch (604-607), supersede-then-insert under `for update` (615-627), `_audit` + `client.fact_recorded` registered `ignore` (485-497), humans only (664-669). Owner floor for lifting `customer_identity_policy` (0063:156-166); close-serialize trigger (0056:798). No kind/scope/applicability/effective dates/trust. Trust vocabulary exists only on `entry_evidence.provenance_tier` (0009:894).
Interview answers DO persist: `clara.update_onboarding_plan` (0017:2632-2706) upserts `onboarding_plan_items` (0017:1040-1060) with answer/answered_by/answered_at + revision snapshot; called from `packages/runtime/workflows/interview.v1.writer.ts:177` (clientOnboarding v1..v4, firmInterview v1..v3). `clara.commit_client_onboarding` (0017:2751-2842) writes nothing into `client_facts` (only 0055's one-time `entity_type` backfill 683-735; msic not backfilled 680-681) — CB-AE2E-030.
Context pack: `clara.get_context_pack` spliced body; facts reach it only via 0055:765 (`entity_type`, `msic` coalesce); carries `books_version` (0011:3354-3356) but no knowledge version. Chat `loadContextStepV10` (`chatTurn.v10.impl.ts:112-139`) reads purpose `wiki_coding` and `catch { contextPack = null }` — re-exported by `chatTurn.v18.impl.ts:32-44`. `claraWork_v2` does not read the pack. `record_client_fact` has zero callers outside `packages/db/tests`.
Correction/withdrawal: only supersession-by-new-value; no withdrawal verb, reason, retraction state, or reassessment.
C13 UI: `apps/web/app/(firm)/clients/[clientId]/knowledge/page.tsx` → `components/registers/knowledge-panel.tsx` (120 lines; `lib/registers/knowledge.ts` PostgREST reads; grouped by key; live + superseded history; read-only). Copy `messages/en.json` `ClientKnowledge` (11 keys). Writes go through `lib/doors.ts`.
Tests: `packages/db/tests/x55-client-facts-trio.test.mjs`, `x55-client-facts-door.test.mjs`, `x55-fixtures.mjs:202`, `name-only-guard.test.mjs`, `x56-concurrency.test.mjs:238-310`, `f-a3-pr1b-*.test.mjs`, `web-reads-and-doors.test.mjs:680`, `rig-meta.mjs:90`. No runtime pack test; no Knowledge e2e.

## 2. Gaps / rows
AC1 no kind/scope/applicability/effective/trust/revision kind. AC2 no promotion of committed answers; no "missing applicable facts" read. AC3 nothing distinguishes imported/inferred. AC4 no correction/withdrawal revision, no C13 surfacing. AC5 four faces collapse into `DataState`. AC6 client scope implicit; no promotion; no conflict question.
Rows: CB-AE2E-009, CB-AE2E-030, C79.6, C-41 to build; CB-AE2E-007 split (agreement half = 0185/#621 verify-only; Knowledge half to build); H-21 verify-only on the literal claim, to build on "into the unified service"; H-52 to build the applicability data (asking surface #648/#649); C48.5/C83.24 to build the record; C-52 out of scope; C79.3 out of scope with named follow-up.

## 3. Slice (one PR)
Migration `0192_client_knowledge_records.sql` (additive; `get_context_pack` NOT spliced): `clara.knowledge_keys` (code-populated catalog: `knowledge_key, kind ∈ (assertion|extracted_fact|preference|policy), value_shape, validated_against, allowed_values, description, scope_default, authority_bearing boolean`; seeds: the five legacy keys as `assertion` + the first real preference/policy keys the A6 plan produces); `clara.knowledge_records` (stable `record_id` + monotone `revision_n`; `firm_id`; `scope_kind ∈ (client|firm)` with `(scope_kind='client')=(client_id is not null)` (0017:1021 idiom) and composite `(client_id, firm_id)` FK (0055:418); `knowledge_key` → catalog; `kind` validated against it; `value jsonb`; `applies_when jsonb` + generated `applies_when_digest`; `effective_from/effective_to`; `source_kind ∈ (user_statement|interview|document_extraction|registry_lookup|imported_bundle|model_inference)`; `trust ∈ (asserted|extracted|imported_unverified|inferred)`; the four source FK columns; `source_work_id`; `asserted_by` + `recorded_via ∈ (human_ui|clara_runtime)`; `revision_kind ∈ (capture|correction|withdrawal)`, `revision_reason`, `supersedes_id`, `superseded_by/superseded_at` paired, `state ∈ (live|superseded|withdrawn)`; partial unique on `(scope_kind, coalesce(client_id,firm_id), knowledge_key, applies_when_digest) where state='live'`; supersede-only trigger cloned from 0055:428-455). Trust rule (see decisions). Functions: `capture_knowledge` (human lane; floor from the catalog: bookkeeper+ preferences, admin+ policy/authority-bearing), `capture_knowledge_for(p_asserted_by, …)` (runtime lane; verifies the named human's live active membership — 0017:2661-2666 precedent; never impersonates), `correct_knowledge`, `withdraw_knowledge`, `list_client_knowledge` (client rows shadow firm rows; UNION legacy), `get_knowledge_record`, `get_knowledge_history`, `get_knowledge_pack(p_client, p_purpose)` → `{knowledge_version, records[], status}`; `promote_plan_answers_to_knowledge(p_plan, p_op_key)` (idempotent, admin+, also `clara_runtime`; maps item_key → knowledge_key; `source_kind='interview'`, `trust='asserted'`, `asserted_by = item.answered_by`). Firm promotion requires explicit parameter + admin+. Events `knowledge.captured` (`ignore`), `knowledge.corrected`/`knowledge.withdrawn` (`wake`) registered (0055:485-497 idiom); no consumer built. RLS/grants: forced; owner all; `clara_authenticated` select in-firm; `clara_runtime` select + execute on `capture_knowledge_for`/`get_knowledge_pack`/`promote_plan_answers_to_knowledge`; `clara_agent_ro` select; no table DML grants. Preintegration gate module + `rig-meta.mjs` cohorts.
Runtime: `packages/runtime/lib/knowledge.mjs` (see decisions) + unit tests. No frozen closure changes.
Web: `/clients/:clientId/knowledge` list (typed category filter, scope/trust badges, applicability + effective dates, source link) + `/knowledge/[recordId]` detail (current value, revision timeline, source, Correct / Withdraw via `lib/doors.ts`); four distinct faces (successful empty, inaccessible source, contradictory — both live rows shown with a conflict face, failed read); `OnboardingChecklistCard`'s commit (`lib/onboarding/api.ts:153-180`) calls the promotion door after commit. e2e `knowledge-walk.spec.ts` + `knowledge-mock.mjs` (one-line hook in serve-built.mjs).
Docs: ARCHITECTURE §7 (replace the "尚未统一" sentence with the implemented record), §11 row Knowledge; CONTEXT.md (`Client knowledge` gains the four kinds + trust levels; new *Knowledge revision*, *Firm knowledge default*); PRD Knowledge behaviour.

## 4. TDD seams (red first)
1. `packages/db/tests/knowledge-records.test.mjs` — inferred assertion cannot be captured as policy: CLR10 `knowledge_trust_insufficient`, no row; same call at `asserted` by admin succeeds.
2. Same — correction and withdrawal are revisions: capture → correct → history has 2 rows (revision 1 superseded with `superseded_by`); withdraw adds revision 3 `withdrawn`; direct update/delete as `clara_fn_owner` raises.
3. Same — default client scope, explicit firm promotion: client preference invisible to another client; firm capture by a bookkeeper CLR04; after admin firm capture a client with its own live row still reads the client value.
4. `knowledge-onboarding-promotion.test.mjs` — commit a plan ⇒ `list_client_knowledge` empty; promote ⇒ one asserted/interview record per mapped item_key with `asserted_by = answered_by`; second call is a no-op replay.
5. `packages/runtime/tests/knowledge-lib.test.mjs` — `readKnowledgePack` with the door forced to raise returns `{status:'unavailable'}` (not null, no throw); on success returns `knowledge_version` verbatim.
6. `apps/web/components/registers/knowledge-detail.test.tsx` — a record with two revisions renders live value, history naming the correcting actor and reason, an unverified trust badge for `inferred`, a Withdraw control issuing `POST /rest/v1/rpc/withdraw_knowledge`; two live contradictory rows render both with a conflict face.

## 5. Risks
`applies_when` as jsonb + digest (equality conditions only in this slice). #647 identity untouched. #658/#663 consume the wake events later; keep `get_knowledge_pack` a new function so it can be superseded without a splice.

## 6. Effort: M–L (without the chat closure). Rig: PG cluster (assigned), runtime unit, web unit + Playwright.
