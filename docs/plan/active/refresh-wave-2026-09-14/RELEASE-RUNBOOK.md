# Hosted release ceremony — 0188…0195 + claraWork_v3/chatTurn_v19 image + web (owner-authorised in session; runbook revised after the six-verifier panel)

Release SHA `<SHA>` = main after wave 3 lands (b963edb8 + the pin fix). Label `refresh-<sha8>`.
Rollback points BEFORE: DB 182 / 0187 (re-read live), runtime v82 = `registry.fly.io/clara-runtime:refresh-98f6eec6`, web `742b09e9`.
Secrets rule: the fly token and the DSN are only ever substituted INLINE inside one pipeline; never assigned to a shell variable, never echoed, never in argv. The two web public vars come from `apps/web/.env.local` via an export loop, never echoed.

## 0. Rehearsal on rig187 (pristine 0187 chain from base 8f0de590, port 55460, db clara_187) — before production
- Census command shape that works from Git Bash: `MSYS_NO_PATHCONV=1 wsl -u root -- psql <conninfo> -v ON_ERROR_STOP=1 -Atc "<self-identify select>" -f /mnt/c/…/0191-field-path-census.sql`.
- DONE 2026-09-14: census on the pristine 0187 estate → `0191 census OK: 0 region row(s) total … 0 non-conforming`, persist_document_extraction at its expected 0123 body (sha 8ba95a52…); timed migrate 0188→0195 from the wave-3 tree → `8 new migration(s) applied · 190 total`, every prestate/tail passed, **wall 4 s** (T). Production adds pooler latency; the abort-decision rule: if the hosted run passes 10 minutes without returning, read pg_stat_activity on a second connection and decide from the ledger (0195 has a 20-minute statement_timeout; 0194 has no lock_timeout) — never kill it.
- Preflight read-relations probe shape (which relations the CLI reads): from `lib/rollback-preflight.mjs`.

## 0a. Gates before any production step (from the completeness critic)
- PR #818 `ci` SUCCESS and mergeStateStatus CLEAN; land by fast-forward; RELEASE_SHA = the sha that landed on main (never a literal from before).
- Owner confirmations, recorded: the owner said in-session (2026-09-14, this conversation) "可以, 这个 session 做" after I stated the release ships with the #631 activation assumption and the pre-v3 grandfather + preflight rule; #815 carries the ruling comment. Re-ask only if the owner objects.
- Build tree = RELEASE_SHA, `git status --porcelain` empty.
- DB RESTORE POINT — TAKEN 2026-09-14T07:30:58Z (before any write): `clara-clara-graphile-worker-workflow-workflow-drizzle-2026-09-14T07-30-58-980Z.sql` (213,307,106 bytes; schemas clara, graphile_worker, workflow, workflow_drizzle; target aws-0-ap-southeast-1.pooler.supabase.com:5432/postgres) + `clara-globals-2026-09-14T07-32-16-546Z.sql` (evidence only; roles come from deploy/roles-bootstrap.sql), under scratchpad/backup/ (gitignored class; copy off-machine if the window is delayed). 86 s wall. The WORKING invocation (three earlier forms failed: an unquoted `$PATH` with Windows parentheses; the DSN's `sslrootcert=` carrying a Windows path into WSL; env-only PGSSLROOTCERT not being treated as the exclusive CA by node-postgres): `export WSLENV='PGHOST:PGPORT:PGUSER:PGPASSWORD:PGDATABASE:PGSSLMODE:PGSSLROOTCERT/p:CLARA_BACKUP_DIR/p'` (no DATABASE_URL in WSLENV) then `<dsn pipeline> | MSYS_NO_PATHCONV=1 node scripts/ops/dsn-pipe.mjs -- wsl -u root -- bash -c 'unset DATABASE_URL WORKFLOW_POSTGRES_URL; export PATH="/opt/node/bin:$PATH"; export NODE_EXTRA_CA_CERTS=/mnt/c/Users/zhant/Desktop/clara-rebuild/ops/tls/pooler-ca.crt; cd /mnt/c/Users/zhant/Desktop/clara-rebuild/packages/db && node scripts/backup.mjs --profile full'`. If the window opens more than ~2 h after this stamp, re-take it (86 s). Original plan text follows.
  Plan text before any write: `backup:full` through the DSN pipe into WSL (pg_dump 17.11 is in WSL; Windows has none): `export WSLENV='DATABASE_URL:PGHOST:PGPORT:PGUSER:PGPASSWORD:PGDATABASE:PGSSLMODE:PGSSLROOTCERT/p'` then `<dsn pipeline> | node scripts/ops/dsn-pipe.mjs -- wsl -u root -- bash -c 'export PATH=/opt/node/bin:$PATH; cd /mnt/c/Users/zhant/Desktop/clara-rebuild/packages/db && node scripts/backup.mjs --profile full'` → record the artefact path. State what a restore does NOT return: Storage bytes, managed Auth config, engine state (packages/db/README.md). Supabase PITR status: unknown from this machine (MCP not authorised) — ask the owner if PITR is on.
- Web rollback lever (the only one-command rollback this release has): `pnpm --dir apps/web exec wrangler versions list` → the CURRENT active version id is `742b09e9-4637-4706-954a-5922f8266dfc` (tag refresh-98f6eec6, 100% since 2026-09-13T08:37Z — read via `wrangler deployments list`), so the one-command web rollback is `pnpm --dir apps/web exec wrangler versions deploy 742b09e9-4637-4706-954a-5922f8266dfc@100% --yes`; no DB implication. The runtime cannot roll back past 0195; the database only via 0196 + a compatibility image, or the restore.
- Boot-log order to expect on the new image: serving → stranded bodies n=0 → durable world started → bundle v1 → v2 → v3 → CONTROL listening → LEADER acquired.
- From Git Bash, any wsl argument that is a /mnt/c path needs `MSYS_NO_PATHCONV=1` (else Git Bash rewrites it to C:/Program Files/Git/mnt/...).

## 1. fly auth — inline only
`FLY_API_TOKEN="$(grep -E '^access_token:' ~/.fly/config.yml | awk '{print $2}')" flyctl <cmd>` on every call.

## 2. Probe machine (OLD image, world off) — destroyed at the END OF STEP 3, before any deploy
`… flyctl machine run registry.fly.io/clara-runtime:refresh-98f6eec6 --app clara-runtime --name probe-<sha8> --region sin --vm-memory 512 --env CLARA_START_WORLD=0 --env PORT=3200 --command "sleep infinity"` → <probe-id>.

## 3. Read-only reads over the probe DSN (each a single inline pipeline: `fly ssh console -a clara-runtime --machine <probe> -q -C "sh -c 'printf %s \"\$WORKFLOW_POSTGRES_URL\"'" </dev/null 2>/dev/null | tr -d '\r\n' | node scripts/ops/dsn-pipe.mjs -- <cmd>`)
3a. Ledger: `select count(*), max(version) from clara.schema_migrations` → expect 182 / 0187. Then `select version, checksum from clara.schema_migrations where version='0187_legal_v1_beta_publication'` → MUST be `5fc28e38283088dddef0150384e9a18949301c9bf0ee89cf52957a2f9c5abd46`; any other value → STOP (migrate.mjs would abort the whole run on checksum drift).
3b. Quiescence census (read now, RE-READ after the machine stop): `agent_tasks` running/stopping; `agent_interruptions` pending; `accounting_work` non-terminal; `workflow.workflow_runs` non-terminal (a stale `clientOnboarding_v4` run is known); PLUS unbound/held tasks: `select kind, status, count(*) from clara.agent_tasks where workflow_run_id is null and status in ('queued','running','held','awaiting_input') group by 1,2` cross-checked with `clara.wake_engine_sources` (an unplaceable row makes step 9's preflight exit 1 on `unbound_task` too — record it).
3c. 0191 CENSUS via WSL psql, DSN through the pipe with WSLENV so the child env crosses into WSL: `export WSLENV='DATABASE_URL:PGHOST:PGPORT:PGUSER:PGPASSWORD:PGDATABASE:PGSSLMODE:PGSSLROOTCERT/p'` then `<dsn pipeline> | node scripts/ops/dsn-pipe.mjs -- wsl -u root -- psql -v ON_ERROR_STOP=1 -c "select inet_server_addr(), inet_server_port(), current_database(), current_user, (select max(version) from clara.schema_migrations)" -f /mnt/c/Users/zhant/Desktop/clara-rebuild/packages/db/deploy/0191-field-path-census.sql`. The self-identification line MUST name the pooler host and `0187_…` before the census is trusted. Any non-conforming row → STOP, report, do not migrate.
3b2. THE OTHER FOUR DATA PRESTATES (one query, expect 0 / 5 / 0 / 0; re-read after the stop): `select (select count(*) from clara.user_preferences where interface ? 'workViews') as workviews_rows, (select count(*) from clara.client_fact_keys) as legacy_fact_keys, (select count(*) from clara.client_egress_purpose_consents where purpose='accounting_work') as aw_consents, (select count(*) from clara.egress_dispatch_authorizations where document_sha256 is not null and purpose in ('wiki_synthesis','bank_matching')) as doc_sha_violations` — read 0189:202, 0192:136, 0195:412, 0195:462 for the exact predicates before running; any mismatch → STOP.
3b3. Every non-terminal `workflow.workflow_runs.name` must be one of the new image's bodies (51): `select distinct name from workflow.workflow_runs where status not in (<terminal set>)` — a name outside the roster would make the boot census refuse the World.
3d. Preflight login probe: through the same pipe, `node -e` counting rows of the seven relations the preflight reads (`workflow.workflow_runs`, `clara.agent_tasks`, `clara.wake_engine_sources`, `clara.schema_migrations`, …) — a denied relation means step 9 needs a different DSN; decide now.
3e. Capture the hosted PRE-IMAGES for the rollback migration (read-only): `pg_get_functiondef` of `clara._record_journal_entry_core`, `clara.admit_journal_work`, `clara._close_gate_closing_stock`, `clara._tf_accounting_work_immutable`, `clara.persist_document_extraction`, `clara.save_my_preferences`, `clara.prepare_egress_dispatch`, `clara.grant_client_egress_purpose`, `clara.revoke_client_egress_purpose`, `clara.activate_client_egress_purpose`, `clara.deactivate_client_egress_purpose` → scratchpad `pre-images/`. The rollback migration is DRAFTED and scratch-proven: `docs/plan/active/refresh-wave-2026-09-14/drafts/0196_restore_pre_0195_bodies.sql` (+ `0196-README.md`): it restores ONLY the two walls (the posting core to its 0194 body `eca58b99…`, `persist_document_extraction` to its 0123 body `8ba95a52…`); the nine other recut bodies stay (restoring `prepare_egress_dispatch` would strand parked v3 runs); the compatibility image MUST still carry `claraWork_v3` (frontier rises to 0196). At step 3e compare the hosted pre-images with the draft's pins; if they differ, the draft must be re-pinned before it could ever be used. To apply it (only in an incident): the dir given to migrate.mjs must hold ALL 190 release files plus the draft (F4 history check).
3e2. Extract the v82 bundle for step 9 from the probe (no docker needed; the registry refused a docker pull): `… flyctl ssh console -a clara-runtime --machine <probe> -q -C "cat /app/.output/server/index.mjs" </dev/null 2>/dev/null > scratchpad/v82/index.mjs` and compare `sha256sum` inside the machine (`-C "sha256sum /app/.output/server/index.mjs"`) with the local file (line endings must not be translated — run from Git Bash with `--binary` semantics or verify the sha).
3f. Keep the probe alive through step 6 (the live machine is STOPPED during the migration, so the probe is the only DSN source then); destroy it at the END of step 6, before step 7's deploy: `flyctl machine destroy <probe-id> --force`; `flyctl machine list -a clara-runtime` → exactly one machine (48ee715b763048).

PHASE B RESULTS (2026-09-14, probe 7845745fed2058, all read-only): ledger 182 / 0187, 0187 checksum MATCH; every recut body at its pin (0189 save_my_preferences, 0191 persist, 0194 core/admit/gate/trigger, 0195 consume unmoved, 0195 five-set 5/5); data prestates 0 / 5 / 0 / 0; 0191 census `OK: 1906 region row(s) total, 0 null, 0 non-conforming`, persist at 0123 body; grant probe: all seven relations readable by the WORKFLOW_POSTGRES_URL login; quiescence: accounting_work non-terminal 0, agent_interruptions pending 0, workflow_runs non-terminal 1 (`clientOnboarding_v4`, carried by the new image), agent_tasks held 10 (orphan wake tasks from 2026-09-03…05 with no wake source row → the preflight refuses `unbound_task` against ANY target, pre-existing; the boot guard counts runs only, so the World starts — filed as a follow-up); preflight with the NEW bundle: 51 bodies, frontier rule OK, `REFUSED (unbound_task)` for those ten only; v82 bundle extracted (sha 6adef601…, 9,537,442 bytes); hosted pre-images of 14 bodies captured to scratchpad/ceremony/hosted-preimages/.

## 4. Runtime image first (build-only + push, by digest)
`… flyctl deploy --config packages/runtime/fly.toml --build-only --push --image-label refresh-<sha8> --build-arg CLARA_BUILD_SHA=<SHA>` (~5 min) → record the `sha256:` digest from the output; release by `registry.fly.io/clara-runtime@sha256:<digest>` in step 7.

## 5. Web build + upload (WSL as root)
```
wsl -u root
export PATH=/opt/node/bin:$PATH
git config --global --add safe.directory /mnt/c/Users/zhant/Desktop/clara-rebuild   # once
cd /home/runner/clara-deploy && git fetch origin main && git checkout --detach <SHA> && git status --porcelain   # prints nothing
corepack pnpm install --frozen-lockfile
while IFS= read -r line; do export "$line"; done < <(grep -E '^(NEXT_PUBLIC_SUPABASE_URL|NEXT_PUBLIC_SUPABASE_ANON_KEY)=' /mnt/c/Users/zhant/Desktop/clara-rebuild/apps/web/.env.local)
CLARA_BUILD_SHA=<SHA> corepack pnpm --filter @clara/web cf:build
corepack pnpm --dir apps/web exec wrangler versions upload --tag refresh-<sha8>   → <web-version-id> (not promoted yet)
```

## 6. Writer quiescence → migrate (0189/0191/0194/0195 recut live bodies — 0189 recuts save_my_preferences; 0194 takes ACCESS EXCLUSIVE on accounting_work with no lock_timeout)
6a. `flyctl machine stop 48ee715b763048` → `flyctl machine status 48ee715b763048` shows `stopped`. Chat and Clara return 502 `runtime_unreachable` during the window; the rest of the app works.
6b. RE-READ 3b (the binding quiescence reading) and `pg_stat_activity`: no non-idle runtime session, no transaction older than ~5 s on `clara.accounting_work` / `clara.document_regions`; keep `pg_cancel_backend` ready (cancel a blocker, never the migration mid-ALTER). RE-RUN 3c (the census binds the estate the migration meets).
6c. `(cd packages/db && <dsn pipeline> | node ../../scripts/ops/dsn-pipe.mjs -- node scripts/migrate.mjs)` → expect `8 new migration(s) applied · 190 total`; expected wall time T from step 0; if > 3×T do not kill it — read `pg_stat_activity` on a second connection and decide from the ledger.
6d. FAILURE BRANCH — decide from the LEDGER, not from which prestate spoke: re-read `select count(*), max(version) from clara.schema_migrations` (each file commits alone; the frontier is definite). (i) max < 0191 → `flyctl machine start 48ee715b763048` on the old image is legal; report and stop. (ii) 0191 committed but < 0195 → starting v82 is the least-bad restore but its structured worker emits XLSX `r=` paths 0191's new grammar refuses (CLR10, task left running): either drive the chain forward to 0195 and release v3 (preferred), or restart v82 only after accepting/announcing that hazard. (iii) max = 0195 → the OLD image is NOT a legal boot target (the preflight forbids it); drive forward: release the v3 image (step 7). Re-record the rollback point from the ledger, not from the pre-window reading.

## 7. Release the runtime (by digest) and start
`flyctl machine list -a clara-runtime` (one machine) → `flyctl deploy --config packages/runtime/fly.toml --image registry.fly.io/clara-runtime@sha256:<digest>` → `flyctl machine start 48ee715b763048` (a deploy onto a stopped machine leaves it stopped) → `flyctl machine list` again (one machine, new image) → wait `/ready` 200 → `flyctl logs`: `serving git_sha=<SHA> frontier=0195_…(190) bodies=… pins … claraWork=claraWork_v3 chatTurn=chatTurn_v19`, three bundle banners (v1/v2/v3), `stranded bodies n=0` BEFORE `durable world started`, `CONTROL listening`, `LEADER acquired`. Signed-in `GET /api/build-info`: `git_sha` = <SHA>, pins, bodies.

## 8. Promote web + smoke
`pnpm --dir apps/web exec wrangler versions view <id>` (six secrets present) → `versions deploy <id>@100% --yes`. Smoke signed out: `/login`, `/favicon.ico`, `/icon.png` 200; `/pending`, `/api/build-info` 307→login; cross-origin POST `/auth/confirm/resend` 403; `/checkout/cancel` 307; `/settings/registrations` and `/admin/registrations` 307→`/operator`. Signed in as the BELCORT operator: `/operator` 200 (first live walk), `/work` 200, `/clients/<id>/plans` and `/plans/new` 200, `/clients/<id>/accounting/adjustments` and `/adjustments/new` 200, `/clients/<id>/knowledge/<recordId>` 200; `/api/build-info` git_sha pairs with the runtime.

## 9. Rollback-preflight demonstration (#637 hosted half, READ ONLY — do NOT roll back)
Preferred: the v82 bundle extracted in step 3e2 (`scratchpad/v82/index.mjs`) → `<dsn from the LIVE machine> | node scripts/ops/dsn-pipe.mjs -- node packages/runtime/scripts/rollback-preflight.mjs --target-bundle <that file>` → expect REFUSED, exit 1, with BOTH reasons printed: the frontier rule section `THE DATABASE'S OWN RULE … REFUSED — claraWork_v3 required, not carried` (the demonstration) and the pre-existing `unbound_task` for the ten orphan held wake tasks (noise; see the follow-up issue). Fallback: `--supported <v82 body list>` which prints the `*** UNVERIFIED SET ***` banner — label it as such. (v82's `/api/build-info` has no `bodies`, so `--target-build-info` would exit 2 "could not answer".) Record; the ONLY honest rollback is 0196 (step 3e draft) + a compatibility image.

## 9b. Lock the frozen manifest (second commit after the release)
`node scripts/check-frozen-workflows.mjs --lock-deployed` — confirm the unlocked set is exactly the 17 v3/v19 entries first (the command locks every unlocked entry) → branch → PR → `ci` → fast-forward. Note in PROGRESS that main's tip then differs from the deployed image's git_sha by this one docs/manifest commit.

## 10. Record + close
Rollback points AFTER (from the ledger): DB 190 / 0195; runtime `refresh-<sha8>` (vN, digest); web <id>. Post hosted evidence on #612 (the open summary issue, in full) and one line on each closed ticket saying it is the hosted completion of a locally-closed ticket (#631 #643 #644 #640 #624 #637 #615 #641 #620 #622 #619); the web rollback command and the runtime/database asymmetry go into PROGRESS Current State; update PROGRESS.md Current State + Known Issues; memory `awaiting-release-ceremony` gains: 0187-checksum check, census self-identify + WSLENV pipe, post-stop re-census, release by digest, `--build-arg CLARA_BUILD_SHA`, probe destroyed before deploy, the ledger-driven failure branch, the lock-deployed step; fix `apps/web/README.md:17` redirect row. Destroy any scratch container; `flyctl machine list` final.
