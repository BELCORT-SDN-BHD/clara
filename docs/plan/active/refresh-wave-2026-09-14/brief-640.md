# Brief: #640 — explicitly authorised recurring / reversing accounting plans

## Orchestrator decisions (binding)
- Migration number: **0193** (`0193_accounting_plans.sql`). Migrations 0188–0192 belong to other in-flight tickets (operator support, work list reads, document byte door, document capability registry, knowledge records); your local DB will not have them — that is fine; do not depend on them.
- `accounting_work.purpose` stays `journal_entry`; the plan link lives on the occurrence row. Add `clara.list_accounting_plan_occurrences(p_plan)` and a tiny read `clara.get_work_plan_origin(p_work uuid)` (null when not plan-initiated) so B3 Work detail can show "From plan <purpose>" — a small, localised addition to `apps/web/components/work/work-detail.tsx` (another ticket, #641, is restructuring the same component into Tabs concurrently; keep your edit to one identity-block row).
- Q1 (double posting vs the live 0045 adjustment-template lane): no refusal. `create_accounting_plan` returns an advisory `overlap_warning` naming any live signed `adjustment_templates` row for the same client whose line account codes intersect; the form renders it as a persistent Alert. The 0045 lane is otherwise untouched (Q4). File the convergence as a follow-up in your report.
- Q2 cadence: scan every leader cycle, DB-gated on `due_date <= (now() at time zone tz)::date` with a partial index; record the cadence decision in ARCHITECTURE §8 (C54.3).
- Q3 timezone: closed one-member CHECK `Asia/Kuala_Lumpur` (additive later).
- Q5: `authority_kind = 'explicit_instruction'` only; `authority_rule` is refused with CLR10 `authority_rule_unsupported`.
- Plan kinds in this slice: `recurring_journal`, `reversing_journal` only. Depreciation/accrual/amortisation/close adapters are excluded and named as such.
- Web route: `/clients/[clientId]/plans` (+ `?plan=<id>` detail Sheet or a `/plans/[planId]` route — choose the stable-route form for full detail per appendix D). Repoint the existing `plans` nav item (`tree.ts:312`) to the new route; leave `registers?tab=adjustments` reachable at its own URL (no redirect). Persistent statement on list and detail: accounting schedules create journal Work; they never initiate bank payments or mandates. There is no global enable-agentic switch (already true by absence — add a negative test).
- Runtime: `packages/runtime/lib/plan-occurrences.mjs` on the `reconciler-adjustments.mjs` shape (feature-detect, bounded, isolated errors), wired in `reconciler.mjs` + `leader.mjs`. Dispatch of admitted Work stays `reconciler-work.mjs` §A. A runtime e2e leg on the real Postgres World (`plan-occurrence-e2e.mjs`, standalone, skip-clean when 0193 absent; wire it into `.github/actions/db-live-gates/action.yml` after `work-cancel-e2e`).

## 1. Current state
The wave_d_b2 lane is LIVE: `0045_wave_d_b2_recurring_adjustments.sql` — `clara.adjustment_templates` (1139–1255; cadence monthly/annual, start/end, auto_reverse, lines, content_hash, lineage), `adjustment_runs` (1459–1482), `adjustment_pair_reversals` (1532); doors `propose_adjustment_template` (3744, bookkeeper), `sign_adjustment_template` (4264, admin), `retire_adjustment_template` (4361), `run_adjustment_occurrence` (5301), `run_adjustment_manual` (5319), `adjustment_run_due` (5513), pair reversal verbs, `list_adjustment_templates`/`list_adjustment_runs`/`get_adjustment_run` (6637–6686); runtime grants (6737, 6740–6741); runtime belt `packages/runtime/lib/reconciler-adjustments.mjs` (feature-detect 84–91, cap 76, op-key idiom), wired `reconciler.mjs:31,717`, `leader.mjs:124,214`; web `components/registers/adjustments-register.tsx` at `registers?tab=adjustments`. It posts JEs directly (no Work, no logical_op_id, no receipts, no questions, no cancel ordering), uses a two-person propose→sign ceremony, has no pause/resume, no timezone, no preview, no unique (template, period) key (idempotency by predicate `ix_je_adj_occurrence` 1660 + `uq_adjustment_runs_op_key` 1487), unscoped automatic catch-up (`_adj_oldest_unmet_period` 5343).
Wake sources: `clara.wake_engine_sources` two rows both `enabled=false` (0133:788–792), writer `set_wake_source_enabled` (0133:283, operator-gated) — wrong vehicle for plans.
Due → Work: `clara.admit_journal_work(p_client, p_author, p_intent_key, p_basis, p_basis_origin, p_source_refs, p_model)` (0178:832) granted to `clara_runtime`; takes the authorising human as a parameter and rechecks membership active, role ≥ bookkeeper, client active, basis validity; idempotent on `uq_accounting_work_intent (firm_id, client_id, intent_key)` (0178:336). Posting credential is minted OBO the Work `initiator` (`claraWork.v1.tools.ts:199`); `mint_wake_credential` refuses a de-authorised OBO (`authority_lost`, 0184). `reconciler-work.mjs:397–412` §A is the primary dispatcher for DB-only admission.
Settlement boundary: 0184 — `accounting_work` row lock; lock order accounting_work → agent_tasks → agent_interruptions; `_record_journal_entry_core` refuses `work_cancelled`/`work_settled` before `_reserve_op`; `settle_work_run` forces completed when a committed receipt exists; `initiated_by` vs `initiator` (0184:401). Rig `packages/db/tests/work-cancel.test.mjs`.
UI: nav item `plans` → `/clients/:id/registers?tab=adjustments` (`tree.ts:312`); no plan form, no pause/resume/end, no preview, no run→Work link, no bank-payment statement; no global agentic switch anywhere.

## 2. Gaps / rows
All plan-shaped acceptance criteria are to build; Work-side rechecks and settlement are verify-only (0178/0184). Rows: C-05 to build (default-agentic + explicit future schedules; no global switch — negative test); C-21 verify-only; C-39 to build (revision model); C48.1 out of scope; C54.3 to build (exact due-key idempotency; argued cadence); C55.10/C83.4 out of scope as close adapters (the generic law is proved on the journal adapter); C88.13 to build the representative synthetic; real-environment evidence separately owed (not claimed).

## 3. Slice (one PR)
Migration `0193_accounting_plans.sql` — tables (force RLS, composite `(client_id, firm_id)` FKs, no app-role DML, append-only where stated): `clara.accounting_plans` (kind CHECK, status active/paused/ended, purpose, authority_kind CHECK, authority_ref jsonb — the Work/chat task carrying the instruction, `authorised_by uuid not null → clara.users`, `authorised_at`, `current_revision`, lifecycle stamps); `clara.accounting_plan_revisions` ((plan_id, revision) unique; frequency monthly/quarterly/annual; day_rule day_of_month/last_day_of_month; day_of_month; timezone CHECK; effective_from/effective_to; basis jsonb + basis_digest; auto_reverse + reversal day rule; created_by; superseded_at; one live revision per plan; immutability trigger); `clara.accounting_plan_occurrences` (**unique (plan_id, due_date)**, unique (work_id), revision, intent_key, work_id → accounting_work, admitted_at, outcome jsonb; append-only). Human doors (`_human_ctx(role_rank('bookkeeper'))`, `_reserve_op`/`_finish_op` op-key idempotency, `_audit`, typed CLR refusals with `detail.reason`): `create_accounting_plan` (with `overlap_warning`), `revise_accounting_plan`, `pause_accounting_plan`, `resume_accounting_plan`, `end_accounting_plan`, `preview_accounting_plan(p_plan, p_count)` (STABLE; next N due dates + rendered basis), `list_accounting_plans(p_client)`, `get_accounting_plan(p_plan)`, `list_accounting_plan_occurrences(p_plan)`, `get_work_plan_origin(p_work)`, `request_plan_catch_up(p_plan, p_from, p_to, p_op_key)` (refuses a window before `effective_from`: CLR10 `catch_up_before_authority`; oldest-first; bounded). `clara.wake_due_plan_occurrences(p_limit int)` — EXECUTE to `clara_runtime` only: oldest-due-first, bounded; per plan `for update`, re-read status / authoriser membership / client status / live revision; compute due_date in the revision timezone; insert occurrence (unique violation ⇒ converge, return existing work_id); call `clara.admit_journal_work(client, authorised_by, 'plan:<plan>:r<rev>:<due_date>', basis, 'user_direct', '[]'::jsonb, model)` — the shared 0178 door. Lock order header: accounting_plans → accounting_work → agent_tasks → agent_interruptions. Tail assertions in house style; preintegration gate module; `rig-meta.mjs` cohort entries.
Runtime: `lib/plan-occurrences.mjs` (see decisions); `tests/plan-occurrence-e2e.mjs` on the real World: two belt passes in one cycle + SIGKILL between admission commit and enqueue ⇒ one entry, one receipt, one occurrence.
Web: list (purpose, kind, frequency + timezone, effective dates, status, next occurrence), detail (basis lines, authority source + authoriser + instruction link, next-occurrence preview, occurrence history linking Work + receipt, revise/pause/resume/end with Dialog confirmations), create/edit forms reusing the composer's money input (`MoneyInput`/`parseAmountToCents`) and `JournalBasisFields` where possible; the persistent non-toast bank-payment statement; all applicable states (loading/empty/partial/stale/invalid/saving/denied/failed/recovery). e2e `plans-walk.spec.ts` + `plans-mock.mjs` (register with a one-line hook in serve-built.mjs).
Docs: ARCHITECTURE §8 (current implementation naming 0193; cadence decision), §6 lock order gains the plan rung, §11 row 自动计划与 close; CONTEXT.md **Plan** and **Occurrence**; PRD §5.4 (timezone, explicit catch-up scope).

## 4. TDD seams (red first)
1. `packages/db/tests/accounting-plan-occurrences.test.mjs` p640.occ.first — plan with a past `effective_from`; one `wake_due_plan_occurrences(10)` yields exactly one occurrence and one `accounting_work` row with `intent_key = 'plan:<plan>:r1:<due_date>'`, `purpose='journal_entry'`, `status='queued'`, `initiated_by = initiator = authorised_by`, `logical_op_id = 'work:'||work_id||':journal_entry:1'`.
2. p640.occ.duplicate — two sessions scan concurrently with a barrier; exactly one occurrence and one Work; the second answer names the same work_id with `converged:true`.
3. p640.occ.pause_race — with an occurrence admitted and running, pause returns `paused`, does not cancel the in-flight Work, blocks the next scan, and the in-flight Work settles completed with its receipt.
4. `accounting-plans.test.mjs` p640.auth.loss / p640.auth.period — deactivated authoriser ⇒ scan refuses that plan with the 0178 CLR04 reason recorded on `outcome`, nothing admitted, plan stays active; locked period ⇒ occurrence admitted but Work settles refused with the period reason and is not re-admitted.
5. p640.catchup.scope — never admits before `effective_from`; catch-up window before authority ⇒ CLR10; in-range window admits oldest-first, at most `p_limit` per call.
6. `apps/web/e2e/plans-walk.spec.ts` plans.walk.authority — basis/authority/preview/receipts/disclaimer render; pause switches the preview to a paused explanation; 320 px, 200% zoom, keyboard, focus return from the pause Dialog, reduced motion.

## 5. Risks
Double posting with the 0045 lane (advisory warning only). Frozen v1/v2 error tables map `(CLR13, work_cancelled)` to `state_changed` — do not assert otherwise. A 03:00 occurrence without a live egress consent must refuse legibly on the occurrence row once #631 lands (note only).

## 6. Effort: L. Rig: PG cluster (assigned) + real Postgres World for the e2e leg (build the runtime once), web unit + Playwright.
