# #631 — STANDARDS review

Diff `46440525...HEAD` (6 commits, 49 files, 7214+/35-), non-empty, confirmed via `git diff --stat`. Cross-checked against WORK-ORDER.md, RIG.md, AGENTS.md, CONTEXT.md, ARCHITECTURE.md §5/§6/§10/§11, brief-631.md, `claraWork.v2.*` (byte diff), 0094/0123/wiki-projection.mjs precedents. Ran `check-frozen-workflows.mjs` (OK, 273 verified), `check-parts-parity.mjs` (REFUSED — confirmed below), `pnpm --filter @clara/web lint` (exit 0).

## Documented-standard checks — all HARD, all pass

- **Frozen bodies untouched** (WORK-ORDER #8). `git diff --stat -- packages/runtime/workflows frozen-workflows.json` shows only new `claraWork.v3.*` (7 files) + `registry.ts` (+38/-1, repoint + exports) + additive manifest rows. `check-frozen-workflows.mjs`: OK.
- **`check-parts-parity.mjs` REFUSES** at `packages/runtime/lib/periodic-adjustment-basis.ts:73` (`...sharedShape`). Verified: `git diff 46440525...HEAD --stat -- packages/runtime/lib/periodic-adjustment-basis.ts` is empty; the file is absent on `origin/main`; `git log --diff-filter=A` names `41b48385` (#643) as sole author. Not #631's, not a v3 part — confirmed, matches the worker's claim exactly.
- **Manifest/test hygiene**: `apps/web/test/manifest.txt` gained exactly the 2 new test files, alphabetical; `pnpm lint` (message-key check) reports 2546 keys resolve, 0 missing; `en.json` carries the new `WorkDiagnostics.*` and `WorkDetail.egressNotAuthorized.*` keys.
- **House shape, `claraWork.v2.*` vs `.v3.*` byte diff**: every delta is a roster item — `dispatchEgress()` prepares then consumes (explicit BEGIN/COMMIT) immediately before `agent.generate`, matching `lib/wiki-projection.mjs:484-505`'s two-phase shape exactly; trace rows at all 4 phases (`claraWork.v3.impl.ts:188,335,388,440,452,605`); `claraWork.v3.errors.ts` diff shows exactly the 3 `#737` pairs (`work_cancelled→cancelled`, `work_settled→refusal`, `egress_not_authorized→refusal`), delegating to v2 as v2 delegates to v1; `claraWork.v3.parts.ts` diff shows exactly `client_id` added to `work_status` (#738), `work_result` restated byte-identical, `work_question` by import. No unreviewed object spread (grepped). `get_journal_entry_for` continues via inherited `confirmEntryStep` (v1's, unchanged) — the bare `get_journal_entry` call survives only in frozen `chatTurn.impl.ts:112`; the census waiver was **not** retired despite the brief saying "retire it" — correctly, since v3 cannot touch a frozen v1 file, and the worker documented this deviation with the grep evidence rather than falsely claiming success.
- **0094:105-112 positive binding**: both `prepare_work_egress_dispatch` and `record_work_execution_trace` join `agent_tasks → accounting_work` on `(work_id, firm_id)` and require `t.kind='accounting_work'`, never resolving to a different firm/kind — matches.
- **Deadlock discipline (§6/0184)**: migration header + `lib/work-trace.mjs` header both state "every row except settle is written outside the posting transaction"; verified `claimWorkRunStepV3`/`runWorkSegmentStepV3` call `traceSafely` via a separate `pools().withRuntime`, never inside the posting transaction opened by `wake_record_journal_entry`; SECTION 12's insertion is a plain read of `egress_dispatch_authorizations`, no new lock. Matches.
- **Docs**: ARCHITECTURE §5/§10/§11 and CONTEXT diffs verified — factual, hosted marked pending, activation assumption verbatim-identical across migration header, ARCHITECTURE §10, and the worker's report (diffed by eye, matches).
- **Provider-eval gating**: `grep -rn "CLARA_PROVIDER_EVAL\|provider-eval" .github/` — zero hits. File is `work-journal-eval.mjs`, outside the `tests/**/*.test.mjs` glob `package.json` runs. Confirmed never-in-CI by absence.

## The ruling the worker asked for — item (1)

**Confirmed as a real, tool-enforced consequence, not a courtesy.** `check-frozen-workflows.mjs`'s import-closure regex (`allImportsOf`, line 167) matches dynamic `import(...)` as well as static `from`, and both `capability-registry.mjs` and `work-trace.mjs` are reached only via `await import(...)` inside `claraWork.v3.impl.ts`/`.tools.ts` — no other source file imports either. Both appear in `frozen-workflows.json` (hash-locked now, `deployed` absent — i.e. merged-but-undeployed, so `--update` can still re-baseline locally per the tool's own comment). Once #637's `--lock-deployed` ceremony runs, both become permanently hash-immutable exactly like a workflow body — this is the tool's documented design (comment: "freeze the transitive closure... so a step body it imports can't change while the workflow hash stays green"), not a bug. **SHOULD**: record this ruling in ARCHITECTURE §10 (the worker's follow-up #6 is the right place) before #637's ceremony runs, so a future redaction hardening isn't discovered to be blocked only after deploy.

## Smells (Fowler baseline) — judgement calls, none blocking

- **Divergent Change / Shotgun Surgery** on the 4th full-body recut of `_record_journal_entry_core`: overridden — this is the repo's established, sha-pinned, tail-verified migration-safety pattern (0182/0184/0194 do the same), not organic drift. Not a finding.
- **Speculative Generality** (NOTE, judgement): `DATA_CLASSES` in `capability-registry.mjs` declares `"public"`, unused by any of the 5 registered capabilities. Harmless, closed vocabulary meant to be extended — not worth blocking.

## Totals

0 BLOCKER, 1 SHOULD (document the frozen-closure ruling before #637's lock ceremony), 1 NOTE (unused `public` data class). Worst finding: the frozen-closure consequence for `work-trace.mjs`/`capability-registry.mjs` — real, correctly self-identified by the worker, not yet written down as an owner-facing decision. Everything else checked (migration tail census, deadlock discipline, parts-parity attribution, provider-eval isolation, doc factuality) verified clean against primary sources.
