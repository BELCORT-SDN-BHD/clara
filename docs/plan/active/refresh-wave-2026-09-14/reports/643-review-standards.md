# #643 — Standards review (branch `impl/643-periodic-adjustments` vs `main`)

Diff confirmed non-empty (40 files, +8661/-43; 9 commits). `check-frozen-workflows.mjs` → OK, 264 files verified; `git diff main...HEAD --stat -- packages/runtime/workflows frozen-workflows.json` → empty. No frozen-body breach anywhere in this branch.

## Documented-standard breaches

**BLOCKER — brief-643.md's web reuse list, silently narrowed.** Brief §3 Web: "the form reuses `JournalBasisFields`, **the evidence chooser**, `lib/work/journal-draft.ts`'s intent key and the composer's lost-response arm." Three of four are genuinely reused (verified: `JournalBasisFields`/`fieldElementId` imported; `journal-draft.ts` reused per its docstring; the lost-response arm mirrors the composer's). The evidence chooser is not merely thin — it is **entirely absent**: `periodic-adjustment-form.tsx` never imports `listClientEvidenceDocuments`/`listSpokenForDocuments`/`EvidenceDocument`, holds no `documentId` state, and `submitPeriodicAdjustmentWork` (`apps/web/lib/work/api.ts:171`) takes no `sourceRefs` parameter at all, unlike `submitJournalWork` beside it. The code even says so out loud: `apps/web/lib/work/api.ts:178` — *"The one arm that cannot occur here is `source_conflict` (evidence is optional on this door and **this journey's form offers no chooser yet**)"*. This is a real gap against a binding brief instruction, and WORK-ORDER.md rule 6 ("do not widen or narrow the brief silently... state the assumption") was not followed: the final report's Assumptions and Follow-ups sections (`docs/plan/active/refresh-wave-2026-09-14/reports/643-final.md`) never mention dropping the evidence chooser. The runtime side is wired for it (`workRoutes.ts` accepts `sourceRefs` on `/api/work/periodic-adjustment`, migration 0194's `admit_periodic_adjustment_work` takes `p_source_refs`), so this is a UI-only omission, not a descope of AC — but it is undisclosed scope narrowing on an explicit brief line.

**SHOULD — false "scoped" claim on the periodic-adjustment control endpoint.** Task item (5) asked to verify every mock handler really scopes by the lane's client id. `apps/web/e2e/e2e-fixture-ownership.test.ts:288-291` declares `"periodic-adjustment-mock.mjs": { unscopeable: [], debt: [] }` with the comment *"every handler is scoped to this lane's own client id (`PA.clientId`) and falls through otherwise, **including the RUNTIME admission route and the control endpoint**."* `serve-built.mjs`'s import comment repeats the claim. But `handlePeriodicAdjustmentRuntime`'s `/api/e2e-periodic-adjustment/control` branch (`apps/web/e2e/periodic-adjustment-mock.mjs:246-270`) never checks a client id for any of its five ops (`refuse_next`, `seed_intent`, `received`, `empty_history`, `reset` all mutate shared `state` unconditionally) — contrast `journal-work-mock.mjs:645`, `if (body?.client !== JOURNAL_WORK.clientId) return false;`, which the sibling lane's own control endpoint actually does. The census test still passes green because its `handlerCensus` heuristic only checks for a literal `return false;` anywhere in the block (by construction, not semantics), so it doesn't catch this. Low real impact (the control path string is unique to this lane, so no cross-lane leak occurs in practice), but it is a claim in source that AGENTS.md rule 7 ("claims need evidence... no evidence, no claim") does not support as written — quote it or drop it.

## Baseline smells (judgement calls)

**NOTE — Shotgun-adjacent, unrelated edit inside a concurrently-edited shared file.** `apps/web/messages/en.json` carries 11 lines that are pure `…`/`—` → literal `…`/`—` re-encodings inside the **`WorkCancel`** namespace (`cancelling`, `completedAfterCancelBody`, `conflictBody`, `lostBody`, `takingOver`, `takeOverConfirmAction`, `takeOverLostBody`, `stopping`, `stopReplyHint`, `stopDenied`) — none of it #643 copy; `WorkCancel` is #630's territory. Semantically inert (identical rendered text), but it touches a file WORK-ORDER.md rule 8 names as shared/concurrent and asks to keep "minimal and localised," and it's undisclosed in the final report. Judgement call, not a functional bug — flag for the integrator so it doesn't read as a stray diff against a concurrent ticket.

**Positive: Repeated Switches → one map.** `work-detail.tsx`, `journal-entry-row.tsx` and `activity.ts` each had their own inline `purpose === "journal_entry" ? … : …` ternary; #643 collapses all three into `lib/work/purpose-label.ts` (`purposeLabel`/`isKnownWorkPurpose`), reused by all three call sites. Correct application of the smell fix, and the edits to `work-detail.tsx`/`journal-entry-row.tsx`/`en.json`'s label maps are otherwise localised as the brief and WORK-ORDER rule 8 ask.

## Verified clean (no breach)

- **Migration house shape**: `0194_periodic_adjustments.sql` matches `0182`/`0184`'s prestate-sha-pin idiom (4 bodies pinned: `_record_journal_entry_core` vs 0184, `admit_journal_work` vs 0182, `_close_gate_closing_stock` vs 0056, `_tf_accounting_work_immutable` vs 0184) and the single-`raise notice` tail-assertion style. Table is `clara.periodic_adjustments`, never `adjustment_*`; the only `adjustment_*` collisions in the whole diff are private helper functions (`_adjustment_date`, `_adjustment_text`, …), and the file explicitly documents and distinguishes itself from 0045's `adjustment_templates`/`adjustment_runs` family (0194:92-99). Migration numbering gap 0188-0193 confirmed absent, as the brief predicted.
- **DB test idiom**: `periodic-adjustment.test.mjs`/`periodic-adjustment-fixtures.mjs`/`rig-meta.mjs` follow the `buildWorkWorld`/cohort/`cohortFailures`/frontier-gate shape used by the 0182/0183/0184 cohorts.
- **Field wrapper a11y (item 2)**: hoisting `Field` to a top-level component (`80a2713f`) preserves `htmlFor`, the `-help`/`-error` ids, `role="alert"`, and `aria-describedby` wiring (`textProps`/`moneyProps`/`describedBy` at `periodic-adjustment-form.tsx:388-406`) — intact.
- **Checkout clock widening (item 1)**: `checkout-faces-a11y.test.tsx`'s `intervalMs`/`budgetMs` widen 5/30→20/400 (waits 120→600, 60→100), evidenced by a stated bisection (main's en.json 25/25 vs branch's 24/25). The assertions still measure a real property (notice appears after budget, and genuinely stops polling afterward) — not vacuous, but it is an unscoped edit to a #628 test file with real reasoning; already flagged as follow-up 3 in the worker's own report. Judgement call, not a blocker.
- **Naming/nav/command-palette**: `tree.ts`, `routes.ts`, `activity.ts` edits are small, one-purpose, well-reasoned, and reuse `purpose-label.ts`.
- **e2e lane hooks**: `serve-built.mjs` hooks are one-line imports/dispatches; `test/manifest.txt` and `e2e-fixture-ownership.test.ts` additions are minimal.
- **Docs (item 6)**: ARCHITECTURE §4/§8/§11 and PRD's scope-note additions are factual, cite file/line evidence, and write hosted evidence as pending throughout. CONTEXT.md adds exactly the two named terms. The §11 "会计能力" row's pre-existing "189/189" figure is untouched by this branch and the final report itself flags it as stale/not-mine — proper disclosure, not a claim violation.
- **Lint/typecheck**: `pnpm --filter @clara/web lint` → exit 0 locally (token-contrast, manifest, message-key gates all green), matching the worker's claim.

## Total

**7 findings: 1 BLOCKER (undisclosed evidence-chooser omission against brief-643.md), 1 SHOULD (false scoping claim on periodic-adjustment-mock's control endpoint), 2 NOTE (unrelated WorkCancel en.json re-encoding; #628 clock-widening judgement call), 3 verified-clean items called out positively (a11y wiring, purpose-label extraction, migration house shape).** Worst: the BLOCKER — ship-review should require either wiring the evidence chooser or an explicit, disclosed descope note before merge.
