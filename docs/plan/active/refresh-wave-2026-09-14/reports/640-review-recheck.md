# #640 — bounded closure RE-CHECK

`impl/640-accounting-plans` @ `9fc18935`, worktree CLEAN. Rig `rig640c` 55452/`clara_640` (183 applied;
`schema_migrations.checksum` for `0193_accounting_plans` = `4b2f7237…a1161` = sha256 of the file at tip).
Everything below was run, not reasoned; scripts in `scratchpad/r640b/`.

| item | verdict | what I measured |
|---|---|---|
| **BLOCKER-1** the reviewer's sequence | **CLOSED** | Reversing plan, `clara_runtime` doors. `scan #1 -> [["2026-08-31","primary","W"]] status=queued receipts=0`; `scan #2 -> [["2026-08-31","primary","W"]] | reversal Works = 0`; `cancel_accounting_work -> cancelled, receipts 0, _plan_primary_entry=null`; `scan #3 ->` same one row. Catch-up on the reversal period: `admitted=0 … "reason":"reversal_before_primary","code":"CLR13","primary_state":"not_posted","primary_due_date":"2026-08-31"` (row recorded, `work_id` null). Catch-up re-attempt of the accrual `admitted=1 attempt=[2]`; reversal STILL `admitted=0 primary_state=["not_posted"]` — the wall is the receipt, not the Work row. POST (claim + `wake_record_journal_entry` + settle) → entry `55f17a07…`; `_plan_primary_entry` now = that entry. Then `admitted=1 reverses_entry_id=["55f17a07…"]`, memo `"… (reversal of entry 55f17a07…)"`. Ledger: `2026-08-31 Dr 6100 / Cr 1150 99000` + `2026-09-01 Dr 1150 / Cr 6100 99000` with the id in the memo. (A scan after the post does not re-pick a recorded refusal — §C's stated law; on a plan with no prior refusal row the scan admits the reversal itself, measured in P3.) |
| …human reverses BEFORE admission | **CLOSED** | `clara.reverse_entry(accrual)` → `_plan_primary_entry=null`; scan admits nothing; catch-up `admitted=0 reason=reversal_before_primary primary_state=entry_not_live`. |
| …**the residual** | **OPEN — acceptable follow-up** | Accrual posted → scan admits the reversal naming `551f7cfe…` → human `clara.reverse_entry(accrual)` → nothing revokes it (work still `queued`, later scan changes nothing) → posting the reversal **SUCCEEDS** (`c586fe2a…`). Ledger consequence: the accrual is reversed **twice** — 6100 nets a 99000 **credit** and 1150 a 99000 **debit** that stand for nothing. Verdict: filed follow-up, not a blocker — it now needs a deliberate human reversal inside the admission→posting gap, the phantom leg **names** the entry (FK + ledger memo) where the old naked leg named nothing, and closing it needs a wall inside `clara._record_journal_entry_core`, which 0194 pins by sha against 0184 and 0195 against 0194 (`clara-wt/631/…0194:175-177`, `0195:196-198`) — 0193 cannot recut it without aborting the merge order. |
| **Race** | **CLOSED** | Posted accrual, `_plan_admissible_event` = reversal `2026-09-01`; two `clara_runtime` scans behind a plan-row barrier; one queued on the Lock. A: `converged:true`, B: `admitted:true` — same `work_id 7083d352…`, same `occurrence_id`. Result: 1 reversal row, 1 distinct reversal Work. |
| **SHOULD-1** | **CLOSED** | Monthly plan (`authority_from 2026-07-01`, covered_through `2026-09-30`): →quarterly @ `2026-07-01` → `CLR10 {"reason":"period_already_covered","period_start":"2026-07-01","covered_through":"2026-09-30","earliest_effective_from":"2026-10-01"}`; @ `2026-10-01` → SUCCEEDED rev 2. Quarterly plan: →monthly @ `2026-09-01` → `CLR10 … period_start 2026-09-01 … earliest 2026-10-01`; @ `2026-10-01` → SUCCEEDED. Unchanged-frequency (fresh plans, live frequency still monthly / quarterly): both SUCCEEDED rev 2 — the wall is the frequency change only. |
| **SHOULD-2** | **CLOSED** | After a cancel + catch-up re-attempt, `list_accounting_plan_occurrences.attempts` = both admissions (`a04574c3…` attempt 1, `ad715365…` attempt 2, `:a2` key); `get_work_plan_origin(old)` → `superseded:true, attempt:1, work_id: ad715365…`; new → `superseded:false`. |
| **NOTE-2** | **CLOSED** | Claimed run + cancel → `stopping`, receipts 0, `_plan_work_stands=false`, `_plan_primary_entry=null`; scan opens no reversal; catch-up `admitted=0 reversal_before_primary/not_posted`; re-attempt `admitted=0 converged=true`, attempt stays 1. |
| **Frozen-body law** | **CLOSED** | `check-frozen-workflows.mjs` → `OK — 264 frozen file(s)`, exit 0. `git diff main...HEAD --stat -- packages/runtime/workflows frozen-workflows.json` → empty. `journalBasisSchema` is `.strict()` over exactly `posting_date/memo/currency/lines`; `reverses_entry_id` appears in 0193 only as a column / CHECK / FK / output key — never a basis key; the link reaches the run through the memo, and the reversal posted. |
| **Batteries, migrate, pins** | **CLOSED** | Plan batteries with the exact `package.json` "test" gate flags: **35 tests · 35 pass · 0 fail · 0 skip** (11.9 s). `migrate.mjs` → `0 new · 183 total`. §J re-read live: 3 RLS-forced plan relations, `relacl` none, 11 human doors, scan runtime-only (authenticated false), `_plan_primary_stands` gone / `_plan_primary_entry` present, `ck_plan_occurrences_reversal_entry` + `fk_plan_occurrences_entry` present, `_plan_work_stands` names `stopping`; blanking an admitted reversal's entry is refused (`CLR08`). 0045: 3 relations + all 5 doors live; 0193 has **zero** executable `create or replace`/`drop`/`alter function`, no `alter table` outside its own three relations, and touches 0045 only by reads — so §0/§J's byte pins, which passed at apply (the file is recorded applied at its on-disk sha), cannot have been walked past. *Unverified:* a from-scratch re-apply as a control is unavailable on this cluster — a second database aborts at `0154` on a cluster-global role census, an environment artifact, not a #640 finding. No hosted evidence claimed. |

## Verdict

**MERGEABLE** — BLOCKER-1, SHOULD-1, SHOULD-2 and NOTE-2 are closed by measurement on the real doors;
the race yields one occurrence and one Work; the frozen bodies are untouched and the reversal's link
rides the memo; batteries 35/35, migrate 0 new, 0045 intact. The one residual is a filed follow-up:
it requires a deliberate human reversal in the admission→posting gap, leaves an auditable entry that
names what it reverses, and its fix belongs in a body 0193 is forbidden to recut.
