# Brief: #615 — operator support destination isolated from client books

## Orchestrator decisions (binding)
- Migration number: **0188** (`0188_operator_support_console.sql`). Additive only: two read bodies + a comment recut; no new table, no new RLS policy, no new writer.
- Scope Q1: admission capacity IN (existing `set_admission_capacity`/`get_admission_capacity`); estate wake-source flips (`set_wake_source_enabled`) OUT — say so.
- Scope Q2: expose EXISTING writers only (approve/reject registration, `resolve_stripe_event_problem`, `set_admission_capacity`); no new recovery writer; name the absence honestly in the UI ("no supported action for this state") and the PR body.
- Scope Q3: `publish_legal_document` UI OUT (belongs to #635).
- Scope Q4: yes, migration (the queue is a DB read door pinned by DB tests).
- The route lives under `apps/web/app/(firm)/operator` (ancestor-covered by the scope spine); `/settings/registrations` and `/admin/registrations` both 307 to `/operator`.
- Add a negative DB cell that an operator read never returns another firm's documents/ledger/Work rows (structural via `jwt_firm()`; make it an assertion).

## 1. Current state
Operator identity: `clara.firms.is_operator` + `uq_firms_one_operator` (`0133_g1_wake_engine.sql:272-279`); BELCORT carries it in production; the authority fragment `_human_ctx(role_rank('owner'))` AND `exists(… f.id = jwt_firm() and f.is_operator)` is byte-copied into every operator body with a census cell (0145 §K/8b) pinning the substring.
Existing doors (operator-firm-owner, `clara_authenticated`-only, PUBLIC revoked): `approve_firm_registration`/`reject_firm_registration` (`0145_p4_tranche2_registration_operator_alias.sql:766`/`:832`, web yes); `firm_registration_requests_visible` (`0145:911-920`, web yes); `list_stripe_event_problems(boolean)`/`resolve_stripe_event_problem(uuid,text,text)` (`0160_checkout_gate_c2_stripe_events.sql` §5, grants `:635-640`, web none); `list_unconsumed_registration_payments()` (`0163_checkout_gate_c3_folded_door.sql:679-697`, web none; comment points at deleted `/admin/registrations`); `set_admission_capacity`/`get_admission_capacity` (`0186_checkout_convergence.sql:633`/`:724`, web none and forbidden by `apps/web/lib/registration/checkout-doors.test.ts:209` whole-tree scan); `publish_legal_document` (0185 §E, out); `set_wake_source_enabled` (0133:283-291, out).
Supporting state: `clara.firm_registration_requests` (0145:324), `clara.firm_registration_payments` (0163:219, `consumed_at`), `clara.stripe_event_problems` (0160:199-217; kinds widened by 0186:785-790 to `expired_after_paid`, `paid_after_terminal`, `processing_timeout`), `clara.checkout_intents.status/status_at/status_reason` (0186), `clara.admission_capacity` (0186:573) via `_admission_capacity_state()` (0186:610).
Web: `/settings/registrations` → `components/admin/registrations-queue.tsx` (eligibility gate `:120-124`, Approve/Reject with reason, op keys `:501-504`); reads in `lib/registration/doors.ts`; registry `operatorOnly` in `lib/navigation/tree.ts:128,267-274` judged by `lib/firm/navigation.ts:48-56` and `lib/firm/capabilities.ts` (`canDecideFirmRegistrations`); `tree.ts:220-222` and the page header mark the address provisional for #615; legacy matrix `lib/navigation/legacy-routes.ts:46`.
Runtime: none needed (console talks to PostgREST RPC via `callDoor`).
Tests: DB `checkout-gate-c2.test.mjs:511-577`, `checkout-gate-c3.test.mjs:2062-2165`, `p4t2-registration.test.mjs`, `g1-wake-engine.test.mjs:259`, `checkout-convergence.test.mjs`; web unit four `registrations-*.test.tsx`; e2e only nav presence/absence and the 307 (`firm-navigation-walk.spec.ts:46,90`; `shell-migration-walk.spec.ts:144`); `serve-built.mjs` mocks the read at `:448` and sets `is_operator: owner` in `caller_context` at `:488`; no approve/reject RPC mock, no support walk.
Audit: `clara._audit` writes exist (`set_admission_capacity`, `set_wake_source_enabled`, `claim_paid_firm`) but there is no human read of `audit_log`; `list_activity` (0181) is firm-scoped — the audit assertion belongs in the DB battery.

## 2. Gaps / rows
AC1: no destination; boundary is one refusal sentence; no assertion that an operator reads zero rows of another firm's ledger. AC2: payments and provider problems have no surface; decided rows and receipts never shown. AC3: only empty+error exist; DB already distinguishes `payment_not_settled`, `intent_mismatch`, `duplicate_payment`, `processing_timeout`, `paid_after_terminal`, `expired_after_paid`. AC4: DB satisfied (`_reserve_op`/`_finish_op`, CLR09/CLR04); no web path for problems/capacity; no lost-response replay pinned for those two. AC5: deep-link denial, concurrent provider event at UI seam, responsive queue/detail absent. AC6/7 not started.
Rows: C-03 to build (payments + firm operations flows); C55.22 verify-only (PRD §7 + ARCHITECTURE:324 already state the boundary; add the assertion); C83.11 duplicate of C55.22.

## 3. Slice (one PR)
Migration `0188_operator_support_console.sql`: `clara.list_operator_support_queue(p_include_settled boolean default false)` — one row per support case across three arms (open registration; unconsumed payment; open stripe event problem) with `case_kind, case_id, registration_id, applicant, firm_name, request_status, intent_status/at/reason, payment_recorded_at/consumed_at, problem_kind/noticed_at/detail, decided_by/decided_at/reason`, ordered `(occurred_at desc, case_id desc)`; modelled on `clara.list_activity` (0181): SECURITY DEFINER owned by `clara_fn_owner`, `set search_path=clara,pg_temp`, `set plan_cache_mode = force_custom_plan` (0183 rule), revoke public, grant `clara_authenticated`, operator predicate byte-copied from `approve_firm_registration`. `clara.get_operator_support_case(p_kind text, p_id uuid)` — no existence oracle (`support_case_not_found` for absent/foreign-kind/unauthorised alike, as `get_activity_event`). Comment recut on `get_admission_capacity` (drop "no web lane calls this door"). Tail asserts: operator fragment present in both bodies, plan_cache pin, no table ACL moved. Preintegration gate module if the house pattern requires one for a new battery.
Web: route `apps/web/app/(firm)/operator/page.tsx` (queue; detail via `?case=<kind>:<id>` Sheet with Title/initial focus/Escape/focus return/Back preserves filters — the `/activity` `?event=` precedent); `lib/navigation/tree.ts` new `FIRM_NAV` row `operator` (`/operator`, `minimumRole: "owner"`, `operatorOnly: true`), remove the `registrations` settings section and its provisional comments; `legacy-routes.ts` points BOTH `/admin/registrations` and `/settings/registrations` at `/operator` (Next `redirects()` does not chain); new `lib/operator/reads.ts` + `doors.ts`; components `components/operator/{support-queue,support-case-sheet,admission-capacity-panel,isolation-banner}.tsx` (move the registrations table/Reject dialog under it); `messages/en.json` `Operator` namespace (queue columns, the visible isolation statement, six distinct states, capacity labels); narrow `checkout-doors.test.ts:209`'s scan to applicant-facing lanes (`app/(entry)`, `lib/checkout`, `lib/registration`) — do not delete the property; `test/manifest.txt` new lines (alphabetical). Retire the stale "cannot be smoke-tested" note in `settings/registrations/page.tsx:21-25`.
Tests: `packages/db/tests/operator-support.test.mjs` (+ cohort in `rig-meta.mjs`, frontier-gated at 0188); web unit `lib/operator/reads.test.ts`, `components/operator/support-queue-a11y.test.tsx`, updates to `lib/navigation/tree.test.ts`, `lib/firm/capabilities.test.ts`, `lib/command/routes.test.ts`, `lib/navigation/legacy-routes.test.ts`; e2e `apps/web/e2e/operator-support-walk.spec.ts` + `operator-support-mock.mjs` (RPC arms for the two reads, approve/reject, resolve, capacity) registered in `serve-built.mjs` (keep the registration hook minimal — #619 is reworking the dispatch helper concurrently), plus a row in `shell-migration-walk.spec.ts`'s redirect matrix.
Docs: ARCHITECTURE §9 (current-implementation paragraph), §10 anchor `admission-and-operator-support` (:320-328), §11 row "准入与运行保障"; CONTEXT.md terms *Operator support case*, *Support receipt*, *Provider problem*; PRD §7 verify-only.

## 4. TDD seams (red first)
1. `packages/db/tests/operator-support.test.mjs` os.01 — under real roles, a bookkeeper of the operator firm and an owner of a non-operator firm get CLR04 from both reads; the operator-firm owner gets exactly the estate's open cases across all three arms.
2. os.05 — `get_operator_support_case` gives one `support_case_not_found` for unknown id / foreign kind / unauthorised caller; the queue never returns a consumed payment or resolved problem unless `p_include_settled`.
3. os.09 — `resolve_stripe_event_problem` same `op_key` replays the first receipt with exactly one resolution stamp; a different resolution under the same key is CLR10; a second decision on an open registration is CLR09 with the first receipt readable through the queue.
4. `apps/web/lib/operator/reads.test.ts` — empty / denied (CLR04) / read-failed / stale-after-reload are four distinct states; a refusal is never rendered as an empty array.
5. `components/operator/support-queue-a11y.test.tsx` — non-operator: refusal line and zero action controls; operator: queue plus the visible isolation statement; provider-unavailable, unsupported-action, duplicate-operation are distinct labelled regions.
6. `e2e/operator-support-walk.spec.ts` — bookkeeper deep-linking `/operator?case=problem:<id>` is denied without existence leak; operator resolves a problem, Escape returns focus to the row, Back preserves filters; 320 px, 200% zoom, reduced motion.

## 5. Risks
0145 §K byte-copy census: reproduce the fragment exactly or extend the census. Plan-cache flip (0183): pin and assert. `checkout-doors.test.ts:209` goes red until narrowed.

## 6. Effort: M. Rig: PG cluster (assigned), web unit + Playwright. No Workflow world — state so explicitly in the PR body.
