# Brief: #651 — 按明确政策执行折旧并保留估计变更历史 (run depreciation under an explicit policy, and keep the estimate-change history)

## Orchestrator decisions (binding)

- **Migration `0227` only**, ONE file `packages/db/migrations/0227_depreciation_history.sql` (DECISIONS §2.1, row 0227; §2.2 numbering discipline). A released number is a gap, never a renumber.
  **CREATES**, in the order the file writes them:
  1. `clara.fixed_assets.change_class text` + `.change_reason text` + `constraint ck_fixed_assets_change_class check ((change_class is null and change_reason is null) or (change_class in ('estimate','policy','error') and supersedes_asset_id is not null and change_reason is not null and btrim(change_reason) <> ''))`.
     **The CHECK is one-directional on purpose** (orchestrator refinement of gap-651 slice item 1's "set iff `supersedes_asset_id is not null`"): a two-directional CHECK would fail to validate against any pre-existing revision row on the hosted database, which carries neither column. The *requirement* that every NEW revision names a class lives in the recut door, never in the CHECK — the estate's standing "door enforces, CHECK guards shape" split.
  2. `clara.fa_depreciation_authorities.authority_kind text not null default 'explicit_instruction' check (authority_kind in ('explicit_instruction'))` · `.authority_ref jsonb check (authority_ref is null or jsonb_typeof(authority_ref) = 'object')` · `.authority_from date` · `constraint ck_fa_authorities_window check ((status = 'proposed' and authority_from is null) or (status in ('live','retired') and authority_from is not null))`. Shape copied from `clara.accounting_plans` (`0193:418-430`, opened this pass: `authority_kind` CHECK, `authority_ref jsonb not null check (jsonb_typeof = 'object')`, `authority_from date not null` — "written once and frozen"), **relaxed to NULL-able for the backfill only**.
     **Backfill — a FLAGGED deviation from DECISIONS §2.1's literal SQL, not folded into prose.** DECISIONS §2.1 writes the backfill as `date_trunc('month', signed_at)`. Write it instead as `update clara.fa_depreciation_authorities set authority_from = clara._fa_month_start((signed_at at time zone 'Asia/Kuala_Lumpur')::date) where signed_at is not null and authority_from is null`, and **record the deviation in your final report by this name: *0227 backfill timezone*.** Three measured reasons, none of them taste: (i) `signed_at` is `timestamptz` (`0041:623`), so `date_trunc('month', signed_at)::date` truncates in whatever `TimeZone` the applying session happens to carry — a row signed between 00:00 and 07:59 MYT on the 1st of a month lands in the PREVIOUS month under a UTC session and in the correct month under a MYT one, which makes the literal session-dependent on a **one-time, unrepeatable stamp**; (ii) D8's own words are 「签署当月一号」— the first of the signing month *in the book's calendar*; (iii) the composition above is byte-for-byte what a sign performed today stamps, because `clara._fa_today()` is `(now() at time zone 'Asia/Kuala_Lumpur')::date` (`0041:1012-1013`) and `clara._fa_month_start(date)` is `date_trunc('month', p_d)::date` (`0041:1016-1017`), with WORK-ORDER rule 8 ("`Asia/Kuala_Lumpur` calendar days") behind both. **The orchestrator has been asked to ratify this one line** (see "Verifier findings not applied"); if it rules for the literal before you write 0227, use the literal and say so in the report. `authority_ref` backfills **NULL** and `authority_kind` takes its default.
  3. `clara._fa_assert_period_open(p_client uuid, p_date date) returns void` — **ungranted**, `stable security definer set search_path = clara, pg_temp`. **The ONE new helper DECISIONS §2.1's roster names, and the only helper this file installs.** It reads `clara.fiscal_years` exactly the way `0056:657-663` does (the FY containing `p_date`, `order by (fy.status in ('closing','closed')) desc, fy.starts_on desc limit 1`), returns silently when there is no row or the row is `open`/`reopened`, and otherwise raises the CLR38 `period_closed` refusal spelled out in §3 item 2. **#678 adopts it unchanged** (SYNTHESIS §4, "#651 settles the locked-period law and #678 inherits it") — say so in the migration header.
     **No second helper — a withdrawn refinement, recorded so it is not re-invented.** An earlier draft of this brief split the law into a boolean `_fa_period_open` predicate PLUS the assert, because `_fa_oldest_unmet_period` is declared `stable` (`0041:1904-1905`) and must **answer**, never raise — its own comment at `0041:1915-1916` says "The sweep never calls into a refusal it can predict, so the probe answers false rather than letting the verb raise". That split is **withdrawn: DECISIONS §2.1's CREATES roster for 0227 carries exactly one new ungranted helper, and a brief may refine but never widen it.** The oracle instead carries the same fiscal-year predicate **inline in its splice** (§3 item 2), bound to the assert by a tail assertion over the normalized fragment — the same binding the preview's duplicated leg aggregation gets. Do **not** wrap the assert in a `begin / exception when sqlstate 'CLR38'` block inside the oracle: catching a raise to manufacture a boolean is precisely the posture that comment forbids, and the block would swallow any other CLR38 reachable from it. Do **not** name anything `_fa_period_open` / `_fa_fy_open_*` either — `clara._fa_fy_open_for(uuid,date)` (`0041:1057`) and `clara._fa_fy_month_open_for(uuid,date)` (`0042:6027`) already exist and mean "the first DAY of the fiscal year containing this month", not "the fiscal year is open".
  4. `clara.preview_depreciation_run(p_client uuid) returns jsonb` — SECURITY DEFINER, `set search_path = clara, pg_temp`, `_human_ctx(clara.role_rank('viewer'))`, firm from the session, EXECUTE to `clara_authenticated` **only** (I6/Q6: grant the wrapper, never `_fa_compute_charges`, which lives in `FA_0041_UNGRANTED_FNS` at `rig-meta.mjs:712` and whose name sits at `:725`; the main sweep fails on any grant appearing on it).
  5. `clara.run_depreciation_period_for(p_client uuid, p_through date, p_op_key text, p_obo uuid) returns jsonb` — **NEW verb name, mandatory not stylistic**; EXECUTE to `clara_runtime` **only**. Argument order is fixed by DECISIONS §1.2 and §2.1 and may not be re-ordered.
  6. **Conditional, on measurement M1's RED arm only** (§3 item 1): `clara.revise_fixed_asset_particulars_classified(p_client uuid, p_asset uuid, p_particulars jsonb, p_effective_from date, p_op_key text, p_change_class text, p_change_reason text)` — the NEW verb DECISIONS §6 / §7.4 #4 names, carrying the classification as two genuine SQL arguments. On M1's green arm this object **does not exist** and the classification travels through the widened validator instead. Nothing else in the file is conditional.
  **RECUTS** — every pin **MEASURED on `clara_651` off `pg_proc.prosrc`**, never transcribed (DECISIONS §2.2 rule 3; SYNTHESIS §7.4 #1 and K4; WORK-ORDER rule 8):
  `_fa_oldest_unmet_period(uuid)` · `_fa_run_period_core(uuid,date,date,text,uuid,uuid,text)` · `_fa_asset_json(uuid,date)` · `revise_fixed_asset_particulars(uuid,uuid,jsonb,date,text)` (**KEEP the 5-arg signature** — DECISIONS §2.1, verbatim) · `sign_depreciation_authority` (DROP+CREATE to 4 args, below) · and, **only on measurement M1's green arm**, `_fa_validate_particulars(jsonb)`, `complete_fixed_asset_particulars(uuid,uuid,jsonb,text)` and `_fa_complete_particulars_core(uuid,uuid,uuid,uuid,jsonb,text,text)`.
- **Two of your recuts are 0042 string splices, so 0227 SPLICES — it does not transcribe.** `_fa_oldest_unmet_period` was spliced by **0042 §S5.15c** (`0042:4097-4221`; prestate marker census `:4142-4162`, anchor `:4166-4172`, postcheck `:4200-4213`, opened this pass) and `_fa_run_period_core` by **0042 §S5.15d** (`0042:4225`), both by `replace()` over `pg_get_functiondef` on the live catalog. A `grep` for `create or replace function` finds neither, which is exactly how gap-651's draft reached "none found". **Therefore 0227 uses 0042's own idiom for both**: read `pg_get_functiondef`, assert the pre-image `sha256(prosrc)`, re-run 0042's marker census, `replace()` a **measured** anchor, `execute` the result, then re-run the marker census AND 0042 S5.15c's ordering postcheck (`position('period_draft_outstanding') < position('clara._wdb_rerun_breach(p_client') < position('for fa in select f.id as id from clara.fixed_assets f')`). Transcribing a 60-line live body into the file is how this wave loses a day.
- **`sign_depreciation_authority` is DROP+CREATE'd to 4 args**: `clara.sign_depreciation_authority(p_client uuid, p_authority uuid, p_op_key text, p_authority_ref jsonb)`, **no default**, so exactly ONE `pg_proc` row and no overload (DECISIONS §2.2 rule 4; the `0018:188` scar is the precedent — "adding `p_resolution` to `seed_fixed_asset` needed a DROP + CREATE and the 4-arg grant died with it", so **re-`grant execute … to clara_authenticated` and re-`alter … owner to clara_fn_owner` in the same file**). The ref is **required and RESOLVED**, on `create_accounting_plan`'s own ladder (`0193:1482-1514`, opened this pass), carried onto the FA family's CLR38 axis so one surface renders one vocabulary: null/non-object → `authority_ref_invalid` `constraint:'object'`; `kind` not in `('accounting_work','chat_task')` → `constraint:'kind'`; unparseable `id` → `constraint:'id'`; not resolving to a `clara.accounting_work` / `clara.agent_tasks` row **in the same firm and client** → `authority_ref_unresolved` with `kind` and `id`. A Knowledge preference, a calculation policy or a repeated debit resolves to nothing and is refused by name — that sentence is AC5's "explicit instruction" and it must be executable, not a comment.
- **D7 (binding)**: depreciation does **not** move into the plan lane. Do not widen `accounting_plans.kind`, do not touch `create_accounting_plan` / `_assert_plan_schedule` / `_plan_occurrence_basis` / `_plan_admit_occurrence` / `preview_accounting_plan`, do not edit `plan-occurrences.mjs`, and do not amend `CONTEXT.md:37` ("Depreciation and close schedules are not plan kinds and are refused by name" — opened this pass; the DB refuses by name at `0223:590-598`). AC5's "register its occurrence adapter with the shared scheduler" is **spec drift**, recorded in your report, not implemented. *(Closes gap-651 Q1 / SYNTHESIS I1.)*
- **D8 (binding)**: `authority_from` defaults to the **first day of the signing month** and freezes; earlier periods reach a run only through an explicit catch-up. Set it at sign time as `clara._fa_month_start(clara._fa_today())`, never from `now()` in UTC.
- **D9 (binding)**: a closed period is refused **at the running door**, on a NEW CLR38 axis `period_closed`, naming the fiscal year and the reopen path; **not one draft is created**. "Move this period into the next open one" (the AC2 "accepted treatment" clause) is **NOT built this wave** — a named residual with the owner question in your report. *(Closes gap-651 Q4 / SYNTHESIS I4.)*
- **D10 (binding)**: record **three** change classes, implement **`estimate` only**. `policy` and `error` refuse by name with CLR37 `fa_change_class_unsupported`, and the refusal **names #680** (锁期后的迟到资料与重开决定 — the retrospective-restatement lane) **and #679's lock law** in the same `detail`. **Orchestrator ruling — supersedes gap-651's AC1 row and slice item 1, which still name #676**: DECISIONS D10 records the correction in its own words ("原写 #676 是错的：#676 管的是已分配账项的更正，与固定资产无关；设计评审核实后改正"), so gap-651's "Conflicts with DECISIONS — LISTED, NOT APPLIED (F2)" is now **applied in the design lens's favour**. A live refusal string is read by an accountant and grepped by the next implementer: it says **#680**.
- **I5 (binding)**: do **not** enable `close_prep`. The wake source is registered-and-disabled (`0133:915-917`, re-asserted `0138:2939-2941`, `0223:247-248`), and `closePrep.v1.tools.ts:8-10` + `0223:105-112` both rule that unparking belongs to a `closePrep_v2` with its own review. **Your tail re-asserts the row is still disabled**, in `0223:247-250`'s exact idiom, so no later reader can read 0227 as having unparked an agent close lane. *(Closes gap-651 Q5 / SYNTHESIS I5.)*
- **No frozen successor on this branch** (DECISIONS §1.1, §1.3). You cut no `_vN`, edit no file inside a frozen closure, and do not touch `packages/runtime/workflows/registry.ts`. You ship the DB doors + the human web doors + **one NEW non-frozen runtime module** `packages/runtime/lib/depreciation-run.ts` + the **successor-contract stanza reproduced verbatim in §3 below**, which the integration worker carries onto `chatTurn_v21` (DECISIONS §1.2, row "#651"). **That module freezes by closure the moment v21 imports it** (SYNTHESIS §1.6; `lib/fixed-asset-acquisition.ts:35-39` is the precedent and it did freeze) — so **every durable rule lives in 0227, never in the module**. AC5's Clara-entrance arm is "**contract delivered, closes at the cut**"; say exactly that and claim nothing more (DECISIONS §1.4).
- **`run_depreciation_manual` must NEVER reach a machine role** — `rig-meta.mjs:691-693` is an executable census whose own words are "must NEVER reach a machine role, or the maker-checker ladder would have a bypass" (opened this pass). That is why the OBO door carries a new name.
- **Ownership (DECISIONS §3, SYNTHESIS §3.1)**: you **OWN** `apps/web/components/registers/*` (the FA panels and the detail page) and `apps/web/lib/registers/depreciation.ts` / `fixed-assets.ts`. #656 owns `?tab=opening` inside the same workbench and the two tabs are disjoint. **Do NOT open**: `components/work/work-detail.tsx` (#658 owns the Sources tab, #655 one link block, #636 one row), `client-workspace-overview.tsx` / `client-home/*` (#660), `components/bank/*` and `lib/bank/*` (#657), `components/clara/*`, `lib/parts/*`, `PartRenderer.tsx`, `lib/parts/types.ts`, `lib/parts/catalog.ts` (#642 — **you register NO part kind; nobody does this wave**), `lib/journals/api.ts` (#655), firm Home and `lib/firm/use-review-queue.ts` (#659), `components/firm-admin/settings-panel.tsx` (#635), `lib/documents/useUploadQueue.ts` (#642/#636), `packages/runtime/workflows/registry.ts`, `closePrep.v1.*`, `wake_engine_sources`, and every frozen module.
- **NO new `REVIEW_QUEUE_ROW_KINDS` kind** (DECISIONS §3, rule carried from 2026-09-15 §1.6; SYNTHESIS §3.1 resolves the #651/#657 contest by rule). A depreciation draft awaiting approval blocks the client's whole queue and **has no Needs-you row**: reuse an existing kind if one genuinely fits, otherwise record a **named residual** and file the follow-up. Do not add a literal to `apps/web/lib/firm/needs-you.ts:97-118`.
- **CONTEXT.md terms you write** (DECISIONS §3.1, house "term / _Avoid_" shape): **Depreciation change class** (_Avoid_: treating an error correction as an estimate change; treating a policy change as prospective) and **Depreciation authority window** (_Avoid_: reading a signature as permission to charge every past period) — **both ratified**. A third, **Depreciation run preview** (_Avoid_: a projection presented as a posted figure), is permitted under §3.1's closing sentence as a refinement the integrator unions; label it as a refinement in your report, not as a ratified term. **Do not amend the "Accounting plan" entry** (D7).
- **Boundaries** (DECISIONS §3.2, SYNTHESIS §4): **#932** owns where particulars COME FROM (a versioned per-account policy stamped at birth by a recut of 0216's trigger) — you mint **no** account-level policy relation and never touch `upsert_fa_account_profile` (`0041:2761`) or the birth trigger; **#933** rides `claraWork_v5`, you write a `chatTurn` contract — different closures, say so in writing so the integrator does not fold them; **#882** is CLR40 (the movement belt), **adjacent not shared** — its stanza is **#658's worker's** to write (DECISIONS §1.2 rider row, §6), not yours; **#678** is blocked by you and inherits `_fa_assert_period_open`; **#656** owns the seed, you own what the run does with a seeded `baseline_as_of` and change none of its semantics; **#660** owns any money figure on Client Home — add no second money read; **#636** owns the "sufficient continues, missing waits alone" vocabulary at Work altitude — coordinate the WORD for a skipped asset, never the code; **#884** reserves its own fixture ground, so your cell prefix is **`p651.`** throughout; **#900** is a different file — if you move the FA dialogs onto `Field`, note the precedent for it. **#635 / #655 / #657 / #658 / #659 / #642: no shared door, no shared body.**
- **Non-goals, in code comments AND in the report**: no retrospective restatement (`policy` / `error` refuse by name); no locked-period "accepted treatment" / period-shifting; no new human catch-up door (withdrawn — see §3, item 3); no `close_prep` unparking; no plan-lane migration; no new `accounting_work.purpose` and no posting-core contact (depreciation posts through `journal_entries` directly; the IN-list at `0195:1711` / live `0204:180` stays closed — SYNTHESIS §0.4 names you explicitly); no scoring; no `ui:add` (appendix D's "local state" column is a 2026-09-08 snapshot — `empty.tsx`, `field.tsx`, `skeleton.tsx`, `collapsible.tsx`, `table.tsx` are all in `apps/web/components/ui/` today, 32 entries); no hosted claims — write "hosted evidence pending" (DECISIONS §4).
- **Q7 ruled here — a BRIEF ruling, adopting gap-651's own Q7 recommendation (`gap-651.md`'s late design pass raised it as 建议 and left it open). It is NOT in `DECISIONS.md`** — grep it for "Q7", "fifth tab" or "revisions tab" and nothing matches, so this is neither an orchestrator ruling nor an owner-marked product call, and the orchestrator or the owner may reverse it at the cost of one tab and one walk leg, with no change to the migration: AC4's four groupings get **four visible groupings**. Add a **fifth tab** `revisions` ("Policy & effective revisions") to `fixed-asset-detail.tsx`, because AC4's word is *separates* and the two empty states are different facts — "this asset has never had its estimate revised" is not "depreciation particulars are not filled in yet", and a merged section can only show one of them. Appendix D **row 58 (Tabs, Production)** permits alternate views in one route **on condition the state is shareable**, so the tab id moves into `?tab=` in the same slice (today it is local `useState` at `fixed-asset-detail.tsx:65`, opened this pass). Cost of the alternative, for the record: one fewer tab and one fewer walk leg, and AC4's "separates" only half done.
- **Evidence law (DECISIONS §4)**: every AC and every historical row closes with a red-first cell (`p651.<area>.<name>`), a browser-walk leg, or a **named** residual. New DB batteries assert through `humanQuery` least-privileged personas — never `rootQuery` for the thing under test (labelled fixture DML excepted). A skipped frontier-gated battery is not evidence. **Blueprints are never edited**; your report carries the three "blueprint drift" lines in §3 Docs.
- **This map had ONE lens of review until late** (SYNTHESIS §7.5 #1, K2; DECISIONS §6 §7.5 #1): both pass-1 refuters returned the CITATION payload byte-identically. The design pass was recovered and applied afterwards and **it moved the slice** — the floor's mechanism, the OBO door's reach, two recut rows recorded as un-spliced, and a new Q7. Treat §3 item 3 and §4's measure-first block as the load-bearing parts of this brief.
- **Playwright port triple** (RIG.md, verbatim): `CLARA_E2E_APP_ORIGIN=https://127.0.0.1:3330 CLARA_E2E_NEXT_PORT=3331 CLARA_E2E_RUNTIME_PORT=3332`.
  **Rig row** (RIG.md, verbatim): worktree `C:\Users\zhant\Desktop\clara-wt\651`, branch `impl/651-assets-depreciation`, PG port **55704**, db **clara_651**, base `abcc5030`, install ok 434s, **219 applied**, seed ok (2 files), smoke `219 · 0224_preview_invite · PG 17.11`.

## 1. Current state

**DB — the engine is built and strong; the law around it is not.** Relations (all 0041, FORCE RLS, `select` to `clara_authenticated` only, no app-role DML): `clara.fixed_assets` (widened by 0041 with the three-value method CHECK `0041:291-296`, `depreciation_rate_bps` `:300-302`, driver coherence `:309-316`, `effective_from` / `superseded_at` / `supersedes_asset_id` / `baseline_as_of` / `accumulated_depreciation_cents`; 0216 added `acquisition_document_id` `0216:269-277`); `fa_account_profiles` (`0041:419`); **`fa_depreciation`** — the append-only charge ledger, `uq_fa_depreciation_live_range (asset_id, period_start, period_end) where is_live` (`:556`), append-only trigger `:569-591` allowing exactly one mutation (`is_live` true→false); **`fa_depreciation_authorities`** (`0041:614-640`, opened this pass) — `status ∈ {proposed,live,retired}`, `cadence ∈ {monthly,annual}`, proposer/signer/retirer + op keys, one live and one proposed per client (`:642-646`), transition trigger `:649-678`, **no `authority_kind`, no `authority_ref`, no window**; `fa_depreciation_runs` (`:699`, 1:1 with the entry `:724`, `uq_fa_runs_op_key` `:726`, immutable `:733-746`, carrying `mode`, `entries`, `charged_cents`, `skipped`); `journal_entries.origin` widened with `'scheduled_run'` (`:763-773`).
Human doors (`clara_authenticated` only, bulk grant loop `0041:4404-4423`): `complete_fixed_asset_particulars(uuid,uuid,jsonb,text)` (`:3035`, bookkeeper+, complete-once `:3061-3066`, opened this pass — **it does NOT route through 0216's shared core; it carries its own full body**), `revise_fixed_asset_particulars(uuid,uuid,jsonb,date,text)` (`:3112`, bookkeeper+), `propose_depreciation_authority(uuid,text,text)` (`:3277`), `sign_depreciation_authority(uuid,uuid,text)` (`:3316`, **admin+**, WD-R9 rationale `:3320-3324`), `retire_depreciation_authority(...)` (`:3361`), `run_depreciation_manual(uuid,date,date,text)` (`:3598`, bookkeeper+), `dispose_fixed_asset(...)` (`:3643`). Machine: `run_depreciation_period(uuid,date,date,text)` → `clara_runtime` only (`:4433`); `depreciation_run_due(uuid)` → both lanes (`:4436-4437`). Agent: `wake_run_depreciation_catchup(...)` (`0138:2413`) over `_agent_depreciation_catchup_core` (`0138:2357`), rung B9 at `:2368-2372`, **its wake source disabled**.
Reads (viewer+): `list_fixed_assets` (`:4110`), **`get_fixed_asset(uuid)`** (`:4131`, opened this pass — returns `asset`, `lineage` (an array of `_fa_asset_json` per ancestor generation, walked upward `:4144-4152`), **`charges`** (`:4155-4158`: `id, period_start, period_end, amount_cents, effective_date, entry_id, run_id, unwind_of`), `schedule`, `uncharged_due`; recut whole by 0216), `list_depreciation_runs` (`:4180-4196`, opened this pass — already returns `skipped` and `entry_id`), `get_depreciation_run` (`:4199`), `get_depreciation_authority` (`:4216`), `fa_register_tie` (`:4257`). **`_fa_asset_json(uuid,date)`** (`0041:4074-4103`, opened this pass) projects `effective_from`, `superseded_at`, `method`, `rate_bps`, `useful_life_months`, `start_date`, `residual_cents`, `cost_cents`, `accumulated_cents`, `nbv_cents`, the three account codes and `uncharged_due` — **everything a revision timeline needs except the two new columns**.
The run, in order (`_fa_run_period_core`, `0041:3422-3611`, opened this pass): op-key reserve → derived `:approve` sub-key before the rung (`:3405-3411`) → advisory rung `203005004` → live authority or CLR38 `authority_not_live` → **the period is the cadence's, not the caller's** (`:3457-3470`, CLR38 `period_request_invalid` axis `not_cadence_aligned`) → period must have ENDED (axis `not_ended`, `:3472-3477`) → sequencing (`period_draft_outstanding`, then `period_earlier_unmet` only when the oracle names an EARLIER period, `:3479-3494`) → `_fa_compute_charges` → zero charges ⇒ `noop`, persists nothing → draft entry with `origin='scheduled_run'`, `last_human_editor = au.signed_by` → legs aggregated per (expense, accumulated) pair → exact debit=credit or CLR07 → ramp predicate → post via `_approve_entry_core` when ramped and not high-stakes, else leave a draft.
**Nothing in the FA lane reads `clara.fiscal_years`.** A run over a closed FY drafts successfully and dies at approve on `0056:694-698` CLR19 `write_into_closed_period` (opened this pass: the FY lookup is `0056:656-662`, the permit is `:678-691`, the raise `:694-698`), and that dead draft then blocks the client's **entire** depreciation queue (`0041:1916-1922`) until a human withdraws it. `clara.withdraw_draft(p_entry, p_reason, p_op_key)` exists (`0009:1882`) and no cell exercises it on a depreciation draft.
**The `skipped` reason vocabulary is FIVE names — not the four the per-asset function returns — and the receipt has exactly ONE writer.** `clara._fa_asset_charges` returns four names plus a null (`0041:1352-1393`, counted this pass): `not_in_service` (three sites), `incomplete`, `none_method`, `fully_depreciated`, and `null` for a chargeable asset, which `0041:1390-1393` calls out as explicitly **not** a skip. Its caller `clara._fa_compute_charges` adds a **fifth name of its own that the per-asset function can never produce** — `disposal_draft_outstanding`, at `0041:1862-1868` (measured this pass; gap-651's Unverified list flagged that no one had traced it). The skip rows are assembled at `:1862-1868` and `:1883-1886`, and the ONLY `insert into clara.fa_depreciation_runs` in the estate is `0041:2320-2324`, which stores `v_recomp -> 'skipped'` verbatim — so a receipt's vocabulary is exactly `_fa_compute_charges`'s, no more and no less (`grep -rln skip_reason packages/db/migrations` → `0041` alone). `_fa_asset_charges` is itself a 0042 splice (§S5.27, `0042:5928-6135`), so **re-read the vocabulary off the live catalog on your rig (M7), never off 0041's file text.**

**Runtime.** `packages/runtime/lib/reconciler-fa.mjs` — the daily belt; per-cycle `to_regprocedure` feature detect (`:55-58`), active-client scan (`:64-67`), per-client isolation (`:108-157`), bounded chase `FA_PERIOD_CAP = 24` (`:50`, `:111`), `noop` breaks the chase (`:146-150`); **not frozen**; its own law is "the DB owns every number" (`:15-20`). `packages/runtime/lib/fixed-asset-acquisition.ts` is **FROZEN BY CLOSURE**, and `FA_PARTICULARS_KEYS` (`:51-58`, opened this pass) mirrors `_fa_validate_particulars`'s closed nine-key set (`0041:2977-2984`, opened this pass); `packages/runtime/tests/fixed-asset-acquisition-unit.test.mjs:38-46` cell `fa.keys` asserts that list is **exactly the nine** and that the `.strict()` answer schema refuses anything outside it. `chatTurn.v20.*` is the pin (`registry.ts:173`, `:990`) and adds exactly two tools, neither about depreciation (`chatTurn.v20.tools.ts:349`, `:365`); `fa_depreciation` / `fixed_assets` are read-only allowlisted at `chatTurn.v15.freeform.ts:64`. `closePrep.v1.*` is registered, frozen and never runs. **No HTTP route for depreciation** — the human doors are reached through PostgREST (`apps/web/lib/doors.ts` → `wire.ts:pgrestRpc`), so a new DB door needs **no route edit**.

**Web** (every component under `apps/web/components/registers/` except where noted). `/clients/:id/registers?tab=fixedAssets` → `registers-workbench.tsx` → `fixed-assets-register.tsx` (194) with `FaAccountProfilesPanel`, `FaRegisterTieBanner`, `DepreciationAuthorityPanel` (41), `DepreciationRunsPanel` (`fa-depreciation-runs-panel.tsx`, 121). `/clients/:id/registers/assets/:assetId` → `fixed-asset-detail.tsx` (376), #639's four tabs — `acquisition / particulars / schedule / history` at `:109-114`, **local `useState` at `:65`, `SectionTabs` at `:167`** (opened this pass), no nav leaf by design (`tree.ts:469-475`), reached through `fixedAssetHref` (`tree.ts:550-558`, opened this pass). `apps/web/lib/registers/depreciation.ts` (146) carries typed wrappers for all five authority/run doors with the refusal vocabulary in doc comments (`:128-136`), and **`runDepreciationManual` mints `crypto.randomUUID()` on every call** (`:140-145`, opened this pass) — the "lost response, click again" defect in this lane. `apps/web/lib/registers/fixed-assets.ts` (353) declares `FaDepreciationCharge` (`:97`) and `charges: FaDepreciationCharge[]` (`:215`); **neither is consumed by any component** (`grep -rn "\.charges\b\|FaDepreciationCharge" apps/web` returns only the type declaration). The run dialog is two raw `Label` + `Input type="date"` controls and a button (`fa-depreciation-runs-panel.tsx:99-119`, opened this pass), not `Field`/`FieldGroup`/`FieldError` (the house shape is `work-question-form.tsx`). The runs table renders period / mode / charged / entries only (`:57-77`) — `skipped` and `entry_id` are returned and never shown. i18n namespaces in play: `FixedAssetDetail`, `ClientRegisters.fixedAssets`, `FixedAssetsDepreciation.runs`, `FixedAssetsDepreciation.authority` (read this pass). `components.json` style is `base-nova`; single locale. `useDecisionKey` exists twice, at `components/work/work-cancel-dialog.tsx:95` and `components/plans/plan-lifecycle-dialogs.tsx:66` — copy the shape, do not import across a lane.

**Tests.** DB (frontier-gated, `x41EnsureReady` + `skip41`): `x41-depreciation.test.mjs` (c1–c3, d1–d10), `x41-reducing-balance`, `x41-belt`, `x41-disposal`, `x41-reversal`, `x41-round3-{arith,guards,lifecycle,tie}`, `x41-round35-*`, `x41-round4-g2`, `x41-round46`, `x41-wave-d-a-fa` (the census battery), `x41-0041-upgrade`, `x42-r7-fa-stamp`; world + fixtures `x41-fa-world.mjs` / `x41-fa-fixtures.mjs`; #639's `fixed-asset-acquisition.test.mjs` (21 `p639.*` cells) with its own fixtures and gate. **The x41 world builds no `clara.fiscal_years` row at all** (zero occurrences in either file), and #639 says so itself (`fixed-asset-acquisition.test.mjs:344-346`) — so **no existing cell can reach the closed-period path**. Runtime: `reconcile-fa-unit.test.mjs` (14 cells, stubbed) and `reconcile-fa.test.mjs:94` (one real-DB end-to-end under the `clara_runtime` group role). Web unit: `fixed-asset-detail.test.tsx`, `fixed-assets-a11y.test.tsx`, `fixed-assets-keyboard.test.tsx`, `work-asset-row.test.tsx`, `register-refresh-siblings.test.tsx`, `lib/registers/depreciation.test.ts`, `lib/registers/fixed-assets.test.ts`. Playwright: `fixed-asset-acquisition-walk.spec.ts` (9 cells) + `fixed-asset-mock.mjs`, whose `RPC_VERBS` set (`:57-64`, opened this pass) already answers `list_fixed_assets`, `get_fixed_asset`, `fa_register_tie`, `get_depreciation_authority`, `list_depreciation_runs`, `complete_fixed_asset_particulars` — **no depreciation walk anywhere**. `rig-meta.mjs`: `FA_0041_COHORT` (`:686-730`, asserted `:2922`) and `FA_ACQUISITION_0216_COHORT` (`:1930-1951`, asserted `:2950`); the gate chain holds exactly **40** `--import` flags, last `preview-invite-preintegration-gate.mjs` (parsed this pass).
**Four live call sites sign an authority by named arguments** and every one of them breaks the moment a ref becomes required: `packages/db/tests/x41-fa-fixtures.mjs:274-277` (`signAuthority`, used by the whole x41 suite), `client-onboarding-identity.test.mjs:473-475`, `f-a4-pr1c-rungs.test.mjs:294-296`, `x56-rest-j.test.mjs:87-88` (all opened this pass).

## 2. Gaps / rows

| Row | Disposition | Evidence the implementer must produce |
|---|---|---|
| **AC1** version method / life / residual / dates **+ estimate·policy·error classification**; a future estimate change does not silently regenerate prior charges | **partial → to build (classification only).** The versioned half is built and proven (`0041:3164-3172` CLR37 `fa_revise_effective_conflict`, `:3170-3186` the baseline and pre-birth bounds, `:3209-3224` supersede-forward insert, `:3225-3226` predecessor closed, `:556` live-range unique, `:569-591` append-only, `x41.c2` at `x41-depreciation.test.mjs:105`). The classification is **absent estate-wide**. | `p651.class.required`, `p651.class.policy_refused`, `p651.class.error_refused`, `p651.class.prior_untouched`; the revisions tab leg of the walk |
| **AC2** atomic occurrence journal + charge/run rows + receipt; duplicate/manual/due converge; **locked periods refuse or use the accepted treatment** | **partial → to build (the locked-period wall).** Atomicity and convergence are proven (`0041:3516-3535`, `:2318-2330`, `:733-746`, `:3432`, `:3437-3441`, `:726`, `:3580-3615` both verbs over ONE core, `x41.d9` at `x41-depreciation.test.mjs:411`, `reconcile-fa-unit.test.mjs:140`/`:159`/`:228`). Locked periods are **wholly unhandled and structurally untested**. "Accepted treatment" is **descoped** — authority **D9**. | `p651.period.closed_refused` (the decisive cell), `p651.period.closed_belt_skips`, `p651.period.blocked_draft_recovery`; the closed-period walk leg |
| **AC3** reread asset/authority/period **at commit**; preserve prior runs under revision, cancellation, restart | **verify-only, re-measured.** Mechanism complete (`0041:2257-2265`, `:2266-2275`, `:2285-2292`, `:2294-2307` CLR38 `depreciation_stale`, `:2309-2316`, `:3209-3226`; `x41.d5`/`d8` at `:293`/`:387`; `reconcile-fa-unit.test.mjs:175`, `:206`). Two named holes to close: **no cell withdraws a depreciation draft**, and "process restart" here means the **belt**, not Workflow. | `p651.period.blocked_draft_recovery` (the withdraw leg); `p651.class.prior_untouched`; the AC7 sentence naming the belt |
| **AC4** detail separates particulars / policy-effective revision / calculated schedule / immutable charge-run-correction history; missing policy blocks dependent only; conflicts get shared questions or typed refusal | **partial → to build (three holes).** `charges` and `lineage` are returned and never read (`0041:4155-4158`, `:4144-4152`; `apps/web/lib/registers/fixed-assets.ts:97`, `:215` are the only consumers). The History tab renders lineage relations, not a revision timeline or a run history (`fixed-asset-detail.tsx:109-114`). The runs panel is client-level and hides `skipped` and `entry_id` (`fa-depreciation-runs-panel.tsx:57-77`). "Blocks dependent only" is proven engine-side (`0041:1881-1886`, `:1370`, `p639.depreciation.independent` at `fixed-asset-acquisition.test.mjs:854`). The **shared-question** half exists only for particulars (#639) — **named residual** for authority/period conflicts, which take typed refusals instead. | `p651.detail.timeline`, `p651.detail.ledger`, `p651.runs.skipped` (web unit); the two detail walk legs |
| **AC5** future plan only under explicit instruction or scoped authorisation; **register its occurrence adapter with the shared scheduler**; preview, exact JE/asset effects, duplicate/restart recovery, links from Clara/Accounting | **to build, with ONE arm descoped by authority and ONE deferred to the cut.** (i) instruction ref + window → build. (ii) "shared scheduler" → **descoped, authority D7 / SYNTHESIS I1**; recorded as spec drift against `CONTEXT.md:37` + `0223:590-598`. (iii) preview → build (I6). (iv) Clara entrance → **contract delivered, closes at the `chatTurn_v21` cut** (DECISIONS §1.2, §1.4). (v) recovery → build (decision key + withdraw path). | `p651.authority.ref_resolves`, `p651.authority.floor`, `p651.authority.floor_sequencing`, `p651.preview.matches_run`, `p651.preview.grants`, `p651.obo.run_for`, `p651.obo.manual_still_human`; the successor stanza in your report |
| **AC6** loading / successful-empty / no-results / partial-stale / invalid-saving / denied-failed / cancelled-recovery; exact money and dates; 320px; 200%; keyboard + focus return; SR names; reduced motion; stable URL/Back; preserved drafts; accepted composition + persistent outcome | **to build.** The estate's only FA walk is #639's acquisition walk (`fixed-asset-acquisition-walk.spec.ts:254`, `:273`, `:307`, `:214`) and **none of it touches depreciation**. No URL state on the detail tabs (`fixed-asset-detail.tsx:65`). No draft preservation across a refused run. The run row links to nothing. | the whole new walk + mock; the four web unit files; `?tab=` Back-stability leg |
| **AC7** prove through the production-facing read/command under real least-privileged roles and current migrations; where Workflow is invoked prove durable execution; label local vs hosted | **partial → to build.** Engine evidence is very strong (the x41 suite, `reconcile-fa.test.mjs:94` under the `clara_runtime` group role, 0216's cohort + gate). Missing: a closed-period cell, a withdraw cell, the classification lane, any Playwright leg. **Depreciation invokes NO Workflow** — say so explicitly and cite `reconciler-fa.mjs:59-61` ("a plain polled belt … it neither listens on a channel nor starts a workflow run") plus `reconcile-fa.test.mjs:94` as the durability evidence in its place. A silent omission fails this AC; a named not-applicable satisfies it. | the whole DB battery through `humanQuery` personas; `p651.census.replay`; the AC7 paragraph |
| **C86.2** — `_approve_entry_core` refusal still named a retired "budget" gate; drafting-trio exact-equality pin (*accounting errors/quality · preserve*; related C-45, `comment-5589271633.md:320`) | **verify-only, re-measured on the rig — never copied.** (a) The stale prose is gone: every `create`/`create or replace` site of `_approve_entry_core` (`0015:1247`, `0016:1220`, `0029:27`, `0035:140`) carries no `budget` substring. The live body is a **four-file splice**, so re-derive off `pg_get_functiondef` on `clara_651`, not file text. (b) The equal-behaviour pin exists: `run_depreciation_period` (`0041:3580`) and `run_depreciation_manual` (`:3598`) are thin wrappers over ONE `_fa_run_period_core` (`:3422`) and `x41.d9` pins "the manual path is the machine path". **Because this migration adds a THIRD writer variant, the pin must be RE-DERIVED to cover all three in one assertion.** Note the correction gap-651 made in reconciliation: the four→six census at `0216:20-30` is **`_subledger_on_approve`'s** caller set (pinned four at `0037:3840-3845`, measured six), not `_approve_entry_core`'s own. | `p651.obo.run_for` extended to assert the three variants' mechanics are identical; the C86.2 paragraph citing the measured shas |

## 3. Slice (one branch)

### Migration `0227_depreciation_history.sql` — in the order the file is written

**Header.** Spec of record #651 + this brief's binding section; the CONTEXT words (*Depreciation change class*, *Depreciation authority window*); the four things a later reader must not mis-read: (1) **`_fa_run_period_core` has THREE callers today and FOUR after this file** — `run_depreciation_period` (`0041:3580`), `run_depreciation_manual` (`:3598`), `_agent_depreciation_catchup_core` (`0138:2398`) and the new `run_depreciation_period_for`; a tail written for three undercounts by one and **passes while the new door sits outside the set**; (2) **the floor removes the agent catch-up lane's reach** — after `authority_from`, `_agent_depreciation_catchup_core` can never propose a pre-floor period, because it derives every period from `_depreciation_run_due_core` (`0138:1130-1138`, a one-line passthrough to the oracle) and `p_through` bounds only the loop's UPPER exit (`0138:2396`); the lane is parked, and **`closePrep_v2` inherits the loss**; (3) **`_fa_assert_period_open` is written for #678 to adopt unchanged**; (4) **this file does not unpark `close_prep`**.

**Prestate.** The 0216 idiom (`0216:165-230`, opened this pass), in three blocks. (a) Refuse if any name this file installs already exists (`to_regprocedure(...) is not null`). (b) The `::regprocedure` prerequisite roster: `clara._fa_oldest_unmet_period(uuid)`, `clara._fa_run_period_core(uuid,date,date,text,uuid,uuid,text)`, `clara._fa_compute_charges(uuid,date,date)`, `clara._fa_asset_json(uuid,date)`, `clara.get_fixed_asset(uuid)`, `clara.revise_fixed_asset_particulars(uuid,uuid,jsonb,date,text)`, `clara.complete_fixed_asset_particulars(uuid,uuid,jsonb,text)`, `clara._fa_complete_particulars_core(uuid,uuid,uuid,uuid,jsonb,text,text)`, `clara.sign_depreciation_authority(uuid,uuid,text)`, `clara._fa_validate_particulars(jsonb)`, `clara._fa_month_start(date)`, `clara._fa_month_end(date)`, `clara._fa_today()`, `clara._wdb_rerun_breach(uuid,text,text[],date,date)`, `clara._human_ctx(int)`, `clara.role_rank(text)`, `clara._reserve_op(uuid,text,text,bytea)`, `clara._finish_op(uuid,text,text,jsonb)`, `clara._audit(uuid,uuid,uuid,text,text,uuid,jsonb)`, `clara._hash(jsonb)`, `clara.withdraw_draft(uuid,text,text)`. (c) **Pre-image `sha256(prosrc)` pins, every one MEASURED on `clara_651` after `pnpm db:migrate`**, for `_fa_oldest_unmet_period`, `_fa_run_period_core`, `_fa_asset_json`, `revise_fixed_asset_particulars`, `sign_depreciation_authority` (+ the same three — `_fa_validate_particulars`, `complete_fixed_asset_particulars`, `_fa_complete_particulars_core` — as RECUT pins on M1's green arm, and as **non-regression** pins on M1's red arm, where the classification never reaches them and their shas must be unchanged in the tail), plus **non-regression pins** for `get_fixed_asset` and `_fa_compute_charges` (this file must not move them). A pin read off a file will not match and 0227 refuses to apply — 0216's own comment says so in as many words.

1. **The classification (AC1, D10).** `change_class` / `change_reason` columns + `ck_fixed_assets_change_class` as ruled above. `revise_fixed_asset_particulars` is recut, **5-arg kept**: it requires a class and a non-blank reason, refuses `policy` and `error` by name with **CLR37 `fa_change_class_unsupported`** whose `detail` carries `{reason, change_class, owning_ticket:'#680', lock_law:'#679'}` and a human remedy, and stamps `change_class` + `change_reason` onto the **successor** row inside the existing supersede-forward insert (`0041:3209-3224`). A missing class is **CLR37 `fa_change_class_required`**. Every existing guard stays byte-identical — `fa_revise_effective_conflict` (`:3164-3172`), the baseline bound (`:3174-3179`), the pre-birth bound (`:3181-3186`). **That paragraph describes the door on M1's GREEN arm.** On the red arm every one of those requirements, refusals, guards and the stamp itself lives in the NEW `revise_fixed_asset_particulars_classified`, and the 5-arg `revise_fixed_asset_particulars` — signature still kept — carries only the `fa_change_class_required` refusal that points at it. **The transport is decided by measurement M1**, not by preference:
   - **M1 green (DECISIONS §2.1's roster)** — widen `_fa_validate_particulars`'s closed key set (`0041:2977-2984`) with `change_class` and `change_reason`, keeping the function `immutable` and ungranted; and because `complete_fixed_asset_particulars` (`0041:3035`) and `_fa_complete_particulars_core` (`0216:685`) **each carry their own body and each call the validator independently** (measured this pass — the human door does NOT route through 0216's core), **both** gain the same wall: **CLR37 `fa_change_class_on_completion`**, "a first completion is not a change". That is the honest reading of DECISIONS §2.1's "+ its 0216 `_for` twin if the validator change reaches it" — the twin's own body has no validator call, so the wall belongs in the core it delegates to.
   - **M1 red — the fallback DECISIONS §6 / SYNTHESIS §7.4 #4 names, taken LITERALLY: a NEW VERB with a separate argument.** The classification then never touches the validator and never travels through jsonb. `_fa_validate_particulars` (`0041:2977-2984`), `complete_fixed_asset_particulars` (`0041:3035`) and `_fa_complete_particulars_core` (`0216:685`) are **not recut at all** — pin all three as non-regression, leave the closed nine-key set byte-identical, and the frozen mirror is provably unmoved. Instead **mint** `clara.revise_fixed_asset_particulars_classified(p_client uuid, p_asset uuid, p_particulars jsonb, p_effective_from date, p_op_key text, p_change_class text, p_change_reason text)`: the live 5-arg body verbatim (same `_human_ctx(clara.role_rank('bookkeeper'))`, same `_reserve_op` over `_hash(...)` — widened with the two new fields so a classified retry is its own identity, same `fa_revise_effective_conflict` / baseline / pre-birth guards, same supersede-forward insert at `0041:3209-3224`) **plus** the two columns stamped on the successor row and the `policy` / `error` refusals. `clara_authenticated` only, owner `clara_fn_owner`, EXECUTE revoked from `public`, in the 0227 cohort and in this file's own single-row census. `change_class` and `change_reason` are **real separate SQL arguments**; nothing about the classification enters `p_particulars`. DECISIONS.md:208-210 and SYNTHESIS.md:725 say exactly this ("the classification must move to a separate argument of a NEW verb") — an earlier draft of this brief proposed stripping the two keys out of `p_particulars` inside the existing 5-arg verb instead; that variant is **withdrawn as a contradiction of a binding ruling**, not offered as an option.
     **Path B's price, measured now so nobody meets it at merge time.** (i) `revise_fixed_asset_particulars` **keeps its 5-arg signature** (DECISIONS §2.1, verbatim) but its recut body becomes a **refusal-only door**: CLR37 `fa_change_class_required`, `detail` naming `revise_fixed_asset_particulars_classified` as the remedy — without that, an unclassified revision stays reachable and AC1 is one door away from false. (ii) That moves every live call site, and they are yours on this branch: `apps/web/lib/registers/fixed-assets.ts:295-300` and its caller `apps/web/components/registers/fa-row-actions.tsx:102`; the web units `apps/web/lib/registers/fixed-assets.test.ts:115-126` (which asserts the `/rpc/revise_fixed_asset_particulars` URL) and `apps/web/components/registers/register-refresh-siblings.test.tsx:158`; and `packages/db/tests/x41-fa-fixtures.mjs:264`, **which the whole x41 suite revises through** — M5 exists so a red there is unambiguous. (iii) Two exact-set censuses move by one name each and are **registered, never fought**: `packages/db/tests/x42-reservation-authority.test.mjs:364-376`'s `EXPECT_DOORS` is a `deepEqual` over every body whose source writes `clara.fixed_assets` (the new verb enters it; the refusal-only old verb leaves it), and `:388-392`'s `INHERITORS` excuses exactly the inserters that reach the role-claim union — the new verb inherits that reach verbatim from the body it copies and must be named there or it lands in `unguarded`. (iv) `packages/db/tests/x41b0-surface.test.mjs:306-316`'s grant census gains the new name (`clara_authenticated` yes; `agentRo` / `wakeInteractive` / `wakeProactive` no). (v) Report the duplicated revision body as a **named residual** with a follow-up to fold the two into one ungranted core once the validator question is settled — do not attempt that fold inside this ticket.
   Either way `revise_fixed_asset_particulars` keeps its 5-arg signature and **this file writes its own single-`pg_proc`-row census** over every name it installs or recuts — `0103:1055-1070` scans only the eleven names 0103 installs (`0103:1063-1069`), the tree holds exactly two such censuses (`0103`, `0126:2129-2139`), and **neither reaches this verb**.
2. **The locked-period wall (AC2, D9).** `_fa_assert_period_open` as specified — **one new helper, not two**. It raises **CLR38**, `detail = {reason:'period_request_invalid', axis:'period_closed', fiscal_year_id, fy_label, fy_status, period_start, period_end, remedy:'reopen_fiscal_year'}` — a new axis on the existing reason, so the FA surface's existing CLR38 rendering holds. **Splice it into `_fa_run_period_core` immediately before the draft insert** (after the `_fa_compute_charges` / zero-charge `noop` arm, so a zero-charge closed period is still a lawful `noop` rather than a refusal). **Splice the SAME fiscal-year predicate INLINE into `_fa_oldest_unmet_period`** — an `exists (…)` over `clara.fiscal_years` whose selection and status text are byte-identical to the assert's, **never a call into the assert and never a `begin/exception` around one** (the oracle is `stable` and must answer, `0041:1904-1905` + `:1915-1916`; the tail binds the two fragments together) — so the oracle **skips** a closed period and offers the next open one — and, because a skipped period is a charge that will never be made, the oracle's answer carries `skipped_closed: [{period_start, period_end, fiscal_year}]` and `depreciation_run_due` surfaces it. **Do NOT floor or wall the poster while leaving the oracle untouched**: `0042:4441` names that exact failure in its own words — "an oracle that does not ask it advertises a period its poster will refuse, once per sweep, forever" — and `reconciler-fa.mjs:108-157` would bury it as one `faFailed` per client per cycle. `dispose_fixed_asset` is **left alone**.
3. **The authority's explicit instruction and window (AC5, D7 + D8).** Columns and backfill as ruled. `sign_depreciation_authority` DROP+CREATE'd to 4 args with the resolution ladder and the re-grant, and it stamps `authority_from := clara._fa_month_start(clara._fa_today())`, written once and never moved thereafter.
   **The floor's mechanism — this is the part the design lens rewrote, and it is binding.** The floor lives **inside `_fa_oldest_unmet_period`'s own name**, with 0042's spliced `_wdb_rerun_breach` gate call still inside that same body. Lifting the arithmetic into a floor-aware helper verb **reds two live CI batteries**: `_wdb_rerun_breach`'s consumer set is `deepEqual`-pinned to exactly `["_adj_oldest_unmet_period","_adj_run_occurrence_core","_fa_oldest_unmet_period","_fa_run_period_core"]` at `x42b2-r7-s5-census.test.mjs:118-120` and `x42b2-period-class.test.mjs:162-167` (both opened this pass). Adding a bypass parameter to the 1-arg verb is equally refused (a DROP+CREATE re-patching four call sites plus a `rig-meta` row, against §2.2 rule 4).
   **What the floor reaches, measured rather than assumed**: the belt (`reconciler-fa.mjs:112`) and the public probe `depreciation_run_due` (`0041:3617-3640`) stop offering pre-floor periods — **that is the point of D8**. `_fa_run_period_core` reads the oracle **only for sequencing** (`0041:3479-3494`), so a **caller-named** pre-floor period still runs, which means **`run_depreciation_manual` (`0041:3598`, bookkeeper+, identical mechanics to the machine path) already IS the explicit human catch-up door D8 names. No new human door is owed** — gap-651's Q2 promise of one is **withdrawn (orchestrator ruling — supersedes gap-651 Q2)**, and DECISIONS §2.1's object roster for 0227 correctly contains none.
   **Two consequences the slice must carry.** (a) The `period_earlier_unmet` guarantee — the thing that pins the reducing-balance arithmetic so a run can never read around an unapproved period — **stops binding below the floor**, and the same hole opens for a closed period the oracle now skips. Take ONE of the two answers and prove it: either re-derive the oldest-unmet bound **unfloored and closed-period-inclusive** for the caller-named path inside `_fa_run_period_core`, or accept the exposure and pin it as a residual. `p651.authority.floor_sequencing` asserts whichever you took. (b) The agent catch-up lane's lost reach, named in the header and in the report for `closePrep_v2`.
   **The four existing sign call sites break the moment the ref is required, and they are yours to fix on this branch**: add a shared fixture helper (a **labelled** `rootQuery` fixture insert into `clara.agent_tasks(firm_id, client_id, kind, status, model_snapshot, created_by)` — the established pattern at `f-a4-pr1c-fixtures.mjs:60`, `activity-feed.test.mjs:163`, `binding-proposal-pr-1.test.mjs:206`) returning `{kind:'chat_task', id}`, then thread it through `x41-fa-fixtures.mjs:274-277`'s `signAuthority` (which the whole x41 suite uses), `client-onboarding-identity.test.mjs:473-475`, `f-a4-pr1c-rungs.test.mjs:294-296` (which already mints an agent task) and `x56-rest-j.test.mjs:87-88`. **Run the whole x41 suite before you believe this is done.**
4. **The preview read (AC5, I6).** `preview_depreciation_run(p_client)` calls `depreciation_run_due` then `_fa_compute_charges` over that period and returns `{due, reason, period_start, period_end, cadence, authority_from, charges:[{asset_id, description, period_start, period_end, amount_cents}], skipped:[{asset_id, reason}], charged_cents, entries, legs:[{account_code, debit_cents, credit_cents}], mode_would_be, skipped_closed}`. **The leg aggregation is duplicated, not extracted** — the same ruling SYNTHESIS J3 gives #657 for the twinned candidate projection: re-plumbing the poster's aggregation inside a 0042 splice is the riskiest edit available for a cosmetic gain. The duplication is bound **two ways**: a tail assertion that the normalized aggregation fragment occurs in **both** `_fa_run_period_core` and `preview_depreciation_run` (the 0042 marker-census idiom), and the behavioural cell `p651.preview.matches_run`. Name the duplication as a residual and file the follow-up. The preview **writes nothing** — no `_reserve_op`, no receipt.
5. **The machine/OBO run door (AC5, successor contract).** `run_depreciation_period_for(p_client, p_through, p_op_key, p_obo)`, `clara_runtime` only, on `complete_fixed_asset_particulars_for`'s ladder **verbatim** (`0216:761-812`, opened this pass): `p_obo` null → CLR04 `obo_not_active`; client unknown → CLR11 `client_not_found`; **`clara.firms … for key share` then `clara.firm_memberships … for share`** (the measured lock pair — without them a demotion committing between the read and the write slips past the check); membership not active → CLR04 `obo_not_active`; below the bookkeeper floor → CLR04 `insufficient_role`; client not active → CLR10 `client_inactive`. Then `_reserve_op` over `_hash({client, through, obo})` for replay identity, then the loop copied from `_agent_depreciation_catchup_core` (`0138:2386-2404`, opened this pass): guard 12, ask the oracle each iteration, exit on not-due or `period_end > p_through` (`p_through` null ⇒ `clara._fa_today()`), calling `_fa_run_period_core` with the derived op key `p_op_key || ':' || period_end`, then `_finish_op`. **This door is FLOORED and it must DELEGATE**: its 4-arg signature carries no floor bypass, so Clara cannot reach a pre-floor period this wave — say so in the module's sentence map and point the person at the human door; and it inserts nothing itself, because the `origin='scheduled_run'` writer set is `deepEqual`-pinned to three names at `x41b2-surface.test.mjs:132-138` and `x42b2-r8-tails.test.mjs:174-180` (both opened this pass).
6. **The detail read (AC4).** Recut **`_fa_asset_json(uuid,date)` only**, adding exactly two keys (`change_class`, `change_reason`) to its `jsonb_build_object` — measured this pass as the one body that projects a generation, and the one every `lineage` element and the `asset` block both flow through. **`get_fixed_asset` needs NO recut**: it merely composes, and its `'lineage' / 'charges' / 'schedule' / 'uncharged_due'` birth-key census at `0216:993` stays true. Both are pinned by `0216:205-219`, so re-measure on the rig and re-assert 0216's own claims at their **post-0227** shas; pin `get_fixed_asset` as **non-regression**.
7. **Grants, events, tail, gate, cohort.** `revoke all … from public` on every new function; `grant execute … to clara_authenticated` on `preview_depreciation_run` and the re-created `sign_depreciation_authority`; `grant execute … to clara_runtime` on `run_depreciation_period_for`; `alter function … owner to clara_fn_owner` on everything new or recut. **No new event** — `asset.depreciated` already exists (0041 EVENTS); if a classified revision earns one, register it with `ignore` (0055 idiom).
   **Tail, every assertion executable**: (a) owner / SECURITY DEFINER / fixed `search_path` / ACL re-proved byte-for-byte for every touched body; (b) 0042 S5.15c's marker census **and** its ordering postcheck re-run against the recut oracle; (c) 0042 S5.15d's markers re-run against the recut poster; (d) `_wdb_rerun_breach`'s consumer set still exactly the four names; (e) the `origin='scheduled_run'` writer set still exactly `_adj_on_approve, _adj_run_occurrence_core, _fa_run_period_core`; (f) **`_fa_run_period_core`'s caller set is now exactly FOUR**, named; (g) a `having count(*) <> 1` name-purity census over **every name this file installs or recuts**; (h) `_fa_compute_charges` holds **no** EXECUTE for any application role; (i) `run_depreciation_manual` holds **no** grant for any machine role; (j) the `close_prep` wake source is **still disabled**, in `0223:247-250`'s idiom; (k) the leg-aggregation fragment appears in both bodies; (l) `get_fixed_asset`'s sha is **unchanged**; (m) `FA_0041_COHORT` and `FA_ACQUISITION_0216_COHORT` still resolve whole; (n) the normalized fiscal-year-selection fragment (`from clara.fiscal_years … order by (fy.status in ('closing','closed')) desc`) appears in **both** `clara._fa_assert_period_open` and the recut `clara._fa_oldest_unmet_period`, so the wall and the skip can never drift apart, and **no function named `_fa_period_open` exists** (`to_regprocedure` is null) — the roster is one helper; (o) on M1's red arm only: `revise_fixed_asset_particulars` no longer writes `clara.fixed_assets` and `revise_fixed_asset_particulars_classified` does, each exactly once.
   **Gate module** `packages/db/tests/depreciation-history-preintegration-gate.mjs` — the stem matches the migration's stem `_depreciation_history$`, **never a number** (DECISIONS §2.2 rule 2; this corrects gap-651's earlier `depreciation-policy-` stem). Mirror `preview-invite-preintegration-gate.mjs` verbatim and append **one** `--import ./tests/depreciation-history-preintegration-gate.mjs` to `packages/db/package.json`'s 40-flag chain in **migration order**, after `--import ./tests/preview-invite-preintegration-gate.mjs` (making 41).
   **`rig-meta.mjs` cohort** — one block, `FA_DEPRECIATION_0227_COHORT`: human `preview_depreciation_run`; runtime `run_depreciation_period_for`; ungranted `_fa_assert_period_open` — the file's ONLY new helper — declared the 0020 way, so a grant appearing on it fails the main sweep; **on M1's red arm the human list also carries `revise_fixed_asset_particulars_classified`**. Its own cohort, not folded into `FA_0041_COHORT`, for the "wholly present or wholly absent" reason `rig-meta.mjs:1930-1934` states. **`sign_depreciation_authority` stays in `FA_0041_HUMAN_FNS` (`:697`) — do not move it; the roster is by name, and the name is unchanged.**

### Runtime

**No frozen edit, no successor cut, no `registry.ts` change, no route.** `reconciler-fa.mjs` needs **no change** — the floor and the closed-period skip are DB-side, which is the module's own law (`:15-20`). New non-frozen module **`packages/runtime/lib/depreciation-run.ts`**: the `.strict()` zod input, the `run_depreciation_period_for` call in the fixed argument order, and the CLR38 / CLR19 / CLR04 → sentence map. **Write it to be final** — it freezes by closure the instant `chatTurn_v21` imports it. **Do not touch** `plan-occurrences.mjs`, `closePrep.v1.*`, `wake_engine_sources`, `lib/fixed-asset-acquisition.ts`.

**Successor-contract stanza — reproduce this VERBATIM in your final report** (DECISIONS §1.2 row "#651"; `chatTurn_v21` is cut ONCE by the integration worker after the ten merges):

> **Body**: `chatTurn_v21`. **Tool name**: `run_depreciation_period_for_client`.
> **Zod input** (`.strict()`): `{ client_id: z.string().uuid(), through: z.string().regex(/^\d{4}-\d{2}-\d{2}$/).optional() }`. **No period start/end — the period is the database's** (`0041:3457-3470` refuses any caller-named window that is not the cadence's). `through` bounds a catch-up and defaults to the current book day.
> **Carrier module**: `packages/runtime/lib/depreciation-run.ts` (NEW, non-frozen at authoring time, freezes on import).
> **Door call, exact argument order**: `select clara.run_depreciation_period_for($1::uuid, $2::date, $3::text, $4::uuid) as r` — `(p_client, p_through, p_op_key, p_obo)`, granted to `clara_runtime` **only**, with live-authority recheck of the named human (active member of the client's firm, bookkeeper floor, active client). **A NEW verb name is mandatory, not stylistic**: `rig-meta.mjs:691-693` is an executable census that fails the moment `run_depreciation_manual` reaches a machine role.
> **Refusal mapping**: CLR38 `authority_not_live` → "This client has no signed depreciation authority. An admin signs one before Clara can run depreciation." · CLR38 `period_draft_outstanding` → "A depreciation draft is already waiting for approval; approve or withdraw it first." · CLR38 `period_earlier_unmet` → name both dates · CLR38 `period_request_invalid` axis `not_ended` → "That period has not ended yet." · CLR38 `period_request_invalid` axis `period_closed` → name the fiscal year and the reopen path · CLR19 `write_into_closed_period` → name the fiscal year and the reopen path · CLR04 `obo_not_active` / `insufficient_role` → "You no longer hold the authority to run this."
> **Floor**: the door clears due periods from `authority_from` forward up to `p_through` and **carries no floor bypass**, so Clara cannot reach a pre-floor period; the sentence map points the person at the human door `run_depreciation_manual`.
> **Part kind**: reuse the existing `work_accepted`-adjacent receipt part shape. **No new part kind and NO `accounting_work.purpose` widening** — depreciation posts through `journal_entries` directly and never reaches the Work lane (`0195:1711` / live `0204:180` stay closed).
> **Prompt stanza**: "You execute an authority; you never sign one. The period is the database's, never yours. Say you have QUEUED/POSTED exactly what the receipt says, including how many assets were skipped and why."
> **Not folded here**: #933 rides `claraWork_v5`, a different closure — do not fold the two. #882(a)'s CLR40 stanza is **#658's worker's** to write.

### Web

- **`fixed-asset-detail.tsx` — a fifth tab, URL state, and two tabs that finally read what the door already returns.** Tab ids move into `?tab=` (`useSearchParams` + `router.replace`, the `?tab=fixedAssets` precedent), so Back and a pasted link both land on the same reading; `SectionTabs` stays. **New `revisions` tab** (Q7): the policy-revision timeline built from the `lineage` array — one row per generation with `effective_from`, method / life / rate / residual, `change_class` and `change_reason`, current generation marked, **its own empty state** ("this asset's estimate has never been revised"), its own SR name. **`history` tab**: the immutable charge ledger from the `charges` array — period, amount, `effective_date`, the run it belongs to and the journal entry it posted via `journalEntryHref(clientId, entryId)` (`tree.ts:565-568`, which renders `…/journals?tab=posted&entry=<id>`), an unwound row struck through and labelled; lineage relations stay. Both are `DataTableCard` + `Table`, the file's existing composition; appendix D **row 57 (Table, Production)** binds the 320px reading strategy ("preserve a narrow-screen reading strategy rather than clipping columns silently").
- **The revision entry point gains the classification — AC1's human half.** `apps/web/components/registers/fa-row-actions.tsx:102`'s revise dialog (through the wrapper at `apps/web/lib/registers/fixed-assets.ts:295-300`) gains a **change-class** control and a required **reason** field. `estimate` is the only selectable value; `policy` and `error` are rendered as **visibly disabled options carrying the reason in words** ("a policy change or an error correction is a retrospective restatement — ticket #680 owns that lane, under #679's lock law") rather than hidden, so a person learns the rule instead of wondering where it went; the typed CLR37 refusal is still rendered **verbatim with its code** if the door is reached any other way. `FieldGroup` / `Field` / `FieldLabel` / `FieldDescription` / `FieldError` (appendix D row 28), `aria-invalid` on a blank reason, and both values preserved across a refusal. **Which door the wrapper posts to is decided by M1** — Path A: `revise_fixed_asset_particulars` with two new keys inside `p_particulars`; Path B: `revise_fixed_asset_particulars_classified` with two new arguments (§3 item 1). Write the wrapper so that **only the wrapper** changes between the two arms.
- **`fa-depreciation-runs-panel.tsx` — preview first, then confirm.** The dialog calls `preview_depreciation_run` and shows the **exact period the DB chose**, the per-asset amounts, the two GL legs, the **skipped list with each reason in words** — all **five** the database can emit (M7): "waiting on depreciation particulars" `incomplete`, "not yet in service" `not_in_service`, "fully depreciated" `fully_depreciated`, "stated as not depreciated" `none_method`, and **"a disposal draft is waiting on this asset" `disposal_draft_outstanding`** (`0041:1862-1868`, the one the per-asset function never returns); an asset that is simply up to date is **not** a skip and never appears (`0041:1390-1393`); **any reason the map does not know renders as its verbatim code beside a neutral sentence — never dropped, never guessed** and whether it will post or draft — then Confirm. Move the controls onto `FieldGroup`/`Field`/`FieldLabel`/`FieldDescription`/`FieldError` (appendix D **row 28 (Field)**: "all new persistent inputs through FieldGroup/Field … `data-invalid` and `aria-invalid`; it replaces ad hoc label/error layout"; precedent `work-question-form.tsx`), **or drop the two date inputs entirely** since the database chooses the period — prefer dropping them and say so. Each run row discloses its `skipped` and links its `entry_id`. Appendix D **row 17 (Collapsible)**: "never hide unresolved questions or errors by default" — the skipped list may collapse only when every reason is benign; a row skipped for incomplete particulars renders **open**. Appendix D **row 23 (Dialog)** is the preview's own checklist (Title, initial focus, Escape/cancel, unsaved input, focus restoration).
- **One decision, one key.** `runDepreciationManual` currently mints `crypto.randomUUID()` per call (`lib/registers/depreciation.ts:140-145`), so "response lost, click again" returns a refusal instead of the original receipt. Derive the op key from a **hash of the intent tuple** (`client_id`, `period_start`, `period_end`) held for the life of the open decision, on `components/work/work-cancel-dialog.tsx:95`'s `useDecisionKey` shape, **in `apps/web/lib/registers/depreciation.ts` and the FA dialogs only**. This is the same defect D15 rules for the bank lane; you fix your own files and **never open `lib/bank/*`**.
- **`depreciation-authority-panel.tsx` + `fa-authority-ceremony.tsx`.** The sign ceremony gains the **instruction reference** input (the Work or chat task the instruction lives in) and renders the resolution refusals verbatim with their codes; the panel shows the resolved reference as a link and the `authority_from` floor with the honest sentence that anything earlier needs an explicit catch-up through the human door. The existing admin-only Sign/Retire ceremony is otherwise untouched.
- **States on every touched surface**: loading (shape-matched `Skeleton`; appendix D **row 53** — "stop immediately on success, empty, denied, or error"); successful-empty ("no depreciation has been run for this client") and no-results (filtered runs) kept apart from denied/failed (appendix D **row 27 (Empty)**: "permission denial and fetch failure are not empty data"); partial-stale (a refusal banner **above** still-good data — `StateBanner`, the file's existing posture; 2026-09-15 §1.7: `StateBanner` stays, Alert is #646's documents surface only); invalid/saving (`aria-invalid` + `FieldError`, Confirm disabled with the reason, typed values preserved); **denied — the verbatim CLR text with its code, never re-worded**; failed; **cancelled-recovery — withdraw the blocked draft and watch the queue reopen, the one real recovery this journey has**. 320px, 200% zoom, keyboard + focus return after every dialog, SR names on the timeline and ledger tables, `prefers-reduced-motion`, stable `?tab=` URL/Back, and a preserved draft across a refused run.
- **New keys in `apps/web/messages/en.json`** at the sorted position, under the existing `FixedAssetDetail`, `ClientRegisters.fixedAssets` and `FixedAssetsDepreciation.*` namespaces. **No new nav leaf, no `tree.ts` row** — `fixedAssetHref` already addresses the detail page and a query parameter needs no new entry.

### Docs

**Never `docs/PRD.md`, never `docs/ARCHITECTURE.md`.** `CONTEXT.md`: the two ratified terms plus the permitted refinement, in the house "term / _Avoid_" shape, at the sorted position. READMEs: `packages/db/README.md` (the new doors, floors, grants, the ungranted-core/granted-wrapper idiom and the census that enforces it; **the locked-period law for the whole FA lane and #678's inheritance**), `packages/db/tests/README.md` (the new gate and battery), `packages/runtime/README.md` (why `reconciler-fa.mjs` needed no change; the new lib's freeze trajectory), `apps/web/README.md`.
**Blueprint drift, recorded in your report, never edited** (DECISIONS §4; SYNTHESIS §6): `docs/PRD.md:109` lists 固定资产与调整 under 当前版本已交付 while the classification and the locked-period law are gaps inside it; `docs/ARCHITECTURE.md §5 D` states the plan lane's law (explicit resolvable instruction, no back-fill, no global agentic switch) and **the depreciation lane meets neither of the first two** while being the estate's strongest unattended poster — the blueprint never says depreciation is exempt, it simply does not mention it; `docs/ARCHITECTURE.md:183`/`:293` pin `chatTurn → v19` / `claraWork → v3` while `registry.ts:173`/`:270` pin **v20 / v4**, and `§4`'s version-pin sentence names only `chatTurn`/`claraWork` (already incomplete since `clientOnboarding` repointed to v5).

## 4. TDD seams (red first)

### Measure first, on `clara_651`, before one line of SQL is written

These are gap-651's "Unverified" list and SYNTHESIS §7.4 #1/#4/#5/#17. **Each one changes what you write**, not merely what you claim.

- **M1 · `p651.measure.frozen_mirror`** — *the single most consequential measurement.* On a scratch `create or replace` against the LIVE `_fa_validate_particulars` body, widen the closed key set with `change_class` / `change_reason`, then run `packages/db/tests/fixed-asset-acquisition.test.mjs` (`p639.particulars.axes` `:663`, `p639.question.dependent` `:700`), `packages/runtime/tests/fixed-asset-acquisition-unit.test.mjs` (`fa.keys` `:38`, `fa.fields` `:48`) and `node scripts/check-frozen-workflows.mjs`. **Green → §3 item 1's Path A (widen the validator). Red → Path B (mint `revise_fixed_asset_particulars_classified` and make the 5-arg door refuse), which is DECISIONS.md:208-210 / SYNTHESIS.md:725 read literally** — and say which arm you took, in the report, with the command output that decided it. Note either way: the frozen `FA_PARTICULARS_KEYS` (`lib/fixed-asset-acquisition.ts:51-58`) becomes a strict SUBSET of the DB's admitted set, and `fa.keys`'s prose claim ("exactly the nine `clara._fa_validate_particulars` accepts") goes stale even where its assertion stays true — record that as drift, do not weaken the assertion. The two keys are **revision-only** and never travel through the frozen `.strict()` answer schema, which is precisely why the mirror stays fit for its purpose.
- **M2 · `p651.measure.prosrc_pins`** — take `sha256(prosrc)` and `pg_get_functiondef` off the catalog for all five (or eight) recut bodies **plus** `get_fixed_asset` and `_fa_compute_charges`. Confirm by inspection that the live `_fa_oldest_unmet_period` and `_fa_run_period_core` carry 0042's spliced text (the `_wdb_rerun_breach` call), i.e. that the file text is **not** the live body. **Write no prestate before this runs.**
- **M3 · `p651.measure.closed_period_blocks`** — build a `clara.fiscal_years` row and formally close it (`finalize_close`), run depreciation into it on **today's** code, and prove the three-step failure the whole locked-period slice exists to remove: the draft succeeds, approve dies CLR19 (`0056:694-698`), and `depreciation_run_due` then answers `due:false / period_draft_outstanding` (`0041:1916-1922`) for **every** later period. The x41 world builds no fiscal year at all, so **this fixture is new work, not a lookup** (`x41-fa-world.mjs` / `x41-fa-fixtures.mjs`: zero occurrences; `fixed-asset-acquisition.test.mjs:344-346` says so itself). Then prove `clara.withdraw_draft` accepts a depreciation draft and the queue reopens — inferred today from the refusal at `0041:5217`, never exercised.
- **M4 · `p651.measure.splice_anchors`** — locate and count the anchors your two splices will `replace()`, and re-run 0042 S5.15c's marker census (`0042:4142-4162`) and ordering postcheck (`:4200-4213`) against the live bodies **before** you change them. An anchor that appears twice is a stop-and-report, not a patch.
- **M5 · `p651.measure.sign_callers`** — run the whole x41 suite, `client-onboarding-identity.test.mjs`, `f-a4-pr1c-rungs.test.mjs` and `x56-rest-j.test.mjs` green at base, so you know which reds afterwards are yours.
- **M6 · `p651.measure.authority_from_backfill`** — count live/retired `fa_depreciation_authorities` rows on the rig and record, **for each row, BOTH candidate stamps**: DECISIONS §2.1's literal `date_trunc('month', signed_at)::date` evaluated under the `TimeZone` your applying session actually carries (print `show timezone` beside it), and the MYT composition this brief specifies. Where they differ, name the rows — that difference is the whole of the flagged deviation, and it is decided on measured rows rather than in the abstract. The **hosted** count is a release-time read in the runbook, reported before the hosted migration and **never assumed zero** (DECISIONS §6 §7.4 #17; beta data is test data, memory 2026-09).
- **M7 · `p651.measure.skip_vocabulary`** — read the **live** `_fa_asset_charges` and `_fa_compute_charges` bodies off `pg_get_functiondef` (both are 0042-era splices, §S5.27 `0042:5928-6135`) and list every distinct skip literal they can emit; re-derive the writer set of `clara.fa_depreciation_runs.skipped` from the catalog (`prosrc ~ 'insert into clara\.fa_depreciation_runs'`). The expected answer, measured from file text this pass, is **five names and one writer**: `not_in_service`, `incomplete`, `none_method`, `fully_depreciated` (`0041:1352-1393`) plus `disposal_draft_outstanding` (`0041:1862-1868`), all stored verbatim by `0041:2320-2324`. If the live bodies show a sixth, the preview and runs-panel sentence maps gain it **in the same commit**. Either way the web **degrades an unmapped reason to its verbatim code** beside a neutral sentence rather than dropping the row or guessing, and a web cell proves that — AC6's "every skip reason in words" is only honest if the vocabulary was measured. *(This cell exists because gap-651's own Unverified list said the vocabulary "was not exhaustively traced"; tracing it found the fifth name.)*

### DB battery `packages/db/tests/depreciation-history.test.mjs`

Stem matching `0227_depreciation_history.sql`, its own preintegration gate and `rig-meta` cohort, **every assertion through `humanQuery` least-privileged personas** — never `rootQuery` for the thing under test (labelled fixture DML excepted). Cell prefix **`p651.`** throughout (`#884` reserves separate ground).

1. `p651.class.required` — a revision with no `change_class` is refused **by name**; one with `estimate` supersedes forward and stamps class and reason on the successor row. On **Path B** the same cell runs twice: the 5-arg door refuses `fa_change_class_required` naming the classified verb, and `revise_fixed_asset_particulars_classified` refuses a null/blank `p_change_class` by the same name. *Red: the column does not exist.*
2. `p651.class.policy_refused` / `p651.class.error_refused` — both refuse CLR37 `fa_change_class_unsupported`, the `detail` names **#680** (and #679's lock law), and **no row is written** — assert the row count, not the message.
3. `p651.class.prior_untouched` — after an `estimate` revision every pre-existing `fa_depreciation` row is byte-identical and `uq_fa_depreciation_live_range` still holds.
4. `p651.class.completion_refused` — a first completion carrying `change_class` is refused (Path A: CLR37 `fa_change_class_on_completion`; Path B: the existing `unknown_key` axis) through **both** `complete_fixed_asset_particulars` and `complete_fixed_asset_particulars_for`.
5. `p651.period.closed_refused` — **the decisive cell.** A client with a CLOSED fiscal year: `run_depreciation_manual` over a period inside it refuses **at the door** with CLR38 axis `period_closed`, **no draft entry exists**, and `depreciation_run_due` still answers a lawful reason rather than pointing at a dead draft.
6. `p651.period.closed_belt_skips` — the oracle skips the closed period, offers the next open one, and names the skip in `skipped_closed`; assert that the skipped period's charge is **never silently posted later**.
7. `p651.period.blocked_draft_recovery` — a draft left outstanding blocks the queue (`period_draft_outstanding`), `withdraw_draft` clears it, the next probe is due again. **This cell exists today in no battery.**
8. `p651.authority.ref_resolves` — signing with a ref resolving to a real Work (or chat task) in the same firm **and** client succeeds; a ref naming another firm's row, a non-existent id, a non-object, a bad `kind` or a Knowledge record is refused by name and **nothing is signed**.
9. `p651.authority.floor` — an authority signed today on a client with two years of uncharged assets makes the belt run **forward only** (`depreciation_run_due` names the floor month, never an earlier one); `run_depreciation_manual` still reaches an earlier month; and `_depreciation_run_due_core` — the agent catch-up lane's only period source (`0138:1130-1138` → `:2392`) — answers not-due for every pre-floor month, asserted as the **measured loss** the header declares, not as a passing behaviour.
10. `p651.authority.floor_sequencing` — with the oracle floored, two pre-floor months run through `run_depreciation_manual` **out of order**; pin whichever answer the slice took (either `period_earlier_unmet` still refuses because the bound was re-derived unfloored, or it does not and the reducing-balance "never read around an unapproved period" guarantee is asserted as **not holding below the floor** and named as a residual).
11. `p651.census.rerun_gate` — after both recuts: `_wdb_rerun_breach`'s consumers are still exactly the four names (`x42b2-r7-s5-census.test.mjs:118-120`, `x42b2-period-class.test.mjs:162-167`); the `origin='scheduled_run'` writers are still the three (`x41b2-surface.test.mjs:132-138`, `x42b2-r8-tails.test.mjs:174-180`), i.e. the new OBO door **delegates and never inserts**; and 0042 S5.15c's ordering law still reads true of the recut oracle.
12. `p651.census.callers_four` — `_fa_run_period_core`'s caller set is now exactly `run_depreciation_period`, `run_depreciation_manual`, `_agent_depreciation_catchup_core`, `run_depreciation_period_for`, derived from the catalog.
13. `p651.preview.matches_run` — `preview_depreciation_run` and the subsequent run agree **exactly** on period, per-asset amounts, the skipped set — built to contain **both** an `incomplete` asset and a `disposal_draft_outstanding` one, the fifth reason the per-asset function can never return (`0041:1862-1868`, M7) — and the two legs; the preview writes nothing (assert receipt and entry counts unchanged).
14. `p651.preview.grants` — the preview is refused to `clara_runtime`, offered to a viewer, refuses another firm's client with CLR11, and `_fa_compute_charges` still holds **no** EXECUTE for any application role.
15. `p651.obo.run_for` — the door runs OBO an active bookkeeper; refuses a demoted or removed initiator with CLR04 **by name**; replay on the same op key returns the identical receipt; a different-args same-key call is refused; and the three writer variants' mechanics are asserted identical in one assertion (**C86.2's re-derived pin**).
16. `p651.obo.manual_still_human` — `run_depreciation_manual` still holds **no** grant for any machine role (the `rig-meta.mjs:691-693` law, proven by calling it as `clara_runtime` and reading the refusal).
17. `p651.census.replay` — a from-scratch apply to this migration leaves `0103`'s privilege census, both FA cohorts and `0223`'s "close_prep still disabled" assertion all green.

### Runtime

Extend `packages/runtime/tests/reconcile-fa-unit.test.mjs` with `p651.belt.unchanged` — the belt is untouched by the floor and the closed-period skip (the DB answers not-due, the belt makes no run call and logs nothing anomalous); the point is that **no client-side mirror appeared**. New unit battery for `lib/depreciation-run.ts`: every CLR the door can raise maps to a sentence, an unmapped code degrades to the verbatim message, the schema is `.strict()`, and the floor sentence points at the human door.
**No Workflow World leg is owed, and that is a finding, not an omission.** Depreciation invokes no workflow — state it plainly and cite `reconciler-fa.mjs:59-61` plus `reconcile-fa.test.mjs:94` (one real-DB end-to-end under the `clara_runtime` group role) as the durability evidence in its place. **Re-run `reconcile-fa.test.mjs` unchanged with the PG env** as inherited evidence: from `packages/runtime`, `PGHOST=127.0.0.1 PGPORT=55704 PGUSER=postgres PGDATABASE=clara_651 node --test tests/reconcile-fa.test.mjs`, and report its counts. **No `.github/actions/db-live-gates/action.yml` World row is added** — only the db-battery gate line.

### Web unit — each file registered in `apps/web/test/manifest.txt` at its sorted position

`components/registers/fa-revision-timeline.test.tsx` (one row per generation with class and effective date; current marked; its own empty state; SR name) · `components/registers/fa-charge-ledger.test.tsx` (`charges` rendered with entry links; an unwound row struck through and labelled; 320px reading strategy) · `components/registers/fa-run-preview.test.tsx` (amounts, legs and every skip reason in words — all five named ones including `disposal_draft_outstanding`, plus an **unknown** reason rendering as its verbatim code rather than vanishing; a refused run keeps the dialog open with the verbatim CLR **and its code** and the typed values intact; the skipped list renders open when any reason is `incomplete`) · `components/registers/fa-detail-tab-url.test.tsx` (the tab id round-trips through `?tab=`; Back returns to the previous tab) · `components/registers/fa-row-actions.test.tsx` (NEW or extended: the class control offers only `estimate`, names #680 and #679 for the other two, requires a non-blank reason, preserves both across a refusal, and posts to whichever door M1 chose) · `lib/registers/depreciation.test.ts` **extended** (the preview wrapper; the 4-arg sign wrapper; **the decision key is stable across retries of one decision and renewed on a new one**).

### Playwright

New `apps/web/e2e/depreciation-walk.spec.ts` + `apps/web/e2e/depreciation-mock.mjs`, registered with one import plus one dispatch line in `apps/web/e2e/serve-built.mjs` and one ownership row in `apps/web/e2e/e2e-fixture-ownership.test.ts`. **Verbs the mock answers**: `preview_depreciation_run`, `run_depreciation_manual`, `sign_depreciation_authority` (exclusive to this lane — SYNTHESIS §3.3) and `get_fixed_asset`, `get_depreciation_authority`, `list_depreciation_runs` — **the last three are already answered by `fixed-asset-mock.mjs:57-64`, so declare them SHARED in `SHARED_RPC_VERBS` (`e2e-fixture-ownership.test.ts:1106`) or the census reds against a sibling mock.** POST bodies only through the shared `readCachedJson`. **Legs**: authority card → preview → confirm → the run row and its entry link; a closed period refusing **before anything is drafted**; the asset detail's charge ledger and revision timeline; **320px and 200% zoom**; keyboard through the preview with focus return; an axe scan under `prefers-reduced-motion`; Back returns to the same `?tab=`.

### Census suites that will red on files you never touched — register, do not fight them

`apps/web`: `tests/sql-oracle.test.ts` (a new migration file and new SQL functions), `tests/parity-holes.test.ts`, `tests/firm-scope-surfaces.test.ts`, `tests/firm-scope-fourth-entrance.test.ts`, `lib/navigation/tree.test.ts` (any href change), `e2e/e2e-fixture-ownership.test.ts` (every new mock verb). `packages/db`: `operation-census.test.mjs` and `rig-isolation.test.mjs` (**both, without the reset flags**), `0103:1055-1070`'s single-`pg_proc`-row census, the two `_wdb_rerun_breach` censuses and the two `origin='scheduled_run'` censuses named above, `x41-wave-d-a-fa.test.mjs`'s roster (`:66`), `x41b0-surface.test.mjs:306-316` (the grant census — every new human door is named there, `clara_authenticated` yes and the three agent roles no), `x42-s5-helpers.mjs:218`, and **on M1's red arm** `x42-reservation-authority.test.mjs:364-376` (`EXPECT_DOORS`, a `deepEqual` over the bodies that write `clara.fixed_assets`) and `:388-392` (`INHERITORS`) — one name in, one name out, registered in the same commit as the verb. **Runtime/freeze**: `node scripts/check-frozen-workflows.mjs` and `node packages/runtime/scripts/check-parts-parity.mjs` are expected **no-ops** — run them anyway and report the counts, because a green there is the evidence for the "no successor" verdict.

## 5. Risks

1. **`_fa_run_period_core` has THREE callers today and FOUR after this file.** A tail written for three passes while the new door sits outside the set — `p651.census.callers_four` is the mitigation, and the header names all four. The recut also changes the behaviour of a **parked agent lane nobody in this wave re-reviews** (`0138:2398`).
2. **Both bodies you recut are 0042 string splices.** `_fa_oldest_unmet_period` (§S5.15c, `0042:4097-4221`) and `_fa_run_period_core` (§S5.15d, `0042:4225`). A pin written from file text refuses to apply, and a `create or replace` that drops 0042's `_wdb_rerun_breach` call **reds two live CI batteries** that are `deepEqual` assertions, not apply-time DO blocks — they bite your branch, not only the from-scratch chain. Splice; do not transcribe.
3. **The classification key may be unreachable through the particulars jsonb.** **Measure M1 before writing the migration**, not after. The fallback is not a workaround inside the old door: DECISIONS.md:208-210 / SYNTHESIS.md:725 rule that the class travels as a **separate argument of a NEW verb**, so a red M1 costs a new granted verb, a refusal-only recut of the 5-arg door, five call-site moves (one of them the fixture the whole x41 suite revises through, `x41-fa-fixtures.mjs:264`) and two exact-set census rosters (`x42-reservation-authority.test.mjs:364-376` and `:388-392`). That cost lands on your branch, in your commits — not on the integrator.
4. **Overload hazard on the most-censused FA verb family.** `revise_fixed_asset_particulars` is granted by exact signature (`0041:4414`) and censused by name (`rig-meta.mjs:697`) — **keep the arity**. And note the correction: **nothing in the estate enforces that today** for this verb (`0103`'s census scans only its own eleven installed names, `0103:1063-1069`; the tree holds exactly two such censuses). This file writes its own.
5. **A required `authority_ref` breaks four live sign call sites**, one of them the fixture the whole x41 suite uses. They are named in §3 item 3 with their remedy; **M5 measures the base state so a red afterwards is unambiguous**.
6. **`authority_from` changes what an existing signed authority means.** Backfilling silently stops back-fill for a client mid-catch-up. Beta data is test data (memory 2026-09), but the **hosted count is read before the hosted migration and reported — never assumed zero** (DECISIONS §6 §7.4 #17).
7. **The floor removes the agent catch-up lane's reach, and the mirror mistake livelocks the belt.** Flooring the poster while leaving the oracle unfloored hands the belt a period its poster refuses **once per sweep, forever** (`0042:4441`), and `reconciler-fa.mjs:108-157` records it as one `faFailed` per client per cycle and nothing louder.
8. **A locked-period wall can strand a client's whole depreciation queue — and today it already does.** Ship the **recovery** leg (withdraw → queue reopens) with the wall, or the fix hardens a trap instead of removing it.
9. **The preview duplicates the poster's leg aggregation.** Bound by a `prosrc` parity assertion and a behavioural cell; named as a residual with a follow-up. This is the same trade SYNTHESIS J3 rules for #657 and the drift class `0040 FIX WAVE A5` records.
10. **`_fa_asset_json` is pinned by `0216:205-219`; after 0227 every future pin must use YOUR post-image.** Say so in the header, the way #657 must for its thirteen re-patched cores.
11. **#932 lands on the same `clara.fixed_assets` row** (a policy-version stamp) and recuts 0216's birth trigger. Not in this wave — but if it lands concurrently, **one migration or strict number order with a re-pin, never two concurrent recuts of one body**.
12. **Do not let this ticket be read as unparking `close_prep`** — tail assertion (j), or a later reader will believe an agent close lane shipped here.
13. **Merge collisions, one line each at the sorted position**: `packages/db/tests/rig-meta.mjs` (nine lanes — the `];` repair is the known hazard), `packages/db/package.json` gate chain (**migration order, not alphabetical**), `.github/actions/db-live-gates/action.yml` (per-line `\` continuations), `apps/web/messages/en.json` (**#635 REPLACES `Settings.unbuiltNote` — land nothing near it**), `apps/web/test/manifest.txt`, `apps/web/e2e/serve-built.mjs`, `apps/web/e2e/e2e-fixture-ownership.test.ts`, `CONTEXT.md`. **No `tree.ts` row, no `needs-you.ts` row kind, no `lib/parts/*` registration.**
14. **Known Windows-only reds you must NOT "fix"**: #707 (x56-rest-c shells out to grep), #693 (EICAR fixture quarantined by Defender), no `pg_dump` on PATH, `thread-live-clarify.test.tsx`'s whole-suite load flake (re-run alone and report both), `rig-isolation.test.mjs` T10b after a Workflow World bootstrap (#866). `next build` may panic `0xc0000142` under ten-lane contention — **retry once** (#869).

## 6. Effort and rig

**Effort: XL** (DECISIONS §7; SYNTHESIS §7.3). A classification (two columns + a CHECK, plus — per M1 — either a recut validator and two recut completion bodies, or a NEW classified verb and a refusal-only recut of the 5-arg door with its five call-site moves and two census rosters), a locked-period assertion spliced into **two shared FA oracles whose recut moves a third, parked, agent caller**, three new authority columns with a resolution rule and a DROP+CREATE'd signature, one new granted read, one new OBO machine door with its own cohort and gate — **plus** two substantial web surfaces (a revision timeline, an immutable charge ledger, a real preview dialog) with the full 320px / 200% / keyboard / SR / reduced-motion pass, **plus** a 17-cell DB battery and six measure-first cells, runtime unit work, five web unit files and a brand-new Playwright walk with its own mock, **plus** a `chatTurn_v21` successor contract. The estate's own precedent: 0216/#639 delivered one trigger, two recut reads and one OBO door and was its own wave slot.
**If the wave runs out of room, the L cut is** (gap-651's own, recorded here so it is not re-invented late): ship the locked-period wall + its recovery cell, the two detail tabs' missing content from arrays the read already returns (zero migration), and the preview read + dialog; split the classification, the authority's instruction reference and window, and the OBO door into a second ticket. That split is the #932/#933 shape the owner already chose for the policy half, and it leaves **#678 unblocked on the locked-period law**, which is the thing it actually waits for. **Do not take this cut unilaterally** — report it, do not do it.

**Rig**: worktree `C:\Users\zhant\Desktop\clara-wt\651`, branch `impl/651-assets-depreciation`, PG **55704** / db **clara_651**. Node 22 first, on every call: `export PATH="/c/Users/zhant/AppData/Local/pnpm:$PATH"` (PowerShell: `$env:PATH = "C:\Users\zhant\AppData\Local\pnpm;" + $env:PATH`). DB env: `export PGHOST=127.0.0.1 PGPORT=55704 PGUSER=postgres PGDATABASE=clara_651 CLARA_ALLOW_DESTRUCTIVE=1 CLARA_RIG_DB=1`. **Never set `CLARA_RIG_ALLOW_RESET=1` or `CLARA_RIG_ALLOW_ROLE_SWEEP=1`** — they reset the schema and trip 0154's cluster-wide role-count pin. No `psql` on Windows: use `node` + `pg`. First action on every start or restart: `git status` and `git log --oneline origin/main..HEAD` in your worktree.

**Before the final report, run**: `pnpm typecheck` and `pnpm lint` from the worktree root (both green); the **whole `apps/web` unit suite** once (`node scripts/run-tests.mjs` from `apps/web`, ~5 min) and report its counts; **`packages/db/tests/operation-census.test.mjs` and `rig-isolation.test.mjs` without the reset flags**, because this slice adds SQL functions; **your battery with the gate flags copied verbatim from `packages/db/package.json`** — now **41** `--import ./tests/*-preintegration-gate.mjs` flags including your own — plus a **focused run of your battery WITHOUT its gate, which must FAIL loudly below 0227** (a skip is not evidence); the **whole x41 suite** and `fixed-asset-acquisition.test.mjs` (the four sign call sites and the frozen-mirror question both land there); `packages/runtime/tests/reconcile-fa-unit.test.mjs` and `reconcile-fa.test.mjs` with the PG env; `node scripts/check-frozen-workflows.mjs` and `node packages/runtime/scripts/check-parts-parity.mjs` from the worktree root (expected no-ops — **report the counts, they are the evidence for "no successor"**); and the walk as `CLARA_E2E_APP_ORIGIN=https://127.0.0.1:3330 CLARA_E2E_NEXT_PORT=3331 CLARA_E2E_RUNTIME_PORT=3332 pnpm --filter @clara/web e2e depreciation-walk` (**never `npx playwright test` alone — it serves a stale build, #865**).

**Copy the house shapes from**: `packages/db/migrations/0216_fixed_asset_acquisition.sql` — the header that states its own measurement, the three-block prestate (`:165-230`), the measured-sha pin comments, the OBO door with its lock pair (`:755-815`), the cohort rationale and the gate; `packages/db/migrations/0042_wave_d_b0_shared_authorities.sql` §S5.15c/§S5.15d — **the splice idiom for these two exact bodies**, its marker census and its ordering postcheck; `packages/db/migrations/0193_accounting_plans.sql` — the `authority_kind` / `authority_ref` / `authority_from` column shape (`:415-430`) and the ref-resolution ladder (`:1482-1514`); `packages/db/tests/rig-meta.mjs:1930-1951` — the cohort block's shape and its "wholly present or wholly absent" rationale; `apps/web/e2e/fixed-asset-mock.mjs` — the `RPC_VERBS` set and the per-server state discipline; `docs/plan/active/refresh-wave-2026-09-15/brief-649.md` and `.../reports/649-final.md`, plus `docs/plan/active/refresh-wave-2026-09-14/brief-640.md` / `brief-643.md` and `.../reports/640-review-closure.md` + `643-final.md` — for what a reviewer later demanded: **behavioural** cells rather than `prosrc` assertions, re-measured rather than copied evidence, an explicit merge-order collision table, and every claim carrying the command that produced it with its pass/fail counts.

## Verifier findings not applied

**None.** All five findings of the 2026-09-19 verifier pass on `brief-651.DRAFT.md` were opened against their cited evidence and **applied**; nothing was refuted back. What changed, and where:

- **F1 (major, `contradicts_decisions`) — applied in full.** The M1-red fallback now mints the NEW verb `revise_fixed_asset_particulars_classified(…, p_change_class text, p_change_reason text)` with the classification as genuine separate SQL arguments, leaves `_fa_validate_particulars`' closed key set and both completion bodies untouched, and makes the 5-arg `revise_fixed_asset_particulars` a refusal-only door (`fa_change_class_required`, remedy = the new verb). The draft's jsonb-stripping variant is withdrawn **by name** so it cannot be re-derived. Verified against DECISIONS.md:208-210 and SYNTHESIS.md:725. Path B's real price is now priced in the brief (§3 item 1): five call-site moves incl. `x41-fa-fixtures.mjs:264`, and the two exact-set censuses `x42-reservation-authority.test.mjs:364-376` / `:388-392` plus the grant census `x41b0-surface.test.mjs:306-316`.
- **F2 (minor, `contradicts_decisions`) — applied.** The `_fa_period_open` predicate is **deleted from the roster**: 0227 installs exactly the one new ungranted helper DECISIONS §2.1 names (`_fa_assert_period_open(uuid,date)`). The `stable` oracle problem the split existed to solve (`0041:1904-1905` + `:1915-1916`) is solved instead by splicing the **same fiscal-year predicate inline** into `_fa_oldest_unmet_period`, bound to the assert by tail assertion (n), with the `begin/exception` variant refused by the oracle's own comment. The naming trap is recorded: `_fa_fy_open_for` (`0041:1057`) and `_fa_fy_month_open_for` (`0042:6027`) already mean something else.
- **F3 (minor, `contradicts_decisions`) — applied as a flagged deviation, and escalated.** The backfill line is now labelled a deviation from DECISIONS §2.1's literal SQL, named *0227 backfill timezone* for the report, and grounded in three measured facts (`0041:623` `signed_at timestamptz`; `0041:1012-1013` `_fa_today()`; `0041:1016-1017` `_fa_month_start`). M6 now requires BOTH candidate stamps to be measured per row with `show timezone` printed beside them. **Because it is a one-time, unrepeatable stamp, ratification is requested of the orchestrator** — see the JSON's `orchestrator_attention`.
- **F4 (minor, `other`) — applied.** The fifth-tab decision is relabelled a **brief ruling adopting gap-651's own Q7 recommendation**, with the grep result (`DECISIONS.md` has no "Q7" / "fifth tab" / "revisions tab") stated in the line itself, and its reversal cost named. It no longer borrows DECISIONS' `[owner-overridable]` device.
- **F5 (minor, `omits_gap_row`) — applied, and the finding's suspicion is CONFIRMED by source.** There **is** a reason `_fa_asset_charges` never returns: `_fa_compute_charges` writes `disposal_draft_outstanding` itself at `0041:1862-1868`. §1 is corrected (five names, not four; one writer, `0041:2320-2324`), M7 `p651.measure.skip_vocabulary` is added, the preview/runs-panel sentence map now carries all five **and degrades an unmapped reason to its verbatim code**, `p651.preview.matches_run` must build a `disposal_draft_outstanding` asset, and the web cell proves the degrade.

Carried in from gap-651's own reconciliation, already resolved: its design-lens finding **F2** (the CLR37 refusal should name **#680**, not #676) was listed there as "conflicts with DECISIONS — NOT APPLIED" against an earlier D10; the current `DECISIONS.md` D10 records the correction and names #680, so it is **applied**, and this brief's §3 item 1 and AC1 row supersede gap-651's slice text on that one point.

**Two lines the orchestrator is asked to rule on** (neither blocks the first day's work — M1 through M7 come first): the *0227 backfill timezone* deviation above, and — **only if M1 reds** — confirmation that DECISIONS §6's NEW-verb fallback is intended to leave `revise_fixed_asset_particulars` as a refusal-only door, since that is the only reading under which AC1 holds while the validator stays untouched.
