# #651 — final report

**Branch** `impl/651-assets-depreciation` · **worktree** `C:\Users\zhant\Desktop\clara-wt\651` · **rig** 127.0.0.1:55704 / `clara_651` (220 migrations) · **HEAD** `9d96ad45` · 11 commits off `origin/main` (`abcc5030`):
`b4b3f62e` 0227 · `7019d085` db battery/gate/cohort · `db17235e` runtime carrier + contract · `10187fe3` web surfaces · `27f81491` docs · `7e540281` walk + mock · `907d6e4c` three gate/browser defects · `93579e4c` seven whole-suite reds · **`2aa00f24` fix round 1 (db)** · **`1abf48db` fix round 1 (web)** · **`9d96ad45` the decision key dies with its dialog**.

**Fix round 1** answers the three reviews (spec accept · standards accept · adversarial fix_then_accept, 10 findings). The finding-by-finding record, with each new cell's RED output, is `reports/651-fixround-1.md`; every claim it moved is moved here.

## Per acceptance criterion / historical row

| Row | Verdict | Evidence |
|---|---|---|
| **AC1** classification | **done** | `0227` §A/§D (`change_class`/`change_reason`, one-directional CHECK, stamp on the SUCCESSOR row); `p651.class.required`, `.policy_refused`, `.error_refused`, `.prior_untouched`, `.completion_refused`; walk leg 3; `fa-row-actions.test.tsx` |
| **AC2** locked-period wall | **done** (accepted-treatment **descoped**, D9) | `_fa_assert_period_open` spliced into the poster; `p651.period.closed_refused`, `.closed_belt_skips`, `.blocked_draft_recovery`; walk leg 2 (refused, **no run row created**) |
| **AC3** re-read at commit | **verify-only + two holes closed** | `p651.class.prior_untouched`, `p651.period.blocked_draft_recovery` (the withdraw leg no battery held before); AC7 names the **belt**, not Workflow |
| **AC4** four groupings | **done** (shared-question half = named residual) | fifth `revisions` tab + `?tab=`; `fa-revision-timeline`, `fa-charge-ledger`, `fa-detail-tab-url`, `fa-runs-panel` unit files; walk leg 3 |
| **AC5** instruction, window, preview, OBO, Clara | **done except (ii) descoped (D7/I1) and (iv) contract-only** | `sign_depreciation_authority(uuid,uuid,text,jsonb)` DROP+CREATE with 0193's ladder; `p651.authority.ref_resolves`, `.floor`, `.floor_sequencing`, `.floor_frozen`, `.retire_unsigned`, `p651.preview.matches_run`, `.grants`, `p651.obo.run_for`, `.manual_still_human`; successor stanza below. **Fix round 1** added §B.2 (the write-once wall on both sign-time columns — an allowlist is not a freeze) and §B.3 (the retire door stamps the window floor, or this file's own CHECK turns the withdrawal of a never-signed authority into a raw 23514). What the instruction ladder proves is now stated exactly in §F: PROVENANCE, not authorship — a named residual, not a claim |
| **AC6** every state | **done** | four browser legs **4 passed** (focus return, 320px, 200%, axe under reduced motion, `?tab=` + Back); `fa-run-preview.test.tsx` (five measured skip reasons + unknown→verbatim code) |
| **AC7** production doors, real roles | **done** | 19 cells through `humanQuery` least-privileged personas, `rootQuery` only as readback or labelled fixture DML (the one exception is `p651.authority.floor_frozen`'s attempt to move a frozen column, which is a TRIGGER law no audited verb can attempt — `accounting-plans.test.mjs:450-456`'s own instrument on the same law); `p651.census.replay`. **Depreciation invokes NO Workflow** — `reconciler-fa.mjs:59-61`; the durability evidence in its place is `reconcile-fa.test.mjs` 1/1 against the rig under the `clara_runtime` group role |
| **C86.2** | **verify-only, re-derived** | every pin measured off `pg_proc.prosrc` on `clara_651`; `p651.obo.run_for` asserts all **three** writer variants identical in one assertion |

**M1 measured GREEN** — the validator's key set is widened and the frozen mirror is unmoved: `fixed-asset-acquisition.test.mjs` **21/21**, `fixed-asset-acquisition-unit.test.mjs` **9/9**, `check-frozen-workflows.mjs` OK (296 frozen files). So `revise_fixed_asset_particulars` keeps its 5-arg signature and no classified verb exists.

**0227 backfill timezone** — written as DECISIONS §6.1 ratified. Measured on the rig: **0 rows differ** between the literal `date_trunc('month', signed_at)::date` and the MYT composition under `Asia/Kuala_Lumpur`, `UTC` and `America/Los_Angeles` sessions. **A row COUNT on this rig is a timestamp, not a claim** (spec review N1): the migration header recorded 157 signed rows at authoring time, this report 285, the review 413, the fix round **541** — a persistent, never-reset rig accumulates fixture rows from every battery anyone runs, the reviewer's included. What is stable and re-measurable is the *divergence*, which is zero, and the invariant `status in ('live','retired') and authority_from is null`, which reads **0** and now stays true by door rather than by luck (fix round §B.3). Hosted count: **hosted evidence pending**, never assumed zero.

## Tests and commands

- `packages/db/tests/depreciation-history.test.mjs` (NEW, **19** `p651.*` cells after fix round 1) + `-fixtures.mjs` + `-preintegration-gate.mjs` (stem `_depreciation_history$`). Gate-preloaded, after the fix round: **19 pass / 0 fail / 0 skip**.
- `operation-census.test.mjs` **10/10**; `rig-isolation.test.mjs` **20 pass / 0 fail / 1 skip** (the destructive cell; reset flags never set). Both re-run after fix round 1.
- x41 family, re-run after fix round 1 over every `tests/x41*.test.mjs` (20 files): **105 tests, 103 pass, 1 fail, 1 skip** — the skip is the destructive 0041 upgrade drill; the fail is `x41.s4` (follow-up 1, now with a dated attribution). `x41-wave-d-a-fa.test.mjs` alone **13/13**.
- Runtime, re-run after fix round 1: `depreciation-run-unit.test.mjs` + `reconcile-fa-unit.test.mjs` + `reconcile-fa.test.mjs` in one run → **26 pass / 0 fail** (7 + 18 + 1, the last against the rig under the `clara_runtime` group role).
- **0227 re-proved against the committed catalog after the fix round**: its whole tail (`$p651_tail$`, including the new `T.11` assertions) re-run inside a rolled-back transaction → **green**; its prestate re-run the same way → refuses as designed (`CLR10 … _fa_assert_period_open already exists`); every prestate pin re-read → the **ten** `recut` pins MOVED, the three `unmoved` pins AT PIN, the 3-arg `sign_depreciation_authority` ABSENT. That is evidence about the catalog, **not** evidence that the file applies (see Unverified).
- `check-frozen-workflows.mjs` OK (296 frozen files, 53 "use workflow" modules, 3 retired) · `check-parts-parity.mjs` OK, no new part kind. Both no-ops — the evidence for "no successor on this branch".
- Playwright: `CLARA_E2E_APP_ORIGIN=https://127.0.0.1:3330 CLARA_E2E_NEXT_PORT=3331 CLARA_E2E_RUNTIME_PORT=3332 pnpm --filter @clara/web e2e depreciation-walk` → **4 passed (31.8s)**; re-run after fix round 1 (which changed all four FA dialogs' confirm and close paths) → **4 passed (18.2s)**, and again at `9d96ad45` → **4 passed (14.0s)**.
- **Whole `apps/web` unit suite** (`node scripts/run-tests.mjs`), re-run after fix round 1: **4168 tests · 4165 pass · 1 fail · 2 skip**. Both skips are the live-Supabase-auth cells, unrelated. The one fail is `lib/clara/use-clara-thread-stop.test.ts` → "630 a stop the door says had ALREADY FINISHED does not re-attach" (`3 !== 0`) — **a whole-suite load flake in a lane this branch never touches**: `git log origin/main..HEAD -- apps/web/lib/clara/` is **empty**, and the file re-run in isolation is **25 pass / 0 fail**. Same family as the `thread-live-clarify.test.tsx` flake WORK-ORDER rule 9 names; both counts reported, as that rule asks.
  - The first fix-round run of this suite was **4168 · 4164 · 2 fail · 2 skip**; the two fails were `tests/firm-scope-db-pins.test.ts`'s reviewed dynamic-SQL barrier, whose pinned sha256 of `0227_depreciation_history.sql` had moved because the fix round edits that file. The pin and its reason are updated (NINE named functions across TEN splice blocks now, the trigger twice and the retire door added), and that census re-runs **22 pass / 0 fail**. It is exactly the kind of census WORK-ORDER rule 3 warns reds on files you never touched.
- `pnpm typecheck` **exit 0** · `pnpm lint` **exit 0** (worktree root), both re-run after fix round 1.

## Docs

`CONTEXT.md` — *Depreciation change class*, *Depreciation authority window* (ratified) + *Depreciation run preview* (**a refinement, not a ratified term**). `packages/db/README.md` (the lane, its grants, the measurement that re-aimed it, two named residuals), `packages/db/tests/README.md` (battery, gate, the three traps), `packages/runtime/README.md` (carrier, freeze trajectory, why the belt needed no change), `apps/web/README.md` (the surfaces). PRD/ARCHITECTURE untouched.

**Fix round 1 added**: `packages/db/README.md` — the recut retire door and why, and that an allowlist is not a freeze; `packages/db/tests/README.md` — nineteen cells, the two new laws, and a FOURTH trap (a fixture that turns a trigger off does it in one transaction, `withTriggerOff`); `apps/web/README.md` — "one decision, one key" on every FA door, the three intent tuples, and the two doors that deliberately still mint their own key; `apps/web/tests/firm-scope-db-pins.corpus.ts` — the reviewed dynamic-SQL barrier for 0227 re-reviewed (NINE named functions across TEN splice blocks) and its sha256 re-pinned.

**Blueprint drift** (recorded, never edited): (1) `docs/PRD.md:109` lists 固定资产与调整 under 当前版本已交付 while the classification and the locked-period law were gaps inside it. (2) `docs/ARCHITECTURE.md §5 D` states the plan lane's law (explicit resolvable instruction, no back-fill) and the depreciation lane met neither before 0227 while being the estate's strongest unattended poster — the blueprint never exempts it, it simply does not mention it. (3) `:183`/`:293` pin `chatTurn → v19` / `claraWork → v3` while `registry.ts:173`/`:270` pin **v20/v4**. (4) NEW: `fa.keys`' prose claim ("exactly the nine `_fa_validate_particulars` accepts") is now stale — the frozen `FA_PARTICULARS_KEYS` is a strict SUBSET of the DB's admitted set. The assertion stays true and was not weakened.

## Successor contract (verbatim)

> **Body**: `chatTurn_v21`. **Tool name**: `run_depreciation_period_for_client`.
> **Zod input** (`.strict()`): `{ client_id: z.string().uuid(), through: z.string().regex(/^\d{4}-\d{2}-\d{2}$/).optional() }`. **No period start/end — the period is the database's** (`0041:3457-3470` refuses any caller-named window that is not the cadence's). `through` bounds a catch-up and defaults to the current book day.
> **Carrier module**: `packages/runtime/lib/depreciation-run.ts` (NEW, non-frozen at authoring time, freezes on import).
> **Door call, exact argument order**: `select clara.run_depreciation_period_for($1::uuid, $2::date, $3::text, $4::uuid) as r` — `(p_client, p_through, p_op_key, p_obo)`, granted to `clara_runtime` **only**, with live-authority recheck of the named human (active member of the client's firm, bookkeeper floor, active client). **A NEW verb name is mandatory, not stylistic**: `rig-meta.mjs:691-693` is an executable census that fails the moment `run_depreciation_manual` reaches a machine role.
> **Refusal mapping**: CLR38 `authority_not_live` → "This client has no signed depreciation authority. An admin signs one before Clara can run depreciation." · CLR38 `period_draft_outstanding` → "A depreciation draft is already waiting for approval; approve or withdraw it first." · CLR38 `period_earlier_unmet` → name both dates · CLR38 `period_request_invalid` axis `not_ended` → "That period has not ended yet." · CLR38 `period_request_invalid` axis `period_closed` → name the fiscal year and the reopen path · CLR19 `write_into_closed_period` → name the fiscal year and the reopen path · CLR04 `obo_not_active` / `insufficient_role` → "You no longer hold the authority to run this."
> **Floor**: the door clears due periods from `authority_from` forward up to `p_through` and **carries no floor bypass**, so Clara cannot reach a pre-floor period; the sentence map points the person at the human door `run_depreciation_manual`.
> **Part kind**: reuse the existing `work_accepted`-adjacent receipt part shape. **No new part kind and NO `accounting_work.purpose` widening** — depreciation posts through `journal_entries` directly and never reaches the Work lane (`0195:1711` / live `0204:180` stay closed).
> **Prompt stanza**: "You execute an authority; you never sign one. The period is the database's, never yours. Say you have QUEUED/POSTED exactly what the receipt says, including how many assets were skipped and why."
> **Not folded here**: #933 rides `claraWork_v5`, a different closure — do not fold the two. #882(a)'s CLR40 stanza is **#658's worker's** to write.

## Assumptions

1. **The M1-red arm was never entered**, so DECISIONS §6.1's "thin delegate" correction is moot on this branch and `revise_fixed_asset_particulars_classified` does not exist. The exact-set censuses gained one name per new *granted* door and lost none.
2. **§3 item 2(a) took the "accept and pin" arm**: `period_earlier_unmet` stops binding below the floor for a caller-named period, and `p651.authority.floor_sequencing` pins the exposure rather than re-deriving the bound unfloored. The only instruments that could re-derive it are the 1-arg oracle (consumer set `deepEqual`-pinned by two live CI batteries) or a bypass parameter on it (a DROP+CREATE against §2.2 rule 4). **Fix round 1 replaced that cell's tautology with the measurement** (adversarial ADV-651-4/5): the out-of-order pre-floor run **is admitted**, and it is a **noop** — no entry, charge set byte-identical, run rows unchanged, because `clara._fa_asset_charges` already charged every uncharged month up to the later period's end. The exposure is real and the damage today is nil; the cell now reds if either half changes, and its non-vacuity was proved by mutation rather than asserted.
3. **No `REVIEW_QUEUE_ROW_KINDS` kind and no `needs-you.ts` literal.** A blocked depreciation draft has no Needs-you row — named residual, follow-up 3.
4. **`apps/web/e2e/fixed-asset-mock.mjs` was edited** (a file no ticket in this wave owns): its private body reader became the shared `readCachedJson`. Two lanes sharing five verbs cannot both read a drained stream, and the exact-verb guard (N7's law) protects only verbs *neither* lane owns. The fix is the root one and dispatch-order-independent.
5. **The rig's migration ledger checksum for 0227 has been repaired in place three times now** — once when §C's splice was unrolled, and twice more in fix round 1 (`5ac4180c…` → `a25fbc0e…` → `d55113b5…`, the runner's own CRLF-normalised recipe). **0227 has therefore NEVER applied end to end from a clean 0224 on any cluster** (adversarial ADV-651-3, accepted in full): the rig's catalog was produced by earlier drafts plus hand-applied blocks, and §J, the tail census, the ACL proofs and every battery cell are evidence about a hand-assembled catalog rather than about this file. What was done instead is stated under "Tests and commands" — the new blocks were executed as their own transactions and their post-images verified, the whole tail and the prestate were re-run in rolled-back transactions, and every pin was re-read. **The integrator's from-scratch `0001 → 0233` chain is this branch's gate**, and this cluster may not run one (brief + RIG.md; 0154's cluster-wide role-count pin).
6. **Two bodies outside the brief's RECUTS roster are recut by 0227** after fix round 1 — `clara.retire_depreciation_authority` (§B.3) and a second splice of `clara._tf_fa_authority_transition` (§B.2). Both repair damage this file would otherwise do; both carry the full apparatus (measured pre-image pin, anchor count, postcheck, tail ACL/owner row, name-purity entry). **Flagged for ratification**, with the alternative for §B.3 — weakening the brief's verbatim `ck_fa_authorities_window` to admit a floor-less `retired` row — recorded and rejected in `651-fixround-1.md`.
7. **The instruction ladder proves PROVENANCE, not authorship** — a named residual rather than a narrowing. `0193:1500-1514`, the ladder the brief binds this door to, is byte-for-byte the same existence test for accounting plans; narrowing the FA lane alone would give a firm two meanings for one word, and D7 forbids touching the plan lane here. 0227 §F now says exactly this in the live comment (adversarial ADV-651-2); the narrowing needs a cross-lane ruling.

## Follow-ups worth filing

1. **`x41.s4` reds on a persistent rig because x41.b3's fixture leaves a retroactively-born register row — and it is NOT this branch's, with a date to prove it.** Every `x41_b3_*` client carries one `clara.fixed_assets` row of 77,000 citing the PRE-ENROLMENT entry (posting 2026-06-07), created ~100 ms after the client — i.e. born when `reverse_entry`'s `reversed_by` UPDATE re-fired the acquisition belt, which is exactly what x41.b3's own comment says the watermark exists to prevent. x41.b3 never looks again after the reversal, so it stays green; the family sweep sees it at every as-of. **The attribution is now dated rather than argued** (adversarial ADV-651-9): `0227_depreciation_history` was applied at **2026-09-18T19:50:37.880Z**, while the two oldest offending rows were born at **19:20:49.192Z** and **19:22:04.028Z** — thirty minutes BEFORE the migration. The client count is not stable and should not be quoted as one (five at first report, six at review, **seven** at fix round); the mechanism and the dates are.
2. **Fold the duplicated leg aggregation.** `preview_depreciation_run` duplicates `_fa_run_period_core`'s aggregation (SYNTHESIS J3's ruling). Bound two ways today — 0227's tail (T.13) and `p651.preview.matches_run` — but it is duplication with a maintenance cost, and the fold belongs in its own ticket with its own review.
3. **A blocked depreciation queue has no Needs-you row.** An outstanding draft blocks the client's whole depreciation lane and nothing surfaces it at firm altitude; the wave's rule forbids a new review-queue kind, so this needs a decision rather than a patch.
4. **"Move this period into the next open one"** (AC2's accepted-treatment clause) is not built — D9. Owner question: is a locked period's charge ever *moved*, or only ever charged as arrears by the next open period's run?
5. **The two completion bodies keep their own copies of the same wall.** Folding `complete_fixed_asset_particulars` into `_fa_complete_particulars_core` is worth doing once the validator question is settled; explicitly out of scope here.
6. **A signed instruction reference proves provenance, not authorship — in BOTH lanes.** `sign_depreciation_authority` and `create_accounting_plan` resolve `authority_ref` by existence in the same firm and client, reading neither `kind` nor `status` nor author, so a machine-born `clara.agent_tasks` row nobody typed satisfies either door. Narrowing it is one cross-lane decision (what counts as an instruction a *person* gave), not two lane-local patches; tightening #651 alone would leave a firm with two meanings for one word. Adversarial ADV-651-2; 0227 §F states the position in the live comment.
7. **Two FA doors still mint their own operation key.** `completeFixedAssetParticulars` and `disposeFixedAsset` (`lib/registers/fixed-assets.ts`) keep #639's `crypto.randomUUID()`-inside-the-wrapper shape, so a retry after a lost response is a new operation there. Fix round 1 threaded the decision key through the four doors the review named (propose / sign / retire / revise) and deliberately stopped at the branch's own scope. The same defect D15 rules for the bank lane.
8. **`clara.get_depreciation_authority` never surfaces a retired authority, so an admin cannot read what was retired or why.** After a withdrawal the read returns `authority: null` and the surface renders the same "none proposed" state a fresh client shows — correct as far as it goes, but the retirement reason, its author and the window it carried are now only in the audit trail. Worth a decision once #651's history surfaces have a reader.

**Owner questions** (not follow-ups — rulings, not work):

- **D9's, unchanged:** is a locked period's charge ever *moved* into the next open period, or only ever charged as arrears by that period's run?
- **NEW (adversarial ADV-651-10): is a mis-cited instruction correctable?** `sign_depreciation_authority`'s replay identity is `{client, authority}` and deliberately excludes `p_authority_ref`, so a retry carrying a CORRECTED reference returns the first receipt and the wrong instruction stays on the row forever; the reference is write-once by the door and by the trigger (fix round §B.2), and the signature cannot be re-taken. The only remedy is retire → propose → sign — **which fix round §B.3 has now restored for the never-signed arm as well**, so the remedy exists. Whether it is the right remedy, or whether a mis-citation should be correctable in place, is the owner's call.

## Unverified

- **All hosted evidence.** Nothing here is hosted; the hosted `fa_depreciation_authorities` row count is a release-time read and is **never assumed zero**.
- **That 0227 APPLIES.** The committed file has never run end to end from a clean 0224 on any cluster, and fix round 1 made that more true, not less: §B.2, §B.3 and §F's re-installed body were applied to the rig as their own transactions and the ledger checksum was repaired again. Everything §J asserts was re-proved against the catalog *as it stands* (the whole tail re-run green in a rolled-back transaction; the prestate re-run and refusing as designed; every pin re-read), and none of that is an apply. **The integrator's from-scratch `0001 → 0233` chain is the gate for this branch specifically** — `220 migrations, 0 new` is not evidence, and neither is §J. A clean `0001 → 0227` run on a second cluster would close it cheaply if one is free before merge; this cluster may not (RIG.md; 0154's cluster-wide role-count pin). Adversarial ADV-651-3, accepted in full.
- **The base-state colour of `x41.s4`.** A from-scratch base chain is forbidden here, so I still cannot show the cell red at `abcc5030`. Its attribution no longer rests on that: follow-up 1 now carries the timestamps (the offending rows are thirty minutes older than the migration's `applied_at`), which is what the reviewer asked for so the integrator does not spend the from-scratch run re-litigating it.
- **The db estate suite, the runtime suite and the whole browser suite** — the orchestrator's/CI's job, per WORK-ORDER rule 3.
- **A focused run of the battery below 0227.** Measured by an earlier pass (ledger row temporarily renamed: focused FAILS loudly on every cell, gate-preloaded SKIPS every cell); not re-measured since, because reproducing it means breaking the rig's own ledger again — and the fix round has already had to repair that ledger twice.
- **A second reviewer on fix round 1 itself.** The two new cells are red-first and the strengthened one is mutation-proved, but nothing has reviewed the fix round's own two recuts except the batteries and the tail.
