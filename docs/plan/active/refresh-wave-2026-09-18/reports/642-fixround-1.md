# #642 — fix round 1

**Branch** `impl/642-chat-stream-admission` · **worktree** `C:\Users\zhant\Desktop\clara-wt\642` · **from** `46a84750` · **HEAD** `566665f0` · 9 commits.
`23ead783 859950d6 bb10d532 40e4b2a0 debb156a f9155ed1 1209dc93 772ad7c9 566665f0`

**NO MIGRATION, again and on purpose.** This round adds no `.sql` file, no `rig-meta` cohort, no gate module, no `packages/db/package.json` row and no `action.yml` row; `git diff --name-only 46a84750..HEAD` touches nothing under `packages/db/` or `packages/runtime/src/` at all. DECISIONS §2 says #642 ships no migration, the door's idempotency arm is already in `0006_runtime_core.sql:954-960`, and every finding below is above the database.

Fifteen findings across three lenses (spec 2, STANDARDS 3, adversarial 10) — **thirteen distinct**, since STANDARDS F1 is ADV-642-1 and STANDARDS F2 is ADV-642-2. **Ten applied red-first with a cell; one applied as a comment correction** (spec F2, which no cell can hold); **one was already resolved** before this round; **one needs no action**. Two items are listed for ratification, and both were APPLIED rather than parked: leaving a silent message loss — or "Reconnecting…" over a removed membership — in place awaiting a ruling would be the worse trade. **Nothing was deliberately left.**

## Finding → what I did → evidence

| # | Finding | What I did | Evidence |
|---|---|---|---|
| **ADV-642-1 / STANDARDS F1** (blocker) | the content-only address made every REPEATED utterance collide with the first one, permanently | the address gains the conversation's POSITION (`transcriptPosition`: the count and last id of the persisted rows the composer has seen) | `23ead783`. Red: `p642.web.repeated_utterance_is_a_NEW_intent` measured `intent-419ab7aa…` twice and the second "yes" swallowed. Green after — and `p642.web.intent_key_survives_a_refusal`, `…_and_changed_attachments`, `distinct_resubmit_reads_state_first` and `p642.e2e.duplicate_send` are untouched and green, which is the control: in every one of them the transcript has not advanced |
| **ADV-642-2 / STANDARDS F2** (major) | the replayed and pre-read lines sat outside the exclusive `role="status"` ladder and announced beside it | both keep their WORDS in every state and carry `role="status"` only when nothing else is speaking (`StateBanner`'s own `silent` decision, #629 §5) | `859950d6`. Red: `["Clara already had that message…","Clara is responding…"]`, 2 !== 1. Cell `p642.web.replayed_says_it_once` |
| **ADV-642-3** | `lastSendReplayed` was cleared by nothing but the next `beginSend`, over a module-level store | it retires on the terminal `message` and on `revoked` — the same authorities that retire the turn clock beside it | `859950d6`, cell `p642.web.replayed_says_it_once — the line retires with the turn it is about` |
| **spec F1** (major) | the smooth jump's landing correction raced its own trailing scroll event; the browser measured the transcript stranded 36px short | the PENDING correction (not a clock) is the window; the correction re-measures the element; an arrival cancels it; bounded retry ×3 | `bb10d532`, `1209dc93`. Red: the trailing-event cell stranded at 804 of 1240, with a control cell proving a reader who scrolls away after the jump lands is not dragged back. Two chat-parity walks since: **11/11 (46.8s)** and **11/11 (58.6s)**, that leg 4.9s and 7.0s |
| **ADV-642-7** | the revision was a SUM, so two opposite changes in one commit cancelled and the append effect did not run | `transcriptRevisionToken` — a join of the five counts | `bb10d532`, cell in `useTranscriptScroll.test.ts`; the render-budget cell stays 25 renders for 25 appends |
| **ADV-642-5** | a revocation discovered at ATTACH came back as 403/404 and was read as a flaky transport — up to eight rounds of "Reconnecting…" | 403 and 404 deliver the same `revoked` event the mid-stream arm delivers; 401 stays a transport failure (it is about the token, not a membership) | `40e4b2a0`. Red: the attach cells rejected instead of announcing; then the view cell measured the composer stuck in `sending`. Cells in `stream-revoked.test.ts` (+ a transport vacuity control), `thread-revoked.test.tsx`, `use-clara-thread-stop.test.ts`, `thread-stop-reply.test.tsx` |
| **ADV-642-4** | the pre-read was gated on a per-MOUNT ref while the `sendStatus` it pairs with is module-level | the last posted key moves to the thread's store row; an unattributable outcome now takes the pre-read | `23ead783`, cell `p642.web.distinct_resubmit_reads_state_first — a REMOUNT does not lose the pre-read` (red at transcript reads 2 → 2) |
| **ADV-642-6** | `aria-invalid` with no accessible reason; `FieldError` unused | the field's `aria-describedby` names the error banner while invalid — the banner is that sentence's ONE home, and a `FieldError` would be a second `role="alert"` copy of it | `debb156a`, cell `an invalid composer SAYS WHY` (red at `aria-describedby="clara-composer-hint"` alone) |
| **ADV-642-9** | two document-global literal ids on a component mounted from two places | `useId` | `debb156a`, cell `the composer's ids are per-INSTANCE`, which mounts both variants at once |
| **ADV-642-8** | `done`/`failed`/`refused` all ranked 2, so a step that returned and then threw kept reading *done* | `failed` outranks the other two terminals; `done`/`refused` stay level (two readings of one chunk); monotonicity kept for the index-0 replay | `debb156a`, cell `a tool-error AFTER a tool-result reads *failed*` with the replay control |
| **ADV-642-5** (knock-on) | three landed cells encoded "the lane cannot serve this stream" as a 404 — the one status that now means a REFUSAL | each keeps every assertion with its fixture re-encoded to 502 (`runtime_unreachable`, what the web proxy actually answers for that premise), and each gains a sibling cell for the refusal arm | `40e4b2a0` (`use-clara-thread-stop.test.ts`, `thread-stop-reply.test.tsx`), `772ad7c9` (`tests/streamReattach.test.mjs`, found by the whole-suite run — the only run that covers that file). Listed under ratification below |
| **spec F2** | `toolLabel.ts:16` named `clara-tool-labels.test.ts`, a file that has never existed | corrected to `toolLabel.test.ts`, with the correction named in the comment | `debb156a`; `git grep clara-tool-labels` now returns only that sentence |
| **STANDARDS F3** | an untracked adversarial scratch file in the worktree | already gone before this round started | `git status` at entry: "nothing to commit, working tree clean"; at exit: clean |
| **ADV-642-10** | scope / no-migration / no-cut re-verified, nothing to fix | no action | recorded so the next reader does not re-litigate it |

## Deliberately left

**Nothing.** Every finding is applied, or was already resolved before this round began.

## Ratification requested (both applied, neither left)

1. **The intent key's address gains a field the brief's literal list does not name.** Brief §3 item 2 specifies `deriveIntentKey({threadId, altitude, draft, attachments})` — "a stable hash over `threadId ‖ altitude ‖ draftText ‖ sorted(attachment.document_id)`". It now also carries the conversation's position. Every property the brief gives a reason for is preserved: *(a)* a retry reuses the key, because a refused or lost send adds nothing to the persisted transcript — which is exactly when a retry is owed; *(b)* a changed intent, including a changed attachment set, still derives a new key; *(c)* the distinct-resubmit pre-read is unchanged; *(d)* `replayed:true` still says so once. The brief's §3 aside — "the same sentence with the same files retyped after a reload derives the same key" — still holds where it matters: a send that was never admitted leaves the transcript exactly as it was. Where it no longer holds is where the admission SUCCEEDED, in which case the reader can see their own message in the re-read transcript and a re-typed sentence is a new instruction, not a retry. The alternative the brief explicitly permits (a session-scoped or handed-out key) is costed there as owing a "your attachment change was not resent" path; this is strictly less.
2. **Three landed cells had their FIXTURE re-encoded off 404, to two different statuses.** `use-clara-thread-stop.test.ts`'s *"a refused stop whose re-attach FAILS keeps the live buffer"* and `thread-stop-reply.test.tsx`'s *"a refusal whose re-attach CANNOT open never claims this tab is reading again"* all encoded "the lane cannot serve this stream" as a 404. Under ADV-642-5's fix a 404 on that route is `assertTaskStreamAccess`'s masked-view refusal, so it now produces a `revoked` — which is exclusive with the stop-refusal line and drops the live buffer with the access it would have answered. Those two keep every assertion; only the status changes, to the one the web proxy actually answers for their own stated premise (`app/api/runtime/[...path]/route.ts` → 502 `runtime_unreachable`). `tests/streamReattach.test.mjs`'s *"a non-ok response throws with the status in the message"* is a different abstraction level — a unit test of `runClaraTaskStream` against a raw `fetchImpl` mock, with no web proxy anywhere in its call path — so the proxy's specific 502 `runtime_unreachable` semantics do not apply there; its fixture moves to **500**, a generic non-40x transport-failure status, stated as such in `772ad7c9`'s own commit message. All three keep every assertion; only the fixture status changes, each to the one that actually reaches its own call path. Each gained a sibling cell for the refusal arm, so no file can be read as saying every non-ok attach rejects. If the wave's owner would rather a 404 stayed a transport failure on the stop path, the narrowing is one line in `openTaskStream`.

   **ratified — DECISIONS §6.2.1**

## Gates re-run (all on this rig, this round)

| Command | Result |
|---|---|
| `pnpm typecheck` (worktree root) | **green**, exit 0 |
| `pnpm lint` (worktree root) | **green**, exit 0 (incl. the manifest, message-key, token-contrast and ui-add-guard selftests) |
| `node scripts/run-tests.mjs` (apps/web, whole suite) | **4210 · 4208 pass · 0 fail · 2 skip**, exit 0 (run 3; runs 1 and 2 below) |
| `pnpm --filter @clara/web e2e chat-parity` ×2 | **11/11 (46.8s)** and **11/11 (58.6s)**, exit 0 both |

**Three whole-suite runs, and what the first two measured.** Run 1 (started minutes after two Playwright/`next build` runs) reported **4141 · 4125 · 14 fail**: thirteen of those were whole FILES crashing the node process — `exitCode` 3221225773 (`STATUS_COMMIT_LIMIT`, i.e. the host ran out of commit charge), 3221226505, 134 — in `components/registers/*`, `components/work/*`, `e2e/e2e-fixture-ownership.test.ts` and `lib/bank/*`, none of which this branch touches; re-run in isolation they are **84 tests, 84 pass, 0 fail**. The fourteenth was real and is fixed in `772ad7c9`. Run 2 reported **4210 · 4207 · 1 fail**: `use-clara-thread-stop.test.ts`'s *"a stop the door says had ALREADY FINISHED does not re-attach"*, which counts stream opens and saw 3 leak in from earlier cells — the cross-cell leak that file's own preceding cell documents by name ("*Measured: the 'already finished does not re-attach' cell below failed exactly that way under a loaded full-suite run while passing 5/5 in isolation*"), and the final report's follow-up #3. That file is **26/26 in isolation** (three times this round), and run 3 is clean. Nothing this round changed opens a stream: the refusal arm ends a read earlier than before, and the two `finally` arms only write state.

No new test FILE was added, so `apps/web/test/manifest.txt` is unchanged — every cell above extends a file this branch already owns. `packages/db` and `packages/runtime/src` are untouched by this round, so the DB and runtime evidence the final report carries stands as measured there (`rig-runtime-lifecycle` 12/12, `chat-turn-replay-db` 2/2, `chat-turn-v20-e2e` PASS, 4 legs).

## Docs updated

`apps/web/README.md` — the status block's announcement rule and the replayed line's lifetime; the landing correction and the window that guards it; the join-not-sum revision; the intent key's position; the tool rank. `packages/runtime/README.md` — a revocation has two doors, and which statuses mean which. `CONTEXT.md` — **Turn key** gains "where in the conversation the instruction was given" and an _Avoid_ clause naming the defect. `642-final.md` — header, AC3, AC5, AC6/AC7 and the commands table. **No PRD/ARCHITECTURE edit.**

## Unverified

Hosted behaviour: everything here is local. Whether the AI SDK can emit `tool-error` after `tool-result` for one call id remains documented-not-measured (ADV-642-8 makes the safe arm the winning one either way). The `use-clara-thread-stop.test.ts` cell-22 flake the final report names is not attributable by inspection and is unchanged by this round.

## Fix round 2

Report-text-only pass (no branch, no code change): DECISIONS §6.2.1 ratifies this ticket's ADV-642-5 knock-on subject to one correction — "the fix-round report's 'all three → 502' sentence is corrected in fix round 2" — and DECISIONS §6.2.2's own Fix round 2 row for #642 reads verbatim "fix-round report sentence: two cells → 502, the unit cell → 500." This section is that correction.

| Finding | What I did | Evidence |
|---|---|---|
| Ratification item 2 (above) claimed uniformly that "Three landed cells had their FIXTURE re-encoded from 404 to 502." That is wrong for one of the three: `tests/streamReattach.test.mjs` (commit `772ad7c9`) was re-encoded to **500**, not 502 — that commit's own message says so ("Only the FIXTURE changes, to 500… 500 is the status that means that") because the cell drives `runClaraTaskStream` directly against a raw `fetchImpl` mock with no web-proxy route anywhere in its call path, so the proxy's specific 502 `runtime_unreachable` semantics do not reach it. Only `use-clara-thread-stop.test.ts` and `thread-stop-reply.test.tsx` — both re-encoded in `40e4b2a0`, both driving the actual `app/api/runtime/[...path]/route.ts` proxy — moved to 502. | Rewrote ratification item 2 above: it now separates the two 502 cells (the proxy-fronted pair) from the one 500 cell (the proxy-free unit test) and states each one's reason, instead of asserting one status for all three. Added a trailing line, `**ratified — DECISIONS §6.2.1**`, under the corrected paragraph. | `git show 772ad7c9 -- apps/web/tests/streamReattach.test.mjs` — the diff's own fixture line reads `fetchImpl: async () => new Response(null, { status: 500 })` (was `404`) and the assertion reads `/stream attach failed \(500\)/` (was `\(404\)`); the commit message states the 500 choice and reason in its own words. `grep -n "status: 502" apps/web/lib/clara/use-clara-thread-stop.test.ts` → line 1125; `grep -n "status: 502" apps/web/components/clara/thread-stop-reply.test.tsx` → line 515; `grep -n "status: 500" apps/web/tests/streamReattach.test.mjs` → line 142. DECISIONS.md §6.2.1's #642 row and §6.2.2's own Fix round 2 row for #642, both read above, state the correction this section makes. |

**Commands re-run this pass, worktree unchanged (`git status --short` clean before and after, HEAD still `566665f0`):**

| Command | Result |
|---|---|
| `node --import ./test/bootstrap.mjs --import tsx --test tests/streamReattach.test.mjs` (from `apps/web`) | **10/10 pass**, 0 fail |
| `node --import ./test/bootstrap.mjs --import tsx --test lib/clara/use-clara-thread-stop.test.ts` (from `apps/web`) | **26/26 pass**, 0 fail |
| `node --import ./test/bootstrap.mjs --import tsx --test components/clara/thread-stop-reply.test.tsx` (from `apps/web`) | **10/10 pass**, 0 fail |

All three cells' assertions are unchanged by this correction — this round edits only the prose in this report, not the tests. The three files' combined 46/46 is a subset of the recheck's `targeted_revocation_group` 58/58 (`642-recheck-1.json`, which also runs `stream-revoked.test.ts` and `thread-revoked.test.tsx` — untouched by this correction and not re-run here since neither carries the mis-stated sentence).

**Deliberately left:** nothing. The correction is applied in full and ratified per DECISIONS §6.2.1; no further action is owed on this finding.
