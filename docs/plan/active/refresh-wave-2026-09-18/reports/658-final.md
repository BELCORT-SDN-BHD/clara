# #658 — knowledge retrieval, the recorded read-set and the drift detector — final report

Branch `impl/658-knowledge-retrieval` · worktree `C:\Users\zhant\Desktop\clara-wt\658` · rig PG 55708 / `clara_658` · head **`141103cb`** · 14 commits (11 + fix round 1; see `658-fixround-1.md`).

```
141103cb fix(web): one fact, one read -- the drift reader coalesces the Work detail's duplicate calls
bd09e79b fix(runtime): the relay stops claiming a core-only failure the atomic door cannot produce
a6553036 fix(db): the drift scan applies the read's own shadow, and tiers / as_of / a replay stop being unwalled
33b99b83 fix(web): a ticket number is a hex colour to the lint, and the shared census keeps its wrapping
b85ce24e fix(web): restore the #644 conflict cell's fixture values and use the estate's reduced-motion probe
d74b1c76 fix(web): the e2e lane reaches its control endpoint through the app proxy, and a 403 is a DENIED face
2b972f3c fix(db,web): the 0220 reader census names the five 0230 reads, and the new web cells use the estate's attribute cast
c9606650 docs: CONTEXT terms, the 0230 door table, the three new runtime modules and the web faces
7466c4cb test(web,runtime): the Playwright lane, the walk extensions and the read-set resume leg
045ba626 feat(web): the C13 freshness line, the "Work that read this record" list and B3's knowledge block
1188ba04 feat(db): 0230's drift envelope carries what the last attempt READ
dc18c00b feat(runtime): three NEW non-frozen modules and their batteries (+ C34.3's three probe cells)
f000ea88 test(db): the knowledge-retrieval battery, its cohort gate and the 0230 rig-meta cohort
639ac4cd feat(db): 0230 — bounded core-first knowledge retrieval, the recorded read-set and the drift doors
```

## Per acceptance criterion / historical row

| Row | Verdict | Evidence |
|---|---|---|
| **AC1** bounded core-first, firm defaults by explicit scope | **partial → DB done, Work-lane behaviour closes at the v5 cut** | `0230:*` `retrieve_knowledge`; `p658.retrieve.core_first/.bounds/.legacy_unshadowed/.shadow_parity/.purpose_is_recorded_not_filtered`; stanza (A) |
| **AC2** versions per attempt; relevant revision replans | **partial → relation + drift done; replan closes at the cut**. Fix round 1 corrected what "relevant" means: the moved-key scan now applies the same per-applicability shadow the READ applied, so a firm default this client never read can no longer be reported as its news | `clara.work_knowledge_reads`; `p658.reads.append_only`, `p658.drift.relevant`, `.shadow`, `.trace_fallback` |
| **AC3** required-read failure → visible recoverable state, explicit status | **partial** — status vocabulary + faces done; the terminal is contract-only AND the contract is now honest: the door is ATOMIC, so D16 fires on ANY unavailable answer and there is no per-tier signal to design against (fix round 1, A1/F1/S1) | `p658.reads.status_vocabulary`, `p658.retrieve.envelope_is_atomic`; `faceStatusOf` (19 runtime cells); projection-lag half named residual → #663 |
| **AC4** conflicting sources; untrusted imports cannot change tools | **verify-only, re-measured** | `knowledge-records` 27/27, `clara-work-v4` 25/25. Residual re-stated: two independent SOURCES disagreeing has no representation (`0192:750-803`) |
| **AC5** C13 + Work expose source/applicability/freshness/historical basis | **done on this branch** (register freshness line, `in_effect:false` mark, seventh door + "Work that read this record", Work knowledge block) | `p658.record_reads.lists/.floor_and_tenancy/.firm_scope_shadow/.bounded/.no_values`, `p658.retrieve.period`; 5 web unit files; `knowledge-walk.spec.ts` 18/18 |
| **AC6** full state ladder / 320px / 200% / keyboard / SR / reduced motion / URL-Back / drafts | **done on the surfaces owned** | `work-knowledge-walk.spec.ts` 9/9 (incl. 320px, 200%, keyboard+URL, reduced motion), `knowledge-walk.spec.ts` +5 cells |
| **AC7** real least-privileged roles + real Postgres | **done** | whole battery via `humanQuery`/`roleQuery`; `work-knowledge-e2e.mjs` exit 0 |
| **C-36** versioned instructions/skills/tools | **partial carry** — schema-covering digest rides v5 (stanza A). OCR ordering / numeric invariants stay **#665's**; the row is not consumed twice |
| **C-84** SQL-boundary analyzer fixture | **verify-only, closed** — `node scripts/check-wiki-dynamic-sql.selftest.mjs` → `wiki-dynamic-sql selftest: OK`, exit 0. No new fixture owed |
| **C-87** Mobbin | **verify-only, partly consumed.** 12 frames inspected (below). Remainder outstanding |
| **C34.3** three `to_regprocedure` probes | **done** — 3 cells in `wave-b-wiki-projection-unit.test.mjs` (54/54) |
| **C55.6** unread source → parked question | tax specifics **descoped (authority)** `PRD:125`; general rule via D16's core tier — contract-only |
| **C55.17** unavailable/stale/partial states | **done** (the four words; `partial` reads as neither neighbour) |
| **C83.14** official-source access | **duplicate** — closes with C55.17 |

## Measure-first (re-measured on `clara_658` at head)

- **pins** — eight non-regression pre-image `sha256(prosrc)` taken with `pg_get_functiondef`/`prosrc`, never file text: `get_knowledge_pack 2deb725f…`, `list_client_knowledge 32999fef…`, `_knowledge_legacy_rows 65f4f0f3…`, `_knowledge_capture_core 2c8526b8…`, `_knowledge_floor 5e4d8069…`, `capture_knowledge b9f1cf6b…`, `get_context_pack 1a0312c9…`, `answer_work_question 15a82c08…`. `p658.census.no_recut` re-reads all eight from the catalog: green.
- **closure** — `check-frozen-workflows.mjs --print-closure`: 296 frozen files. `knowledge.mjs`, `knowledge-conflicts.mjs`, `work-trace.mjs`, `capability-registry.mjs` are IN; `knowledge-retrieval.mjs`, `capability-registry-v2.mjs`, `work-trace-bounds.mjs`, `wiki-projection.mjs`, `wiki-projection-ops.mjs` are OUT (zero hits).
- **core size — 7 keys, small and enumerable**, so D16's terminal cannot stop legitimate accounting: `accounting_basis`, `banking_arrangement`, `customer_identity_policy`, `entity_type`, `msic`, `reporting_framework`, `trade_nature` (13 `knowledge_keys` ∪ 5 `client_fact_keys`).
- **grant baseline/after** — `has_function_privilege`: `get_knowledge_pack`, `retrieve_knowledge`, `read_knowledge_record_for`, `read_knowledge_history_for` = `clara_runtime` only; false for `clara_authenticated`, `clara_agent_ro` and all six wake roles. `list_work_knowledge_reads_for_record` = `clara_authenticated` only. `clara.work_knowledge_reads` holds table privileges for `clara_fn_owner` and **no app role**; `relrowsecurity`+`relforcerowsecurity` both true.
- **part kinds — FINDING: none fits.** `lib/parts/catalog.ts` carries 29 kinds. `knowledge_receipt` is a capture receipt (`record_id`, `knowledge_key`, `knowledge_version`, `revision_kind`) with **no status field**; `refusal` carries `code`/`reason`/`message` but no version, no as-of, no key set and no partial/unknown distinction. So no existing kind can carry a knowledge-read status honestly — the integrator decides `knowledge_unavailable` at the v21 cut. I registered none and opened none of `lib/parts/*`.
- **C-87 Mobbin, 12 frames inspected** (web): [Coda](https://mobbin.com/screens/4fc64df6-e6be-4f55-82f9-dcf0942e257e) (freshness + row-cap in one sentence, with Refresh), [Neon](https://mobbin.com/screens/60b6a7af-334c-4a71-bf97-e7bbba0a8f20) ("You're viewing data as it existed at …" — an as-of banner naming the instant), [Slite](https://mobbin.com/screens/784fd37d-3429-4f47-b58c-0b6fc0ffed07) (knowledge docs under a WORD status set: Outdated / Verification expired), [Zapier](https://mobbin.com/screens/9fb0b917-eff7-437a-9d16-24a27db70f4a) (truncation banner naming the cap), [Pin](https://mobbin.com/screens/760114c0-9a50-4d4e-81d5-851cb24043c1), [Clay](https://mobbin.com/screens/8fcf5c91-9a9f-404a-8e68-8f839d372434); and for the basis-changed affordance [Unity](https://mobbin.com/screens/2ce44892-7dee-470c-a87d-357ae20cebdc) — **the closest real practice**: a persistent inline panel beside an open composer whose typed draft survives, which is what the drift banner copies — against [Navan](https://mobbin.com/screens/5fc0dbfb-f06d-4d7e-91f8-465d42a3e060) and [Kiwi.com](https://mobbin.com/screens/4ee4e518-e3f7-4eac-83e1-71ce326b1729) (modal "prices may have changed" — the anti-pattern: it interrupts and discards context), [Lovable](https://mobbin.com/screens/045dc5db-fcf4-4992-a7e0-ccd65e0cc2df), [Zoho CRM](https://mobbin.com/screens/504b3751-d7f3-4efd-94e7-efc3ca319416), [Hashnode](https://mobbin.com/screens/9a4331b4-dec0-4a7e-b6b6-53c95cc5da81). **No video/flow motion was viewed — that remainder is still outstanding.**

## Tests and commands (all local; hosted evidence pending)

| Command | Result |
|---|---|
| `pnpm typecheck` (root) | **exit 0** (re-run at head) |
| `pnpm lint` (root) | **exit 0, 0 errors** — it was **RED at `b85ce24e`** on two `no-restricted-syntax` hits: `"#658"` inside a string literal matches the raw-colour selector (`#` + 3 hex). Fixed in `33b99b83` |
| whole `apps/web` suite (`node scripts/run-tests.mjs`) | at `141103cb`: **4171 tests · 4169 pass · 0 fail · 2 skipped**, exit 0. (At `33b99b83` it was 4164 · 4161 · 1 fail · 2 skipped -- the cross-lane `use-clara-thread-stop` flake below, which did NOT recur in this run) |
| — the one fail | `lib/clara/use-clara-thread-stop.test.ts:1021` "ALREADY FINISHED". **Not this branch**: `git diff origin/main..HEAD -- apps/web/lib/clara/` is empty. Isolated: **fail, 25/25, 25/25** (2 of 3 green) — the known cross-lane flake #642/#656/#657/#660 all report |
| db battery, 41 gate flags (40 verbatim + mine after `preview-invite`) on `clara_658` | `tests/knowledge-retrieval.test.mjs` **28/28 pass, 0 skip** (23 + 5 fix-round cells) |
| focused, gate UNSET, **frontier 0224** | measured at `33b99b83`, when the battery held 23 cells: **23/23 FAIL loudly** — "the 0230 knowledge-retrieval cohort is required for a focused run". Measured on a throwaway `create database … template clara_658` clone with 0230's nine objects dropped; clone dropped afterwards, `clara%` roles still **18** (0154's pin intact). Same clone WITH the gate preloaded: **23 skip, 0 fail**. NOT re-measured after fix round 1: the gate module and the cohort predicate are untouched by it, so both arms hold; only the count in the message is now 28 |
| `operation-census.test.mjs` + `rig-isolation.test.mjs` (no reset flags) | **31 tests · 30 pass · 1 skip · 0 fail**; **T10b green** (the World leg bootstraps no Workflow World, so #866 is not triggered) |
| `knowledge-records` / `knowledge-firm-defaults` / `knowledge-legacy-readers-converge` | **27/27 · 21/21 · 8/8** |
| runtime units | `knowledge-retrieval` **19/19**, `work-trace-bounds` **9/9**, `wave-b-wiki-projection-unit` **54/54**, `knowledge-lib` **16/16**, `clara-work-v4` **25/25**, `work-trace-redaction` **21/21** |
| World leg — `PGDATABASE=clara_658 WORKFLOW_POSTGRES_URL=… node tests/work-knowledge-e2e.mjs` | **exit 0**: one read-set row survived a SIGKILL and replayed onto its own id, seq 2 is a second read, a non-`accounting_work` task is CLR11, and `work_knowledge_drift_for` observed from the READ |
| `check-frozen-workflows.mjs` | OK — 296 frozen files, 53 `"use workflow"` modules, exit 0 |
| `check-parts-parity.mjs` | OK — reader ⊇ emittable, exit 0 (no kind added) |
| `check-wiki-dynamic-sql.selftest.mjs` | OK, exit 0 |
| Playwright (`pnpm --filter @clara/web e2e knowledge`, triple 3370/3371/3372) | at `33b99b83`: **41 passed, 0 failed (3.5m)**: `knowledge-walk` 18, `knowledge-firm-walk` 14, `work-knowledge-walk` 9. Re-run at `141103cb`: **40 passed, 1 failed** — `knowledge-firm-walk.spec.ts:198` died on `locator.fill: Target crashed` (a browser crash under host contention, in a spec fix round 1 did not touch); **alone it is 14/14, exit 0**. Both runs reported. |
| new web unit files | `knowledge-panel-freshness` 5, `knowledge-record-reads` 5, `work-knowledge-block` 7, `work-diagnostics-observed` 4, `work-question-drift-banner` 7; touched: `work-detail` 47, `work-question-form` 22 |

**Tests added → what they prove.** `packages/db/tests/knowledge-retrieval.test.mjs` (28) — the seven doors' floors, grants, tenancy-without-oracle, the core-first bound, the period mark, the #783 negative grant, the seventh door's shadow/cap/no-values wall, drift relevance and the `relevant:null` trace arm, the no-recut census, and fix round 1's five: the drift shadow, the closed `tiers` vocabulary, the named replay, the finite `as_of` and the atomic envelope. `packages/runtime/tests/knowledge-retrieval.test.mjs` (19) — never-null/never-throw, `partial` distinct from both neighbours, `faceStatusOf` total over the FIVE frozen reasons and never a sixth, every door failure landing on a non-`ok` face word, and the relayed replay signal. `work-trace-bounds.test.mjs` (9) — the writer's two clauses are **no tighter** than 0210's door. `work-knowledge-e2e.mjs` — replay idempotence on `(work_id, run_id, seq)` across a real SIGKILL.

## Docs updated

`CONTEXT.md` — **Knowledge read status** and **Knowledge read-set** (the two DECISIONS §3.1 ratifies, not four), plus two additions to **Knowledge pack**'s _Avoid_. `packages/db/README.md` — the seven doors with floors/grants, the #783 negative assertion and the seventh door's positive one, the relation and its deliberate FK absence with 0195's measurement, the no-recut tail. `packages/db/tests/README.md` — the battery, cells and cohort gate (both arms measured). `packages/runtime/README.md` — the three non-frozen modules with freeze-by-closure warnings, the `faceStatusOf` table, the registry-v2 sibling, and **#847/#791 moved from "owed on v4" to "owed on v5, stanza written"**. `apps/web/README.md` — the new faces, the drift banner's two wordings and its draft rule. **No `docs/PRD.md`, no `docs/ARCHITECTURE.md`.**

Fix round 1 (see `658-fixround-1.md`) extended four of those: `packages/db/README.md` (the drift shadow and its two deliberate non-symmetries, the read-set's four walls, the replay receipt), `packages/db/tests/README.md` (28 cells), `packages/runtime/README.md` (five reasons not six, with D16's terminal restated, and the replay relay) and `apps/web/README.md` (the drift reader's coalescer: why it is not a prop and not a cache). `CONTEXT.md` unchanged — the round added no vocabulary.

## Blueprint drift (reported, not edited)

1. `ARCHITECTURE:183`/`:293` state pins v19/v3; live pins are `chatTurn_v20`/`claraWork_v4` (`registry.ts:173`, `:270`).
2. `ARCHITECTURE:373-386` makes the writer-side trace bounds binding on v4; v4 shipped without them. The code now exists (`lib/work-trace-bounds.mjs`) but binds only from v5 — #847.
3. `ARCHITECTURE:435-445` makes a schema-covering bundle digest binding on v4; `claraWork.v4.bundle.ts:53` still hashes `tools:{id,names}`. Owed on v5.
4. `ARCHITECTURE:297-299` — this slice **complies**; the drift is the opposite one: the sentence names only `get_knowledge_pack`, while the owner's ruling governs every assembled pack-shaped read. A Wayfinder pass should widen it from the function to the SHAPE.
5. `PRD:123` defers automatic re-evaluation to #658 **and** #663. This slice ships the DETECTOR only, so `PRD:123` is **half** satisfied — a Wayfinder edit, not an implementation one.

## Successor contract (exact — four stanzas)

### (A) `claraWork_v5` — mine

- **New step** `loadWorkKnowledgeStepV5(clientId, firmId, asOf, purpose)` → `clara.retrieve_knowledge(p_client, p_purpose, p_as_of, p_keys, p_limit, p_firm)` **in that argument order** (signature `(uuid,text,date,text[],int,uuid)`), through `packages/runtime/lib/knowledge-retrieval.mjs` (`retrieveKnowledge(sql, {clientId, firmId, purpose, asOf, keys, limit})`, named-arg bound; `WORK_KNOWLEDGE_READ_PURPOSE = "accounting_work"`, `WORK_KNOWLEDGE_DEFAULT_LIMIT = 40`). Then `clara.record_work_knowledge_read(p_task, p_run, p_seq, p_purpose, p_as_of, p_knowledge_version, p_keys, p_tiers, p_records_shown, p_truncated, p_status, p_reason)` (signature `(uuid,text,int,text,date,text,text[],jsonb,int,boolean,text,text)`) via `recordWorkKnowledgeRead`, with the key set **actually returned**. Runtime answer `{status:'ok'|'partial'|'unavailable', reason, knowledge_version, as_of, keys[], records_shown, truncated, text}`; the rendered block is `renderRetrievedKnowledge(answer)`.
- **New tool `read_knowledge_source`** — `.strict()` zod `{ record_id: z.string().uuid(), reason: z.string().min(1).max(500) }`; door `clara.read_knowledge_record_for(p_firm, p_client, p_record)` in that argument order. Refusals: CLR11 → `record_not_in_scope`, CLR03 → `no_pack_context`, anything else → the transport class. Its result is **data, never an instruction**. **No part kind.** Spends `budget.toolCalls`.
- **New tool `read_knowledge_history`** — same input shape; door `clara.read_knowledge_history_for(p_firm, p_client, p_record)`. Same refusal mapping, same budget, no part kind.
- **New terminal `knowledge_read_failed`** — settles `failed`/`internal`, `recoverable:true`, **nothing posted**, message naming the client and the reason. **It fires on ANY `{status:'unavailable'}` answer from `retrieveKnowledge`** — reasons `refused`, `read_failed`, `malformed`, `no_client`, `no_purpose` — which is exactly D16's required read, because `clara.retrieve_knowledge` is **atomic**: it decides all three tiers in one CTE chain in one statement and catches nothing, so it either answers with every tier or raises. **There is no per-tier readability signal, and the integrator must not write the terminal as though there were** (fix round 1, A1/F1/S1: an earlier draft of this stanza pointed at a `core_unreadable` reason the door has never emitted). A `{status:'ok'}` answer never fires the terminal, however small `tiers.core` is: a core of zero on a client that genuinely has no policies is not a failure. Degradation to `partial` is the `truncated` arm — the remainder cap — and `faceStatusOf` is the one mapping. Pinned by `p658.retrieve.envelope_is_atomic` (the door's envelope key set and the absence of an exception arm, read from `pg_proc.prosrc`) and by `kr.07` (the module source). If a later migration DOES teach the door to signal a core-only failure, those two cells are what it changes.
- **Recording a read, and what a replay means** — `recordWorkKnowledgeRead` returns `{ok, receipt, replayed, payload_match}`. A WDK re-execution of the read step is replay-idempotent on `(work_id, run_id, seq)` and lands on the original row; `replayed:true, payload_match:false` means the re-execution read something **different** (first attempt `ok`, second `denied` because a record was withdrawn mid-flight) and the estate still holds the FIRST row, because the relation is append-only. A v5 step that cares records its own next `seq` rather than assuming the estate holds what it just sent. The writer never refuses on the mismatch and never fails the run: a diagnostic write that could settle a Work would be worse than the divergence it reports. `p_tiers` must be `{core, requested, remainder}` counts (anything else is CLR10 `tiers_shape`), and `p_as_of` must be a finite date.
- **Replan trigger** — after a resume and before the next segment, `clara.work_knowledge_drift_for(p_firm, p_work)` via `readKnowledgeDrift(sql, firmId, workId)`. `relevant:true` spends **one existing** `budget.replans` and re-enters the segment with a stated note; `relevant:false` continues untouched; **`relevant:null` (`observed_from:'trace'`) is surfaced, never treated as `false`**. **Budgets do not move** (`claraWork.v4.bundle.ts:39-45`; replans stays 2).
- **New capability ids** `accounting_work.retrieve_knowledge` and `accounting_work.inspect_knowledge_source`, both `modelBound: true`, from `lib/capability-registry-v2.mjs` (`CAPABILITY_REGISTRY_VERSION_V2 = "clara-capability-registry/v2"`) — never an edit to the frozen v1.
- **Bundle `clara-work/v5`**, digest covering **each tool's JSON schema and its declared dependencies**, not `tools:{id,names}` (ARCHITECTURE:435-445). A cell must fail when a tool's schema changes and its name does not.
- Registry: repoint `claraWork` to v5; **keep v1…v4 exported and in `workflowBodies`/`WORKFLOW_BODY_IDS`** or the stranded-body gate refuses World startup database-wide.

### (B) `chatTurn_v21` — conditional (the integrator may reduce this to contract-only and must say so)

A SIBLING step beside the frozen `loadContextStepV10`, which stays **byte-untouched** and keeps being called for the history read: `loadClientBasisStepV21(clientId, firmId, createdBy, asOf)` returning `{status, reason, text}` that **never collapses to null**, repointing the chat-turn knowledge preload from the recency dump to `clara.retrieve_knowledge(p_client, p_purpose, p_as_of, p_keys, p_limit, p_firm)` with `p_purpose='chat_turn'`, and surfacing a read failure as a **typed status** rather than `catch { contextPack = null }` (`chatTurn.v10.impl.ts:136`). **`clara.get_context_pack` is not recut and not repointed — not one byte.** The five frozen `knowledge.mjs` reasons map onto the four face words through `faceStatusOf`. Part kind: **none registered by me**; `knowledge_unavailable` only if the integrator accepts the finding above that no existing kind fits. Engine stamp `llm-openai:<modelId>:chatturn-v21`.

### (C) Rider #847 — writer-side trace bounds

`packages/runtime/lib/work-trace-bounds.mjs` (a v5 sibling the v5 body imports; **`lib/work-trace.mjs` is never edited**) mirrors 0210's two door-side bounds on the writer side: `boundedRevisionNumber(v)` — `abs(v) < TRACE_NUMBER_MAX_ABS (1e12)` and `scale(v) <= TRACE_NUMBER_MAX_SCALE (6)`; `boundedRunId(v)` — `^wrun_[0-9ABCDEFGHJKMNPQRSTVWXYZ]{26}$` **or** no run of 13+ digits. Call them in `observedRevisions`' caller inside the v5 impl, before `traceSafely`. **The writer's clause stays NO TIGHTER than the door's** — a writer stricter than the door loses rows the database would have accepted, and `traceSafely` swallows the loss (`tests/work-trace-bounds.test.mjs` asserts the admitted side by value). Sources: the ticket, `ARCHITECTURE:373-386`, `0210:32-41`, `work-trace.mjs:293-303`. README sentence from v5: *the door bounds these two fields in shape, and from v5 so does the writer.*

### (D) Rider #882(a) — CLR40 classification

ONE row in `claraWork.v5.errors.ts`: `(CLR40, fa_cost_adjustment_deferred) → refusal`, carrying the door's own human remedy verbatim — "reverse the acquisition entry and re-book it at the corrected cost" (`0041:2727-2731`). The other two CLR40 reasons — `fa_k_gl_balance_on_enrolled` (`0041:2717`) and `fa_belt_unregistered_movement` (`0041:2733`) — are **not** added by this stanza; named here so the integrator does not widen it. A cell must prove such a Work settles as a **refusal**, not as the transport class. Keyed on `(code, reason)` like `claraWork.v1.errors.ts`; adjacency is the FA belt, **not** the CLR38 depreciation family.

## Assumptions

1. **Seventh door, firm scope** — resolved exactly as DECISIONS §6.1 rules: `scope_kind='client'` → that client; `scope_kind='firm'` → every client in the firm **except** those whose own live record shadows the key, using the `not exists` shadow copied from the live body measured on the rig (`0192:1355-1363`, `:1508-1519`). Cell `p658.record_reads.firm_scope_shadow`.
2. **The drift envelope carries a `read` block** (key NAMES, tier counts, face word, reason — `null` on the trace arm) rather than an eighth door, because B3 asks "what did it read" and "has it moved" as one question. `p658.drift.relevant` asserts by value that no record value, `applies_when` or pack content rides with it.
3. **`clara_658` was migrated to 0230 before this session**; the no-gate proof therefore ran on a template clone, not by rolling the rig back. No from-scratch chain was run; `CLARA_RIG_ALLOW_RESET`/`_ROLE_SWEEP` were never set.
4. `work-knowledge-mock.mjs` dispatches **above** `home-board-mock.mjs` (comment at both sites), per DECISIONS §6.1's #657 ruling that `serve-built.mjs`'s chain is semantic, not sorted.
5. **Non-goals held, and stated in code**: #658 captures, corrects, promotes nothing, accrues no experience, writes no wiki/OKF page, registers no event, mints no knowledge key or side table, widens no `accounting_work.purpose`, widens no `freeform_read`, adds no part kind, no needs-you row kind and no route.

## Merge notes for the integrator

`components/work/work-detail.tsx` — ONE mount line inside `TabsContent value="sources"`; sequence **#658 → #655 → #636**. `work-diagnostics.tsx` one `observed_revisions` block; `work-question-form.tsx` the drift banner. Shared files touched minimally: `messages/en.json` (one `WorkKnowledge` namespace), `test/manifest.txt` (5 lines), `serve-built.mjs` (1 import + 2 dispatch lines), `e2e-fixture-ownership.test.ts` (1 lane row, 1 declaration, 6 shared-verb entries), `rig-meta.mjs` (one cohort block), `packages/db/package.json` (one gate flag, migration-order position), `action.yml` (one step), `CONTEXT.md` (2 terms + 1 _Avoid_ extension). No `tree.ts`, no `registry.ts`, no `lib/parts/*`.

## Follow-ups worth filing

1. **Widen ARCHITECTURE:297-299 from the function to the SHAPE.** The #783 ruling is written about `get_knowledge_pack`, but `retrieve_knowledge` is equally pack-shaped and the tail had to assert the absence of a human grant by hand. A Wayfinder pass should restate the rule over *any assembled pack-shaped read*, so the next pack-shaped door inherits it instead of re-litigating it.
2. **Surface `last_projected_seq` / `has_stale_sources`.** They are computed inside `get_context_pack`'s wiki block (`0209:272-274`, `:316-322`) and reach no read and no surface, so AC3's projection-lag half has no face. Surfacing them needs a recut this brief forbids; it belongs with #663's engine.
3. **Two independent SOURCES disagreeing has no representation.** `_knowledge_source_pins` pins ONE document/extraction/region per record (`0192:750-803`), so AC4's "conflicting independent sources" is only ever two RECORDS, never two sources of one record. Worth a shape decision before #665 measures accuracy against it.
4. **Should the retrieval door be able to signal a CORE-ONLY failure?** Today it is atomic, D16 fires on any unavailable answer, and both sides say so (fix round 1, A1). Making the layering real means assembling the core tier -- including the pinned legacy union -- in its own `begin … exception` arm and returning `core_readable boolean`: `partial` would gain its third case and the terminal could distinguish "we could not read the policies" from "the read failed". It mints a durable envelope field and a new per-tier failure semantics this brief does not specify, and a blanket `when others` in a read door is a posture decision, so it is **ratification requested**, not built. `p658.retrieve.envelope_is_atomic` and `kr.07` are the two cells such a change must flip.
5. **The read-set key grammar is stricter than the catalog's own CHECK.** `record_work_knowledge_read` requires `^[a-z][a-z0-9_]{0,62}$`; `clara.knowledge_keys` and `clara.client_fact_keys` require only `btrim(...) <> ''`. All 13 + 5 live keys conform today (measured), so nothing is broken -- but a future migration minting `Sst_Regime` would make every read touching that client retrievable and UNRECORDABLE. Written into 0230's header (fix round 1, A8); worth stating once as the estate's key law.
6. **A `#NNN` ticket reference inside a string literal is lint-banned and nobody knows it.** `NO_RAW_COLOR_VALUES` matches `#` + three hex digits, so `"#658"`, `"#642"`, `"#655"` in any linted literal are errors while the same text in a comment is fine. One sentence in the rule's own message would have saved this round-trip.

## Unverified

- **Hosted evidence pending** — every number above is local, on `clara_658` / this worktree.
- **The behavioural effect of core-first tiering on real Work output is unmeasured** — no hosted pilot exists, and #665 (not this ticket) owns accuracy; I pinned no threshold.
- **AC1/AC2/AC3's Work-lane behaviour is contract-delivered, not fixed** — the step, the two tools, the terminal and the replan land at the single shared `claraWork_v5` cut. `#885`'s human-visible half **is** closed here (the real drift banner over `observed_revisions`, which deployed v4 already writes).
- **C-87's motion/video remainder** is outstanding; only the 12 still frames above were inspected.
- **The cohort gate's two arms were measured at `33b99b83`, when the battery held 23 cells**, and were NOT re-measured after fix round 1. The gate module and the cohort predicate are untouched by that round, so both arms hold; only the count in the failure message is now 28.
- The `use-clara-thread-stop.test.ts` flake is **characterised, not diagnosed** — it is outside this branch's diff and outside its ownership.
