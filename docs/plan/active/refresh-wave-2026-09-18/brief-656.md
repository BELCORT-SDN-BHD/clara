# Brief: #656 — 导入并核对有来源的期初总账 (import and tie out a SOURCED opening general ledger)

*A6 client / accounting opening → sourced opening basis → tie-out → client Home · C3 the opening journal as an inspectable accounting result*

## Orchestrator decisions (binding)

- **Migration `0228` only**, ONE file `packages/db/migrations/0228_opening_ledger_source.sql` (DECISIONS §2, row 0228). It **creates no function, no table, no column, no trigger, no CHECK, no policy and no grant.** Its whole content is: house header → prestate pins → the capability-registry republication → (no grants, no events) → tail → gate module → `rig-meta.mjs` cohort. **Recuts: `none by default`** (§2 row 0228). The ONE conditional recut arm is §3 item 3 below and it is gated on a measurement, never on a reading.
- **D13 (§0), restated as four binding lines, each one a thing you must do or must not do:**
  1. **The trial-balance reader is wired IN-LINE, at the OCR pass** — `normalizeAzureLayout` in `packages/runtime/lib/egress.mjs:136-183` — through ONE new non-frozen module. No new processing lane, no new `engine_kind`, no CHECK widening, no `_enqueue_invoice_facts_core` splice. **The routed option is REFUSED** (this supersedes gap-656 Q1's two-option framing; the map's own recommendation agrees). You therefore never read `ck_processing_task_lane_*` / `ck_processing_task_lane_engine_*` / `ck_document_extractions_engine_kind_*` out of `pg_constraint`, and SYNTHESIS §7.4 #6 is closed by ruling rather than by measurement. This is what keeps the ticket **L**.
  2. **An unreadable row forfeits the whole document, and the refusal NAMES every failing row.** AC4's "partial or unsupported rows remain explicit Work children" is read as *"the refusal must name them"*, never as *"admit the readable ones"* (supersedes gap-656 Q2's open framing). Do not relax one refusal law in `opening-tb-cells.mjs` to make a fixture pass.
  3. **The capability registry moves to `registry_version = 2` by UPDATE, never DELETE-then-INSERT** (#846; 0207's monotone trigger permits a raise and refuses a decrease with CLR08 `registry_version_monotone`, `0207:124-140`). `prior_gl`'s and `opening_balance_doc`'s `business_operation` and `basis` are corrected; `prior_gl` additionally carries a named `limits` entry for the missing browser entrance.
  4. **Clara gets NO opening entrance this wave.** You write the successor-contract stanza (§3 "Successor contract") and build no tool, no step, no part kind. C-39 closes `partial` with the written contract plus the follow-up ticket your report files.
- **The registry republication is WHOLE-REGISTRY, and that is a refinement of §2 row 0228 forced by a live census — not a contradiction of it.** `packages/db/tests/document-capability-registry.test.mjs:204-212` asserts `count(distinct registry_version) = 1` ("the registry publishes exactly one version at a time"), and its rollback-hygiene cell `:441-465` asserts `min(registry_version) = 1` **and** that the `pdf × invoice` row reads `registry_version = 1`; that battery's own header (`:136-139`) names "exactly one distinct registry_version" as a registry-wide invariant it exists to hold. Raising only two kinds to 2 reds it. **So: every row goes to `registry_version = 2`; only the `prior_gl` and `opening_balance_doc` rows change `business_operation` / `basis` / `limits`.** The two named rows end at 2 exactly as §2 orders. **That battery is yours to update in the same commit, with the reason written beside each changed number** — the precedent is exact: `af3b5955` (#779) shipped `0207_document_capabilities_version_monotone.sql` and **+147 lines of this same battery** in one commit. Re-base `:204-212` on version 2, re-base the rollback-hygiene numbers, and leave the monotone probe cells' *transition* assertions (a `5 → 3` UPDATE refused, a `+1` raise permitted) doing exactly what they do today. **Measure `p656.m1` before you write a line of SQL; if the census does not behave as this paragraph reads, STOP and report instead of patching.**
- **No frozen successor, and that is a measurement** (DECISIONS §1.1: "no `documentIngest_v3` (#656 measured its closure needs nothing)"). `documentIngest`'s frozen closure is its own six files under relative import; `services.analyzeDocument` / `services.parseStructured` are **injected** (`packages/runtime/lib/intake.mjs:483-484`), so the frozen behaviour module never imports your producer. **#656 appears in no row of DECISIONS §1.2**, so the `chatTurn_v21` / `claraWork_v5` cuts carry **nothing of yours** (D17 cuts both, for #655 / #651 / #658 — this corrects the pre-design-lens draft that read #642 as a v21 driver; `gap-642.md:141-142`). You cut no `_vN`, edit no file in a frozen closure, and never touch `packages/runtime/workflows/registry.ts`.
- **`packages/runtime/lib/opening-tb-produce.mjs` freezes on import by any future opening tool** (§1.3, §1.6; `lib/periodic-adjustment-basis.ts` is the estate's proof that the trap has sprung once already). Therefore **durable rules live in the migration and in `opening-tb-cells.mjs`'s existing laws, never in the new module** — it delegates and adds no grammar of its own.
- **AC5's "Work and receipt" is `descoped (authority)`, and the authority is measured, not asserted:** the opening lane writes `op_receipts` (0002's idempotency ledger) and never `operation_receipts` (`0178:411`); `accounting_work.purpose` is a closed three-value IN-list (`0194:230`, receipts `:232-233`) held by an executable census pin (`0194:2180`) and by the posting core's own Work lookup (`0204:180`); SYNTHESIS §0.4 measures that **no ticket this wave widens it**, and §2 row 0228 forbids it for you by default. **Build no `accounting_work` row, no `operation_receipts` row, no needs-you row kind.** The owner question ("does an opening basis need a Work-shaped record, and what would a receipt mean for a lane with no Work row?") is a **named residual in your report** (§2.3).
- **Two follow-up tickets your report MUST file** (DECISIONS §2.3, both measured as ownerless in the gap pass): (a) **a browser entrance to `POST /api/seeding/prepare`** — nothing in `apps/web` calls it and no open ticket owns building one; you name that ticket in `prior_gl`'s `limits` sentence, which is 0191's own instrument for a named gap inside a supported level (`0191:235-236`, "A limit is not a lower level"); (b) the **AC5 Work/receipt owner question** above.
- **The design/safety lens was applied late to gap-656 and its four findings are BINDING** (DECISIONS §6 → §7.5 #1; the first reconcile step matched the lens label case-sensitively and fed itself the citation payload twice): **F1** the two registry rows are corrected for *different measured reasons* and the asymmetry must be argued, not implicit; **F2** #642 is not a `chatTurn_v21` driver; **F3** AC5's Work/receipt clause is dispositioned `descoped (authority)`; **F4** every NEW input this slice adds goes through `Field`/`FieldGroup` (GAP-ORDER §2's house fact, `GAP-ORDER.md:81`). F1's *first* branch ("leave `prior_gl` at `stored_only` until an entrance ships") was **refused as contradicting D13** — do not re-open it.
- **Ownership (DECISIONS §3).** You **OWN** `apps/web/components/registers/opening-*.tsx`, `apps/web/lib/registers/opening*.ts`, your new runtime module, battery, walk and mock. **ONE hunk, in your own commit**, in `apps/web/lib/journals/api.ts`: add `is_opening_balance` to `ENTRY_SELECT` (`:63-66`) plus one badge — **#655 owns that file** (§3's ruling supersedes gap-656's "coordinate with #655 or defer"). **Do NOT open**: `components/firm/client-workspace-overview.tsx` and `components/firm/client-home/*` (#660 owns them — the client-Home link into `?tab=opening` **already exists** from #649, `client-onboarding-progress.tsx:159-170`; your walk follows it **read-only**), `components/work/work-detail.tsx` (#658 → #655 → #636), `components/clara/*` and `lib/parts/*` (#642 — you register **no part kind**), `lib/firm/needs-you.ts` (**no new row kind this wave**, §3; any opening affordance rides the existing `lint_finding` kind), `components/bank/*` (#657), firm Home (#659), `components/firm-admin/settings-panel.tsx` (#635), `packages/runtime/workflows/registry.ts`, `docs/PRD.md`, `docs/ARCHITECTURE.md`, and any frozen module. **`clara.document_capabilities`: you change only the `prior_gl` and `opening_balance_doc` rows' level / basis / limits — #782 owns the invoice `limits`** (§5; SYNTHESIS §3.2).
- **Shared files: one line each, at the sorted position** — `apps/web/test/manifest.txt`, `apps/web/messages/en.json`, `apps/web/e2e/serve-built.mjs`, `apps/web/e2e/e2e-fixture-ownership.test.ts`, `packages/db/tests/rig-meta.mjs`, `packages/db/package.json` (gate chain, **migration order, not alphabetical**), `.github/actions/db-live-gates/action.yml`, `CONTEXT.md`. **No `apps/web/lib/navigation/tree.ts` row**: the opening tab is deliberately absent from the sidebar and `registers-workbench.tsx:58-64` says why ("naming every tab twice would make the sidebar a second, competing tab strip"). **No new file under `app/**`** — `?tab=opening` already exists.
- **CONTEXT.md terms you write** (DECISIONS §3.1 — ratified, do not re-derive, do not invent a fifth, do not edit #658's knowledge terms), in the house "term / _Avoid_" shape: ***Opening basis***, ***Opening source***, ***Opening target***, ***Provenance: document-sourced vs keyed***. The shared freshness word set is `ok / partial / unknown / denied` and **no face and no DB column ever says `unavailable`** (SYNTHESIS §4).
- **Boundaries (DECISIONS §3.2 + SYNTHESIS §4)**: **#656 vs #661** — you deliver the **targets** side (`opening_tb_targets`) and its provenance; #661 keeps every `opening_items.item_kind`; **you do not touch `opening_items` authoring at all**. **#656 vs #636** — you mint no Work shape. **#656 vs #657** — read-only adjacency: an approved opening `bank_uncleared` item is already booked; assert nothing about bank matching. **#656 vs #660** — #660 becomes the estate's first product caller of `clara.trial_balance_as_of(uuid,date)`; **call it, never change it** (`0057:1876-1904` carries two live assertions that fail a migration if it stops filtering to exactly `{approved}`). **#656 vs #854** — the opening lane's two-session race is #854's and is **unmeasured**; a real tie document enlarges the contention surface: say so, claim no concurrency proof, add no cell that contradicts #854's framing. **#656 vs #861** — do not recut the activity ladder; record the residual. **#656 vs #857** — adopt its rule pre-emptively: **every `opening_tb.line` fixture is created through `clara.persist_document_extraction`, never a raw INSERT.** **#656 vs #884** — out of scope; note the enabling, build nothing. **#656 vs #927→#929** — those retire the **0045 recurring-adjustment template** lane, not the COA template lane (`clara.apply_coa_template` is alive and gated by `0173`); propose nothing in either.
- **Non-goals, in code comments AND in the report**: no new processing lane and no facts-router splice; no recut of `persist_document_extraction`, `_enqueue_invoice_facts_core`, `approve_opening_correction`, `_draft_opening_item_core` or any opening door (the one conditional arm is §3 item 3); no second approval ritual for the document read (AC5 forbids it; `approve_opening_seed`'s distinct-checker / self-attestation door is the ceremony the estate already has, and `PRD.md:114` blesses 期初确认 as an Admin responsibility); no partial admission; no Clara tool; no new granted door name (so `0042:5385` and `0044:6353-6356`'s verb rosters stay untouched); no percentage and no single "onboarding %"; **no hosted claims — write "hosted evidence pending"** (DECISIONS §4).
- **Numbering discipline (DECISIONS §2.2)**: **your number is `0228` and SYNTHESIS's table is not your authority** — SYNTHESIS §2.3 proposes `0229 #656`, and DECISIONS §6 rules explicitly that *"this file's numbers stand"* (0228 #656 · 0229 #636 · 0230 #658 · 0231 #659 · 0232 #660 · 0233 #635); both orders are valid because no two files recut the same function, so the numbers are a **merge order, not a dependency order**. Never renumber, never pick a number, never edit a merged migration. **Gate modules take a STABLE STEM** (matching the migration stem), **cohorts take the NUMBER**, the gate line goes into `packages/db/package.json` at its **migration-order** position, and **a focused run of your battery without its gate must FAIL loudly below 0228**. A new parameterisation would be a NEW verb, never a defaulted argument (`0103:1055-1070`) — you install no name, so this bites only if you drift. **ONE STEM for this ticket's whole new-file set, and no second spelling anywhere**: migration `0228_opening_ledger_source.sql` · gate `packages/db/tests/opening-ledger-source-preintegration-gate.mjs` · battery `packages/db/tests/opening-ledger-source.test.mjs` · env var `CLARA_ALLOW_MISSING_OPENING_LEDGER_SOURCE` · World leg `packages/runtime/tests/opening-ledger-source-e2e.mjs` · walk `apps/web/e2e/opening-ledger-source-walk.spec.ts` + mock `apps/web/e2e/opening-ledger-source-mock.mjs`. `opening-source*` is not one of these names; the gate-to-battery pairing is by the env-var STRING and §3 item 5 carries the measurement that says why it cannot be derived.
- **Evidence law (DECISIONS §4), binding on every line of your report**: every AC and every historical row closes with a **red-first cell**, a **browser-walk leg**, or a **named residual** — never a claim; **REDESIGN rows are re-measured, never copied**; new DB batteries assert through `humanQuery` **least-privileged personas** (`rootQuery` only for fixture DML and for facts a masked door deliberately never returns, and labelled as such); **a skipped frontier-gated battery is not evidence**; the World leg runs on **your own rig** with `WORKFLOW_POSTGRES_URL`; **local ≠ hosted** — write "hosted evidence pending" and never claim otherwise; **blueprints are never edited** and your report carries "blueprint drift" lines for #683's sync (the ones this ticket owns are listed under **Docs** in §3).
- **Playwright port triple (RIG.md, verbatim)**: `CLARA_E2E_APP_ORIGIN=https://127.0.0.1:3350 CLARA_E2E_NEXT_PORT=3351 CLARA_E2E_RUNTIME_PORT=3352`. **Rig row (RIG.md, verbatim)**: worktree `C:\Users\zhant\Desktop\clara-wt\656`, branch `impl/656-opening-ledger`, PG port **55706**, db **clara_656**, base `abcc5030`, 219 migrations at `0224_preview_invite`, PG 17.11.

## 1. Current state

**DB — the whole opening lane exists and is battery-covered; only its SOURCE is missing.** Tables, all born in `0017_wave_b.sql`, all FORCE-RLS with owner + human/agent/runtime SELECT policies and **zero app-role DML** (`0017:1395-1445`): `opening_seed_registry` (`:1076-1116`; `uq_opening_seed_registry_once` `:1117-1118` is a partial unique on `client_id where state <> 'cancelled'`; `ck_opening_seed_tie_pair` pairs `tie_document_id`/`tie_document_sha256`; `ck_opening_seed_terminal` `:1105-1115` makes each state carry exactly its own columns), `opening_items` (`:1121-1175`, 1:1 with a `journal_entries` row, `supersedes_item_id` chain, `item_kind` in `gl_balance, ar_open_item, ap_open_item, bank_uncleared, fixed_asset, equity_net, obe_plug`), `opening_tb_targets` (`:1179-1214`: `line_key` unique per seed, **nullable** `account_code` = unmapped, `ck_opening_tb_targets_side` `:1203` admits exactly one positive side, `ck_opening_tb_targets_provenance` `:1205-1213` makes a `document` row carry `document_id` + `source_sha256` + an object `extraction_ref` and **no** `entered_by` and a `keyed` row the inverse, `uq_opening_tb_targets_extraction_fact_0017` `:1215-1219`), `opening_seed_approvals` (`:1220-1243`, append-only, `attestation_kind in (distinct_checker, self_approval_attestation)`), `lint_findings` (`:1319-1360`, kinds include `opening_tb_tie_broken` / `opening_doc_unfiled` at `:1325`).

Doors, exact signatures as granted (roster `0017:5125-5165`, re-censused by name at `0042:5385` and `0044:6354`): `create_opening_seed(uuid,uuid,date,uuid,text,text)` bookkeeper / `clara_authenticated` — the tie document must be an **active verified filing** (`clara._active_document_filing(p_document uuid, p_sha256 text, p_client uuid, p_lock boolean default false)`, `0007:982-1003`: filing not retired **and** `documents.bytes_verified_at is not null`, else CLR02) of kind `opening_balance_doc` or `management_account` (`0017:2913-2917`, CLR02), CLR31 `duplicate_seed` on the partial unique (`:2931`); `cancel_opening_seed(uuid,text,text)` admin — only an **empty** open seed (`:2988-2995`); `record_opening_target(uuid,jsonb,text)` bookkeeper — **refuses any tied seed** with CLR31 `parsed_target_writer_required` (`0017:3010-3016`) and upserts on `(seed_id, line_key)` otherwise; `record_opening_targets_parsed(uuid,jsonb,uuid,text)` **`clara_runtime` only, no `_human_ctx`** — re-derives every triple through `clara._assert_opening_target_fact(uuid,uuid,jsonb,text,bigint,bigint)` (`0017:1771-1801`, tokens `opening_target_account_mismatch` / `opening_target_cents_mismatch` / `opening_target_sign_mismatch`), which first calls `clara._assert_opening_extraction_ref(uuid,uuid,jsonb)` (`0017:1683-1727`, tokens `extraction_ref_malformed`, `extraction_ref_not_found`, `extraction_not_accepted` when `status <> 'done'` **or** `superseded_by is not null`, and `stale_extraction_version` when `documents.authoritative_extraction_id` is a different run, `:1723-1726`); `draft_opening_item(uuid,uuid,jsonb,jsonb,uuid,uuid,text,text)`; `seed_fixed_asset(uuid,uuid,jsonb,text,uuid)` (5-arg since the DROP+CREATE at `0018:188-191` — the 4-arg grant died with it); `record_opening_keyed_resolution(uuid,uuid,jsonb,text)` (`0018:94-150`); `get_opening_dryrun(uuid)` viewer (`:3739-3765` → `obe_net_cents`, `deltas`, `unmapped_labels`, `missing_must_asks`, `as_of`); `approve_opening_seed(uuid,uuid,text,jsonb,text,text)` admin — SERIALIZABLE asserted in body (`:3833-3836`) **and** proconfig-pinned since `0171:98-99`, plan-revision CAS (`:3857-3860`), client advisory rung `203005004` (`:3856`), per-entry revision tokens, distinct-checker / `self_attestation` (CLR05), `_assert_opening_tie` (`:3674-3697` — four arms: zero targets, any unmapped `account_code`, any nonzero delta → `tie_mismatch`; a nonzero OBE net → the **distinct** token `obe_not_nil`), `_assert_fa_baseline`, and a **re-run of `_assert_opening_target_fact` over every target** at `:3884-3891`; `approve_opening_correction(uuid,jsonb,text,text)` admin (`:4162`); `supersede_opening_item(uuid,jsonb,text)` bookkeeper (`:4047`, puts a finalized registry back to `open` at `:4078`); `reopen_opening_seed(uuid,text,text)` admin; `trial_balance_as_of(uuid,date)` SECURITY INVOKER, human + runtime.

**Live bodies that are NOT their creating file's text** (measure on the rig, never transcribe — SYNTHESIS K4): `approve_opening_seed` = `0017:3825` spliced by `0018 §3b` (the K5 `correction_draft_present` + `entry_count` guards, anchors asserted "exactly once" at `0018:293` and `:305`) and by `0056 §S9` (which stashes the live body at `0056:169-186` and asserts the `_assert_opening_tie` anchor appears exactly once), attribute-only ALTER at `0171:98`; `approve_opening_correction` = `0017:4162` + five splices (`0018`, `0041 §S1.8`, `0042`, `0043`, `0044`, `0056`, `0057`), attribute-only at `0171:100`; `_draft_opening_item_core` = `0017:3162` + `0018 §1` + `0041 §S4.5` + `0042 §S5.7/§S5.17`, censused at `0043:5095-5111` and `0045:8443-8480`; **`_approve_opening_entry(uuid,uuid,uuid,text,int)` = recut WHOLE at `0037:2410`**, not `0017:3784`; `persist_document_extraction(uuid,text,integer,jsonb,jsonb,text,text,text)` = `0007` + `0026:508`/`:531` + `0017:1553-1600+`'s opening-fact chain-of-responsibility + `0191`'s `_assert_field_path`.

**The closed-period wall is a TRIGGER, not a door check.** `clara._tf_period_wall()` (`0056:643-712`; trigger `t_period_wall` BEFORE INSERT OR UPDATE on `clara.journal_entries`, `:711-712`) takes `pg_advisory_xact_lock_shared(203005007, hashtext(client_id))`, finds the FY containing the row's `posting_date`, returns early when it is `open`/`reopened` or absent, and then refuses only an **approved-class touch** — an INSERT arriving approved, an UPDATE that approves, or any touch of an already-approved row — with CLR19 `write_into_closed_period` unless a `close_write_permits` row for this transaction exists. `_approve_opening_entry` flips `status='approved'` by UPDATE (`0037:2435-2437`), so the wall is reached **at approval and not at draft**. `approve_opening_seed` itself carries no period guard of its own; the seal wall lives on the other side (`0161:71-76`: `finalize_close` refuses while the seed is not `finalized`). **This is the B6 measurement — `p656.m3` — and DECISIONS §2 row 0228's conditional arm turns on it.**

**Capability registry.** `clara.document_capabilities` (`0191:200-216`: primary key `(format, document_kind)`, four closed-set level columns, `registry_version int not null check (>= 1)`, `basis text not null`, `limits jsonb not null default '{}'`), global, FORCE RLS (`:241`), `grant select … to clara_authenticated` (`:271`), **no write grant to any application role**; the agent reaches it only through `clara._document_capability(text,text)`. Every row is at `registry_version = 1`. Both `prior_gl` and `opening_balance_doc` carry `business_operation = 'stored_only'` (`0191:354`) with the basis sentence "The filing appears as work a person completes; Clara derives nothing to drive it" (`:404-409`). The column's own contract is "supported where Clara can carry typed facts into it; stored_only where the filing is work a PERSON completes … the honest 'no placeholder success'" (`:229-230`); the four levels are defined at `:24-30` with `supported` = *"Clara does this today, and a test proves it"* (`:25`); the table comment promises a professional "is never shown a success that did not happen" (`:217-218`); `limits` is the named-gap instrument (`:235-236`). `0207:124-148` adds the BEFORE UPDATE monotone trigger (CLR08 `registry_version_monotone`); its own comment records the **DELETE-then-INSERT hole as #846** and cross-row uniformity as convention — while `document-capability-registry.test.mjs` makes that convention **executable** in two cells.

**Runtime — the producer is written, tested and called by nothing; the consumer is live and unreachable from the browser.** `packages/runtime/lib/opening-tb-cells.mjs` (434 lines, **not** in `frozen-workflows.json`): `cellsToOpeningTb(cells)` returns `null` (not a trial balance — the conservative default: it returns `null` the moment it sees a `Code :` general-ledger block header, `:320-322`, or fails to learn a header carrying code + description + **both** amount columns, or sees a `date` column), `{status:'refused', reason, …}` (this IS a trial balance and cannot be used), or `{status:'ok', lines, regions, refusals, nilRows, totals, printedTotals, statedTotals}` (`:378-433`). Four refusal laws in its own words: any line-level refusal **forfeits the document** (`:400-404`), two different printed totals cannot be adjudicated (`:405-412`), ΣDr≠ΣCr is not a trial balance (`:418-419`), read-total ≠ printed-total is a dropped row (`:425-432`). `toRegion` (`:141-152`) emits exactly the element shape `persist_document_extraction(p_regions)` accepts — `field_path:'opening_tb.line'`, `monetary_cents` as a decimal string — and every emitted line is re-parsed through `parseOpeningTbLine`, the byte-for-byte mirror of the DB's `_derive_opening_region_fact` (`0017:1466-1505`). Its input shape is `{region_id?, text_content, locator:{polygon, page_number}}` (`table-cell-geometry.mjs:36-71`: `groupRows` needs the polygon and the page and silently drops anything unplaceable) — **which is exactly the shape `normalizeAzureLayout` has already built for every `tables.N.cells.M` region** (`egress.mjs:154-172`) one statement earlier in the same function.

`packages/runtime/lib/opening-parse.mjs` (the consumer, live): `parseOpeningTargets(client, {seedId, firmId, reassert})` (`:198-250`) → 404 (missing **or** foreign-firm seed, deliberately indistinguishable), 409 `registry_not_open`, 422 `no_tie_document`, 422 `namedUnparseableReason('opening_tb.line region(s)', …)` naming the failing region ids (`:90-94`), 422 `no_opening_tb_lines` (the honest keyed-fallback signal), else `record_opening_targets_parsed` under the stable key `openingOpKey(seed, document)` = `openingparse:<seed>:<document>` (`:97-99`) → 202 `{status:'parsed', lines}`. Its selector (`:103-131`) is **the newest done extraction that CARRIES `opening_tb.line` rows**, never `superseded_by is null` — the header records that this bug class shipped three times. `mapOpeningDbError` (`:166-178`) maps CLR11 → 404, CLR10 → 422 `unparseable`, CLR31 `registry_not_open` → 409, every other CLR31/CLR02/CLR28 → a typed 409 carrying `code` and `reason`. `packages/runtime/src/openingRoutes.ts:32` `POST /api/opening/parse-targets {seedId}` is **mounted** (`src/index.ts:115`), bookkeeper+ floor re-resolved on the same connection immediately before the audited write (F-H7), 503 while draining. **No web caller anywhere.** The same is true of `POST /api/seeding/prepare` (`seedingRoutes.ts:35`, mounted at `index.ts:116`) — whose reader `seeding-parse.mjs:377-378` **does** drive `clara.create_seeding_batch` off a filed prior GL today (proposals carry counterparty + account + date, **never an amount**, `prior-gl-cells.mjs:11-15`) and is proved by DB-backed cells (`packages/db/tests/wave-b-seeding-prepare.test.mjs:252-464`). **No workflow of any kind touches the opening lane** (`grep -l "opening_seed\|opening_tb" packages/runtime/workflows/*` → empty). The web reaches a runtime route through the same-origin proxy `apps/web/app/api/runtime/[...path]/route.ts`, which maps `/api/runtime/*` → the runtime's `/api/*` and allow-lists exactly `authorization`, `content-type`, `content-length`.

**Web — the tab is built; the document-tied half is unreachable end to end.** Route `/clients/:clientId/registers?tab=opening`, one tab of `RegistersWorkbench` (`registers-workbench.tsx:28` `TABS`, default `aging` at `:40`, `setTab` → `router.replace` at `:43-47`, deliberately absent from the sidebar `:58-64`). `opening-register.tsx` reads seeds + plans + chart + counterparties, picks the single non-cancelled seed, else `NoOpeningSeedState` (its `:66-75` records the `error={null}` defect where a 403 fell through to "no opening seed yet"). `opening-position-gate.tsx:82-96` renders three honest branches. `opening-seed-workbench.tsx` mounts the dialogs and the dry-run strip keyed by an act-epoch (`:136`, its header `:130-135` recording the count-based-remount defect), and **`OpeningTargetKeyedPanel` only when `!seed.tie_document_id` (`:138`) — a tied seed renders NOTHING there.** `opening-dryrun-strip.tsx:55-64` renders the four tie gates in `_assert_opening_tie`'s own order, each carrying the DB's own reason token, and **mints no arithmetic** (`:30-44` records the quiet-pass defect that law exists to prevent). `opening-seed-lifecycle.tsx:62` hardcodes **`tieDocumentId: null, tieSha256: null`**; its as-of input is a pre-`Field` `Label` + `Input` pair (`:66-70`). `opening-target-keyed-panel.tsx` columns are line-key / account / debit / credit — **no provenance column**. `lib/registers/opening-doors.ts:21-38` is `createOpeningSeed(session, {client, plan, asOf, tieDocumentId, tieSha256})`, fresh `crypto.randomUUID()` op key per call; `opening-types.ts:68-84` already types `provenance_kind`, `document_id`, `source_sha256`, `extraction_ref`, `entered_by`. The tie-document picker's data already exists: `lib/documents/reads.ts:46-50` `listActiveFilingsForClient(clientId)` and `:31-35` `listDocumentsByIds(ids)` whose `DOC_COLS` (`:21-23`) carries `sha256`, `original_filename`, `document_kind`, `bytes_verified_at`. `apps/web/components/ui/field.tsx` **is installed and used by 16 production components** (measured this pass — appendix D row 28 "Field — None" is a 2026-09-08 snapshot and is **stale**; record it, do not act on it). `messages/en.json` carries `OpeningCarryDown.{heading,noSeedYet,noPlanNote,dialog,openingPosition,seed,dryrun,items,itemFields,supersede,keyed,fixedAsset,approve}` (142 keys, single locale). `lib/journals/api.ts:63-66`'s `ENTRY_SELECT` omits `is_opening_balance` and `flags`, so an opening entry (`origin='manual'`, `is_opening_balance=true`, `flags={opening_seed_id, opening_item_key}`, `0017:3375-3384`) is indistinguishable from a hand-keyed JV on C3's own surface. `components/documents/capability-tiers.tsx:72-79` renders each `limits` entry as an outline `Badge` through `capabilityLimitUnknown({name, level})` — **your `limits` key and value are read by a professional, so keep them short and legible.**

**Tests.** DB (real per-role personas through `humanQuery`): `wave-b/wb-k-registry.test.mjs` (12 cells), `wb-k-approval.test.mjs` (15 — isolation, stale plan, stale entry token, maker=checker, per-line tie mismatch, OBE-not-nil, byte-identical replay, a two-session race, solo attestation, GATE-2 preflight and mid-mutation rollback), `wb-k-obwriter.test.mjs` (11 — including "the keyed fallback refuses under a tie" and the sen-exact dry-run), `wb-k-supersede-fa.test.mjs`, `wb-o-lifecycle.test.mjs`, `opening-balance-evidence-link.test.mjs` (#821's `obw.evidence_first`), `document-regions-unique-field-path.test.mjs` (pins `opening_tb.line` and `prior_gl.line` OUTSIDE the partial unique key), `document-capability-registry.test.mjs` (totality, the four-level closed set, the ONE-version census `:204-212`, and the #779 monotone probes `:386-465`, which write only inside a rolled-back transaction because "the battery's other cells assert registry-wide invariants", `:136-139`). Runtime: `kdoc-opening-tb-cells.test.mjs` (~25 cells), `kdoc-opening-tb-adversarial.test.mjs` (~11), `kdoc-opening-tb-e2e.test.mjs` (round-trip against the DB grammar), `wave-b-opening-parse.test.mjs`, `wave-b-prior-gl-cells.test.mjs`, `wave-b-seeding-prepare.test.mjs` — **all pure/unit; no real-Postgres leg for this journey.** Web unit: 14 files (`opening-tie-gates.test.tsx`, `opening-refusal-and-staleness.test.tsx`, `opening-a11y.test.tsx` 2 axe cells, `opening-keyboard.test.tsx` 2 cells, `opening-i18n-keys.test.tsx`, `lib/registers/opening*.test.ts`, …), manifest lines 217-225 and 369-371. **Playwright: none reaches `?tab=opening`** (`responsive-shell-walk.spec.ts:392-399` exercises the workbench at its default `aging` tab; `home-board-mock.mjs:61` and `agentic-finish-mock.mjs:254` stub `/rest/v1/opening_seed_registry` for other journeys only). The opening lane has **no cohort gate of its own**; `packages/db/package.json`'s chain holds **exactly 40** `--import ./tests/*-preintegration-gate.mjs` flags, in migration order, last `preview-invite-preintegration-gate.mjs`.

## 2. Gaps / rows

| Row | Disposition — and the evidence you must produce |
|---|---|
| **AC1** discover and document the current opening producer/operation before modifying it; preserve the unverified historical ambiguity rather than copying old amounts or ceremony labels | **to build — the census IS the deliverable, and it lands in the registry.** Three findings must survive into the migration and the report: (i) `cellsToOpeningTb` has **no production caller** (every import is a test; `parts-parity-exemptions.mjs:71` names the file but is a lint roster, not a caller); (ii) the consumer side is fully live (`openingRoutes.ts:32`, mounted `index.ts:115`), so the missing half is the **producer**; (iii) the two registry rows are wrong **for different measured reasons** (design lens F1) — `prior_gl`'s operation exists today and is proved (`seeding-parse.mjs:377-378`; route `index.ts:116`; cells `wave-b-seeding-prepare.test.mjs:252-464`), which is 0191's own bar for `supported` (`:25`), and lacks only a **browser entrance no open ticket owns**; `opening_balance_doc`'s operation does not exist at all until this slice wires it, so its correction ships **in the same migration as the wiring, never ahead of it**. Evidence: `p656.registry.prior_gl_operation` + `p656.registry.no_app_write` + the filed follow-up. **Preserve the ambiguity**: copy no historical amount, no old ceremony label. |
| **AC2** ingest a representative opening source; retain source/account mapping and unsupported rows; exact-cent balance with zero-value/non-meaningful proposals rejected | **to build (the wiring + the entrance).** Exact-cent balance and zero rejection are **already law and already coded** — `opening-tb-cells.mjs:418-419`, `:405-412`, `:425-432`; `opening-tb-grammar.mjs:36-63` (absence means EMPTY and nothing else; a parenthesised / negative / OCR-mangled figure is refused, never sign-flipped); a printed `0.00`/`-` is a `nilRow` that still claims its account code; `ck_opening_tb_targets_side` (`0017:1203`) forbids a zero or two-sided line at the table. The gap is the three broken wires: the producer's caller, `opening-seed-lifecycle.tsx:62`'s hardcoded `tieDocumentId: null`, and `opening-seed-workbench.tsx:138`'s empty tied-seed panel. Evidence: `p656.tie.parsed_writer_only`, `p656.tie.fact_must_match`, the World leg, the walk's create-with-document leg. Note for the picker: a `prior_gl` tie is **CLR02 today** (`0017:2913-2917`) and stays so — offer only `opening_balance_doc` / `management_account`. |
| **AC3** commit the opening journal/basis once under current client, authority and period rules; duplicate admission or cancelled onboarding cannot seed a second chart/opening effect | **verify-only, re-measured on the surfaces you change.** One-per-client (`0017:1117` → CLR31 `duplicate_seed` `:2931`), cancel frees the slot only when empty (`:2988-2995`), SERIALIZABLE pinned by migration (`0171:98-101`), plan-revision CAS (`:3857-3860`), advisory rung `203005004` (`:3856`), byte-identical replay (`wb-k-approval:177`), a second chart walled by `0173:8-16`. Owed: `p656.seed.duplicate_and_cancelled`, `p656.seed.replay`, **and the period answer from `p656.m3`**. |
| **AC4** partial or unsupported rows remain explicit Work children and do not turn an unknown tie into PASS; correction preserves original mapping and creates the governed replacement effect | **partial, with the reading fixed by D13.2.** Unknown-tie-never-PASS is built twice over (`0017:3680-3697` + `opening-dryrun-strip.tsx:30-64`) → `p656.tie.unmapped_blocks`, `p656.tie.obe_not_nil`. Correction is fully built (`supersede_opening_item` `0017:4047` + `approve_opening_correction` `:4162` + `ck_opening_items_supersede_state` `:1166`, the original readable at `state='superseded'`) → verify-only. **"Explicit Work children" closes as: the named refusal that lists every failing row, the unmapped row rendered as an ACTION (never a dash), and the existing `lint_finding` kinds** `opening_tb_tie_broken` / `opening_doc_unfiled` (`0017:1325`, `:4798-4808`) which already reach needs-you as `lint_finding` (`needs-you.ts:97-118`). **No `accounting_work` row, no new needs-you kind** (§3 and AC5's authority). |
| **AC5** A6 shows provenance, mapped/unmapped totals, exact discrepancy, Work and receipt; **no blanket opening approval ritual is added** | **partial + one element `descoped (authority)`.** Exact discrepancy: built (`get_opening_dryrun` `0017:3739-3765`) → verify-only. **Provenance: to build** (`provenance_kind` / `extraction_ref` are read into `opening-types.ts:75-81` and rendered nowhere). **Mapped/unmapped totals: to build — on the target panel's own footer**, over the rows it displays, **never inside `OpeningDryrunStrip`** (orchestrator ruling: the strip's law is "no numeral is minted and no tie is re-derived", `opening-dryrun-strip.tsx:30-44`, pinned by `opening-tie-gates.test.tsx`; a cent total is a coverage figure, not a tie figure, and C-25's defect was exactly a coverage figure worn as a tie). **Work and receipt: `descoped (authority)`** — see the binding section; a **named residual with an owner question** in the report. **No blanket ritual — hold the line**: the parse action is a plain action with a persistent outcome, never a gated one; do not widen `approve_opening_seed`'s attestation. **C3 half: to build** — `is_opening_balance` into `ENTRY_SELECT` + one badge + a link back to `?tab=opening`, ONE hunk in #655's file. |
| **AC6** the full state ladder, scope and exact values, 320px, 200% zoom, keyboard/focus return, SR names, reduced motion, stable URL/Back, preserved drafts, accepted shadcn/Base composition with a **persistent** outcome | **to build on every surface you touch.** Today: 2 axe cells, 2 keyboard cells, a refusal/staleness battery, an i18n-key battery, **no Playwright leg reaching `?tab=opening` at all**, no 320px / 200% / reduced-motion assertion on any opening surface, drafts unproven across a refusal. Evidence: the five web-unit files, plus the new walk at desktop **and** 320px with an axe scan, a 200%-zoom leg, a keyboard-only path and a focus-return assertion. **Field/FieldGroup for every NEW input** (design lens F4). |
| **AC7** prove the declared outcome through the production-facing read/command under real least-privileged roles and current migrations; **Workflow only where the journey invokes it**; label local vs hosted | **partial → build the real-Postgres leg.** The DB half is strong and already rides `humanQuery` personas. **No Workflow leg is owed for the opening verbs** — measured: no workflow touches the lane, and AC7's own sentence ("Database or Storage-only operations use their actual transaction/access boundaries") is the applicable clause. Say that plainly rather than claiming a World proof you did not run. **What you DO owe**: `packages/runtime/tests/opening-ledger-source-e2e.mjs`, a real-Postgres leg over the real route core and the real writer, because **nothing today proves producer → persist → cite → approve end to end**. Carry 0171's three named blind spots forward: the rig issues `begin isolation level serializable` at **session** level (`wb-fixtures.mjs:252-256`), so the proconfig pin is structurally invisible to the battery — the absolute instrument is the `dr-verify` cell, and it must **FAIL, never SKIP**, on a restored target. Local evidence only; "hosted evidence pending". |
| **CB-AE2E-004** opening approval impossible in production and silently failing (historical FIXED; refresh **REDESIGN**) | **verify-only, re-measured — never copied.** Structurally closed by `0171:5-27`, `:98-101` (proconfig set by migration and asserted by exact regprocedure) plus the operator hint at `opening-seed-workbench.tsx:118-124` when CLR31 `not_serializable` does surface. Owed on the surfaces you change: the **empty-versus-unknown** and **recovery** legs, and proof that the absolute `dr-verify` cell FAILS (not SKIPs) on a restored target. |
| **C-25** empty green-light gate (PARTIAL/DRIFT; refresh **VERIFY**) | **verify-only, re-measured.** The exact defect is fixed: four gates render in the DB's order with the DB's own tokens, after a measured defect where one gate painted a quiet pass and named the wrong token. **Re-measure specifically** that a dry-run read FAILURE renders through `DataState`'s error branch and never as a passed gate strip, and that your new mapped/unmapped totals do not resurrect the quiet pass. UNKNOWN is not readiness. |
| **C-30** opening trial-balance producer (CARRIES; refresh **REDESIGN**) | **the ticket's spine — `to build`.** Provenance, mapping, business objects and tie-out are all modelled (`opening_tb_targets.provenance_kind`, `extraction_ref`, `uq_opening_tb_targets_extraction_fact_0017`, `opening_items` + `journal_entries`, `_assert_opening_tie`) and the producer that would populate them is written and called by nothing (`opening-tb-cells.mjs:1-14`). Closing evidence: the in-line wiring + `p656.tie.*` + the World leg + the walk. "Old review-gated rollout labels do not define the new ceremony": the ceremony is `approve_opening_seed`'s distinct-checker door, **kept and not widened**. |
| **C-39** five capability gaps — map remap, schedule replacement/history and document-supported opening flow to direct UI **plus Clara** (PARTIAL; refresh **REDESIGN**) | **partial, deliberately.** Remap = supersede + governed correction with the original mapping preserved (built → verify-only); schedule replacement/history = `seed_fixed_asset(uuid,uuid,jsonb,text,uuid)` + `fixed_assets.supersedes_asset_id` + `_assert_fa_baseline` (built → verify-only; **#661/#651 territory — author no assets**); **document-supported opening flow = this slice's direct UI**; **"plus Clara" = the written successor contract + the follow-up ticket** (D13.4). "Thread switching alone never closes the group" — the direct UI closes it. |
| **COA seeded before cancelled onboarding** (explicit follow-up; source has no status column) | **verify-only + an owed COUNT, and no repair migration.** Recurrence is structurally closed (`0173:8-16`, `:28-31`; `0170:19-26`; `0017:2865`). The row's own instruction is *require actual current affected rows before data repair* → a **hosted** count (archived clients holding a non-empty chart with a cancelled plan; the opening seed is the second effect that count must consider). Write "hosted evidence pending"; ship no repair; retain the cancel/restart regression. |
| **C08.4** read the latest opening-balance operation and UI; test **mixed supported/unsupported batch handling and provenance**, not old migration `0018` wording | **partial → the test lands, the "mixed batch" reading is fixed by D13.2.** The "not old 0018 wording" instruction is satisfied by reading the splice history above rather than the file text. The row's actual test becomes: a document with one unreadable row produces **zero** targets and a refusal that **names that row**, and every recorded target renders its provenance. Cells: the producer's forfeit arm in `opening-tb-produce.test.mjs`, the web parse-refusal cell, and `p656.tie.fact_must_match`. |
| **C08.7** locate the `SeedingProposalRow` successor and prove proposal, evidence, refusal and applied-receipt states; do not restore a deleted component by name | **verify-only + one OWNED follow-up.** Successor located and named by its own migration: `apps/web/components/reports/SeedingBatchesPanel.tsx`, mounted on the client Reports tab under "Internal", reached from the ninth needs-you kind `seeding_proposal` (`0146:10-19`, `seeding-proposal-affordance.tsx:7-18`, `needs-you.ts:106-108`); the four states render through `DoorDialog` over `tick_seeding_proposal` / `decline_seeding_proposal` / `complete_seeding_batch` / `cancel_seeding_batch`, with an a11y + keyboard battery including the F1 regression pin (`reports-snapshots-seeding-a11y.test.tsx:184`, `:218`). **Do not rebuild it and do not restore any deleted component by name.** The measured residual is ownership: nothing in `apps/web` CREATES a batch and **no open ticket owns building that entrance** — your report **files it**, and `prior_gl`'s `limits` names it. |
| **C33.9** unadjudicated `opening_items` aggregate of +7,850,406 cents with no `obe_plug`; sign unknown | **verify-only (discovery) — the figure is carried as a HISTORICAL CLAIM ONLY.** The only instrument that may adjudicate it is `_opening_seed_obe_net` (`0017:3664-3672`), whose nonzero result refuses approval with the distinct token `obe_not_nil` (`:3693-3697`), read per seed through `get_opening_dryrun`. **Recompute on a rig or hosted; never transcribe.** Your rig recompute is local evidence; the hosted recompute is release-time work ("hosted evidence pending"). Never copy the amount as a present fact. |
| **C88.1** recover the current opening-position ambiguity and accepted decision from present SOT before any implementation | **verify-only (discovery) — resolved; nothing to re-adjudicate.** `commit_client_onboarding` accepts a finalized seed OR `first_year_zero_opening` OR `carry_down_deferred` (`0017:2807-2822`); the gate renders three honest branches (`opening-position-gate.tsx:82-96`, first-year-zero showing **no** Create trigger by design); #649 shipped the client-Home link on the deferred branch (`client-onboarding-progress.tsx:159-170`); `CONTEXT.md:178-180` states the term with its _Avoid_. Owed: **re-measure the three branches on any surface this slice changes** — and since you may not edit `client-home/*`, that is a walk leg following the existing link **read-only**. |

## 3. Slice (one branch)

**Migration `0228_opening_ledger_source.sql`**, written in this order: **house header** (spec of record #656 + this brief's binding section; the CONTEXT words; the sentence "this file creates no function and recuts nothing by default") → **prestate pins** → **the registry republication** → (no new objects, no grants, no events) → **tail** → **gate module** → **cohort**.

1. **Prestate (non-regression pins, `0195:390-409`'s idiom — `sha256(convert_to(prosrc,'UTF8'))` off `pg_proc`, each compared to the value you MEASURED on `clara_656` after `pnpm db:migrate`; a pin transcribed from file text will not match and 0228 refuses to apply).** Pin, as bodies this file must leave byte-identical: `clara.approve_opening_seed(uuid,uuid,text,jsonb,text,text)`, `clara.approve_opening_correction(uuid,jsonb,text,text)`, `clara._draft_opening_item_core(...)`, `clara._approve_opening_entry(uuid,uuid,uuid,text,int)`, `clara.persist_document_extraction(uuid,text,integer,jsonb,jsonb,text,text,text)`, `clara.record_opening_targets_parsed(uuid,jsonb,uuid,text)`, `clara._assert_opening_target_fact(uuid,uuid,jsonb,text,bigint,bigint)`, `clara._assert_opening_extraction_ref(uuid,uuid,jsonb)`, `clara._assert_opening_tie(uuid)` and `clara._tf_period_wall()`. Also assert the prestate facts the republication rests on: the monotone trigger `t_document_capabilities_version_monotone` is installed on `clara.document_capabilities` (`0207:146-148`), the `registry_version >= 1` positivity CHECK is on the column, and **every row currently reads `registry_version = 1`** (`select count(distinct registry_version) = 1 and min(registry_version) = 1`) — if that is false, the republication's arithmetic is wrong and the file must refuse rather than guess.
2. **The registry republication (AC1).** ONE `update clara.document_capabilities set registry_version = 2` over **every** row (the one-version invariant, above), and in the same statement or an adjacent one, for the two kinds only:
   - **`prior_gl` × every format → `business_operation = 'supported'`**, basis: *"A filed prior general ledger drives `clara.create_seeding_batch` through the deterministic reader `packages/runtime/lib/seeding-parse.mjs`; its proposals carry counterparty, account and date and never an amount."* — because the capability **exists and is proved today** (`0191:25`'s bar) — **and `limits` gains `{"browser_entrance":"absent"}`** with a sentence naming the follow-up ticket your report files, because `limits` is 0191's own instrument for a named gap inside a supported level (`:235-236`) and it is what keeps a `supported` row honest on `capability-tiers.tsx` while the entrance is unbuilt. `typed_facts` stays `stored_only` for `prior_gl` (`prior_gl.line` still has no producer) and `limits` says so.
   - **`opening_balance_doc` × pdf/image → `business_operation = 'supported'`** and **`typed_facts = 'supported'` for exactly the formats whose reader exists**, basis: *"A filed opening trial balance is read into `opening_tb.line` evidence regions and carried into the governed opening-seed door by `clara.record_opening_targets_parsed`; every figure is re-derived by the database."* — **and only because §3's runtime wiring lands in the same branch.** Shipping this row ahead of the wiring is the dishonesty `0191:217-218` exists to prevent (Risk 7). Leave every other format at its current level.
   **UPDATE only, never DELETE-then-INSERT (#846).** No `INSERT`, no `DELETE`, no new grant, no policy change.
3. **CONDITIONAL, and ONLY if `p656.m3` proves there is no period guard** (DECISIONS §2 row 0228): add the refusal as a **narrow recut of `approve_opening_seed(uuid,uuid,text,jsonb,text,text)`** — a string splice on a measured live pin, preserving `0018 §3b`'s and `0056 §S9`'s anchors verbatim (both assert "exactly once"), with the new refusal raised **before any write** and carrying a distinct `detail.reason` that names the fiscal year and the reopen path. **If `p656.m3` shows CLR19 `write_into_closed_period` already refuses at approval (the reading of `0056:643-712` + `0037:2435-2437` says it does), you write NO recut**: you record a **named residual** ("the opening approval's period wall is `t_period_wall`'s, so a draft can be authored into a closed period and only the approval refuses; the refusal names the fiscal year, not the opening basis"), you add the honest copy on the surface, you keep the cell as the pin, and your report files the follow-up. **Do not write this migration section before the measurement.**
4. **Tail.** Re-assert every prestate pin unchanged (this file recut nothing — or, under item 3, exactly one body and nothing else). Assert the two corrected rows read back at `registry_version = 2` with their new `business_operation` and their new `basis`, and that **`prior_gl`'s `limits` is non-empty and carries the named browser-entrance gap** (a `supported` business operation with `limits = '{}'` is precisely the placeholder success this correction exists to remove). Assert `count(distinct registry_version) = 1` and `min(registry_version) = 2` over the whole table. Assert **no application role gained any privilege on `clara.document_capabilities`** — re-prove 0191's posture after the UPDATE (`grant select … to clara_authenticated` and nothing else; `clara_runtime` and both agent read roles hold no write). Assert the monotone trigger is still installed and still `BEFORE UPDATE … FOR EACH ROW`.
5. **Gate module `packages/db/tests/opening-ledger-source-preintegration-gate.mjs`, battery `packages/db/tests/opening-ledger-source.test.mjs`, env var `CLARA_ALLOW_MISSING_OPENING_LEDGER_SOURCE` — ONE stem for all three, and the variable is COPIED character-for-character, never derived from a file name.** The stem is the migration's own stem `_opening_ledger_source$` (DECISIONS §2.2). The gate mirrors `preview-invite-preintegration-gate.mjs` verbatim: a four-line header saying why it is not a test file and why a FOCUSED run must fail loudly (`:1-10`), then the single statement `process.env.CLARA_ALLOW_MISSING_OPENING_LEDGER_SOURCE = "1";` (the shape of `:11`). The battery's `before()` hook reads **that same string** — the hook is `preview-invite.test.mjs:52-67` and the line to copy is `:57`. **Do not infer the variable from any stem, yours or anyone's.** Measured on this checkout: the estate has forty gate modules and the mapping is not a transform — `client-onboarding-identity-preintegration-gate.mjs` sets `CLARA_ALLOW_MISSING_CLIENT_ONBOARDING_FACTS`, `fixed-asset-acquisition-preintegration-gate.mjs` sets `CLARA_ALLOW_MISSING_FA_ACQUISITION`, `firm-setup-preintegration-gate.mjs` sets `CLARA_ALLOW_MISSING_FIRM_SETUP_0218`, `rs-guard-preintegration-gate.mjs` is read by `name-only-guard.test.mjs`, `knowledge-0192-preintegration-gate.mjs` by `knowledge-records.test.mjs`, `document-source-revision-preintegration-gate.mjs` by `rig-docs-source-revision.test.mjs`, and `document-capability-preintegration-gate.mjs` by three batteries carrying three different stems. A gate and a battery that agree only by pattern are an unpaired gate, and an unpaired gate has one failure mode: an estate sweep against a pre-0228 chain **hard-fails every cell** instead of loud-skipping them — precisely what the mechanism exists to prevent (`preview-invite-preintegration-gate.mjs:1-10`). Append ONE `--import ./tests/opening-ledger-source-preintegration-gate.mjs` to `packages/db/package.json`'s chain **in MIGRATION order, after `--import ./tests/preview-invite-preintegration-gate.mjs`** (measured: the chain holds exactly **40** such flags and `preview-invite` is the last, so yours is the 41st and last). **Your battery's premise probe is not a `to_regprocedure` check** (0228 installs no function): it is *"the registry publishes `registry_version = 2`"*, read in that `before()` hook exactly as `preview-invite.test.mjs:55-66` reads its door — throwing loudly when `CLARA_ALLOW_MISSING_OPENING_LEDGER_SOURCE` is unset (the focused shape, which is the acceptance shape) and setting `ready = false` when it is set.
6. **`rig-meta.mjs` cohort**: one 0228 block, **comment-only** (DECISIONS §2.2 admits this for 0228) — 0228 installs no granted name and recuts none, so the cohort's content is the statement of that fact plus the re-affirmation that `clara.document_capabilities` carries no app-role write.

**Runtime.**
1. **NEW non-frozen module `packages/runtime/lib/opening-tb-produce.mjs`** — the only new runtime file. It exports one function that takes the table-cell regions `normalizeAzureLayout` has just built (elements whose `field_path` starts with `tables.`) and returns `{ regions, refusals, totals, reason }` by delegating to `cellsToOpeningTb`. It **adds no grammar of its own** (the reader already round-trips every line through `parseOpeningTbLine`), **never throws** (a producer fault must not fail an OCR pass that is otherwise good), and returns an empty region set with a named reason on `null` (not a trial balance) and on `{status:'refused'}` (it is one and cannot be used). Header states the freeze-by-closure warning verbatim: *any future Clara opening tool that imports this module freezes it — durable rules belong in `0228`/`0017`, not here.*
2. **The in-line wiring** in `packages/runtime/lib/egress.mjs`'s `normalizeAzureLayout(payload, task)`: after the `result.tables` loop has built its cell regions and **before** the return, run the producer over those regions and append its `opening_tb.line` regions to the same `regions` array. **Kind-blind by construction and that is accepted**, because (a) the reader must POSITIVELY identify a balancing trial balance and returns `null` otherwise (`opening-tb-cells.mjs:305-322`, `:378-390`), and (b) an `opening_tb.line` region is **inert** until an opening seed ties that document, and the door re-checks the document's kind at that moment (`0017:2913-2917`). Touch nothing else in that function — the two page-key spellings (`page` and `page_number`) and every existing `field_path` stay byte-identical, because `0191`'s `_assert_field_path` grammar and the F-A1 witness estate both read them.
3. **`openingRoutes.ts` and `opening-parse.mjs` need NO edit** — the consumer already selects the newest done extraction carrying `opening_tb.line`. Prefer reusing `POST /api/opening/parse-targets` **verbatim** from the web. Add a read-only preflight **only if the walk proves it necessary**: `GET /api/opening/parse-preview?seedId=` returning `{lines, totals, refusals}` **without writing**, same bookkeeper+ floor, same 404 masking, same F-H7 re-assert discipline. Default: do not add it.
4. **No successor, no `registry.ts` edit, no frozen file.**

**Web** (no new route, no `tree.ts` row).
1. **`CreateOpeningSeedDialog` gains a tie-document picker** (`opening-seed-lifecycle.tsx:62`): a bounded list of this client's **active verified filings** of kind `opening_balance_doc` / `management_account`, composed from `listActiveFilingsForClient(clientId)` ⋈ `listDocumentsByIds(ids)` and filtered on `document_kind` + `bytes_verified_at != null`; each row shows filename, filed date and the first 12 characters of the sha. Selecting one sends **both** `tieDocumentId` and `tieSha256` (the door's XOR guard is CLR10 otherwise). **"No document — I will key the balances" stays an explicit second choice, never a silent fallback.** **Composition (design lens F4, GAP-ORDER §2):** every NEW input is a `Field` inside a `FieldGroup` with `FieldLabel` / `FieldDescription` / `FieldError` carrying the label, the filed-date + sha hint and the refusal; the dialog's existing as-of input (`:66-70`) moves into the same `FieldGroup` so one dialog does not carry two compositions. **The rest of the opening dialogs keep their pre-`Field` composition** — a whole-lane retrofit is #900's shape and is a named residual.
2. **A `Read this document` action on a tied seed** in `opening-seed-workbench.tsx`, calling `POST /api/runtime/opening/parse-targets` (the same-origin proxy; the runtime route is `/api/opening/parse-targets`) with a Bearer session JWT and `redirect: "manual"`, through the house runtime-wire discipline (`lib/documents/runtime-wire.ts`: a genuine network failure surfaces typed, a deliberate abort stays distinguishable, raw body text is never quoted). Its result is a **persistent outcome, never a toast**: a parsed-N-lines banner, or the producer's/route's named refusal **verbatim** with its counts and failing row keys. **It is a plain action, not a gated one** (AC5).
3. **`OpeningTargetDocumentPanel`** — the tied-seed sibling of `OpeningTargetKeyedPanel`, currently absent (`opening-seed-workbench.tsx:138` renders nothing for a tied seed). Columns: line key; **source label exactly as printed**; account (mapped, or **unmapped rendered as an ACTION, not a dash**); debit; credit; **provenance** (document vs keyed, with the document name and the region id out of `extraction_ref`). **Its footer carries the mapped / unmapped totals** (count **and** cents, both sides), labelled as coverage — never a percentage, never inside the dry-run strip.
4. **Provenance on the seed header**: "from `<document>` (sha `<12>`), read `<date>`" for a tied seed; "keyed by `<n>` of your firm" for an untied one.
5. **C3 seam — ONE hunk in `apps/web/lib/journals/api.ts`**: `is_opening_balance` added to `ENTRY_SELECT` (`:63-66`) plus one badge on the entries table, linking back to `/clients/:clientId/registers?tab=opening`. Nothing else in #655's file.
6. **States on every touched surface**: loading (shape-matched `Skeleton`); successful-empty (a seed with no targets yet); no-results (a document that parsed to zero lines — 422 `no_opening_tb_lines`, the honest keyed-fallback signal, **not** an error); partial/stale (targets recorded, then the tie document's extraction superseded → the DB's `stale_extraction_version` rendered verbatim through `StateBanner`); invalid/saving (`aria-invalid` + `FieldError`, **the typed as-of and the picked document survive a refusal**); denied (viewer floor, CLR04 — names the restriction, offers no fake retry); failed (the CLR31 family verbatim with its code); cancelled-recovery (cancel an empty seed, create a new one). 320px; 200% zoom; keyboard + focus return after every dialog; SR names for the provenance and gate cells; `prefers-reduced-motion`; **`?tab=opening` stable across reload and Back** (`registers-workbench.tsx:43-47` uses `router.replace`, so it creates no history entry — reload-stability is what the walk asserts and Back returns to the previous page); drafts preserved across a refusal in the create and target dialogs. New keys under `OpeningCarryDown.source.*` in `messages/en.json` at the sorted position.

**Docs.** `CONTEXT.md` — the four ratified terms in the house "term / _Avoid_" shape: ***Opening basis*** (the one versioned set of opening balances a client's books start from, with its source, its mapping and its tie; _Avoid_: calling a single journal entry "the opening basis"; treating a keyed basis and a document-read basis as different kinds of truth); ***Opening source*** (the prior GL or trial balance a firm receives, filed as a document and bound to the basis by identity and hash; _Avoid_: "the opening file"; treating a re-uploaded copy as the same source); ***Opening target*** (one line of the printed source, mapped or not yet mapped to an account; _Avoid_: calling an unmapped target an error; calling a target a journal line); ***Provenance (document / keyed)*** (whether a target came from stored evidence on the bound document or from a named professional's keying; _Avoid_: showing a keyed figure as if it had been read from the document). READMEs: `packages/db/README.md` (the corrected capability rows and why the two corrections differ; the `record_opening_target` vs `record_opening_targets_parsed` split and its floors; the whole-registry republication and the census that forces it), `packages/db/tests/README.md` (the new battery, its cells and its cohort gate), `packages/runtime/README.md` (the producer's home, the in-line integration point and the freeze-by-closure warning), `apps/web/README.md` (the tied-seed entrance and the parse action's persistent outcome). **Never `docs/PRD.md` or `docs/ARCHITECTURE.md`.** Record as **blueprint drift** in the report, do not edit: `ARCHITECTURE.md:207-211` (calls `opening-tb-cells.mjs` the 真实 producer of `opening_tb.line` and marks the block *[已实现，hosted evidence pending]* while no production code calls it — **your merge makes the sentence true**); `ARCHITECTURE.md:535` (the §7 row contradicts §5.A's `<!-- #821 -->` block at `:276-283`, on your own cross-reference which a doc-sync ticket must confirm with the owner; and its right cell is wrapped in a `<!-- #764 -->` block whose body is about the chat lane — one row, two subjects, one tag); `ARCHITECTURE.md:293` (says `chatTurn_v19`; the registry pins `chatTurn_v20`, `registry.ts:173`); `PRD.md:108` (客户建立与期初导入 listed under 当前版本已交付 while the 导入 half is unreachable from a browser today — **your merge changes that and the line needs a qualifier**).

**Successor contract (written, NOT cut this wave).** D13.4 rules no Clara entrance; **#656 appears in no row of DECISIONS §1.2**, so the `chatTurn_v21` cut carries none of this — it belongs to the follow-up ticket, which must start from written text rather than from scratch. Put this stanza in your report verbatim:
- **Class**: `chatTurn` → a future `chatTurn_vN` (not this wave's v21).
- **Tool** `read_opening_source`, `.strict()` zod input `{ client_id: uuid, seed_id: uuid }` — **no amounts, no account codes and no document id from the model**: the seed already binds its tie document, and letting a model name either would put a figure in the model's hands.
- **Door call**: the tool calls the existing route core `parseOpeningTargets(client, { seedId, firmId, reassert })` on a `clara_runtime` connection, **in that argument order**. It must **not** call `clara.record_opening_targets_parsed` directly.
- **Refusal mapping**: 404 → `not_found`; 409 `registry_not_open` → "this opening basis is not open"; 409 `refused` + the CLR31 reason **verbatim**; 422 → the named unparseable reason **verbatim with its counts and failing region ids** (the producer's whole value is that it says which rows it could not read).
- **Part kind**: reuse the existing typed receipt part; **no new wire kind.**
- **Prompt stanza**: one paragraph — Clara may ask to read an opening source a human has already attached to an opening basis; she may never state an opening figure that did not come back from the door; she surfaces a refusal rather than a partial reading.

## 4. TDD seams (red first)

**Measure first, on `clara_656`, before one line of SQL or TypeScript is written.** These are the gap map's "unverified" items plus SYNTHESIS §7.4's list for #656, and each one changes what you write:

- **`p656.m1.registry_version_census`** — run `packages/db/tests/document-capability-registry.test.mjs` **green, unchanged**, then read `select count(*), count(distinct registry_version), min(registry_version) from clara.document_capabilities` and the row count per `(format, kind)`. This decides whether the republication is whole-registry (this brief's ruling) and which cells you re-base. **If the census does not behave as the binding section reads it, STOP and report.**
- **`p656.m2.authoritative_race`** — `_tf_set_authoritative_extraction_0017` (`0017:1506-1546`) hands the pointer to the newest done extraction, **kind-blind**. Drive a producer run, then a `doc_classify`-shaped extraction on the same document, and measure whether a recorded target then refuses `stale_extraction_version` **or** `extraction_not_accepted` (`_assert_opening_extraction_ref` refuses on both arms, `0017:1719-1726`). `0177_classify_after_extraction.sql` orders classify after extraction, but **whether a re-classification after a producer run is possible is unverified.** If it is, ordering becomes load-bearing: say so, keep the in-line wiring (D13.1 is a ruling, not a preference), and name the residual.
- **`p656.m3.period_wall`** — build a `clara.fiscal_years` row with status `closed` covering the seed's `as_of` (the wave-b fixtures build none), draft an opening item, then approve the seed. Record **exactly** what refuses, at which step, with which errcode and `detail.reason`. Expected from source (`0056:643-712` + `0037:2435-2437`): the draft succeeds and the approval refuses CLR19 `write_into_closed_period` from `t_period_wall`. **This measurement decides §3 item 3** — a guard that fires means NO recut, a named residual and honest copy; no guard at all means the narrow recut with measured pins.
- **`p656.m4.live_pins`** — take every prestate sha off `pg_get_functiondef` / `sha256(prosrc)` on the rig for the ten bodies in §3 item 1. **Never transcribe from file text**: four of them are demonstrable splices (`approve_opening_seed`, `approve_opening_correction`, `_draft_opening_item_core`, `_approve_opening_entry`, plus `persist_document_extraction`), and a transcribed pin refuses to apply.
- **`p656.m5.activity_ladder`** (cheap, one read) — whether `opening_seed.batch_approved` is filed under `documents` by `list_activity`'s live body. **Do not recut the ladder** (#861 owns it; 2026-09-15 D13 stands); record the residual either way.
- **`p656.m6.region_counts`** — the present count of `opening_tb.line` and `prior_gl.line` regions, and C33.9's `opening_items` aggregate, recomputed **through `get_opening_dryrun`** on the rig. `opening-tb-cells.mjs:12-13` and `prior-gl-cells.mjs:7-8` both state "0 rows on live" as a **past** measurement. The hosted counts are release-time work: "hosted evidence pending".
- **Closed by ruling, not by measurement** (state this, do not spend a cell): the three migration-numbered CHECK names `ck_processing_task_lane_*` / `ck_processing_task_lane_engine_*` / `ck_document_extractions_engine_kind_*`, whose live names were read off `0090` and never verified — the routed arm is refused (D13.1), so **you never drop, re-add or transcribe any of them**; and whether a new shadcn primitive is needed (**none** — `components/ui/field.tsx` is installed with 16 production consumers; appendix D row 28 "Field — None" is a stale 2026-09-08 snapshot; the **retrofit** of the other opening dialogs is #900's shape and stays a named residual).
- **Single-sourced anchors — re-open before you rely on one.** gap-656's citation lens sampled ~50 of ~200 citations. Reviewed by ONE pass only, and therefore to be re-opened in source before you build on them: the DB doors table's splice history beyond `approve_opening_seed` / `approve_opening_correction` / `seed_fixed_asset`, the ~30 test-file names, the Overlaps table's shared-paths column, and appendix C/D's rows against the web components they name. Three anchor corrections the lens already made and this brief carries: `opening-seed-workbench.tsx:138` (not `:140`), `openingRoutes.ts:32` (not `:35` — but `seedingRoutes.ts:35` **is** correct), `opening-tb-cells.mjs:419` (not `:418`).

**DB battery `packages/db/tests/opening-ledger-source.test.mjs`** — ONE stem with its migration and its gate module, and its `before()` hook tests **`process.env.CLARA_ALLOW_MISSING_OPENING_LEDGER_SOURCE !== "1"` verbatim**, the exact string the gate sets (§3 item 5; the pairing is by that string, never by a shared spelling). Its cohort block is §3 item 6's `rig-meta.mjs` entry. Every assertion under test drives a real least-privileged persona through `humanQuery`, never `rootQuery` (fixtures and deliberately-invisible facts only). **Every `opening_tb.line` region fixture is created through `clara.persist_document_extraction`** (#857's rule, adopted here).

1. `p656.registry.prior_gl_operation` — the two corrected rows read back at `registry_version = 2` with their new `business_operation` and new `basis`; **`prior_gl`'s `limits` is non-empty and names the missing browser entrance** (a `supported` row with `limits = '{}'` fails this cell — it is the instrument that keeps AC1's correction from becoming the placeholder success `0191:217-218` forbids); a DOWNGRADE attempt is refused by 0207's trigger with CLR08 `registry_version_monotone`; `count(distinct registry_version) = 1`. *Red: the rows read `stored_only` at version 1 today.*
2. `p656.registry.no_app_write` — after the UPDATE, no application role holds INSERT/UPDATE/DELETE on `clara.document_capabilities`; `clara_authenticated` still holds SELECT; the agent still reaches it only through `clara._document_capability(text,text)`. *Red-by-construction guard: this is the cell that catches a republication written with a convenience grant.*
3. `p656.tie.parsed_writer_only` — on a **tied** seed, `record_opening_target(uuid,jsonb,text)` refuses CLR31 `parsed_target_writer_required` as `clara_authenticated`; `record_opening_targets_parsed(uuid,jsonb,uuid,text)` is refused `42501` to `clara_authenticated` and admitted to `clara_runtime`. *Red-first as the wiring's premise: it is the fact that makes the producer the only road in.*
4. `p656.tie.fact_must_match` — a `p_lines` element whose account / cents / side disagrees with the stored region fact refuses with the three field-specific reasons `opening_target_account_mismatch`, `opening_target_cents_mismatch`, `opening_target_sign_mismatch`; assert **nothing was recorded** by counting rows, never by matching a message.
5. `p656.tie.stale_extraction` — a target citing a **non-authoritative** extraction refuses `stale_extraction_version` (and a superseded one `extraction_not_accepted`); re-running the producer makes a newer extraction authoritative and the same cite then succeeds. **This is the cell that proves the producer and the DB agree about which extraction wins** (Risk 2 / `p656.m2`). *Red: there is no producer to re-run today.*
6. `p656.tie.approve_rebinds` — `approve_opening_seed` re-runs `_assert_opening_target_fact` for every target (`0017:3884-3891`), so mutating a region between parse and approval refuses. Assert nothing was approved by counting rows.
7. `p656.tie.unmapped_blocks` — one target with a null `account_code` refuses `tie_mismatch`, and `get_opening_dryrun` names it in `unmapped_labels`.
8. `p656.tie.obe_not_nil` — a basis whose OBE net is nonzero refuses with the **distinct** token `obe_not_nil` (C-25's own lesson: the face must not name `tie_mismatch` for this arm).
9. `p656.seed.duplicate_and_cancelled` — a second live seed raises CLR31 `duplicate_seed`; cancelling an empty seed frees the slot; a cancelled onboarding cannot produce a second opening effect (`0173:8-16`).
10. `p656.seed.replay` — the parse op key `openingparse:<seed>:<document>` replays byte-identically and records no second target.
11. `p656.roles.viewer_denied` — a viewer cannot create a seed, record a target or approve; a below-floor caller **cannot distinguish a foreign firm's seed from a missing one**.
12. `p656.period.closed_fy` — the pin for whatever `p656.m3` measured: the refusal's errcode, `detail.reason` and the step it fires at, with the draft/approve row counts asserted. *Red first: no existing cell builds a `clara.fiscal_years` row for this lane.*

**Runtime.**
- `packages/runtime/tests/opening-tb-produce.test.mjs` (pure): the wiring module returns `{regions: []}` **plus a named reason** on a refusal and on `null`, **never throws**, never a partial set; a balanced synthetic trial balance yields regions whose `field_path` is `opening_tb.line` and whose `monetary_cents` round-trips through `parseOpeningTbLine`; a general-ledger fixture (the `Code :` block header) yields `null` and appends nothing. Reuse `tests/kdoc-opening-tb-testkit.mjs`'s `cell` / `HEADER` / `tbRow` / `BALANCED` builders — **the geometry there is measured from a real Malaysian GL and a synthetic layout with invented coordinates would prove nothing.**
- Extend `packages/runtime/tests/wave-b-opening-parse.test.mjs` with the stale-extraction and all-or-nothing arms against the real selector.
- **The real-Postgres leg `packages/runtime/tests/opening-ledger-source-e2e.mjs`** (standalone, not collected by `node --test`), registered with ONE step in `.github/actions/db-live-gates/action.yml` after the existing legs. **Copy `tests/prepayment-occurrence-e2e.mjs:59-75`'s fail-closed gate verbatim** — `LOCAL_HOSTS` + `ALLOWED_DB = /^clara_(rt_test|wave_b_ci|[0-9]{3,4})$/` + the parsed-DSN check — so it runs on `clara_656`; **do not copy `intake-e2e.mjs`'s allowlist, which admits only `clara_rt_test|clara_intake_ci` and would throw on your rig.** It must SKIP CLEANLY (exit 0, printed reason) when 0228 is absent. Legs: file an opening-balance fixture → run the producer through the real OCR normalizer → assert `opening_tb.line` regions exist on the **authoritative** extraction carrying the exact triple → `POST /api/opening/parse-targets` → assert `targets_recorded` and every row's `provenance_kind='document'` → approve → assert the approved entries, the `opening_seed_approvals` rows, `state='finalized'`, `tie_asserted_at` and both domain events. **This closes AC7's named hole.** Exact command (from `packages/runtime`): `PGHOST=127.0.0.1 PGPORT=55706 PGUSER=postgres PGDATABASE=clara_656 RELAY_TEST_MODE=1 WORKFLOW_POSTGRES_URL=postgres://postgres@127.0.0.1:55706/clara_656 node tests/opening-ledger-source-e2e.mjs`.

**Web unit** — five files, each added to `apps/web/test/manifest.txt` at its sorted position (alphabetical by plain string compare; an unregistered file silently never runs, and `pnpm --filter @clara/web lint` checks it):
- `components/registers/opening-create-tie-picker.test.tsx` — the picker lists only active verified filings of the two admitted kinds, sends **both** id and sha, keeps the typed as-of across a refusal, and renders **every new input through `Field`/`FieldGroup`** with its own accessible name and its refusal in `FieldError` (the testable half of design-lens F4).
- `components/registers/opening-target-document-panel.test.tsx` — provenance renders for document and keyed rows; an unmapped row renders as an **action**, not a dash; the footer's mapped/unmapped counts and cents are correct and carry no percentage.
- `components/registers/opening-parse-action.test.tsx` — a 422 named refusal renders **verbatim** with its counts and failing row keys, as a persistent block (never a toast), and the seed stays usable; a 403 renders the denied state, not "no opening seed yet".
- `components/registers/opening-dryrun-unchanged.test.tsx` — the existing `openingTieGates` behaviour is untouched by the new totals, and a dry-run read FAILURE still renders through the error branch rather than as a passed gate strip (C-25's re-measure).
- `lib/journals/opening-badge.test.ts(x)` — an `is_opening_balance` row renders its badge and its link back to `?tab=opening`; a non-opening row renders neither.

**Playwright** — new `apps/web/e2e/opening-ledger-source-walk.spec.ts` + `apps/web/e2e/opening-ledger-source-mock.mjs`, **the first walk ever to reach `?tab=opening`**, on the triple `CLARA_E2E_APP_ORIGIN=https://127.0.0.1:3350 CLARA_E2E_NEXT_PORT=3351 CLARA_E2E_RUNTIME_PORT=3352`. Register it with ONE import + ONE dispatch line in `e2e/serve-built.mjs` for the Supabase handler and ONE more in the `startMockRuntime` dispatcher (`serve-built.mjs:1057-1073`) for the runtime handler, and ONE row in `e2e/e2e-fixture-ownership.test.ts` (`LANE_MOCKS` at `:53`, plus a `LANE_DECLARATIONS` entry `{unscopeable: [], debt: []}` at `:216`). **Read POST bodies only through the shared `readCachedJson` from `mock-dispatch.mjs`, never a private reader**, and mint a client id no other lane mints (the client-id census at `:1421`). **The verbs the mock answers, all exclusive to this lane (SYNTHESIS §3.3), so none needs a `SHARED_RPC_VERBS` row — declare them anyway in the ownership row**: relations `opening_seed_registry`, `opening_items`, `opening_tb_targets`, `onboarding_plans`, `onboarding_plan_items`, `coa_accounts`, `counterparties`, `documents`, `document_filings`; RPC `rpc/create_opening_seed`, `rpc/record_opening_target`, `rpc/get_opening_dryrun`, `rpc/approve_opening_seed`, `rpc/cancel_opening_seed`; runtime `POST /api/opening/parse-targets`. **If any verb turns out to be answered by a sibling mock, add a `SHARED_RPC_VERBS` row with the reason** (`:1106`) — the census reds otherwise. Legs: land on `?tab=opening` **by following the existing client-Home deferred link read-only** (C88.1's re-measure) → create a seed **with** a tie document → run the parse → see targets with provenance and the four tie gates → map an unmapped row → approve → see the finalized badge and the persistent outcome; a reload keeps `?tab=opening` and Back returns to the previous page; then the same walk at **320px** with an axe scan, plus a 200%-zoom leg, a keyboard-only path and a focus-return assertion after each dialog.

**Census suites that red on files you never touched — register, do not fight them**: `apps/web/tests/sql-oracle.test.ts` (it scans SQL surface — 0228 adds no function, so expect a no-op and **say so with the counts**), `tests/parity-holes.test.ts`, `tests/firm-scope-surfaces.test.ts`, `tests/firm-scope-fourth-entrance.test.ts` (no new file under `app/**`, so expect green), `e2e/e2e-fixture-ownership.test.ts` (the new mock and its verbs), `lib/navigation/tree.test.ts` (no href change expected). On the DB side: `packages/db/tests/document-capability-registry.test.mjs` (**yours to re-base, in the same commit, with the reason**), `document-regions-unique-field-path.test.mjs` and `packages/db/deploy/0191-field-path-census.sql` (the producer roster — `opening_tb.line` is already a registered namespace, so expect green; the deploy census is an operator probe for the hosted release, not a repo runner), `0042:5385` / `0044:6353-6356`'s name and verb rosters (green — you add no granted name), `0103:1055-1070`'s single-`pg_proc`-row overload census (green — you install no name), `operation-census.test.mjs` and `rig-isolation.test.mjs` (run both **without** the reset flags anyway: they are also the evidence for "this file installs no function").

## 5. Risks

1. **The producer is the single most consequential reader in the estate, and this ticket turns it on.** A wrong opening balance is invisible at every downstream checkpoint — `opening-tb-grammar.mjs:36-63` records the exact failure (`9OO.00` read as an absence turned a two-sided row into a clean, balanced, wrong one the database accepted without complaint). **Do not relax one refusal law to make a fixture pass**; every relaxation must be argued in the header the way the existing ones are.
2. **Authoritative-extraction contention.** `_tf_set_authoritative_extraction_0017` (`0017:1506-1546`) hands the pointer to the newest done extraction, **kind-blind**; a `doc_classify` run landing after the producer's would take it and every recorded target would then fail at approval (`0017:1723-1726`). `p656.m2` / `p656.tie.stale_extraction` is the instrument; if it reds, ordering is load-bearing — name it, do not silently re-order anything.
3. **The registry census is the merge hazard nobody expects.** `document-capability-registry.test.mjs`'s one-version cells red the moment two rows move alone; the fix is the whole-registry republication **plus** the same-commit re-base with the reason (precedent `af3b5955`). A reviewer who sees only the two-row UPDATE will reasonably think §2 row 0228 was widened — **write the reason into the migration header, the battery diff and the report.**
4. **`approve_opening_seed` is pinned SERIALIZABLE and its race against the evidence lane is unmeasured (#854).** Adding a document-tied path **enlarges the contention surface**, because the tie document becomes a genuinely live document with filings and extractions. Do not claim the lane's concurrency is proven; add no cell that contradicts #854's framing.
5. **Constraint names carry migration numbers** (`ck_processing_task_lane_0038` → `_0090`), and a migration that drops a transcribed name fails on apply. D13.1 removes that whole hazard by refusing the routed arm — **so the mitigation is discipline, not a prestate read: do not touch those three CHECKs at all.**
6. **Every pin must be measured.** Four bodies in this blast radius are splices (`approve_opening_seed`, `approve_opening_correction`, `_draft_opening_item_core`, `_approve_opening_entry` — plus `persist_document_extraction`); `0018` and `0056` each assert their splice anchors occur **exactly once**, so the conditional recut in §3 item 3 must preserve them byte-for-byte or the from-scratch chain refuses.
7. **Merge collisions, by exact file.** Real: `apps/web/lib/journals/api.ts` (**#655 owns it**; your ONE hunk, your own commit) and the `clara.document_capabilities` rows (**#782 owns the invoice `limits`**; ratify one `registry_version` bump between you — your republication moves **every** row's version, so if #782 lands in the same integration it must ride version 2, not mint a 3). One-line registrations shared with nine lanes: `apps/web/messages/en.json`, `apps/web/test/manifest.txt`, `apps/web/e2e/serve-built.mjs`, `apps/web/e2e/e2e-fixture-ownership.test.ts`, `packages/db/tests/rig-meta.mjs` (**the `];` repair is the known hazard**), `packages/db/package.json` (**migration order, not alphabetical**), `.github/actions/db-live-gates/action.yml` (per-line `\` continuations), `CONTEXT.md`. **No `tree.ts` change is expected** — the opening tab is deliberately not in the sidebar.
8. **A registry correction is owner-visible truth, and the rule is NOT the same for the two rows.** `capability-tiers.tsx` shows the basis sentence and the `limits` badges to a professional, and `0191:217-218` promises that professional "is never shown a success that did not happen". `opening_balance_doc`'s UPDATE ships **in the same migration as the wiring, never ahead of it**; `prior_gl`'s ships as `supported` **plus** the named `limits` gap **plus** the follow-up ticket. Leaving `prior_gl` at `stored_only` is **refused, not chosen** (D13, §2 row 0228).
9. **AC5's "no blanket approval ritual" cuts both ways.** Adding a second confirmation for the *document read* would be exactly the ritual the AC forbids: the parse action is a plain action with a persistent outcome.
10. **Known Windows-only reds you must NOT "fix"**: #707 (x56-rest-c shells out to grep), #693 (EICAR fixture quarantined by Defender), no `pg_dump` on PATH (four runtime files), `thread-live-clarify.test.tsx`'s whole-suite load flake (re-run it alone and report both), `rig-isolation.test.mjs` T10b after a Workflow World bootstrap (#866). `next build` may panic `0xc0000142` under ten-lane contention — retry once (#869). `npx playwright test` alone serves a **STALE** build (#865) — always go through `pnpm --filter @clara/web e2e`.

## 6. Effort and rig

**Effort: L** (DECISIONS §7: "L (+XL arm refused)"; SYNTHESIS §7.3). L because the DB lane and the producer are **both already built and tested**: one runtime wiring module, a migration that installs nothing and recuts nothing by default, four web surfaces (create dialog, parse action, document target panel, coverage totals) with the full state / responsive / a11y pass, one new DB battery with its own cohort gate, one real-Postgres runtime leg and the first Playwright walk of this tab. **The XL arm is refused by ruling, not left open** (D13.1): no new processing lane, no three CHECK widenings read out of `pg_constraint`, no facts-router splice, no new worker claim/retry/terminal semantics.

**Rig**: worktree `C:\Users\zhant\Desktop\clara-wt\656`, branch `impl/656-opening-ledger`, PG **55706** / db **clara_656**, base `abcc5030`, 219 migrations at `0224_preview_invite`, PG 17.11. Node 22 first, on every call: `export PATH="/c/Users/zhant/AppData/Local/pnpm:$PATH"`. DB env: `export PGHOST=127.0.0.1 PGPORT=55706 PGUSER=postgres PGDATABASE=clara_656 CLARA_ALLOW_DESTRUCTIVE=1 CLARA_RIG_DB=1`. **Never** set `CLARA_RIG_ALLOW_RESET=1` or `CLARA_RIG_ALLOW_ROLE_SWEEP=1`; never run a second from-scratch chain on this cluster (0154 pins the cluster-wide `clara%` role count). No `psql` on Windows — use `node -e` with `pg`. Playwright triple: `CLARA_E2E_APP_ORIGIN=https://127.0.0.1:3350 CLARA_E2E_NEXT_PORT=3351 CLARA_E2E_RUNTIME_PORT=3352`. If a World leg leaves `rig-isolation.test.mjs` T10b red (#866), say so as that; if you need it green, clone a sibling database first (`create database clara_656_world template clara_656`) and point the leg at the clone.

**Before the final report, run**: `pnpm typecheck` and `pnpm lint` from the worktree root (both green); the **whole `apps/web` unit suite** once (`node scripts/run-tests.mjs` from `apps/web`, ~5 min) and report its counts; `packages/db/tests/operation-census.test.mjs` and `rig-isolation.test.mjs` **without the reset flags**; `packages/db/tests/document-capability-registry.test.mjs` **before and after** your migration, reporting both results and naming every cell you re-based; your own battery with the **40** `--import ./tests/*-preintegration-gate.mjs` flags copied verbatim from `packages/db/package.json` (plus your 41st), **and a focused run of your own battery WITHOUT its gate, which must FAIL loudly below 0228**; `node scripts/check-frozen-workflows.mjs` and `node packages/runtime/scripts/check-parts-parity.mjs` from the worktree root — expected **no-ops**, and run anyway, because a green there **is** the evidence for this brief's "no successor" verdict (SYNTHESIS §7.2); from `packages/runtime`, `node --test tests/opening-tb-produce.test.mjs tests/wave-b-opening-parse.test.mjs` and the World leg command in §4; and `pnpm --filter @clara/web e2e opening-ledger-source` with your triple.

**Copy the house shapes from**: `packages/db/migrations/0207_document_capabilities_version_monotone.sql` and its commit **`af3b5955`** (the only prior migration to touch this table — header, prestate, trigger, tail, **and the same-commit battery edit you are repeating**); `packages/db/migrations/0213_opening_balance_evidence_link_wall.sql`, commit **`e0044f36`** (the most recent opening-lane migration: it replaces one trigger body and re-pins the others by sha, and its battery is `opening-balance-evidence-link.test.mjs`); `packages/db/migrations/0195_*.sql:390-409` (the prestate sha-pin idiom, including the NON-REGRESSION form); `packages/db/migrations/0219_client_onboarding_facts.sql` (a "this file recuts nothing" migration's discipline); `packages/db/tests/preview-invite-preintegration-gate.mjs` + `preview-invite.test.mjs:33-80` and commits **`ebe0b1c7`** / **`31e3ab77`** (gate module, the `before`-hook premise probe, the focused-run-must-fail rule, `DOOR_PINS` measured on a rig, `rig-meta.mjs:2295-2315`'s cohort block); `packages/db/tests/wave-b/wb-k-approval.test.mjs` + `wb-fixtures.mjs` (persona-driven opening battery idioms and the two-session shape); `packages/runtime/tests/prepayment-occurrence-e2e.mjs:44-90` (standalone World-leg header, the `clara_<digits>` gate and the clean-skip rule); `packages/runtime/tests/kdoc-opening-tb-testkit.mjs` (the measured synthetic geometry); `apps/web/components/registers/registers-workbench.tsx:28-47` (URL-as-truth tabs); `apps/web/components/work/work-question-form.tsx:70` (the `Field`/`FieldGroup` precedent); `apps/web/e2e/bank-close-registers-walk.spec.ts` + `bank-close-registers-mock.mjs` (walk/mock pair and its exact-verb allow-list guard); and `docs/plan/active/refresh-wave-2026-09-15/brief-649.md` plus `.../reports/640-review-closure.md` for what a reviewer demanded last wave — **behavioural cells rather than `prosrc` assertions, re-measured rather than copied evidence, an explicit merge-order collision table, and every claim carrying the command that produced it with its pass/fail counts.** (Note: repo history before the tag `pre-rebase-630` was rewritten, so `git log --follow` on the opening-lane runtime files returns one squashed commit — read the files, not their ancestry.)

## Verifier findings not applied

**None. The one finding returned — F1, major, `inexact` — is applied in full, and nothing is refuted back.**

F1's evidence was re-opened in source and it holds: `packages/db/tests/preview-invite-preintegration-gate.mjs:11` sets `CLARA_ALLOW_MISSING_PREVIEW_INVITE`, and `packages/db/tests/preview-invite.test.mjs:57` — inside the `before()` hook at `:52-67` — reads that same string. A gate and a battery are paired by the **env-var string**, not by a shared file-name pattern, and the draft named the gate off the migration stem (`opening-ledger-source`) while naming the battery, the World leg, the walk and the mock `opening-source`.

Applied, taking both halves of F1's suggested fix rather than choosing between them:
- **One stem across every new file this ticket names.** Battery → `packages/db/tests/opening-ledger-source.test.mjs`; World leg → `packages/runtime/tests/opening-ledger-source-e2e.mjs` (and its exact command in §4); Playwright pair → `apps/web/e2e/opening-ledger-source-walk.spec.ts` + `apps/web/e2e/opening-ledger-source-mock.mjs`; the §6 run filter → `pnpm --filter @clara/web e2e opening-ledger-source`. Measured: neither `opening-source` nor `opening-ledger-source` collides with any tracked file today.
- **The variable is written out wherever it is used, with the measurement that says it cannot be derived.** §3 item 5 and §4's battery paragraph both carry `CLARA_ALLOW_MISSING_OPENING_LEDGER_SOURCE` in full. The reason is measured, not asserted: of the estate's forty gate modules, at least seven do not transform their own stem — `client-onboarding-identity-*` → `CLARA_ALLOW_MISSING_CLIENT_ONBOARDING_FACTS`, `fixed-asset-acquisition-*` → `…_FA_ACQUISITION`, `firm-setup-*` → `…_FIRM_SETUP_0218`, `rs-guard-*` read by `name-only-guard.test.mjs`, `knowledge-0192-*` by `knowledge-records.test.mjs`, `document-source-revision-*` by `rig-docs-source-revision.test.mjs`, and `document-capability-*` by three batteries with three different stems (`document-capability-registry.test.mjs`, `document-fact-validation-belt.test.mjs`, `field-path-grammar.test.mjs`). "Derive it from the file name" is therefore not a safe rule in this estate, whatever this brief's own stems look like.
- **The binding section carries the roster** (Orchestrator decisions → numbering discipline), so an implementer meets the whole name set before reaching §3.

One correction F1 did not ask for, made in the same sentences because they were being rewritten: the draft cited the precedent's `before()` hook as `preview-invite.test.mjs:55-68`; measured, the hook is `:52-67`, the env-var read is `:57`, and the catalog-probe-through-`ready = false` span is `:55-66`.

Two claims the finding's fix implies were checked and are NOT restated as house law, because they are false of the estate: (a) a battery must share its gate's stem — `name-only-guard.test.mjs` / `rs-guard-preintegration-gate.mjs` and five more pairs disprove it; (b) every brief in this wave pairs them — #655 (`trade-invoice-*`), #658 (`knowledge-retrieval-*`) and #635 (`firm-commercial-settings-*`, with its env var written out) do, but #657 pairs gate `bank-match-evidence-preintegration-gate.mjs` with battery `bank-line-existing-booking.test.mjs`. One stem is this brief's choice for its own files because it removes a real ambiguity at zero cost — it is not a rule this brief invents for anyone else.
