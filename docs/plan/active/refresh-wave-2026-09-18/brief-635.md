# Brief: #635 — 清楚管理事务所设置、法律与真实商业状态 (Firm Settings shows the firm's real legal, commercial and usage state)

## Orchestrator decisions (binding)

- **Migration `0233` only**, ONE file `packages/db/migrations/0233_firm_commercial_settings.sql` (DECISIONS §2.1, row 0233). **No table, no column, no trigger, no CHECK, no policy, no purpose widening, no `drop function` anywhere.** It creates THREE `security definer` reads and recuts ONE body. Every one: owner `clara_fn_owner` (`set role clara_fn_owner` before §A, `reset role` before the tail — `0224_preview_invite.sql:177`, `:249`), `set search_path=clara,pg_temp`, `revoke all … from public`, `grant execute … to clara_authenticated`, and `set plan_cache_mode=force_custom_plan` on the two new firm-bound reads (`0185:643-646`'s rule, restated at `0195:879-880`).
  1. **`clara.get_firm_legal_standing() returns jsonb`** — `stable`, floor `clara._human_ctx(clara.role_rank('viewer'))`, **arity 0 forever** (a client argument would re-open the per-client existence oracle `0195:871-874` deliberately closed; a tail catalog assertion pins arity 0). Returns `{documents:[{kind,version,status,title,effective_from,published_at,firm_accepted,accepted_at,accepted_by,accepted_by_name,my_accepted_version,my_accepted_at}],standing_live,can_accept_for_firm,masked}`. **No `body` key** — the bytes stay `get_current_legal_documents`' job (`0185:634-671`), so no second surface can render bytes a different digest was taken over.
  2. **`clara.get_firm_commercial_state() returns jsonb`** — floor `clara._human_ctx(clara.role_rank('admin'))` (the literal `capabilities.test.ts:133`'s `FLOOR_RE` matches). Returns `{firm:{id,name,created_at,is_operator},plan:{local_key,name,currency,amount_cents,amounts_ruled},payment:{recorded,recorded_at,subscription_present,customer_present},invoices:{available:false,reason:'not_collected'},capacity:{docs_per_day,pages_per_day,ocr_concurrency,llm_witness_concurrency,source:'firm_document_limits'}}`. **Raw `stripe_customer_id` / `stripe_subscription_id` never leave the database** (booleans only — D1/H3).
  3. **`clara.get_firm_ai_usage(p_period date) returns table(scope text, call_kind text, calls bigint, input_tokens bigint, output_tokens bigint, priced_calls bigint, unpriced_calls bigint, spend_cents bigint, price_currency text)`** — floor admin; body = floor, then `return query select …, 'USD' from clara.get_llm_usage_summary(clara.jwt_firm(), p_period, null)`. `p_period` NULL → CLR10 `invalid_period`. It survives the base-door recut for two reasons of its own: it binds `jwt_firm()` **inside** the database (no firm id crosses the wire) and it carries `price_currency` **without** a `returns table` widening the base door cannot take through `create or replace` (the `0186:1703` lesson — a widened return type needs DROP+CREATE and the old grant dies with it).
  4. **RECUT — `clara.get_llm_usage_summary(uuid,date,uuid)`**, body-only `create or replace`, **signature, argument names, `default null`, return-table column list, grant and ACL byte-unchanged**. Creation `0110_f_a9_llm_usage_reshape.sql:706`; **no later splice** (grep over all 219 files returns 0110 alone) — pin it anyway, MEASURED. The ONE change: `perform clara._human_ctx(clara.role_rank('admin'));` as the **first statement of the body**, before the existing `p_firm is distinct from clara.jwt_firm()` wall (`0110:714-717`), so an under-ranked caller meets **CLR04** and a foreign-firm caller still meets **CLR11 `client_not_in_firm`**. Everything from `-- TWO BUCKETS, NEVER ONE FIGURE` (`0110:718`) to `end $$` is carried verbatim, including the UTC row filter (`0110:750`).
- **Pins are MEASURED on your rig, never transcribed** (DECISIONS §2.2; WORK-ORDER rule 8). Take the pre-image off `pg_proc.prosrc` on `clara_635` after `pnpm db:migrate`, full 64-hex, using `0224_preview_invite.sql:153-171`'s loop verbatim (`encode(sha256(convert_to(p.prosrc,'UTF8')),'hex')` keyed by `to_regprocedure('clara.get_llm_usage_summary(uuid,date,uuid)')`). A pin written from file text will not match and 0233 refuses to apply. Pin these three as **non-regression** prestate pins in the same loop and re-assert them in the tail: `clara.get_current_legal_documents()` (`0185:634`), `clara.accept_legal_document(text,integer,text,text)` (`0185:684`), `clara._accounting_work_egress_live(uuid,uuid)` (`0195:875`) — this file claims to change none of the three, and the standing door's predicate is copied from the third.
- **D1 (§0) — not one price appears.** `billing_plans` holds one row `('clara-beta-2026','Clara Beta',0,'MYR',false,true)` (`0163:214-215`); `amounts_ruled=false` is the DATABASE saying the price is unruled. The card reads "Beta — no price has been set for this plan yet"; `amounts_ruled` is the **render condition**, so a later owner ruling shows a figure with **no code change**. Never `RM 0.00`, never a substitute number (C-01 / C-56).
- **D2 (§0) — the owner can accept a new legal version in-app, and only an owner can.** The control is gated on door 1's `can_accept_for_firm`, derived from `0195:905`'s own membership predicate (`m.firm_id=<firm> and m.status='active' and m.role='owner'`); the act goes through the **existing, untouched** `clara.accept_legal_document(p_kind,p_version,p_body_sha256,p_op_key)` (`0185:684`, human-only by `is_agent`, `0185:710-714`). **Do not add a rank floor to that door** — `(entry)` accepts before any membership exists, so `_human_ctx` would refuse the shipped registration flow outright.
- **D3 (§0) — usage: tokens and calls always; USD shown, labelled "provider price (USD), not your books", never converted; the month window labelled from the door's own UTC bounds** (`0110:711-712` + `:750`); and the rank wall the usage door lacks today is added **to the door itself** (the recut), not decorated with a parallel walled wrapper.
- **§6 H5 / 2026-09-15 D10 stands** — this page renders **no firm identity facts** (registered name, SSM, address); it links to `/settings/setup` and owns none of it (#934/#935/#891/#895 own that page).
- **§5 non-goals, in code comments AND in the report**: no price, no Stripe ids, **`firm_document_limits` is floored nowhere this wave** (its viewer-readable grant `0007:810-811` + `:2742-2744` is a written, reviewed grant; changing it turns this ticket into a relation-level migration — file the follow-up, and say in the panel's header comment that the admin-gated card is an **affordance, not a wall**), no lane-enable switch, no seat count, no billing-management control, no operator content (`firms_count` is business-confidential, `0186:738-742`).
- **No successor, no frozen byte, no runtime change** (DECISIONS §1.1/§1.4; gap-635 Frozen-body verdict). This journey invokes no Workflow: reads plus one already-built governed write. **You write no successor-contract stanza** — say "no cut needed" in as many words in the report, because that is evidence the integration worker needs. AC7's Workflow clause does not apply; its database-boundary clause does.
- **Ownership (§3)**: you OWN `/settings/*` (`components/firm-admin/*`, the `/settings/firm` route) and add **exactly ONE** `FIRM_CAPABILITY_FLOORS` row in `lib/firm/capabilities.ts` (plus the matching key on `FirmCapabilities`, on `NOTHING` `:236-249`, and in `firmCapabilities()` `:260-275` — three structures, one file). **Do not open**: `components/work/work-detail.tsx` (#658 owns the Sources tab, #655 one link block, #636 one row), `components/firm/firm-home/*` (#659 — you DECLARE the door a tile would read and build no tile), `components/registers/knowledge*` and `/settings/knowledge` (#658 — link only), `lib/navigation/tree.ts`'s `vendorBindings` row (#921), `components/admin/*` (the members roster is #625's, merged — no second roster, no seat count), `lib/journals/api.ts`, `components/bank/*`, `lib/firm/needs-you.ts` (**no new row kind this wave**, §3).
- **The `tree.ts` edit attributed to this lane is really an `en.json` edit** (orchestrator refinement, measured). Provenance stated exactly, so nobody reads this as overturning a binding ruling: **DECISIONS §3 (`DECISIONS.md:119`) names `lib/navigation/tree.ts` only in the all-lanes shared row** ("one-line registrations at the sorted position") and makes **no #635-specific attribution**; the claim that #635's tree.ts edit *is* the `sections.firm.purpose` string is **SYNTHESIS §3.1's matrix row** (`SYNTHESIS.md:354`, `E (sections.firm.purpose string)` in the 635 column). This refinement therefore supersedes a **SYNTHESIS observation (advisory)**, not a DECISIONS ruling. Measured on disk at `abcc5030`: `tree.ts:276` holds only the KEY `purposeKey: "sections.firm.purpose"` (inside the `firm` row, `tree.ts:272-279`) — no prose; the sentence is `messages/en.json`'s value at `Settings.sections.firm.purpose` ("Review the firm-wide authority controls.", `en.json:383`). Change the value; **leave `tree.ts` untouched** — the same edit at its real address, and one lane fewer on a file four lanes plus open #921 all touch.
- **CONTEXT.md**: *Firm legal standing* is the wave-ratified term (§3.1) and is mandatory. Add *Billing plan* and *Model usage summary* in the same house `term / _Avoid_` shape (SYNTHESIS §3.1 counts three for this lane); no other lane claims either word, and if the integrator's union finds a collision yours yields. Carry the #655 boundary sentence too: unqualified **"invoice" = a client's accounting document**; the firm's own billing document is a **subscription invoice** and does not exist.
- **Boundaries (§3.2)**: **#635 vs #660** — model-usage USD never appears beside book money, and this page installs **no Chart** (#660 owns `ui:add chart`; appendix D's readable-table rule is yours). **#635 vs #659** — #659 owns firm Home and any tile; you own the destination. **#635 vs #636** — neither builds a `firm_document_limits` editor; the residual is named twice. **#635 vs #658** — link, render nothing. **#628 is CLOSED**: you read its residue (one boolean) and build nothing in it.
- **Evidence law (§4)**: every AC and historical row closes with a red-first cell, a browser-walk leg, or a **named** residual; the new battery asserts through `humanQuery` least-privileged personas, never `rootQuery` except labelled fixture DML; a skipped frontier-gated battery is not evidence; **hosted evidence is never claimed** — write "hosted evidence pending". Blueprints are never edited; the four drift lines below go in the report for #683.
- **§7.4 #16 (out-of-repo callers of `get_llm_usage_summary`) is ruled**: zero callers in this repository (measured grep over `apps/web` + `packages/runtime`), beta data — **proceed with the floor recut** and put the question to the owner in the wave report. Do not defer the recut on it.
- **Playwright port triple** (RIG.md, verbatim): `CLARA_E2E_APP_ORIGIN=https://127.0.0.1:3300 CLARA_E2E_NEXT_PORT=3301 CLARA_E2E_RUNTIME_PORT=3302`. **Rig row** (RIG.md, verbatim): worktree `C:\Users\zhant\Desktop\clara-wt\635`, branch `impl/635-firm-commercial-settings`, PG port **55701**, db **clara_635**, install ok 438s, 219 applied, seed ok (2 files), smoke `219 · 0224_preview_invite · PG 17.11`.

## 1. Current state

**DB — legal (the complete, unsurfaced family).** `legal_documents` (`0185:234-256`): PK `(kind,version)`, `kind in ('terms','dpa')`, `status in ('draft','published','superseded')`, `ck_legal_documents_body_sha` recomputes the digest in the database (`0185:251-252`), `uq_legal_documents_published` = one published row per kind (`0185:259-260`), FORCE RLS, **owner policy only, zero table grant** (`0185:229-232`). `legal_acceptances` (`0185:327-339`): append-only, composite FK to `(kind,version,body_sha256)` — bound to the BYTES — `op_key` on the row, **no `firm_id` column anywhere**. `get_current_legal_documents()` (`0185:634-671`) is `clara_authenticated`, returns `distinct on (kind)` over `status in ('published','draft')` ordered `(status='published') desc, version desc` (`0185:653-659`) plus the CALLER's own acceptance; CLR04 `no_actor` with `detail.reason` (`0185:650`). `accept_legal_document(text,integer,text,text)` (`0185:684`, grant `:785`) is idempotent by `op_key` and by `(user,kind,version)`, returning the **original** `accepted_at` (`0185:766-775`); refusals CLR10 `invalid_op_key|invalid_kind|op_key_conflict|hash_mismatch`, CLR09 `not_published|stale_version`, CLR04 `agent_actor`. `0187:14-46` published beta v1 of both kinds, data only. **Consequence chain**: `_accounting_work_egress_live(p_firm,p_client)` (`0195:875`, ungranted `0195:911`) — limb (b) an ACTIVE client of this firm (`0195:885-889`), limb (a) a published version of each kind (`0195:890-895`) **and ONE ACTIVE OWNER holding BOTH acceptances** (`0195:896-906`, the membership predicate at `0195:905`); a newer publication withdraws authority the moment it lands (`0195:890-892`). The only in-app accept surface is `(entry)` (`components/entry/signup-legal-stage.tsx:13,20`), so the remedy `WorkDetail.egressNotAuthorized.body` names today **has no destination**.

**DB — commercial.** `billing_plans` (`0163:196-215`, seed `:214-215`), widened `0164:97-107` with a computed `payment_method_collection` (`0164:26-31`); `get_current_checkout_plan` returns **no amount by design** (`0164:120-121`, source row `0164:134-135`). `firm_registration_payments` (`0163:219-240`; `consumed_dpa_signature` repointed to `legal_acceptances` by `0185:552-556`) — FORCE RLS, owner policy only, **`consumed_firm_id` read by exactly ONE reader in the estate, the operator console (`0188:304`)**. `firms` = id, name, high_stakes_amount_cents, created_at, is_operator (`0002:199-206` + `0133:273`), SELECT to `clara_authenticated` under `p_firms_human`, no rank (`0002:503-504`). `caller_context` view `0141:544-552`. `_human_ctx(p_min_rank)` (`0004:299-309`) raises **CLR04 with NO `detail.reason`** ('no authenticated actor' / 'actor has no active membership' / 'insufficient role') — measured; the surface keys its denied face off the CODE and renders the message verbatim, and invents no reason token. `users_visible` = `(id, display_name)` over firm-mates and agents, no rank floor (`0137:291-298`).

**DB — usage / capacity.** `llm_usage_events` human SELECT policy `(scope='firm' and firm_id=jwt_firm()) or scope='platform'` (`0110:355-358`). `llm_price_table` is USD by CHECK (`0110:497`) with the FX non-goal beside it (`0110:494-496`), platform data, no app grant. `get_llm_usage_summary(uuid,date,uuid)` (`0110:706-756`): SECURITY DEFINER, month bounds `0110:711-712`, **UTC row filter `0110:750`**, two buckets the door refuses to fold (`0110:718-727` rule, `:748-752` code), `unpriced_calls` as the published freshness tripwire (`0110:702-704`), **only wall = `jwt_firm()` (`0110:714-717`), NO rank floor**, grant `clara_authenticated` (`0110:756`), **zero callers in `apps/web` + `packages/runtime`**. `firm_document_limits` (`0007:364-371`; `llm_witness_concurrency` added `0090_f_a1_walls.sql:298`) is viewer-readable (`0007:810-811`, `:2742-2744`) and has **no human writer at all** (`0196:36-40`, whose header names **#635** at `:42-43`). `firm_usage_daily` / `task_usage` carry no app grant (`0006:700-712`); `firm_limits.daily_token_limit` is DROPPED (`0151:721-722`).

**Runtime.** Nothing in this slice. Registry pins `chatTurn_v20` / `claraWork_v4` / `clientOnboarding_v5` (`registry.ts:990-998`); the Stripe webhook lane (`packages/runtime/src/stripeRoutes.ts:22-47`) is boundary, not scope.

**Web.** Eight routes under `(firm)/settings`; `registrations` does NOT exist (#615 moved it; `lib/navigation/legacy-routes.ts:54-55` 307s both spellings). `SETTINGS_SECTIONS` (`tree.ts:263-320`) drives hub + nav + ⌘K; the `firm` row is viewer-floored (`tree.ts:272-279`). `/settings/firm` = `PageHeader` + `SettingsNav` + `SettingsPanel`; `SettingsPanel` (`components/firm-admin/settings-panel.tsx:59-90`) renders two static Cards under the `FirmAdminCompliance.settings` namespace (`:62`) and **reads nothing** (`:20`). P4-6: one request-scoped provider, **no child re-reads the session or `caller_context` merely to shape an affordance** (`app/(firm)/layout.tsx:66-68`, `firm-scope-provider.tsx:11-18`, `:24-26`); the only epoch machinery is `ClientScopeProvider`, `clientId`-keyed (`client-scope-provider.tsx:16-18`). `lib/firm/capabilities.ts`: 11 FLOOR rows each citing a live door's migration:line (`:110-224`), two conjunct rows of one predicate shape (`:214-233`), `NOTHING` (`:236-249`), `firmCapabilities()` (`:260-275`). `lib/registration/legal-reads.ts` (draft-is-a-preview at `:27-31`, a positive-shape decoder that DROPS an unreadable row `:100-119`) and `legal-doors.ts` (verbatim sha forwarding, caller-held op key, no retry, `STALE_ACCEPT_REASONS`) have five importers, **all outside `(firm)/`**. `lib/download-mechanism.ts:53` already exports `triggerDownload({blob,filename})`. `components/ui/` = 32 entries (30 primitives); **no `chart.tsx`**; everything this slice needs (card, badge, alert, field, table, select, skeleton, empty, separator, tooltip, dialog) is installed — **no `ui:add` this ticket**. `Settings.unbuiltNote` (`en.json:375`) is asserted **verbatim** by `firm-admin-pages-a11y.test.tsx:97`.

**Tests.** DB: `legal-acceptance.test.mjs`, `checkout-gate-c1/c2/c3/c6.test.mjs` (c3 carries the prosrc-pin idiom `:58` and the money-store roster cell `c3.53` at `:2128-2199`), `f-a9-usage-reshape.test.mjs` (behaviour `:385,:400,:420,:461,:478,:597,:617,:637`; ACL `:663-680`), `f-a9-pr-1b.test.mjs:215` (whole-catalog `prosrc` scan for three dropped `firm_limits` columns), `firm-document-limits.test.mjs`, `operation-census.test.mjs`, `rig-isolation.test.mjs`, `rig-meta.mjs` (`F_A9_PR1A_COHORT` `:352-368`; the sentence to amend is `:357-358`). Fixtures: `buildWorld()` gives firm A alice(owner)/bob(bookkeeper)/carol(viewer), firm B dave(owner), firm S erin(owner) — **there is no admin persona**; `addMember(sub,{firm,user,role,opKey})` (`rig-fixtures.mjs:44`) is the audited writer that mints one. Web: `firm-admin-pages-a11y.test.tsx` (`:97` the hub sentence, `:253-263` the `/settings/firm` composition + the no-threshold pin), `lib/firm/capabilities.test.ts` (`FLOOR_RE` `:133`, floors census `:134-166`, conjunct cell `:196-203`). Playwright: `personal-settings-walk.spec.ts` (the D1 template — mocks by **`page.route` inside the spec**, `serve-built.mjs:794-804` carries only a generic default; **no lane mock file**), `firm-navigation-walk.spec.ts:175-197` (visits `/settings/firm` as **owner and bookkeeper**, pins both legacy sentences and axe-zero), `shell-migration-walk.spec.ts:131-140` (redirects) and `:133` (the `<h1>` "Firm settings"), `documents-viewer-walk.spec.ts:497-533` (the object-URL download recorder: **Playwright's `download` event does not fire under this harness**). `serve-built.mjs:553-577` derives personas from the email prefix — `owner@` (rank 3, is_operator), `bookkeeper@` (1), `viewer@` (0); **rank 2 does not exist**. `e2e-fixture-ownership.test.ts`: `LANE_MOCKS` `:53`, `LANE_DECLARATIONS` `:216` (every lane mock needs a row, asserted `:500`), `SHARED_RPC_VERBS` `:1106` with `RPC_VERB_OPENER` `:1077` matching `verb === "…"`, `fn === "…"` and literal `path === "/rest/v1/rpc/…"`.

## 2. Gaps / rows

| Row | Disposition | Evidence the implementer must produce |
|---|---|---|
| **AC1** route firm/member/commercial by rank; membership reuses the one lifecycle | **partial → verify-only + build the commercial half.** Routing and the roster are DONE and **must not be touched** (`tree.ts:263-320`, `components/admin/members-panel.tsx:42-58`, `0141:526`/`:538`) | `p635.web.rank_shaping` + walk legs (4)(5); a report line that no second roster and no seat count was added |
| **AC2** versioned, attributable legal; plan/payment/invoice/usage with source/range/freshness, download only where a real contract exists | **to build** | `p635.db.legal_standing_*` (8 cells), `p635.db.commercial_*` (5), `p635.db.usage_*` (8), `lib/firm/usage-csv.test.ts`, walk legs (1)(2)(3)(6)(7) |
| **AC3** absence/failure/empty has an explanation and a support route, never synthetic controls or invented pricing | **to build** | `p635.web.commercial_absent_named_zero`, `p635.web.no_billing_control`, `p635.db.commercial_invoices_constant`; the one blanket hub sentence replaced by three honest ones |
| **AC4** owner/admin/bookkeeper/viewer + operator separation; role loss removes protected data/actions immediately | **to build — both holes closed** | `p635.db.usage_base_floor_added` (the recut, both directions), `p635.web.revocation_clears` + walk leg (8); the `firm_document_limits` affordance-not-a-wall residual **named** |
| **AC5** plan present/absent, payment/provider failure, immutable legal history, usage range/download, old admin redirects, keyboard/focus, narrow Settings | **partial: "old admin redirects" is `verify-only` (CLOSED by #614 — `lib/navigation/legacy-routes.test.ts:35-42`, `shell-migration-walk.spec.ts:131-140`); everything else to build** | the walk's ten legs + `p635.db.accept_replay`; keyboard/focus/320px **re-measured on the new page**, never inherited |
| **AC6** the eight states, exact money/date, 320px, 200%, keyboard/focus return, SR names, reduced motion, stable URL/Back, preserved drafts, accepted composition with a persistent outcome | **to build** | the states table in §3, `firm-settings-a11y.test.tsx`, walk legs (2)(9)(10). **No draft exists** (the only input is a confirmation) — state that in the report rather than leave it silent |
| **AC7** production-facing reads under real least-privileged roles and current migrations | **to build** | the 24-cell battery through `humanQuery` personas including a **minted admin**; **no Workflow leg — that is a finding, not an omission** |
| **CB-AE2E-012 / H-18 / H-19** (A.1:129/176/177) default-agentic supersedes a manual lane-enable panel | **descoped (authority)** — `0195:35-37` ("there is NO per-client 'AI on' switch; UI-28/UI-29 are REPLACED, not implemented") | report line naming the boundary; the ONLY contribution is the third recovery path (`ARCHITECTURE:393-403`), which this ticket builds |
| **C-01 / C-56** (A.2:13/68) pricing is an owner decision | **descoped (authority)** — rendered as the fact it is (`amounts_ruled=false`) | `p635.web.no_numeral_when_unruled` (a regex over rendered text); D1 restated in the report |
| **C-02** (A.2:14) recover the real metering/billing requirement | **verify-only → to build (the surface)** — four relations, three truths; the reader is `llm_usage_events` via the rollup, never `firm_usage_daily` | the usage card's door call + the four-relation census paragraph in the report |
| **C-09 / C77.8** (A.2:21, B:225) webhook assertions, loud rejections | **verify-only, OUTSIDE this boundary** (`stripeRoutes.ts:22-47`, the three `c5-stripe-*` batteries) | one report line: preserved, owned by the webhook lane; the firm card shows a payment RECORD, never an event state |
| **F-02** (A.2:119) do not restore an opt-in switch | **descoped (authority)** — nothing to restore | report line |
| **C55.21** (B:202) monthly usage dashboard · **discovery** | **to build, and the discovery result is the RECUT** — source `llm_usage_events` via `0110:706`; freshness `unpriced_calls` + the two buckets; **authorised users: nobody was floored — this ticket floors it** | `p635.db.usage_base_floor_added` + `usage_base_firm_wall_intact` + the sentence separating this from #660's figures |
| **C83.10** (B:285) duplicate of C55.21 | **verify-only** — folded; no separate work | one report line |
| **C81.5** (B:253) Supavisor headroom · **discovery** | **descoped (authority)** — pool capacity is a release/ops measurement, not a Settings surface | report line naming the two per-firm caps this page DOES render (`firm_document_limits`, `firm_limits.max_concurrent_runs`) + a recommended ops follow-up |
| **C88.2** (B:339) retirement census of `firm_usage_daily` / `task_usage` · **discovery** | **verify-only — and the discovery result is "do not drop here."** Writers: `settle_chat_turn` (`0006:1048-1058`) + the autodraft reserve/refund family (`0011:2539-2567`, `:2670-2676`, `0031:232-266`, `0034:277-395`, `0036:955-960`, `:1302-1304`, `:1393`); readers: the `0006:970` budget gate was removed by `0105:40-52`, leaving in-body reads and test-only helpers | the census IS the deliverable (report §); plus a line that this card reads `llm_usage_events`, so the eventual drop gains no new blocker |

## 3. Slice (one branch)

**Migration `0233_firm_commercial_settings.sql`**, written in this order (`0224_preview_invite.sql` is the shape: header `:3-104`, prestate `:105-175`, `set role` `:177`, §A `:179-238`, §B grants `:239-248`, `reset role` `:249`, §C tail `:251-…`):

1. **Header** — what it creates, the one recut and why the floor goes on the door instead of a walled wrapper, the three non-goals, and the sentence that it creates/alters/drops no relation.
2. **Prestate (`do $w635_pre$`)** — the three new names do NOT resolve; `legal_documents` / `legal_acceptances` / `billing_plans` / `firm_registration_payments` each carry **zero** application-role table grant; `firm_document_limits`' grant is present and unmoved; the four **measured** sha pins through `0224:153-171`'s loop; a `raise notice` naming what was found clean.
3. **§A objects**, in the order above (legal standing → commercial state → ai usage → the recut). Door 1's per-kind selection **mirrors `0185:653-659`'s `distinct on (kind) … order by (status='published') desc, version desc`**, so a kind with only a draft appears with `status='draft'` — while `standing_live` counts **published rows only**, computed by the same literal predicate fragment as `0195:890-906` so a census can compare like with like (**orchestrator refinement — supersedes gap-635's "one entry per PUBLISHED kind"**, which would have hidden a draft the surface already previews). `accepted_by_name` is resolved **inside the definer body** from `clara.users` joined to `clara.firm_memberships` on the bound firm — **not** through `clara.users_visible`, whose predicate re-derives `jwt_firm()` and admits agents (`0137:291-298`) — and is masked to NULL below bookkeeper (`0141:526` is the row floor the mask mirrors; `0141:517` masks email below admin and is NOT the precedent here). `can_accept_for_firm` applies `0195:905`'s predicate to the CALLER. The plan comes from `billing_plans where is_current` — the same row `get_current_checkout_plan` reads (`0164:134-135`), so a rotation cannot make the two disagree; the payment from `firm_registration_payments where consumed_firm_id = clara.jwt_firm()` — **the first firm-scoped reader of that relation**.
4. **§B grants** — `revoke all … from public` + `grant execute … to clara_authenticated` for the three new names; the recut's grant is **not re-issued** (`create or replace` preserves the ACL — the tail proves it).
5. **Events**: none. These are reads: no `_reserve_op`/`_finish_op`, no `_audit`, no receipts, no DML of any kind by the file itself.
6. **§C tail (fail-closed, re-reads the live catalog after privileges are final)** — each of the four names resolves to **exactly one** `pg_proc` row (`0103:1055-1070`'s single-row law); `get_firm_legal_standing` has **arity 0**; owner `clara_fn_owner`, `prosecdef`, `search_path=clara,pg_temp` on all four and `plan_cache_mode=force_custom_plan` on the two new firm-bound reads; each new door's ACL is exactly `{clara_authenticated=X/clara_fn_owner}` with PUBLIC, `clara_runtime`, `clara_agent_ro` and both wake roles holding nothing; **`get_llm_usage_summary`'s ACL is byte-identical to the pre-image and its new `prosrc` differs from the pinned sha**, with a normalised diff showing the ONLY textual delta is the floor statement; `llm_price_table`'s `currency` CHECK still reads `'USD'` (`0110:497`) — so a later widening reds this migration instead of silently mislabelling money; the four money/legal relations still carry zero application-role table grants; `firm_document_limits`' grant unmoved; no relation created, altered or dropped; and a behavioural probe from a real `clara_authenticated` session proves door 2 and the recut door both refuse a bookkeeper CLR04 while door 1 masks for a viewer.
7. **Gate module** `packages/db/tests/firm-commercial-settings-preintegration-gate.mjs` — **stable stem** (§2.2), copied from `preview-invite-preintegration-gate.mjs` verbatim, setting `CLARA_ALLOW_MISSING_FIRM_COMMERCIAL_SETTINGS = "1"`; registered in `packages/db/package.json`'s chain **at its migration-order position** — on your branch that is after `preview-invite-preintegration-gate.mjs` (the 41st of 41); at integration it lands after the other eight new gates because 0233 is the wave's highest number. Say that in the report so the integrator does not re-sort it.
8. **`rig-meta.mjs`: comment-only** (§2.1 permits it for 0233 — no new governed relation). Add one sentence to `F_A9_PR1A_COHORT`'s comment at `:357-358` recording that the rollup is now **admin-floored** and that its grant line is unchanged. **Do not add a cohort array** — say so in the report rather than leave the absence to be discovered.

**Runtime.** No change. No module, no route, no registry edit, no part kind, no `WORK_ACCEPTED_PURPOSES` widening. `packages/runtime/README.md` gains a sentence only if the UTC-window residual needs a home there.

**Web.** `/settings/firm` stays the one address (**no new route** — so `tests/firm-scope-fourth-entrance.test.ts` and `tests/firm-scope-surfaces.test.ts` should stay green; verify, do not assume). The usage period is a search param `/settings/firm?period=YYYY-MM`: stable URL, Back restores the prior month, a bad value falls back to the current month **with a visible note**, never an error.

New components under `components/firm-admin/`, copy in a new `FirmSettings` namespace (the existing panel's `FirmAdminCompliance.settings` is a compliance namespace, `settings-panel.tsx:62`):
- `firm-settings-panel.tsx` — five cards, **keeping the two existing legacy cards verbatim at the bottom** (`approvalsNote` + `capabilitiesNotBuilt`; pinned by `firm-admin-pages-a11y.test.tsx:253-263` and `firm-navigation-walk.spec.ts:189-190` — moving them is fine, changing their text is not).
- `firm-identity-card.tsx` — name, created-at, your role, firm id (copyable) and **two links only**: "Firm details and setup" → `/settings/setup`, "Firm accounting rules" → `/settings/knowledge`. No identity fact (D10/H5).
- `legal-standing-card.tsx` — per kind version/status/effective date; "Accepted by <name> on <date>" at bookkeeper+, "Accepted for this firm" below; **your own** acceptance line at every rank; when `standing_live=false` a `StateBanner tone="warning"` naming the consequence in the accountant's words ("Clara cannot use a model on any client's books until an owner accepts the current versions", sourced from `ARCHITECTURE:402-403`) and — **iff `can_accept_for_firm`** — the `AcceptLegalDialog` trigger. Everyone else reads "an owner of this firm must accept the current versions", with the owner's name where their rank may see it.
- `accept-legal-dialog.tsx` — **imports `lib/registration/legal-reads.ts` + `legal-doors.ts` unchanged** (the sixth importer, the first inside `(firm)/`; do not fork them): renders the exact `body` the read returned, forwards `body_sha256` verbatim, mints ONE op key per `(kind,version)` attempt and **holds it across retries**, maps `stale_version`/`hash_mismatch` to a **re-read, never a resubmit**, reports a replay as "already accepted on <original instant>", and renders a `draft` row as a labelled preview with **no accept control** (`legal-reads.ts:27-31`).
- `commercial-state-card.tsx` (admin+) — plan name + "Beta — no price has been set for this plan yet" when `amounts_ruled=false` (**never a numeral**); payment "Recorded on <date>" / "No payment is recorded for this firm"; subscription present/absent; invoices = an explanation and a support route (`mailto:`), **never a "Manage billing" button**.
- `ai-usage-card.tsx` (admin+) — a month `Select` bound to the URL; a `Table` with the firm bucket then the platform bucket, **never summed**, carrying call kind / calls / input+output tokens and, when `priced_calls>0`, `spend_cents` formatted with the door's `price_currency` under the label "provider price (USD), not your books"; the window rendered from the door's own UTC bounds; a coverage line when `unpriced_calls>0`; one sentence separating this from accounting figures (C55.21) linking to client Home; and a **Download CSV** action. **No Chart.**
- `processing-capacity-card.tsx` — the four `firm_document_limits` numbers from door 2's `capacity` object (no second read), labelled "processing caps set by the operator", plus "there is no per-firm seat count; seats are accepted product direction with no delivery date" (`PRD:126`). **No control** (`0196:36-40`), and the header comment says affordance-not-a-wall (Q6/H6).

New libs under `lib/firm/`: `commercial-reads.ts` (the three door constants + positive-shape decoders in the `legal-reads.ts:100-119` idiom — an unreadable row is DROPPED, never half-rendered), `usage-period.ts` (URL ↔ month, honest that the door's window is UTC-derived), `commercial-format.ts` (**never converts a currency**), `usage-csv.ts` (rows → CSV text; pure, no DOM). The CSV is **client-side only**: serialise the rendered rows and call `triggerDownload({blob,filename})` (`lib/download-mechanism.ts:53`) — no route, no door, no byte path, no `Content-Disposition` parse. Its first two lines are a provenance header (firm name, the exact UTC window the door used, `price_currency`), so a spreadsheet cannot lose the unit or the timezone the screen carried.

**Revocation (AC4), three layers, none of which re-reads `caller_context` in a child:** (1) `app/(firm)/layout.tsx:69` re-runs `requireFirmScope()` on the server for every navigation; (2) the commercial and usage cards **re-issue their own door call** on `visibilitychange`→visible and on window focus, and a CLR04 **clears the fetched rows from state and renders the denied face** — never a stale payload behind a disabled control (polling the rank-bearing claim is refused: that is exactly the child-side `caller_context` re-read P4-6 rules out, and it would make the surface trust a mirrored rank instead of the wall); (3) the accept trigger is gated on `can_accept_for_firm`, re-derived by the door on every call. **Named residual**: a tab never refocused and never navigated keeps its last payload until one of those happens; closing that needs a server-push channel the estate does not have.

**States (all eight, each with its source):** *loading* — `Skeleton` shaped to each card, stopping on success/empty/denied/error (appendix D §137). *successful-empty* — "No payment is recorded for this firm" / "No model calls in this period": a **named zero after a complete read**, with the CSV action **absent**, not disabled-with-zero-rows (appendix D §136). *no-results* — a month with zero rows keeps the selector and offers "current month". *partial-stale* — `unpriced_calls>0` → "N calls in this period have no price on record" beside the total, and the CSV carries the same count (`0110:702-704`). *invalid-saving* — the accept control disabled with a Spinner inside it; `stale_version` → a re-read banner naming the new version, dialog stays open (`legal-doors.ts`'s `STALE_ACCEPT_REASONS`). *denied* — below-admin: the commercial and usage cards are **absent**, not disabled (裁-187, `capabilities.ts:14-18`); a typed deep link meets the door's CLR04 and renders the DB's own sentence; a live demotion converges on the next focus/visibility re-read and clears the rendered rows. *failed* — `StateBanner tone="error"` + retry + `TechnicalDetail`, **never** Empty. *cancelled-recovery* — closing the dialog cancels nothing committed; a replayed accept reports the ORIGINAL instant (`0185:766-775`). Plus 320px (the cards stack; the usage table uses `TableScrollRegion`), 200% zoom, keyboard + focus return to the trigger after the dialog, SR names on every badge and figure, reduced motion (no new motion is introduced), stable URL/Back for the period, **no draft to preserve** (stated in the report, not silently omitted).

**Capabilities**: ONE `FIRM_CAPABILITY_FLOORS` row — `canReadFirmCommercialState` → door `get_firm_commercial_state`, role `admin`, migration `0233_firm_commercial_settings.sql`, `line` = the **measured** line of the `clara._human_ctx(clara.role_rank('admin'))` statement in the finished file — plus the key on `FirmCapabilities`, on `NOTHING` and in `firmCapabilities()`. **No row for the accept action** (measured: a conjunct reds on `capabilities.test.ts:196-203`'s `f.is_operator` + `clara.jwt_firm()` assertion, and a FLOOR row reds on `FLOOR_RE` because `accept_legal_document` contains no `_human_ctx(role_rank(...))` — **the DOOR answers it instead**, through `can_accept_for_firm`).

**Shared-file edits, one line each, at the sorted position**: `messages/en.json` (the new `FirmSettings` namespace, the **replacement** of `Settings.unbuiltNote` at `:375`, and the new `Settings.sections.firm.purpose` value at `:383`) — **land this in ONE early commit and say so**; `apps/web/test/manifest.txt` (one line per new test file); `apps/web/e2e/serve-built.mjs` (generic honest defaults, §4); `packages/db/package.json` gate chain; `.github/actions/db-live-gates/action.yml`; `CONTEXT.md`; `packages/db/tests/rig-meta.mjs` (comment only). **No `tree.ts`. No `needs-you.ts`. No `e2e-fixture-ownership.test.ts`** — see the e2e ruling in §4.

**Docs.** `CONTEXT.md` (three terms, house shape). `packages/db/README.md` — the three doors with floors and grants, the recut with its new floor, and the C-3 money-store roster widening. `apps/web/README.md` — `/settings/firm` is the firm's commercial destination, and accepting a new legal version in-app is the only remedy for a withdrawn derived egress authority. `packages/db/tests/README.md` — the new battery and its cell prefix. **Never `docs/PRD.md` or `docs/ARCHITECTURE.md`**; record four **blueprint drift** lines in the report: `PRD:126`'s 共享 AI 用量 clause now has a real surface; ARCHITECTURE has **no §10** while `0195:21` and `0186:7` both cite one (the content lives in §5.E `:358-372`); ARCHITECTURE §7's accepted-but-unimplemented table carries **no commercial row**; `ARCHITECTURE:372`'s "no export route, by absence" is stated about `work_execution_traces` only, and this ticket's usage CSV makes the ambiguity worth naming.

## 4. TDD seams (red first)

**Measure-first RED cells — the FIRST work on the rig, before a line of the migration is written.** Each is an "unverified" item from gap-635 §Unverified or SYNTHESIS §7.4; each changes what the slice says, not merely what the report claims.

- **`p635.measure.usage_viewer_today`** — as carol (viewer, firm A) call `clara.get_llm_usage_summary(firmA, <date>, null)` on the rig **before** 0233. It must **succeed**. That success IS the hole C55.21 charters this ticket to close; if it refuses, the recut's premise is wrong — stop and report. *(Unverified 4.)*
- **`p635.measure.prosrc_pin`** — the pre-image `sha256(prosrc)` of `clara.get_llm_usage_summary(uuid,date,uuid)` off `pg_proc`, plus the three non-regression pins. *(DECISIONS §2.2; SYNTHESIS §7.4 #1.)*
- **`p635.measure.legal_prestate`** — what `legal_documents` actually holds on your rig (0187's beta v1 of both kinds) and whether any acceptance row exists. The battery builds its own world; this cell records the starting state instead of assuming the hosted one. *(Unverified 2.)*
- **`p635.measure.price_rows`** — whether `llm_price_table` holds any row at all. If it holds none, **every call is `unpriced` and the money column never renders** — that is the card's PRIMARY shape, not an edge case. *(Unverified 6.)*
- **`p635.measure.priced_view`** — `pg_get_viewdef('clara.llm_usage_events_priced')` and its ACL, so the rollup's dependency is read rather than assumed; neither the wrapper nor the recut may touch it. *(Unverified 7.)*
- **`p635.measure.payment_row_absent`** — whether any `firm_registration_payments` row carries a `consumed_firm_id`. The absent branch is the primary branch of the commercial card. *(Unverified 3.)*
- **`p635.measure.download_instrument`** — install `documents-viewer-walk.spec.ts:497-533`'s object-URL recorder and prove it captures `created` / the anchor's `download` attribute / `revoked`. **Playwright's `download` event does NOT fire under this harness** (`documents-viewer-walk.spec.ts:498-501`) — **orchestrator ruling, supersedes gap-635's Tests leg (7) and Unverified 11**: the CSV walk leg asserts the recorder; the CSV's BYTES are asserted in the unit cell, where they can be read.
- **`p635.measure.fixture_census`** — run `e2e/e2e-fixture-ownership.test.ts` after the `serve-built.mjs` defaults land. **Orchestrator ruling (supersedes gap-635's `firm-commercial-mock.mjs` and SYNTHESIS §3.3's shared-verb row): ship the walk in the `personal-settings-walk.spec.ts` shape — `page.route` inside the spec, NO lane mock file — so `LANE_MOCKS`, `LANE_DECLARATIONS` and `SHARED_RPC_VERBS` need no edit at all and one nine-lane shared file stays untouched.** If you nevertheless introduce a lane mock, the two legal verbs MUST then be declared in `SHARED_RPC_VERBS` as exactly `["firm-commercial-mock.mjs","fs4-checkout-mock.mjs"]` and a `LANE_DECLARATIONS` row added — measured: `fs4-checkout-mock.mjs:283,:288` uses the `fn === "…"` shape, which `RPC_VERB_OPENER` (`:1077`) **does** match, so the census will see two claimants and an undeclared share reds (and a declaration with no second claimant reds the other way).
- **`p635.measure.serve_built_defaults`** — `firm-navigation-walk.spec.ts:175-197` walks `/settings/firm` as **owner and bookkeeper with no mock for your three verbs**. Add generic honest defaults to `serve-built.mjs` in the `get_my_preferences` idiom (`:794-804`): legal standing whose `can_accept_for_firm` follows the CORE's own email-prefix persona (`:553-577`), the beta plan row with `amounts_ruled:false`, `payment.recorded:false`, `invoices.available:false`, the `0007`/`0090` capacity defaults, and an empty usage table. Then re-run that spec: both legacy sentences visible, axe-zero at both ranks.

**DB battery — `packages/db/tests/firm-commercial-settings.test.mjs`** (+ `-fixtures.mjs`, + the gate module), every assertion through `humanQuery` personas under real least-privileged roles; **mint the admin through `addMember(alice,{firm:firmA,user:<new>,role:'admin',opKey})` (`rig-fixtures.mjs:44`)** — `buildWorld()` has no admin and raw DML for the subject under test is forbidden.

1. `p635.db.legal_standing_shape` — one entry per kind by `0185:653-659`'s selection, version/status/effective_from present, **no `body` key**. *Red: the function does not exist.*
2. `p635.db.legal_standing_live_true` — an active owner holding both current acceptances ⇒ `standing_live:true`, **equal to what `_accounting_work_egress_live`'s limb (a) answers for the same firm**.
3. `p635.db.legal_standing_owner_removed` — demote/remove that owner ⇒ flips false with no data change.
4. `p635.db.legal_standing_new_version` — publish terms v2 ⇒ flips false **at publication**, before any sweep (`0195:890-892`).
5. `p635.db.legal_standing_two_people` — acceptances split across two different people never make standing live (`0195:896-906`).
6. `p635.db.legal_standing_mask_viewer` — carol gets `masked:true` with `accepted_by`/`accepted_at`/`accepted_by_name` NULL and `firm_accepted` intact.
7. `p635.db.legal_standing_can_accept` — true for an ACTIVE OWNER only: false for admin, bookkeeper, viewer and for a removed owner.
8. `p635.db.legal_standing_no_client_arg` — catalog assertion: arity 0, so a later widening that would re-open 0195's oracle reds here.
9. `p635.db.commercial_admin_only` — bookkeeper and viewer both meet CLR04; admin and owner are admitted.
10. `p635.db.commercial_plan_unruled` — `amounts_ruled:false` travels; the same `is_current` row `get_current_checkout_plan` reads (`0164:134-135`).
11. `p635.db.commercial_payment_booleans` — with a consumed payment row, `recorded:true` and `subscription_present` reflect the column, and **a string scan over the whole jsonb finds no `cus_`/`sub_` value**.
12. `p635.db.commercial_foreign_firm` — firm B's admin sees firm B's zeros, never firm A's payment.
13. `p635.db.commercial_invoices_constant` — `invoices.available=false` with `reason='not_collected'`.
14. **`p635.db.usage_base_floor_added`** — the recut refuses a VIEWER and a BOOKKEEPER with **CLR04** and still answers an admin and an owner. *Red before the migration by construction (`p635.measure.usage_viewer_today` succeeded).*
15. **`p635.db.usage_base_firm_wall_intact`** — dave (owner, firm B) asking for firm A still meets **CLR11 `client_not_in_firm`** (the `f-a9.C19` contract, re-proven at this boundary).
16. **`p635.db.usage_base_prosrc_delta`** — the live `prosrc` differs from the pinned pre-image and a normalised diff shows the ONLY delta is the floor statement, so the recut cannot have smuggled a query change.
17. **`p635.db.usage_base_acl_unmoved`** — `has_function_privilege('clara_authenticated', …)` is still true and PUBLIC still false (the ACL `f-a9-usage-reshape.test.mjs:671-679` pins).
18. `p635.db.usage_wrapper_admin_only` — the wrapper refuses viewer/bookkeeper CLR04 and takes **no firm argument**, so a caller cannot name another firm.
19. `p635.db.usage_buckets_separate` — firm and platform rows come back as separate rows and are never summed by the door.
20. `p635.db.usage_unpriced` — a day with no `llm_price_table` row yields `unpriced_calls>0` and `spend_cents` excluding it.
21. `p635.db.usage_currency_literal` — `price_currency='USD'` on every row **and** `llm_price_table`'s CHECK still pins `'USD'` — the literal asserted against its source, not assumed.
22. `p635.db.acl_census` — the three new doors' ACLs exactly; PUBLIC, `clara_runtime`, `clara_agent_ro` and both wake roles hold nothing.
23. `p635.db.no_new_table_grant` — `legal_*`, `billing_plans`, `firm_registration_payments` still carry zero app-role table grants; `firm_document_limits`' grant is unmoved.
24. `p635.db.accept_replay` — `accept_legal_document` replayed under the same op key returns the **original** `accepted_at` (`0185:766-775`).

**Runtime World leg: NONE — and that is a finding, not an omission.** This journey invokes no Workflow; the report says so in as many words, and **no runtime row is added to `.github/actions/db-live-gates/action.yml`** (the DB battery's gate-chain line still goes in).

**Web unit (node:test + the house render harness); every file added to `apps/web/test/manifest.txt`:**
`components/firm-admin/legal-standing-card.test.tsx` (the seven faces: live / not-live-with-accept / not-live-without-accept / draft-preview / viewer-masked / read-failed / accept-replayed — each its own cell; the accept trigger present **iff** `can_accept_for_firm`) · `accept-legal-dialog.test.tsx` (op key held across a retry; `bodySha256` forwarded byte-identically, asserted on the captured request; `stale_version` re-reads instead of resubmitting; a `draft` renders no accept control) · `commercial-state-card.test.tsx` (`amounts_ruled:false` renders **no numeral** — a regex over rendered text; absent payment is a named zero, not Empty; no "Manage billing" control at any rank; **a CLR04 on a re-read clears the previously rendered figures**) · `ai-usage-card.test.tsx` (buckets never summed; `unpriced_calls` surfaces; the currency label present whenever a figure is; URL period round-trip; the CSV action absent on an empty period; **a CLR04 on a visibility re-read clears the rows and renders the denied face**) · `lib/firm/usage-csv.test.ts` (quoting, escaping, no reordering; the provenance header carries the firm, the UTC window and the currency; an `unpriced_calls>0` period says so in the file) · `lib/firm/commercial-reads.test.ts` (the decoders drop an unreadable row; the door-name constants) · `components/firm-admin/firm-settings-a11y.test.tsx` (axe-zero for viewer / bookkeeper / admin / owner, with the two pinned legacy cards present at every rank) · `lib/firm/capabilities.test.ts` (extended by the one new floor row — the existing census walks it) · **`components/firm-admin/firm-admin-pages-a11y.test.tsx:97` updated in the SAME commit** to the replacement sentence, deliberately and with the reason beside it.

**Playwright — `apps/web/e2e/firm-commercial-walk.spec.ts`**, D1 template (`personal-settings-walk.spec.ts:1-45`): stateful `page.route` pairs inside the spec; `serve-built.mjs` gains only the generic defaults. Ten legs: (1) an owner lands on `/settings/firm`, sees five cards and a live legal standing; (2) the mock publishes terms v2 → reload shows the warning naming the consequence → the owner accepts → the banner clears **and survives a reload** (a persistent outcome, not a toast); (3) a replayed accept shows the original instant; (4) a **bookkeeper** sees no commercial and no usage card, axe clean; (5) a typed deep link as a bookkeeper meets the door's CLR04 and renders the DB's own sentence; (6) the month selector changes the URL, Back restores the prior month, the two buckets render as two labelled groups; (7) the CSV action creates an object URL and an anchor whose `download` attribute carries the filename, and revokes the URL (**the recorder — not `waitForEvent('download')`**); (8) **mid-session demotion**: the mock flips the door to CLR04, the page is backgrounded and refocused, and the commercial figures are **gone rather than stale**; (9) 320px + 200% zoom + keyboard/focus return after the dialog + reduced motion; (10) axe at WCAG 2.1 AA on every leg. **Admin rank is proven in the battery and the unit cells, not in the browser** — `serve-built.mjs:553-577` has no rank-2 persona and **you do not add one** (nine lanes share that CORE); name it as a residual.

## 5. Risks

- **`apps/web/messages/en.json` — you REPLACE `Settings.unbuiltNote` while nine lanes add keys** (SYNTHESIS §7.1 risk 5). A replacement is the collision-prone shape: land it in one early commit with the a11y cell updated in the same commit, and say so in the report. Your hunk wins on that one key; every other lane's additions survive beside it.
- **`lib/firm/capabilities.ts`** — one appended FLOOR row plus the key on three structures in the same file. #625 (merged) set the file's shape; #871/#872 may touch its invite neighbours. Keep the diff to those four places.
- **`packages/db/package.json` gate chain (nine lanes)** — migration order, not alphabetical; yours is last. **`.github/actions/db-live-gates/action.yml` (nine lanes)** — per-line `\` continuations, your own step order. **`packages/db/tests/rig-meta.mjs` (nine lanes)** — yours is a comment, so the `];` merge hazard is not yours to create; do not add an array. **`CONTEXT.md` (ten lanes, ~37 terms)** — three of them yours, sorted, no other lane claims your words.
- **Blast radius of the recut** — an under-ranked caller of `get_llm_usage_summary` moves from a successful read (own firm) or CLR11 (another firm) to **CLR04**. No product code and no existing cell exercises that class; the hosted estate could in principle hold an out-of-repo consumer (§7.4 #16 — proceed, ask the owner in the wave report). Measured green-by-construction: the ACL cell `f-a9-usage-reshape.test.mjs:671-679` asserts a GRANT a body floor does not touch; every behaviour persona there is a firm OWNER (`rig-fixtures.mjs:421`); `rig-meta.mjs:358-364` declares the grant unchanged; no prosrc sha is pinned on this body anywhere; and `f-a9-pr-1b.test.mjs:215`'s whole-catalog scan for `daily_token_limit|sweep_budget_share|sales_admission_daily_cap` finds none of them in your body. **Run `f-a9-usage-reshape.test.mjs` FIRST anyway — if a cell reds, the recut is wrong, not the cell.**
- **Census suites that red on sight, all to be green in the same branch**: `firm-admin-pages-a11y.test.tsx:97` (the replaced sentence) and `:253-263` (the page composition — keep both legacy cards); `tests/sql-oracle.test.ts` (a new migration enters its corpus); `packages/db/tests/operation-census.test.mjs` (three new names, one changed body); `lib/firm/capabilities.test.ts:134-166` (the cited line must sit inside a **surviving** body containing `clara._human_ctx(clara.role_rank('admin'))` verbatim — re-read the line number off the FINISHED file); **`checkout-gate-c3.test.mjs`'s `c3.53` money-store roster** — `get_firm_commercial_state` reads `firm_registration_payments`, so insert `"get_firm_commercial_state"` into the `deepEqual` list **between `claim_paid_firm` and `get_own_checkout_progress`** (it is sorted by `proname`) with the reason beside the name, exactly as `0164:39-47` and the `_operator_support_cases` / `resolve_operator_support_applicants` widenings did; `tests/firm-scope-surfaces.test.ts` / `firm-scope-fourth-entrance.test.ts` (no new route and no second `requireFirmScope()` — should stay green, **verify**).
- **Behaviour blast radius**: `firm-navigation-walk.spec.ts:175-197` (axe-zero at `/settings/firm` for owner and bookkeeper, both legacy sentences, no threshold control) and `shell-migration-walk.spec.ts:133` (the `<h1>` "Firm settings" — **do not rename the page**). Five cards, a dialog, a table and a download action must keep those green at every rank.
- **The revocation layer is focus-driven, not push-driven** — a backgrounded tab never refocused keeps its last payload. Named, not hidden.
- **`firm_document_limits` stays viewer-readable** (`0007:810`) — rendering it behind admin+ is an **affordance, not a wall**; say so in the panel's header comment (the `capabilities.ts:19-23` idiom) and file the follow-up.
- **C81.5 is not answered here** — pool/concurrency remeasurement needs a load run on a real stack; recommend a separate ops ticket rather than letting a settings page carry a number nobody measured.
- **Host contention**: ten lanes on 24 cores / 32 GB; `next build` may panic `0xc0000142` — retry once (#869). Known Windows reds you must NOT "fix": #707, #693, no `pg_dump` on PATH, `thread-live-clarify.test.tsx`'s whole-suite load flake, `rig-isolation.test.mjs` T10b after a World bootstrap (#866 — you run no World leg, so it should not arise).

## 6. Effort and rig

**Effort: L** (DECISIONS §7). Two new governed doors plus **one governed recut with a rig-measured pre-image sha**, a 24-cell battery under real roles, a full eight-state matrix across five cards, and a browser walk with a publish-a-new-version leg and a mid-session-demotion leg, against three shared-file censuses that red on sight. **Not XL**: no relation created, altered or dropped; no successor; no runtime change; no Workflow leg; no `ui:add`. The one thing that would push it to XL is an owner ruling that the firm may **set** a commercial value — nothing in the estate supports a write today (`0196:36-40`; `billing_plans` has no door at all), so that would be a new relation-touching migration plus a governed write door plus receipts.

**Rig** (RIG.md): worktree `C:\Users\zhant\Desktop\clara-wt\635`, branch `impl/635-firm-commercial-settings`, PG **55701** / db **clara_635**. Bash: `export PATH="/c/Users/zhant/AppData/Local/pnpm:$PATH"`, then `export PGHOST=127.0.0.1 PGPORT=55701 PGUSER=postgres PGDATABASE=clara_635 CLARA_ALLOW_DESTRUCTIVE=1 CLARA_RIG_DB=1`. **Never** set `CLARA_RIG_ALLOW_RESET=1` or `CLARA_RIG_ALLOW_ROLE_SWEEP=1`. No `psql` on Windows — use node + `pg`. Playwright triple: `CLARA_E2E_APP_ORIGIN=https://127.0.0.1:3300 CLARA_E2E_NEXT_PORT=3301 CLARA_E2E_RUNTIME_PORT=3302`; always go through `pnpm --filter @clara/web e2e firm-commercial` (`npx playwright test` serves a stale build, #865).

**Before the final report, run and report counts for**: `pnpm typecheck` and `pnpm lint` (worktree root, both green); the **whole `apps/web` unit suite** once (`node scripts/run-tests.mjs` from `apps/web`, ~5 min); from `packages/db`, your battery with the **40** `--import ./tests/*-preintegration-gate.mjs` flags copied verbatim from `package.json` **plus your own gate**, then a **focused** run of your battery **without** its gate, which must **FAIL loudly** below 0233; `packages/db/tests/operation-census.test.mjs` and `rig-isolation.test.mjs` (no reset flags — this slice adds SQL functions); `f-a9-usage-reshape.test.mjs`, `f-a9-pr-1b.test.mjs` and `checkout-gate-c3.test.mjs` (the recut's blast radius); from `apps/web`, `e2e/e2e-fixture-ownership.test.ts`, `tests/sql-oracle.test.ts`, `tests/firm-scope-surfaces.test.ts`, `tests/firm-scope-fourth-entrance.test.ts`, `lib/firm/capabilities.test.ts`; and the Playwright specs `firm-commercial-walk`, `firm-navigation-walk`, `shell-migration-walk`. **Parts-parity / frozen check**: run `node scripts/check-frozen-workflows.mjs` and `node packages/runtime/scripts/check-parts-parity.mjs` anyway and **report the counts** — a green there is the evidence for the "no successor" verdict (SYNTHESIS §7.2).

**Copy the house shapes from**: `packages/db/migrations/0224_preview_invite.sql` (header, prestate sha-pin loop `:153-171`, `set role` / grant matrix / `reset role`, tail census) and `0185_legal_acceptance.sql` (the legal family's own posture); `packages/db/tests/preview-invite.test.mjs` + `preview-invite-preintegration-gate.mjs` (battery + gate + focused-run discipline — delivered at `ebe0b1c7`, `git show --stat ebe0b1c7`); `checkout-gate-c3.test.mjs:58,:2128-2199` (the prosrc-pin idiom and the reviewed roster widening); `apps/web/e2e/personal-settings-walk.spec.ts` with `serve-built.mjs:794-804` (the D1 walk and its generic default — delivered at `3d728770`, `git show --stat 3d728770`); `apps/web/e2e/documents-viewer-walk.spec.ts:497-533` (the download recorder); and `docs/plan/active/refresh-wave-2026-09-15/brief-649.md` + `../refresh-wave-2026-09-14/brief-640.md` for the brief-to-delivery shape, with `.../refresh-wave-2026-09-15/reports/` for what a reviewer later demanded — **behavioural** cells rather than `prosrc` assertions, re-measured rather than copied evidence, an explicit merge-order collision table, and every claim carrying the command that produced it with its pass/fail counts.

## Verifier findings not applied

**None — nothing was refuted back.** The verifier returned ONE finding (F1, severity `note`, verdict `ready`) and it is **applied** above.

- **F1 — provenance of the `tree.ts` → `en.json` refinement** (Orchestrator-decisions bullet). Applied, and re-measured while applying rather than taken on the verifier's word: `DECISIONS.md:119` carries only the generic all-lanes shared-file row for `lib/navigation/tree.ts` with **no #635 attribution**; the #635-specific claim lives at `SYNTHESIS.md:354` (`E (sections.firm.purpose string)`, column 635). The bullet now cites both and labels what it supersedes — a SYNTHESIS observation (advisory), not a DECISIONS ruling (binding). The underlying technical facts were re-measured on disk at `abcc5030` and stand unchanged: `apps/web/lib/navigation/tree.ts:276` = `purposeKey: "sections.firm.purpose"` (a key, no prose) and `apps/web/messages/en.json:383` = `"purpose": "Review the firm-wide authority controls."`. **Nothing that gets built changed**: the ticket still edits `en.json` and still does not open `tree.ts` (§3 slice, §4's e2e ruling and §5's shared-file risk list are unaffected).
