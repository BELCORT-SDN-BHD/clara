# Brief: #655 — 完整记录发票、账单及对应应收应付

*B6 chat entrance (contract only) · C1 source intake (cited document) · C3 typed accounting operation · C6 receivables/payables (birth only) → ONE classified business object + exact-cent balanced journal + due-dated signed AR/AP open item + receipt, in ONE commit, on the Work lane. Written from `DECISIONS.md`, `SYNTHESIS.md`, `gap-655.md`, `WORK-ORDER.md`, `RIG.md`, `GAP-ORDER.md` §2, the ticket (no comments), appendix C (B6/C1/C3/C6) and appendix D, at `main@abcc5030`.*

## Orchestrator decisions (binding)

- **Migration `0225` only, ONE file `packages/db/migrations/0225_trade_invoices.sql`** (DECISIONS §2.1). Stable stem — the gate module is `packages/db/tests/trade-invoice-preintegration-gate.mjs` (unnumbered stem, SYNTHESIS §2.4 rule 2) and the cohort constant is `TRADE_INVOICES_0225_COHORT` in `packages/db/tests/rig-meta.mjs` (the number lives on the cohort, as `STAFF_EXPENSE_CLAIMS_0221_COHORT` at `rig-meta.mjs:1980` and `PREVIEW_INVITE_0224_COHORT` at `:2311` already spell it). The gate chain in `packages/db/package.json` runs in **migration order, not alphabetical** — your `--import ./tests/trade-invoice-preintegration-gate.mjs` is the **41st**, appended **after** `preview-invite-preintegration-gate.mjs` (measured this pass: the chain holds exactly 40 today). A **focused run of your battery without its gate must FAIL loudly** below 0225; a skipped frontier-gated battery is not evidence (§4).
- **0225 CREATES**: (a) `clara.trade_invoices` — the typed business object, `0221:344-360`'s tenant-carrying shape; (b) `clara.trade_invoice_status` — the append-only child ledger (`state in ('admitted','posted','refused')`, `unique (invoice_id, state)`, carrying `entry_id` **and** `receipt_id` on the `posted` row), the `clara.staff_expense_claim_status` shape at `0221:533-556`; (c) an **AFTER INSERT trigger on `clara.operation_receipts`** `for each row when (new.outcome = 'committed')` appending that `posted` row — `clara._tf_staff_expense_claim_posted()` / `t_operation_receipts_staff_expense_claim_posted` (`0221:1423-1443`) is the shape, structurally verbatim; (d) `clara.admit_trade_invoice_work(...)`, granted to `clara_runtime` and to **nothing else**; (e) `clara._assert_trade_invoice_basis(...)`, ungranted to every application role; (f) the **lane-agnostic deferred constraint trigger** on `clara.journal_entries` that births the `open_items` row; (g) the read `clara.get_trade_invoice(p_work uuid)`.
- **Door signatures, floors and grants — exact, and they are the wire contract for the runtime route and the v21 tool alike:**
  - `clara.admit_trade_invoice_work(p_client uuid, p_author uuid, p_intent_key text, p_kind text, p_particulars jsonb, p_basis jsonb, p_basis_origin text, p_source_refs jsonb, p_model text) returns jsonb` — `security definer`, `set search_path = clara, pg_temp`, owner `clara_fn_owner`. **`grant execute … to clara_runtime` ONLY.** There is **NO `_for` twin and NO `clara_authenticated` door** — `0221:1202-1206` rules it verbatim ("Work admission on this lane is a runtime act OBO a named human — 0194:1316-1318 grants exactly that … A second door with a different authority model would be a second answer to 'who admitted this'"), `0221:1880-1888` asserts both halves of the negative in the tail, and `apps/web/lib/work/api.ts:1-11` states the same law for the whole lane (admission also ENQUEUES a run, which PostgREST cannot produce). Copy both tail assertions.
  - `clara._assert_trade_invoice_basis(p_client uuid, p_kind text, p_particulars jsonb, p_basis jsonb, p_world boolean) returns void` — `stable security definer`, `revoke all … from public`, granted to nobody. `p_world => false` at the cheap check, `true` under the advisory rung.
  - `clara.get_trade_invoice(p_work uuid) returns jsonb` — `stable security definer`, `_human_ctx(clara.role_rank('viewer'))` floor (the `get_work_claim_origin` precedent, asserted granted to `clara_authenticated` at `0221:1889-1893`), granted to `clara_authenticated` **and** `clara_runtime`.
- **0225 RECUTS EXACTLY ONE BODY — `clara._record_journal_entry_core(uuid,uuid,text,uuid,uuid,text,jsonb,text,text,text)`, the SIXTH full copy.** Creation → splice history: born `0178:1223` → `0182:763` → `0184:860` → `0194:1364` → `0195:1685` → **live `0204:152-761`** (measured this pass: the body opens at `0204:152`; the file is 892 lines). **ONE new conditional** at §7's control-leg arm (`0204:550-570`; measured this pass, the CLR10 raise is at `0204:566-570`): the refusal stands **unless `clara.trade_invoices` holds a row for this `work_id`**, and inside that branch the core (i) asserts the control leg's `coa_accounts.account_class` matches the row's `kind` (`sales_invoice`⇒`receivable`, `supplier_bill`⇒`payable`) and (ii) **after** the `journal_lines` INSERT (`0204:644-649`) stamps `counterparty_id` on the control lines from the row — the `_approve_entry_core` idiom (`0037:1885-1888`). It writes **NO back-pointer** into `clara.trade_invoices`: `0221:350-355` states the rule and its reason verbatim ("THE ENTRY AND THE RECEIPT ARE DERIVABLE BY JOIN, NEVER STORED HERE … Storing them would need an UPDATE of an append-only row at posting time, which is exactly the shape this table refuses"). The purpose filter at `0204:178-181` carries `aw.purpose in ('journal_entry','periodic_stock_adjustment','payroll_obligation')` **verbatim**, or `0194:2180`'s `position(...)` probe fails on the from-scratch chain (SYNTHESIS §0.4). *(Orchestrator ruling — supersedes gap-655 §5's citation: the filter is at `0204:180`, not `0204:189-191`; SYNTHESIS §0 measured it.)*
- **0225 does NOT recut**, each with its reason: `_admit_accounting_work_core` (`0194:1062-1064`) — it takes no `p_work` and mints the id itself (`0194:1231`) and answers a repeated key with `replayed:true` and the SAME id (`0194:1170-1189`, and again out of the concurrent-race arm at `0194:1239-1256`), so the typed row can only be written **after** it returns; `_subledger_on_approve` (`0037:1050`) — you do **not** become its seventh caller, the caller set stays the measured SIX (`0216:938-947`); `_validate_entry_lines` (`0009:257`) — it drops every key but `account_code/debit_cents/credit_cents/description` (`0009:295-299`), which is exactly why the party is stamped after the insert, and recutting it would touch ten call sites plus four body-census pins; `_assert_journal_basis` (`0178:693`); `_approve_entry_core`; `list_accounting_work` / `get_accounting_work_row` (**FROZEN for this wave**, SYNTHESIS §3.2 — #905 owns the receipt-dated widening); `trial_balance_as_of`; the whole opening ladder (`_approve_opening_entry`, `item_kind='opening'`, `_subledger_classify_entry` LADDER 2).
- **Pins are MEASURED on your rig, never transcribed** (WORK-ORDER rule 8; SYNTHESIS §2.4 rule 3, §7.4 #1). Take the pre-image `sha256(prosrc)` off `pg_proc` on `clara_655` after `pnpm db:migrate`. Pin the recut target's live body, and pin these five as **non-regression** prestate pins re-asserted byte-identical in the tail: `_admit_accounting_work_core`; `_subledger_on_approve` (**a composite** — born `0037:1050`, spliced `0040:6004-6018` for the due-date stamp and `0041:4530` for the FA hook; 0037's text is NOT the live body); `_validate_entry_lines`; `_subledger_classify_entry` (`0037:992-1027`); `_approve_entry_core` (the NINTH body, `0106 §E`). A pin written from file text will not match and 0225 refuses to apply.
- **D11 (binding).** 发票/账单要不要重切过账核心？ **切第六次，只开一条极窄的口**: the control-account leg is admitted **only** when the Work has a `clara.trade_invoices` row; the other sixteen refusals stay byte-for-byte and **all seventeen tokens are re-asserted by name in the tail**. The upload autodraft lane **is not moved this wave**; you ship the "two lanes produce equivalent accounting" test instead. *This is DECISIONS §6's explicit override of the 2026-09-15 §1.3 "no core recut" rule, for #655 only; you are the wave's sole recutter of this body (SYNTHESIS §0.1).*
- **D12 (binding), three parts.** (a) **An ambiguous counterparty refuses AT ADMISSION with the candidate list carried verbatim** (`party_ambiguous`); the person picks in the form or in chat and resubmits. A fact discovered mid-run goes through the existing shared question — `claraWork_v4`'s `ASK_QUESTION_TOOL` is already in its closed roster (`claraWork.v4.tools.ts:96-101`, parks at `:344-353`), **which is why no `claraWork_v5` is needed here**. (b) **Credit notes are not this ticket**: a credit-note-shaped payload is refused **by name**, `credit_shape_not_admitted`, raised as a typed CLR10 **before** the `kind` column CHECK could fire as an unclassifiable 23514 — `0194:1078-1081` forbids exactly that pattern ("a 23514 out of the INSERT carries no typed reason and the runtime could not classify it"). (c) **Due date**: stated on the document wins, the counterparty's terms are the fallback, neither ⇒ honestly **absent**; the origin is a durable column `due_date_source in ('stated','counterparty_terms','absent')`. The legacy producer (`0040:6008-6018`, `posting_date + counterparties.payment_terms_days` scoped to `item_kind in ('invoice','bill')`) is **left untouched** for the coding lane.
- **D17 / §1.2 (binding).** Two bodies are cut this wave, **by the integration worker after the ten merges, never by you**: `chatTurn_v21` and `claraWork_v5`. **`chatTurn_v21` carries YOUR `start_trade_invoice_work`.** You write the contract stanza (§3, verbatim) and the NEW non-frozen carrier `packages/runtime/lib/trade-invoice-basis.ts`; you cut nothing, edit no frozen file, and do not touch `packages/runtime/workflows/registry.ts`. **No `autoDraft_v11`** (§1.1 — the upload lane stays as it is). **AC3's chat half is "contract delivered, entrance closes at the cut"** — say exactly that in the report; do not claim it built.
- **What freezes on import.** `packages/runtime/lib/trade-invoice-basis.ts` is not in the 296-entry manifest today (SYNTHESIS §1.6) and **freezes forever the moment `chatTurn_v21` imports it**. Therefore **durable rules live in 0225, never in that module** — it carries the zod schema, a builder, a local refusal mapper and a display helper, and nothing a database must enforce (`lib/periodic-adjustment-basis.ts` is the proof: it is in the manifest today, frozen BY CLOSURE).
- **No purpose widening, no part kind, no needs-you row kind.** A trade invoice is a `journal_entry`-purpose Work exactly as #638's claim is. `WORK_ACCEPTED_PURPOSES_V19` (`chatTurn.v19.parts.ts:91`) stays at three; `apps/web/lib/parts/types.ts` / `catalog.ts` / `PartRenderer.tsx` are **not opened** (DECISIONS §6: nobody registers a part kind this wave, so the parts-parity pair must not move); `REVIEW_QUEUE_ROW_KINDS` (`apps/web/lib/firm/needs-you.ts:97-118`) gains nothing (2026-09-15 §1.6, carried).
- **2026-09-15 D11 stays (DECISIONS §6): identity provenance only — #655 writes NO counterparty alias.** You CONSUME 0215 (`counterparty_aliases.recorded_via`, `counterparty_identity_revisions`, the three `_human_ctx`-floored reads, `0215:44-60`); `add_counterparty_alias_for` does not exist and is not yours to build (`chatTurn.v20.tools.ts:24-25` names the omission; open #915).
- **Ownership (§3).** You **OWN**: `packages/db/migrations/0225_trade_invoices.sql`; `packages/runtime/lib/trade-invoice-basis.ts`; `packages/runtime/src/workRoutes.ts`'s new `POST /api/work/trade-invoice`; `apps/web/components/accounting/accounting-hub.tsx` (88 lines — one more primary act); `apps/web/lib/journals/api.ts` (**you are the journals-workbench owner this wave**, SYNTHESIS §3.1 — #656 adds `is_opening_balance` to `ENTRY_SELECT` + one badge **in ITS OWN hunk** — DECISIONS §3 grants that hunk outright, so owning the file means keeping its shape stable for it, never vetoing it; #660 only links to the existing `?entry=` address); the new route, form, draft module, battery, World leg, walk and mock. You may touch with **ONE hunk**: `apps/web/components/work/work-detail.tsx` (1332 lines) — **ONE link block inside an existing block**, never a restructure; **#658 owns the Sources tab's new content and lands first, then you, then #636**. You must **NOT open**: `components/clara/*`, `lib/clara/*`, `lib/parts/*` (#642); `components/documents/*`, `lib/documents/useUploadQueue.ts` (#636/#642); `components/bank/*`, `lib/bank/*` (#657); `components/firm/firm-home/*` (#659); `components/firm/client-workspace-overview.tsx`, `client-home/*` (#660); `components/registers/*` (#651/#656); `components/firm-admin/*`, `/settings/*` (#635); `e2e/home-board-walk.spec.ts` + `home-board-mock.mjs` (#659/#660); `apps/web/lib/firm/use-review-queue.ts` (#659).
- **CONTEXT terms YOU write** (§3.1, house `term` / `_Avoid_` shape; integration unions them): ***Trade invoice*** — and with it the vocabulary boundary agreed with #635: unqualified "invoice" = **a client's accounting document**; the firm's own billing document is a "subscription invoice" and **does not exist**; ***Due-date basis*** (stated / counterparty_terms / absent); and the boundary sentence that a trade invoice's `lines` are **journal basis lines, never extracted invoice line items** (#782 — `ARCHITECTURE:529` records line items as `planned`). Amend **Open item** (`CONTEXT.md:407`) and **Control account** (`:419`) in the same commit.
- **Boundaries ruled (§3.2) — decided, not yours to re-open.** #655 **births** the open item, **#657 allocates against it**: you ship no settlement door, no allocation UI and no `settlement_allocation` flag. **#660 / #669**: the AR/AP outstanding tiles are **#669's**, in a later wave, consuming #660's envelope shape and your items; #660 ships cash + profit + two charts only; you add **no aging UI** beyond the ONE link from the Work to its open item. **#636** owns the batch parent and the join and never mints a child Work; you never mint a parent. **#656** delivers the opening **targets** side and — measured this pass and recorded so you do not budget for it — **recuts no governed function, so the serialisation gap-655 §13 feared does not exist** (SYNTHESIS §4, "the feared collision does not exist"). **#666** owns cash purchase and supplier credit; **#662** the open-item list, non-bank settlement, credit application and offsets; **#665** the upload-lane cutover and the thirteen-rung retirement; **#669** the aging display; **#676** posted-effect correction (2026-09-15 D6); **#661** opening AR/AP alignment (blocked by you — do not touch `opening_items` or `opening_item_id`); **#946** must be able to reuse your admission gate **by name**, so build it reusable rather than bespoke.
- **Riders folded in (§5):** **#868** — your tail re-derives and re-pins the stale `open_items`-writer and subledger-caller censuses **in this file only**, and the report says so. **Not folded**: #905, #782, #866 (T10b — report, do not fix), #876, #877 (its Tier-A-complete autodraft fixture is a dependency-if-available, never built here), #904, #913, #921.
- **Non-goals, stated in code comments AND in the report:** admits **no credit shape**; **moves no upload lane**; ships **no settlement/allocation surface**; writes **no counterparty alias**; touches **no opening ladder**; widens **no purpose**; is born with **NO `p_attestation` argument** and the `x37.u`/`x37.v` high-stakes ceremony (`0009:1513-1523`) **must not be reachable from it** — removing `p_attestation` from `clara.approve_entry` is a different lane's retirement and is explicitly **not** yours.
- **Evidence law (§4).** Every AC and every historical row closes with a **red-first cell**, a **browser-walk leg**, or a **named residual**; REDESIGN rows are re-measured, never copied. New DB assertions run through **`humanQuery` least-privileged personas**, never `rootQuery` for the subject under test (labelled fixture DML excepted). The World leg runs on **your own rig** with `WORKFLOW_POSTGRES_URL`. Local ≠ hosted: write **"hosted evidence pending"**. **Never edit `docs/PRD.md` or `docs/ARCHITECTURE.md`** — record **blueprint drift** for #683: `ARCHITECTURE:232-235` (**[已实现]** says confirming an invoice requires its GL and open item as one transaction boundary — measured, the Work lane cannot confirm an invoice at all today, `0204:550-570` + `0216:30-38`; that atomicity exists only on the legacy coding lane), `ARCHITECTURE:529`, `PRD:65`, `PRD:79`.
- **Effort: XL** (§7), lane `claude-opus-5` xhigh. Reviews, the successor cut, integration and release are the orchestrator's.
- **Playwright port triple**: `CLARA_E2E_APP_ORIGIN=https://127.0.0.1:3340 CLARA_E2E_NEXT_PORT=3341 CLARA_E2E_RUNTIME_PORT=3342`. **Rig row (verbatim, RIG.md)**: `| 655 | C:\Users\zhant\Desktop\clara-wt\655 | impl/655-invoice-bill | 55705 | clara_655 | https://127.0.0.1:3340 / 3341 / 3342 | ok 434s | 219 applied | ok (2 files) | 219 · 0224 · PG 17.11 |`. (That is RIG.md:24 character for character; its smoke column abbreviates the stem to `0224`. The file on disk is `packages/db/migrations/0224_preview_invite.sql` — frontier 219 migrations applied, newest stem `0224_preview_invite`, PG 17.11 — and §6 spells the stem out as a fact, not as a quote.)

## 1. Current state

**DB — the Work lane (where this lands).** `clara.accounting_work.purpose` is a closed set of three, enforced at the column CHECK (`0194:230`), the receipts CHECK (`0194:232-233`), `_admit_accounting_work_core`'s own guard (`0194:1078-1082`) and the posting core's Work lookup (`0204:178-181`), with a census pin at `0194:2180`. Admission doors: `clara.admit_journal_work(uuid,uuid,text,jsonb,text,jsonb,text)` (`0194:1281`) and `clara.admit_periodic_adjustment_work(...)` (`0194:1298`), both `clara_runtime` only, both thin wrappers over `clara._admit_accounting_work_core(p_client,p_author,p_intent_key,p_purpose,p_basis,p_adjustment,p_basis_origin,p_source_refs,p_model)` (`0194:1062-1064`); **#638's `clara.admit_staff_expense_claim_work` (`0221:1222`) is the third sibling and the precedent you copy step for step**. Basis shape `clara._assert_journal_basis(jsonb)` (`0178:693`) = `posting_date` + `memo` + `currency` + `lines`; **no party, no reference, no due date, no tax facts**. Idempotence is `uq_accounting_work_intent unique (firm_id, client_id, intent_key)` (`0178:336`) plus the replay probe and its three-half payload comparison (`0194:1164-1190`) and the concurrent-race re-read (`0194:1239-1256`) — **never** `_reserve_op`, which is born `0004:46` and is the POSTING core's reservation (`0204:504`). Live posting core `0204:152-761`: Work lookup FOR UPDATE + purpose filter (`:178-181`) → cancel/settled/abort arms (`:214-235`) → logical-op identity → egress gate → #787 reversal-liveness → `_reserve_op` → basis echo + digest wall (`:520-525`, `basis_mismatch`) → `_assert_adjustment_relationships` → chart-at-commit → **§7 control-leg rule (`:550-570`, `generic_control_leg`)** → #634 evidence re-read (`:572-594`) → `_validate_entry_lines` (`:638`) → draft INSERT (`:639-643`) → lines INSERT (`:644-649`, four fields only) → `_assert_balanced` (`:650`) → raw `set status='approved'` (`:651-654`) → `operation_receipts` (`:671-678`) → `entry_evidence_links` (`:687-700`) → `periodic_adjustments` → Work `result` (`:735`) → `_audit` → `_finish_op`. Its tail pins **seventeen** refusal tokens by name at `0204:776-782` (`egress_not_authorized, work_cancelled, work_settled, obo_not_initiator, basis_mismatch, generic_control_leg, source_conflict, periodic_adjustments, adjustment_basis, operation_payload_conflict, unknown_account, client_inactive, logical_op_mismatch, obo_not_active, insufficient_role, work_not_found, wake_task_unbound`), plus `_work_egress_event_seq` (`:786-788`), the consent/activation re-read (`:789-791`), the frozen bundle id and two grandfathered ids. Wake wrapper `clara.wake_record_journal_entry(...)` (`0178:1486`, allowlisted for `interactive_client` only, `0178:1496`; `0107:251-257` states `interactive_client` "is not a posting row"). Work questions: `clara.open_work_question(p_task,p_hook_token,…)` (`0180:578`, recut `0184:1581`, granted `clara_runtime` only, `0180:686`), `clara.answer_work_question` (`0180:761`, recut `0200:406`).

**DB — the subledger.** `clara.open_items` (`0037:726-781`): domain `ar|ap`, `counterparty_id` NOT NULL, `item_kind` in seven values, `item_date` NOT NULL, `due_date` NULLABLE, grain `uq_open_items_grain (entry_id, domain, counterparty_id)`. `ck_open_items_kind_matrix` (`0037:764-769`, measured) is the sign law: `invoice`⇒ar&&>0, `credit_note`⇒ar&&<0, `bill`⇒ap&&>0, `settlement`⇒<0, `adjustment/opening/reversal_unwind` bimodal. The ONE writer today is `clara._subledger_on_approve(uuid)` (`0037:1050`), whose kind comes from `journal_entries.coding_kind` through `_subledger_classify_entry` (`0037:995-1007`: **NULL ⇒ `'adjustment'`**), whose amounts are the signed control net per canonical counterparty (`0037:1012-1026`, `_canonical_counterparty` born `0011:1316`), whose counterparty-kind ↔ domain refusal is at `0037:1088-1096`, whose insert uses `on conflict on constraint uq_open_items_grain do nothing` (`0037:1103-1111` / spliced `0040:6006-6018`) and whose `open_item.created` event payload is at `0037:1111-1118`. Due date is produced **only** by 0040's splice (`0040:6010-6015`, `posting_date + counterparties.payment_terms_days`, scoped to `item_kind in ('invoice','bill')`); `payment_terms_days` (`0040:754-760`, 1–365) is written only by `set_counterparty_terms`; `0037:717` records the pre-0040 truth, "due_date ships as a nullable column with NO PRODUCER". **The Work lane reaches none of this** — measured for #639 and written at `0216:30-38`.

**DB — stale censuses and the birth precedent.** `0037:3774-3782` pins the approve-path set at FOUR and `0037:3841-3847` pins the subledger-hook caller set at FOUR; both are **stale** — `0216:938-947` re-derived the callers as **SIX** behind a fail-closed whole-catalog `prosrc` scan, and `0216:948-952` asserts the FA birth trigger does **not** call the hook. `0037:3830-3833` pins the `open_items` writer set at **ONE**. #639's instrument is the model: `clara._tf_fa_acquisition_birth()` + `create constraint trigger t_je_fa_acquisition_birth after insert or update on clara.journal_entries deferrable initially deferred for each row when (new.status = 'approved')` (`0216:293-353`), with its stated reason (an entry may be born approved on a lane that does not draft first) and its measured physics recorded in the comment: *"deferred triggers fire in alphabetical trigger-name order — measured on clara_639, PG 17.11"* (`0216:347-349`).

**DB — the coding lane (today's only invoice/bill producer)** and **evidence**: `_agent_post_entry_core` (`0106:866`, rungs B1–B11/B14/B15, `B14 generic_control_leg`), `_approve_entry_core` (ninth body, `0106 §E`, stamps `journal_lines.counterparty_id` on payable/receivable lines, `0037:1885-1888`), human `draft_entry` passes `null` for `p_coding_kind` (`0009:1424-1425`) so **a human can never create a coded invoice entry**; `approve_entry(p_entry,p_expected_revision,p_attestation,p_op_key)` (`0009:1523`) still carries an attestation gated by `is_high_stakes` (`0009:1513-1521`). `entry_evidence_links` + `uq_entry_evidence_links_document` (partial unique, `0182:345`), `_document_posting_entry` reads BOTH lanes (`0182:578`), 0197 (#718/#821) makes both walls take `clara.documents FOR UPDATE` first. `ix_operation_receipts_work` (`0178:451`) and `ix_operation_receipts_entry on clara.operation_receipts((effects->>'entry_id')) where (outcome='committed')` (`0178:454-455`, measured) — compared as **TEXT**, never a uuid cast of the jsonb expression.

**Runtime.** Registry pins at base: `chatTurn_v20` (`registry.ts:173`), `claraWork_v4` (`:270`), `documentIngest_v2`, `invoiceFacts_v1`, `autoDraft_v10`. `frozen-workflows.json` holds 296 entries, **every one `deployed:true`** including all five `chatTurn.v20.*` and all six `claraWork.v4.*` — the "NOT deploy-locked yet" sentence inside each entry's `note` is stale prose, refuted in gap-655 §15 C-F1 and re-verified by SYNTHESIS §0; `scripts/check-frozen-workflows.mjs:425-427` makes a deployed hash immutable ("Ship a new `_vN`"). `claraWork_v4`'s roster is `ASK_QUESTION_TOOL, LIST_ACCOUNTS_TOOL, RECORD_JOURNAL_ENTRY_TOOL (+ ANSWER_ACCRUAL_TERM_TOOL, ASK_KNOWLEDGE_CONFLICT_TOOL)` (`claraWork.v4.tools.ts:96-101`, measured); `runRecordJournalEntryV4` (`:244-315`) is a thin pass-through that **asserts nothing about the lines**, with the `exit_after_commit` crash barrier at `:310-313`. `applyParticularsStepV4` (`claraWork.v4.impl.ts:22-24`, `:742-765`) is hard-coded to `complete_fixed_asset_particulars_for` — FA-specific and frozen, which is why party resolution stays at admission (D12a). `workRoutes.ts` is **not** a frozen-manifest file: `POST /api/work/journal` at `:873`, `/periodic-adjustment` at `:960`, `/staff-expense-claim` at `:1054` (measured). The upload lane (`intake-lanes.mjs:53-59` → `_enqueue_invoice_facts_core` `0009:659` → `invoiceFacts_v1` → `autodraft.mjs:38-46` → `autoDraft_v10` → `wake_post_entry` → `_agent_post_entry_core` → open items) creates **no `accounting_work` row at all**.

**Web.** `/clients/[clientId]/accounting` hub: ONE primary act, "Record a journal entry", gated by `canOpenClientLeaf(scope, "journalComposer")` (`accounting-hub.tsx:48-58`, measured). Sibling typed-operation routes exist and are the shape to copy: `accounting/journal/new` → `journal-composer.tsx` (767 lines; intent key in `sessionStorage` under user+firm+client, one silent re-POST on a lost response, `StateBanner` per refusal, 202 ⇒ the Work's page), `accounting/adjustments/new`, `accounting/claims/new` → `staff-expense-claim-form.tsx` (its page file states the "route not Dialog" reasoning verbatim). A leaf is registered in **three** places in `apps/web/lib/navigation/tree.ts`: the `ClientLeafId` union (`:121`), the `CLIENT_LEAVES` row (`:444-457`, each with its bookkeeper-floor comment) and an href helper beside `journalComposerHref` (`:498-500`). AR/AP **reads** are already built at `/registers` (`aging-register.tsx`, `lib/registers/aging.ts:28-58` carrying `item_id/item_kind/item_date/due_date/overdue/outstanding_cents/bucket/recorded_counterparty_id`), with nine counterparty/allocation dialogs. `lib/work/api.ts:262` is the staff-claim POST to copy (camelCase in, typed outcomes back, `lost` ≠ `unavailable`).

**Tests.** DB: `x37-wave-c-a-subledger.test.mjs` (30 cells — `x37.a` tie-from-zero, `x37.c` one typed supplier_bill ⇒ one AP item, **`x37.c2`** due-date exactness, `x37.l` reversal matrix, `x37.aa` grain/append-only/FORCE-RLS/DDL matrix, `x37.ab` AP mirror, `x37.u/v` attestation paths), `x40-wave-c-c-tieout.test.mjs`, `x46-wave-7a-sales-lane.test.mjs` (several `skip`ped), `coding-lane-evidence-link.test.mjs`, `fixed-asset-acquisition.test.mjs` (incl. **`p639.census.approve_paths` at `:242-267`**, measured: it asserts the Work-lane core IS an approve path and does NOT call the hook), `staff-expense-claim.test.mjs` + `-fixtures.mjs` + `-preintegration-gate.mjs` (the three-file house shape). Runtime: `work-journal-db.test.mjs`, `work-cancel-e2e.mjs`, `clara-work-v4.test.mjs`, `staff-expense-claim-e2e.mjs`. Playwright: `journal-work-walk.spec.ts`, `staff-expense-claim-walk.spec.ts`, `periodic-adjustment-walk.spec.ts` and their mocks. **No invoice/bill anything, anywhere.**

## 2. Gaps / rows

| Row | Disposition | The evidence pointer you must produce |
|---|---|---|
| **AC1** explicit invoice/bill intent + typed facts; preserve party role, date, reference, currency, lines/totals, tax facts, due-date basis | **to build** | `clara.trade_invoices` + `_assert_trade_invoice_basis` + `admit_trade_invoice_work`; cells `p655.post.bill_one_commit`, `p655.due.stated`. Today: `0178:693` (basis is four keys), `0194:1281` (no particulars argument), `0009:1424-1425` (human cannot set `coding_kind`) |
| **AC2** atomic object + journal + control open item + receipt under least privilege and open period; wrong client/role/period/source version ⇒ no partial state | **to build** (the atomicity machinery exists; the open-item leg does not) | `p655.post.bill_one_commit`, `p655.atomic.no_partial`, `p655.authority.floors`. Reuse `0204:214-235` / `:520-525` / `:671-678` verbatim |
| **AC3** same economic intent via upload, chat-with-attachment, chat-without-attachment, direct UI; no default approval, no impersonation | **partial — named residual + contract** | Direct UI + document-cited path built and walked; **upload arm closes as `p655.parity.source_vs_direct` (equivalence proof, not a cutover — #665 owns the cutover)**; **chat arm = successor contract only, closes at the `chatTurn_v21` cut**. Non-impersonation is verify-only (`0204:641`, `0106:1319`) |
| **AC4** duplicate / lost-response / restart ⇒ ONE receipt; ambiguous party or missing fact parks the SAME Work through the shared question | **partial → to build on the new lane** | `p655.replay.one_receipt` (the probe at step 4 writes nothing) and the World leg's `exit_after_commit` leg. **Ambiguity is refused at admission (D12a), so the park cell proves a RUN-discovered missing fact (an absent account code) parks and resumes** — `open_work_question` needs a live task + hook token (`0180:578`), which an admission-time ambiguity has not got |
| **AC5** Documents, Work, Journals and AR/AP mutually linked after reload | **partial → to build the link set** | `clara.get_trade_invoice` + the ONE `work-detail.tsx` link block + the JE back-link; walk leg `trade-invoice.walk.reload_links`. The upload lane's half stays a **named residual** (no `accounting_work` row exists there) |
| **AC6** capture client, counterparty role/identity, source or user-stated basis, invoice/bill date, due date, currency, exact amounts, inventing nothing | **partial → to build the missing columns** | `document_date`, `reference`, `due_date` + `due_date_source`, `tax_facts` (opaque, echoed, validated against nothing — the #638 rule); provenance consumed from 0215. Cells `p655.due.stated` / `.terms_fallback` / `.absent` |
| **AC7** sufficient facts execute through the SAME operation for source, direct UI and Clara; missing/conflicting facts ask only the affected Work question | **partial, with the boundary stated** | "The same operation" cannot be built this wave: the wake allowlist (`0107:251-257`) and `entry_post_receipts.via_wake_kind` wall `interactive_client` out of the coding lane. You deliver **one operation for direct UI + Clara + document-cited**, prove **equivalence** with the upload lane (`p655.parity.source_vs_direct`), and name #665 as the owner of the merge |
| **AC8** success links document, JE, signed item, source, Work, receipt and ties control balances; **no default second-person approval** | **partial → verify + build the tie-out** | `p655.tieout.control` (the `x37.a` identity re-proven on the new lane) + `p655.grants`; the no-ceremony half is verify-only (`0204:651-654` approves in the same call). A cell must pin that the new door carries **no** `p_attestation` |
| **AC9** partial / undated / credit balance, locked period, stale basis, permission refusal, duplicate/lost, source correction | **partial + boundary** | `p655.due.absent` (undated is undated, never today), `p655.authority.floors` (locked period by name), `p655.reversal.unwinds` (LADDER 1 unwinds unchanged, `0037:940-957`). **Correction of posted effects is #676's** (2026-09-15 D6) — state the boundary, do not build it |
| **AC10** exact-cent sales/purchase fixtures, source/direct parity, duplicate replay, aging/control tie-out, narrow tables, keyboard/focus, read-only | **to build** | The whole battery + `trade-invoice-form-a11y.test.tsx` + `-keyboard.test.tsx` + the 320px/200%/reduced-motion walk legs |
| **AC11** invoice/bill polarity and complete open-item recognition established HERE; the cash-purchase/supplier-credit operation keeps its own semantics | **partial → recognition to build, polarity verify-only** | `p655.polarity.matrix` — a negative total, an AR party on an AP kind and a credit-shaped payload each refused **by name** (`invalid_total`, `wrong_control_domain`, `credit_shape_not_admitted`), the last proven to arrive as a typed CLR10 and not a bare 23514. The sign law itself is already DDL (`0037:764-769`) |
| **AC12** the UX ladder: loading / successful-empty / no-results / partial-stale / invalid-saving / denied / failed / cancelled-recovery; 320px; 200%; keyboard + focus return; SR names; reduced motion; stable URL/Back; preserved drafts; accepted shadcn/Base composition with a persistent outcome | **to build** | `trade-invoice-form.test.tsx` + the two a11y/keyboard files + every walk leg in §4.12. Idioms to copy: `journal-composer.tsx` (draft in `sessionStorage`, `StateBanner` per refusal, **no toast**, 202 ⇒ the Work's own page) |
| **AC13** verify through the production-facing read/command under real least-privileged roles and current migrations; prove durable execution, races and recovery on the real Workflow/Postgres World; label local vs hosted | **to build** | The three-file battery through `humanQuery` personas + `packages/runtime/tests/trade-invoice-e2e.mjs` on YOUR rig + the exact commands and counts in the report, with "hosted evidence pending" |
| **CB-AE2E-013** supersede per-action approval ceremonies with accepted role authority + automatic complete receipts | **verify-only on the Work lane · boundary on the coding lane** | A cell pinning that `admit_trade_invoice_work` has **no** `p_attestation` and that `is_high_stakes`/`x37.u`/`x37.v` are unreachable from it. Say in the report that removing `p_attestation` from `clara.approve_entry` (`0009:1523`) is a different lane's retirement, not silently left |
| **C08.5** direction-aware domain noun in Work rows | **to build (small)** | The Work row and detail read "Supplier bill · Alpha Supplies · AP" from **`get_trade_invoice`**, never from a `purpose` value (the `get_work_claim_origin` precedent). `QueueRowView` is gone with the legacy tree (`needs-you.ts:55-70` says "gone, not moved") |
| **C08.8** discovery: do direct-UI and agent callers reach the same versioned operation and receipt? | **to build — this row is the ticket's spine** | **Record the discovery result verbatim in the report**: they do NOT. Direct UI → `admit_journal_work` → `_record_journal_entry_core`, which refuses a control leg (`0204:550-570`); the agent (upload) → `wake_post_entry` → `_agent_post_entry_core` (`0106:866`) → `_approve_entry_core` → `_subledger_on_approve`; walled apart by `0107:251-257` and by document/extraction anchoring; receipts differ (`entry_post_receipts` vs `operation_receipts`). The bounded repair is §3; the equivalence proof is `p655.parity.source_vs_direct` |
| **C33.7** re-census bank operations; subject/account/input digest at admission and commit; thirteen is historical | **verify-only, carried forward on the new lane** | A cell proving the new admission binds its subject (client + counterparty + document/user-stated basis) and input digest at **admission** (`0194:1164-1190`) and again at **commit** (`_reserve_op` `0204:504`, basis echo `0204:520-525`), and that a changed payload under one key is `intent_payload_conflict`, not a second effect. **No bank operation is touched** — the row's related `C-40` (`已有门缺 UI`, CARRIES → REDESIGN, bank family) is #657/#667/#662's surface; say so |

## 3. Slice (one branch, `impl/655-invoice-bill`)

### Migration `0225_trade_invoices.sql` — additive, FORCE RLS, no app-role DML, integer cents, `Asia/Kuala_Lumpur` days

Write it in this order: **house header → prestate pins → §A objects → §B door → §C the recut → §D the birth trigger → §E reads → grants → events → §F tail → gate module → cohort.**

**§A · The typed object.** `clara.trade_invoices`: `id uuid pk`, `firm_id`, `client_id`, `work_id uuid not null unique`, `logical_op_id text`, `kind text not null check (kind in ('sales_invoice','supplier_bill'))`, `counterparty_id uuid not null` (D12a resolves the party at admission, so it is NOT NULL), `document_date date not null`, `due_date date`, `due_date_source text not null check (due_date_source in ('stated','counterparty_terms','absent'))`, `reference text`, `currency text not null check (currency = 'MYR')` (`PRD:127` — multi-currency is a stated boundary), `total_cents bigint not null check (total_cents > 0)`, `tax_facts jsonb` (opaque, echoed, validated against nothing), `source_document_id uuid`, `recorded_by uuid not null`, `on_behalf_of uuid`, `created_at`. **No `entry_id`, no `receipt_id` column** — `0221:350-355`'s rule, verbatim, with its reason. Tenant-carrying composite FKs throughout (`(work_id, firm_id, client_id)`, `(counterparty_id, firm_id, client_id)`), `source_document_id` NULLABLE and MATCH SIMPLE so a documentless chat-stated bill skips the check (`0221:344-349`). `check (due_date is null or due_date >= document_date)` and `check ((due_date is null) = (due_date_source = 'absent'))`. Append-only belt written **column by column** in the `0221:474-502` idiom "so a future writer cannot quietly widen this by adding a SET" — here it admits **ZERO** updates (there is no back-stamp to admit) — plus `before truncate` and DELETE refusals by name, FORCE RLS, owner-only policy, zero DML grants. Child `clara.trade_invoice_status` on the `0221:533-556` shape: `state in ('admitted','posted','refused')`, `entry_id`, `receipt_id`, `detail jsonb`, `unique (invoice_id, state)`, `ix_trade_invoice_status_entry on (entry_id) where entry_id is not null`, its own append-only belt and FORCE RLS. Its `posted` row is appended by an AFTER INSERT trigger on `clara.operation_receipts` `when (new.outcome = 'committed')` that resolves the invoice by `work_id` and inserts `on conflict (invoice_id, state) do nothing` — `0221:1423-1443` verbatim in structure.

**§B · The admission door — ONE door, `clara_runtime` only. The body order is #638's, step for step (`0221:1207-1220` narrates each step and why it sits there).**
1. **The intent key, first** — blank or whitespace ⇒ `invalid_intent_key` before anything durable (`0221:1232-1236`; the core's own rule at `0194:1074-1077`).
2. **The payload half** — `clara._assert_trade_invoice_basis(p_client, p_kind, p_particulars, p_basis, false)`: `kind` one of the two admitted values with a **credit-note-shaped payload refused as a typed CLR10 `credit_shape_not_admitted` before the column CHECK could fire** (`0194:1078-1081`); exactly ONE control leg of the right domain (`control_leg_missing` / `wrong_control_domain`); the control leg's signed amount equal to `total_cents` with the right sign, **and `total_cents` itself positive** — both refused `invalid_total`, raised as a typed CLR10 **before** the column CHECK `total_cents > 0` could fire as an unclassifiable 23514, which is the rule `0194:1078-1081` states for exactly this case; balanced, exact cents (`unbalanced_basis`); `due_date >= document_date` (`invalid_due_date`).
3. **The authority preamble** — firm, membership, active member, **`bookkeeper` floor**, active client: the core's own arms, order and tokens (`0194:1089-1116`) restated here because step 8 writes durably and must never run for a caller the core would refuse (`0221:1242-1269`), **including the no-existence-oracle rule** — an unknown firm and a non-member both answer `client_not_found`.
4. **THE REPLAY PROBE, before any write** — one read of `clara.accounting_work` on (firm, client, intent_key), the unique being `0178:336`. A Work already under this key resolves to its `trade_invoices` row and the door returns **the core's own replay answer** plus that `invoice_id`; a Work under this key that is **not** a trade invoice, or whose canonical particulars differ, is `intent_payload_conflict` naming the offending field. The world half is deliberately **not** re-asked (0182's rule: a property of the world may not refuse a lost-response retry). `0221:1271-1296` is the verbatim shape.
5. **The world half, asked cheaply** (`0221:1298-1300`) — party resolution through the 0215 identity primitives: `party_ambiguous` **carrying the candidate list verbatim**, `party_unresolved`, and `wrong_control_domain` when the party's `kind` contradicts `p_kind` (the `0037:1088-1096` rule restated at admission so it is typed rather than a commit-time surprise); due date resolved `stated → counterparty_terms → absent`; `source_already_posted` when the cited document already backs a posted entry (`0194:1210-1222`, same token, same CLR13).
6. **The rung** — `pg_advisory_xact_lock` on the client (`0221:1302-1326`), respecting the house lock order `accounting_plans → accounting_work → agent_tasks → agent_interruptions`.
7. **Re-ask the world half under the rung** — 0221's measured reason: another admission can COMMIT between a cheap check and the write, and the loser must leave as a typed refusal rather than a raw 23505.
8. **The UNCHANGED core** — `clara._admit_accounting_work_core(p_client, p_author, p_intent_key, 'journal_entry', v_basis, null, p_basis_origin, p_source_refs, p_model)` (`0221:1327-1333`), returning `work_id, task_id, logical_op_id, status, replayed`. **THEN** the `trade_invoices` row in the same transaction with that `work_id` and `on conflict (work_id) do nothing` so the core's replay branch converges here instead of raising (`0221:1334-1356`), then the `admitted` status row, then `_audit` naming **this door**, not the core (`0194:1263-1271`).

**Full refusal map — FOURTEEN tokens (`CLRxx` + `detail.reason`), identical in the door, the runtime module and the v21 stanza:** `party_ambiguous` · `party_unresolved` · `credit_shape_not_admitted` · `invalid_total` · `unbalanced_basis` · `control_leg_missing` · `wrong_control_domain` · `invalid_due_date` · `source_already_posted` · `client_inactive` · `insufficient_role` · `invalid_intent_key` · `intent_payload_conflict` · `period_locked`. **Orchestrator ruling — supersedes gap-655's thirteen-token stanza** (`gap-655.md:348-355`, written before DECISIONS reconciled it): **DECISIONS.md:50 fixes this map at FOURTEEN tokens INCLUDING `invalid_total`**, adding that "the count is descriptive, the door's own raise ladder is the contract". So `invalid_total` is a first-class `detail.reason` in the CLR10 class — it is raised by `_assert_trade_invoice_basis` (§B step 2), asserted by `p655.polarity.matrix`, and it travels to `trade-invoice-basis.ts` and the v21 stanza with the other thirteen. If your measured door ends up raising a fifteenth typed reason, the map grows with it and the report names it: the ladder binds, the number describes.

**§C · The posting-core recut (SIXTH copy).** Exactly as the binding section: the ~610 lines carried verbatim, ONE new conditional at `0204:550-570`, the party stamp after `0204:644-649`, nothing else. Prestate pins the live body by **measured** pre-image `sha256(prosrc)`; the tail re-proves owner / SECURITY DEFINER / `search_path` / ACL byte-for-byte.

**§D · The open-item birth trigger — the 0216 instrument, lane-agnostic.** `create constraint trigger t_je_open_item_birth after insert or update on clara.journal_entries deferrable initially deferred for each row when (new.status = 'approved')`, declared exactly as `0216:350-353` and for 0216's stated reason. **Name and ordering are MEASURED on your rig, not argued** (`p655.rig.trigger_order`): enumerate every trigger on `clara.journal_entries` from `pg_trigger`, classify which of them read `clara.open_items` at commit, and choose a name that sorts **before** every such reader; record the measurement in the function comment exactly as `0216:347-349` does. Subject resolution, in this order: **(1)** `clara.trade_invoice_status` on `(state='posted', entry_id = new.id)` through `ix_trade_invoice_status_entry` — this is the indexed handle `0221:353-355` says the ledger exists to provide, and the ledger's own AFTER INSERT trigger on `operation_receipts` is a **non-deferred** row trigger that has already fired by the time the deferred queue runs; **(2)** the equivalent receipt join `o.effects->>'entry_id' = new.id::text` (a **TEXT** comparison so `ix_operation_receipts_entry` at `0178:454-455` is usable, never a uuid cast of the jsonb expression); **(3)** the named fallback `clara.accounting_work.result->>'entry_id'` (`0204:735`). Measure which are visible at deferred-queue time (`p655.rig.receipt_join`) and implement the one the rig proves, naming the others in the comment. If neither read finds anything the trigger returns — which is what every entry from every other lane does. It then computes the signed control-account net per canonical counterparty **exactly as `_subledger_classify_entry` does** (copy the `nets` CTE at `0037:1012-1026`, including `clara._canonical_counterparty`), enforces the counterparty-kind ↔ domain rule (`0037:1088-1096`), and inserts ONE `clara.open_items` row with `item_kind = case kind when 'sales_invoice' then 'invoice' else 'bill' end`, `item_date = posting_date`, `due_date = trade_invoices.due_date`, `on conflict on constraint uq_open_items_grain do nothing`, then appends `open_item.created` with the same payload the hook appends (`0037:1111-1118`). **Do NOT call `clara._subledger_classify_entry(entry)`**: it derives the kind from `journal_entries.coding_kind`, which this lane leaves NULL, so it would return `'adjustment'` (LADDER 5, `0037:995-996`). **Do NOT call `_subledger_on_approve`**, and the tail asserts it, mirroring `0216:948-952`.

**§E · Reads.** `clara.get_trade_invoice(p_work uuid)` returning kind, party (id + display identity from 0215), `document_date`, `due_date` + `due_date_source`, `reference`, `currency`, `total_cents`, and the **derived** `entry_id`, `receipt_id`, `open_item_id`, `outstanding_cents` — so the Work list can label a trade invoice **without a purpose value** (C08.5) and the Work detail can render the mutual links (AC5).

**§F · Tail.** Re-derive and re-assert: (a) the `open_items` writer set — now **TWO** (`_subledger_on_approve` + your birth-trigger function), superseding `0037:3830-3833`'s ONE; (b) the subledger-hook caller set — still **SIX**, `0216:938-947`'s array, superseding `0037:3841-3847`'s stale four (**this closes #868's copy in this file only** — say so in the report); (c) the approve-path set including `_record_journal_entry_core`, superseding `0037:3774-3782`'s stale four; (d) the `generic_control_leg` arm still present **and still reachable** for a Work with no trade-invoice row; (e) all seventeen tokens at `0204:776-782` present by name, plus `_work_egress_event_seq`, the consent/activation re-read, the frozen bundle id and the two grandfathered ids; (f) the purpose filter string verbatim (`0194:2180`'s probe); (g) `clara_runtime` HOLDS and `clara_authenticated` does NOT hold `admit_trade_invoice_work` (`0221:1880-1888`'s pair); (h) the three reads are `clara_authenticated`-reachable and the two internals ungranted to every app role; (i) zero DML grants on both new relations, FORCE RLS on; (j) a whole-estate probe that no `trade_invoices` row names an entry whose control leg contradicts its kind; (k) the five non-regression pins unmoved.

### Runtime

- **NEW non-frozen module `packages/runtime/lib/trade-invoice-basis.ts`** (the `staff-expense-claim-basis.ts` precedent, `0221`'s sibling at `packages/runtime/lib/`): the `.strict()` zod schema, `tradeInvoiceFromInput`, `localTradeInvoiceRefusal`, the due-date-source vocabulary, and a `basisFromTradeInvoice` **display** helper (never for the wire — the #638 footer rule). Unit-tested here; imported by the v21 tool later, at which moment it freezes.
- **ONE route edit**: `POST /api/work/trade-invoice` in `packages/runtime/src/workRoutes.ts`, the **FOURTH** sibling of `:873` / `:960` / `:1054` — never a widened version of any of them. Both the human form and, later, the v21 chat tool post through it, which is what makes ONE `clara_runtime` door the whole lane's admission. The web wire mirrors `apps/web/lib/work/api.ts:262`: camelCase in, typed outcomes back, `lost` ≠ `unavailable`, 202 ⇒ the Work's page.
- **Successor contract stanza — put this verbatim in your report under "Successor contract":**

  > **Body** `chatTurn_v21` (cut ONCE by the integration worker after the ten merges; engine stamp `llm-openai:<modelId>:chatturn-v21`).
  > **Tool name** `start_trade_invoice_work`.
  > **Input schema** `.strict()`, a discriminated union on `kind`, living in the NEW non-frozen module `packages/runtime/lib/trade-invoice-basis.ts` (importing it puts the module in the v21 closure, which is intended): `kind: z.enum(["sales_invoice","supplier_bill"])`, `counterparty: { id?: uuid } | { name: string, registration_no?: string, tin?: string }`, `document_date: z.string().date()`, `due_date: z.string().date().nullable()`, `due_date_source: z.enum(["stated","counterparty_terms","absent"])`, `reference: z.string().min(1).max(64).nullable()`, `currency: z.literal("MYR")`, `total_cents: z.number().int().positive()`, `tax_facts: z.record(z.unknown()).nullable()` (opaque, carried and echoed, validated against nothing — the #638 rule), `lines: […]` (the journal basis), `document_id: uuid.nullable()`, `basis_origin: z.enum(["user_direct","clara_interpreted"])`.
  > **Door call** `select clara.admit_trade_invoice_work($1 client, $2 author, $3 intent_key, $4 kind, $5 particulars::jsonb, $6 basis::jsonb, $7 basis_origin, $8 source_refs::jsonb, $9 model)` — **argument order fixed here**; the door is granted to `clara_runtime` (the #915 lesson: a `clara_authenticated`-only door makes the tool a guaranteed grant refusal).
  > **Op key** deterministic (`task + tool + canonical input`), so a replayed step re-reserves rather than admitting a second Work.
  > **Refusal map → message — FOURTEEN tokens, the door's own raise ladder (DECISIONS.md:50; orchestrator ruling — supersedes gap-655's thirteen)**: `party_ambiguous` (carry the candidate list verbatim, ask the person to pick) · `party_unresolved` · `credit_shape_not_admitted` (a credit-note-shaped payload, refused BY NAME so the #666/#662 boundary is spoken rather than silent, raised as a typed CLR10 before the `kind` column CHECK could fire — `0194:1078-1081`) · `invalid_total` ("the stated total does not match the control leg's signed amount" — the same token for a zero or negative total, raised before the `total_cents > 0` CHECK could fire as a bare 23514) · `unbalanced_basis` · `control_leg_missing` · `wrong_control_domain` · `invalid_due_date` (due before document date) · `source_already_posted` · `client_inactive` · `insufficient_role` · `invalid_intent_key` · `intent_payload_conflict` · `period_locked`.
  > **Part kind** the existing `work_accepted` — **no `WORK_ACCEPTED_PURPOSES` widening**, because a trade invoice is a `journal_entry`-purpose Work exactly as #638's claim is (`chatTurn.v19.parts.ts:91`, pinned by `packages/runtime/tests/p6-1-parts-parity.test.mjs` — measured this pass, and `chatTurn.v20.tools.ts:38` names the pin).
  > **Prompt stanza** must say "I've queued it": the tool ADMITS and posts nothing; the entry is written moments later by a `claraWork` run under a wake credential minted OBO the same human, with the database rechecking role, period, chart and cents at commit.
  > **Nothing is added to `claraWork_v4`, `autoDraft_v10` or `invoiceFacts_v1`**, and no file inside the `chatTurn.v20.*` or `claraWork.v4.*` closure changes.

  **One honest limit on the stanza, carried from `gap-655.md` §14.5**: `chatTurn_v19`'s complete tool roster and its prompt stanzas were never enumerated — only `chatTurn.v18.tools.ts:52` (`START_JOURNAL_WORK_TOOL`), `chatTurn.v19.tools.ts:98` and `:380`, and `chatTurn.v20.tools.ts:6-9`'s delta of exactly two (`start_staff_expense_claim_work`, `start_accrual_work`). Your stanza must therefore **name only `start_trade_invoice_work` and assume no other tool's shape**; enumerating the full v21 roster and reconciling the four other stanzas (#651's, #658's, and the riders) is the integration worker's job at the cut, not yours. Say so in the report rather than implying you checked.

### Web

- **Route** `/(firm)/clients/[clientId]/accounting/invoices/new` — a sibling address of `…/accounting/journal/new` and `…/accounting/claims/new`, **not a tab or a Dialog** (appendix C §4 reserves a Dialog for "one focused bounded form or decision"; this form carries a party, two dates, an unbounded basis and a draft that must survive a reload, and a stable URL is what makes the draft recoverable at all). Register the leaf in **three** places in `lib/navigation/tree.ts`: the `ClientLeafId` union (`:121`), one `CLIENT_LEAVES` row at the sorted position with the **bookkeeper** floor and its reason comment (`:444-457`'s idiom — the write door behind it can only ever refuse a viewer, 裁-187's rule), and an href helper beside `journalComposerHref` (`:498-500`).
- **Hub**: `accounting-hub.tsx` gains ONE more primary act beside "Record a journal entry", gated by the same `canOpenClientLeaf` predicate (`:48-58`).
- **Component** `components/accounting/trade-invoice-form.tsx`, composed from **installed** primitives only — `Field`/`FieldGroup`/`FieldLabel`/`FieldDescription`/`FieldError` for every new input (appendix D #28: "It replaces ad hoc label/error layout"), `Input`, `Label`, `Textarea`, `Button`, `Badge`, `Table` for the basis lines, `StateBanner` for every refusal, plus the existing `EvidenceChooser` and `JournalBasisFields` and the checked money input (`MoneyInput` / `parseAmountToCents`). **Install nothing**: Combobox and Popover are uninstalled (appendix D #18, #43) and this form does not need them — the party picker reuses the counterparty reads from `lib/registers/counterparty.ts` and renders `party_ambiguous`'s candidates **inline as a choice**, never as a toast. Dates are plain calendar dates typed precisely; do not coerce, do not timezone-shift (appendix C §3 "Dates and money"). If you believe a primitive must be added, it goes through `pnpm --filter @clara/web ui:add <name> --dry-run` first and **never** overwrites a protected file (DECISIONS §6 §7.4 #15: the guard in `apps/web/scripts/ui-add.mjs` exists because `add chart` wanted to overwrite `card.tsx`).
- **States, each with its own cell or walk leg**: *loading* (skeleton fitted to the form, no placeholder zero) · *successful-empty* (a client with no counterparties yet offers creation) · *no-results* (party search preserves the query and offers Clear) · *partial-stale* (a failed counterparty read degrades independently of the chart read, labelled) · *invalid-saving* (field error beside its control, focus to the first invalid field, **user input preserved**) · *denied* (a viewer typing the address reaches the composer's own denied state, never a blank) · *failed* (`StateBanner` with the door's own words **and its code**, one real next action, **never a toast**) · *cancelled-recovery* (Cancel Work is a separately named action; already-admitted operations show 正在停止).
- **Draft** in `sessionStorage` under user+firm+client+intent-key, cleared on a scope switch, never moved across clients — the `lib/work/journal-draft.ts` rule; add `apps/web/lib/work/trade-invoice-draft.ts` on that shape.
- **The link render (AC5)**: on the Work detail, **ONE** block naming the document, the JE, the signed open item with its due date and outstanding, and the receipt — all from `get_trade_invoice`; on the journal-entry detail, a back-link to the Work (your `lib/journals/api.ts` hunk). **No new aging UI** (#669).
- **320px / 200% / keyboard / SR names / reduced motion / stable URL+Back**: single column at 320px; the basis-lines editor gets its **own labelled horizontal viewport** rather than a page-wide scroll (appendix C §4); amount, identity, status and the primary action stay visible at both; Back from the Work page returns to the composer with the draft intact.
- **Shared files — one-line registrations at the sorted position only**: `lib/navigation/tree.ts`, `messages/en.json` (one namespace), `test/manifest.txt` (four lines), `e2e/serve-built.mjs` (one import + one dispatch line), `e2e/e2e-fixture-ownership.test.ts` (one ownership row declaring **every** RPC verb your mock answers, shared ones declared shared in `SHARED_RPC_VERBS` at `:1106`), `packages/db/tests/rig-meta.mjs` (one cohort block — mind the `];` repair), `packages/db/package.json` (one gate flag at its migration-order position), `.github/actions/db-live-gates/action.yml` (one line for the World leg), `CONTEXT.md` (the terms above). POST bodies only through the shared `readCachedJson`, never a private reader.

### Docs

`CONTEXT.md` (the three terms + the two amendments above, house shape). READMEs in the same commits: `packages/db/README.md` (the new relations, the three doors with floors and grants, the trigger's **measured** ordering premise, the two censuses moved), `packages/db/tests/README.md` (the battery + its cohort), `packages/runtime/README.md` (the new non-frozen module and the successor contract), `apps/web/README.md` (the new route and its state ladder). **Never `docs/PRD.md` or `docs/ARCHITECTURE.md`** — drift goes in the report.

## 4. TDD seams (red first)

**Measure-first cells — these are the FIRST work you do on the rig, before a line of the migration is written.** Each replaces an "unverified" item in `gap-655.md` §14 or a DECISIONS §6 §7.4 measurement. Record each measurement's command and output in the report.

1. `p655.rig.core_prosrc_pin` — take `sha256(prosrc)` of `clara._record_journal_entry_core` and of the five non-regression targets off `pg_proc` on `clara_655` after `pnpm db:migrate`. *Red first: a pin transcribed from file text will not match, and several of these bodies are demonstrably splices (`_subledger_on_approve` = 0037 + 0040 + 0041; `_approve_entry_core` = the ninth body) — gap-655 §14.1, SYNTHESIS §7.4 #1.*
2. `p655.rig.trigger_order` — enumerate `pg_trigger` on `clara.journal_entries`, reproduce 0216's alphabetical-by-name deferred-queue measurement **with your trigger name in place**, and prove your birth trigger precedes every deferred trigger that reads `clara.open_items`. *Red first: `0216:347-349` measured this on `clara_639`; SYNTHESIS §2.1 LOUD #1 says reproduce it, never assume it.*
3. `p655.rig.receipt_join` — prove which of the three subject-resolution paths (§D) is satisfiable at the moment the deferred queue runs. *Red first: gap-655 §14.13 — the transaction boundary (`0204:651` approve → `0204:671` receipt → commit) makes it follow, but it was never executed.*
4. `p655.rig.coding_kind_untouched` — prove the entry this lane writes carries `coding_kind IS NULL`, so `_tf_assert_supplier_bill_shape` / `_tf_assert_sales_invoice_shape` and their document-anchored verified-total arms (`0009:477-520`, live `0022:714`) never arm. *Red first: gap-655 §14.2 — whether a documentless coded entry can satisfy them was never traced to the end, and this cell makes the question moot instead of answering it.*
5. `p655.rig.lines_validator_path` — prove `_validate_entry_lines` is the only path into `journal_lines` on this lane, so the party stamp must come after the insert. *Red first: gap-655 §14.3 read it for `0204:638-649` only.*
6. `p655.rig.aging_floor` — open `0040:4790-4801` on the rig and record `ar_aging`/`ap_aging`'s actual grants and floors before any walk asserts an aging number. *Red first: gap-655 §14.4 took them from a header citation, not the migration.*
7. `p655.rig.a11y_precedent_scan` — open `aging-register.tsx`'s specs and confirm no existing cell asserts 320px / 200% / reduced motion there, so your own cells are additions and not duplicates. *Red first: gap-655 §14.6 scanned filenames only.*

**DB battery** `packages/db/tests/trade-invoice.test.mjs` + `-fixtures.mjs` + `-preintegration-gate.mjs` (the `staff-expense-claim.*` three-file shape), every assertion through `humanQuery` personas, never `rootQuery` for the subject under test:

8. `p655.post.bill_one_commit` — Alpha Supplies RM1,060 supplier bill for Rome Properties: ONE commit yields the `trade_invoices` row + the balanced entry (Dr 1,000 expense / Dr 60 SST input / Cr 1,060 payable) + ONE `open_items` AP `bill` row of exactly **+106000** cents + the `operation_receipts` row — and the four ids cross-reference. *Red: nothing admits it today.*
9. `p655.post.invoice_one_commit` — the AR mirror: `item_kind='invoice'`, `domain='ar'`, positive cents.
10. `p655.post.control_leg_still_refused` — a plain `admit_journal_work` Work naming the SAME payable account is **still** refused `generic_control_leg`. *The recut opened a door, not a hole.*
11. `p655.due.stated` / `p655.due.terms_fallback` / `p655.due.absent` — a stated due date reaches `open_items.due_date` verbatim with `due_date_source='stated'`; no stated date + `payment_terms_days=30` ⇒ `posting_date + 30` with `'counterparty_terms'` (the `x37.c2` arithmetic, on the new lane); neither ⇒ NULL due date, `'absent'`, and the aging read reports `overdue:false` rather than inventing one.
12. `p655.polarity.matrix` — a negative `total_cents`, an AR party on an AP kind, and a credit-note-shaped payload are each refused **by name** (`invalid_total`, `wrong_control_domain`, `credit_shape_not_admitted`), the last proven to arrive as a typed CLR10 carrying its reason and **NOT** as a bare 23514 out of the `kind` CHECK. *AC11's "not a negative bill".*
13. `p655.replay.one_receipt` — same `intent_key`, same payload, twice ⇒ ONE Work, ONE `trade_invoices` row, one entry, one open item, one receipt; the second call answers `replayed:true` with the SAME `work_id` and `invoice_id` **out of the replay probe, having written nothing** (assert `trade_invoices.created_at` and the status-ledger row count unmoved — the probe is step 4, before any write). Same key + different particulars ⇒ `intent_payload_conflict` naming the field; same key over a Work that is NOT a trade invoice ⇒ `intent_payload_conflict` too (`0221:1278-1285`), never a replay handing back somebody else's basis; a concurrent pair under one key leaves one row and a typed conflict, never a raw 23505.
14. `p655.atomic.no_partial` — an injected failure after the entry insert leaves zero entries, zero items, zero receipts and zero events (the outbox law); and an injected failure **between `_admit_accounting_work_core` returning and the `trade_invoices` insert** leaves **no Work either** — the two are one transaction, which is exactly why the typed row is written after the core.
15. `p655.authority.floors` — viewer ⇒ `insufficient_role`; deactivated member ⇒ `actor_not_active`; another firm's client ⇒ `client_not_found` **with no existence oracle**; a locked period refused by name.
16. `p655.reversal.unwinds` — reversing a trade-invoice entry unwinds its open item through LADDER 1 (`0037:940-957`) with **no change to that ladder**.
17. `p655.tieout.control` — AR and AP control-account GL balances equal the sum of their open items' outstanding, **from zero**, after the new lane posts (the `x37.a` identity re-proven here).
18. `p655.census.writers` — the `open_items` writer set is exactly **TWO**; the subledger-hook caller set is still **SIX**; the birth trigger does not name the hook; the approve-path set includes `_record_journal_entry_core`.
19. `p655.parity.source_vs_direct` — a coding-lane `supplier_bill` and a Work-lane trade-invoice bill for the SAME economic facts produce the same control-account movement, the same `item_kind`, the same signed amount and the same due date. *This is AC3's upload arm and C08.8's bounded repair evidence.*
20. `p655.grants` — the ONE admission door is granted to `clara_runtime` and nothing else, and `clara_authenticated` holding it is asserted as a **failure** (`0221:1880-1888`); `get_trade_invoice` is reachable by `clara_authenticated`; the new relations carry zero DML grants and are FORCE-RLS; `_assert_trade_invoice_basis` and the birth-trigger function are ungranted to every application role; the door carries **no `p_attestation`** and `is_high_stakes` is unreachable from it (CB-AE2E-013).

**Runtime World leg** `packages/runtime/tests/trade-invoice-e2e.mjs`, registered with one line in `.github/actions/db-live-gates/action.yml`. Legs: admit → a `claraWork_v4` run → posted; `CLARA_WORK_TEST_FAULT=exit_after_commit` (the barrier at `claraWork.v4.tools.ts:310-313`) then respawn ⇒ **ONE** receipt and **ONE** open item; an `ask_question` park on a run-discovered missing account code, answered through `answer_work_question`, resumes to a posted entry (AC4's park half); a cancel arriving in the commit window settles per `0204:214-235`. **Exact command**, from `packages/runtime` in your worktree:
`export PATH="/c/Users/zhant/AppData/Local/pnpm:$PATH"; PGHOST=127.0.0.1 PGPORT=55705 PGUSER=postgres PGDATABASE=clara_655 CLARA_ALLOW_DESTRUCTIVE=1 CLARA_RIG_DB=1 WORKFLOW_POSTGRES_URL=postgres://postgres@127.0.0.1:55705/clara_655 node tests/trade-invoice-e2e.mjs`
Bootstrapping a World on `clara_655` makes `rig-isolation.test.mjs` T10b red afterwards (#866) — report it as that, not as your defect, or clone a sibling database first (`create database clara_655_world template clara_655`) and point the World leg at the clone.

**Web units** — four files, **each added to `apps/web/test/manifest.txt`** at the sorted position or it silently never runs: `components/accounting/trade-invoice-form.test.tsx` (field validation, exact cents, draft survival across a tab change, the `party_ambiguous` candidate render, every refusal as a `StateBanner` with its code and **never** a toast); `components/accounting/trade-invoice-form-a11y.test.tsx` (axe + SR names + reduced motion); `components/accounting/trade-invoice-form-keyboard.test.tsx` (tab order, focus to the first invalid field, focus return); `lib/work/trade-invoice-draft.test.ts`.

**Playwright** `apps/web/e2e/trade-invoice-walk.spec.ts` + `apps/web/e2e/trade-invoice-mock.mjs`, on **your** triple (`CLARA_E2E_APP_ORIGIN=https://127.0.0.1:3340 CLARA_E2E_NEXT_PORT=3341 CLARA_E2E_RUNTIME_PORT=3342`), always through `pnpm --filter @clara/web e2e trade-invoice` (`npx playwright test` alone serves a STALE build, #865). Legs: compose a bill → 202 → the Work page shows the persistent outcome → **reload shows document / JE / open item / receipt mutually linked**; a refused party renders inline and preserves the draft; a duplicate submit after a dropped acknowledgement resolves to ONE Work; a viewer typing the address reaches the denied state; **320px and 200% zoom** keep amount, identity, status and the primary action visible with no page-wide horizontal scroll; reduced motion; Back restores the composer with the draft. **RPC verbs the mock answers, every one declared in `e2e-fixture-ownership.test.ts`** (shared ones marked shared): `admit_trade_invoice_work`, `get_trade_invoice`, `list_coa_accounts`, `list_counterparties`, `get_accounting_work`, `list_entry_links`, `ar_aging`, `ap_aging`, `caller_context`. Per SYNTHESIS §3.3, `admit_trade_invoice_work` and `get_trade_invoice` are **exclusive to your lane**; `caller_context` and the three member reads are answered in `serve-built.mjs`'s CORE dispatcher and `CORE_RELATION_HANDOVERS` (`:1311`) reds if a lane takes one over.

**Census suites that will red on files you never touched, and how you keep them green**: `apps/web/tests/sql-oracle.test.ts` (every new SQL function enters its corpus), `tests/firm-scope-fourth-entrance.test.ts` and `tests/firm-scope-surfaces.test.ts` (any new file under `app/**`, any changed href — put components under `components/accounting/`), `tests/parity-holes.test.ts`, `lib/navigation/tree.test.ts`, `e2e/e2e-fixture-ownership.test.ts` (every new mock verb). On the DB side run `packages/db/tests/operation-census.test.mjs` and `rig-isolation.test.mjs` (**without** the reset flags — never set `CLARA_RIG_ALLOW_RESET=1` or `CLARA_RIG_ALLOW_ROLE_SWEEP=1`, they destroy your database and trip 0154's cluster-wide role pin), and **re-read `x37-wave-c-a-subledger.test.mjs`'s `x37.aa` writer assertions before landing** and `fixed-asset-acquisition.test.mjs:242-267` (`p639.census.approve_paths`) — its `!sub.includes("_record_journal_entry_core")` assertion **stays true** after your slice (the core births through the trigger and still does not call the hook), but a reader will expect the new writer named somewhere, so name it.

## 5. Risks

- **Merge collisions, exact files and whose hunk wins.** `packages/db/migrations/0204_…` is never edited (append-only) but the sixth copy is derived from its **live body** — any other branch recutting `_record_journal_entry_core` silently forks it; measured, **nobody else does** (SYNTHESIS §0.1) and **#656 recuts nothing** (SYNTHESIS §4), so do not budget a serialisation. `work-detail.tsx` — three lanes; **#658's hunk wins the Sources tab, yours is ONE link block in an existing block, #636's is one row, sequence #658 → #655 → #636.** `lib/journals/api.ts` — three lanes; **you own it**, #656 lands one `ENTRY_SELECT` field + one badge in its own hunk by DECISIONS §3 (not yours to veto), #660 only links. `accounting-hub.tsx` — yours alone. `rig-meta.mjs` (nine lanes, the `];` repair is the known hazard), `packages/db/package.json` gate chain (nine lanes, **migration order not alphabetical**), `action.yml` (nine lanes, per-line `\` continuations), `e2e-fixture-ownership.test.ts` (nine lanes across four structures), `en.json` (ten lanes; #635 **replaces** `Settings.unbuiltNote` and lands early — do not touch that key), `tree.ts` (four lanes plus open #921), `CONTEXT.md` (ten lanes, ~37 terms).
- **Blast radius of the recut.** The sixth copy inherits every arm of a ~610-line governed body; a lost arm is a silent authority hole, which is why all seventeen tokens plus `_work_egress_event_seq`, the consent/activation re-read, the bundle ids and the purpose-filter string are re-asserted in the tail. The recut also moves **two catalog censuses** (`open_items` writers ONE→TWO; the stale four-caller and four-approve-path pins) — re-derive, never transcribe.
- **Blast radius of the birth.** New `invoice`/`bill` items change what `ar_aging`/`ap_aging`, #660's client dashboard, #659's firm home and the close-readiness checks render. **No read changes**, but their fixtures now have a second producer — **state this in your report** so those lanes build fixtures against a lane that can produce `invoice`/`bill` items rather than only `adjustment` ones.
- **Trigger ordering is the slice's one physics assumption** (SYNTHESIS §2.1 LOUD #1). `clara.journal_entries` will carry a SECOND deferred constraint trigger beside 0216's. Measure (`p655.rig.trigger_order`); do not argue. #651's depreciation runs produce approved entries that traverse both — yours must return cleanly when no `trade_invoices` row names the Work, and a cell must prove it.
- **`coding_kind` is a trap.** Setting `journal_entries.coding_kind` on a Work-lane entry arms `_tf_assert_supplier_bill_shape` / `_tf_assert_sales_invoice_shape` and their document-anchored verified-total arms. §D deliberately does not set it; if you ever reach for it, `p655.rig.coding_kind_untouched` must be re-measured first.
- **Host contention.** Ten lanes share 24 cores / 32 GB. `next build` can panic `0xc0000142` — retry once (#869). Known Windows-only reds you must **not** "fix": #707 (x56-rest-c shells out to grep), #693 (EICAR fixture quarantined by Defender), no `pg_dump` on PATH, `thread-live-clarify.test.tsx`'s whole-suite load flake (re-run alone and report both), `rig-isolation.test.mjs` T10b after a World bootstrap (#866).
- **Beta-phase data.** Hosted data is test data (owner ruling 2026-09), so first minting `invoice`-kind items on a hosted estate carries no repair obligation — **say so rather than assume it**, and claim no hosted evidence.

## 6. Effort and rig

**Effort: XL** (DECISIONS §7; SYNTHESIS §7.3). Four independently sufficient reasons: (1) the **sixth full recut of a ~610-line governed posting core** with seventeen pinned refusal tokens and a measured pre-image sha — the wave's heaviest single artefact; (2) a **new lane-agnostic birth instrument** whose firing-order premise and subject join must both be measured on a rig, plus two stale catalog censuses to re-derive and re-pin; (3) a **new typed business object with an append-only status ledger, three doors and their floors**, delivered in the three-file battery shape with its own gate module and cohort; (4) a **complete new C3 surface with the full state ladder**, a Playwright walk and mock, plus the `chatTurn_v21` successor contract. The C6 read side (aging, counterparty statement, allocation dialogs) is already built, which is the only thing keeping this out of two tickets.

**Rig**: worktree `C:\Users\zhant\Desktop\clara-wt\655`, branch `impl/655-invoice-bill`, PG **55705** / db **clara_655**, base `abcc5030`, 219 migrations, newest `0224_preview_invite`, PG 17.11. Node 22 first, every call: Bash `export PATH="/c/Users/zhant/AppData/Local/pnpm:$PATH"`; PowerShell `$env:PATH = "C:\Users\zhant\AppData\Local\pnpm;" + $env:PATH`. DB env: `export PGHOST=127.0.0.1 PGPORT=55705 PGUSER=postgres PGDATABASE=clara_655 CLARA_ALLOW_DESTRUCTIVE=1 CLARA_RIG_DB=1`. No `psql` on Windows — use node + `pg`. **Never** set `CLARA_RIG_ALLOW_RESET=1` / `CLARA_RIG_ALLOW_ROLE_SWEEP=1`. Never touch another ticket's worktree, cluster or port.

**Before your FINAL report, run and report counts for every one of these**: `pnpm typecheck` and `pnpm lint` from the worktree root (both green); the **whole `apps/web` unit suite** once (`node scripts/run-tests.mjs` from `apps/web`, ~5 min) with its counts; your DB battery with the **41** `--import ./tests/*-preintegration-gate.mjs` flags copied verbatim from `packages/db/package.json` plus your own, `node --test --test-concurrency=1 $GATES tests/trade-invoice.test.mjs` from `packages/db`; a **focused run of your battery WITHOUT its gate** to prove it fails loudly below 0225; `packages/db/tests/operation-census.test.mjs` and `rig-isolation.test.mjs` **without the reset flags** (this slice adds SQL functions); `x37-wave-c-a-subledger.test.mjs` and `fixed-asset-acquisition.test.mjs` (the two census neighbours); `node scripts/check-frozen-workflows.mjs` and `node packages/runtime/scripts/check-parts-parity.mjs` from the worktree root — **expected no-ops, run them anyway and report the counts, because a green there is the evidence for the "no successor cut on this branch" verdict**; the World leg command above; `pnpm --filter @clara/web e2e trade-invoice` with your triple.

**Copy the house shapes from** (`git show --stat <sha>`, then the files): **#638 `6123efbe`** (merge of `impl/638-staff-claim` — migration header, prestate pins, tail assertions, the admission door's eight steps, the append-only belt, the status ledger and its receipt trigger, the preintegration gate, the `rig-meta` cohort, `workRoutes.ts`'s third sibling, `lib/work/api.ts`'s call, the walk + mock, the runtime basis module, the `-e2e.mjs` leg) — this is the closest precedent in the estate and `0221` is the file to read end to end; **#639 `a2b5d124`** (merge of `impl/639-asset-acquisition` — the lane-agnostic deferred birth trigger, its measured alphabetical-ordering comment, the re-derived censuses, `p639.census.approve_paths`); **#647 `12c10d57`** (merge of `impl/647-counterparty-identity` — 0215's provenance you consume); **`49f49acb`** (`0204`, the live body you recut); **`5f6ce604`** (0197, the two-lane document serialization). Read `docs/plan/active/refresh-wave-2026-09-15/brief-649.md` and `../refresh-wave-2026-09-14/brief-640.md` for the brief's own shape, and `.../reports/640-review-closure.md` + `643-final.md` for what a reviewer later demanded: **behavioural** cells rather than `prosrc` assertions, evidence re-measured rather than copied, an explicit merge-order collision table, and every claim carrying the command that produced it with its pass/fail counts.

## Verifier findings not applied

**None.** Both findings in the verifier's report were opened against their evidence and **applied**; nothing was refuted back.

- **F1 (blocker, contradicts_decisions) — applied.** Verified at the source: `DECISIONS.md:50` (the §1.2 contract-owner row for `chatTurn_v21` / #655) rules "the door's refusal map as the brief fixes it (the gap map's thirteen tokens PLUS `invalid_total` — fourteen; the count is descriptive, the door's own raise ladder is the contract)", while the draft fixed the map at thirteen in two places and instructed the implementer to keep `invalid_total` out. The draft's own §B step 2 already had `_assert_trade_invoice_basis` raising it, and `p655.polarity.matrix` already asserted it by name, so the door's raise ladder did include it and the map was the thing that was wrong — the draft had copied `gap-655.md:348-355` verbatim, which predates the reconciliation. **Applied in three places:** (a) the general map is now FOURTEEN tokens with `invalid_total` as the fourth, carrying the required "orchestrator ruling — supersedes gap-655's thirteen-token stanza" phrasing (BRIEF-ORDER §1), citing DECISIONS.md:50 and carrying its "the ladder binds, the number describes" clause; the "do not add it to the v21 map" sentence is **deleted**; (b) the successor-contract stanza's "Refusal map → message" line carries `invalid_total` with its chat-facing message ("the stated total does not match the control leg's signed amount"), is headed FOURTEEN tokens and cites the ruling; (c) §B step 2 now states that a zero or negative `total_cents` is **also** `invalid_total`, raised as a typed CLR10 before the `total_cents > 0` column CHECK could fire as an unclassifiable 23514 — `0194:1078-1081`'s stated rule, and the only way `p655.polarity.matrix`'s "negative total refused by name" leg can be green given the DDL this brief specifies.
- **F2 (minor, inexact) — applied.** Verified at `RIG.md:24`: row 655's smoke column reads `219 · 0224 · PG 17.11`; the fuller `0224_preview_invite` spelling is RIG.md:20's row for #635. The "verbatim" quote in the binding section now reproduces RIG.md:24 character for character, with a following sentence (not inside the quote) recording that the file on disk is `packages/db/migrations/0224_preview_invite.sql` — measured at `main@abcc5030`, where `packages/db/migrations/` ends 0221 / 0222 / 0223 / **0224_preview_invite.sql**. §6's "newest `0224_preview_invite`" is left standing: it is a stated fact, never labelled a quote.

**Two reconciler-initiated consistency fixes**, neither from the verifier, both made because DECISIONS is binding on the brief: (1) the `apps/web/lib/journals/api.ts` ownership line and its Risks twin said #656 contributes its field and badge "coordinated with you or defers it as a named residual" — DECISIONS §3 grants #656 that hunk outright ("#656 adds `is_opening_balance` to `ENTRY_SELECT` + one badge in ITS OWN hunk"), so both lines now say owning the file means keeping its shape stable for that hunk, never vetoing it. (2) No other divergence from DECISIONS §§1–7 was found on a full re-read.

**One item left for the orchestrator, named here and deliberately NOT raised as a start-blocker in the reconciler JSON** (it blocks neither the migration, nor the door, nor the surface): DECISIONS §3.1 ratifies #655 writing the NEW terms *Trade invoice*, *Due-date basis* and the `lines` boundary, and grants #658 an EXPLICIT exception to extend an EXISTING entry so the ten-lane CONTEXT union does not read it as an unratified edit. This brief also asks #655 to amend the existing *Open item* (`CONTEXT.md:407`, measured) and *Control account* (`:419`, measured) entries. That amendment carries no such exception. Land it, and say in the report that the integrator should treat it as ratified-by-analogy or drop it — the orchestrator rules at the CONTEXT union, not before you start.
