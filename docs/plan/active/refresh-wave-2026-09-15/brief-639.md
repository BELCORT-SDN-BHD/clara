# Brief: #639 — fixed-asset acquisition completes while depreciation particulars wait

## Orchestrator decisions (binding)

Where this section and `gap-639.md`'s "Proposed slice"/"Questions" differ, **this section wins** and the difference is marked *(orchestrator ruling)*.

- **Migration number: `0201`**, one file, house header (DECISIONS §1.2). The gap map's "0199" is superseded *(orchestrator ruling)* — 0199 is #650, 0200 is #647. A released number is a gap, never renumbered.
- **Birth path = option B, ruled in** (DECISIONS §2 #639; wave rule §1.4; owner call D5). A **lane-agnostic deferred constraint trigger** on `clara.journal_entries`, named to fire **before** `t_je_fa_movement_belt` (`0041_wave_d_a_fa_register.sql:2741-2743`), performing arm-4's registration idempotently (`on conflict (acquisition_line_id) do nothing`, `0041:2630`), **proven from the Work lane on the real Postgres World**. **Do not recut `clara._record_journal_entry_core`** (`0195_work_egress_purpose_and_execution_trace.sql:1685`, approve at `:2110-2113`) — §1.3 forbids **any** ticket recutting it this wave, and its closed list of wave shared-body recuts gives #639 exactly `_fa_asset_json` + `get_fixed_asset`; per D5 #638 closes its own Work-lane arm with its own lane-agnostic birth trigger (§1.4), not a core recut. Option A is out.
  - **Fallback, and the proof that picks it:** if you measure on the rig that a deferred constraint trigger cannot be ordered before the belt (the gap map lists this firing-order premise as *unverified*, `gap-639.md:266`), make the birth trigger **non-deferred** (`after insert or update … for each row when (new.status='approved')`) so it runs at statement time, strictly before any deferred belt. Ship the measurement — a cell printing both triggers' actual firing order on clara_639 — before choosing. If neither ordering works, descope the Work-lane arm to the existing human/document door and mark AC2 **partial by name**; never a core recut (§1.4).
- **No new `accounting_work` purpose** (purpose does not change which approve path runs). So #639 touches **none** of the four Work-purpose-vocabulary surfaces (`lib/work/purpose-label.ts`, `accounting-work-list.tsx:114`, `work-list-filters.tsx:55`, `messages/en.json`) — #638 owns all four (§1.7).
- **Recuts allowed to this ticket, and only these: `clara._fa_asset_json(uuid,date)` (`0041:4074`) and `clara.get_fixed_asset(uuid)` (`0041:4131`).** Their pre-image `sha256(prosrc)` pins are the **first FA pins ever written** (no migration 0180–0198 mentions fixed assets) and **must be MEASURED on clara_639, never transcribed from 0041's text** (§1.2; SYNTHESIS risk 7).
- **Provenance: store `acquisition_document_id` only.** The receipt is inserted *after* the approve (`0195:2130` vs `:2110`) and the Work `result` is built in the core (`0195:2194`), so an approve-time write of a receipt/work id stamps NULL forever. Derive Work and receipt **by join** from `acquisition_entry_id`. Read `clara.entry_evidence_links` **directly under RLS; wrap nothing** (§1.7).
- **Particulars question: reuse #629's estate, fork nothing** (`0180_work_questions.sql`). The human lane ships **fully** on this branch (register, new detail route, Needs-you answer through `complete_fixed_asset_particulars`, `0041:3035`). **Additionally ship a runtime-callable `_for` overload** OBO the initiator with live-authority rechecks, granted to `clara_runtime`, so the successor needs no migration *(orchestrator ruling — the gap map's Q3 "grant nothing to `clara_runtime`" is overridden)*.
- **No `_vN` successor on this branch** (§1.1): write a **claraWork_v4 successor-contract stanza** — open the dependent question after posting, apply the answer, **no second journal**. The integration worker cuts it once, after merge.
- **Route:** asset detail as a **real route segment** on the `knowledge/[recordId]` precedent (`apps/web/app/(firm)/clients/[clientId]/knowledge/[recordId]/page.tsx`); the list **stays** at `registers?tab=fixedAssets` (§1.8).
- **Correction = a visible predecessor/successor chain on a History tab; no re-link.** Credit-financed acquisitions are **out of scope by boundary**: cash/bank via the composer, supplier-invoice via intake→coding (the control-leg rule `0178_accounting_work_journal_successor.sql:1355-1367` refuses a payable leg on a generic basis); the composer refusal must **name the other door**.
- **Blueprints (`docs/PRD.md`, `docs/ARCHITECTURE.md`) are never edited**; contradictions go under "blueprint drift" in your report. **Activity kind ladder untouched** (D13) — record the residual by name.
- **Rig row:** worktree `C:\Users\zhant\Desktop\clara-wt\639` · branch `impl/639-asset-acquisition` · PG port **55504** · db **clara_639** (193 migrations applied, PG 17.11). **Playwright port triple:** `CLARA_E2E_APP_ORIGIN=https://127.0.0.1:3260`, `CLARA_E2E_NEXT_PORT=3261`, `CLARA_E2E_RUNTIME_PORT=3262` (DECISIONS §1.9).
- **Sibling deliveries to copy, by path:** `packages/db/migrations/0193_accounting_plans.sql` + `0194_periodic_adjustments.sql` (header, sha-pin idiom `0194:171-196`, tail assertions, cohort); `docs/plan/active/refresh-wave-2026-09-14/brief-640.md` + `brief-643.md`; and `…/reports/640-review-closure.md` — what a reviewer later demanded: a *behavioural* cell per finding, a re-measured pin, a raced concurrency cell, a merge-order table. Write those in, not after.

## 1. Current state

**db.** `clara.fixed_assets` born `0003_books_core.sql:155-181`, widened `0041:291-363`. Acquisition is a **hook, not a verb**: `_fa_on_approve` arm 4 (`0041:2595-2641`) soft-births one row per debit line on an enrolled cost account with a placeholder description (`:2618-2625`), `on conflict … do nothing` (`:2630`), and appends `asset.acquired`. The hook is invoked by one spliced line inside `clara._subledger_on_approve` (`0041:4530`); its callers are pinned at four in `0037_wave_c_a_subledger.sql:3840-3845`. **The Work lane is not one of them** — `_record_journal_entry_core` approves with a raw `update … set status='approved'` (`0195:2110-2113`) and calls no hook; the deferred belt `t_je_fa_movement_belt` (`0041:2665-2743`, trigger `:2741-2743`) then raises CLR40 `fa_belt_unregistered_movement` (`:2733-2738`) at COMMIT, after the receipt and Work result were written. Two stale in-tree sentences assert the opposite: `0041:4528` and `packages/db/tests/x41-wave-d-a-fa.test.mjs:186` (which counts *functions* containing a literal, not approve paths). Particulars doors: `complete_fixed_asset_particulars` (`0041:3035`, bookkeeper floor `:3040`, op-keyed, complete-once `:3066-3071`, writes no journal), `revise_fixed_asset_particulars` (`:3112`), validation `_fa_validate_particulars` (`:2970-3033`, closed key set, `start_date` always required). Reads `list_fixed_assets` (`:4110`), `get_fixed_asset` (`:4131`), `_fa_asset_json` (`:4074-4103`) — **no** `acquisition_entry_id`, source document, `work_id` or `receipt_id`. All FA doors are `clara_authenticated`-only (`:4400-4423`); `clara_runtime` holds only `run_depreciation_period` + `depreciation_run_due` (`:4431-4440`). Depreciation already skips an incomplete asset alone and continues (`:1370`, `:1883-1890`). #629's estate is complete: `open_work_question` (`0180:578-684`, runtime-only `:686`), `answer_work_question` (`:761`, granted `:863`, typed CLR13 `stale_question`/`expired`/`state_changed`/`basis_changed` `:823-838`, resume NOTIFY `:854`), `get_work_pending_question` (`:900`, granted `:912`).

**runtime.** `registry.ts:143, 205, 898-901` pin `claraWork → claraWork_v3`; `claraWork.v3.tools.ts:1-14` is a fixed 3-key roster; `ask_question` carries **no `execute`** (`:30-33`) — the frozen workflow body opens questions. `frozen-workflows.json` is at the **repository root** (281 entries); all 13 `claraWork.v3.*`/`chatTurn.v19.*` files are `deployed: true`. No FA module under `packages/runtime/lib`.

**web.** `fixed-assets-register.tsx` (list, `DataState` loading/empty/error `:101`, Complete/Revise/Dispose dialogs `:143-153`, incomplete note `:171-173`); `apps/web/lib/registers/fixed-assets.ts:120` `getFixedAsset` — **three references app-wide, none a UI caller**; `apps/web/components/work/work-detail.tsx` contains **zero** occurrences of `asset`; `find apps/web/app -ipath '*asset*'` returns nothing. `fa-particulars-fields.tsx` is the one shared field set; `needs-you-affordances.tsx:93` registers `FixedAssetIncompleteAffordance`; `needs-you-links.ts:59` maps `fixed_asset_incomplete → "/registers"`. #629's `work-question-panel.tsx`/`-form.tsx`/`WorkQuestionAffordance` (`needs-you-affordances.tsx:115`) ship. `lib/registers/money.ts:31` `fmtCents`; `components/common/section-tabs.tsx` is the accepted Tabs composition.

**tests.** `x41-wave-d-a-fa.test.mjs` (13 cells, **none** driving `wake_record_journal_entry` — which is why the defect survived 137 migrations), `x41-depreciation`, `x41-fa-world.mjs`, `x41-fa-fixtures.mjs`, `x42-r7-fa-stamp`. **No FA e2e anywhere.**

## 2. Gaps / rows

**AC1** partial → **build**: date/cost/accounts are captured today (`0041:2595-2641`); source and identity are not. Add `acquisition_document_id`; keep the placeholder name until answered. Multi-currency is the accepted PRD:127 deferral — **state the MYR-only boundary, do not fill it**.
**AC2** partial → **build**: the atomic commit works on the document/human/opening lanes (`0041:2227-2232`, `0178:1249-1278`) and **aborts on the Work lane**. Closed by the birth trigger; lineage/receipt by join.
**AC3** gap → **build** (human half on-branch; run half = the claraWork_v4 stanza).
**AC4** partial → **build the visible chain** (predecessor/successor History tab); duplicate/restart already hold on birthing lanes (`0041:2630`, `0178:1320-1335`); in-place cost adjustment stays refused by name (`0041:2726-2733`).
**AC5** partial → **build**: detail route + acquisition block; Work detail gains the asset row; journal/document links from the join.
**AC6** partial → **verify-only on the second half** (`0041:1370`, `:1883-1890` — re-measure, never copy), **build the first half**.
**AC7** partial → **build** the axis→control mapping (nothing under `apps/web/lib/registers` reads `.axis` today) **and specify** how a commit-time CLR40 settles the Work (`refused` vs `failed`).
**AC8** partial → **build** the C7 detail; 320px/200%/keyboard/focus return unproven for this journey.
**AC9** partial → **build** the whole browser walk (a new lane: spec + mock + `serve-built.mjs` hook + ownership declaration).
**AC10** partial → **build** the production-command cell and the World e2e. Per AC10's own DB-boundary clause the World e2e budget is the **acquisition** half; the particulars answer is DB-only.
**Historical rows: none.** `gh issue view 639 --comments` returns a zero-length comments array and no "Historical obligations" section. **C55.16 / C83.13** (capital-allowance asset population, issues/612#issuecomment-5589271633) belong to a **sibling** list — **out of scope, not adopted**; authority: they are a tax-output obligation about asset *population*, not acquisition. Say so in your report.
**Journeys (ticket names C1, C3, C7).** C7 is the build. **C1** is satisfied by the provenance projection only — the coding lane that files the document is #633's; you render the link. **C3** is satisfied by the composer's boundary refusal naming the other door. Record both scopings explicitly; do not build a second intake or composer.
**Named residuals:** activity-kind misfiling (D13); credit-financed acquisition; hosted evidence (none exists; #631's provider eval void until #836).

## 3. Slice (one branch)

**Migration `0201_fixed_asset_acquisition.sql`** (house header; prestate → DDL → bodies → grants → tail):
1. **Prestate.** `sha256(convert_to(p.prosrc,'utf8'))` pins for `clara._fa_asset_json(uuid,date)` and `clara.get_fixed_asset(uuid)`, **measured on clara_639 after `pnpm db:migrate`**, in `0194:171-196`'s idiom; a `::regprocedure` prerequisite roster (`0180:102-159` idiom) covering `_fa_on_approve`, `_tf_fa_movement_belt`, `complete_fixed_asset_particulars`, `open_work_question`, `answer_work_question`; a partial-birth guard.
2. **Doors.** Birth trigger function + trigger, carrying arm-4's predicate verbatim (`not is_opening_balance and reversal_of is null and not (flags ? 'fa_disposal')`, enrolled `fa_account_profiles.asset_account_code`, `debit_cents > 0`) **and additionally excluding `origin='scheduled_run'`** (`gap-639.md:252`'s live hazard). One nullable column `fixed_assets.acquisition_document_id`. Recut `_fa_asset_json` (+`acquisition_entry_id`, `acquisition_line_id`, `acquisition_document_id`) and `get_fixed_asset` (an **`acquisition` block separate from** particulars/schedule/charges, Work + receipt joined from `acquisition_entry_id`). New `complete_fixed_asset_particulars_for(p_client, p_asset, p_particulars, p_op_key, p_obo)` — live-authority recheck (membership active, role ≥ bookkeeper, client active), complete-once, **writes no journal**.
3. **Grants** via the `0041:4400-4423` loop idiom for the recut reads; the `_for` overload to **`clara_runtime` only**.
4. **Tail.** Re-assert `0042_wave_d_b0_shared_authorities.sql:171-179`'s single-marker check; re-derive and re-assert `0037:3840-3845`'s **four-caller** array (the trigger is not a fifth caller; if #638's ruling adds one, inherit its re-derivation, §1.4); re-assert the five belt doors and, only if the belt is touched (it should not be), `0056_wave_e_close_model.sql:3018-3032`'s six literals. **Preintegration gate** `tests/fixed-asset-acquisition-preintegration-gate.mjs` + chain registration; **`rig-meta.mjs` cohort** on `PERIODIC_ADJUSTMENTS_0194_COHORT`'s shape (`rig-meta.mjs:1803-1834`).

**Runtime.** New non-frozen `packages/runtime/lib/fixed-asset-acquisition.ts`: the typed particulars schema (the closed key set `_fa_validate_particulars` admits) + a `localRefusal()` mapping CLR37 axes (`start_date`, `drivers`, `non_depreciable`) to field names. Nothing frozen imports it on this branch — say so, and say that IMPORT-ESCAPE hash-locks it the moment claraWork_v4 does. **World e2e leg** `packages/runtime/tests/fixed-asset-acquisition-e2e.mjs` (standalone, skip-clean when 0201 absent), registered with **one step in `.github/actions/db-live-gates/action.yml` after the #643 periodic-adjustment e2e**. `reconciler-fa.mjs` untouched.

**Web.** New route `app/(firm)/clients/[clientId]/registers/assets/[assetId]/page.tsx` using `section-tabs.tsx`: **Acquisition | Particulars & policy | Schedule | History** — "policy/schedule clearly separate" becomes structural (appendix D row 58, comment 5589272729: Tabs are for "alternate views inside one route when state can remain local/shareable", "do not duplicate Sidebar routing as Tabs"; the same comment's Overlay-hierarchy note keeps a bounded write in a Dialog, never in a Tab). The Acquisition tab links JE, Work, source document and receipt id; exact cents via `fmtCents`. `fixed-assets-register.tsx`: asset cell becomes a `Link` + a "waiting on depreciation particulars" Badge. Reuse `work-question-panel.tsx`/`-form.tsx` and `fa-particulars-fields.tsx` unchanged. `work-detail.tsx`: **one identity-block row** naming the asset (brief-640's precedent; #638/#652 edit the same file). Refusal→focus: read `details.axis`/`details.field` off the `DoorRefusal` and focus the named control. `StateBanner` stays (§1.7) — no `Alert` rebuild. States required: loading (a skeleton that does not unmount loaded data — `fixed-assets-register.tsx:96-103`'s idiom), first-use empty vs filtered no-results vs no-permission, partial/stale, invalid/saving, denied/failed, cancelled/recovery; 320px, 200% zoom, keyboard + focus return from every Dialog, SR names, reduced motion, stable URL/Back between list and detail, drafts preserved across tab switches and never across clients.

**Docs.** `CONTEXT.md` +4 terms in the house "term / Avoid" shape: **Fixed asset acquisition**, **Pending particulars**, **Depreciation particulars**, **Dependent particulars question**. `packages/db/README.md` operation-contract census: the trigger, the `_for` overload, their grants, and the **two boundaries** (a generic Work basis cannot carry a payable leg → credit-financed acquisitions reach the register only via the coding lane; MYR-only at admission). `apps/web/README.md`: `/registers/assets/:assetId`. `packages/runtime/README.md`: the new module.

**Successor contract (report stanza, exact):** tool name, `.strict()` zod input schema, door call + arg order for `open_work_question` (after posting) and `complete_fixed_asset_particulars_for` (apply), refusal→message map (CLR13 `stale_question`/`expired`/`state_changed`/`basis_changed`; CLR37 axes), part kind, and the explicit "**no second journal**" invariant. No `WORK_ACCEPTED_PURPOSES` widening (no new purpose).

**Shared-file one-liners this ticket adds.** `tree.ts` — `ClientLeafId` gains `"fixedAsset"` (:112), one `CLIENT_LEAVES` row parented on the `assets` accounting item (:331, leaf idiom :386), one `fixedAssetHref()` (:417-421 idiom). `messages/en.json` — one block at its sorted position. `test/manifest.txt` — one line per new web test (alphabetical, plain string compare; unregistered = silently never runs). `e2e/serve-built.mjs` — one dispatch hook for `fixed-asset-mock.mjs`. `e2e-fixture-ownership.test.ts` — one import + verb declaration; `answer_work_question` / `get_work_pending_question` are **declared shared** with the #629 lane. `rig-meta.mjs` — one cohort block. `packages/db/package.json` — one `--import` gate. `db-live-gates/action.yml` — one e2e step. `CONTEXT.md` — four terms. `needs-you-links.ts:59` — repoint `fixed_asset_incomplete` to the detail route. `apps/web/tests/firm-scope-db-pins.corpus.ts` — **only if** your web code builds dynamic SQL (it should not); if it does, sorted position + ≥40-char reason.

## 4. TDD seams (red first)

New battery `packages/db/tests/fixed-asset-acquisition.test.mjs` beside `x41-wave-d-a-fa.test.mjs`, reusing `x41-fa-fixtures.mjs`/`x41-fa-world.mjs`; every assertion under test runs through **`humanQuery` personas**, never `rootQuery` (§1.10).

1. **`p639.birth.work_lane` — write FIRST; it defines the ticket.** Drive an acquisition through the **production command** `clara.wake_record_journal_entry`, real `clara_runtime` credential OBO the initiator, debiting an enrolled FA cost account. **Red today: the transaction aborts at COMMIT with CLR40 `fa_belt_unregistered_movement`.** Green: one approved entry + one `fixed_assets` row + one committed receipt + the Work `result`, one transaction.
2. `p639.birth.fire_order` — print both triggers' actual firing order on clara_639; assert the birth precedes `t_je_fa_movement_belt`. **This is the fallback's decision proof.** Red before the trigger exists.
3. `p639.birth.idempotent` — the document lane (`_approve_entry_core`) births **exactly one** row with hook and trigger both live. Red on a second row.
4. `p639.birth.exclusions` — K-family opening entries (belt door (e), `0041:2712-2723`), reversal mirrors, `fa_disposal` entries and `origin='scheduled_run'` depreciation debits birth **nothing**. Red on any phantom row.
5. `p639.census.approve_paths` — re-derive `0037:3840-3845`'s four-caller array **and** add the cell the estate lacked: enumerate approve *paths* (bodies setting `status='approved'`) and assert the set; correct the two stale sentences (`0041:4528`, `x41-wave-d-a-fa.test.mjs:186`) in the same commit. Red against today's source.
6. `p639.provenance.document` — `acquisition_document_id` equals the approving entry's `document_id`, NULL for a composer entry; Work and receipt resolve **by join**, never stored. Red before the column exists.
7. `p639.read.acquisition_block` — under a viewer persona, `get_fixed_asset` returns `acquisition` separate from `particulars`/`schedule`/`charges`. Red: the key is absent.
8. `p639.refusal.viewer` / `.locked_period` / `.credit_leg` — CLR04 on the `_for` overload under a viewer; CLR19 on a seeded closed FY (copy `work-journal-post.test.mjs:517-527`); CLR10 `generic_control_leg` naming the coding-lane door (`0178:1355-1367`). Red: nothing asserts these for this journey.
9. `p639.settle.clr40_classification` — assert the **specified** settlement of a commit-time CLR40 (`refused` with a nameable reason, or `failed`). Red: unspecified today, so AC7's denied cell has no expectation.
10. `p639.particulars.for_overload` — the `_for` overload under `clara_runtime` OBO the initiator completes once, refuses a demoted initiator by name, leaves the journal-entry count **unmoved**. Red: overload and grant absent.
11. `p639.question.dependent` — after posting, one versioned question parks the Work; the register, the detail route and Needs-you all converge on `answer_work_question`; a stale version returns CLR13 `stale_question` without discarding the draft; **no second journal**. Red end to end.
12. `p639.correction.chain` — reverse + rebook births a second row, the History projection names predecessor and successor, in-place cost adjustment still refuses `fa_cost_adjustment_deferred`. Red: no projection exists.
13. **Runtime unit** `fixed-asset-acquisition.test.mjs` — axis→field mapping, closed key set. Red: module absent.
14. **Runtime World e2e** `fixed-asset-acquisition-e2e.mjs` — admission barrier, SIGKILL between commit and checkpoint, replay returns the **same** asset id, current-authority recheck after a demotion. Red: no FA e2e exists.
15. **Web unit** — detail-route render states; `fixed-assets-a11y.test.tsx` / `-keyboard.test.tsx` extended to the detail; one axis→focus cell. Red: route absent.
16. **Playwright** `fixed-asset-acquisition-walk.spec.ts` + `fixed-asset-mock.mjs`, cell-for-cell on `periodic-adjustment-walk.spec.ts`: loading, empty, partial/stale, invalid/saving, denied (viewer), 320px, 200% zoom, keyboard + focus return, SR names, reduced motion, stable URL/Back, preserved draft. Red: `ls apps/web/e2e | grep -i asset` returns nothing.

## 5. Risks

- **The birth trigger is the highest-risk edit.** A new write inside the approve transaction has no remedy if it raises: `claraWork_v3` is deploy-locked, so a FORCE-RLS miss under the definer, a missing grant, or a guard admitting a K-family or scheduled-run entry is fixable only by a successor. Cells 3 and 4 are not optional.
- **Pins cannot be authored from source.** Neither read has ever been pinned; measure both on clara_639 — a pin read off 0041's text will not match the live `prosrc` and 0201 refuses to apply.
- **Cross-ticket merge.** No other ticket recuts `_fa_asset_json`/`get_fixed_asset`, and no ticket recuts the posting core at all (§1.3, D5) — there is no serialized spine to wait behind, so the twelve branches merge in number order. Contention is entirely in the shared registration files listed above — one line each, at its sorted position. `work-detail.tsx` is also touched by #638/#652/#653: one identity-block row only. `needs-you-affordances.tsx` gains **no new row kind** (§1.6); reuse `fixed_asset_incomplete`.
- **Census suites red on files you never touched** — `643-final.md` records ~139 `en.json` lines tipping an unrelated real-timer cell red.
- **The stale census stays green on its own** (`x41-wave-d-a-fa.test.mjs:186` counts functions, not approve paths; it hid this defect for 137 migrations). Cell 5, or the next reader repeats it.
- **Receipt-shape ripple:** anything touching `operation_receipts.effects` reaches 0195's execution-trace readers — run `operation-census` and the close batteries too.
- **Hosted evidence: none exists** here; #631's provider eval is void until #836. Write "hosted evidence pending".

## 6. Effort and rig

**XL.** Rig: worktree `C:\Users\zhant\Desktop\clara-wt\639`, branch `impl/639-asset-acquisition`, PG `127.0.0.1:55504`, db `clara_639`; Playwright triple **3260 / 3261 / 3262**. Node 22 via `export PATH="/c/Users/zhant/AppData/Local/pnpm:$PATH"`. Never set `CLARA_RIG_ALLOW_RESET=1` or `CLARA_RIG_ALLOW_ROLE_SWEEP=1`.

Before the final report: `pnpm typecheck` + `pnpm lint` (root, green); the **whole** `apps/web` unit suite once (`node scripts/run-tests.mjs` from `apps/web`, ~5 min) with counts reported; `packages/db/tests/operation-census.test.mjs` and `rig-isolation.test.mjs` (no reset flags — the destructive cell skips); the db battery with the verbatim 28-gate `$GATES` list from `packages/db/package.json` over your new file **plus** `x41-wave-d-a-fa`, `x41-depreciation`, `x42-r7-fa-stamp`, `x41b0-surface`, `x41b2-surface`, `work-journal-post`; and — the new runtime module sits one import away from the v3 closure — `check-parts-parity.mjs` and `check-frozen-workflows.mjs`. Known Windows reds to leave alone: #707, #693, and the `thread-live-clarify.test.tsx` whole-suite load flake (re-run isolated, report both).
