# Brief: #650 — client-home Work attention facets (needs you / active / recent success)

## Orchestrator decisions (binding)

- **Migration number: 0199 only** (`0199_client_work_pack.sql`; DECISIONS §1.2). One file, house header, tail assertions, a preintegration gate module, `rig-meta.mjs` cohort row. A released number is a gap, never renumbered; 0200–0209 belong to siblings and will be absent locally — expected.
- **One new door: `clara.get_client_work_pack(p_client uuid, p_preview int default 5)`**, SECURITY **INVOKER**, floored at **bookkeeper** by restating 0189's own three-predicate floor verbatim (`0189_work_list_reads.sql:344-347`). **Client-only signature** — no nullable `p_client` "for #659" (orchestrator ruling; gap map Q6).
- **Two facets in the pack, three tiles on the page.** The needs-you tile keeps the **shipped viewer-floored** `counts.work_questions` (`0180_work_questions.sql:1065-1080,1105,1118` → `apps/web/components/firm/needs-you-counts.tsx:30`). Do **not** re-aggregate `agent_interruptions` into the pack — two numbers over one relation under one noun is the 裁-190 defect. A viewer sees one honest tile and two named no-permission tiles.
- **active** = runnable/executing children (`status in ('queued','running')`, `count(distinct w.id)`). **"Retrying" is a LABEL on preview rows only** (≤ `p_preview`), never a whole-population number (orchestrator ruling — closes gap map Q0 at option (c); option (b), recutting `clara._work_run_attempts`, is **refused**: it would be a governed recut and the helper hard-refuses >101 ids, CLR10 `invalid_work_ids`, `0189:262-274`).
- **recent success** = a **committed** `clara.operation_receipts` row within **seven MYT calendar dates ending today**, resolved in the DB against `Asia/Kuala_Lumpur`. A Work `completed` with **no committed receipt** is not counted and drives `coverage='partial'`.
- **Facets may overlap and are never summed.** No total key in the envelope; state the overlap in words, in the shape of `client-home/client-docs-backlog.tsx:13-18`.
- **No `domain_events` watermark** (orchestrator ruling; the gap map proved it a dead signal — `_append_event` is the single emission point, `0005_event_spine.sql:477-491`, with zero Work-lane calls). The envelope carries the pack's **own read instant** (`computed_at`) plus per-facet `coverage`; the UI says "read at …", never a mutation position.
- **Freshness**: refresh after commits, scope change and visible return; **30 s compensation while visible** as a deliberate, documented extension of C77.12's owner; **`WORK_STALE_AFTER_MS` by reference** for the 60 s delay (`apps/web/lib/work/use-work-detail.ts:41-42`); values cleared **immediately** on client/access change.
- **Readiness**: a label + a "measured by #677" note **inside the existing section** (`client-home/client-close-summary.tsx:35-47`; its own header already assigns unmeasured gates to the close tab, `:41-44`). No new component, no glass token — extend the existing `supports-backdrop-filter:backdrop-blur-xs` idiom (`apps/web/components/ui/sheet.tsx:85`) on the facet band only; do not touch `components/app-shell/shell-header.tsx` or `app/globals.css`.
- **Ownership** (DECISIONS §1.7; SYNTHESIS:246): **#650 owns `client-workspace-overview.tsx`'s layout** — land it early; #649 and the affordance tickets add registry rows only. Do **not** edit the Work purpose vocabulary (`purpose-label.ts`, `accounting-work-list.tsx`, `work-list-filters.tsx`, the `en.json` purpose keys) — **#638 owns all four** (SYNTHESIS:249); render preview purposes through it unchanged. Do **not** rebuild `StateBanner` on `ui/alert.tsx` (SYNTHESIS:250). Add **no eleventh `needs-you.ts` row kind** (§1.6).
- **The board stays read-only.** No Retry/cancel/act control on a facet row (that is B3's). This is what keeps AC6's durable-execution half inapplicable; an act reactivates it and re-opens the frozen-closure question.
- **No frozen successor, no chat tool, no successor-contract stanza** (`WORK-ORDER.md:8`; frozen verdict `touches_frozen_closure: false`) and **no runtime module, no `db-live-gates/action.yml` row** — this journey invokes no Workflow; the board reads PostgREST/RPC from the browser (`apps/web/README.md:22`). Say both in the report so a reviewer does not hunt a missing half.
- **Activity ladder untouched** (D13): the last-activity section misfiles permission changes under documents; **record it as a named residual**, do not fix (SYNTHESIS:216).
- **Blueprints are never edited.** Contradictions go under "blueprint drift" in the report.
- **Preferred mechanism + fallback, each with the proof that picks it:**
  1. *30 s timer.* **Preferred**: declare the interval in `use-client-work-pack.ts` beside the imported `WORK_STALE_AFTER_MS`, with the distinguishing argument **in the file** (the pack has one non-terminal subject and two integers, where the list has a hundred mostly-terminal rows; no realtime channel exists — both `pg_notify` channels are server-side). **Fallback**: if it cannot be made green honestly, descope to the visible-return re-read (`use-work-list.ts:176-190`) and mark **AC4's compensation clause partial by name**. *Proof*: the fake-timer cell's output plus the whole `apps/web` suite counts.
  2. *Supporting index.* **Preferred**: none — `ix_operation_receipts_client(client_id, created_at desc)` (`0178:450`) already serves the window. **Fallback**: add `clara.accounting_work(client_id, status, id)` **only** if a measured `EXPLAIN (ANALYZE)` on your rig shows a seq scan at a realistic cohort; paste the plan in the report (the #640 reviewer required exactly this — `reports/640-review-closure.md` NOTE-1).
  3. *INVOKER.* **Preferred**: SECURITY INVOKER, proven relation by relation on the rig (`accounting_work` `0178:345-351`; `operation_receipts` `0178:461-463`; `clients`). **Fallback**: if any relation lacks a `clara_authenticated` grant behind a forced firm policy, do **not** open a definer surface — descope that facet and name it. *Proof*: a catalog query listing `relacl` + policies for each.
- **Playwright port triple** (DECISIONS §1.9): `CLARA_E2E_APP_ORIGIN=https://127.0.0.1:3310`, `CLARA_E2E_NEXT_PORT=3311`, `CLARA_E2E_RUNTIME_PORT=3312`.
- **Rig row** (RIG.md): worktree `C:\Users\zhant\Desktop\clara-wt\650` · branch `impl/650-client-home-work` · PG port **55509** · db **clara_650** · base `4464e471`, 193 migrations applied.

## 1. Current state

**DB.** `clara.accounting_work` (`0178_accounting_work_journal_successor.sql:301-337`): nine-member `status` CHECK; `purpose` widened to three values by `0194_periodic_adjustments.sql:230`; **no parent/child/batch columns, no completion instant** — only `created_at`/`updated_at` (`:324-325`); FORCE RLS + firm-scoped SELECT to `clara_authenticated` (`:345-351`). `clara.operation_receipts` (`0178:411-465`): `work_id` NOT NULL + FK (`:415`), `outcome in ('committed','refused')` (`:425`), `ix_operation_receipts_client` (`:450`), `purpose` widened by `0194:233-234`, SELECT behind a firm policy (`:461-463`) — **the only durable completion instant.** `clara.list_accounting_work` (`0189:301-506`): bookkeeper floor (`:344-347`), keyset page, nine-member status roster (`:366-370`), three canonical signals per row (`:470-482`); envelope `{rows, next_cursor, truncated}` — no counts, no watermark. `clara._work_run_attempts` refuses >101 ids (`0189:262-274`); `clara.agent_tasks` has no `clara_authenticated` grant. `clara.list_review_queue` floors at **viewer** (`0016_a21_compliance_watch.sql:4563`); its live body is a 0016+0017+0180 **prosrc splice**, not 0016's text.

**Runtime.** Nothing serves the client home.

**Web.** `/clients/:clientId` → `ClientWorkspaceOverview`; `useAsyncRead` reads **once** by contract (`client-workspace-overview.tsx:116-138`), only `onClientRecordChanged` re-reads; seven sections in a container-query grid (`:176-187`), `ClientNeedsYou` at `:179`. Sections read **independently** so one failure cannot blank the others (`:112-118`; `client-docs-backlog.tsx:7-18`). Work list: seven URL axes (`lib/work/work-list-url-state.ts:67-70`) — **none of them a period axis**; built-in needs-you view → `status:['awaiting_input']` (`:117-119`), cursor dropped on filter change (`:181-218`); `workDetailHref` (`lib/navigation/tree.ts:413-415`); day→timestamp via `businessDayStart`/`businessDayEnd` (`lib/firm/activity.ts:258,272`), already used at `lib/work/work-list.ts:111-112`, which exposes **no count beyond `rows.length`** (`:16-17`). `use-work-list.ts:34-45` is the verbatim C77.12 no-timer ruling; `:176-190` the focus/visibility re-read. `lib/firm/use-review-queue.ts:166` **receives the envelope's watermark and drops it** (`lib/firm/needs-you.ts:242`). `components/ui/` has card, badge, empty, skeleton, table, alert, pagination; **`item` is not installed**; no `glass` token. This page's Badge is `@/components/parts/PartBadge`, the Work list's is `@/components/ui/badge` — settle the two spellings here.

**Tests.** `packages/db/tests/work-list.test.mjs:1-20` (frontier gate + least-privileged roles — the pattern to copy); `client-workspace-overview.test.tsx` (13 cells, incl. the close-summary pins and the axe/container cell); `e2e/home-board-walk.spec.ts:155,177,190,223` + `home-board-mock.mjs`; `work-list-walk.spec.ts:139,158,229` (Back restores the query).

## 2. Gaps / rows

| Row | Disposition |
|---|---|
| **AC1** | **partial → build.** needs-you ships (`0180:1065-1080`, `needs-you-counts.tsx:30`) — restate once. **active** and **recent success**: build. "technically retrying" → **row label, not a count** (ruling). "child" has one referent (the `agent_tasks` run) until #636; no batch progress. A-composition = Card tiles in the existing grid + the `backdrop-blur-xs` idiom; **no `Item` install**. |
| **AC2** | **build.** Overlap stated, never subtracted; no total. **95/5 batch degrades honestly** — per-item independence is #636's (`docs/ARCHITECTURE.md:360`). **Period never filters Work attention**: vacuously true today (the only picker is `components/close/FiscalYearPicker.tsx`, Close tab) — write the contract **and close it with seam 11 `p650.pack.no_period_axis`** (DECISIONS §1.10: a red-first cell, a walk leg or a named residual), so #660 inherits a cell, not a promise. |
| **AC3** | **partial → build.** Click targets exist; the facet→href builder, the home→list→Back proof, and the readiness label do not. **'retrying' is not an expressible filter** (`0189:366-370`) — the active drilldown names `queued,running` and **discloses** the retry subset rather than faking it. |
| **AC4** | **partial → build.** Clear-on-change is done (`client-scope-provider.tsx:3-34`, `use-work-list.ts:140-158`, `use-work-detail.ts:66-69`). Watermark → `computed_at` + coverage (ruling). 30 s / 60 s / visible-return / post-commit refresh: build. |
| **AC5** | **partial → build.** Missing here: 320 px, 200 % zoom, reduced motion, keyboard/focus return from a facet, screen-reader names, partial/stale, denied, unknown-vs-empty, stable URL/Back. **No "preserved drafts" obligation** — the board has no input; say so rather than invent one. |
| **AC6** | **gap → build.** The DB battery under real least-privileged roles **is** the evidence. **Workflow half not applicable** (read-only board) — state the authority. Label all evidence **local**; hosted evidence is not yours to claim. |
| **UI-23** (REDESIGN) | **partial → build the A treatment.** The 裁-190 rebuild is real; its four pinned behaviours **are** the preserve clause (`client-workspace-overview.test.tsx` heading-order, the two close-summary cells, the axe/container cell; `home-board-walk.spec.ts:190-222`) — keep green. |
| Activity-kind misfiling (D13) | **named residual**, not fixed. |
| Seat/period/batch engines (#636, #659, #660, #677) | **out of scope** — authority: DECISIONS §2 #650 ("measured by #677"), gap map overlaps table, `docs/ARCHITECTURE.md:360`. |

## 3. Slice (one branch)

**Migration `0199_client_work_pack.sql`** — zero new tables, columns, policies or widened table grants; one `grant execute … to clara_authenticated`. Body: `clara.get_client_work_pack(p_client uuid, p_preview int default 5)` → one jsonb `{computed_at, window:{from,to,timezone:'Asia/Kuala_Lumpur'}, facets:{active:{status,count,coverage,rows[]}, recent_success:{…}}, needs_you_ref:{source:'list_review_queue.counts.work_questions'}}`; per-facet `status ∈ {ok,partial,unknown,denied}`; `p_preview` clamped 1..25. **Prestate**: the 0189 doors present, `operation_receipts` at its 0178+0194 shape, `get_client_work_pack` absent — **and no sha pin**. SYNTHESIS §2.2 gives 0199's "Must re-pin" column as **nothing**, and the pack calls none of `_work_run_attempts` / `list_accounting_work` / `list_review_queue` (it reads base relations; `needs_you_ref` is static metadata); the NON-REGRESSION idiom (`0195:371-407`) protects an assumption the new body depends on, not decoration. `pinned_function_recut: []`. If you keep a pin anyway, **name the dependency it protects in the migration's own comment** (e.g. `_work_run_attempts`'s 101-id ceiling, on which the Q0 ruling rests) and **measure it on your rig**, never transcribe (§1.2) — `list_review_queue`'s live body is a 0016+0017+0180 splice; read it off `pg_proc`. **Tail**: exactly one new function, `prosecdef=false`, EXECUTE only to `clara_authenticated`, no new table privilege. **Gate** `tests/client-work-pack-preintegration-gate.mjs` on the shape of `accounting-plans-preintegration-gate.mjs`; **`rig-meta.mjs` cohort** row ("wholly present or wholly absent"), idiom `rig-meta.mjs:1685-1700,2508`.

**Runtime** — **none.** No module, no World e2e leg, no `action.yml` row (see decisions).

**Web** — (1) `lib/work/client-work-pack.ts`: wire types, `callDoor('get_client_work_pack')`, hydrate-never-trust degradation (a malformed facet → `status:'unknown'`, **never `0`**), and **one** facet→href builder composing `/clients/:id/work?…` through `applyWorkListUrlState`/`workListStateQuery` so home and list cannot spell a filter two ways. (2) `lib/work/use-client-work-pack.ts` on `use-work-list.ts`'s ref-guarded epoch shape, plus focus/visibility, the 30 s visible interval and the 60 s delayed flag. (3) `components/firm/client-home/client-work-attention.tsx`: a Card per tile in the existing `@container` grid; `Badge` from `@/components/parts/PartBadge`; the count as a link into its scoped list; ≤ `p_preview` rows to `workDetailHref`; Skeleton on first read; Empty for successful zero; failure+Retry through the **existing** state components; explicit **unknown ≠ 0 ≠ denied** arms; a "read at … / update delayed since …" line; one overlap sentence. (4) `client-close-summary.tsx`: label + "measured by #677" note only. (5) Wire it into `client-workspace-overview.tsx:176-187` **above** `ClientNeedsYou`, keeping per-section independent reads — two reads, not one pack for three tiles. (6) Per tile: loading, empty, no-results, partial-stale, invalid-saving (n/a, no input — say so), denied, failed, cancelled-recovery (n/a); 320 px, 200 % zoom, keyboard/focus return, screen-reader names, reduced motion, stable URL/Back.

**Docs** — `apps/web/README.md` (the `/clients/:clientId` row: attention tiles, scoped drilldowns, reserved readiness note); `packages/db/README.md` + `packages/db/tests/README.md` (new door + battery); `CONTEXT.md`, two terms in the house **term / _Avoid_** shape: **Work attention facet** (a count of DISTINCT Work ids satisfying one attention predicate for one client; facets overlap and are never summed; no financial period narrows one; an unread facet is *unknown*, not zero. _Avoid_: a total; a page length) and **Work pack** (the one client-scoped read carrying the active and recent-success facets with `computed_at` and per-facet coverage. _Avoid_: calling `computed_at` a watermark; treating it as the needs-you source).

**Successor contract** — **none owed.** Write one sentence in the report saying so, with the authority.

## 4. TDD seams (red first)

Land the migration's `schema_migrations` stem **first** so the frontier gate **opens** and each cell goes **red**, not skipped — a skip is not evidence. Every DB cell runs through `humanQuery`/`roleQuery` personas at the least privilege that should pass; never `rootQuery` for the assertion under test (DECISIONS §1.10). Battery `packages/db/tests/client-work-pack.test.mjs`, pattern `work-list.test.mjs:1-20`, fixtures `work-journal-fixtures.mjs` / `work-question-fixtures.mjs` / `work-cancel-fixtures.mjs`.

1. **p650.pack.active_distinct** — five Works queued/running, one with two run rows: `active.count = 5`. *Red*: function absent, or the count inflated by task rows.
2. **p650.pack.recent_window** — a receipt committed 23:59 MYT on day-6 is IN, one at 23:59 MYT on day-7 is OUT, exercised across a UTC/MYT midnight. *Red*: a UTC-derived day boundary admits the day-7 row.
3. **p650.pack.refused_receipt** — an `outcome='refused'` receipt (`0178:425`) is not recent success. *Red*: counted.
4. **p650.pack.completed_no_receipt** — `status='completed'`, no committed receipt: not counted **and** `recent_success.coverage='partial'`. *Red*: coverage stays `ok`.
5. **p650.pack.over_101_active** — 102 queued/running Works: the pack still answers; retry appears only as a row label. *Red*: CLR10 `invalid_work_ids` (`0189:262-274`) darkens the pack. **This cell proves the ruling.**
6. **p650.pack.no_sum** — one Work parked on a pending question with a live run: assert the **inequality** `active + recent_success + counts.work_questions ≠ client Work population`, and that no total key exists. *Red*: a summable envelope.
7. **p650.pack.floor_viewer** — a viewer is refused CLR04 before any read, **and** still receives `counts.work_questions` from `list_review_queue` (`0016:4563`). *Red*: the pack answers a viewer, or the shipped chip regresses.
8. **p650.pack.cross_firm** — another firm's bookkeeper: zero rows, zero counts, no leak; an invented client id refuses identically.
9. **p650.pack.distinct_defence** — **a regression check on the shipped `clara.list_review_queue`, not an assertion about the pack's output**: `get_client_work_pack` never reads `agent_interruptions` (the 裁-190 rule), and this cell is in the battery only to document that the page-wide "distinct Work id" invariant AC1 demands still holds on the needs-you source the pack *references* — never a licence to compute a needs-you number inside the pack. Two pending `agent_interruptions` rows at different `question_version` for one Work, built by **owner-level fixture DML** because the verb refuses `question_already_pending` (unique `(work_id, question_version)`, `0180:211-212`): the chip's Work-keyed count stays 1. **Defence-in-depth, not verb-reachable.**
10. **p650.pack.catalog** — `prosecdef=false`; no new table privilege; `clara.agent_tasks` still ungranted to `clara_authenticated`. *Red*: a definer surface or grant slipped in.
11. **p650.pack.no_period_axis** — **the cell AC2's period clause closes with.** DB half: `pg_get_function_arguments('clara.get_client_work_pack(uuid,int)'::regprocedure)` is exactly `p_client uuid, p_preview integer DEFAULT 5` — no period, fiscal-year or basis parameter — and the window derives only from `Asia/Kuala_Lumpur` "today", so no caller can narrow a facet to a financial period. Web half (in `lib/work/client-work-pack.test.ts`): the href builder emits only members of `WORK_LIST_FILTER_AXES` (`work-list-url-state.ts:67-70`, no period axis), and the board renders identical counts and hrefs with and without a fiscal-year query parameter on the home URL. *Red*: a period parameter on the door, or a period value reaching a facet query or href. #660 inherits a cell, not a sentence.
12. **`lib/work/client-work-pack.test.ts`** — facet→href round-trips through `parseWorkListUrlState`: needs-you → the built-in view (`work-list-url-state.ts:117-119`); active → `status=queued,running`; recent success → `status=completed` + `since`/`until` via `businessDayStart`/`businessDayEnd`. *Red*: a hand-spelled query the parser normalises away.
13. **same file** — a malformed envelope degrades to `unknown`. *Red*: `?? 0`.
14. **`lib/work/use-client-work-pack.test.ts`** (fake timers) — the 30 s visible re-read fires; a hidden tab does not; 60 s without success sets `delayed` from the **imported** `WORK_STALE_AFTER_MS`; denied clears values; a transient failure keeps the dated ones. *Red*: no timer, or a second `60_000` literal.
15. **`client-work-attention.test.tsx`** — Empty, unknown and denied are three renderings; the overlap sentence is present; the Badge carries the **word**, not only a colour (C08.6), and comes from `parts/PartBadge`. *Red*: colour-only state, or `0` standing in for unknown.
16. **`client-workspace-overview.test.tsx`** — the four pinned preserve-clause cells stay green with the new section in the grid, **plus** a new cell: a failed pack read leaves needs-you and docs-backlog populated. *Red*: one failure blanks the board.
17. **`home-board-walk.spec.ts` / `home-board-mock.mjs`** `home.facets.drilldown` — each count → the scoped list URL the builder spells → Back restores the **home's** URL, focus returned to the invoking control.
18. **same spec** `home.facets.responsive` — 320 px and 200 % zoom, no horizontal page scroll; reduced motion; axe on the **populated** board; an accessible name per count.
19. **same spec** `home.facets.states` — unknown, empty and denied render distinctly; the "read at …" line; "update delayed since …" after the delay.

## 5. Risks

1. **Fabricated freshness** is this ticket's own failure mode: no Work lifecycle event exists, so any claim past "read at `computed_at`" is invention on a ticket about not inventing.
2. **Two numbers over one relation** — re-creating 裁-190. Render needs-you once, from the shipped read; seam 9 guards that read, it does not move into the pack.
3. **One read for three tiles** fights the page's own law (`client-workspace-overview.tsx:112-118`).
4. **The regression the ticket names is inherited for free** — the Documents workbench stays `running` until reload; the active tile gets the same defect unless the visible-return and 30 s re-reads are wired and fake-timer tested.
5. **Pinned cells break easily**: restructuring `ClientCloseSummary`, `ClientNeedsYou` or the grid reds the preserve-clause cells and `home-board-walk.spec.ts:190-222`.
6. **Two Badge spellings** land side by side if a facet card reuses `workStateTone` unexamined.
7. **Cross-ticket merge risk**: #649 edits this file's data seams (land your layout first); #638 owns the four purpose surfaces; #633/#639/#652/#653 mint the Work your facets count; #625 flips the two halves of this page at **different floors** (viewer vs bookkeeper) mid-session — never show one populated and one denied without saying why.
8. **Exact shared-file one-liners this ticket adds** (sorted position; logic in your own modules): `apps/web/test/manifest.txt` — three unit-test paths; `e2e/e2e-fixture-ownership.test.ts` — declare the `get_client_work_pack` verb under the `home-board-mock.mjs` row (`:307`); `packages/db/tests/rig-meta.mjs` — one cohort row; `packages/db/package.json` `"test"` — one `--import ./tests/client-work-pack-preintegration-gate.mjs` (28 gates today); `messages/en.json` — one `ClientWorkAttention` block; `CONTEXT.md` — two terms. **No** `tree.ts` edit (no new route leaf; facets compose the existing `/clients/:id/work`), hence no `require-firm-scope.ts` row — both scope-census walls still run. **No** `serve-built.mjs` edit (`home-board-mock.mjs` already imported at `:47`). **No** `db-live-gates/action.yml` row.

## 6. Effort and rig

**Effort: L.** Rig: worktree `C:\Users\zhant\Desktop\clara-wt\650` · branch `impl/650-client-home-work` · PG `127.0.0.1:55509`, db `clara_650` · Playwright **3310 / 3311 / 3312**. Node 22 via `export PATH="/c/Users/zhant/AppData/Local/pnpm:$PATH"`; never set `CLARA_RIG_ALLOW_RESET`/`ALLOW_ROLE_SWEEP`.

Copy the house shapes from `packages/db/migrations/0193_accounting_plans.sql` (header / prestate / tail / gate), `packages/db/tests/work-list.test.mjs:1-20` (frontier gate + roles), `packages/db/tests/accounting-plans-preintegration-gate.mjs`, `apps/web/e2e/work-list-walk.spec.ts:139,158,229` (Back restores), and the delivery shapes in `docs/plan/active/refresh-wave-2026-09-14/brief-640.md`, `brief-643.md` and `reports/640-review-closure.md` — that reviewer demanded **behavioural** cells over `prosrc` assertions, pins that are **measured and motivated** where a pin is kept at all (`0194:171-195` is the idiom; §3 rules none is owed here), a measured plan behind any performance claim and a catalog sweep of RLS/ACL/DEFINER. Produce all four.

Before the final report, run and report counts: the **whole** `apps/web` unit suite once (`node scripts/run-tests.mjs` from `apps/web`, ~5 min) — it carries `sql-oracle`, `parity-holes`, `firm-scope-surfaces`, `firm-scope-fourth-entrance` and `e2e-fixture-ownership`, which red on files you never touched; `packages/db/tests/operation-census.test.mjs` and `rig-isolation.test.mjs` (no reset flags), because the slice adds a SQL function; `pnpm typecheck` and `pnpm lint` from the worktree root. **Parts-parity / frozen check: not applicable** (no `packages/runtime` file is touched) — run `node scripts/check-frozen-workflows.mjs` once anyway and report OK. #707 and #693 are known Windows reds, not yours. Report to `reports/650-final.md` in the `WORK-ORDER.md:10` shape; label every run **local**; hosted evidence pending.

## Verifier findings not applied

None — both "should" findings (AC2's unclosed period clause; the three unmotivated non-regression pins) and the note (seam 9's ambiguity) are applied above; none contradicted DECISIONS.md.
