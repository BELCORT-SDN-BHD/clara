# Orchestrator decisions — wave 2026-09-15 (binding for every brief)

Written by the orchestrator after reading `SYNTHESIS.md` and the twelve `gap-<n>.md` maps. Every brief's
"Orchestrator decisions (binding)" section is derived from this file; a brief may refine but never contradict it.
Decisions marked **[owner-overridable]** were product calls the orchestrator made conservatively from
PRD / ARCHITECTURE / CONTEXT so the wave could proceed; the owner can reverse them and the affected slice is
reversible (beta, test data). Everything here is local-evidence scope; hosted evidence is never claimed.

## 0 · 大白话给 owner（可推翻的裁定）

| # | 问题（产品语言） | 今天 | 我裁定 | 另一选项的代价 |
|---|---|---|---|---|
| D1 | 受邀人接受邀请后，进工作台前要不要先看到「加入哪家所、什么角色」？ | 接受成功立刻跳首页，信息被丢掉 | **要**：先做一个「已加入 X 事务所，角色 Y → 进入工作台」的中间页；登录后、设密码前再加一扇最小预览门 `preview_invite`。登录前就能看邀请内容 → 另开票 | 不做则 AC2 后半句不成立 |
| D2 | 「重发邀请」和「席位不够拒绝」两条验收词 | 仓库刻意没有重发（明文令牌不落盘），席位计费在 PRD 里推到以后 | **不做**，明写「撤销后重新邀请」，席位记为范围外（PRD:126） | 做重发会重新打开明文令牌泄漏面；做席位等于把已推迟的计费拉回 |
| D3 | 报销、应计、预付款摊销三种业务要不要走对话入口？ | 对话工具清单冻结在 v19 | **要**，但由集成阶段一次切 `chatTurn_v20` 承载；三张票各自交付数据库门 + 网页门 + 工具合同 | 不给则 PRD §3 写的「对话、文件或 Accounting 直接处理」不成立 |
| D4 | 员工报销的「报销人」是自由文本还是结构化身份？ | 只有垫款账户上的一个标签 | **结构化**：以员工垫款登记（staff-advance enrolment）为身份句柄，报销门遇到新人自动登记，绝不生成 vendor 对方 | 自由文本无法把同一人两笔报销归在一起，而且现状会污染供应商主数据 |
| D5 | Work 车道过分录时，垫款登记簿 / 固定资产登记簿的钩子不触发（提交被打回） | 记账核心不调用子账钩子，只有四条老路调用 | **不改记账核心**：两票各自加一条「出生触发器」（deferred constraint trigger，任何车道都生效，幂等），钩子调用者普查如有变动在同一迁移尾部重推 | 改核心要第六次重切被两次 sha 钉住的函数，且三张票串行 |
| D6 | 「更正 Work」是不是 `accounting_work` 的一行？ | 每一行 accounting_work 受理时必须带完整可过账分录 | **不是**：#646 建更正记录并链接；已入账结果的联动更正归 #676 | 走 accounting_work 等于把 #676 拉进本票 |
| D7 | 客户财年「哪一天」缺失，问还是推导？ | 访谈只问月份，数据库要求月+日 | **问**：网页表单要求填日（把月末作为可选建议显示，不静默）；访谈的日问题在集成阶段切 `clientOnboarding_v5` | 推导 = 在专业人士记录上发明会计事实 |
| D8 | 事务所默认规则可以推广哪些知识键？ | `scope_default` 列是死列，任何键都能按全所写 | **建一张事务所可推广键表**（fail-closed）：`default_currency`、`reporting_framework`、`accounting_basis` 及偏好/政策类记录可推广；客户身份类（entity_type、SSM、财年）不可 | 不限则客户专属事实会被当成全所默认 |
| D9 | 事务所默认一旦落库，会出现在每个没有例外的客户 Knowledge 页 | 读门本来就按「同键同适用条件」遮蔽 | **可以**，明确标成「事务所规则」，客户例外显示为覆盖 | 不显示则 #654 AC2 不成立 |
| D10 | 事务所自己的身份事实（注册名、SSM、地址）落在哪？ | 无人拥有 | **留在事务所设置计划条目**（有作者、有修订），不进 knowledge_records、不铸身份键 | 铸错的键是 append-only、删不掉 |
| D11 | Clara 这一波能不能自己写交易对方别名？ | 只解析、只提案，从不写 | **不写**：本波只做出处（谁写的、来自哪份文件、人还是机器）；写入工具作为 v20 合同记录 | 给写权限要同时冻结一个新模块 |
| D12 | 应计走哪条计划车道？ | `reversing_journal` 计划已经是「应计→转回」两条腿 | **骑 `reversing_journal`**，应计明细放自己的关系表；预付款独占 `accounting_plans.kind` 的加宽 | 各开一种 kind 会在同一条 CHECK 上相撞 |
| D13 | 活动流把权限变更归错档（documents）这一波修不修？ | 两扇门共用一把梯子，四张票都碰 | **不修**，各票记残留，我另开一张 issue | 四票并发改同一函数会静默错合并 |

## 1 · Wave-wide rules (all tickets)

1. **Frozen bodies.** No branch cuts a `_vN` successor or edits any file in a frozen closure (`frozen-workflows.json`). Where a journey needs a chat-lane or Work-lane tool, the branch ships the DB door(s) + the human web door + a NEW runtime module outside every closure + a "successor contract" stanza (tool name, `.strict()` zod schema, door call/arg order, refusal→message map, part kind, and any `WORK_ACCEPTED_PURPOSES` widening). The integration worker cuts, once, after merge and review: `chatTurn_v20` (tools for #638, #652, #653), `claraWork_v4` (observedRevisions `knowledge_version`; #639 particulars question; #653 term park), `clientOnboarding_v5` + `interview.v4.questions.ts` (#649: ask-only-missing incl. H-52, fy-end day). `firmInterview_v4` and `documentIngest_v3` are NOT cut.
2. **Migration numbers** (use only yours; a released number is a gap, never renumbered): 0199 #650 · 0200 #647 · 0201 #639 · 0202 #646 · 0203 #648 · 0204 #649 · 0205 #654 · 0206 #638 · 0207 #652 · 0208 #653 · 0209 #625 · #633 none. One file per ticket, house header, prestate pins by pre-image `sha256(prosrc)` **measured on your migrated rig** (never transcribed from a creating file — several live bodies are splices), tail assertions, preintegration gate module, `rig-meta.mjs` cohort rows.
3. **No ticket recuts `clara._record_journal_entry_core`, `_admit_accounting_work_core`, `_tf_accounting_work_immutable`, `admit_journal_work`, the `accounting_work` / `operation_receipts` purpose CHECKs, `ck_accounting_work_adjustment_basis`, the `_adjustment_*` validators, the activity kind ladders (`list_activity` / `get_activity_event`), `_knowledge_capture_core`, `_knowledge_floor`, `capture_knowledge`, or `clara.periodic_adjustments`' purpose CHECK.** Prefer new relations, new doors, new guard functions and new triggers over recuts. The only shared-body recuts in the wave are: #653 → the 0193 plan family (`create_accounting_plan`, `_assert_plan_schedule`, `_plan_occurrence_basis`, `_plan_admit_occurrence`, `preview_accounting_plan`, `accounting_plans.kind` CHECK); #639 → `_fa_asset_json`, `get_fixed_asset`; #646 → `set_document_kind` (body only, signature unchanged); #647 → `add_counterparty_alias`, `rename_counterparty`, `set_counterparty_identifiers`; #638 → **none** (amended 2026-09-15 after brief review: a new `staff_expense_claim` purpose cannot post without recutting the posting core, whose IN-list and validator arms are closed — so the claim rides the existing `journal_entry` purpose, see §2 #638); #649 → none. Because no two tickets recut the same body, **all twelve branches run in parallel** and merge in number order. There is no serialised spine.
4. **Lane-agnostic birth triggers** (#638 advances, #639 fixed assets): a deferred constraint trigger on `clara.journal_entries` that fires before the respective belt for approved entries touching an enrolled account with no registered movement, performs the registration idempotently (`on conflict … do nothing`) and is proven from the Work lane on the real Postgres World. If a pinned caller census (e.g. `0037:3840-3845`) moves, re-derive and re-assert it in the same migration's tail. Fallback if the worker proves the trigger cannot derive what the hook needs: descope that arm to the existing human door and mark the AC partial by name — never a core recut.
5. **Knowledge catalog**: `clara.knowledge_keys` / `knowledge_plan_item_map` are touched by **#654 only**; #647, #648, #649 mint no keys and no map rows. `get_knowledge_pack`'s envelope is additive-only (new rows carry `authoritative: true`).
6. **Shared web files** (`tree.ts`, `en.json`, `manifest.txt`, `serve-built.mjs`, `e2e-fixture-ownership.test.ts`, `rig-meta.mjs`, `package.json` gates, `db-live-gates/action.yml`, `CONTEXT.md`, `firm-scope-db-pins.corpus.ts`): one-line registrations at the sorted position; logic lives in your own modules. Every new firm leaf registers in `require-firm-scope.ts`'s three tables and passes both scope-census walls. Every lane mock declares its RPC verbs; shared verbs are declared shared. New `needs-you.ts` row kinds: none this wave (use an existing kind or a dedicated read).
7. **Ownership**: `client-workspace-overview.tsx` layout → #650 (#649 adds data seams via registry rows only); `InterviewRunCard.tsx`, `useUploadQueue.ts`, `intake.ts` transport → #633 (#649 does not edit them); `lib/firm/capabilities.ts` → #625; Work purpose vocabulary (`purpose-label.ts`, `accounting-work-list.tsx`, `work-list-filters.tsx`, `en.json`) → #638 fixes all four once and carries the two #643 purposes; C13 firm-scope surface → #654; `StateBanner` stays (Alert only on #646's documents surface via `DoorFeedback`); `entry_evidence_links` read directly, never wrapped.
8. **Settings section ids**: `members` (#625, exists), `setup` (#648, new), `knowledge` (#654, new). **Routes**: #633 firm intake leaf `(firm)/documents`; #652 `/clients/[clientId]/accruals`; #653 `/clients/[clientId]/prepayments`; #639 asset detail as a real route segment (knowledge/[recordId] precedent; list stays where it is); #646 `?document=<id>&tab=original|facts|accounting`; #647 identity detail as a registered nav leaf under the client's Knowledge area.
9. **Playwright port triples** (`CLARA_E2E_APP_ORIGIN=https://127.0.0.1:<p0>`, `NEXT_PORT=<p1>`, `RUNTIME_PORT=<p2>`): 625→3230/3231/3232 · 633→3240/3241/3242 · 638→3250/3251/3252 · 639→3260/3261/3262 · 646→3270/3271/3272 · 647→3280/3281/3282 · 648→3290/3291/3292 · 649→3300/3301/3302 · 650→3310/3311/3312 · 652→3320/3321/3322 · 653→3330/3331/3332 · 654→3340/3341/3342.
10. **Evidence**: every AC and historical row closes with a red-first cell, a browser walk leg, or a named residual; REDESIGN rows are re-measured, never copied; skipped frontier-gated batteries are not evidence; new DB batteries call through `humanQuery` personas (least-privileged roles), never `rootQuery` for the assertion under test; journeys that invoke Workflow get a real Postgres World e2e leg registered in `db-live-gates/action.yml`. Blueprints are never edited; contradictions go under "blueprint drift" in the report.

## 2 · Per-ticket rulings

### #625 membership lifecycle — migration 0209 (conditional → **ruled in**)
- D1: joined interstitial (firm name + role + explicit "enter workspace") replaces the immediate `router.replace('/')`; `clara.preview_invite(p_token)` granted to `clara_authenticated`, same JWT-email wall as `accept_invite`, returns `{firm_name, role, effective status, masked email}`; signed-out preview is a named residual (needs a server route + service key → separate ticket).
- D2: no resend door (copy: revoke then re-invite; old link dies immediately; write it in `apps/web/README.md`); seat/capacity refusal out of scope (PRD:126) — record it; no capacity control in the UI.
- Four invite faces from the typed `InviteVerificationFailure` already computed in `lib/invite-verification.ts` (expired / revoked / already accepted / wrong email), each with its own next action; dialogs receive the `refusal` prop they already declare; confirmations name the firm (from `caller_context.firm_name`) and the member.
- Downgrade/removal: a second `caller_context` read folded into the members panel's post-act re-read (no change to `lib/parts/hooks.ts`, no polling); browser cell proves role menu + Invite entry vanish on the next settled read and the email column masks.
- Members door lock order is an invariant other migrations depend on — do not touch. Activity ladder untouched (D13): residual named.
- Walks: invite / revoke / role change / remove to result; 320px, 200% zoom, keyboard, focus return; mutable e2e fixture declared in the ownership census.

### #633 document intake — no migration
- Zero migration. Upload queue → Attachment rows in a Data Table with real byte progress (`XMLHttpRequest` + `upload.onprogress` behind a seam; fetch kept for Node), durable receipt re-read from `readIntake` / `listProcessingTasksForDocument` after reload, local cancel (pre-custody) distinct from Work cancel (post-custody), per-file retry, 20 `document_kind` labels + null as "Needs classification" (actionable in list/receipt rows by mounting #646's existing `set_document_kind` control — a declared boundary crossing; the detail panel's facts verdict stays as #624 built it), 9 failure codes each with a next step; batch = per-item independence only (aggregate progress is #636 — say so).
- Firm intake leaf `(firm)/documents` over the existing granted `clara.list_unassigned_documents` + `record_client_resolution` + `file_document`; registered in all three `require-firm-scope.ts` tables. #648 links to it.
- Document → Work link rendered from `clara.entry_evidence_links` (direct select under RLS) + `list_spoken_for_documents`; "no Work yet" stated honestly. No `accounting_work` minting at intake.
- "Start processing again" stays per filing (#614 ruling). `'Filed'` terminal word and `ComposerAttachmentControl`'s `IN_FLIGHT` state strings stay stable (or `chat-parity-walk.spec.ts` is updated in the same commit and stays green).
- Real Postgres World e2e: upload → custody → auto-admission without a human gate; lost finalize response resolved by intake re-read; duplicate bytes converge; same-name different bytes stay distinct.
- Owns `useUploadQueue.ts`, `intake.ts` transport, `InterviewRunCard.tsx` (upload half).

### #638 staff expense claim — migration 0206
- **Amended 2026-09-15 (supersedes the "new purpose" line below the fold of any earlier draft): NO new `accounting_work.purpose`.** The posting core's Work lookup is a closed IN-list (`0195:1711`) and INSERTION 1/2 call `_assert_adjustment_basis` / `_assert_adjustment_relationships`, which raise `invalid_purpose` for any fourth purpose (`0194:587-589`, `:797`); a new purpose therefore cannot post without recutting the core, which §1.3 forbids. Ruling: a staff expense claim is a **`journal_entry`-purpose Work** whose typed particulars live in a new relation `clara.staff_expense_claims` (unique `work_id`, tenant-carrying composite FKs, append-only status ledger child) written **inside the admission transaction** by a new door `clara.admit_staff_expense_claim_work(...)` — the `admit_periodic_adjustment_work` OBO shape (`0194:1298-1314`, actor-explicit, granted to `clara_runtime`; a `clara_authenticated` twin for the human form if the runtime route is not used) — which validates the claim particulars with a NEW `clara._assert_claim_basis(...)` (claimant, incurred date ≤ posting date, itemised lines summing to the amount, expense-class item accounts, non-control payable leg, typed `settlement`), derives the balanced journal basis from the items and the settlement, inserts the claim row (+ the auto-enrolment), and then calls the **unchanged** `_admit_accounting_work_core` with `purpose='journal_entry'` and `adjustment_basis` NULL. No purpose CHECK, no `ck_accounting_work_adjustment_basis`, no `_admit_accounting_work_core`, no `_tf_accounting_work_immutable`, no posting-core change: **#638 recuts nothing shared.** Posted state = a NEW AFTER INSERT trigger on `clara.operation_receipts` (`outcome='committed'`) that appends `posted` to the claim's status ledger when a claim row names that `work_id` (fallback: derive by join at read time). The posted entry and receipt are always derivable by join (`work_id` → committed receipt → entry). A read `clara.get_work_claim_origin(p_work)` (the `get_work_plan_origin` precedent, 0193) lets Work list/detail label a claim without a purpose value; `WORK_ACCEPTED_PURPOSES` needs **no** widening (the chat `work_accepted` part is a `journal_entry` Work). **No new column on `accounting_work`.** `0195:2165` arm untouched.
- D4: claimant = staff-advance enrolment handle (auto-enrolled by the claim door when new; label + optional identifier); never a vendor counterparty. Employee payable = non-control liability leg (`Other Payables`, `account_class=null`) + the claim register (state it in CONTEXT.md). Reimbursement vs advance application vs already settled are typed `settlement` values.
- D5: advance application in the Work lane via a lane-agnostic birth trigger (rule 1.4); if infeasible, descope that arm to `book_staff_advance_application` and mark AC2 partial by name.
- Tax facts: opaque, carried and echoed, never validated against a vocabulary (beta tax off). No second human on the Work lane (PRD:114); pin with a cell; note `is_high_stakes`'s non-amount axes as a finding.
- Questions: refuse before admission for anything the basis needs (claimant, incurred date, items); a parked Work question only for run-discovered facts (successor contract). Per-item continuation designed inside one claim (independent items continue; a missing-date item waits alone).
- Owns the Work purpose vocabulary fix (four surfaces) and carries `periodic_stock_adjustment` + `payroll_obligation` labels/filters; the claim label on those surfaces comes from `get_work_claim_origin`, not from a purpose value. Chat tool `start_staff_expense_claim_work` → successor contract only (no `WORK_ACCEPTED_PURPOSES` widening).
- Human web door: C3 form + C6 open-item/advance lineage + C1 attachment optional; real World e2e (admission → posting → claim row + liability + receipt in one commit; replay = one receipt).

### #639 fixed-asset acquisition — migration 0201
- Option B birth trigger (rule 1.4) so a Work-lane acquisition to an enrolled FA account births the register row before `t_je_fa_movement_belt`; store `acquisition_document_id`; derive Work/receipt by join; recut `_fa_asset_json` + `get_fixed_asset` (pins measured on the rig chain). No new purpose.
- Particulars question: reuse #629's mechanism; human lane fully delivered (register / new detail route / Needs-you answer via `complete_fixed_asset_particulars`); add the runtime-callable `_for` overload OBO the initiator with live-authority rechecks, granted to `clara_runtime`, so the successor needs no migration; claraWork_v4 stanza (open the dependent question after posting; apply the answer; no second journal). Placeholder name until answered.
- Credit-financed acquisitions: boundary stated (cash/bank via composer; supplier-invoice via intake → coding); composer refusal names the other door. Correction = visible predecessor/successor chain (History tab), no re-link.
- C7 list/detail: acquisition separate from pending depreciation configuration; walk at 320px/zoom/keyboard; real World e2e.

### #646 document correction — migration 0202
- D6: correction record (`clara.document_fact_revisions` + existing `filing_corrections`), NOT an `accounting_work` row; posted-effect integration is #676 (named residual with the link shown).
- Human fact revision = appended declarative extraction under `clara-fact-human:v1` (0169 shape); never UPDATE `document_regions`; `_document_posting_entry` non-regression pin. Orphan classification question closed by a new narrow door `dismiss_orphaned_classification_question` (no widening of `_active_document_filing`).
- Views: `?document=<id>&tab=original|facts|accounting`; Dialog for a bounded field/type change; Sheet for wrong-client impact context; inline Alert (documents surface only, via `DoorFeedback`) for refusals/persistent results.
- Knowledge/Work reassessment: read-only projection of affected records/questions (PRD:123 defers the engine to #658/#663); invalidate only the obsolete classification question; a changed source cannot continue under the old question version (version bump proven).
- Original client sees "transferred to another client on <date>, correction <id>" from `get_document_state`; bytes stay behind the existing no-oracle read. No chat entrance.

### #647 counterparty identity — migration 0200
- D11: provenance only — `recorded_via` NOT NULL (`human_ui` / `agent` / `seeding` / `legacy_unknown` default), a trigger refusing `human_ui` without `jwt_sub()`, source document/extraction pins on aliases, `clara.counterparty_identity_revisions` (append-only; rename, alias add/retire, identifier change, merge), `set_counterparty_identifiers` history kept, the live human alias writer repointed to the provenance signature; four domain events routed `context_update` with NO consumer (PRD:123).
- Identity stays in the counterparty relations (no move into `knowledge_records`, no identity knowledge keys this wave); three reads as SECURITY DEFINER functions (never masked views); aging/statement expose recorded + canonical identity; merge lineage rendered; unmerge = bounded discovery read only (no reversal door).
- Cross-client matching stays ambiguous; no identity fact nets open items or grants authority (cells). Identity detail as a registered nav leaf reusing `knowledge-shared.tsx`; C13 shows source/history/conflict + one versioned correction path. Stale comments fixed only where still misleading. Activity ladder untouched (D13).

### #648 firm setup — migration 0203
- `/settings/setup` section (id `setup`) + a firm-home tile; `/settings/firm` untouched. Seeding is a **reconciliation** against the firm plan (never a blind insert); human doors on the firm plan (answer / skip / reconcile-seed / commit) + `get_firm_setup`; progress from actual required items; resume after reload and concurrent editing (CAS on plan version); optional education skippable, never an approval gate.
- D10: firm identity facts stay as plan items (author, revision snapshots) — not knowledge records, no keys minted. Firm-level defaults (`default_currency`, `reporting_framework`, `accounting_basis`) confirmed through `capture_knowledge(p_scope_kind=>'firm')`, subject to #654's eligibility wall; #654 owns the firm-knowledge register; #648 renders a "facts confirmed in setup" confirmation face only.
- Reuse the Work-question components (Field / FieldGroup / stepper, leave-pending, drafts) with records in `onboarding_plan_items`; C48.5/C83.24 closed here (#654 gets a pointer). Admin floor; per-key elevation is catalogue data. Firm intake documents link to #633's leaf.

### #649 client onboarding — migration 0204
- D7: `settle_client_onboarding_facts` writes canonical client records (fy-end via `set_client_fy_end` as-is with month+day); the web Field asks the day (month-end offered as a visible suggestion, never silent). The interview's day question + ask-only-missing (H-52) ship in `clientOnboarding_v5` at the integration cut — the brief says so up front; AC2's interview half is "contract delivered, closes at the cut".
- Ambiguity: 0 → proceed; 1 → show the candidate and require an explicit "different business" acknowledgement in the face (new read wrapper `client_identity_candidates`; no new parameter on `begin_client_onboarding`); ≥2 → DB refuses with candidate ids (`name_family_collision`).
- Opening books: existing seed lane, link + honest first-year-vs-deferred sentence (0170 key read); no enum widening. Non-goal: never `p_promote_firm_scope=true`, no firm-default entrance. Does not edit `InterviewRunCard.tsx` or the client-home layout (data seams via #650's registry rows). Stale correction, concurrent editor, cancel, role revocation: cells; drafts never cross clients.

### #650 client home work facets — migration 0199
- `clara.get_client_work_pack(p_client, p_preview)` SECURITY INVOKER-style read floored at bookkeeper; needs-you tile keeps the shipped viewer-floored `counts.work_questions`; active = runnable/executing children; "Retrying" as a label on preview rows only (no whole-population retry number); recent success = committed operation receipt within seven MYT calendar dates ending today; completed-without-receipt drives `coverage='partial'`; facets may overlap and are never summed.
- Watermark = pack's own read instant + coverage; refresh after commits, scope change and visible return; 30 s compensation while visible as a deliberate, documented extension of C77.12's owner (`WORK_STALE_AFTER_MS` by reference for the 60 s delay); values cleared immediately on client/access change.
- Readiness: label + "measured by #677" note inside the existing section; no glass token (extend the `backdrop-blur` idiom on the facet band only); client-only signature. Owns the client-home layout. Facet → scoped Work filter with preserved return state.

### #652 accrual & reversal — migration 0207
- D12: rides `kind='reversing_journal'`; NO new `accounting_work` purpose, NO posting-core recut, NO widening of `accounting_plans.kind`. New relation `clara.accrual_adjustments` (append-only; keyed by plan + revision; typed particulars: amount basis, expense/liability accounts and purpose, service period, method as a stated jsonb selection-rule enum, authority + source/instruction refs). Door `create_accrual_adjustment` writes particulars + creates the plan/revision through the existing plan core + admits the current-period occurrence (0193:1191 idiom) in ONE commit; posting stays with the run (entry + receipt in one commit); posted entry/reversal derived by join (occurrence → work → receipt → entry, `reverses_entry_id`). Configuration receipt ≠ executed occurrence (both boundaries stated in the UI).
- Silent term or zero-value balanced proposal refused (CLR10); refuse before admission at the form; park (document/instruction-admitted absent term) → claraWork_v4 contract; model-extracted periods never enter the durable record (0140 law). Locked-period catch-up only via the existing window door; a future instruction never authorises history.
- Route `/clients/[clientId]/accruals` (list, detail with plan/occurrence/receipt lineage, form); chat tool `start_accrual_work` → successor contract. Real World e2e: configure → occurrence posts → reversal waits for the posted accrual → posts naming it; replay = one receipt. C88.13 hosted leg owed (follow-up).

### #653 prepayment amortisation — migration 0208
- Extend 0193: `amortisation_schedule` kind (sole widener of `accounting_plans.kind`), per-period basis (`_plan_occurrence_basis` recut), `_plan_admit_occurrence` / `create_accounting_plan` / `_assert_plan_schedule` recut with measured pins; new `clara.prepayment_schedules` relation (recognition entry, term carrier `document_service_periods` row, exact-cent allocation, final-period residual, per-occurrence links). Reuse `prepayment_schedule_v1` verbatim (document-bound only this wave; human-stated typed term = follow-up); expense-side pairing + its three walls re-derived in the new door (not by calling the 0045 lane).
- Occurrence purpose stays `journal_entry`. Atomicity = each occurrence's entry + receipt + occurrence row in one commit; recognition-then-schedule-failure residue is surfaced by a refusal-visibility read (per-occurrence refusal reason visible on schedule detail with an explain-and-choose surface; CLR19 for locked periods; recovery through the existing catch-up window). Repetition / lost response / restart cannot create another occurrence (cells + World e2e).
- Route `/clients/[clientId]/prepayments`; preview / effective revision / pause / end retain prior runs. Chat tool `start_prepayment_schedule_work` and the AC5 park → successor contracts; AC5/C55.13 marked not claimed.

### #654 firm defaults — migration 0205
- D8/D9: new fail-closed relation `clara.knowledge_key_firm_eligibility` (seeded: `default_currency`, `reporting_framework`, `accounting_basis`; preference/policy-kind records eligible by kind) enforced by a NEW BEFORE INSERT trigger on `knowledge_records` for `scope_kind='firm'` (CLR10 `knowledge_scope_not_firm_defaultable`); private-evidence wall as a second BEFORE INSERT trigger: a firm-scope record may not pin a document with any live client filing (CLR10 `firm_scope_client_evidence`); zero recuts of the 0192 bodies. `list_firm_knowledge` (viewer floor aligned with `list_client_knowledge`) + `get_knowledge_applicability`.
- Entrance: `/settings/knowledge` section (id `knowledge`): firm register, Promote dialog (scope, actor, applicability/effective period, source, authored reason), Correct/Withdraw, and the firm-rule-vs-client-exception pair in every client's C13 (firm rows labelled, exception shown as overriding). Web entrance only; chat capture of firm scope → successor stanza.
- Re-evaluation engine: not built (PRD:123); ship the human-review affordance (which clients hold exceptions; which live Work cites the key) and the claraWork_v4 `observedRevisions({knowledge_version})` contract; record the ticket-vs-blueprint contradiction as blueprint drift. Cells: current permission on promotion/use, revoked membership, cross-client evidence refusal, independent client overrides, a firm preference is not a posting grant nor plan authority. #648 consumes the same doors.

## 3 · Post-review ratifications (2026-09-17, orchestrator; owner-overridable)

Ruled after the three-lens review and the fix/re-check rounds (`reports/<n>-review-*.json`, `reports/<n>-fixround-*.md`, `reports/<n>-recheck-*.json`; the digest is `reports/WAVE-DIGEST.md`). Each row is a deviation a worker or reviewer asked the orchestrator to rule on. Reversible in beta.

### 3.0 · 大白话（产品相关的几条）

| # | 问题 | 裁定 | 为什么 |
|---|---|---|---|
| R1 | 邀请人后来被降级/移除，他发出的邀请预览还显示「待接受」，要不要多加一种状态？ | **不加**，四种结果不变；预览页与管理员名册保持一致；差异用一个 cell 钉住并记为残留，另开 issue | 多一种状态会让预览和名册各说各话 |
| R2 | #649 的「全屏入门」那条走查（UI-21）本波没跑，怎么记？ | **记为具名残留**，另开 issue（需要 docker/PostgREST 的 live-stack runner，本机十二条 lane 都没有） | 写一条从没跑过的断言不是证据 |
| R3 | 客户首页「处理中」要不要把「正在停止」的 Work 也算进去？ | **不算**，按 brief 原文（queued/running）；残留具名 | 改口径是产品决定，不在本波 |
| R4 | 应计（#652）授权窗口必须落在服务期间之内、且至少产生一个到期日，否则拒绝？ | **要**（两条新拒绝都采纳） | 一条永远不会入账的应计，是账本不会兑现的承诺；比默默接受更诚实 |
| R5 | 事务所默认规则撤回后，它引用过的文件还能不能归到某个客户名下？ | **能**：只统计「在用」的规则；历史版本只提示不阻止 | 撤回就是拒绝理由里指向的补救办法，不能让文件永远无法归档 |

### 3.1 · Rulings (all thirteen)

| Ticket | Item | Ruling |
|---|---|---|
| #625 | `CORE_RELATION_HANDOVERS` census instead of the brief's `SHARED_RPC_VERBS` declaration for three relation reads | **Ratified.** The brief's instruction was mechanically inert (that census matches RPC verbs only); the substitute is stronger. brief-625 §3 is corrected by this row. |
| #625 | Issuer-rank blind spot (invite from a since-demoted issuer previews `pending`) | **Ratified as shipped** (R1): four outcomes stay; `p625.preview.issuer_rank` pins the divergence; follow-up issue. |
| #639 | Keep `client_not_found` vs `obo_not_active` distinct on `complete_fixed_asset_particulars_for` | **Ratified.** The runtime door is OBO-explicit; 0195's ladder separates the classes; the browser-reachable door already folds both to CLR11. |
| #639 | `serve-built.mjs` gained a ten-line `viewer@` rank-0 persona branch; `test/domInspect.ts` gained `getElementById` | **Ratified.** Needed to drive the denied leg by role. Integration keeps the hunk as a union; if a second lane added a competing persona branch the integrator reconciles them into one. |
| #646 | Two unconditional persistent Alerts ("revision accepted" / "accounting impact pending") | **Ratified**; brief-646 §64 specifies two rows. A conditional second row is #676's concern. |
| #647 | No FK from `counterparty_identity_revisions` to `counterparties`; tenant congruence by a lock-free assertion inside the sole ungranted writer | **Ratified.** The FK's `FOR KEY SHARE` re-opened a measured deadlock between two shipped human doors. Follow-up: document `merge_counterparties`' lock order (0149 splice). |
| #647 | Only `KnowledgeSourceBlock` reused from `knowledge-shared.tsx`; the kind/trust vocabularies are not called | **Ratified.** An alias has no kind/trust concept in 0200; calling those helpers would invent data. brief-647 "call verbatim" is narrowed to the one applicable export. |
| #649 | UI-21 altitude leg not authored | **Ratified as a named residual** (R2); follow-up issue for a docker-free path or a mock lane owning the rail + full-screen thread. |
| #649 | `name_family_collision` detail carries candidate ROWS, not bare ids | **Ratified.** brief-649 lines 51/74 are corrected by this row. |
| #650 | `active` facet = queued/running only; `stopping` Work carries no tile | **Ratified** (R3); residual named on the surface and in the report. |
| #652 | Authority window must lie inside the stated term (`effective_to` required) | **Ratified** (R4). The chat tool's schema is the module's (`lib/accrual-basis.ts`): `effective_to` is no longer optional there and the cut follows the module, not the older stanza text. |
| #652 | CLR10 `accrual_schedule_yields_no_occurrence` for a schedule that reaches no due date | **Ratified** (R4). Follow-up for the plan lane's own door (owned by #653's recuts) to share the refusal. |
| #654 | Firm-scope evidence census counts LIVE violations only; superseded/withdrawn revisions raise NOTICE | **Ratified** (R5). It is the invariant the three guards enforce and what every read emits. |
| #649 | Round 2: settle door takes the client advisory rung FIRST (rung → client row → plan) rather than B1's literal remedy (client row → rung → plan) | **Ratified.** The re-checker measured that the literal order inverts against `set_client_fy_end` (rung @1127 before its `clara.clients` update @2479) — the very door settle calls — while rung-first sits above every neighbour (0037 §K, rung before rows). `clara.approve_wrong_client_correction` takes a `clara.clients` row before the rung — a pre-existing violation, follow-up issue, not this ticket's. |

### 3.2 · Blueprint drift — recorded for the #683 blueprint sync, NOT edited this wave (owner ruling 2026-09-15)

| Pointer | Claim | Measured state |
|---|---|---|
| `docs/PRD.md:114` | high-stakes gating reads as amount-based | `clara.is_high_stakes` (0004:72-78) also fires on opening balance, year end and tax-affecting; the Work lane never calls it, `book_staff_advance_application`'s drafted branch (0043:2666) still does (#638). |
| `docs/ARCHITECTURE.md` §3.5 / PRD F3 | fixed-asset materialisation is intrinsic because the hook runs at every approve path | False since 0178; true again only because 0201 adds a lane-agnostic birth trigger. `0041:4528-4529` and `x41-wave-d-a-fa.test.mjs:186` still say the old thing; `p639.census.approve_paths` enumerates the paths (#639). |
| `docs/ARCHITECTURE.md:171` | pins are `chatTurn→chatTurn_v19`, `claraWork→claraWork_v3` only | omits `clientOnboarding→clientOnboarding_v4` today; after this wave's cut the three pins are v20 / v4 / v5 (#649, integration). |
| `docs/PRD.md:69` | "an existing authorisation rule" is accepted plan authority | `accounting_plans.authority_kind` admits exactly `explicit_instruction` (0193:418); `create_accounting_plan` refuses `authority_rule` by name (0193:1474-1480). Unchanged by 0208 (#653). |
| `docs/PRD.md:69` | prepayment amortisation is handled "through conversation, documents or Accounting directly" today | The chat half is delivered only by the `chatTurn_v20` cut in this wave's integration (#653). |
| `docs/PRD.md:123` | re-evaluation deferred to #658/#663 with human review as interim | Confirmed as built: the human-review affordance ships; no engine (#654). |
| `docs/PRD.md:122` | #654's remainder is "界面或对话入口" | Interface half delivered; the chat capture of firm scope is a `claraWork_v4` contract only (#654). |

### 3.3 · Integration notes derived from the reports (binding for the integration worker)

1. Merge order (migration order, no-migration branch first): #633 → #650 (0199) → #647 (0200) → #639 (0201) → #646 (0202) → #648 (0203) → #649 (0204) → #654 (0205) → #638 (0206) → #652 (0207) → #653 (0208) → #625 (0209).
2. 0201 (T.5) and 0206 (§0) both pin the `_subledger_on_approve` caller roster at six names by whole-schema regex; a sibling body that merely NAMES the hook trips 0206's prestate. On the from-scratch merged chain: if §0 refuses, re-measure and re-issue the roster string in 0206 (unmerged, so allowed — record it); never weaken the assertion.
3. The from-scratch chain needs a cluster that has never run a clara migration (0154's role census is cluster-global, `= 14`): `rigint` on 55600 is fresh; never re-apply into a second database on it.
4. Shared files with known merge hazards: `e2e-fixture-ownership.test.ts` (`SHARED_RPC_VERBS`, `CORE_RELATION_HANDOVERS`, `LANE_MOCKS`, `LANE_DECLARATIONS`), `serve-built.mjs` (#639 persona branch, #648/#639 default responses, twelve hooks), `packages/db/package.json` gate chain (migration order, not alphabetical), `rig-meta.mjs` cohorts (`];` repairs), `action.yml` (each ticket's stated step order; per-line `\` continuations), `tree.ts`/`routes.ts` leaves (re-run #652's `resolveActive` wall in `tree.test.ts`), `en.json`, `manifest.txt`, `CONTEXT.md`, `kp.01` stays exactly ten (no lane minted a knowledge key).
5. Successor cut reads EVERY report's stanza, not §1.1's summary: `chatTurn_v20` carries #638 `start_staff_expense_claim_work`, #652 `start_accrual_work`, #653 `start_prepayment_schedule_work`; #647's `record_counterparty_alias` needs an OBO door no branch shipped → NOT cut, stays a contract (D11). `claraWork_v4` carries #654 (a) knowledge-context step + `observedRevisions({knowledge_version})` and (b) `ask_knowledge_conflict`, #639 `apply_fixed_asset_particulars`, #652 `answer_accrual_term` park, #653 term park (`read_prepayment_source` + prompt sentence) — only what each stanza specifies; anything a stanza leaves open stays out and is named. `clientOnboarding_v5` + `interview.v4.questions.ts` per #649 (`fye_day` stays `required_for_commit:false`).
6. Host-contention flakes are documented per report (sign-in timeouts; `thread-live-clarify`, `use-clara-thread-stop` cell 630, `onboarding-checklist`, `work-detail` "641 switching a tab"); a red in those files is a merge regression only if it reproduces in isolation.
7. Rigs: `rig654r` (55612) is STALE (pre-round-2 0205) — drop before any use; all `rig<n>r` and `rig647z` are dropped at wave close.
