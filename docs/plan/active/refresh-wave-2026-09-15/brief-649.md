# Brief: #649 — create a client and continue accounting onboarding from what is already known

## Orchestrator decisions (binding)

- **Migration `0204` only**, ONE file (`packages/db/migrations/0204_client_onboarding_facts.sql`): house header, prestate pins, tail assertions, a preintegration gate module, `rig-meta.mjs` cohort rows (DECISIONS §1.2).
- **This branch recuts NOTHING** (§1.3: "#649 → none"). `settle_client_onboarding_facts` calls `clara.set_client_fy_end` **as-is**. Do **not** recut `begin_client_onboarding` / `create_client` (orchestrator ruling — overrides gap-649 slice item 2). So the `(text,text)` overload hazard (`0103:1055-1070`; roster `0017:5139-5140`) never arises and the twelve branches stay parallel-mergeable.
- **Pins are MEASURED on your rig, never transcribed** (§1.2; SYNTHESIS §7 risk 7). Take them off `pg_proc.prosrc` on `clara_649` after `pnpm db:migrate`; pin these **five** as **non-regression** prestate pins (`0195:396-409` idiom) and re-assert them in the tail: `clara.set_client_fy_end(uuid,integer,integer,text)` — **live body is a two-stage splice** (created `0041:3242`, then 0042 §S5.12 and 0045 §S5.12-b2; 0041's text is NOT the live body); `clara.commit_client_onboarding(...)` (`0017:2751`, **spliced by 0018 SS4**); `clara.begin_client_onboarding(text,text)` (`0017:2492`); `clara.create_client(text,text)` (`0017:2529`); and **`clara.name_family_candidates(uuid,text)` (`0103:755-772`)** — the wrapper's contract (leading-token match over clients UNION counterparties) *is* that body, so pin its shape, not only its ACL. A pin off file text will not match and 0204 refuses to apply.
- **D7 is ruled, not open** (orchestrator ruling — supersedes gap-649 Q1 "blocked"): the settle door writes the canonical client record; fy-end through `set_client_fy_end` with **month + day**. The web Field **asks the day**; month-end is a **visible suggestion**, never silently derived. **The day reaches the DB as a parameter of the new settle door** (§3 item 2) — no plan-item write, no catalog row (§1.5). The interview's day question and ask-only-missing (H-52) ship in `clientOnboarding_v5` at the **integration cut**.
- **No successor on this branch** (§1.1). You cut no `_vN` and touch no frozen file; you ship DB doors + the human web door + a **successor contract** stanza. **AC2's interview half is "contract delivered, closes at the cut"** — say so; do not claim H-52 fixed. Anything `v5` imports freezes on import (`lib/periodic-adjustment-basis.ts` proves it: `frozen-workflows.json`, `deployed:true`, "frozen BY CLOSURE"), so **durable rules go in 0204**, never in a runtime lib.
- **Ambiguity arities** (orchestrator ruling — supersedes gap-649 Q2): **0 → proceed; 1 → show the candidate and require an explicit "different business" acknowledgement in the face; ≥2 → the DB refuses with candidate ids**, reusing the existing token `name_family_collision` (`0142:451-453`), never a second vocabulary. **No new parameter on `begin_client_onboarding`.**
  - **Preferred mechanism**: one SECURITY DEFINER wrapper `client_identity_candidates` returning the bounded list at arity 0/1 and **raising** CLR10 `name_family_collision` with candidate ids in `detail` at ≥2 — the wall is in the DB with zero recuts. **Proof to keep it**: `p649.identity.wall_two` (the raise, no client row born) **plus** `p649.identity.direct_birth_residual` (a direct `begin_client_onboarding` at ≥2 still succeeds). **Fallback**: not a recut and not a new birth verb — name the residual ("the ≥2 wall lives at the candidates read, not inside the birth door") and file a follow-up. Do not grow the slice.
  - **Grant EXECUTE on the wrapper only.** Never grant `name_family_token` / `name_family_candidates` / `name_family_is_ambiguous`: `0103:1225-1239` is a *live executable census* over five app roles raising CLR10 on any EXECUTE (repeated `0126:509`, `0154:551`). A replay to 0204 must leave it green — a red-first cell.
- **You mint no catalog rows** (§1.5 — supersedes gap-649 slice item 5 and cell (f)): no `knowledge_keys`, no `knowledge_plan_item_map`. #654 owns the catalog; a fy-end-day map row, if needed, is filed through **0205**.
- **Non-goal**, in code comment and report: never `p_promote_firm_scope=true` (`0192:1564`), never a firm-default entrance (#654 is PRD §4 deferred, `PRD.md:120`).
- **Ownership (§1.7)**: `client-workspace-overview.tsx` layout → **#650**, you add data seams only; `InterviewRunCard.tsx`, `useUploadQueue.ts`, `intake.ts` → **#633** and **#649 does not edit them** (orchestrator ruling — supersedes gap-649 slice Web item 2), so AC6's `Field` re-composition applies only to surfaces you own (`OnboardingItemRow.tsx`, the new candidate face, the fy-end Field). `StateBanner` (`components/common/state.tsx`) stays; Alert is #646's documents surface only. Nobody changes `lib/parts/hooks.ts`.
- **Shared-file one-liners only** (§1.6), at the sorted position: `apps/web/test/manifest.txt`, `apps/web/messages/en.json`, `apps/web/e2e/serve-built.mjs`, `apps/web/e2e/e2e-fixture-ownership.test.ts` (declare every RPC verb your mock answers; shared verbs declared shared), `packages/db/tests/rig-meta.mjs`, `packages/db/package.json` gates, `CONTEXT.md`. **No `tree.ts` row, no `require-firm-scope.ts` change** (no new route, no new firm leaf), **no new `needs-you.ts` row kind** (§1.6: none this wave).
- **World e2e**: extend the already-registered `packages/runtime/tests/interview-e2e.mjs` (`.github/actions/db-live-gates/action.yml:96`) — no new action.yml row; only a standalone leg needs one line after `interview-kill-resume-e2e.mjs` (`:100`).
- **Evidence (§1.10)**: every AC and historical row closes with a red-first cell, a browser-walk leg, or a **named** residual; REDESIGN rows re-measured, never copied; new DB batteries assert through `humanQuery` least-privileged personas, never `rootQuery`; a skipped frontier-gated battery is not evidence. **Blueprints are never edited** — ARCHITECTURE §4's version-pin sentence names only `chatTurn`/`claraWork` and goes stale at the cut; record that under "blueprint drift".
- **Playwright port triple**: `CLARA_E2E_APP_ORIGIN=https://127.0.0.1:3300`, `CLARA_E2E_NEXT_PORT=3301`, `CLARA_E2E_RUNTIME_PORT=3302`. **Rig row**: worktree `C:\Users\zhant\Desktop\clara-wt\649`, branch `impl/649-client-onboarding`, PG port **55508**, db **clara_649**, base `4464e471`, 193 migrations, PG 17.11.

## 1. Current state

**DB.** `clara.clients` = name + status (`0003:34-41`, widened `0017:659`) + `fy_end_month`/`fy_end_day` with `ck_clients_fy_end` admitting **only both-NULL or both-set** (`0041:774-778`, read this pass). Birth + plan in one transaction: `begin_client_onboarding(text,text)` (`0017:2492`) and twin `create_client(text,text)` (`0017:2529`) — both admin-floored, both mapping the `(firm_id, lower(name))` unique violation to CLR10, **neither consulting any family predicate**. Plan model: `onboarding_plans` (`0017:994`, one-open-per-client), `_items` (`0017:1041`), `_revisions` (`0017:1066`); doors `update_onboarding_plan` (`0017:2632`, the **runtime** interview CAS writer — `p_answered_by` revalidated as an active bookkeeper), `resolve_onboarding_plan_item` (`0017:2706`, the web's one plan writer), `commit_client_onboarding` (`0017:2751`), `cancel_client_onboarding` (`0017:2843`). Opening lane `opening_seed_registry` (`0017:1074`); commit accepts a finalized seed OR `first_year_zero_opening` OR `carry_down_deferred` (`0017:2812-2822`). Knowledge 0192: `promote_plan_answers_to_knowledge` (`0192:1563`, granted to both `clara_authenticated` and `clara_runtime`, role-discriminated `:1763-1764`); **`fye` is already promoted, as a month** (`0192:305`, "JSON number 1-12"); nine identity keys excluded (`0192:271-278`). Identity primitives deliberately ungrantable: `name_family_token/candidates/is_ambiguous` (`0103:725/755/781`), the predicate literally `count(*) > 1` (`0103:781-783`, read this pass). `set_client_fy_end` is `clara_authenticated`-only (`0041:4414`, `:4421`); live refusals CLR04/CLR11/CLR37 **and CLR38 `fy_end_locked_by_annual_cadence` ×2**. `apply_coa_template` refuses while the newest plan is open (`0173:8-16`); `coa_chart_state` gained `seed_decision_plan_state` (`0170:19-26`); `upsert_account` (`0009:1460`) has no plan gate.

**Runtime.** `clientOnboarding` is registry-pinned to `clientOnboarding_v4` (`packages/runtime/workflows/registry.ts:264`, `:809`, `:889`, `:908`); v1–v4 all exported, all `deployed:true`. `CLIENT_SEGMENTS_V2` (`interview.v2.questions.ts:75-98`): `sst_regime` → `sst_no` with **no `appliesTo`** (`:83-84`), `fye` **month-only** (`:88`; `validateFye` returns 1–12, `interview.v1.core.ts:177-182`). `segmentApplies` (`interview.v2.core.ts:245`) is the only skip mechanism; `prior` starts `{}` and holds this run's answers alone (`clientOnboarding.v4.ts:119`, `:151`). `interviewRoutes.ts` enqueues through the registry, so a pin change repoints it with no route edit; `registry.ts` is **not** a frozen manifest key.

**Web.** `/clients` carries the H-51/CB-AE2E-024 `AddClientControl` (`components/firm/client-register-list.tsx:66`, `:88`, `:130`) reusing ⌘K's dispatch. Client Home = `ClientWorkspaceOverview` (`:59`, `:81`); the layout renders no chrome since #614 (`clients/[clientId]/layout.tsx:13`); `ContinueOnboardingCard` links the rail to the full-screen thread, read-only, `?from=` preserved (`client-workspace-overview.tsx:96-99`). Onboarding is **rail-mounted, never a route**: `OnboardingChecklistCard.tsx` → `BeginOnboardingCard`/`ClientOnboardingCard` mounting `InterviewRunCard`, `OnboardingItemRow` (bare `Textarea` resolve and amend controls, `:152`, `:212`), `ApplyStandardChartControl`, `OnboardingSettledCard`. `readCoaChartState` (`apps/web/lib/onboarding/coa.ts:82-97`, read this pass) maps exactly seven fields and **never reads `seed_decision_plan_state`**. `ClientIdentityBand` (`components/firm/client-home/client-identity-band.tsx:63-64`) reads `client_facts` only. `opening-position-gate.tsx:84,:95,:111` has three honest branches; **no onboarding surface links to it** (`client-onboarding-progress.tsx:110`). `REVIEW_QUEUE_ROW_KINDS` has ten kinds, none an onboarding plan item (`apps/web/lib/firm/needs-you.ts:97-118`, read this pass). `field.tsx` is installed and used by `work-question-form.tsx:70`; no `questionnaire.tsx`, no `progress.tsx`; style `base-nova`.

**Tests.** DB `wave-b/wb-o-lifecycle.test.mjs` (O1–O7 under real per-role users), `knowledge-onboarding-promotion.test.mjs` (13 cells), `coa-template-pr-b.test.mjs`; family-predicate cells are `rootQuery`-based (`f-a7-pi.test.mjs`). Runtime `interview-e2e.mjs` (real in-process WDK Postgres World, **version-agnostic by construction**, `:7-12`) and `interview-kill-resume-e2e.mjs`. Web `onboarding-checklist.test.tsx` (14 cells), `add-client-control.test.tsx` (7 cells), `onboarding-amend-and-chart.test.tsx` (pins the four-parameter resolve call, `:214`). Playwright `interview-walk.spec.ts` (13-segment walk inside the full-screen route + axe + typed cancel + two-context race) against **pre-created fixtures** (`:47-51`) — **no client-creation walk, no duplicate face, no rail→full-screen altitude leg, no 320px/200%/reduced-motion on any onboarding surface**.

## 2. Gaps / rows

| Row | Disposition |
|---|---|
| **AC1** duplicate/ambiguous identity + scope | **to build** — the wrapper + the three-arity face. Scope preservation **verify-only** (`client-scope-provider.tsx`, `clients/[clientId]/layout.tsx:35`). |
| **AC2** ask-only-missing / canonical record / Knowledge / resume | **partial-with-named-residual.** (a) ask-only-missing + H-52 → **successor contract only**, closes at the v5 cut. (b) Knowledge → verify-only (`0192:305`) plus the identity-band seam that makes a promoted fact visible (own cell). (c) canonical record → **to build** (settle door, D7). (d) resume → verify-only (`interview-kill-resume-e2e.mjs`). |
| **AC3** first-year vs deferred + opening link | **to build (the link only)**; separation already exists end-to-end. **Out of scope: widening `accounting_work.purpose`** — authority: DECISIONS §1.3 (no recut) + `0194:229-230`; gap-649 Q3 promoted to a decision. |
| **AC4** stale / concurrent / cancel / revocation | **verify-only, re-measured on the new surfaces** (`0017:2657`, `:2661`, `:2781`, `:2843`; `interviewRoutes.ts:341`). Drafts never cross clients. |
| **AC5** reach Home; no blanket ritual | **partial.** Build the `seed_decision_plan_state` seam. The open-plan chart refusal (`0173:8-16`) is **intended** — state it, do not remove. An onboarding plan-item needs-you row kind is **out of scope** — authority: DECISIONS §1.6 ("new `needs-you.ts` row kinds: none this wave"). |
| **AC6** states / 320px / zoom / keyboard / SR / motion / URL-Back / drafts | **to build** on surfaces you own: the candidate face, the fy-end Field, **`OnboardingItemRow`'s resolve + amend controls re-composed onto `Field`** (§3 Web, seam 11), and the progress section. `InterviewRunCard` re-composition **out of scope** — owner #633 (§1.7); name the residual. |
| **AC7** least-privileged roles + real World | **to build** — new battery through `humanQuery` personas + the `interview-e2e.mjs` extension. |
| **UI-21** full-screen onboarding | **verify-only, re-measured by the new altitude leg** (seam 12): keyboard/focus-return, typed-data survival across the rail→full-screen change, 320px/200%. #649 edits no file on that route — the walk follows `ContinueOnboardingCard`'s existing link read-only. |
| **CB-AE2E-024 / H-51** Add Client on `/clients` | **verify-only + extend** — the AC1 face lands on exactly this control; carry the named question/keyboard/format/count/focus regressions (`add-client-control.test.tsx:113,:149,:163,:176,:212`). |
| **H-50** stale "Onboarding" page head | **verify-only** — structurally removed (`clients/[clientId]/layout.tsx:13`); re-measure the announce→re-read chain. |
| **H-52** SST number still asked | **successor contract only** — live and unfixed (`interview.v2.questions.ts:83-84`); the fix is `CLIENT_SEGMENTS_V4`'s `appliesTo`, cut by integration. A named residual, not a claim. |
| **COA seeded before cancelled onboarding** | **verify-only + owed count.** Recurrence closed (`0173:8-31`). **No repair migration**: the row's own instruction is a hosted COUNT first — write "hosted evidence pending". |

## 3. Slice (one branch)

**Migration `0204_client_onboarding_facts.sql`** — additive; FORCE RLS; no app-role DML; `Asia/Kuala_Lumpur` days.
1. `clara.client_identity_candidates(p_name text, p_identifier jsonb default null) returns jsonb` — SECURITY DEFINER, `search_path = clara, pg_temp`, `_human_ctx(role_rank('admin'))` (the birth door's floor), **firm from the session, never a parameter**. Composes `name_family_candidates(firm, p_name)` with an exact `lower(name)` hit and, when supplied, a `client_identifiers (kind, value_normalized)` match; returns `{arity, candidates:[{id,name,status,match_reason}]}`; **raises CLR10 `name_family_collision` with candidate ids in `detail` at ≥2**. EXECUTE to `clara_authenticated` only.
2. `clara.settle_client_onboarding_facts(p_plan uuid, p_fy_end_month int, p_fy_end_day int, p_op_key text)` — **human lane only**, no `_for` twin (`_human_ctx` raises CLR04 with no `jwt_sub`, `0004:302-303`; `set_client_fy_end` is `clara_authenticated`-only, `0041:4414`). **This four-argument signature is the day's whole transport**: the web Field types the day and passes it here. `p_fy_end_day` NULL → CLR10 `fy_end_day_required`, **never derived** (D7); `p_fy_end_month` NULL → the plan's settled `fye` answer (`0192:305`); supplied but different → CLR10 `fy_end_month_contradicts_plan` (the face shows both and sends the human back to the interview answer); neither present → CLR10 `fy_end_month_unanswered`. **No `update_onboarding_plan` call, no new `item_key`, no map row** (§1.5) — the runtime owns that writer (`0017:2632`), so the day lands on `clara.clients.fy_end_day` and **not in Knowledge this wave**: name that residual (`clientOnboarding_v5` makes the day an interview answer; a `financial_year_end_day` key is #654's through 0205). `_reserve_op`/`_finish_op` idempotency (the op hash covers month and day, so a replay with a different day is the house receipt-hash CLR10), `_audit`, typed `detail.reason`. Calls `set_client_fy_end(client, month, day, op_key)` **as-is** and **surfaces CLR38 honestly** — a refused fy-end write presented as a successful onboarding is the worst outcome here. Refuses an OPEN plan; CLR37 on an impossible calendar day.
3. Events registered with `ignore` (0055 idiom); **no new table DML grants; no catalog rows**.
4. Prestate: the five measured non-regression pins. Tail: re-prove owner/definer/`search_path`/ACL for both new functions, re-assert the `name_family_candidates` body pin, and re-assert that the three `name_family_*` helpers stay EXECUTE-unreachable to all five app roles. Preintegration gate module + its own `rig-meta.mjs` cohort ("wholly present or wholly absent", `rig-meta.mjs:257-259`).

**Runtime.** No successor, no registry edit, no frozen file. If a rule must live in TypeScript, put it in a module the **web alone** imports. Extend `interview-e2e.mjs` (registered at `action.yml:96`): assert that `sst_regime=not_registered` **today** still announces an `sst_no` park — the successor's red, recorded as owed. Keep `interview-kill-resume-e2e.mjs` green. **Strike** the stale `clientOnboarding_v3` parenthetical at `interview-e2e.mjs:9` rather than repointing it to v4 — that header's own rule forbids naming a version in prose.

**Web** (no new route, no `tree.ts` row).
- **Candidate face** on `AddClientControl`: call the wrapper before Confirm; render a bounded list, each row a real link with its match reason; arity 1 requires an explicit "different business" acknowledgement (`Checkbox` in a `FieldSet` — appendix D #16/#28) before Confirm re-enables; arity ≥2 renders the DB refusal **verbatim with its code** beside the same list via `StateBanner`; the typed name stays. Never a second client-side predicate.
- **Fy-end Field**: `FieldGroup`/`Field`/`FieldLabel`/`FieldDescription`/`FieldError` on the `work-question-form.tsx:70` precedent; month **and day**, month-end shown as a suggestion button, never pre-applied. **Same pass, same primitives**: re-compose `OnboardingItemRow`'s resolve and amend `Textarea`s (`:152`, `:212`) onto `Field`/`FieldLabel`/`FieldError` with `aria-invalid`, keeping the four-parameter `resolve_onboarding_plan_item` call its existing cell pins (`onboarding-amend-and-chart.test.tsx:214`). `Questionnaire` is **not** installed and is not used (appendix D #45: the durable workflow and plan CAS already own identity/version/persistence/branching/cancellation).
- **Settle call** beside `promotePlanAnswersToKnowledge` at commit, passing the Field's month **and** day: a projection failure stays swallowed; **CLR38 and the two fy-end refusals are shown**.
- **Two data seams only** on client Home: `seedDecisionPlanState` added to `readCoaChartState` (`coa.ts:82-97`) with 0170's honest copy in `ApplyStandardChartControl`; the opening link from `ClientOnboardingProgress` into the registers opening tab on the deferred branch, and a "first year — no opening needed" line otherwise. `ClientIdentityBand` reads promoted Knowledge alongside `client_facts` (`client-identity-band.tsx:63-64`), keeping the unknown-key raw-key fallback.
- **States on every touched surface**: loading (shape-matched `Skeleton`), successful-empty, no-results (query preserved + Clear), partial/stale (`StateBanner`), invalid/saving (`aria-invalid` + `FieldError`, draft preserved), denied (names the restriction, no fake retry) / failed (verbatim code), cancelled/recovery; 320px, 200% zoom, keyboard + focus return, screen-reader names, `prefers-reduced-motion`, stable URL/Back, preserved drafts.

**Docs.** `CONTEXT.md` in the house "term / Avoid" shape: **Client onboarding plan**; **Opening position** (*Avoid*: treating a deferred carry-down as "no opening needed"); **Client identity candidate** (*Avoid*: presenting an exact-name check as duplicate detection — state the `count(*) > 1` arity plainly). READMEs: `packages/db/README.md` (both functions, floors, grants, the day-as-parameter ruling, the ungranted-helper/granted-wrapper idiom and the census enforcing it), `packages/db/tests/README.md`, `apps/web/README.md`. **Never** `docs/PRD.md` or `docs/ARCHITECTURE.md`.

**Successor contract stanza** (exact, in your report): `clientOnboarding_v5` + `interview.v4.questions.ts` exporting `CLIENT_SEGMENTS_V4 = CLIENT_SEGMENTS_V3.map(...)` replacing only changed segments **by reference** (`interview.v3.questions.ts:107`'s discipline, asserted by `coa-interview-v4.test.mjs`). Change 1 — `sst_no` gains `appliesTo: prior => prior.sst_regime !== 'not_registered'` (H-52). Change 2 — a `fye_day` segment after `fye`, with validator and `toItems` shape, so the day becomes an answer instead of a settle parameter. Change 3 — a known-facts pre-read step so an answered fact is "an absent question, not an unanswered one". Registry: import/export v5, repoint `registry.ts:264` and `:908`, keep v1–v4 exported and in `workflowBodies` (the stranded-body gate refuses **database-wide** otherwise, ARCHITECTURE §5 F). No door call, no refusal map, no part kind, no `WORK_ACCEPTED_PURPOSES` widening.

## 4. TDD seams (red first)

New battery `packages/db/tests/client-onboarding-identity.test.mjs`, asserting through `humanQuery` personas (admin / bookkeeper / viewer), never `rootQuery`.

1. `p649.identity.family` — two clients sharing a leading token: the wrapper returns both with `match_reason`; a third firm's matching client is **invisible** under real RLS. *Red: the function does not exist.*
2. `p649.identity.wall_two` — arity ≥2 raises CLR10 with `detail.reason='name_family_collision'` and candidate ids; assert the **`clients` row count is unmoved**, not the message.
3. `p649.identity.arity_one` — arity 1 returns exactly one candidate and **does not raise** (the wall is the face's, by ruling).
4. `p649.identity.direct_birth_residual` — `begin_client_onboarding` called directly at ≥2 still succeeds; the cell **documents the named residual**, it does not assert a wall.
5. `p649.identity.floor` — the wrapper is CLR04 to a bookkeeper and 42501 to `clara_runtime`; a below-floor caller cannot distinguish a real foreign firm from a random uuid.
6. `p649.identity.census_replay` — after replay to 0204, `has_function_privilege` over five app roles × the three `name_family_*` signatures is still empty (`0103:1225-1239`, `0126:509`, `0154:551`). Cheap, decisive; the wrapper design turns on this negative.
7. `p649.settle.fy_end` — the door writes `fy_end_month`+`fy_end_day` **from its parameters** and satisfies `ck_clients_fy_end`; a NULL day is CLR10 `fy_end_day_required` with nothing written and nothing derived; a NULL month takes the plan's `fye` answer; a month contradicting that answer refuses; an OPEN plan refuses; 31 February returns CLR37; the op key replays byte-identically and a replay carrying a **different** day is the receipt-hash CLR10.
8. `p649.settle.clr38` — under a live ANNUAL adjustment template or depreciation authority the door surfaces CLR38 `fy_end_locked_by_annual_cadence` **and writes nothing** (copy `packages/db/tests/x42-adjustments.test.mjs:247`).
9. `p649.settle.replay_isolation` — the door never touches another client's plan; a second concurrent settle CASes cleanly.
10. Runtime, in `interview-e2e.mjs`: `p649.interview.sst_park_today` — with `sst_regime=not_registered` the park is **still** announced today (the successor's red, recorded as owed); the kill/resume leg stays green.
11. Web unit — **five** files, each added to `apps/web/test/manifest.txt`: `add-client-candidates.test.tsx` (candidates rendered; Confirm gated until the arity-1 acknowledgement; ≥2 refusal verbatim + code; typed name preserved); `onboarding-field-composition.test.tsx` (the fy-end Field's label/description/error wiring, `aria-invalid`, the month-end suggestion is a click not a default, draft preserved across a validation failure — **and** `OnboardingItemRow`'s re-composed resolve/amend controls keeping their four-parameter door call); `coa-seed-decision-state.test.tsx` (`readCoaChartState` returns `seedDecisionPlanState`; the control renders the open-plan copy); `client-onboarding-progress-opening-link.test.tsx` (link only on the deferred branch); `client-identity-band-knowledge.test.tsx` (a promoted Knowledge fact renders beside `client_facts`, unknown-key fallback intact).
12. Playwright `apps/web/e2e/client-create-walk.spec.ts` + `client-create-mock.mjs`: register → candidate face → acknowledge / rename → new client → land on client Home, at desktop **and 320px**, with an axe scan, 200% zoom, a keyboard-only path, focus return from the candidate dialog and stable URL/Back. Extend `interview-walk.spec.ts` with a focus-return/Back-stability arm **and the UI-21 altitude leg**: type an answer in the rail on client Home, follow `ContinueOnboardingCard`'s link (`client-workspace-overview.tsx:96-99`; read-only, no edit to #650's file), then prove the typed draft survives the altitude change, focus lands in the thread and Back returns to Home — at 320px and 200%.

## 5. Risks

- **Merge**: 0204 is additive with zero recuts, so it cannot collide with 0199–0209. The live collisions are web: `InterviewRunCard.tsx` (#633 owns it — **do not open the file**) and `client-workspace-overview.tsx` (#650 owns the layout — **two data seams only**; the UI-21 leg reads it, never edits it).
- **Exact shared-file one-liners this ticket adds**: `packages/db/package.json` → one `--import ./tests/client-onboarding-identity-preintegration-gate.mjs` (the 29th gate); `packages/db/tests/rig-meta.mjs` → one 0204 cohort block; `apps/web/test/manifest.txt` → **five** lines; `apps/web/messages/en.json` → one namespace; `apps/web/e2e/serve-built.mjs` → one `import { handleClientCreateSupabase } from "./client-create-mock.mjs";` + one dispatch line; `apps/web/e2e/e2e-fixture-ownership.test.ts` → one ownership row declaring every RPC verb the mock answers (`client_identity_candidates`, `begin_client_onboarding`, and any shared verb declared shared); `CONTEXT.md` → three terms. **No `tree.ts`, no `require-firm-scope.ts`, no `needs-you.ts`, no `action.yml`.**
- **Census suites red on files you never touched** — a new file under `app/**`, a changed href, a new SQL function or a new mock verb trip `tests/firm-scope-fourth-entrance.test.ts`, `tests/parity-holes.test.ts`, `tests/sql-oracle.test.ts` and `e2e-fixture-ownership.test.ts`. **A transcribed pin refuses to apply** — both spliced bodies *and* `name_family_candidates` must be measured.
- **An ambiguity wall can block legitimate onboarding, and at arity 1 the predicate does not fire at all** (`0103:781-783`). Both failure modes are live; the three-arity ruling is the mitigation and the arity-1 face is the only protection there.
- **AC2 cannot fully close on this branch** (SYNTHESIS §7 risk 5). Without the "closes at the v5 cut" sentence the report claims an outcome the branch cannot produce. Same discipline for the fy-end day: a **settle parameter this wave**, an interview answer only after the cut.
- **Do not "fix"** the known Windows-only reds #707 (grep shell-out) and #693 (EICAR quarantine); re-run `thread-live-clarify.test.tsx` in isolation if it flakes in the whole-suite run.

## 6. Effort and rig

**Effort: XL** — one migration (two SECURITY DEFINER functions, five measured pins, an ACL/census tail), a new DB battery with its own cohort gate, the first client-creation walk plus mock, five web unit files at 320px/200%/reduced-motion/focus, an `interview-e2e.mjs` extension, and a successor contract that closes later.

**Rig**: worktree `C:\Users\zhant\Desktop\clara-wt\649`, branch `impl/649-client-onboarding`, PG **55508** / db **clara_649** (`export PGHOST=127.0.0.1 PGPORT=55508 PGUSER=postgres PGDATABASE=clara_649 CLARA_ALLOW_DESTRUCTIVE=1 CLARA_RIG_DB=1`). Node 22 via `export PATH="/c/Users/zhant/AppData/Local/pnpm:$PATH"`. Never set `CLARA_RIG_ALLOW_RESET=1` / `CLARA_RIG_ALLOW_ROLE_SWEEP=1`. **Port triple**: `CLARA_E2E_APP_ORIGIN=https://127.0.0.1:3300 CLARA_E2E_NEXT_PORT=3301 CLARA_E2E_RUNTIME_PORT=3302`.

**Before the final report, run**: `pnpm typecheck` and `pnpm lint` (both green); the **whole `apps/web` unit suite** once (`node scripts/run-tests.mjs` from `apps/web`, ~5 min), reporting its counts; `packages/db/tests/operation-census.test.mjs` and `rig-isolation.test.mjs` (no reset flags), since this slice adds SQL functions; your DB battery with the 28 `--import` gate flags copied verbatim from `packages/db/package.json`; and **both runtime World legs on your own rig** — from `packages/runtime`, `PGHOST=127.0.0.1 PGPORT=55508 PGUSER=postgres PGDATABASE=clara_649 WORKFLOW_POSTGRES_URL=postgres://postgres@127.0.0.1:55508/clara_649 node tests/interview-e2e.mjs`, then the same line for `tests/interview-kill-resume-e2e.mjs` (the CI shape at `action.yml:96`, `:100`). Seam 10 is unevidenced without those two commands and their exit codes. **Parts-parity / frozen check is not applicable** — this branch touches no module reachable from a frozen body; if that changes, run `check-parts-parity.mjs` and `check-frozen-workflows.mjs` and say so.

**Copy the house shapes from**: `packages/db/migrations/0193_accounting_plans.sql` and `0194_periodic_adjustments.sql` (header, prestate pins, tail assertions, gate module, cohort rows); `docs/plan/active/refresh-wave-2026-09-14/brief-640.md` and `brief-643.md` (slice shape); and `.../reports/640-review-closure.md` + `643-final.md` for what a reviewer later demanded — **behavioural** cells rather than `prosrc` assertions, re-measured rather than copied evidence, an explicit merge-order collision table, and every claim carrying the command that produced it with its pass/fail counts.

## Verifier findings not applied

None — all six findings were applied; none contradicted DECISIONS.md.
