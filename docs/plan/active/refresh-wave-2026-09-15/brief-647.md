# Brief: #647 — counterparty identity provenance, revision history and a routed identity surface

## Orchestrator decisions (binding)

- **Migration: `0200_counterparty_identity_provenance.sql`.** gap-647 assumed `0199`; that is #650's (orchestrator ruling, DECISIONS §1.2). ONE file, house header + prestate + tail census in the shape of `packages/db/migrations/0193_accounting_plans.sql` and `0176_counterparty_alias_kind_scope.sql`; sha pins in the `0194_periodic_adjustments.sql:171-196` idiom, **measured on YOUR migrated rig** (`encode(sha256(convert_to(p.prosrc,'UTF8')),'hex')`), never transcribed. Preintegration-gate module + its own `rig-meta.mjs` cohort (wholly-present-or-wholly-absent).
- **D11 — provenance only; Clara gets no write verb.** `counterparty_aliases.recorded_via text NOT NULL default 'legacy_unknown' check (recorded_via in ('human_ui','agent','seeding','legacy_unknown'))` — **four values** (orchestrator ruling; gap-647 §(A) proposed three with `clara_runtime`). A trigger refuses `'human_ui'` when `clara.jwt_sub()` is null. Source pins (`source_document_id` / `_extraction_id` / `_region_id` / `_field_path`) with firm-congruent composite FKs (`0192:413-418` idiom) **and client congruence asserted in the door** — a #646 re-attribution otherwise leaves an alias pinned to a foreign client's document.
- **ONE revisions relation, not an identifier log.** `clara.counterparty_identity_revisions`, append-only, carries **rename, alias add, alias retire, identifier change and merge** (orchestrator ruling; gap-647 §(B) scoped it to identifiers). It IS AC5's "one versioned correction path". `set_counterparty_identifiers` keeps history by appending a revision in the same transaction.
- **Repoint the live human alias writer.** Recut `add_counterparty_alias(uuid,uuid,text,text,text)` **in place, at its existing signature, with DEFAULTed trailing provenance params** — never a new overload (the web door posts five NAMED args, `counterparty-doors.ts:89-101`, and `wake_fn_allowlist` is keyed by bare name). Also recut `rename_counterparty(uuid,uuid,text,text)` and `set_counterparty_identifiers(uuid,uuid,text,text,text)`. `merge_counterparties` (`0015:2295`) and `tick_seeding_proposal` (`0118:315-319`) are **declared residue** in the migration header and the report; their rows take the default.
- **Four events, no consumer.** `counterparty.renamed / .alias_added / .alias_retired / .identifiers_set`: client-scoped `event_types` + `trigger_taxonomy` rows at the ACTIVE version, decision `context_update`, the `0149:724-735` coupled-pair idiom. `docs/PRD.md:123` parks re-evaluation on #658/#663 — build no consumer.
- **gap-647 slice (F) is STRUCK (orchestrator ruling).** DECISIONS §1.5 + the #647 block: `knowledge_keys` / `knowledge_plan_item_map` are **#654's alone**. Mint no keys, no map rows, move nothing into `knowledge_records`, and **do not recut `get_knowledge_pack` or `list_client_knowledge`**. Therefore `knowledge-onboarding-promotion.test.mjs` kp.01 (`:92-113`) does **not** red, the frozen dependency on `knowledge.mjs:188` / `chatTurn.v19.prompt.ts:175` does not arise, and gap-647's runtime cells (c) are not owed.
- **Three reads are SECURITY DEFINER FUNCTIONS, never masked views** — a new masked human-read view joins debt-BAR1's catalog-derived roster (`debt-human-read-surfaces.test.mjs:264-271`) and reds its closed world. Widening `counterparty_aliases_visible` in place (`0145:960-964`) is permitted and reds nothing.
- **Unmerge: bounded discovery read only, no reversal door.** Merge lineage rendered; aging/statement expose recorded **and** canonical identity.
- **Surface ownership, and the `knowledge-shared.tsx` reuse boundary — it is not uniform, so it is named here** (this refines SYNTHESIS §3.3's "verbatim"; DECISIONS §2 #647 says only "reusing"). Identity detail is a registered nav leaf **under the client's Knowledge area** (DECISIONS §1.8). Call **verbatim**: `KnowledgeSourceBlock` (`:123` — props `{clientId, source: KnowledgeSourcePins, sourceKind: string}`; map your four pins into that shape with `work_id: null` and pass an existing `ClientKnowledge.sourceKind.*` key — `document_extraction` where an extraction pin rides, `user_statement` otherwise — or add one key there), `knowledgeValueText` (`:29`), `appliesWhenText` (`:43`), `KnowledgeKindBadge` (`:50`), `KnowledgeTrustBadge` (`:72`). **Do not call — follow the shape:** `KnowledgeBadges` (`:82`), `KnowledgeApplicability` (`:95`) and `KnowledgeProvenance` (`:167`) all take `record: KnowledgeRecordRow`, whose `recorded_via` is the two-value union `"human_ui" | "clara_runtime"` (`lib/registers/knowledge.ts:124`), rendered by looking up `recordedVia.<value>`, which has exactly those two keys (`en.json:2611-2614`). D11's `agent` / `seeding` / `legacy_unknown` therefore neither typecheck nor translate — and `legacy_unknown` is the backfill default every pre-migration alias carries. Build counterparty-typed equivalents in your own components at the same dl/label shape. **Do not widen `KnowledgeRecordRow`**: `lib/registers/knowledge.ts` is the C13 register #654 edits concurrently (SYNTHESIS §3.3), and a widened union puts every existing call site on keys `ClientKnowledge.recordedVia` does not have. The four counterparty `recordedVia.*` strings go under `ArApCounterparty`. #654 owns `/settings/knowledge` — render nothing there. #650 owns `client-workspace-overview.tsx`; #625 owns `lib/firm/capabilities.ts`; `StateBanner` stays as built.
- **Do not touch:** `_record_journal_entry_core`, `_tf_accounting_work_immutable`, `admit_journal_work`, `list_activity` / `get_activity_event` (D13 — record the residual), `_knowledge_capture_core`, `_knowledge_floor`, `capture_knowledge`, `periodic_adjustments`' purpose CHECK (DECISIONS §1.3); `_draft_entry_core` / `_approve_entry_core` (position-probed, `0197:638-642`); `_document_posting_entry` (sha-pinned twice by 0197); `_resolve_counterparty` and `merge_counterparties` (live bodies are splices — `0149:436-536` re-substitutes from `pg_get_functiondef`). No `wake_fn_allowlist` row (C-22); no `clara_runtime` / `clara_agent_ro` EXECUTE on any identity door.
- **Two mechanism choices, with the proof that picks them.** (1) *Alias door* — preferred: in-place recut with defaulted trailing params; the 7-arg-overload fallback is forbidden unless a rig probe proves the in-place form impossible. Proof either way: `select oid::regprocedure from pg_proc where proname='add_counterparty_alias'` returns **exactly one row before and after apply**, plus a door cell posting the five named args. (2) *Route* — preferred: `/clients/:clientId/knowledge/parties/:counterpartyId` with a new `ClientLeafId` member and a `leafFor` arm for `parent==="knowledge" && rest.length===3 && rest[1]==="parties"` — today `leafFor` (`tree.ts:569-579`) answers `knowledgeRecord` at length 2 (`:577`) and `null` at 3 (`:578`), so a three-segment knowledge path gets no leaf and the crumb stops at "Knowledge". Fallback: a dedicated `counterparties` client-nav parent, only on a measured crumb collision. Proof: a `tree` cell asserting the leaf resolves and the breadcrumb names it, plus the Playwright stable-URL/Back leg.
- **Successor contract:** no chat/Work tool ships here; `claraWork_v4` is not implied. If you build the non-frozen `packages/runtime/lib/counterparty-identity.ts`, write the full stanza (tool name, `.strict()` zod input, door call + arg order, refusal→message map, part kind) in the report for the integration worker's `chatTurn_v20`. Cut no successor.
- **Port triple** (DECISIONS §1.9): `CLARA_E2E_APP_ORIGIN=https://127.0.0.1:3280`, `CLARA_E2E_NEXT_PORT=3281`, `CLARA_E2E_RUNTIME_PORT=3282`. **Rig row** (RIG.md:23): worktree `C:\Users\zhant\Desktop\clara-wt\647`, branch `impl/647-counterparty-identity`, PG port **55506**, db **clara_647**.
- **Blueprints (`docs/PRD.md`, `docs/ARCHITECTURE.md`) are never edited.** Contradictions go under "blueprint drift".

## 1. Current state

**DB.** Stable uuid PK + `unique(id,firm_id,client_id)` (`0009:812-839`); both live uniques kind-scoped (`0015:159-160`, `:187-192`). `clara.counterparty_aliases` (`0011:651-672`) — `origin` a bare three-value CHECK at `0011:658`, `created_by` at `:659`, **no source pin, no machine-lane bit**. Four live insert sites: `0011:1736`, `0011:1807`, `0015:2295`, `0118:315` (which hard-writes `origin='human'`). Writers, all `_human_ctx` bookkeeper+: `add_counterparty_alias` (`0011:1706`), `retire_counterparty_alias` (`:1750`), `rename_counterparty` (`:1774`), `merge_counterparties` (`:1820`), `create_counterparty` (`0021:42`), `set_counterparty_identifiers` (`0174:785`, admin floor, overwrites in place; former values only in `_audit`, `0174:846-849`). `clara.counterparty_merges` (`0149:312-382`) — append-only-plus-one-reversal, `uq_cm_live_merged` (`:381`), forced RLS with an owner + human-SELECT policy pair and grant (`0149:425-431`). Canonicalising reads already ship `resolution` per group + `recorded_counterparty_id` per item (`0149:587-608`) and on both statement legs (`0149:649-661`). `counterparty_aliases_visible` is a masked `security_barrier` view since `0145:960-964`; `origin`/`created_by`/`client_id`/`kind` deliberately unprojected (`0145:946-947`). `t_counterparty_aliases_update` (`0011:973`) refuses any update changing a column other than `retired_at`; the disable/re-enable precedent is `0176:261-266` with its prestate at `0176:140-143`. Events: only `counterparty.created`, `.merged`, `.unmerged` (`0149:724-735`, no producer), `.binding_resolved`. `knowledge_records.recorded_via` is the NOT-NULL precedent (`0192:424`). `clara.client_identifiers` is readable by the web **directly under RLS** — `select` policy to `clara_authenticated` at `0007:793` plus the table grant at `0007:2741-2744` — and has no list door and no web caller.

**Runtime.** No counterparty or alias reference in `packages/runtime/src/*Routes.ts`, `chatTurn.v19.*` or `claraWork.v3.*`; `wiki-projection.mjs:111-112` subscribes to `created`/`merged` only.

**Web.** `counterparty-hygiene-panel.tsx` ends in `NotBuiltNote{aliasListNotBuilt}` (`:196-198`) — no alias list, retire control, identifiers control or history. `counterparty-doors.ts:89-101` posts five NAMED args and exports no `setCounterpartyIdentifiers`; `retireCounterpartyAlias` (`:105`) is exported and unused. `counterparty.ts:94-105` carries a **stale** frontier-0140 finding ("no honest way to discover an alias id") that `0145:960-964` refuted. `aging.ts:30-49` drops `recorded_counterparty_id` and `resolution`; `counterparty.ts:218-226` drops `recorded_counterparty_id`. C13 = `components/registers/knowledge-panel.tsx` (four faces, `:20-34`) + `knowledge-detail.tsx` (revision timeline + Correct/Withdraw, `:6-18`) + `knowledge-shared.tsx` (see the reuse boundary above). `tree.ts`: `ClientLeafId` (`:112`), `CLIENT_LEAVES` `knowledgeRecord` (`:386`), `knowledgeRecordHref` (`:420`), `leafFor` by exact length (`:569-579`). A leaf under `app/(firm)/clients/...` inherits the `app/(firm)/layout.tsx` entrance (`lib/require-firm-scope.ts:257`) — **no new `require-firm-scope.ts` row**, but `tests/firm-scope-surfaces.test.ts` walks `app/**` and must be run.

**Tests.** `counterparty-merge-pr-1.test.mjs` (`:22-30`, `:109-160`, `:317-334`), `counterparty-alias-kind.test.mjs` + gate (`packages/db/package.json:18`), `p4t2-counterparty-aliases.test.mjs:72-101` (a GRANT matrix, **not** a column census — it will not red), `knowledge-walk.spec.ts` (320px `:191`, 200% zoom `:202`) is the accepted walk shape. **No counterparty/identity walk or mock exists.**

## 2. Gaps / rows

| Row | Disposition |
|---|---|
| **AC1** ids reused; alias/identifier origin, source, actor; agent writes not labelled human | **partial → build.** Ids/uniqueness verify-only (`0009:812`, `0015:187`); build `recorded_via` + four source pins + the trigger + three writer recuts. Named residual: `merge_counterparties` and `tick_seeding_proposal` take the default. |
| **AC2** rename keeps identity + prior-name alias; merge lineage; recorded + canonical | **partial → build the surface half only.** DB done and proven (`0011:1807`, `0149:312-382`, `:587-608`, `:649-661`); build the two wire-type widenings and the lineage rendering. |
| **AC3** no general unmerge; bounded discovery | **gap → build** `list_counterparty_merge_corrections(p_client)` → `representable true\|false` + reason (carrier row vs bare `merged_into`). **No unmerge door**; the prohibition holds by absence (`counterparty-doors.ts:29`). |
| **AC4** roles distinct, cross-client ambiguous, no netting/authority | **verify-only + two negative cells.** Shipped (`0015:159`, `:187`, `0155:402-414` keyed from `client_id` on purpose, `0021:31-33`). Build the cells and **state it on the surface**. No firm-wide identifier unique (C-17). |
| **AC5** source/history/conflict + one versioned correction path | **gap → build** the identity leaf + detail on the `knowledge-detail.tsx:6-18` timeline shape, the revisions relation, the four events. Work-reassessment consumer **out of scope — authority `docs/PRD.md:123`** (deferred to #658/#663; not an owner question). |
| **AC6** full state/a11y/route journey | **partial → build** the walk + mock (none exists). jsdom cells exist (`counterparty-hygiene-a11y.test.tsx:136`, `-keyboard.test.tsx:196`, `:369`). |
| **AC7** production reads under least-privileged roles | **partial → build** `humanQuery`-persona cells for everything new. Say it exactly: the journey invokes **no** Workflow (every door `_human_ctx`-fronted, `0011:1711`, `0174:793`) and, slice (F) struck, changes no read a live Workflow consumes. No World leg. |
| **CB-AE2E-005** | verify-only — run `packages/db/scripts/operation-census.mjs --strict` after apply; record the frontier. |
| **H-09** payer identifier unenterable | build — `setCounterpartyIdentifiers` in `counterparty-doors.ts` + its face on the identity detail. |
| **H-17** | implemented, verify-only; a binding stays non-prerequisite. One cell for the kind-blind writer pre-checks (`0011:1730-1734`). |
| **H-20** `add_client_identifier` no face | **partial-with-named-residual — and the face has an address.** The client's OWN identifiers land in the **same Identity section on `knowledge/page.tsx`**, beside the counterparty link-out (C-41: one section, no control per function). Read `clara.client_identifiers` **directly** under RLS (`0007:793` + `0007:2741-2744`) — wrap nothing (SYNTHESIS §3.1); write through `add_client_identifier` (`0007:1508`, recut `0155:426-459`) in one Dialog (23). Residual: no knowledge record, no key minted this wave (DECISIONS §1.5, owner #654). |
| **H-34** | REDESIGN — **re-measure, never copy**: every count from a read that ran; empty names what was asked; filtered-to-nothing ≠ successful empty. |
| **C-17 / C-22** | verify-only; no allowlist row, no firm-wide identifier unique. |
| **C-41** | build — one identity section (both halves) + routed detail; no control per function. |
| **C-63** | **out of scope — DECISIONS §2 #625 owns the admission surface**; recorded. |
| **Two stale comments** | build — delete `counterparty.ts:26-30`, `:94-105` and `counterparty-hygiene-panel.tsx:10-20`; fix `counterparty-alias-kind-preintegration-gate.mjs:1`'s "unnumbered" header (it is 0176). `identifier-promotion-row.tsx:4-5` is CLEAN — leave it. |
| **C08.3** | partial → build the resolve-pending-ambiguity face against current identity; binding stays optional. |
| **C13.3 / C33.5 / C55.24 / C83.2** | one slice, not four: write-side provenance + a projection carrying it + the revision timeline. Do **not** retire the binding tables; record the convergence as the accepted boundary. |
| **C55.19** | build the never-grants-authority cells (`0021:31-33` says it; no test does). |
| **C77.6** | build — a cell per new dialog asserting typed action identity (Correct vs Cancel) and the persistent outcome. |
| **C79.4 / C79.5** | build provenance + the retirement event; scope stays client+kind; consumer parked (PRD:123). |
| **C83.21** | duplicate of C55.19 — recorded, no separate obligation. |
| **C86.1** | partial → build the human read of the chain; do not re-cite 0149's measured performance numbers. |
| **Activity kind ladder** | **out of scope — DECISIONS §0 D13 / §3.1**: identity and permission events keep falling to `else 'documents'` (`0184:2078`, `:2320`). Named residual; the orchestrator files the issue. |

## 3. Slice (one branch)

**Migration `0200`.** Prestate: re-read every body, index, policy and grant it touches; sha-pin the three recut writers **measured on the rig**; assert `t_counterparty_aliases_update` present-and-enabled (`0176:140-143` shape). Then: (a) alias provenance columns + widened `origin` CHECK (`+'extracted'`, `+'agent_proposed'`) + composite firm-congruent source FKs and the two-way "an extraction pin rides a `document_extraction` and nothing else" CHECKs; backfill inside the disable/re-enable pair (`0176:261-266`), enable re-asserted in the tail. (b) `clara.counterparty_identity_revisions` — forced RLS, the `0149:425-431` owner + human-SELECT policy pair and grant, **zero app-role DML**, no-delete/no-truncate triggers, `(counterparty_id, revision_n)` unique, `act in ('rename','alias_added','alias_retired','identifiers_set','merged')`, before/after payloads, `basis text not null`, `changed_by`, `recorded_via`, the four source pins. (c) the three writer recuts + the `human_ui`-without-`jwt_sub()` trigger. (d) the four event types + taxonomy rows. (e) widen `counterparty_aliases_visible` in place (`create or replace view`, `security_barrier` kept, base grants untouched) to project `client_id`, `kind`, `origin`, `recorded_via`, `created_by`, `source_document_id`; plus three `_human_ctx(clara.role_rank('viewer'))` SECURITY DEFINER functions with `search_path=clara,pg_temp` and `plan_cache_mode=force_custom_plan`: `get_counterparty_identity(p_client,p_counterparty)` → `{current, aliases, identifier_revisions, merges, conflicts}`, `list_counterparty_identity(p_client,p_kind)`, `list_counterparty_merge_corrections(p_client)`. Tail census naming every object and grant.

**Runtime.** Nothing under `packages/runtime` is required. Build `lib/counterparty-identity.ts` (zod + refusal map) **only** as the successor-contract carrier, unit-tested standalone, headed "non-frozen only until the successor imports it". No World e2e leg, no `db-live-gates/action.yml` row — this journey invokes no Workflow.

**Web.** New leaf `counterpartyIdentity` at `/clients/:clientId/knowledge/parties/:counterpartyId` — four minimal `tree.ts` edits (`:112` union member, a `CLIENT_LEAVES` row at the `:386` shape, an href builder at the `:420` shape, a `leafFor` arm at `:578`). New `lib/registers/counterparty-identity.ts` over the three reads; add `setCounterpartyIdentifiers` and repoint `addCounterpartyAlias` to the provenance signature in `counterparty-doors.ts`. **ONE Identity section on `knowledge/page.tsx` carrying BOTH halves** — the client's own identifiers (direct RLS read + the `add_client_identifier` Dialog, H-20) and the counterparty identity list linking out to the routed detail — with no change to `knowledge-panel.tsx`'s four faces. `counterparty-hygiene-panel.tsx`: delete the stale block, retire `NotBuiltNote{aliasListNotBuilt}`, wire `RetireCounterpartyAliasDialog`, link each row to its detail. Expose `recorded_counterparty_id` / `resolution` on `aging.ts:30-49` and `counterparty.ts:218-226`, rendering "recorded as <name>" only when it differs from canonical. Compositions (appendix D): **Item (33)** for identity/alias/revision rows, never a clickable row disguised as static; **Textarea (59)** + Field description/error for the correction reason, draft preserved across validation failure; **Toggle Group (62)** for the vendor/customer filter; **Dialog (23)** for one bounded correction and for Add identifier (Title, initial focus, Escape, focus restoration); **Alert Dialog (3)** only for the destructive merge; **Sheet (51)** for supporting context only, never the durable detail; **Tooltip (63)** never carries provenance, status or errors; **Alert (2)** / persistent `StateBanner` for the outcome, never a Toast alone; Skeleton (53) stops on any terminal face. States: loading, successful empty, filtered-to-nothing ≠ empty, partial/stale, invalid/saving, denied 403 / failed read, cancelled correction + recovery; 320px, 200% zoom, keyboard + focus return, SR names, reduced motion, stable URL/Back, preserved drafts. `messages/en.json`: extend `ClientKnowledge` (`:2527`) and `ArApCounterparty` (`:3815`) — the four counterparty `recordedVia.*` strings under `ArApCounterparty`, any new `sourceKind.*` key under `ClientKnowledge`; no third namespace.

**Docs.** `apps/web/README.md` application map (identity route on the Client row; reads/corrections live in the C13 neighbourhood, create/terms/merge stay acts on registers); `packages/db/tests/README.md` (new battery + gate); `packages/db/README.md` operation-contract census only if a waiver moves. `CONTEXT.md`, house "term / *Avoid*" shape: **Counterparty identity**, **Counterparty alias**, **Identity correction**, **Merge lineage**; amend **Client knowledge** (`CONTEXT.md:118-121`) to name what now carries identity.

**Shared-file one-liners (one line each, sorted position):** `apps/web/lib/navigation/tree.ts` ×4 · `apps/web/messages/en.json` · `apps/web/test/manifest.txt` (one per new test file — unregistered files silently never run) · `apps/web/e2e/serve-built.mjs` (mock hook) · `apps/web/e2e/e2e-fixture-ownership.test.ts` (declare every RPC verb, marking shared ones) · `packages/db/package.json:18` (gate `--import` only) · `packages/db/tests/rig-meta.mjs` (cohort) · `CONTEXT.md`. `apps/web/tests/firm-scope-db-pins.corpus.ts` **only if** 0200 carries dynamic SQL (the recommended shape has none) — then at merge prep, sorted position, ≥40-char reason, sha over final content. No `.github/actions/db-live-gates/action.yml` row.

## 4. TDD seams (red first)

DB battery `packages/db/tests/counterparty-identity.test.mjs`, all assertions through `humanQuery` least-privileged personas (`rootQuery` only for catalog censuses):

1. `p647.provenance.human` — an alias added through the door by a bookkeeper persona stamps `recorded_via='human_ui'`, `created_by = jwt_sub`. **Red:** column absent.
2. `p647.provenance.no_lie` — a direct insert claiming `'human_ui'` with no `jwt_sub()` is refused; the seeding path lands `seeding`/`legacy_unknown`, never `human_ui`. **Red:** no trigger.
3. `p647.provenance.source_pins` — an extraction id on a non-extraction origin refused; a cross-firm document refused by the FK; a **cross-client** document refused **by the door** (the #646 hole). **Red:** no pins; door accepts.
4. `p647.revisions.append_only` — rename, alias add, alias retire, identifier change and merge each append exactly one revision with monotonic `revision_n`; UPDATE/DELETE/TRUNCATE refused; `clara_authenticated` has SELECT and no DML. **Red:** no relation.
5. `p647.identifiers.history` — two `set_counterparty_identifiers` calls leave two revisions with before/after values, the door's `_finish_op` payload unchanged (`0174:850-852`). **Red:** in-place overwrite only.
6. `p647.reads.shape` — the three reads return the documented envelope for a **viewer**; zero rows cross-firm; `CLR11` on a foreign client; all three `prokind='f'` with `search_path`/`plan_cache_mode` set and **no new view** in the debt-BAR1 family. **Red:** functions absent.
7. `p647.merge.representable` — a carrier-backed merge reports `true`; a bare `merged_into` with no carrier reports `false` + reason. **Red:** read absent.
8. `p647.events.taxonomy` — four types, client-scoped, `context_update` at the ACTIVE version, catalog anti-join empty. **Red:** types absent.
9. `p647.ac4.same_tin` — two clients of one firm, same TIN, both admitted, no auto-link, `_resolve_counterparty` still refuses cross-client; `p647.ac4.no_netting` — an identity fact creates no allocation and grants no authority. **Red:** no such cell today.
10. `p647.overload.one` — `proname='add_counterparty_alias'` resolves to exactly one `regprocedure` after apply.

Web unit (each registered in `apps/web/test/manifest.txt`): 11. `counterparty-identity-panel.test.tsx` — conflict face, inaccessible-source face (through the verbatim `KnowledgeSourceBlock`), filtered-to-nothing ≠ empty, verbatim CLR refusal in a persistent banner, and the four `recorded_via` values each rendering a real `ArApCounterparty.recordedVia.*` string (no missing key, no cast). 12. `client-identity-section.test.tsx` — **H-20's face**: the client's own identifiers listed from a read that ran, the Add-identifier Dialog posting `add_client_identifier`, a duplicate `(kind, value)` rendering the `uq_client_identifiers_client_kind_value` refusal verbatim in the persistent banner. **Red:** no such control today (`add_client_identifier` has no web caller — grep-bounded, gap-647 H-20). 13. `counterparty-identity-correct.test.tsx` — C77.6: Correct vs Cancel at equal visual weight, asserting **which typed action fired** and the outcome it left, draft preserved across a validation failure. 14. `tree.test.ts` — the new leaf resolves; the breadcrumb names it. 15. Update `counterparty-hygiene-*.test.tsx`, `aging.test.ts`, `counterparty.test.ts`.

16. Playwright `e2e/counterparty-identity-walk.spec.ts` + `counterparty-identity-mock.mjs` on the `knowledge-walk.spec.ts` shape — every state above, exact date values, 320px, 200% zoom, keyboard + focus return, SR names, reduced motion, **stable URL and Back**, preserved draft, axe on every face.

## 5. Risks

1. **`clara.counterparty_aliases` is the first member of `wave-a-shape.test.mjs`'s fn-fronted-only family** (`0145:928-940`) — zero direct DML/SELECT to any app role. `counterparty_identity_revisions` copies `counterparty_merges`' policy pair (`0149:425-431`), never `counterparties`'. The first cut of that ruling got this wrong and had to be redone as a masked view.
2. **The backfill needs the trigger disabled** (`0011:973`, CLR08) — use `0176:261-266` and **assert** the re-enable in the tail rather than trusting it.
3. **Pins are MEASURED, never transcribed** (SYNTHESIS §7.7). Measure on the rig: `clara.add_counterparty_alias(uuid,uuid,text,text,text)`, `clara.rename_counterparty(uuid,uuid,text,text)`, `clara.set_counterparty_identifiers(uuid,uuid,text,text,text)`, plus the live `pg_get_viewdef` of `counterparty_aliases_visible`. Re-measure `pg_policy` and `information_schema.role_table_grants` on `counterparty_aliases`, `counterparty_aliases_visible` and `counterparty_merges` before writing the prestate — gap-647's grant/policy claims rest on migration text plus a **frontier-0140** live finding, the very staleness this ticket fixes.
4. **Merge risks.** `tree.ts` (also #650, #653), `en.json`, `manifest.txt`, `serve-built.mjs` + `e2e-fixture-ownership.test.ts`, `rig-meta.mjs`, `packages/db/package.json:18`, `CONTEXT.md`: one line, sorted position, logic in your own modules. `lib/registers/knowledge.ts` and `knowledge-shared.tsx` are **#654's C13 surface — read them, do not edit them** (the reuse boundary above is why). `lib/registers/counterparty.ts` is shared with #653 (do not let it re-declare `CounterpartyStatementRow`) and `aging.ts` with #638 — add fields, rename nothing. `identifier-promotion-row.tsx` / `needs-you-gaps.tsx` are #650's; add **no** `needs-you.ts` row kind (DECISIONS §1.6).
5. **Census suites red on files you never touched** — `firm-scope-surfaces`, `firm-scope-fourth-entrance`, `parity-holes`, `sql-oracle`, `e2e-fixture-ownership` react to a new `app/**` file, a changed href, a new SQL function or a new mock verb.
6. **Reviewer expectation** (`docs/plan/active/refresh-wave-2026-09-14/reports/640-review-closure.md`): cells asserting `prosrc` were flagged, every claim was re-measured, the concurrent cases were raced and the tail was re-read against the live catalog. Write **behavioural** cells and race the concurrent correction.
7. **No hosted evidence exists.** Label local / hosted / provider separately; "hosted evidence pending".

## 6. Effort and rig

**Effort: XL.** Rig: worktree `C:\Users\zhant\Desktop\clara-wt\647`, branch `impl/647-counterparty-identity`, PG `127.0.0.1:55506` / `clara_647` (RIG.md:23); Playwright triple **3280 / 3281 / 3282**. Before the final report, from the worktree: the new DB battery with the exact 28 gate flags from `packages/db/package.json:18`; neighbours `counterparty-merge-pr-1`, `counterparty-alias-kind`, `wave-a-aliases`, `wave-a2-counterparty-kind`, `s6-counterparty`, `p4t2-counterparty-aliases`, `debt-human-read-surfaces`, `knowledge-records`, `knowledge-onboarding-promotion`; `operation-census.test.mjs` + `rig-isolation.test.mjs` **without** the reset flags (the slice adds SQL functions); the **whole** `apps/web` unit suite once (`node scripts/run-tests.mjs`, ~5 min) with its counts; `pnpm typecheck` and `pnpm lint` green; `node scripts/check-frozen-workflows.mjs` and `node packages/runtime/scripts/check-parts-parity.mjs` to prove nothing frozen moved. Ignore the known Windows reds #707 and #693.

## Verifier findings not applied

None — both findings applied; neither contradicts DECISIONS.md.
