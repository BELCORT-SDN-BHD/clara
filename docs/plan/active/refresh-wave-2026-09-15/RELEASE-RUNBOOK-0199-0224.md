# Hosted release ceremony — 0199…0224 (riders batch PR #838 + wave 2026-09-15) + chatTurn_v20 / claraWork_v4 / clientOnboarding_v5 image + web

**DRAFT — not owner-authorised, not ready to run.** Written before the wave's integration branch
(`integration/wave-2026-09-15`, worktree `C:\Users\zhant\Desktop\clara-wt\int`) has a PR, before CI
has run on it, and before `origin/main` moves again. At the time of writing, `origin/main`
(`7e40e3be`) already carries PR #838 (the riders batch, migrations 0199–0213); the wave's own eleven
migrations were renumbered 0214–0224 in the worktree at commit `ac6626e8`, and a follow-up commit
`7a495135` ("re-issue the four prestate/tail pins the riders batch moved") is the worktree's HEAD as
of this draft. DECISIONS.md §3.4 still requires, before this can be a real ceremony: `origin/main`
merged (not rebased) into the integration branch (done), the manifest `frozen-workflows.json`
regenerated by `check-frozen-workflows.mjs --update` **on top of main's file** (unverified whether
this has run since the pin re-issue commit), the three World-e2e verification legs re-run on
separate clusters, a PR opened against `main`, and `ci` green. **Do not run any step below until
all of that is true and the owner has authorised the window in-session**, per this repo's own
`AGENTS.md` protocol.

RELEASE_SHA = `<SHA>` — the commit that lands on `main` when the wave's PR merges. Unknown until
that PR exists; every command below is written against the placeholder. Label `refresh-<sha8>`.

**Migration count — corrected from the brief's placeholder.** `git ls-tree -r --name-only
origin/main -- packages/db/migrations/ | wc -l` = **208** files today (max version `0213`; five
pre-existing gaps at 0032, 0073–0076, present since before this wave and reproduced identically on
`origin/main`). The wave's worktree HEAD carries **219** files, max version `0224`
(`packages/db/migrations/0214_client_work_pack.sql` … `0224_preview_invite.sql`, eleven files,
contiguous, confirmed by `git ls-tree -r --name-only HEAD` in `clara-wt/int`). Hosted is at **193 /
0198** (unchanged since the two 2026-09-14 ceremonies — `git show origin/main:docs/PROGRESS.md`
"Current State"). So the migrate step this release runs should report **`26 new migration(s)
applied · 219 total`**, not "31 new … 224 total": `219 total` is the file count (`migrate.mjs`'s own
counter, confirmed against every prior runbook's usage — e.g. "193 total" at frontier `0198` with
five of the same gaps already present), and `224` is only the highest version *number*, which is
not what the tool prints as "total" because of those five gaps. Re-verify this arithmetic against
whatever `origin/main` and the wave branch actually are at window time — both have moved during
this draft's own research.

Rollback points BEFORE (from the ledger, current hosted state, unchanged since 2026-09-14): DB
**193 / 0198**. Runtime **v84** = `registry.fly.io/clara-runtime@sha256:b6dd2fa56525e04aa6fab2c27235ca419aa40f32dfb61af518b53ed42970b79e`
(tag `refresh-3486b6c2`), single Fly machine `48ee715b763048`. Web
**`0290977b-74a4-4c4a-849f-ee60efd631bb`** (built at `70c731ef`).

Secrets rule (unchanged from both prior runbooks): the fly token and the DSN are substituted
INLINE inside one pipeline only — never assigned to a shell variable, never echoed, never in argv.
Every `flyctl` below is shorthand for `FLY_API_TOKEN="$(grep -E '^access_token:'
~/.fly/config.yml | awk '{print $2}')" flyctl <cmd>`; every `<dsn pipe>` is `flyctl ssh console -a
clara-runtime --machine <id> -q -C "sh -c 'printf %s \"\$WORKFLOW_POSTGRES_URL\"'" </dev/null
2>/dev/null | tr -d '\r\n' | node scripts/ops/dsn-pipe.mjs -- <cmd>`. From Git Bash, a `wsl`
argument that is a `/mnt/c` path needs `MSYS_NO_PATHCONV=1`. `fly ssh console` has exited 1 with
"The handle is invalid." after streaming, on this Windows host — verify any streamed artefact by
sha256, do not trust the exit code alone.

---

## 0. Rehearsal on a pristine-0198 chain, then the full 26-file replay — before production

`rigmain` (`127.0.0.1:55620` / `clara_main`) is **not** usable for this rehearsal as-is: DECISIONS
§3.4 says it already carries `0001…0213`, i.e. past the hosted starting point. Two options, in
order of preference:

- **(a) Split-chain replay with `CLARA_MIGRATIONS_DIR`.** `packages/db/README.md` ("Migration and
  deployment behavior") documents this env var for exactly a split test rig. Build a brand-new
  cluster on an unused port (not 55600/55601/55620, all live wave infrastructure); point
  `CLARA_MIGRATIONS_DIR` at a directory holding only `0001_smoke.sql` … `0198_chat_clarify_expiry.sql`
  (a copy of `origin/main`'s tree truncated at 0198, matching hosted's real starting frontier) and
  run `pnpm db:migrate` — expect the same shape hosted would show if replayed from empty:
  193 files applied (five gaps, as above). Then re-point `CLARA_MIGRATIONS_DIR` at the wave
  worktree's full `packages/db/migrations/` (219 files, 0001…0224) and run `pnpm db:migrate` again
  from that same cluster — this second run is the one whose timing and tail notices are the
  rehearsal of record: expect **`26 new migration(s) applied · 219 total`**, every one of the
  eleven wave prestates and the fifteen riders prestates printing `clean`/`OK`, and record the wall
  time as **T**. `pnpm db:seed` before the second run if any prestate depends on seed data (0201's
  duplicate-region prestate and 0209's legacy-reader census are the two most likely to care about
  real rows; an empty rehearsal cluster proves the DDL and the guard logic but not row-shaped
  hazards hosted's real data could hit — flag this gap explicitly in the rehearsal report rather
  than treating a green empty-cluster run as equivalent to hosted).
- **(b) A worktree checked out at `origin/main` pre-#838** (`4464e471`) migrated to 0198, then the
  wave worktree's migrations directory copied in for the second run — same effect as (a), heavier
  to set up, useful only if `CLARA_MIGRATIONS_DIR` turns out not to behave as documented.

Either way, this rehearsal has never been run — there is no rig or timing number to report yet.
**Unverified**, by design of this being a draft.

Preflight read-relations probe shape (unchanged): `lib/rollback-preflight.mjs`'s own relation list.

## 0a. Gates before any production step

- Wave PR exists, targets `main`, `ci` SUCCESS, `mergeStateStatus` CLEAN, fast-forward merge.
  RELEASE_SHA = the sha that actually lands on `main` — never a literal written before the merge.
- DECISIONS §3.4's re-verification is done: manifest regenerated on top of `main`'s
  `frozen-workflows.json` (not hand-merged), the four re-issued sha pins (`7a495135`) match the
  moved riders bodies named in §3.4 (`cancel_accounting_work`; `_tf_accounting_work_immutable`,
  `answer_work_question`; `list_accounting_work`, `get_accounting_work_row`;
  `_record_journal_entry_core`; `_bank_registry_ledger_state`, `_close_gate_closing_stock`,
  `get_context_pack`; the two `_work_trace_*_ok`; `_adjustment_basis_canonical`,
  `_assert_adjustment_basis`, `_assert_adjustment_relationships`; `_tf_source_binding_wall`), and
  the three World-e2e legs (`version-cutover-e2e`, `two-build-cutover-e2e`,
  `chat-turn-v20-e2e`) are green on `rigmain`'s own chain, not only on the pre-rebase `rigint2`.
  **Unverified as of this draft** — the worktree's HEAD is the pin re-issue commit; nothing after
  it has been checked here.
- **Owner confirmation, in-session, for this specific window** — none recorded yet. The prior two
  ceremonies each cite a literal owner sentence authorising that session; this draft has none to
  cite, and none should be assumed.
- Build tree = RELEASE_SHA, `git status --porcelain` empty.
- **A hard STOP condition unique to this release: zero non-terminal `chatTurn_v1` runs.** PR #838
  (#810, owner ruling) does not merely supersede `chatTurn_v1` the way every prior version bump
  has — it DELETES it. `frozen-workflows.json`'s new `"retired"` section on `origin/main` pins
  `packages/runtime/workflows/chatTurn.impl.ts` and `chatTurn.prompt.ts` (plus a third file) by
  sha with `"ruling": "#810 owner ruling 2026-09-15"`, and `registry.ts` on `origin/main` no longer
  imports, exports or lists `chatTurn_v1`/`chatTurn` in `workflowBodies` at all (`grep chatTurn_v1
  registry.ts` → zero hits). Every prior ceremony's superseded body stayed in `workflowBodies`
  precisely so the stranded-body boot guard would never refuse it; this is the first body this
  estate has ever actually removed. A still-open run of the un-versioned `chatTurn` body would be
  **permanently unservable** by any image built from this SHA or later — not "stranded until
  rolled forward" but stranded with no forward fix, because the code that could run it is gone.
  `docs/PROGRESS.md`'s own "Known Issues" already names this ("re-confirm zero non-terminal
  chatTurn_v1 runs first"). **Before step 3f, before the migrate step, and again right before step
  7's deploy**, read: `select count(*), array_agg(id) from workflow.workflow_runs where name =
  'chatTurn' and status not in (<terminal set>)` (confirm the exact stored `name` value against the
  live catalog first — `chatTurn` pre-versioning vs some other literal — this is unverified from
  static reading alone). Any non-zero count is a STOP: do not deploy the new image; escalate to the
  owner, because the only fix is either draining that run to a terminal state first or reversing
  #810's retirement, both product decisions.
- **DB RESTORE POINT** — not yet taken (no window scheduled). Shape unchanged from both prior
  runbooks: `backup.mjs --profile full` through the probe's DSN into WSL, WSLENV pipeline, ~90–160s
  observed historically for a full dump; if the window opens more than ~2h after the stamp,
  re-take it. A restore does not return Storage bytes, managed Auth config or engine state
  (`packages/db/README.md`).
- **Web rollback lever** (one command, unchanged mechanism): `pnpm --dir apps/web exec wrangler
  versions list` → current active version `0290977b-74a4-4c4a-849f-ee60efd631bb` (100% since
  2026-09-14) → one-command rollback `pnpm --dir apps/web exec wrangler versions deploy
  0290977b-74a4-4c4a-849f-ee60efd631bb@100% --yes`. No DB implication.

## 1. fly auth — inline only

`FLY_API_TOKEN="$(grep -E '^access_token:' ~/.fly/config.yml | awk '{print $2}')" flyctl <cmd>` on
every call. Authenticated identity: `tools@belcort.com` (per operator memory, unverified freshly in
this session — re-run `flyctl auth whoami` at window start).

## 2. Probe machine (OLD image, world off) — destroyed before step 7's deploy

`… flyctl machine run registry.fly.io/clara-runtime:refresh-3486b6c2 --app clara-runtime --name
probe-<sha8> --region sin --vm-memory 512 --env CLARA_START_WORLD=0 --env PORT=3200 --command
"sleep infinity"` → `<probe-id>`.

## 3. Read-only reads over the probe DSN

3a. **Ledger.** `select count(*), max(version) from clara.schema_migrations` → expect **193 /
`0198_chat_clarify_expiry`**. Then re-read the `0198` checksum and compare against the file on disk
(the same pattern every prior ceremony used for its own frontier row) — STOP on any mismatch,
`migrate.mjs` would abort the whole run on checksum drift regardless.

3b. **Quiescence census**, re-read after the stop (3f/6a): `agent_tasks` running/stopping,
`agent_interruptions` pending, `accounting_work` non-terminal, `workflow.workflow_runs`
non-terminal (grouped by `name`, so the `chatTurn` census above and any parked `claraWork_v3` /
`chatTurn_v19` / `clientOnboarding_v4` runs are all visible in one read), unbound/held
`agent_tasks` cross-checked with `wake_engine_sources` (the ten orphan held `wake` tasks from
#820 are known pre-existing noise, unrelated to this release — confirm the count is still ten and
not something new before treating it as noise again).

3c. **The chatTurn_v1 hard-stop census** (3f above) — run here, and its own re-read after 6a.

3d. **Live pre-images of every body either batch recuts**, captured for the rollback record.
Riders batch (0199–0213), confirmed by direct reading of the migrated files on `origin/main`:
`cancel_accounting_work` (0199), `_tf_accounting_work_immutable` + `answer_work_question` (0200),
`persist_document_extraction` + `persist_invoice_facts` (0201), `list_activity` — third recut
(0202), `list_accounting_work` + `get_accounting_work_row` (0203), `_record_journal_entry_core` —
the posting core, fifth copy (0204), `resolve_stripe_event_problem` (0205), `get_context_pack` +
`_close_gate_closing_stock` + `_bank_registry_ledger_state` (0209), `_work_trace_revisions_ok` +
`_work_trace_text_ok` (0210), `_adjustment_basis_canonical` + `_assert_adjustment_basis` +
`_assert_adjustment_relationships` (0212), `_tf_source_binding_wall` (0213 — recut, but the file's
own header says no writer-quiescence window is owed for it; see §W below). Wave batch (0214–0224,
confirmed by reading the worktree's files): `add_counterparty_alias` (dropped 5-arg / created
10-arg — the wave's one destructive-then-recreate signature change), `rename_counterparty`,
`set_counterparty_identifiers` (0215); `_fa_asset_json`, `get_fixed_asset` (0216, reads only);
`set_document_kind` (0217 — the file's own header says "D1 WRITE-QUIESCE IS OWED"); five plan-family
bodies — `create_accounting_plan`, `_assert_plan_schedule`, `_plan_occurrence_basis`,
`_plan_admit_occurrence`, `preview_accounting_plan` (0223). `pg_get_functiondef` of each, to
scratchpad `pre-images/`.

3e. **§W — the writer-quiescence set, stated plainly.** `packages/db/README.md` ("Migration and
deployment behavior"): *"Before deploying a change to an active writer body, stop new writes and
drain in-flight calls, apply the migration, then resume."* That is a general rule, not a per-file
opt-in, and it is why every migration above that recuts a live body — whether or not its own header
uses the words "consumer-first obligation" (a narrower, separate concept about DB-vs-app deploy
*order*, not about the drain-and-resume window) — is treated as needing the runtime stopped. Two
migrations explicitly and individually rule the window is NOT owed: **0213**
("`CONSUMER ORDER. None owed and no writer-quiescence window: the only body replaced is a trigger`"
— it only narrows an existing refusal) and **0224** ("owes no writer-quiescence window and cannot
join the lock order… takes no row lock of any kind"). Several more recut nothing at all and say so
in their own headers or in `packages/db/README.md`'s worked-example paragraphs: **0206** ("this
file adds a door, it recuts none"), **0208**, **0211** ("no body was recut in passing"), **0214**,
**0218** ("RECUTS NOTHING… no `alter table` on a [live] table"), **0219**, **0222** ("owes no
consumer-first obligation… recuts nothing"). Despite recutting no *body*, three more migrations add
brand-new triggers on tables that are live and actively written by the running v84 image —
`clara.journal_entries` (0216's `t_je_fa_acquisition_birth`, 0221's
`t_je_adv_claim_application_birth` — both `create constraint trigger`, and DECISIONS §1.3 confirms
0221 recuts no shared body, it only adds this trigger), `clara.knowledge_records` and
`clara.document_filings` (0220's three new BEFORE triggers), and `clara.document_capabilities`
(0207's registry-version monotonicity trigger). `CREATE TRIGGER` takes a lock class that conflicts
with the `ROW EXCLUSIVE` an `INSERT`/`UPDATE` holds, so these are operationally the same hazard as a
body recut even though DECISIONS' own "shared-body recuts" list (§1.3) does not name them (that
list is about which *pre-existing* bodies a wave ticket may touch, not about new DDL on live
tables). **Net recommendation, matching both prior ceremonies' own practice of stopping for the
whole migrate window rather than trying to split it per file:** stop the runtime for the entire
26-file migrate step. At minimum ten of the fifteen riders files and four of the eleven wave files
touch a live body or a live table's trigger set; splitting the window buys nothing and adds risk.

3f. Keep the probe alive through step 6 (the live machine is stopped during the migration); destroy
it at the end of step 6, before step 7's deploy.

## 4. Runtime image first (build-only + push, by digest)

`… flyctl deploy --config packages/runtime/fly.toml --build-only --push --image-label
refresh-<sha8> --build-arg CLARA_BUILD_SHA=<SHA>` (~5 min) → record the `sha256:` digest; release
by `registry.fly.io/clara-runtime@sha256:<digest>` in step 7. Expect the built manifest to report,
via `check-workflow-bundle.mjs` at build time, a bundle roster that has GAINED
`chatTurn_v20`/`claraWork_v4`/`clientOnboarding_v5` and LOST `chatTurn`/`chatTurn_v1` entirely (not
merely superseded) — `successors-final.md` §5 measured **54 "use workflow" module(s)** and **299
frozen file(s)** at the wave's own pre-rebase commit (up from 51/281); re-measure on the rebased
tree, since PR #838 also changed this manifest independently (chatTurn_v1's retirement) and the two
deltas have not yet been combined and verified together.

## 5. Web build + upload (WSL as root)

Unchanged mechanism from `RELEASE-RUNBOOK.md` step 5. **Unlike the second 2026-09-14 ceremony
(0196–0198), this release DOES ship a new web build**: both batches carry `apps/web` changes that
assume their own migrations are live — riders: `intent_key`, `applicant_name`, `p_work`,
`settledCents`, the restate and re-activate actions (`docs/PROGRESS.md` Known Issues, verbatim);
wave: twelve tickets' worth of UI (client home-work facets, counterparty identity detail, asset
registers, document correction bands, firm setup, client onboarding, knowledge settings, staff
expense claims, accruals, prepayments, member lifecycle, preview-invite). Build and upload from
RELEASE_SHA exactly as before; tag `refresh-<sha8>`.

## 6. Writer quiescence → migrate (all 26 files, one run)

6a. `flyctl machine stop 48ee715b763048` → confirm `stopped`. Chat and Clara return 502
`runtime_unreachable` during the window; the rest of the app works.

6b. Re-run 3b (quiescence) and 3c (the chatTurn hard-stop census) through the probe. Also read
`pg_locks`/`pg_stat_activity` for any `clara_authenticated` writer holding
`clara.journal_entries`, `clara.counterparty_aliases`, `clara.accounting_plans`,
`clara.knowledge_records` or `clara.document_filings` — the five tables §W's trigger/recut set
touches — because unlike the 0196–0198 ceremony (which explicitly left the web app up and accepted
a possible 5s lock-timeout retry), this release should treat all five as needing the same
stop-the-world discipline the runtime stop already buys for the `journal_entries` triggers; the web
app talking directly to PostgREST can still hold a lock on the other four while the runtime is
down, so re-check `pg_locks` specifically for those, not only for runtime sessions.

6c. `(cd packages/db && <probe dsn pipe> | node ../../scripts/ops/dsn-pipe.mjs -- node
scripts/migrate.mjs)` from the RELEASE_SHA checkout → expect **`26 new migration(s) applied · 219
total`** (see the corrected arithmetic above), with each of the 15 riders and 11 wave prestate/tail
notices printing clean/OK by name. Expected wall time ≈ T from step 0 + pooler latency (T is
unmeasured — step 0 has not been run).

6d. **FAILURE BRANCH — decide from the ledger**, per this repo's own established pattern (each file
commits alone; `migrate.mjs` stops at the first failure): re-read `select count(*), max(version)
from clara.schema_migrations`. (i) max < `0201` (persist recuts) → starting v84 is legal, report
and stop. (ii) `0201`–`0203` committed but `0204` did not → v84 is still a legal boot target (it
predates every new pin requirement; the one `FRONTIER_BODY_RULES` entry only requires
`claraWork_v3`, which v84 carries) but the web build already assumes `intent_key`/`p_work` exist —
do not promote web past this point. (iii) `0204`–`0213` committed, `0214`+ did not → same: v84
boots, riders' web features would work, wave's would not; hold web promotion at the OLD version.
(iv) full 219 committed → drive forward to steps 7–8. A `55P03 lock_not_available` on any of the
`CREATE TRIGGER` or `ALTER TABLE ADD CONSTRAINT` statements (0207, 0216, 0220, 0221, 0223) times out
at that file's own `lock_timeout` (5s or 15s where set; unset on 0216/0221 — see §W, this is a real
gap: those two files have no local `lock_timeout` override, so a stuck lock there could wait far
longer than 5s before the runner's own default takes over, and that default is unverified from
static reading — confirm `packages/db/scripts/migrate.mjs`'s connection setup before the window,
not during it) and rolls that one file back cleanly; re-run step 6c after clearing the blocker.

## 7. Release the runtime (by digest) and start

Before this step, re-run the chatTurn hard-stop census one more time (3c). `flyctl machine list -a
clara-runtime` (one machine) → `flyctl deploy --config packages/runtime/fly.toml --image
registry.fly.io/clara-runtime@sha256:<digest>` → `flyctl machine start 48ee715b763048` → `flyctl
machine list` again (one machine, new image) → wait `/ready` 200 → `flyctl logs`. **Expected boot
line, unverified in exact numeric form until the rebase's frozen-manifest regeneration is measured
end to end:** `serving git_sha=<SHA> frontier=0224_preview_invite(219) bodies=54 pins …
chatTurn=chatTurn_v20 claraWork=claraWork_v4 clientOnboarding=clientOnboarding_v5`, **four** bundle
banners for claraWork (v1→v2→v3→v4 — `successors-final.md` §2: `plugins/startWorld.ts` prints a
fourth `CLARA_WORK_BUNDLE_V4_BANNER`; `buildInfoRoutes.ts` serves `[…V4(), …V3(), …V2(), …()]`,
pinned first), `stranded bodies n=0` BEFORE `durable world started` (this is the number the
chatTurn hard-stop census exists to keep at zero — a non-zero count here, after everything above,
means the pre-check missed a run, and it is now unrecoverable by rolling forward, only by fixing
the run's status by hand or reversing the retirement), `CONTROL listening`, `LEADER acquired`.
Signed-in `GET /api/build-info`: `git_sha` = `<SHA>`, `frontier` `0224(219)`, `bodies: 54`, three
pins as above.

**The two-build cutover note.** `successors-final.md` §7.2 (measured, pre-rebase):
`two-build-cutover-e2e.mjs`'s v3→v4 pair passes with the required-bodies check now derived from
`FRONTIER_BODY_RULES` itself rather than hand-written, and the SAME single rule
(`0195` requires `claraWork_v3`) is unchanged and still satisfied by v84 — v84 is not made illegal
by this release at the frontier-rule layer. #794 (riders batch) separately adds a registry-derived
`chatTurn` leg to the same drill. **Neither drill has been re-run since the rebase**
(DECISIONS §3.4's own obligation) — treat both as unverified until that happens.

## 8. Promote web + smoke

`pnpm --dir apps/web exec wrangler versions view <id>` → `versions deploy <id>@100% --yes`. Smoke
signed out: the same roster as `RELEASE-RUNBOOK.md` step 8 (`/login`, `/favicon.ico`, `/icon.png`
200; `/pending`, `/api/build-info` 307→login; cross-origin POST 403; `/checkout/cancel` 307;
`/settings/registrations`, `/admin/registrations` 307→`/operator`). Signed in, add per-batch
checks: riders — a Work's Activity tab loads with the `p_work` filter present, an operator-support
applicant shows a name rather than a raw uuid; wave — `/clients/<id>` home shows the new work-pack
tiles (#650), `/clients/<id>/accruals` and `/clients/<id>/prepayments` load (#652/#653),
`/settings/knowledge` and `/settings/setup` sections exist (#654/#648).

## 9. Rollback-preflight demonstration (READ ONLY — do NOT roll back)

**Two separate gates, not one — state which is being demonstrated.** (a) The `FRONTIER_BODY_RULES`
check: v84 carries `claraWork_v3`, the only body the one rule (pinned at `0195`) requires, so v84
**passes** this gate at the new frontier — this is a *positive* control, not a refusal, and it
should be recorded as such rather than assumed to fail the way the 0195 ceremony's v82 demonstration
did. (b) The stranded-body census: v84's manifest does not carry `chatTurn_v20`, `claraWork_v4`,
`clientOnboarding_v5`, or (going the other direction) the now-deleted `chatTurn_v1`. v84 is a
**legal** rollback target only until the first non-terminal run of any of the three new bodies
exists; after that, rolling back to v84 would strand it, and that is a *different* refusal from
(a). Run `node packages/runtime/scripts/rollback-preflight.mjs --target-build-info <v84's
/api/build-info>` (or `--target-bundle` against an extracted v84 index.mjs, same pattern as prior
ceremonies) through the LIVE machine's DSN immediately after step 7, before any real traffic could
have created a new-body run, and expect **ALLOWED** on gate (a) with **zero** stranded-body hits on
gate (b) at that instant — then state explicitly that gate (b)'s clean result is a snapshot, not a
standing guarantee: it degrades the moment a `claraWork_v4` (etc.) run is created, which is soon
after the image starts serving. Rolling back the DATABASE below `0217` (`set_document_kind`'s
recut) or below `0223` (the plan-family recut) has no drafted successor migration in this wave —
unlike the 0195 ceremony's `restore_pre_0195_bodies.DRAFT.sql`, nothing here has been drafted for a
below-frontier database rollback. **If a below-frontier DB rollback is ever needed, it must be
drafted before the window, not during an incident.**

---

## Tickets this release closes

**Riders batch, PR #838 — 17 `awaiting-release`.** Confirmed via `gh issue list --label
awaiting-release --state open` (16 rows) plus `gh issue view 787` (17th; **CLOSED already but still
carries the label** — flag this to the owner rather than silently including or excluding it, since
it is unclear whether its hosted evidence was already posted before closing or the label was simply
never stripped):

| # | Title | Migration / area |
|---|---|---|
| #750 | A cancelled Work leaves no Activity row | 0199 |
| #721 | A Work question's answer cannot complete an incomplete basis | 0200 |
| #778 | No unique index on document_regions(extraction_id, field_path) | 0201 |
| #770 | Add a p_work door filter for list_activity | 0202 |
| #809 | Converge the two readers of clara.accounting_work | 0203 |
| #787 | A human reversing an accrual's entry … creates a phantom reversal | 0204 (**already CLOSED — verify label**) |
| #775 | resolve_stripe_event_problem writes no operator audit row | 0205 |
| #776 | Operator support console shows the applicant only as a raw uuid | 0206 |
| #779 | document_capabilities.registry_version monotonicity is convention only | 0207 |
| #780 | 0191's tail pins document_fact_validations RLS by policy count, not row | 0208 |
| #784 | Four legacy readers still read client_facts directly | 0209 |
| #811 | Bound observed_revisions numeric values and the run id grammar by shape | 0210 |
| #812 | Add a restore door for deactivate_client_egress_purpose | 0211 |
| #797 | Store the payroll settlement split (settled_cents) | 0212 |
| #821 | Opening-balance lane can still approve an item over a live evidence link | 0213 |
| #810 | Retire chatTurn_v1's registry export | runtime / manifest |
| #764 | chat-lane clarify: hook_missing resting state (stays open — Half 2 of #720; only its own number, if any, gets a comment, it does not close) |

**Wave 2026-09-15 — 12 tickets**, all currently OPEN, migrations 0214–0224 (per DECISIONS §1.2):

| # | Title | Migration |
|---|---|---|
| #650 | 在 A 风格客户首页直接看见需要你、处理中和近期完成 | 0214 |
| #647 | 让 Clara 维护交易对方、别名与身份更正历史 | 0215 |
| #639 | 完成资产购入与登记，独立等待缺失折旧资料 | 0216 |
| #646 | 纠正文件事实、类型及归属，并处理后续影响 | 0217 |
| #648 | 从真实缺失资料继续事务所设置 | 0218 |
| #649 | 建立客户并从当前资料继续会计入门 | 0219 |
| #654 | 明确推广事务所默认规则并保留客户例外 | 0220 |
| #638 | 完整处理员工报销、垫款与应付明细 | 0221 |
| #652 | 建立并执行有依据的应计与转回调整 | 0222 |
| #653 | 按明确服务期间处理预付款与摊销 | 0223 |
| #625 | 完成邀请、加入、改权及移除成员的完整流程 | 0224 |
| #633 | 上传或附加文件后自动受理并开始处理 | none (no migration) |

**Comment to leave on each** (the shape both prior ceremonies used — "Hosted release evidence —
`<migration>` (`<date>`, release session)" ending "Closing per the awaiting-release rule: local and
CI evidence in the lane comment above, hosted evidence here."), adapted per ticket: the migration's
own `applied_at` from the ledger, the prestate/tail notice text for that ticket's file, and — for
tickets whose AC specifically named a hosted behaviour (e.g. #810's chatTurn_v1 retirement, #764's
sweep count) — the specific reading step 6/7/8 produced. **#653 and #647 need one more sentence
each**, because their tickets' own chat-tool contracts were stopped at a grant wall in the successor
cut (`successors-final.md` §3: `start_prepayment_schedule_work` / `read_prepayment_source` for
#653, `record_counterparty_alias` for #647) — the hosted evidence comment should say the DB/web
halves are released and the chat half remains an open contract, not imply the ticket's chat AC
shipped.

---

## What this draft could not verify

- The wave's PR does not exist; RELEASE_SHA, the build tree diff, and CI status are all unknown.
- DECISIONS §3.4's manifest regeneration and the three re-run World-e2e legs, on `rigmain`'s own
  chain rather than the pre-rebase `rigint2` — not measured here.
- The exact stored `workflow.workflow_runs.name` value for the retired `chatTurn` body (assumed
  `'chatTurn'` from the un-versioned filenames; not read off a live catalog).
- `packages/db/scripts/migrate.mjs`'s default `lock_timeout`/`statement_timeout` when a migration
  file (0216, 0221) sets neither locally — relevant to the failure-branch timing in step 6d.
- Rehearsal timing (T) — step 0 has not been run.
- Whether Supabase PITR is enabled (asked and left unanswered in the 2026-09-14 runbook; unverified
  again here).
- Everything hosted about the wave's own contents: `integration-merge.md` and `successors-final.md`
  state explicitly that no hosted run exists anywhere in this wave.
