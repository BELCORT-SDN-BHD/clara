# Brief: #653 — prepayment recognition and amortisation over an explicit service period

## Orchestrator decisions (binding)

- **Migration `0208_prepayment_amortisation.sql`** — yours alone (DECISIONS §1.2). One file, house header, prestate + tail + a preintegration gate module + `rig-meta.mjs` cohort rows. A released number is a gap, never renumbered.
- **All twelve branches run in parallel (orchestrator ruling, DECISIONS §1.3).** This overrides SYNTHESIS §4's "Group 3 — #638 → #652 → #653 strictly serialised" and gap-653's Overlaps/Risk 9/Q6 "coordinate the `kind` CHECK with #652 first". D12 rules #652 onto `kind='reversing_journal'`, so **you are the sole widener of `accounting_plans.kind`** (`0193:413`). Do not wait on #652, do not write your prestate against another branch's output: **measure the live CHECK text on your own rig**.
- **The only shared bodies you may recut** are the 0193 plan family: `create_accounting_plan(uuid,text,text,text,jsonb,text,text,int,text,date,date,jsonb,text,text)` (`0193:1438`), `_assert_plan_schedule(text,text,text,int,text,date,date,text)` (`:1572`), `_plan_occurrence_basis(jsonb,date,text,uuid)` (`:1037`), `_plan_admit_occurrence(uuid,date,text,text,boolean)` (`:1191`), and the `kind` CHECK — plus `preview_accounting_plan(uuid,int)` (`:1970`) and any read door whose projection you widen, because the basis callers move with the signature. **Never touch** (DECISIONS §1.3) `_record_journal_entry_core`, `_tf_accounting_work_immutable`, `admit_journal_work`, `_admit_accounting_work_core`, the activity kind ladders, the knowledge cores, or `clara.periodic_adjustments`' purpose CHECK. `revise_accounting_plan` (`0193:1628`) is **not** on the recut list and needs no edit — see the cadence ruling below.
- **Occurrence purpose stays `journal_entry`** (Q5, DECISIONS §2). Therefore you add **nothing** to the Work purpose vocabulary: `lib/work/purpose-label.ts`, `accounting-work-list.tsx`, `work-list-filters.tsx` are #638's (DECISIONS §1.7) — do not edit them.
- **Reuse `clara.prepayment_schedule_v1(uuid,uuid)` verbatim, document-bound only this wave.** A human-stated typed term is a follow-up, not this slice (closes gap-653 Q2/Q3). No `_v2`. **Mint no grant**: it is in the closed ungranted census (`packages/db/tests/rig-meta.mjs:1203-1206`) and frozen by `evaluator_versions` over `pg_get_functiondef` (`0140:1184`, `scripts/check-frozen-evaluators.mjs`) — a grant reds the rig, an edit reds the apply. Your door reaches it as SECURITY DEFINER owned by the same owner role.
- **Re-derive the expense side in your own door** — never by calling the 0045 lane, never by unparking `close_prep` (`0138:2939-2941`). Same tokens, no new ones: `prepayment_target_ineligible` (`0140:3468,3474,3485`), `prepayment_target_underivable` (`0140:3460,3493`), `prepayment_amount_below_period_granularity` (`0140:3522`); use `clara._adj_line_eligibility_breach(uuid,jsonb)` (`0042:643`).
- **Cadence is derived from the evaluator, never typed, and the join key is exact.** `prepayment_schedule_v1` emits whole calendar months — `period_end` is always a month end (`0140:1115-1136`) — while 0193's generic machinery would happily take `day_of_month = 1..28` (`0193:1583-1590`), putting every due date off every line boundary and making the join miss **silently**. So `create_prepayment_schedule` fixes `frequency='monthly'`, `day_rule='last_day_of_month'`, `day_of_month null`, `reversal_day_rule null`, `effective_from` = the first line's `period_end`, `effective_to` = the last line's — all six read off the evaluator's output, none accepted from the caller. Pin it **inside the `_assert_plan_schedule` recut you already own** (its `p_kind` arm is the existing idiom, `0193:1605-1620`; its only callers are `create_accounting_plan` `0193:1519` and `revise_accounting_plan` `0193:1647`), so one arm closes both paths and `revise_accounting_plan` stays untouched. `_plan_amortisation_period_line` joins **`period_end = p_due`**; no matching line is a typed CLR10 refusal (`amortisation_period_line_missing`) on the occurrence, **never** a fall-back to the revision's constant basis.
- **What a revision means here.** That arm makes frequency/day-rule unrevisable for this kind, so a revision can only move the authority window inside the derived bounds, and the missing-line refusal makes widening it typed rather than silently constant. **Re-derivation from a corrected term is not claimed this wave**: a superseded `document_service_periods` row (`0140:452`, #646's discipline) needs a fresh derivation on a new revision — name it as a follow-up, and never let the report read "revision" as "re-amortise".
- **Atomicity (Q8 ruled):** "each occurrence's entry + receipt + occurrence row in one commit". Recognition journal + schedule configuration in one commit is **unbuildable** — the evaluator refuses a non-`approved` source entry (`0140:1040-1044`). Say so in the UI and the report. The residue is **two** residues with different owners: **(b)** a live schedule whose occurrence later refuses → the §E read's arm A; **(a)** a *create-time* refusal (`prepayment_term_underivable`, `nonzero_total`, `prepayment_target_*`) after the recognition has posted writes **no plan and no schedule row**, so no schedule-scoped read can ever reach it — it is answered synchronously by the door's inline refusal and durably by **arm B** of the same read (§D). Say which arm owns which; never cite one arm as closing both.
- **The door configures; the belt admits.** Do not copy #652's "admit the current-period occurrence inside the door" arm — that is #652's ruling, not yours. `wake_due_plan_occurrences` stays the only scan; mint no second one.
- **Locked period:** refuse the occurrence CLR19 `write_into_closed_period`, record the typed reason on the occurrence row, recover through the existing window-only `request_plan_catch_up` (`0193:1878`). **Do not widen it** — #652 shares it this wave (DECISIONS §2 puts #639 on a deferred FA birth trigger with no plan-lane touch at all; depreciation is a future consumer, not a current one).
- **AC5's park, C55.13 and C83.7 are NOT CLAIMED.** `clara.open_work_question` is `clara_runtime`-only and hook-token-gated (`0180:686`, `0184:1643,1696`) and the frozen prompt forbids the run citing a source document (`claraWork.v1.prompt.ts:68`). Write **two successor contracts** in the report — `claraWork_v4` (the term park) and `chatTurn_v20` (`start_prepayment_schedule_work`) — and cut neither (WORK-ORDER 8, DECISIONS §1.1).
- **Route `/clients/[clientId]/prepayments`** (DECISIONS §1.8) under `apps/web/app/(firm)/clients/[clientId]/`. Knowledge catalog untouched; `clara.entry_evidence_links` read directly, never wrapped; `StateBanner` stays as is (no `Alert` rebuild — that is #646's documents surface only).
- **Playwright port triple:** `CLARA_E2E_APP_ORIGIN=https://127.0.0.1:3330 CLARA_E2E_NEXT_PORT=3331 CLARA_E2E_RUNTIME_PORT=3332`.
- **Rig row (RIG.md):** worktree `C:\Users\zhant\Desktop\clara-wt\653`, branch `impl/653-prepayment-amortisation`, cluster port **55511**, database **clara_653**, 193 migrations applied, PG 17.11.

## 1. Current state

**Database.** `clara.document_service_periods` is the DB-owned term carrier at document grain — supersede-never-mutate, one live period per document, FORCE RLS (`0140:452`); its one human door `record_document_service_period` is bookkeeper-floored and structurally `basis_kind='human_stated'` (`0140:944`), human-only by law (`rig-meta.mjs:1203-1206`). `clara.prepayment_schedule_v1` (`0140:1009`) refuses a non-approved source entry (`:1037-1055` `prepayment_source_unfit`), refuses a source entry that does not debit **exactly one** asset line (`:1052-1064`, same token), refuses a memo-only entry that binds no document (`:1070-1075`) and a document with no live term (`:1076-1086` `prepayment_term_underivable`), and emits whole-calendar-month straight-line lines with `remainder_placement:'final_period'` (`:1130-1155`) — **the prepaid-asset half only**; the judged expense account and its three walls live in `clara._agent_prepayment_schedule_core` (`0140:3314`), stated verbatim at `0140:3529` as applied "HERE … and NOWHERE ELSE", bound to the 0045 template lane. #640's plan lane is complete and merged: three relations (`0193:408,523,633`), eleven human doors, one runtime scan (`0193:2161`), grants at `0193:2224-2235`, reads at viewer floor (`0193:2051`), writes at bookkeeper. `kind` admits two members (`0193:413`); `create_accounting_plan` refuses amortisation by name with `plan_kind_unsupported` (`0193:1466-1472`). `_assert_plan_schedule` already branches on `p_kind` (`0193:1605-1620`) and admits `day_of_month` 1–28 or `last_day_of_month` (`:1583-1590`). `_plan_occurrence_basis` is `language sql immutable` and rewrites only `posting_date` plus a reversal's side swap (`0193:1037-1065`) — **a revision's basis is one constant amount for every occurrence**; its two callers are `0193:1299` and `0193:1996`. Occurrence identity is `unique (plan_id, due_date)` + `unique (plan_id, leg, period_key)` + `unique (work_id)` + `unique (firm_id, client_id, intent_key)` (`0193:663-668`), append-only (`0193:709`), with the `attempts` ledger and `reverses_entry_id` from #640's review round (`0193:648-658`). A **refused** occurrence writes only `outcome` and returns — no Work, no `_audit` (the only one is on the admitted path, `0193:1400`), no interruption (`0193:1369-1382`). The window is bounded by `_plan_window_ceiling` (`:1008`), the due event chosen by `_plan_admissible_event` (`:1077`).

**Runtime.** `packages/runtime/lib/plan-occurrences.mjs:28-31` states it computes no date and reads no plan row — a new kind rides it unchanged. `workRoutes.ts:614` (`POST /api/work/journal`) and `:701` (`POST /api/work/periodic-adjustment`) are the route shapes. `plan-occurrence-e2e.mjs` is registered at `.github/actions/db-live-gates/action.yml:251`. No runtime module references `prepayment_schedule_v1` or `document_service_periods` anywhere.

**Web.** `/clients/:clientId/plans` exists (`apps/web/app/(firm)/clients/[clientId]/plans`, `components/plans/*`, `lib/plans/{api,schedule}.ts`); nav rows `periodicAdjustments` (`tree.ts:328`) and `plans` (`tree.ts:339`). **Nothing for prepayments** — three textual traces only: `lib/registers/adjustments.ts:43-44`, `messages/en.json:4832` ("Depreciation, prepayment and close schedules are not created here."), `e2e/work-list-mock.mjs:164`.

**Tests.** `f-a4-pr2a-{schedule,books,carrier,census}.test.mjs` prove the arithmetic — behind `prepayGate` (`f-a4-pr2a-fixtures.mjs:52`) and through `rootQuery` (`:178-179`). Use `humanQuery`/`runAs`/`namedCall` instead (`rig-helpers.mjs:183,262,280`; shape at `accounting-plans-fixtures.mjs:160`).

## 2. Gaps / rows

| Row | Disposition |
|---|---|
| **AC1** required full set + zero-value refusal | **build.** One door requires amount basis, expense account **with its stated basis**, term, method, authority and source; route the proposal through `_assert_journal_basis` (`0178:785-787`) — mint no second zero check. Cadence is derived, not asked (decisions above). |
| **AC2** exact-cent allocation, final-period residual, no invented term/authority | **build.** Both structural holes: the per-period basis on an exact `period_end = due_date` join, and the expense-side pairing + three walls. |
| **AC3** one occurrence under repetition / lost response / restart | **build on verified foundations** (`0193:663-668`, `packages/runtime/tests/plan-occurrence-e2e.mjs:11-40`) — re-prove for the new kind. |
| **AC4** atomic occurrence, configuration ≠ posted | **partial-with-named-residual** (Q8 ruling above): per-occurrence atomicity built; recognition+configuration atomicity is unbuildable; residue (a) is surfaced by the door's inline refusal + the attention read's arm B, residue (b) by arm A. |
| **AC5** park / locked-period catch-up / no historical authority | **partial-with-named-residual.** Locked-period and no-history halves are built (CLR19 + window-only catch-up); the **park is not claimed** — claraWork_v4 contract only. |
| **AC6** register the adapter; preview/revision/pause/end retain prior runs; explanatory failures in C8 | **build**, with revision bounded as ruled (cadence unrevisable, re-derivation not claimed) and including the attention read (the only projection today is `0193:2098`). |
| **AC7** the real journey, all states, 320px/zoom/keyboard/SR/motion/URL/drafts | **build** — zero UI exists. |
| **AC8** production read/command, least-privileged roles, real World, labelled evidence | **build.** Every door cell through `humanQuery` as a bookkeeper; World e2e labelled scripted-model/supplementary; web e2e is a mocked RPC boundary serving AC7 only; hosted evidence pending. |
| **C08.1** adjustment UI · reframe | **partial** — the 0045 register exists (`registers-workbench.tsx:28,74`); your surface renders the schedule, its authority and its period feedback. Say what is still unrendered (`adjustment_templates.schedule`, `0140:660`). |
| **C08.2** zero-value proposal | **verify-only + one red cell at the new door** (owner `clara._assert_journal_basis`, `0178:780-790`). |
| **C55.13** re-read evidence then ask a bounded question | **not claimed** — machinery exists, the lane does not. Contract only. |
| **C83.7** duplicate | **closes with C55.13's amended verdict** — not a separate cell, not "done". |
| **C88.13** representative synthetic + real-environment evidence | **partial** — extend the synthetic to amortisation (trigger / one effect / retry / cancellation); real-environment evidence separately owed, **not claimed**. |

## 3. Slice (one branch)

**Migration 0208.** *§0 PRESTATE*: refuse a partial cohort; pin by pre-image `sha256(prosrc)` **measured live off `pg_proc` on your migrated rig** (`0194:174-196` / `0195:371-404` idiom) every body you recut — `create_accounting_plan`, `_assert_plan_schedule`, `_plan_occurrence_basis`, `_plan_admit_occurrence`, `preview_accounting_plan`, plus any widened read door. **Pins are measured, never transcribed** from 0193's file text. Non-regression assertions: `prepayment_schedule_v1` present at its registered `evaluator_versions` closure, `_agent_prepayment_schedule_core` untouched, the five 0045 doors untouched, the `close_prep` wake row still disabled, `revise_accounting_plan`'s body unmoved, the new lane wholly absent. *§A*: drop/re-add the `kind` CHECK to admit `'amortisation_schedule'` (`0194:228-234` idiom), pinning the text you measured; widen the by-name refusal list so depreciation and close still answer `plan_kind_unsupported`. *§B*: `clara.prepayment_schedules` — source recognition entry, prepaid-asset account read off that entry, judged expense account + its stated basis, the `document_service_periods` row id, `basis_kind`, derived `period_lines`/`total_cents`/`period_count`/`remainder_placement`, evaluator version, plan + revision ids, per-occurrence links. FORCE RLS, owner policy, **zero relacl**, append-only trigger, composite `(client_id, firm_id)` FKs. *§C*: the per-period basis. `_plan_occurrence_basis` is `immutable` and cannot read a table, so the recut takes the resolved line **as an argument** (`p_line_override jsonb default null`) and a new `stable` helper `clara._plan_amortisation_period_line(p_plan, p_due)` resolves it by `period_end = p_due` — the exact key the fixed `last_day_of_month` cadence guarantees (`0140:1133-1136`); **no row is a typed refusal, never a constant**. Both callers (`0193:1299`, `:1996`) move in this file. Fallback, only if you *measure* that the argument form cannot keep `preview_accounting_plan` pure: add `clara._plan_amortisation_basis(...)` and branch on kind inside both callers — record the volatility proof (`provolatile` of the live body) that made you pick. The `_assert_plan_schedule` recut carries the amortisation cadence arm (decisions above), so the finite term rides `effective_to` and `_plan_admissible_event`/`_plan_window_ceiling` need no recut. *§D* doors, bookkeeper floor, `_human_ctx` + `_reserve_op`/`_finish_op`, granted to `clara_authenticated`: `create_prepayment_schedule` (resolves authority the 0193 way, calls the evaluator as definer, derives all six schedule fields from its output, surfaces every refusal token verbatim, feeds `_plan_overlap_warning`), `get_prepayment_schedule`, `list_prepayment_schedules`, and **`list_prepayment_attention(p_client)`** — §E of the gap map, widened to two arms because one arm cannot reach both Q8 residues: **arm A** = live amortisation plans whose most recent occurrence refused, with its typed reason (without it a 24-period schedule fails monthly, invisibly); **arm B** = "recognised, not yet scheduled" — approved entries that bind a document and debit exactly one asset line (`0140:1052-1075`, the evaluator's own predicate: no new judgement, no expense-coding false positives) that no `prepayment_schedules` row names as source, each projecting **whether a live `document_service_periods` row exists** so the surface names the next act. Lock order: the `accounting_plans` row `for update` **first** (`0193:253-262`). *§TAIL*: census the new CHECK members, zero relacl, every grant, the untouched 0045 doors, the still-disabled wake row.

**Runtime.** No new belt, no new workflow version. ONE new non-frozen module `packages/runtime/lib/prepayment-schedule-basis.ts` on `periodic-adjustment-basis.ts`'s shape: `.strict()` zod input schema, a refusal **mirror** (never a rule of its own), the door payload builder, and a `WHAT THE SUCCESSOR MUST WIRE` footer carrying **both** contracts. It must be imported by **no** frozen body, including dynamically (`scripts/check-frozen-workflows.mjs` matches `import("…")`). New standalone `packages/runtime/tests/prepayment-occurrence-e2e.mjs` on the real Postgres World, skip-clean when 0208 is absent, registered with **one line** in `.github/actions/db-live-gates/action.yml` after `plan-occurrence-e2e` (`:251`).

**Web.** Routes `/clients/[clientId]/prepayments` (+ `/new`, `/[scheduleId]`) under `app/(firm)/…`; `lib/prepayments/api.ts` (callDoor only, hydrate-never-trust, a fresh `op_key` per decision) and `lib/prepayments/schedule.ts` (pure mirror). Accepted compositions (appendix D): FieldGroup + Field; Calendar + Popover as the Date Picker recipe keeping the exact ISO date with no timezone shift and typed precision (row 22/12) — for the **term** fields only, never for a cadence the door derives; Select — Combobox for a large chart — for the expense account, **with its stated-basis field, required** (rows 49/18); a **disabled** Table preview of the derived period lines showing each period's own `period_end` as its posting date (a preview, not an editor); DataTableCard occurrence history with receipts, journal links **and refused occurrences with their typed reason**; Dialog for pause/end with Title, initial focus, Escape and focus restoration (row 23); `StateBanner` carrying the database's own words for every refusal — never a toast; the persistent boundary statement (a schedule creates journal Work; it never initiates a bank payment) and the configuration-vs-posted-occurrence sentence on list, detail and form. The list opens with the **attention band** from `list_prepayment_attention`: arm A rows ("last period refused: <reason>") lead to the detail's explain-and-choose surface; arm B rows ("posted, not yet amortised") lead to the term door or back into the create form with the entry prefilled — the only durable trace of a create-time refusal, so not optional. On a create-time refusal the form keeps its draft, names the refusal verbatim, and says the recognition entry is already posted and still needs a schedule. States: loading skeleton with no placeholder zero; empty distinguishing first-use / filtered / valid-no-data / no-permission; partial-stale; invalid-saving; denied-failed; cancelled-recovery. 320px, 200% zoom, keyboard/focus return, SR names, reduced motion, stable URL/Back, drafts preserved and never crossing clients.

**Docs.** `packages/db/README.md` §Operation-contract census; `packages/db/tests/README.md` §Freshness and split chains; `packages/runtime/README.md` §Current structure + both successor contracts; `apps/web/README.md` §Application map (one row); `CONTEXT.md` in the `term: / _Avoid_:` shape — **Prepayment schedule**, **Service period**, and the `Accounting plan` kind list (`CONTEXT.md:23-31`), extending **Plan occurrence** (`:33-42`) rather than minting a rival term. Never `docs/PRD.md` or `docs/ARCHITECTURE.md`; record the `authority_rule` drift (`PRD.md:69` vs `0193:418`) under "blueprint drift".

**Successor contract stanza** (report, exact): tool name, `.strict()` zod input schema, door call and argument order, refusal→message map, part kind, and for the claraWork park the hook-token flow and what an expired question means (0198 removed the `work_id is not null` predicate).

## 4. TDD seams (red first)

All db cells through `humanQuery(sub, namedCall(...))` as a **bookkeeper** (`rig-helpers.mjs:183,280`); a `rootQuery` cell is not AC8 evidence.

1. `packages/db/tests/prepayment-schedule.test.mjs` **p653.schedule.exact_cents** — a 12-month term over 100,000 cents: twelve period lines summing exactly to the total, residual wholly in the final period, both sides present on every line. Red today: the door does not exist.
2. **p653.schedule.due_dates_cover** — the created plan is `monthly`/`last_day_of_month` with the derived `effective_from`/`effective_to`, and every due date `preview_accounting_plan` generates matches **exactly one** `period_lines` entry by `period_end`: no gap, no double cover, count equal to `period_count`. A caller-supplied `day_of_month` is refused.
3. **p653.schedule.term_missing** — a source entry whose document has no live service period refuses `prepayment_term_underivable` naming `document_service_periods`; no plan, no schedule row.
4. **p653.schedule.zero_basis** — a balanced all-zero proposal refuses `nonzero_total` **at the new door** (C08.2's owed cell).
5. **p653.schedule.granularity** — 1 cent over 2 months refuses `prepayment_amount_below_period_granularity`.
6. **p653.schedule.target_ineligible / p653.schedule.target_underivable** — a non-expense-class or ineligible target, and a target with no stated basis, refuse with those exact tokens.
7. **p653.schedule.source_unfit** — an unapproved source entry, and one debiting two asset lines, each refuse `prepayment_source_unfit`.
8. **p653.kind.unsupported** — `depreciation` and `close` still answer `plan_kind_unsupported` after the widening; `amortisation_schedule` is accepted.
9. **p653.revision.cadence_pinned** — `revise_accounting_plan` on an amortisation plan with a changed frequency or day rule refuses CLR10 through the recut `_assert_plan_schedule`, the stored `period_lines` do not move, and prior occurrences keep naming their revision (`0193:579`).
10. `prepayment-occurrences.test.mjs` **p653.occ.per_period** — each occurrence's basis carries **its own** period amount (not the constant), the final occurrence carries the residual; a due date with no matching line refuses `amortisation_period_line_missing` and posts nothing.
11. **p653.occ.identity** — two concurrent scans under a real lock barrier leave one occurrence and one Work; the second converges on the same `work_id`.
12. **p653.occ.locked_period** — a closed period refuses CLR19 `write_into_closed_period`, writes the typed reason on the occurrence, admits nothing.
13. **p653.attention.arm_a** — a deactivated authoriser and a lapsed egress authorisation each refuse, and both are returned by `list_prepayment_attention` with their typed reason (red today: the read does not exist).
14. **p653.attention.arm_b** — a posted recognition whose `create_prepayment_schedule` refused `prepayment_term_underivable` appears as "recognised, not yet scheduled" with `has_live_term:false`; recording the term flips it to `true`; creating the schedule removes it from the read. An ordinary expense coding with no debited asset leg never appears.
15. **p653.catchup.window** — catch-up below `authority_from` refuses; an in-range window admits oldest-first; a completed period is not re-attemptable.
16. **p653.census.grants** — the new relation carries no app-role grant; `prepayment_schedule_v1` is still ungranted and still matches its `evaluator_versions` member hash.
17. Runtime: `prepayment-occurrence-e2e.mjs` — two belt passes → one occurrence/Work; `CLARA_WORK_TEST_FAULT=exit_after_commit` replays onto one entry/receipt/occurrence; **the final period posts the residual, not the base**; a third pass admits nothing. Plus a unit test for `prepayment-schedule-basis.ts`.
18. Web unit: `prepayments-render-states.test.tsx` (both attention arms rendered distinctly), `-a11y`, `-keyboard`, `lib/prepayments/schedule.test.ts` (mirror parity with the DB tokens).
19. Playwright `prepayments-walk.spec.ts` + `prepayments-mock.mjs` — configure → preview → occurrence history → refused occurrence with its explain-and-choose surface → an arm-B attention row back into the prefilled form → pause/end; 320px, 200% zoom, keyboard, focus return from the Dialog, reduced motion, Back, preserved draft.

## 5. Risks

Silent monthly failure is the largest hole (`0193:1369-1382`) — §D's attention read is the mitigation, not an extra. **A posted recognition that never got a schedule is the second**: arm B reaches it whenever the entry binds a document and debits one asset line, but a **memo-only** recognition binds no document, so the evaluator refuses outright (`0140:1070-1075`) and nothing — not arm B, not any read this wave builds — can list it; a memo-based prepayment has no amortisation path at all (gap Q2). State that residual; do not imply Q8 is closed. Model egress is a standing precondition for every occurrence (`0195:502`, `claraWork.v3.ts:10-19`): a superseded legal version stops every future period with a typed CLR13. Double posting across the 0045 lane is **not closed** — `_plan_overlap_warning` is called only at create/revise (`0193:1552,1751`), so a template signed after the plan is unwarned; state the residual. A deactivated authoriser silently stops a multi-year schedule (`0193:74-77`; #625 owns the other side). The frozen tool schema is `.strict()` — anything extra rides the memo, never the basis. Freezing by closure is silent: run `node scripts/check-frozen-workflows.mjs` and `node packages/runtime/scripts/check-parts-parity.mjs` before every commit touching `packages/`.

**Shared-file one-liners this ticket adds** (logic lives in your own modules): `tree.ts` — one `ACCOUNTING_ITEMS` row `prepayments` beside `plans` (`:339`); `messages/en.json` — a new `Prepayments` block **and** the now-false word in `plans.form.kindNote` (`:4832`, keep depreciation/close); `apps/web/test/manifest.txt` — one line per new test file at its plain-string sorted position; `e2e/serve-built.mjs` — one import + one dispatch for `prepayments-mock.mjs`; `e2e-fixture-ownership.test.ts` — the mock in the list (`:66`), its row in the map (`:340`) and **every RPC verb it answers**, shared verbs declared shared; `rig-meta.mjs` — a `PREPAYMENT_0208_*` cohort on the `ACCOUNTING_PLANS_0193_*` idiom (`:1841-1876`, `:2108`, `:2326`, `:2512`); `packages/db/package.json` — one gate import; `db-live-gates/action.yml` — one e2e step; `CONTEXT.md` — two terms; `apps/web/tests/firm-scope-db-pins.corpus.ts` — an entry **only** if you add dynamic SQL (sorted, ≥40-char reason), sequenced at merge prep.

## 6. Effort and rig

**XL.** Rig: worktree `C:\Users\zhant\Desktop\clara-wt\653`, branch `impl/653-prepayment-amortisation`, PG cluster `127.0.0.1:55511` / `clara_653` (`CLARA_ALLOW_DESTRUCTIVE=1 CLARA_RIG_DB=1`; never the reset/role-sweep flags), Playwright triple **3330/3331/3332**, plus a real Postgres World for the e2e leg. Before the final report run: the **whole `apps/web` unit suite** (`node scripts/run-tests.mjs`, ~5 min, report counts), `packages/db/tests/operation-census.test.mjs` and `rig-isolation.test.mjs` (no reset flags), `node scripts/check-frozen-workflows.mjs` and `node packages/runtime/scripts/check-parts-parity.mjs`, plus `pnpm typecheck` and `pnpm lint` from the worktree root. Copy the house shapes from `packages/db/migrations/0193_accounting_plans.sql` and `0194_periodic_adjustments.sql`, `docs/plan/active/refresh-wave-2026-09-14/brief-640.md`, `brief-643.md`, and `docs/plan/active/refresh-wave-2026-09-14/reports/640-review-closure.md` — that last file is what a reviewer later demanded of this exact lane: behavioural cells rather than `prosrc` assertions, a measured race, a measured cadence, and a blocker found in the gap between "admitted" and "posted".

## Verifier findings not applied

- The Q8 finding offered a second branch — accept the create-time residue as a pure §5 risk with no durable surface. Not taken: DECISIONS §2 (#653) assigns that residue to the refusal-visibility read, so the read is widened (arm B) instead and only the memo-only sliver stays a named risk.
