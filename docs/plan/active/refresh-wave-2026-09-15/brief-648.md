# Brief: #648 — resume firm setup from the facts that are actually missing

## Orchestrator decisions (binding)

- **Migration 0203** (`packages/db/migrations/0203_firm_setup.sql`), one file. 0199–0202 and 0204–0209 belong to other in-flight tickets and are **absent on your rig** — expected; depend on none. A released number is a gap, never renumbered (DECISIONS §1.2).
- **Section id `setup`**, route `/settings/setup`, `minimumRole:'admin'` — one row in `SETTINGS_SECTIONS` (`apps/web/lib/navigation/tree.ts:245-287`). `/settings/firm` **untouched** (its two honest paragraphs are pinned by `firm-navigation-walk.spec.ts` and `firm-admin-pages-a11y.test.tsx`, awaiting 裁-188). #625 edits `members`, #654 adds `knowledge`: keep your diff to your own row (DECISIONS §1.8, SYNTHESIS:252).
- **Seeding is a reconciliation, never a blind insert.** The firm plan arrives in two states: empty (claim path, `0145:492-494` via `0186:1652`) or already carrying up to fourteen `firmInterview_v3` items (`firmInterview.v3.ts:163-166`). `uq_onboarding_plan_items_key` (`0017:1056`) makes a blind insert raise 23505.
- **Four human doors + one read** on the firm plan: reconcile-seed / answer / defer / commit, plus `clara.get_firm_setup()`. **Admin floor on all four**; per-key elevation is **catalogue data** (a `min_role` column), never a second code branch — `_knowledge_floor` already takes the higher of scope-implied and key-carried floors (`0192:996-1008`).
- **D10 (orchestrator ruling): firm identity facts stay plan items.** Registered name, SSM, TIN, address, MIA get an author and a revision snapshot on `onboarding_plan_items` and nothing else. Mint **no** `knowledge_keys` row and **no** `knowledge_plan_item_map` row — #654 owns the catalog this wave (DECISIONS §1.5, SYNTHESIS:217); both are append-only (`0192:185`, `:286-289`) and `kp.01` is a ten-entry `deepEqual` (`knowledge-onboarding-promotion.test.mjs:92-113`).
- **D8 (orchestrator ruling — overrides gap-648 Q1's six keys):** only **`default_currency`, `reporting_framework`, `accounting_basis`** are firm-defaultable and therefore captured, through the live `clara.capture_knowledge(p_scope_kind=>'firm', p_client=>null, …)` (`0192:1112-1147`). `entity_type`, `turnover_band`, `financial_year_end_month` are client-identity keys and are **not** captured. Catalogue rows carry `knowledge_key` non-null for exactly `currency`, `framework`, `accounting_basis`.
- **Do not use `promote_plan_answers_to_knowledge`** (orchestrator ruling): its loop joins `knowledge_plan_item_map` on one global `item_key` namespace with no scope discriminator (`0192:1685`, `:279-284`), so one firm row would promote out of every committed **client** plan. Avoiding it is also why 0203 recuts nothing.
- **#654 owns the firm-knowledge register** (`/settings/knowledge`, `list_firm_knowledge`, Promote dialog, the firm-rule-vs-client-exception pair). #648 renders a **"facts confirmed in setup"** face only (DECISIONS §2 #654, SYNTHESIS:251, `docs/PRD.md:121`).
- **C48.5 / C83.24 close here** (#654 gets a pointer). Read "use shared Work questions" as reuse of the **components and their rules** — one fact = `Field`, a bounded set = the local stepper, leave-pending, preserved drafts — with **records on `onboarding_plan_items`**. Work questions are pinned to a client-scoped `accounting_work` row (`0178:304`; `0180:617,666-669`) and cannot carry a firm fact.
- **Runtime: none.** No lane, route, registry repoint or successor. `firmInterview_v4` is explicitly **not** cut this wave (DECISIONS §1.1) — the durable-interview fork is out. Do not relax `interviewRoutes.ts:238-254`'s 403 `already_member`; do not add a firm-scope helper to `packages/runtime/lib/knowledge.mjs` (frozen, `frozen-workflows.json:44-48`) — that mints chatTurn_v20. **This journey invokes no Workflow**: no World e2e leg, **no `db-live-gates/action.yml` row**. Write that sentence in the report.
- **Successor contract: none owed.** Firm-scope chat capture is #654's stanza; state that in one line with the reason.
- **`update_onboarding_plan` stays runtime-only and byte-identical** (never widen `0017:5125-5135`); **never widen `commit_client_onboarding`** (it forces a client, `0017:2751-2771`) — build your own commit door.
- **Firm intake documents link to #633's `(firm)/documents` leaf** (DECISIONS §2 #633). If #633 has not merged, render an honest named line, not a dead href, and say so.
- **Not touched:** activity kind ladder (D13 — named residual only); `lib/firm/capabilities.ts` (#625); Work purpose vocabulary (#638); `components/common/state.tsx` (`StateBanner` stays; `Alert` is #646's documents surface only); no eleventh `needs-you.ts` row kind (DECISIONS §1.6).
- **Playwright triple:** `CLARA_E2E_APP_ORIGIN=https://127.0.0.1:3290 CLARA_E2E_NEXT_PORT=3291 CLARA_E2E_RUNTIME_PORT=3292`.
- **Rig row:** worktree `C:\Users\zhant\Desktop\clara-wt\648` · branch `impl/648-firm-setup` · port **55507** · db **clara_648** · base `4464e471` (193 migrations).

## 1. Current state

**db.** `onboarding_plans` supports `scope_kind='firm'` with null `client_id` (`0017:995-1039`); `_create_firm_core` opens exactly one per firm with a revision-1 snapshot (`0145:492-494`); `claim_paid_firm`'s replay arm reads it with a bare `select … into`, no `LIMIT`, no `STRICT` (`0186:1555-1557`), and `uq_onboarding_plans_one_open` is on `(firm_id, client_id)` (`0017:1038-1039`) where NULLs are distinct — firm plans are **not** deduplicated. The only item writer, `update_onboarding_plan`, is `clara_runtime`-only (`0017:5125-5135`); its CAS raises `CLR06` + `detail.reason='stale_plan'` (`0017:2655-2659`, whose comment warns `CLR31/'stale_plan'` is the SEED family). `onboarding_plan_items` has **no `reason` column**; the `deferred` arm of `ck_onboarding_plan_items_answer` names only `answered_by`/`answered_at` (`0017:1058-1064`). `clara_authenticated` already holds RLS-scoped SELECT on all three plan tables (`0017:5114-5122`). `capture_knowledge` is live, accepts `p_scope_kind='firm'`, floors at admin (`0192:1112-1147`, `:996-1008`) — with **zero callers repo-wide** (only `capture_knowledge_for`, `chatTurn.v19.tools.ts:50`). `promote_plan_answers_to_knowledge`'s firm arm (`0192:1662-1679`) sits behind a committed gate (`0192:1654-1657`) and has never executed. Thirteen seeded keys, all `scope_default='client'`; identity keys deliberately absent, routed to #647 (`0192:271-278`). Frontier 0198; **no migration pins any onboarding or knowledge body by `sha256(prosrc)`** — the nearest guard is a string anchor at `0018:385-400`.

**runtime.** `firmInterview_v1/v2/v3`, `interview.v1.*`, `interview.v2.*`, `interview.v3.questions.ts` are frozen, `deployed:true`; v3 is a **pre-admission bootstrap** that asks everything then writes the plan once (`firmInterview.v3.ts:88-166`). `FIRM_SEGMENTS_V2` is thirteen segments (`interview.v2.questions.ts:33-47`), `bookkeeper_email` `requiredForCommit:true` at `:39`; `buildFirmPlanItemsV2` unconditionally appends a `first_client_onboarding` todo (`:120-129`).

**web.** No firm setup surface exists. `SETTINGS_SECTIONS` is exactly five rows (`tree.ts:245-287`); `/settings/firm` renders two paragraphs and reads nothing (`components/firm-admin/settings-panel.tsx:59-89`); `firm-home-board.tsx` has no setup tile (its `:213` "onboarding" is `clientStatusTally`'s client count); claim success redirects to `/` with no guidance (`app/(entry)/checkout/success/claim/handler.ts:143-144`). The shared question form — one Field per fact, local next/back/review stepper, leave-pending, preserved draft — is at `components/work/work-question-form.tsx:8-45`, and Questionnaire is not installable (`apps/web/components.json:24` is `"registries": {}`, re-verified). `OnboardingChecklistCard.tsx:148-152,279-291` is the reusable N/N counter. `apps/web/lib/require-firm-scope.ts` holds the three registries: `SCOPE_ENTRANCES:253`, `SCOPE_UNSCOPED_SURFACES:288`, `SCOPE_EXEMPT_SURFACES:477`.

**tests.** `packages/db/tests/knowledge-records.test.mjs:286-300` (kn.11/kn.12) is the journey's **only** real-least-privilege evidence: firm-scope `capture_knowledge` refused for bookkeeper, accepted for admin, reaching a client with no row of its own. Helpers `humanQuery`/`roleQuery`/`rootQuery`/`ROLES`/`assertRaises`/`opk` come from `packages/db/tests/rig-fixtures.mjs` (`knowledge-records.test.mjs:19`). Nothing reads, commits or promotes a `scope_kind='firm'` plan.

## 2. Gaps / rows

| Item | Disposition |
|---|---|
| **AC1** progress from real required items; resume after reload and concurrent editing | **to build** — reconciling seed + CAS answer door + `get_firm_setup`; accepted facts are never re-asked because the seed preserves them |
| **AC2** Field for one fact, Questionnaire for a bounded set; optional education skippable, never an approval gate | **partial-with-named-residual** — build Field + the local stepper (Questionnaire uninstallable, `components.json:24`); prove skippability on the real `skippable:true` items `mia` (`interview.v2.questions.ts:37`) and `currency` (`:42`). **Residual: no optional-education content exists** — an owner input; invent no accounting guidance |
| **AC3** scope/source/actor in the canonical record; conflicts, stale, required source, permission refusal | **partial-with-named-residual** — the record half lands for the **three** eligible keys (D8). **Residual: firm identity facts have no knowledge record this wave** (D10); they carry author + revision snapshot on the plan |
| **AC4** partial completion + an authorised next step; workspace stays usable | **to build** — home tile + the DB-derived N/N; "still usable" becomes an **assertion** (cell 15), not an assumption |
| **AC5** saved/resumed, conflict, stale, denied, completion + responsive/focus/zoom/motion/announcement | **to build** |
| **AC6** ten states, exact values, 320px/200%, focus return, SR names, reduced motion, stable URL/Back, preserved drafts | **to build** |
| **AC7** production-facing read/command under real least-privileged roles and current migrations | **to build** — every battery through `humanQuery` personas, never `rootQuery` for the assertion under test (DECISIONS §1.10) |
| **C48.5** | **to build, closed here** — current route, persisted answers, applicability and correction path, using the shared question **components** with records on `onboarding_plan_items` |
| **C83.24** | **duplicate — closed with C48.5**, no independent work; leave the pointer for #654 |

**Out of scope, with authority.** `list_firm_knowledge` + firm register + Promote dialog + the exception pair → **#654** (DECISIONS §2 #654; `docs/PRD.md:121`). Firm unassigned-documents leaf → **#633** (DECISIONS §2 #633). Identity knowledge keys → deferred, none minted (D10, DECISIONS §1.5). `promote_plan_answers_to_knowledge` → ruled out. Cross-batch aggregate progress → **#636** (`docs/PRD.md:119`). Activity kind mis-filing → **D13 residual**. `firmInterview_v4` → not cut this wave (DECISIONS §1.1).

## 3. Slice (one branch)

**Migration `0203_firm_setup.sql`** — house header, FORCE RLS, no app-role DML, `Asia/Kuala_Lumpur` for any date.

*Catalogue.* `clara.firm_setup_keys(item_key pk, knowledge_key text null references clara.knowledge_keys, item_kind check in ('must_ask','capture','todo','education'), required_for_commit bool not null, min_role text not null default 'admin', question text not null, note text not null, sort_order int not null)` — code-populated; append-only + no-truncate triggers on the `0192:286-289` idiom; owner policy + read policy. **SELECT granted to `clara_authenticated` only** — do **not** copy `0192:296-297`, which also grants `clara_agent_ro`/`clara_runtime` and would contradict this file's own no-runtime rule. Seed from `FIRM_SEGMENTS_V2` (`interview.v2.questions.ts:33-47`) **minus `bookkeeper_email`** (member provisioning, #625, `:39`) and **minus `first_client_onboarding`** (`:120-129`, #649's journey).

*Doors* — `clara_fn_owner`-owned SECURITY DEFINER, `set search_path = clara, pg_temp`, `set plan_cache_mode = force_custom_plan`, PUBLIC revoked, EXECUTE to `clara_authenticated` only, `_human_ctx(clara.role_rank('admin'))`, reserve-before-mutable-validation via `_reserve_op`/`_finish_op` (`0004_governed_fns.sql:46`), `_audit`, typed CLR with `detail.reason`, registered events:

1. `seed_firm_setup_plan(p_op_key)` — **reconciling**: existing items keep `answer`/`answered_by`/`answered_at`/`state` untouched; only missing catalogue rows are inserted; rotate `revision_token`; append a snapshot; same op_key replays one receipt.
2. `answer_firm_setup_item(p_plan, p_expected_revision, p_item_key, p_answer, p_op_key)` — CAS mismatch → `CLR06` + `detail.reason='stale_plan'`, the `0017:2655-2659` wording (never `CLR31`). Where the catalogue row carries a `knowledge_key`, the same transaction calls `capture_knowledge(p_scope_kind=>'firm', p_client=>null, …)`.
3. `defer_firm_setup_item(p_plan, p_expected_revision, p_item_key, p_reason, p_op_key)` — only where `required_for_commit=false`. **Preferred:** `{"deferred_reason": …}` inside `answer` — the `state='deferred'` arm of `ck_onboarding_plan_items_answer` (`0017:1063`) names only `answered_by`/`answered_at`. **Fallback:** the reason into `_audit` detail only, with the UI saying so. **Proof to pick:** a red-first cell driving the door with a non-null `answer` on a deferred item — if the CHECK admits it take the preferred form, if it refuses take the fallback and quote the refusal. Assume no column that does not exist.
4. `commit_firm_setup(p_plan, p_expected_revision, p_op_key)` — any `required_for_commit` item still pending → `CLR10 required_items_outstanding` **naming them**; otherwise `state='committed'`.

*Read.* `clara.get_firm_setup()` → `{plan_id, revision_token, revision_n, state, items[] (item_key, kind, question, required, state, answer, answered_by, answered_at, knowledge_key, knowledge_record_id), required_outstanding[], confirmed_facts[]}`. **The journey's only production read** — leave no parallel `lib/read.ts getRows` PostgREST path, or AC7's battery tests a door nobody calls.

*Index.* `create unique index uq_onboarding_plans_one_open_firm on clara.onboarding_plans (firm_id) where state='open' and scope_kind='firm'` — a **correctness fix to a live door** (`0186:1555-1557`), not merely a new-door guard. The tail proves no firm violates it first; a loud failure there is the correct outcome.

*Prestate / tail / pins.* **Zero `create or replace` of any governed function and zero foreign `alter table`** beyond the new index — reproduce 0193's §0/§J census shape (`reports/640-review-closure.md` §C confirms 0193 achieves exactly that). **No pre-image sha pins are owed** (gap-648 "Pinned function recut: NONE"; SYNTHESIS:166). If you nevertheless recut anything, **measure** the pre-image `sha256(prosrc)` off `pg_proc` **on your migrated rig** (55507/`clara_648`) — never transcribe it from a creating file; several live bodies here are splices. House-style tail census over owner/definer/search_path/ACL/RLS/append-only; one preintegration gate module; `rig-meta.mjs` cohort rows.

**Runtime.** None; report the one sentence.

**Web.** `app/(firm)/settings/setup/page.tsx` (PageShell/PageHeader/SettingsNav, like its five siblings) + `components/firm-setup/{firm-setup-checklist,firm-setup-item-form,firm-confirmed-facts-panel}.tsx` + `apps/web/lib/firm-setup/{api,types}.ts` (read only through `get_firm_setup`; doors through `lib/doors.ts`, a freshly minted op_key per attempt). Compositions per appendix D: Card + CardHeader + `components/common/section-header.tsx` per group; **one fact = one `Field`/`FieldGroup`** (`components/ui/field.tsx`, row 28); a bounded related set = the local stepper of `work-question-form.tsx:8-15`; `Badge` for scope/source/trust, never the sole colour cue (row 7); `Progress` only for the measured N/N (row 44); `Dialog` for one bounded decision; loading/empty/denied/failed through `components/common/state.tsx` — do not rebuild on `ui/alert.tsx`.

Every state rendered and tested: **loading** (skeleton fitting the checklist; no placeholder zero); **successful empty/no-results** (all catalogue rows answered → the completion face, distinct from a not-yet-seeded plan); **partial/stale** (`CLR06` converges **inline**, re-reads the authoritative plan, **preserves the typed draft** — never a toast); **invalid/saving** (error beside its control, focus the first invalid control, input preserved); **denied/failed** (the section is absent from a bookkeeper's nav by rank, and a deep link renders a named denied face; `CLR04` and `capture_knowledge`'s `knowledge_key_unknown` / `knowledge_already_live` each get a designed face, not a raw door error); **cancelled/recovery** (leave-pending on a step; on a lost response re-read `get_firm_setup` before offering a distinct resubmit, same op_key for a retry). Plus 320px, 200% zoom, keyboard and focus return after each step and after the Dialog, screen-reader names with exactly one announcement owner per transition, reduced motion, stable URL/Back (`/settings/setup` is its own address, steps are local state, Back returns to firm home), preserved drafts under the same user/firm/item/revision.

Counter: **required-answered / required-total, read from the DB** — never a percentage, never a sum of overlapping facets (#650 AC2), not #636's cross-batch aggregate. Firm-home tile in `components/firm/firm-home/firm-home-board.tsx`: admin+ only, rendered only while required items remain, links `/settings/setup`, **gates nothing**. Confirmed-facts panel shows only this setup's confirmed facts, reusing `components/registers/knowledge-shared.tsx` so it cannot drift from C13; correct/withdraw through `correct_knowledge`/`withdraw_knowledge`; it must visibly distinguish a **firm default** from a still-live legacy `client_facts` row (0192 §D.8 — those five are UNIONed in and never shadowed).

**Docs.** `apps/web/README.md` Application map: `/settings/setup` on the Settings row (that table is the route SOT; start no new section). `packages/db/README.md`: the four doors + one read in the operation-contract census, with floors and roles. `packages/db/tests/README.md`: `firm-setup.test.mjs` and its rig dependency. `packages/runtime/README.md`: unchanged — say so. `CONTEXT.md`, house "term / Avoid" shape: **Firm setup** (the resumable list of facts a firm must state about itself, derived from real required items; _Avoid_: a progress bar with no required items behind it, an onboarding approval ritual, optional education as a prerequisite) and **Firm profile fact** (a firm-scope knowledge record about the firm's own circumstances — distinct from a client fact and from a **Firm knowledge default**, the cross-client instruction #654 owns; _Avoid_: recording a firm fact as a client fact, or reading one as posting authority), plus one boundary sentence on **Firm knowledge default**. **Never edit `docs/PRD.md` or `docs/ARCHITECTURE.md`**; contradictions go under "blueprint drift".

## 4. TDD seams (red first)

DB battery `packages/db/tests/firm-setup.test.mjs`, copying the ROLES/`humanQuery` helpers and preintegration-gate shape from `knowledge-records.test.mjs` (`:19`, `:286-300`). Every assertion under test runs through a least-privileged persona, never `rootQuery`.

1. `p648.seed.reconcile` — a plan already carrying `firmInterview_v3` items: the seed inserts only missing catalogue rows and leaves each existing item's `answer`/`answered_by`/`answered_at`/`state` byte-identical. **Red:** a blind insert raises 23505 on `uq_onboarding_plan_items_key`.
2. `p648.seed.empty` — the claimed plan is `scope_kind='firm'`, `state='open'`, zero items; after the seed, exactly the catalogue rows with their `required_for_commit` flags, `revision_token` rotated, one snapshot appended; same op_key replays one receipt. **Red:** no such function.
3. `p648.seed.excludes` — the seeded catalogue holds **no** `bookkeeper_email` and **no** `first_client_onboarding`. **Red:** a wholesale `FIRM_SEGMENTS_V2` seed.
4. `p648.answer.floor` — bookkeeper answer → `CLR04` and zero rows written (re-read the item **and** the audit count); admin succeeds.
5. `p648.answer.stale` — a stale `p_expected_revision` → `CLR06` with `detail.reason='stale_plan'` (assert it is **not** `CLR31`); the item unchanged.
6. `p648.defer.reason` — the §3 door-3 mechanism probe, then: defer `mia`/`currency` and prove the reason survives a re-read at whichever landing place the probe chose.
7. `p648.commit.outstanding` — a pending required item → `CLR10 required_items_outstanding` naming it; once all required items are answered or deferred, `state='committed'`; op_key replay yields one receipt.
8. `p648.capture.eligible` — answering `currency`/`framework`/`accounting_basis` writes a `scope_kind='firm'` `knowledge_records` row with `asserted_by` = the answering admin and `recorded_via='human_ui'` (`0192:1112-1147`); op_key replays.
9. `p648.capture.ineligible` — answering `entity_type`, `fye`, `turnover`, `legal_name`, `ssm`, `address`, `tin`, `mia` writes **no** knowledge record and moves **neither** `knowledge_keys` nor `knowledge_plan_item_map` row counts. D8/D10's wall on your side, provable at 0203 alone.
10. `p648.capture.shadow` — the firm `default_currency` row appears in `list_client_knowledge` for a client with no row of its own and is shadowed for one that has (`0192:1338-1356`).
11. `p648.authority.downgrade` — the answering admin is downgraded; a later read judges the fact's authority by the member's **current** rank (`0192:1694-1703`). Do **not** attempt the bookkeeper-authored variant: an admin-floor door can never make a bookkeeper the `answered_by`, and staging it needs a `clara_fn_owner` back door — exactly the mocked helper AC7 forbids.
12. `p648.isolation.oracle` — firm B's admin naming firm A's real `plan_id` gets a refusal byte-identical to a random uuid's (`0192:1580-1590`).
13. `p648.acl.census` — every new door: owner/definer/`search_path`/`plan_cache_mode`/PUBLIC-revoked/EXECUTE-only-`clara_authenticated`; the catalogue table's SELECT likewise; **zero** runtime/agent/wake EXECUTE anywhere in 0203; `update_onboarding_plan`'s grant unchanged (`0017:5125-5135`).
14. `p648.plans.one_open` — the partial unique index refuses a second open firm plan, and `claim_paid_firm`'s replay can no longer resolve to an arbitrary first row (`0186:1555-1557`).
15. `p648.workspace.open` — AC4's second half as an assertion: with the firm plan uncommitted, a bookkeeper persona still reads clients, Work and activity through their production doors.

Web unit (node:test + testing-library; **every file registered in `apps/web/test/manifest.txt`**):

16. `firm-setup-checklist.test.tsx` — the counter is the DB's required-answered/required-total (never a percentage, never a facet sum); `CLR06` converges inline to the re-read plan with the typed draft intact; a bookkeeper session renders the denied face with zero write controls; an optional item skips and the checklist still completes; the lost-response arm re-reads before offering a distinct resubmit.
17. `firm-setup-a11y.test.tsx` / `firm-setup-keyboard.test.tsx` — copied from `onboarding-checklist-{a11y,keyboard}.test.tsx` and `interview-run-{a11y,keyboard}.test.tsx`: axe, focus return after each step and after the Dialog, one announcement owner per transition.

Playwright:

18. `apps/web/e2e/firm-setup-walk.spec.ts` + `firm-setup-mock.mjs` — claim landing → home tile → `/settings/setup` → answer one single-`Field` fact and one bounded related set through the stepper → **full reload resumes without re-asking accepted facts** → a second browser context answers the same item and the first context's submit converges to the stale face **with its draft intact** → finish the required set → commit → confirmed facts render scope/source/actor and the tile clears → axe WCAG 2.1 A/AA, one 320px pass, one 200%-zoom pass, Back to firm home with the section URL stable. Ports 3290/3291/3292.

## 5. Risks

**Seeding** — two arrival states; reconcile or collide (`0017:1056`). **Live-door fix** — the new partial index changes `claim_paid_firm`'s resolution; prove no firm violates it in the tail. **Cross-ticket key set** — #654's 0205 adds the eligibility wall and the private-evidence wall, neither of which exists on your rig; your capture face must already refuse everything outside the three eligible keys **and** map #654's two `CLR10` reasons (`knowledge_scope_not_firm_defaultable`, `firm_scope_client_evidence`) as designed faces. **Never cite a client-filed document as a firm-scope source.** **Append-only catalogs** — mint no key and no map row; if you believe you need one, stop and report (`kp.01` moves in #654's file only). **Unexercised door** — `capture_knowledge` has no caller today; every refusal needs a face. **Frozen** — `firmInterview.v1/v2/v3`, every `interview.v*` and `lib/knowledge.mjs` are `deployed:true`, and `scripts/check-frozen-workflows.mjs:8-9,110-113` locks each body **and its relative import closure**; the only genuine zero-freeze precedent is #640's `plan-occurrences.mjs` — `periodic-adjustment-basis.ts` is **not** one (`frozen-workflows.json:54-58`). **Census reds on files you never touched** (a new `app/**` file, a new href, a new SQL function, a new mocked RPC verb). **Named residuals owed:** activity kind mis-filing (D13); no optional-education content; firm identity facts have no knowledge record.

**Merge risks / the exact shared-file one-liners this ticket adds:**

1. `apps/web/lib/navigation/tree.ts` — one `SETTINGS_SECTIONS` row `{ id:"setup", href:"/settings/setup", labelKey/purposeKey "sections.setup.*", icon, minimumRole:"admin" }` after the `members` row (`:269`). #625 and #654 edit the same literal.
2. `apps/web/lib/require-firm-scope.ts` — one `SCOPE_ENTRANCES` row (`:253`) for `/settings/setup`. A firm-scoped entrance belongs in **neither** `SCOPE_UNSCOPED_SURFACES` (`:288`) **nor** `SCOPE_EXEMPT_SURFACES` (`:477`). #633 adds its own leaf here.
3. `apps/web/messages/en.json` — one `FirmSetup` namespace.
4. `apps/web/test/manifest.txt` — one line per new web test file, plain-string alphabetical position (the runner does not directory-scan).
5. `apps/web/e2e/serve-built.mjs` — one mock registration hook.
6. `apps/web/e2e/e2e-fixture-ownership.test.ts` — one ownership row declaring every RPC verb your mock answers (`get_firm_setup`, `seed_firm_setup_plan`, `answer_firm_setup_item`, `defer_firm_setup_item`, `commit_firm_setup`, `capture_knowledge`, `caller_context`); `caller_context` is answered by other lanes — declare it **shared**.
7. `packages/db/tests/rig-meta.mjs` — one cohort row for `firm-setup.test.mjs`.
8. `packages/db/package.json` — one `--import ./tests/<0203 gate>.mjs` flag in the `test` gate chain, plus the gate module itself.
9. `CONTEXT.md` — two new terms + one boundary sentence.
10. `apps/web/tests/firm-scope-db-pins.corpus.ts` — one entry **only if** you add dynamic SQL, at the sorted position with a ≥40-character reason; sequence at merge prep.
11. `.github/actions/db-live-gates/action.yml` — **no row.** No Workflow, no World leg.

## 6. Effort and rig

**XL.** Four human doors on a plan model that has never had one, a read, a catalogue, a live-door correctness fix, a settings section built from zero (no manual-capture UI exists at any scope to adapt), fifteen real-role DB cells, three web unit files and a full walk.

**Rig:** worktree `C:\Users\zhant\Desktop\clara-wt\648`, branch `impl/648-firm-setup`, PG `127.0.0.1:55507`, db `clara_648`, base `4464e471`. Node 22 via `export PATH="/c/Users/zhant/AppData/Local/pnpm:$PATH"`; PG env per `RIG.md`; **never** set `CLARA_RIG_ALLOW_RESET=1` or `CLARA_RIG_ALLOW_ROLE_SWEEP=1`. **Port triple 3290/3291/3292.**

**Before the final report, run and report counts:** `pnpm typecheck` and `pnpm lint` (worktree root, green); the **whole `apps/web` unit suite** once (`node scripts/run-tests.mjs` from `apps/web`, ~5 min — `firm-scope-fourth-entrance`, `firm-scope-surfaces`, `parity-holes`, `sql-oracle`, `e2e-fixture-ownership` red on files you never touched); the DB battery with the exact 28 `--import` gate flags copied verbatim from `packages/db/package.json`'s `"test"`; because this slice adds SQL functions, `packages/db/tests/operation-census.test.mjs` and `rig-isolation.test.mjs` (**without** the reset flags — the destructive cell skips); the Playwright walk on your triple. **Parts-parity does not apply** (nothing under `packages/runtime` changes), but run `node scripts/check-frozen-workflows.mjs` once and report it — it is the first claim a reviewer checks. Known Windows reds you must not "fix": #707, #693. Write "hosted evidence pending".
