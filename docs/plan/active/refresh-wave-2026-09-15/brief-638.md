# Brief: #638 — staff expense claims, employee payables and advance settlement

## Orchestrator decisions (binding)

- **Migration 0206 only** (`0206_staff_expense_claims.sql`), DECISIONS §1.2. One file, house header, tail assertions, a `tests/staff-expense-claim-preintegration-gate.mjs` module registered in `packages/db/package.json`'s gate chain (the 28th is `firm-document-limits-preintegration-gate.mjs`), `rig-meta.mjs` cohort rows. A released number is a gap, never renumbered.
- **You are the spine head. #638 → #652 → #653 is serialised** (SYNTHESIS §2.3, §4 Group 3): 0207 and 0208 write their prestates against *your committed bytes*. Keep 0194's delimited, commented insertion-point idiom (`0194:1327-1331`, `0195:1929-1935`) verbatim in anything you recut so they re-derive rather than re-type. Do not read their branches; do not wait for them.
- **Purpose spine (lands whatever the owner rules on the advance arm).** Widen `accounting_work_purpose_check` and `operation_receipts_purpose_check` with `staff_expense_claim` (`0194:228-233` verbatim shape); widen `_admit_accounting_work_core`'s closed purpose set (`0194:1080-1082`, re-pin); **recut `ck_accounting_work_adjustment_basis`** (`0194:244-247`) so this purpose carries **NULL** `adjustment_basis` — today that CHECK reads `(purpose = 'journal_entry' and adjustment_basis is null) or (purpose <> 'journal_entry' and adjustment_basis is not null …)`, so a claim row **cannot be inserted** without the recut (SYNTHESIS §2.3 correction; gap-638 does not name it). These two are DECISIONS §1.3's authorised list for #638 — they are the *whole* of it.
- **No new column on `accounting_work`** (DECISIONS §2 #638 — overrides gap-638 §B's `claim_basis` column and its `_tf_accounting_work_immutable` recut). Particulars live in `clara.staff_expense_claims` (unique `work_id`, append-only status ledger), **written inside the admission transaction**; the posted entry and receipt are derived by join (`work_id` → `operation_receipts.effects->>'entry_id'`, indexed `0178:451`, `:454`). `0195:2165` INSERTION 5 stays untouched — its `if w.adjustment_basis is not null` guard is a no-op for a claim. **Do not recut `_tf_accounting_work_immutable`** (DECISIONS §1.3).
- **The posting core is BLOCKED by DECISIONS §1.3. Measure first; then Track A or Track B — never a recut on this brief's authority.** §1.3 reads "No ticket recuts `clara._record_journal_entry_core` …" and names #638's authorised shared-body recuts exhaustively as `_admit_accounting_work_core` (purpose set) + `ck_accounting_work_adjustment_basis`. SYNTHESIS §2.2's older row assumed a fifth core recut; **DECISIONS supersedes it and this brief does not authorise one.** Repo text says the claim cannot post without it: the core filters `aw.purpose in ('journal_entry','periodic_stock_adjustment','payroll_obligation')` (`0195:1711`) → `work_not_found`; INSERTION 1 calls `_assert_adjustment_basis(w.purpose, w.adjustment_basis)` (`0195:1933`), which raises CLR10 `invalid_purpose` for any fourth purpose (`0194:587-589`); INSERTION 2 calls `_assert_adjustment_relationships` (`0195:1990`), which early-returns only for `journal_entry` (`0194:797`).
  **Your first act, before one line of SQL:** measure those three live bodies on your rig (`sha256(prosrc)` from `pg_proc`), confirm or refute the three facts, and report the measurement to the orchestrator immediately — first commit message plus the top of your report. The §1.3 amendment is the orchestrator's to make, not yours.
  **Track A — only with a written DECISIONS §1.3 amendment naming `clara._record_journal_entry_core` in #638's list.** Then exactly ONE body moves, with the smallest diff that exists: (i) `staff_expense_claim` added to the IN-list at `0195:1711`; (ii) INSERTION 1 (`0195:1933-1934`) and INSERTION 2 (`0195:1990-1991`) wrapped in the body's own `if w.adjustment_basis is not null then … end if;` guard — the identical idiom already live twice in the same function (`0195:2126`, `0195:2165`). That guard is behaviour-preserving for the three existing purposes (`journal_entry` early-returns inside both validators; both typed purposes carry NOT NULL particulars by `ck_accounting_work_adjustment_basis`) — **prove it by re-running the whole #643 battery unchanged and green**. The two `_adjustment_*` validators are **not** recut on either track.
  **Track B — no amendment.** Everything else in this brief still ships: both CHECKs, the purpose set, the claim relation, the doors, the enrolment handle, the runtime module and route, the whole web lane, the question design, the vocabulary fix. A claim Work then admits, carries every particular and reads back — and cannot post. Mark AC2's "balanced journal … atomically" and AC4's receipt half **descoped by rule, by name**, with cell 2 standing red as the evidence, and record in the report that the block is one token and two guards wide.
  **Admission owes the validators nothing on either track:** both calls sit *inside* `_admit_accounting_work_core` (`0194:1149`, `0194:1160`) — a body you ARE authorised to recut — so route `staff_expense_claim` to your new `_assert_claim_basis` there and leave the `_adjustment_*` pair alone.
- **Pins are MEASURED on your migrated rig, never transcribed** (DECISIONS §1.2; SYNTHESIS risk 7 — several live bodies are splices). Pin by pre-image `sha256(prosrc)` from `pg_proc`: `clara._admit_accounting_work_core(uuid,uuid,text,text,jsonb,jsonb,text,jsonb,text)` (**recut pin**); `clara._record_journal_entry_core(uuid,uuid,text,uuid,uuid,text,jsonb,text,text,text)` (**recut pin on Track A, non-regression pin on Track B**); `clara._assert_adjustment_basis(text,jsonb)` and `clara._assert_adjustment_relationships(uuid,text,jsonb,jsonb,boolean)` (**non-regression pins — you edit neither**). `admit_journal_work`'s 7-argument signature must not move; `_document_posting_entry` is out of scope.
- **D4 — the claimant is a staff-advance enrolment handle** (`clara.staff_advance_accounts.id`, label + optional identifier), never a vendor counterparty. The claim door auto-enrols a new claimant *inside* the admission transaction through a definer-internal write — **never** by calling the admin-floored `clara.enrol_staff_advance_account` (`0043:1946`, `_human_ctx(role_rank('admin'))`, WDB-G6). Re-derive that door's walls into the branch: the account exists in `coa_accounts`, `confirm_dedicated` acknowledged, non-blank attestation, `_adv_enrolment_admission`. **Fallback:** if the branch cannot honestly supply one of those facts, require an existing active enrolment, refuse `claimant_not_enrolled` naming the enrol route, and mark that half partial by name.
- **D5 — advance application posts in the Work lane through a lane-agnostic birth trigger** (DECISIONS §1.4), **never** by making the posting core a fifth caller of `clara._subledger_on_approve` (`0037:3840-3845` pins the roster at four). A deferred constraint trigger on `clara.journal_entries`, named to sort **before** `t_je_adv_movement_belt` (`0043:3176`, `deferrable initially deferred`, `when (new.status='approved')`), registers the movement / mints the `staff_advance_applications` allocation idempotently (`on conflict … do nothing`) from the claim row it reaches by join. **Prove the firing order with a cell.** Re-derive and re-assert the caller census at its unchanged cardinality in your tail. **Fallback:** descope `settlement='advance_application'` to `clara.book_staff_advance_application` (`0043:2513`) and mark AC2's "atomically" unmet **for that arm by name**. (On Track B the arm is unreachable anyway — say it once, not twice.)
- **Tax facts are opaque supplied particulars** — carried and echoed, never validated (no `tax_code` vocabulary exists; `0150:525` calls the statutory tag a hint; PRD:124). **No second human on the Work lane** (PRD:114); pin it with a cell and record `is_high_stakes`'s non-amount axes (`0004:72-78`) as a ticket finding, not a blueprint edit.
- **Questions:** refuse **before admission** for anything the basis needs (claimant, incurred date, items) — #721's ruling, `brief-643.md` §1. A parked `0180` question is reserved for a fact only the run discovers, and ships as successor contract. **Per-item continuation is designed inside one claim** — #636 defers batch aggregation across works (PRD:118), not this.
- **You own the Work purpose vocabulary fix, four surfaces, once**, and you carry #643's two purposes with it: `lib/work/purpose-label.ts:20-24` SUFFIX, `accounting-work-list.tsx:114` `KNOWN_PURPOSE_LABELS` (`new Set(["journal_entry"])`, stale since #643), `work-list-filters.tsx:55` `KNOWN_PURPOSES` (same), `messages/en.json`. Also fix `tree.ts:344-372`'s stale "one-member CHECK" comment.
- **No `_vN` successor** (WORK-ORDER rule 8). Chat tool `start_staff_expense_claim_work` and the `WORK_ACCEPTED_PURPOSES_V19` widening (`chatTurn.v19.parts.ts:91`) are **successor-contract stanzas only**; a claim Work has no nameable chat `work_accepted` part until `chatTurn_v20`. Say so.
- **Do not touch:** `clara.periodic_adjustments`' purpose CHECK; `counterparties_kind_check` / `clara.open_items` (0042 tail 20(a)/(b), re-asserted by 0043/0044/0045 — assert both in *your* tail); the activity kind ladders (D13 — record a named residual); `lib/firm/capabilities.ts` (#625 owns; read `firmCapabilities`); `client-workspace-overview.tsx` layout (#650); `StateBanner` (no rebuild); `knowledge_keys` (#654); `useUploadQueue.ts`/`intake.ts`/`InterviewRunCard.tsx` (#633); `documents_document_kind_check` (`0165:95`/`0191:142`) and `document_capabilities.registry_version`. No eleventh `needs-you.ts` row kind. **Never edit `docs/PRD.md` or `docs/ARCHITECTURE.md`** — contradictions go under "blueprint drift".
- **Rig row:** worktree `C:\Users\zhant\Desktop\clara-wt\638`, branch `impl/638-staff-claim`, port **55503**, db **clara_638** (193 migrations applied, PG 17.11). **Playwright port triple:** `CLARA_E2E_APP_ORIGIN=https://127.0.0.1:3250`, `CLARA_E2E_NEXT_PORT=3251`, `CLARA_E2E_RUNTIME_PORT=3252`.

## 1. Current state

**db.** `accounting_work`/`operation_receipts` carry the three-value purpose CHECK (`0194:228-233`) and the frozen `adjustment_basis` column with its NULL-iff-`journal_entry` CHECK (`0194:243-247`); admission runs through `_admit_accounting_work_core` (`0194:1062`) behind `admit_journal_work` (`:1281`) and `admit_periodic_adjustment_work` (`:1298`), checking the closed purpose set (`:1080-1082`), then `_assert_adjustment_basis` (`:1149`), then `_assert_journal_basis`, then `_assert_adjustment_relationships(…,false)` (`:1160`) before any durable write. Posting is `_record_journal_entry_core`, live fourth copy at `0195:1685`; it refuses any payable/receivable-class leg (`0195:2021-2028`) and flips a draft to approved with a bare UPDATE (`0195:2098-2112`) — so `_subledger_on_approve` never runs here, while the advance belt is a constraint trigger firing on **every** approved row (`0043:3176`): a debit meets CLR40 `advance_movement_unregistered` (`0043:3164-3172`), a credit `advance_application_missing` (`0043:3158-3165`). One `logical_op_id` already keys both receipt families (`0195:1963`, `0178:448`) — one-receipt-per-act is inherited, not to be rebuilt. `operation_receipts` carries `work_id`, `purpose`, `outcome`, `effects` (`0178:411-433`) and today holds only append-only / no-truncate triggers (`0178:465-467`). The staff-advance family is complete and governed (`0043:335`, `:415`, `:535-544`, `:631-663`; doors `:1946`, `:2089`, `:2188`, `:2513`; reads `:3200`, `:3269`, `:3366`; review row kind `:3634`). Two hard walls: no employee/staff counterparty (`0042:6832-6840`), no advance concept in `open_items` (`0042:6815-6830`). **No claim object exists anywhere.** `claim_form` is codeable (`0165:216-217`) but levelled `stored_only` (`0191:329,344-358`).

**runtime.** `workRoutes.ts` carries `POST /api/work/journal` (`:614`) and `POST /api/work/periodic-adjustment` (`:701`) — your route is a literal sibling of the latter. `claraWork_v3` reads `basis` off the Work row and nothing else (`claraWork.v3.impl.ts:330`); its tools are a fixed three-tool literal (`claraWork.v3.tools.ts:275-307`); `capability-registry.mjs:80-90` is purpose-blind. `packages/runtime/lib/periodic-adjustment-basis.ts` is **frozen, `deployed:true`** (`frozen-workflows.json:54-57`) — clone its shape, never edit it.

**web.** Composition to clone: `accounting/adjustments/new/page.tsx` + `periodic-adjustment-form.tsx` + `periodic-adjustments-table.tsx` + `lib/work/periodic-adjustment*.ts`, declared at `tree.ts:328`, `:377-380`. The advance surface is complete at `registers?tab=staffAdvances`, reachable only through `registers-workbench.tsx:28`. C6 aging is AR/AP-only by construction (`aging-register.tsx:27-40`). Persistent-outcome discipline is in-component (`staff-advances-register.tsx:52-70`). **Zero Playwright coverage of `?tab=staffAdvances`** (30 specs, no match).

## 2. Gaps / rows

**AC1 partial → build.** Nothing carries claimant, incurred-vs-posting date, itemised lines or tax facts (`0178:693-790` is the whole basis shape today). "Do not invent" becomes a *stated* refusal, not absence.
**AC2 gap → build; its posting half is Track-gated and its advance arm conditional.** Claim object + balanced journal + employee-payable leg atomically. The employee payable is a non-control liability leg (`2010 Other Payables`, `account_class=null`, `0150:1503`) plus the claim register — an open item is structurally forbidden (four live tail assertions). `reimbursement` and `already_settled` are unconditional **once the core admits the purpose**; `advance_application` additionally rides the birth trigger with the named fallback.
**AC3 partial → build.** The question machinery is complete (`0180:308-404`, `:578`, `:761`, `:900`); the producer is missing. Ask before admission; design per-item continuation; prove "no redundant approval" with a cell.
**AC4 partial → build.** One-receipt-per-act is structural (`0195:1963`); build the claim's unwind (reversal + linked correction claim + the hook-born `kind='correction'` advance row, `0043:944-947`). Its receipt-bearing arms are Track-gated with AC2.
**AC5/AC6 partial → build** the claim faces; the advance faces are verify-only. **AC6's date half is `verify_only` — reviewed no-gap** (C08.9).
**AC7 partial → build** one Playwright walk for the claim lane with every state.
**AC8 partial → build** a DB battery through least-privileged personas + a standalone real-World e2e **wired** at `.github/actions/db-live-gates/action.yml:173`. Hosted evidence pending.
**C08.9 `verify_only`** — record the reviewed no-gap: the caller states the date by contract (`0043:2513`), a null date refuses with `axis:posting_date` (`:2528-2533`), `business-date.ts:16-34` renders MYT. Add the two regression cells; **do not move date ownership into the DB**.
**C55.23 gap → build** — the reframed obligation is AC1+AC3's; never revive it as a fix-queue trigger.
**Out of scope, with authority:** posted-effect correction integration (#676, D6); batch aggregate progress (#636, PRD:118); activity kind ladder (D13); chat entrance (WORK-ORDER rule 8 → v20); tax vocabulary (PRD:124).

## 3. Slice (one branch)

**Migration `0206_staff_expense_claims.sql`.** §0 PRESTATE: the four measured sha pins above (each labelled recut or non-regression); assert the advance relations exist, no claim relation/door exists, both purpose CHECKs are at the 0194 three-valued form, `ck_accounting_work_adjustment_basis` is at its 0194 text, and the `_subledger_on_approve` caller census is four. §A both purpose CHECKs widened. §B `ck_accounting_work_adjustment_basis` dropped and re-added: NULL for `journal_entry` **and** `staff_expense_claim`, NOT NULL object otherwise. §C `clara.staff_expense_claims` — FORCE RLS, owner + `firm_id = clara.jwt_firm()` read, zero app-role DML, append-only + no-truncate (the `0194:333-460` idiom), unique `work_id`, carrying `logical_op_id`, the claimant enrolment id, source kind/document/instruction, `incurred_date`, items jsonb (description, expense account code, cents, optional opaque `supplied_tax`), `amount_cents`, `currency`, typed `settlement`, payable/advance/payment account codes, `advance_id`, `corrects_claim_id`/`corrected_by_claim_id`, `recorded_by`, `on_behalf_of` — plus the append-only **status ledger** child keyed on claim id. §D `clara._assert_claim_basis` (field-scoped like `_assert_adjustment_basis`, `0194:573`) and `clara.admit_staff_expense_claim_work(...)` delegating to the recut `_admit_accounting_work_core` (which routes this purpose to `_assert_claim_basis` and past the two `_adjustment_*` validators), writing the claim row + the auto-enrolment in the same transaction. **That INSERT is the claim's one and only birth** — unique `work_id`, append-only. §E (**Track A only**) the single guarded core recut described above: it **inserts nothing into `clara.staff_expense_claims`** and reads no particulars off the Work row; it only lets a claim Work be found and posted. The status ledger's `posted` transition is appended by a NEW AFTER INSERT trigger on `clara.operation_receipts` for `outcome='committed' and purpose='staff_expense_claim'` (`0178:411-433` gives it `work_id`; only append-only/no-truncate triggers live there today, `0178:465-467`) — never by an edit to the core; **fallback** if the rig shows a wall: derive the posted state by join at read time and say so. §F the deferred birth trigger + its census re-derivation. §G typed refusals with `detail.reason`: `claimant_missing`, `claimant_not_enrolled`, `incurred_date_missing`, `incurred_after_posting`, `item_account_not_expense`, `items_do_not_sum`, `claim_all_zero`, `payable_account_is_control` (reuse `generic_control_leg`), `advance_not_enrolled`, `advance_allocation_mismatch`; inherited CLR04/CLR19/CLR40. §H reads `list_staff_expense_claims(p_client,p_from,p_to)` + `get_staff_expense_claim(p_claim)`. §I TAIL: 0042 tail 20(a)/(b) re-run live, the hook caller census re-asserted, the trigger's firing position asserted, `documents_document_kind_check` and `registry_version` unmoved. Lock order: `_reserve_op` **before** the client rung, matching `0043:2605-2618`, inside the wave order `accounting_plans → accounting_work → agent_tasks → agent_interruptions`. Integer cents, MYT calendar days.

**Runtime.** `POST /api/work/staff-expense-claim` as a literal sibling of `workRoutes.ts:701` (same closed-set validation, 202 + replayed/enqueue contract, `sendAdmissionError` classification). New non-frozen module `packages/runtime/lib/staff-expense-claim-basis.ts` on `periodic-adjustment-basis.ts`'s shape: `.strict()` zod per settlement, `claimFromInput`, `basisFromClaim`, `localClaimRefusal`; header states it is non-frozen **only until the shared successor imports it** (IMPORT-ESCAPE). New standalone `tests/staff-expense-claim-e2e.mjs` on the real Postgres World (skip-clean without 0206; SIGKILL between commit and checkpoint via `CLARA_WORK_TEST_FAULT=exit_after_commit`, the barrier at `claraWork.v3.tools.ts:265-266`, spawn idiom `periodic-adjustment-e2e.mjs:562`), **registered in `action.yml` after the periodic-adjustment block (`:173`)**; under Track B its posting legs assert the `work_not_found` refusal instead and the file header says so. No edit to `claraWork.v3.*`, `capability-registry.mjs`, `work-trace.mjs` or `periodic-adjustment-basis.ts`.

**Web.** Leaf `/clients/:clientId/accounting/claims/new` (bookkeeper floor, 裁-187 — DENIED, not blank) + history `/clients/:clientId/accounting/claims` (viewer): one `ACCOUNTING_ITEMS` row, one `CLIENT_LEAVES` row, one href builder beside `periodicAdjustmentHref` (`tree.ts:328`, `:377-380`). The claim register lives on its own leaf (the advance tab is deliberately sidebar-unreachable, `tree.ts:335-339`) and the advance tab carries at most a link back. Form: `JournalBasisFields`, `evidence-chooser.tsx`, `lib/work/journal-draft.ts`'s intent key + lost-response arm, a Radio Group settlement switch preserving typed values across switches (appendix D #46), an itemised-lines editor on `staff-advance-lines-editor.tsx`'s shape, Field/FieldGroup for every new input (#28), the Date Picker recipe keeping the exact ISO date for incurred **and** posting dates (#22, labelled distinctly), editable-and-shown account defaults, `fieldForServerPath` mapping every server refusal to a focusable control. History: Data Table + Table (#21/#57) with exact cents, claimant, incurred vs posting date, settlement, source, links to Work / entry / receipt / advance statement, and the two-way correction chain. C1: the attachment is genuinely optional, and the document's own detail reaches the claim Work through `clara.entry_evidence_links` read **directly** under RLS (DECISIONS §1.7 — never wrapped). C6: the claimant's advance lineage renders through the shipped statement panel; no employee payable ever appears in `aging-register.tsx`. Persistent outcome only — StateBanner, never a toast alone. States: loading / successful-empty / no-results-with-clear-filters / partial-stale / invalid-saving / denied / failed / cancelled-recovery; 320px, 200% zoom, keyboard + focus return, SR names, reduced motion, stable URL + Back, drafts preserved and never crossing clients.

**Docs.** `apps/web/README.md` Client row gains `/accounting/claims` (+`/new`) beside the #640/#643 entries; `packages/runtime/README.md` "### Standalone e2es" gains the new e2e and its skip-clean condition; `packages/db/tests/README.md` the new battery; `CONTEXT.md` three terms beside **Periodic adjustment** — **Staff expense claim** (*Avoid:* a balanced journal with a memo; a payroll run; an employee master record), **Employee payable** (*Avoid:* an AP open item; a counterparty), **Advance application** (*Avoid:* a silent FIFO; a GL credit with no named advance) — plus the named limit that the claimant handle is an enrolment, not a person record.

**Successor contract stanza** (final report): tool name `start_staff_expense_claim_work`, the `.strict()` zod input schema, the door call and argument order, the refusal→message map, the part kind, and the `WORK_ACCEPTED_PURPOSES_V19` widening (`chatTurn.v19.parts.ts:91`, pinned by `p6-1-parts-parity.test.mjs:548`).

## 4. TDD seams (red first)

DB cells live in `packages/db/tests/staff-expense-claim.test.mjs`, named `p638.<area>.<case>`, driven through `humanQuery` personas at the least privilege that should succeed — never `rootQuery` for the assertion under test.

1. `p638.spine.check` — insert a `staff_expense_claim` Work with NULL `adjustment_basis`. **Red: 23514 `ck_accounting_work_adjustment_basis`.**
2. `p638.spine.core_finds_work` — admit, claim, `wake_record_journal_entry`. **Red: the posting verb answers `work_not_found`** (`0195:1711`); a sibling arm reds on `_assert_adjustment_basis`'s `invalid_purpose` (`0194:587-589`). **This cell is also the Track B evidence**: absent the §1.3 amendment it stays red, is reported red by name, and is the measured proof of the block.
3. `p638.core.no_regression` (**Track A**) — the whole #643 battery (`periodic-adjustment.test.mjs`, `work-journal-post.test.mjs`) re-run unchanged and green after the guarded recut, plus an explicit cell that a `journal_entry` and a `periodic_stock_adjustment` still post identically.
4. `p638.reimbursement.happy` (`interactive_client` OBO the initiator) — exactly one approved entry, one `operation_receipts` row `purpose='staff_expense_claim'`, one `staff_expense_claims` row with exact cents and itemised lines (born at admission, unchanged by posting), one status-ledger `posted` row, `accounting_work.result.entry_id` matching, **and one `op_receipts` row under the same `logical_op_id`**.
5. `p638.advance.debit` — a Work-lane entry debiting an enrolled advance account. **Red: CLR40 `advance_movement_unregistered`** (no existing cell covers this direction; `periodic-adjustment.test.mjs:301-324` pins the credit only).
6. `p638.advance.order` — the new trigger fires **before** `t_je_adv_movement_belt`: the belt stays silent, plus a `pg_trigger` name-order assertion.
7. `p638.advance.happy` / `p638.advance.race` — `settlement='advance_application'` mints `kind='claim'` rows and outstanding moves by exactly the allocated cents at the effective date; two concurrent sessions over `_adv_over_application`'s boundary walk (`0043:1220`) under the client rung. Both descoped under the D5 fallback or Track B.
8. `p638.claimant.autoenrol` / `p638.claimant.floor` — a bookkeeper's claim auto-enrols a new claimant with every enrol wall satisfied, **and the same bookkeeper is still refused by `enrol_staff_advance_account` directly** (CLR04).
9. `p638.items.continue` — one item missing its incurred date waits alone while the independent items post; the status ledger shows both.
10. `p638.refusals` (table-driven) — `claimant_missing`, `incurred_date_missing`, `incurred_after_posting`, `items_do_not_sum`, `claim_all_zero`, `payable_account_is_control`, `advance_not_enrolled`, CLR19 on a seeded closed FY (copy `work-journal-post.test.mjs:517-527`), CLR04 `obo_not_initiator`; entry and committed-receipt counts unmoved in every arm.
11. `p638.no_second_approval` — a claim above `firms.high_stakes_amount_cents` posts in one transaction with no checker ceremony; the cell's comment records that `0043:2666`'s drafted branch is deliberately not inherited and that `is_high_stakes` also fires on non-amount axes.
12. `p638.walls` — 0042 tail 20(a)/(b) live; a payable-class leg still refuses `generic_control_leg`; the hook caller census still four.
13. `p638.correction` — post, `reverse_entry`, admit the corrected claim: two rows linked both ways, the `kind='correction'` advance row hook-born, the document released by `t_entry_evidence_release`, two committed receipts under two logical identities.
14. `p638.rls` — role posture and RLS on the new relations + a `rig-meta.mjs` cohort entry beside `ADV_0043_*` (`:772-796`).
15. **Runtime** — `staff-expense-claim-unit.test.mjs` (red on the absent module's schemas); `staff-expense-claim-e2e.mjs` asserting exactly one entry / receipt / claim row / advance application after kill-and-resume, plus a revoked-authority replay leg.
16. **Web unit** — `staff-expense-claim-form.test.tsx`: server field errors reach focusable controls; itemised-sum mismatch and stale basis render as `StateBanner`; switching settlement preserves typed values. Plus a purpose-label cell proving all four surfaces label and filter the three non-journal purposes.
17. **Playwright** `apps/web/e2e/staff-expense-claim-walk.spec.ts` + its mock, on `periodic-adjustment-walk.spec.ts`'s shape: the full state matrix, 320px, 200% zoom, keyboard + focus return, SR names, reduced motion, stable URL + Back, preserved draft, axe. The first walk that opens `?tab=staffAdvances` is a **follow-up**, not this ticket.

## 5. Risks

1. **The §1.3 gate is the ticket's critical path.** Measure and escalate in your first hour, then build every Track-independent part while the ruling is outstanding (WORK-ORDER rule 6 — do not stop; take the conservative reading and record it). Never cut the core without the written amendment, and if it comes, never let the diff grow past the IN-list token and the two NULL guards — #652/#653 must re-derive whatever you leave behind.
2. **The prestate chain.** Your committed SQL is #652's pre-image. A late re-edit of 0206 after 0207 is authored breaks it loudly; freeze your bytes and say so in the report. If the amendment lands late, the core recut belongs in 0206 itself — never a second file, never an edit to a merged migration.
3. **Trigger firing order is by name** and is easy to get wrong silently; cell 6 exists for that.
4. **Auto-enrolment crosses an admin floor.** If the branch cannot re-derive the enrol door's walls honestly, take the fallback rather than a weakened attestation.
5. **The WDB-G7 wall is unrecoverable** — assert it in your own tail, never trust review.
6. **Shared-file one-liners** (keep each to one line at its sorted position): `tree.ts` (one `ACCOUNTING_ITEMS` row `staffExpenseClaims`, one `CLIENT_LEAVES` row `staffExpenseClaim`, one href builder); `messages/en.json` (the claim namespaces + `links.purposeStaffExpenseClaim` and the two #643 keys); `apps/web/test/manifest.txt` (one line per new web test, alphabetical plain-string compare — an unregistered file silently never runs); `apps/web/e2e/serve-built.mjs` (one mock hook); `e2e-fixture-ownership.test.ts` (declare `admit_staff_expense_claim_work`, `list_staff_expense_claims`, `get_staff_expense_claim`; declare `list_accounting_work` and `staff_advance_summary` as **shared** verbs); `packages/db/tests/rig-meta.mjs` (one cohort block); `packages/db/package.json` (one gate import); `.github/actions/db-live-gates/action.yml` (one block after `:173`); `CONTEXT.md` (three terms). `apps/web/tests/firm-scope-db-pins.corpus.ts` only if you add dynamic SQL — sequence it at merge prep, after the migration bytes are final.
7. **Census suites red on files you never touched** — a new file under `app/**`, a changed href, a new SQL function or a new RPC verb answered by a mock.
8. **Today's workaround pollutes the vendor master** (`x37-wave-c-a-subledger.test.mjs:1998`) — an argument for shipping the lane, and a reason your tail must prove you widened neither `counterparties_kind_check` nor `open_items`.

## 6. Effort and rig

**XL** (Track B trims the posting legs, not the lane). Rig: worktree `C:\Users\zhant\Desktop\clara-wt\638`, branch `impl/638-staff-claim`, PG port **55503**, db **clara_638**; Playwright triple **3250/3251/3252**; a built runtime + real Postgres World for the e2e leg. Siblings to copy, by path: `docs/plan/active/refresh-wave-2026-09-14/brief-643.md` + `reports/643-final.md` (the 0194 shape you extend) and `brief-640.md` + `reports/640-review-closure.md` (what a reviewer later demanded: every finding closed by a cell that would go red, a measured merge-order table, evidence re-measured rather than copied). Before the final report: the whole `apps/web` unit suite (`node scripts/run-tests.mjs` from `apps/web`, ~5 min — report counts); `packages/db/tests/operation-census.test.mjs` and `rig-isolation.test.mjs` (no reset flags); `check-parts-parity.mjs` and `check-frozen-workflows.mjs` (you add a module no frozen body imports — prove it); `pnpm typecheck` and `pnpm lint` green from the worktree root. Label everything **local evidence; hosted evidence pending**.

## Verifier findings not applied

None — all three are applied. Finding 1's remedy is partly the orchestrator's: the DECISIONS §1.3 amendment naming `clara._record_journal_entry_core` in #638's authorised list is owed **before** Track A may be taken, and is written above as a gate rather than as a worker-side authorisation.
