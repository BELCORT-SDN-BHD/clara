# Brief: #638 — staff expense claims, employee payables and advance settlement

## Orchestrator decisions (binding)

- **Migration 0206 only** (`0206_staff_expense_claims.sql`), DECISIONS §1.2. House header, measured prestate pins, tail assertions, a `tests/staff-expense-claim-preintegration-gate.mjs` in `packages/db/package.json`'s gate chain (the 28th is `firm-document-limits-preintegration-gate.mjs`), `rig-meta.mjs` cohort rows. A released number is a gap, never renumbered. All twelve branches run in parallel (DECISIONS §1.3): you wait for no one, and no sibling pins your bytes.
- **#638 recuts NOTHING shared** (DECISIONS §1.3 as amended 2026-09-15; §2 #638 first bullet). No new `accounting_work.purpose`; no CHECK moves; no edit to `_admit_accounting_work_core`, `_record_journal_entry_core`, `_tf_accounting_work_immutable` or the `_adjustment_*` validators; **no new column on `accounting_work`**. A fourth purpose cannot post — the posting core's Work lookup is a closed IN-list (`0195:1711`) and INSERTION 1/2 call validators raising `invalid_purpose` for anything outside the three (`0194:587-589`, `:797`). So **a staff expense claim is a `journal_entry`-purpose Work** with `adjustment_basis` NULL, which the live core already finds, posts, and correctly skips INSERTION 5 for (`0195:2165` is guarded by `if w.adjustment_basis is not null`). Everything new is a new relation, a new door, new guard functions, new triggers, new reads.
- **Typed particulars live in `clara.staff_expense_claims`** — unique `work_id`; tenant-carrying composite FKs (`(client_id, firm_id) → clara.clients(id, firm_id)`, the `0178:429-433` shape); FORCE RLS, owner policy + `firm_id = clara.jwt_firm()` read, **zero app-role DML**; append-only + no-truncate (`0194:333-460` idiom); plus an append-only **status-ledger child** keyed on claim id. Written **inside the admission transaction**, never by the posting core.
- **The door.** `clara.admit_staff_expense_claim_work(p_client, p_author, p_intent_key, p_claim, p_basis_origin, p_source_refs, p_model)` on `admit_periodic_adjustment_work`'s OBO shape (`0194:1298-1314`): `security definer set search_path = clara, pg_temp`, actor-explicit (`p_author` is the authorising human; membership, role ≥ bookkeeper and client status are rechecked inside the core), `revoke all … from public`, `grant execute … to clara_runtime`. **The human form goes through the runtime route family** — `POST /api/work/staff-expense-claim`, a literal sibling of `workRoutes.ts:701` — **not** a `clara_authenticated` twin: Work admission here is a `clara_runtime` act OBO a named human (`0194:1316-1318` grants exactly that), and a second door with a different authority model would be a second answer to "who admitted this". Body order: `_assert_claim_basis` → derive the balanced basis → auto-enrol a new claimant → call the **unchanged** `clara._admit_accounting_work_core(p_client, p_author, p_intent_key, 'journal_entry', v_basis, null, p_basis_origin, p_source_refs, p_model)` (`0194:1062`) → insert the claim row on the returned `work_id` with `on conflict (work_id) do nothing`, so the core's replay branch converges instead of raising → return the core's answer with `claim_id` folded in.
- **`clara._assert_claim_basis(p_claim jsonb)`** — field-scoped like `_assert_adjustment_basis` (`0194:573`), every refusal typed with `detail.reason` + `field`, all of it before anything durable: claimant is a live enrolment handle in `clara.staff_advance_accounts` (`0043:335`); `incurred_date` present and ≤ `posting_date`; ≥1 itemised line, each naming a client-owned **expense-class** account, integer cents, summing **exactly** to `amount_cents` (no all-zero claim); `currency = 'MYR'`; `settlement ∈ {reimbursement, advance_application, already_settled}`; the reimbursement leg names a **non-control** payable (`account_class is null` — `2010 Other Payables`, `0150:1503`), refused here by its own name because downstream it is only `generic_control_leg` (`0195:2021-2028`); `supplied_tax` carried **verbatim**, validated against nothing.
- **Posted state** is a NEW `after insert` trigger on `clara.operation_receipts` for `outcome='committed'`, appending `posted` to the status ledger when a claim row names that `work_id` — the table carries `work_id` (`0178:411-433`) and today holds only append-only/no-truncate triggers (`0178:465-467`). Entry and receipt stay **derivable by join** (`work_id` → committed receipt → `effects->>'entry_id'`, indexed `0178:451`, `:454`). **Fallback:** derive posted state by join at read time and say so.
- **`clara.get_work_claim_origin(p_work)`** on the `get_work_plan_origin` precedent (`0193:2129`): viewer-floored, firm-scoped, one jsonb object or NULL, so Work list and detail label a claim **without a purpose value**. Plus `list_staff_expense_claims(p_client,p_from,p_to)` and `get_staff_expense_claim(p_claim)`.
- **D4 — the claimant is a staff-advance enrolment handle** (label + optional identifier), never a vendor counterparty. The door auto-enrols a new claimant **inside** the transaction through a definer-internal write, **never** by calling admin-floored `clara.enrol_staff_advance_account` (`0043:1946`, `_human_ctx(clara.role_rank('admin'))`, WDB-G6). Re-derive its walls in-transaction: the account exists in `coa_accounts` and is client-owned, `confirm_dedicated` acknowledged, a non-blank attestation under 0043's deliberately stricter-than-house rule (`0043:1990-2000`), `_adv_enrolment_admission`. **Fallback:** require an existing active enrolment, refuse `claimant_not_enrolled` naming the enrol route, mark that half partial by name.
- **D5 — the advance-application arm posts in the Work lane through a lane-agnostic deferred constraint birth trigger** (DECISIONS §1.4) on `clara.journal_entries`, named to sort **before** `t_je_adv_movement_belt` (`0043:3176`, `deferrable initially deferred`, `when (new.status='approved')`), registering the movement and minting the `staff_advance_applications` allocation idempotently (`on conflict … do nothing`) from the claim row it reaches by join on `work_id`. **Never** a fifth caller of `clara._subledger_on_approve` (`0037:3840-3845` pins the roster at four); re-derive and re-assert that census at its **unchanged** cardinality in your tail, and prove the firing order with a cell. **Fallback:** descope `settlement='advance_application'` to `clara.book_staff_advance_application` (`0043:2513`) and mark AC2 partial **by name** for that arm.
- **Pins are MEASURED on your migrated rig, never transcribed** (DECISIONS §1.2 — several live bodies are splices). The prestate carries **non-regression** `sha256(prosrc)` pins for six bodies you edit none of: `_admit_accounting_work_core(uuid,uuid,text,text,jsonb,jsonb,text,jsonb,text)`, `_record_journal_entry_core(uuid,uuid,text,uuid,uuid,text,jsonb,text,text,text)`, `_assert_adjustment_basis(text,jsonb)`, `_assert_adjustment_relationships(uuid,text,jsonb,jsonb,boolean)`, `book_staff_advance_application(uuid,date,text,jsonb,jsonb,text,text,text)`, `_adv_on_approve(uuid)`. The **tail re-asserts all six unchanged** and re-asserts **both purpose CHECK texts byte-identical** to their 0194 form (`0194:228-233`) — that pair is the machine-checkable proof #638 stayed off the core — plus 0042 tail 20(a)/(b) live, the hook caller census, and the new trigger's firing position.
- **Tax facts are opaque supplied particulars** — carried and echoed, never validated (no `tax_code` vocabulary exists; `0150:525` calls the statutory tag a hint; PRD:124). **No second human on the Work lane** (PRD:114): pin it with a cell and record `is_high_stakes`'s non-amount axes (`0004:72-78` — opening balance, year end, `tax_affecting`) as a ticket finding, never a blueprint edit.
- **Questions:** refuse **before admission** for anything the basis needs — claimant, incurred date, items (#721's ruling, `brief-643.md` §1). A parked `0180` question is reserved for a fact only the run discovers (which of two live enrolments a named claimant is) and ships as successor contract, not code. **Per-item continuation is designed inside one claim** — #636 defers batch aggregation across works (PRD:118), not this.
- **You own the Work purpose vocabulary fix, four surfaces, once**, carrying #643's two purposes with it: `lib/work/purpose-label.ts:20-24` SUFFIX, `accounting-work-list.tsx:114` `KNOWN_PURPOSE_LABELS` (`new Set(["journal_entry"])`, stale since #643), `work-list-filters.tsx:55` `KNOWN_PURPOSES` (same), `messages/en.json`. A claim's label on those surfaces comes from `get_work_claim_origin`, **not** a purpose value. Also fix `tree.ts:344-372`'s stale "one-member CHECK" comment.
- **No `_vN` successor** (WORK-ORDER rule 8). `start_staff_expense_claim_work` is a successor-contract stanza only, carrying **no `WORK_ACCEPTED_PURPOSES` widening** (`chatTurn.v19.parts.ts:91`): a claim Work *is* a `journal_entry` Work, so the chat `work_accepted` part already names it. Say so — it is the one thing the amendment made cheaper for chatTurn_v20.
- **Do not touch:** `clara.periodic_adjustments`' purpose CHECK; `counterparties_kind_check` / `clara.open_items` (0042 tail 20(a)/(b), re-asserted by 0043/0044/0045 — assert both in *your* tail); the activity kind ladders (D13 — named residual); `lib/firm/capabilities.ts` (#625 owns; read `firmCapabilities`); `client-workspace-overview.tsx` layout (#650); `StateBanner`; `knowledge_keys` (#654); `useUploadQueue.ts` / `intake.ts` / `InterviewRunCard.tsx` (#633); `documents_document_kind_check` (`0165:95`, `0191:142`) and `document_capabilities.registry_version`. No eleventh `needs-you.ts` row kind. **Never edit `docs/PRD.md` or `docs/ARCHITECTURE.md`** — contradictions go under "blueprint drift".
- **Rig row:** worktree `C:\Users\zhant\Desktop\clara-wt\638`, branch `impl/638-staff-claim`, port **55503**, db **clara_638** (193 migrations, PG 17.11). **Playwright triple:** `CLARA_E2E_APP_ORIGIN=https://127.0.0.1:3250`, `CLARA_E2E_NEXT_PORT=3251`, `CLARA_E2E_RUNTIME_PORT=3252`.

## 1. Current state

**db.** `accounting_work` / `operation_receipts` carry the three-value purpose CHECK (`0194:228-233`) and the frozen `adjustment_basis` column (`0194:243-247`). Admission runs through `_admit_accounting_work_core` (`0194:1062`) behind `admit_journal_work` (`:1281`) and `admit_periodic_adjustment_work` (`:1298`): closed purpose set (`:1078-1083`), `_assert_adjustment_basis` (`:1149`), `_assert_journal_basis`, `_assert_adjustment_relationships(…,false)` (`:1160`) — all before any durable write — then idempotency on `(firm, client, intent_key)`. Posting is `_record_journal_entry_core`, live fourth copy at `0195:1685`: finds the Work by the IN-list (`0195:1711`), refuses any payable/receivable-class leg (`0195:2021-2028`), inserts a draft and flips it approved with a bare UPDATE (`0195:2098-2112`) — so `_subledger_on_approve` never runs here, while the advance belt is a constraint trigger firing on **every** approved row (`0043:3176`): a debit meets CLR40 `advance_movement_unregistered` (`0043:3166-3172`), a credit `advance_application_missing` (`0043:3158-3165`). One `logical_op_id` already keys both receipt families (`0195:1963`, `0178:448`) — one-receipt-per-act is inherited, not rebuilt. The staff-advance family is complete and governed (`0043:335`, `:415`, `:535-544`, `:631-663`; doors `:1946`, `:2089`, `:2188`, `:2513`; reads `:3200`, `:3269`, `:3366`; review kind `:3634`). Two hard walls: no employee/staff counterparty (`0042:6832-6840`), no advance concept in `open_items` (`0042:6815-6830`). **No claim object exists anywhere.** `claim_form` is codeable (`0165:216-217`) but levelled `stored_only` (`0191:329,344-358`).

**runtime.** `workRoutes.ts` has `POST /api/work/journal` (`:614`) and `POST /api/work/periodic-adjustment` (`:701`) — your route is the latter's sibling (closed-set door validation, 202-on-admitted, replayed/enqueue contract, `sendAdmissionError`). `claraWork_v3` reads `basis` off the Work row and nothing else (`claraWork.v3.impl.ts:330`); tools are a fixed three-tool literal (`claraWork.v3.tools.ts:275-307`); `capability-registry.mjs:80-90` is purpose-blind. `packages/runtime/lib/periodic-adjustment-basis.ts` is **frozen, `deployed:true`** (`frozen-workflows.json:54-57`) — clone its shape, never edit it.

**web.** Composition to clone: `accounting/adjustments/new/page.tsx` + `periodic-adjustment-form.tsx` + `periodic-adjustments-table.tsx` + `lib/work/periodic-adjustment*.ts` (`tree.ts:328`, `:374-382`). The advance surface is complete at `registers?tab=staffAdvances`, reachable only through `registers-workbench.tsx:28`. C6 aging is AR/AP-only by construction (`aging-register.tsx:27-40`). Persistent-outcome discipline is in-component (`staff-advances-register.tsx:52-70`). **Zero Playwright coverage of `?tab=staffAdvances`** (30 specs, no match).

## 2. Gaps / rows

**AC1 partial → build.** Nothing carries claimant, incurred-vs-posting date, itemised lines or tax facts (`0178:693-790` is the whole basis shape today). "Do not invent" becomes a *stated* refusal, not absence.
**AC2 gap → build; only the advance arm is conditional.** Claim object + balanced journal + employee-payable leg in one commit. The employee payable is a non-control liability leg (`0150:1503`) plus the claim register — an open item is forbidden by four live tail assertions. `reimbursement` and `already_settled` post today through the unchanged core; `advance_application` rides the birth trigger with the named fallback.
**AC3 partial → build.** The question machinery is complete (`0180:308-404`, `:578`, `:761`, `:900`); the producer is missing. Ask before admission; design per-item continuation; prove "no redundant approval" with a cell.
**AC4 partial → build.** One-receipt-per-act is structural (`0195:1963`); build the claim's unwind — reversal + linked correction claim + the hook-born `kind='correction'` advance row (`0043:944-947`).
**AC5 / AC6 partial → build** the claim faces; the advance faces are verify-only. **AC6's date half is `verify_only` — reviewed no-gap** (C08.9).
**AC7 partial → build** one Playwright walk for the claim lane with every state.
**AC8 partial → build** a DB battery through least-privileged personas plus a standalone real-World e2e **wired** at `.github/actions/db-live-gates/action.yml:173`. Hosted evidence pending.
**C08.9 `verify_only`** — record the reviewed no-gap: the caller states the date by contract (`0043:2513`), a null date refuses with `axis:posting_date` (`:2528-2533`), `business-date.ts:16-34` renders MYT. Add the two regression cells; **do not move date ownership into the DB**.
**C55.23 gap → build** — the reframed obligation is AC1+AC3's; never revive it as a fix-queue trigger.
**Out of scope, with authority:** posted-effect correction integration (#676, D6); batch aggregate progress (#636, PRD:118); activity kind ladder (D13); chat entrance (WORK-ORDER rule 8 → v20); tax vocabulary (PRD:124).

## 3. Slice (one branch)

**Migration `0206_staff_expense_claims.sql`.** **§0 PRESTATE** — the six measured non-regression pins; assert the advance relations exist, no claim relation or door exists, both purpose CHECKs are at the 0194 three-valued form, the `_subledger_on_approve` caller census is four. **§A** `clara.staff_expense_claims` as decided, carrying `work_id` (unique), `logical_op_id`, claimant enrolment id, source kind / document / instruction, `incurred_date`, `posting_date`, items jsonb (description, expense account code, cents, optional opaque `supplied_tax`), `amount_cents`, `currency`, typed `settlement`, payable / advance / payment account codes, `advance_id`, `corrects_claim_id` / `corrected_by_claim_id`, `recorded_by`, `on_behalf_of` — plus the status-ledger child. **§B** `_assert_claim_basis` and the basis derivation helper (items → expense debits; settlement → the single credit leg: non-control payable, the enrolled advance account, or the stated payment account). **§C** `admit_staff_expense_claim_work` + the definer-internal auto-enrolment. **§D** the `operation_receipts` posted-state trigger. **§E** the deferred advance birth trigger + its census re-derivation. **§F** typed refusals with `detail.reason`: `claimant_missing`, `claimant_not_enrolled`, `incurred_date_missing`, `incurred_after_posting`, `item_account_not_expense`, `items_do_not_sum`, `claim_all_zero`, `payable_account_is_control`, `advance_not_enrolled`, `advance_allocation_mismatch`; inherited CLR04 / CLR19 / CLR40. **§G** reads `list_staff_expense_claims`, `get_staff_expense_claim`, `get_work_claim_origin`. **§H TAIL** — the six pins re-asserted, both purpose CHECK texts byte-identical, 0042 tail 20(a)/(b) live, the hook census, the trigger firing position, `documents_document_kind_check` and `registry_version` unmoved. Lock order: `_reserve_op` **before** the client rung, matching `0043:2605-2618`, inside `accounting_plans → accounting_work → agent_tasks → agent_interruptions`. Integer cents; MYT calendar days.

**Runtime.** `POST /api/work/staff-expense-claim` as the `workRoutes.ts:701` sibling. New non-frozen module `packages/runtime/lib/staff-expense-claim-basis.ts` on `periodic-adjustment-basis.ts`'s shape: `.strict()` zod per settlement, `claimFromInput`, `basisFromClaim`, `localClaimRefusal`; its header states it is non-frozen **only until the shared successor imports it**. New standalone `tests/staff-expense-claim-e2e.mjs` on the real Postgres World (skip-clean without 0206; SIGKILL between commit and checkpoint via `CLARA_WORK_TEST_FAULT=exit_after_commit`, barrier `claraWork.v3.tools.ts:265-266`, spawn idiom `periodic-adjustment-e2e.mjs:562`), **registered in `action.yml` after the periodic-adjustment block (`:173`)**. No edit to `claraWork.v3.*`, `capability-registry.mjs`, `work-trace.mjs` or `periodic-adjustment-basis.ts`.

**Web.** Leaf `/clients/:clientId/accounting/claims/new` (bookkeeper floor, 裁-187 — DENIED, not blank) + history `/clients/:clientId/accounting/claims` (viewer): one `ACCOUNTING_ITEMS` row, one `CLIENT_LEAVES` row, one href builder beside `periodicAdjustmentHref`. The claim register lives on its own leaf (the advance tab is deliberately sidebar-unreachable, `tree.ts:335-339`), which carries at most a link back. Form: `JournalBasisFields`, `evidence-chooser.tsx`, `lib/work/journal-draft.ts`'s intent key + lost-response arm, a **Radio Group** settlement switch preserving typed values across switches (appendix D #46), an itemised-lines editor on `staff-advance-lines-editor.tsx`'s shape, Field/FieldGroup for every new input (#28), the Date Picker recipe keeping exact ISO dates for incurred **and** posting (#22, labelled distinctly), editable-and-shown account defaults, `fieldForServerPath` mapping every server refusal to a focusable control. History: Data Table + Table (#21/#57) with exact cents, claimant, incurred vs posting date, settlement, source, links to Work / entry / receipt / advance statement, and the two-way correction chain. C1: the attachment is genuinely optional and the document's detail reaches the claim Work through `clara.entry_evidence_links` read **directly** under RLS (DECISIONS §1.7 — never wrapped). C6: the claimant's advance lineage renders through the **shipped** statement panel; **no employee payable ever appears in `aging-register.tsx`**. Persistent outcome only — StateBanner, never a toast alone. States: loading / successful-empty / no-results-with-clear-filters / partial-stale / invalid-saving / denied / failed / cancelled-recovery; 320px, 200% zoom, keyboard + focus return, SR names, reduced motion, stable URL + Back, drafts preserved and never crossing clients.

**Docs.** `apps/web/README.md` Client row gains `/accounting/claims` (+`/new`) beside the #640/#643 entries; `packages/runtime/README.md` "### Standalone e2es" the new e2e and its skip-clean condition; `packages/db/tests/README.md` the new battery; `CONTEXT.md` three terms beside **Periodic adjustment** — **Staff expense claim** (*Avoid:* a balanced journal with a memo; a payroll run; an employee master record), **Employee payable** (*Avoid:* an AP open item; a counterparty), **Advance application** (*Avoid:* a silent FIFO; a GL credit with no named advance) — plus the named limit that the claimant handle is an enrolment, not a person record.

**Successor contract stanza** (final report): tool name `start_staff_expense_claim_work`, the `.strict()` zod input schema, the door call and argument order, the refusal→message map, the part kind, and the explicit statement that **no `WORK_ACCEPTED_PURPOSES` widening is required** because the admitted Work's purpose is `journal_entry`.

## 4. TDD seams (red first)

DB cells live in `packages/db/tests/staff-expense-claim.test.mjs`, named `p638.<area>.<case>`, driven through `humanQuery` personas at the least privilege that should succeed — never `rootQuery` for the assertion under test.

1. **`p638.claim.posts`** — the Work-lane claim posting, end to end: admit through the new door, claim, `wake_record_journal_entry` under a real `interactive_client` credential OBO the initiator. Exactly one approved entry, one committed `operation_receipts` row, one `staff_expense_claims` row with exact cents and itemised lines (born at admission, unchanged by posting), one status-ledger `posted` row, `accounting_work.result.entry_id` matching, and one `op_receipts` row under the same `logical_op_id` (`0195:1963`). **Red today: nothing writes a claim row** — no door, no relation; the advance arm additionally reds at the belt.
2. `p638.core.no_regression` — the whole #643 battery (`periodic-adjustment.test.mjs`, `work-journal-post.test.mjs`) re-run unchanged and green, plus a cell that both purpose CHECK texts and the six pinned bodies are byte-identical after 0206.
3. `p638.advance.debit` — a Work-lane entry debiting an enrolled advance account. **Red: CLR40 `advance_movement_unregistered`** (no existing cell covers this direction; `periodic-adjustment.test.mjs:301-324` pins the credit only).
4. `p638.advance.order` — the new trigger fires **before** `t_je_adv_movement_belt`: the belt stays silent, plus a `pg_trigger` name-order assertion.
5. `p638.advance.happy` / `p638.advance.race` — `settlement='advance_application'` mints `kind='claim'` rows and outstanding moves by exactly the allocated cents at the effective date; two concurrent sessions over `_adv_over_application`'s boundary walk (`0043:1220`) under the client rung. Both descoped together under the D5 fallback.
6. `p638.claimant.autoenrol` / `p638.claimant.floor` — a bookkeeper's claim auto-enrols a new claimant with every enrol wall satisfied, **and the same bookkeeper is still refused by `enrol_staff_advance_account` directly** (CLR04).
7. `p638.items.continue` — one item missing its incurred date waits alone while the independent items post; the status ledger shows both.
8. `p638.refusals` (table-driven) — every §F token, CLR19 on a seeded closed FY (copy `work-journal-post.test.mjs:517-527`), CLR04 `obo_not_initiator`; entry and committed-receipt counts unmoved in every arm.
9. `p638.no_second_approval` — a claim above `firms.high_stakes_amount_cents` posts in one transaction with no checker ceremony; the comment records that `0043:2666`'s drafted branch is deliberately not inherited and that `is_high_stakes` also fires on non-amount axes.
10. `p638.walls` — 0042 tail 20(a)/(b) live; a payable-class leg still refuses `generic_control_leg`; the hook caller census still four.
11. `p638.correction` — post, `reverse_entry`, admit the corrected claim: two rows linked both ways, the `kind='correction'` advance row hook-born, the document released by `t_entry_evidence_release`, two committed receipts under two logical identities.
12. `p638.origin` — `get_work_claim_origin` answers for a claim Work and NULL for a plain journal Work, at the viewer floor, firm-scoped.
13. `p638.rls` — role posture and RLS on the new relations, plus a `rig-meta.mjs` cohort entry beside `ADV_0043_*` (`:772-796`).
14. **Runtime** — `staff-expense-claim-unit.test.mjs` (red on the absent module's schemas); `staff-expense-claim-e2e.mjs` asserting exactly one entry / receipt / claim row / advance application after kill-and-resume, plus a revoked-authority replay leg.
15. **Web unit** — `staff-expense-claim-form.test.tsx`: server field errors reach focusable controls; itemised-sum mismatch and stale basis render as `StateBanner`; switching settlement preserves typed values. Plus a purpose-label cell proving all four surfaces label and filter the three known purposes **and** that a claim Work is labelled from `get_work_claim_origin`.
16. **Playwright** `apps/web/e2e/staff-expense-claim-walk.spec.ts` + its mock, on `periodic-adjustment-walk.spec.ts`'s shape: the full state matrix, 320px, 200% zoom, keyboard + focus return, SR names, reduced motion, stable URL + Back, preserved draft, axe. The first walk that opens `?tab=staffAdvances` is a **follow-up**, not this ticket.

## 5. Risks

1. **The temptation to widen.** A fourth purpose reads cleaner and cannot post. If you find yourself editing `0195:1711` or a purpose CHECK, stop — that is §1.3's forbidden ground, and §H's assertions exist to catch it. If a genuine wall appears, take the conservative reading, build everything around it, and report it (WORK-ORDER rule 6).
2. **Trigger firing order is by name** and fails silently; cell 4 exists for that. Constraint-trigger events on one row queue in trigger-name order, so the name is the mechanism, not a convention.
3. **Auto-enrolment crosses an admin floor.** If the branch cannot re-derive every enrol wall honestly — the attestation above all — take the fallback rather than a weakened attestation.
4. **The WDB-G7 wall is unrecoverable** — assert it in your own tail, never trust review.
5. **`already_settled` must still balance.** It is not "no journal": the expense debits land against the stated payment account. A settlement producing one leg is a refusal, not a shortcut.
6. **Shared-file one-liners** (one line each, at its sorted position): `tree.ts` (one `ACCOUNTING_ITEMS` row `staffExpenseClaims`, one `CLIENT_LEAVES` row `staffExpenseClaim`, one href builder); `messages/en.json` (claim namespaces, the claim-origin label, the two #643 purpose keys); `apps/web/test/manifest.txt` (one line per new web test — alphabetical plain-string compare; an unregistered file silently never runs); `apps/web/e2e/serve-built.mjs` (one mock hook); `e2e-fixture-ownership.test.ts` (declare `admit_staff_expense_claim_work`, `list_staff_expense_claims`, `get_staff_expense_claim`, `get_work_claim_origin`; declare `list_accounting_work` and `staff_advance_summary` as **shared** verbs); `packages/db/tests/rig-meta.mjs` (one cohort block); `packages/db/package.json` (one gate import); `.github/actions/db-live-gates/action.yml` (one block after `:173`); `CONTEXT.md` (three terms). `apps/web/tests/firm-scope-db-pins.corpus.ts` only if you add dynamic SQL — sequence it at merge prep, after the migration bytes are final.
7. **Census suites red on files you never touched** — a new file under `app/**`, a changed href, a new SQL function, a new RPC verb answered by a mock.
8. **Today's workaround pollutes the vendor master** (`x37-wave-c-a-subledger.test.mjs:1998` births the employee as a `kind='vendor'` counterparty) — an argument for shipping the lane, and the reason your tail must prove you widened neither `counterparties_kind_check` nor `open_items`.

## 6. Effort and rig

**L**, at the top of the band. gap-638's XL rested on a fifth full-body recut of the posting core plus a ruling on becoming the fifth caller of a censused hook; the amendment removes both. What remains: a relation and its ledger, a validator, a door, two triggers, three reads, a route, a non-frozen runtime module, a web leaf with form and history, four label surfaces, and the batteries. Rig: worktree `C:\Users\zhant\Desktop\clara-wt\638`, branch `impl/638-staff-claim`, PG port **55503**, db **clara_638**; Playwright triple **3250/3251/3252**; a built runtime + real Postgres World for the e2e leg. Siblings to copy, by path: `docs/plan/active/refresh-wave-2026-09-14/brief-643.md` + `reports/643-final.md` (the 0194 shape you extend without touching) and `brief-640.md` + `reports/640-review-closure.md` (what a reviewer later demanded: every finding closed by a cell that would go red, a measured merge-order table, evidence re-measured rather than copied). Before the final report: the whole `apps/web` unit suite (`node scripts/run-tests.mjs` from `apps/web`, ~5 min — report counts); `packages/db/tests/operation-census.test.mjs` and `rig-isolation.test.mjs` (no reset flags); `check-parts-parity.mjs` and `check-frozen-workflows.mjs` (you add a module no frozen body imports — prove it); `pnpm typecheck` and `pnpm lint` green from the worktree root. Label everything **local evidence; hosted evidence pending**.
