# Brief: #625 — invitation, joining, role change and member removal, end to end

## Orchestrator decisions (binding)

This section overrides `gap-625.md`'s own "建议切片" and "问题与建议" wherever they differ; each such point is marked **(orchestrator ruling)**.

- **Migration number: 0209** (`0209_preview_invite.sql`), one file. Slice (A) is **ruled in** — gap-625 left it conditional on Q1 and SYNTHESIS §2.2 (`:172`) listed 0209 "conditional"; D1 rules it in **(orchestrator ruling)**. 0199–0208 belong to siblings and will be absent on your rig — expected; depend on none of them. A released number is a gap, never renumbered; never pick your own.
- **`clara.preview_invite(p_token text) returns jsonb`** — SECURITY DEFINER, `search_path = clara, pg_temp`, `revoke … from public`, EXECUTE granted to `clara_authenticated` **only** (this estate has no `anon` role: `packages/db/deploy/roles-bootstrap.sql` declares none). Same JWT-email wall as `accept_invite` (live body `0145:694`). Returns `{firm_name, role, status, masked_email}`; effective status computed by copying `firm_invites_visible`'s expression (`0141:532-534`); **never** `token_hash`, never the address. **Zero** new grant on `clara.firm_invites` — 0141 §B stands untouched. Unknown token and valid-token-wrong-email must produce a **byte-identical** refusal (no existence oracle).
- **The door's one caller, and why 0209 exists at all.** `invite-accept-form.tsx` calls `preview_invite(token)` once `verifyOtp` has a session and **before the password fields render** — between `setVerifiedSubject(...)` and `setStage("set-password")` at `:299-300`, exactly the window gap-625's Q1 names ("verifyOtp 建立会话之后、设密码前", `gap-625.md:197`). That wiring is the only reason this migration is ruled in: **a migration with no caller is not a delivery (orchestrator ruling)**. Read it through a new fail-closed reader `apps/web/lib/firm/invite-preview.ts` shaped on `lib/firm/caller-context.ts` (column census `:73-88`, row validation `:133-136`) — never an inline `.rpc()`. `pending` → firm name, role, masked address **above** the password fields; `revoked`/`expired`/`accepted` → that status's own face and **no password form at all**; a refusal → the single no-oracle face below. **A definite negative blocks; an indefinite read degrades**: a transport failure or unreadable row renders the password form with the preview block absent and one honest line saying the firm could not be shown, because the door stays the authority (`accept_invite`'s verbatim CLR path, `:388-402`). Put that sentence in the component header.
- **Faces — two distinct sets, both enumerated here. Invent no collapsing scheme (orchestrator ruling).**
  **(i) Four invite-outcome faces** (DECISIONS §2 #625), sourced from `preview_invite`'s effective status, *not* from `InviteVerificationFailure`: **pending** (preview block + password form) · **expired** · **revoked** · **already accepted**. The **wrong-email** outcome is the door's own refusal and, because §3's no-oracle rule makes it byte-identical to an unknown token, it is **one** face worded to name neither ("this link is not valid for the address you are signed in as"). Four faces, four next actions; the joined stage below is the fifth, positive, outcome.
  **(ii) Five pre-session verification reasons → four next actions.** `InviteVerificationFailure` has **five** members, not four (`apps/web/lib/invite-verification.ts:66-76`: `rejected`, `no-session`, `no-user`, `no-access-token`, `subject-mismatch`); today `:291-296` reads only `.ok` and writes one `Invite.errorDescription` for all five. Mapping, exactly: `rejected` → **P1**, keeping today's verbatim provider message (`:291-293`) and stating honestly that a used and an expired link are indistinguishable at this point (`refresh-wave-2026-09-14/brief-622.md:29`); `no-session` → **P2** (verified nobody — the `email_change` shape the module header documents at `:11-21`; next action: sign out, reopen the link); `no-user` **+** `no-access-token` → **P3** (a partial success from the provider; retry once, then ask for a new invite) — these two share a face **only** because the invitee's next action is identical, and each still renders its own `.reason` as a diagnostic token so a cell can tell them apart; `subject-mismatch` → **P4** (the link belongs to a different sign-in; sign out fully first) — the security-critical branch `:364` already names.
- **D1 joined interstitial** replaces the bare `router.replace("/")` at `apps/web/components/invite-accept-form.tsx:339`: firm name + accepted role + an explicit "Enter workspace" control, rendered from the `caller_context` row the form already reads (`readCallerContextForSubject`, outcome carries `firm_name`/`role`). **Signed-out preview is a named residual** (needs a server route + service key → separate ticket). Do not build it.
- **D2: no resend door.** Copy says "revoke, then re-invite; the old link dies immediately", and that sentence goes into `apps/web/README.md`. Do **not** build a compound revoke-and-reinvite control: it violates the one-call contracts written in the tree (`members-confirm-dialog.tsx:66-67` "exactly one governed call"; `invite-dialog.tsx:58-60` "exactly one courier round trip"). **Seat/capacity refusal is out of scope** on `docs/PRD.md:126` (seats deferred, delivery date undecided) + `CONTEXT.md:205` (`_Avoid_: A per-firm seat count`) — record it as an out-of-scope row; no capacity control anywhere in the UI.
- **Dialog refusals.** Both `MembersConfirmDialog` instances (`members-panel.tsx:256-300`) receive the `refusal` prop they already declare (`members-confirm-dialog.tsx:50`, `:65`, rendered at `:98`); `invite-dialog.tsx` gains the same `DoorDialogRefusal` region. Both destructive confirmations **name the firm** from `caller_context.firm_name` (view `0141:544`, already read at `members-panel.tsx:119`).
- **Downgrade/removal: all four act sites (orchestrator ruling).** The panel has **four** mutating `act()` call sites — `submitInvite` (`:147`), `onPickRole` (`:194`), the remove dialog's `onConfirm` (`:270`) and the revoke dialog's `onConfirm` (`:294`). Fold the second `caller_context` read into the post-act settle of **all four**, through one local helper in this component (not four copies), so a downgraded caller whose next action is a role change *on someone else* or an invite attempt also loses the control on that authoritative response. **Do not touch `apps/web/lib/parts/hooks.ts`** — `act()`'s reload discipline (`:231-255`) is shared by every hydrated surface (SYNTHESIS `:255`: "Nobody changes it"). No polling, no server push.
- **Members door lock order is an invariant other migrations depend on — do not touch the five door bodies.** `clara._record_journal_entry_core` takes `clara.firms … for key share` *because* `set_member_role`/`remove_member` take `for update` on firms then the member row; the alternative was a measured 40P01 (`0194:1461-1470`), and that core is sha-pinned at `0194:192-195` and `0195:396-400`.
- **Activity kind ladder untouched (D13) — gap-625's slice (B) is ruled OUT (orchestrator ruling).** `list_activity` (`0184:1915`, ladder `:2078`) and `get_activity_event` (`0184:2248`, ladder `:2320`) are shared with #633/#647/#650; `af.15` (`packages/db/tests/activity-feed.test.mjs:540`) forces both to move together. Record the misfiling as a **named residual**; recut neither, and touch no file in `lib/firm/activity.ts`'s consumer set.
- **Ownership:** `apps/web/lib/firm/capabilities.ts` is yours (#638/#654 only read it). Settings section id `members` already exists at `tree.ts:266-273` — **add no row**; #648 adds `setup`, #654 adds `knowledge`. `StateBanner` stays as-is.
- **Runtime: zero changes.** No Workflow is invoked by this journey (frozen closure verified three ways in the gap map), so **no World e2e leg and no `.github/actions/db-live-gates/action.yml` edit**. Re-run `packages/runtime/tests/c5-stream-reauth-db.test.mjs` and cite it as this journey's live-authority evidence.
- **Successor contract: none.** No chat/Work-lane tool is required by any AC. Do not add one. If review reopens it, write the stanza in your report (tool name, `.strict()` zod schema, door call, refusal map, part kind) — never ship it.
- **Playwright port triple:** `CLARA_E2E_APP_ORIGIN=https://127.0.0.1:3230`, `CLARA_E2E_NEXT_PORT=3231`, `CLARA_E2E_RUNTIME_PORT=3232`.
- **Rig row (RIG.md):** worktree `C:\Users\zhant\Desktop\clara-wt\625`, branch `impl/625-membership-lifecycle`, PG port **55501**, db **clara_625**, 193 migrations applied at `4464e471`, PG 17.11.

## 1. Current state

**db — the lifecycle is done and proven.** `clara.firm_invites`: FORCE RLS, owner-only policy, zero `clara_authenticated` grant, two unique indexes (`0141:186`). Human doors, all granted `clara_authenticated` only — `claim_identity` (`0141:251`), `invite_member` (live `0147:372`), `accept_invite` (`0145:694`), `revoke_invite` (`0157:420`), `add_member` (`0145:671`), `set_member_role` (`0157:248`), `remove_member` (`0157:346`); cores `_claim_identity_core`/`_add_member_core` (`0141:220`, `:325`) ungranted everywhere. Five walls in order: `_human_ctx` admin+ floor → role **ceiling** (`0145:592`) → target-rank + self-action walls (0157 §1/§3) → `_tf_guard_last_owner` backstop (`0003:415`, `:477`). Three `security_barrier` views: `firm_members_visible` (9 cols, bookkeeper+ floor, email nulled below admin, `0141:512`), `firm_invites_visible` (10 cols, admin+ floor, effective status from `expires_at`, never `token_hash`, `0141:532`), `caller_context` (6 cols incl. `firm_name`/`role_rank`, self-limited by `jwt_sub`, `0141:544`). Events registered `context_update` (0141 §I). **No per-firm seat table exists.** `clara.preview_invite` does not exist (grepped, zero hits).

**runtime.** No workflow body touches a member door; `frozen-workflows.json` lists no member/invite/roster file and no `apps/**` path. `authz.mjs:11`,`:134-151` re-resolves live firm+role per request, no cache; `streamRoute.ts` re-authorises per poll tick; `mint_wake_credential` refuses a de-authorised OBO (`0184:1731-1738`).

**web.** `/settings/members`, `/invite/[token]` (public via `proxy.ts:63`), `POST /api/invite`. `members-panel.tsx`: two sections, capability-shaped, forced re-read after every act — but the capability object is derived **once at mount** (`:135-141`) and `context` is referenced nowhere else; four mutating `act()` sites (`:147`, `:194`, `:270`, `:294`). `members-tables.tsx:140` renders `row.email ?? emailWithheld` (data-driven, so it self-heals). `members-confirm-dialog.tsx` declares a `refusal` slot nobody passes; `invite-dialog.tsx` has no refusal region. `invite-accept-form.tsx` stages are `confirm | verifying | set-password | saving | unconfirmed | error` (`:34-40`): verifyOtp (`:283`) → `readInviteVerification` (`:289`) → subject bind (`:364`) → `updateUser` → `accept_invite` (verbatim CLR `:388-402`) → `caller_context` → `router.replace("/")` (`:339`). Nothing between `:299` and the password fields. `lib/invite-verification.ts:66-76` already computes **five** typed reasons; the caller reads `.ok` only. `lib/firm/capabilities.ts` is the one fail-closed derivation (`:260`, `:287`, `:339`, `:349`). `en.json` `Invite` = 22 keys, none naming firm or role.

**tests.** Real least-privileged personas via `p4t1-fixtures.mjs:11-36`: `p4t1-invite.test.mjs` (31 cells), `p4t1-identity`, `p4t1-reads`, `p4t1-add-member-regression`, `p4-4-role-rank-pin`, `mdrw-rank-walls.test.mjs` + its gate (already on the `packages/db/package.json` `"test"` chain). Web unit: members-a11y/-keyboard/-walls/-single-fire/-dates, invite-dialog-*, invite-accept-*. Playwright reaches `/settings/members` for two personas with axe clean (`firm-navigation-walk.spec.ts:115-139`) but **no walk drives any member RPC**: `serve-built.mjs` answers `caller_context` (`:486`), `firm_members_visible` (`:505`), `firm_invites_visible` (`:520`) in its **CORE** with fixed fixtures and stubs none of the five write verbs. The narrow face loop has two faces only (`responsive-shell-walk.spec.ts:625-627`).

## 2. Gaps / rows

| Row | Disposition |
|---|---|
| **AC1** delivery/pending separate; signed-out handoff, wrong email, expiry, revocation, **resend**, changed role, **capacity refusal**, duplicate/already-accepted | **build** both face sets (four invite outcomes from `preview_invite`; five verification reasons → four next actions); **resend out of scope** (D2; 裁-16a + `0147:399`/`:411` CLR10); **capacity out of scope** (`docs/PRD.md:126`, `CONTEXT.md:205`). Both written as records, not silence |
| **AC2** valid invite enters the intended firm; role+scope visible **before** the workspace | First half **verify-only** (`0145:694`, `p4t1-invite.test.mjs:128`); second half **build** — `preview_invite` **called before the password fields** plus the joined stage before navigation. Pre-authentication visibility = **named residual** |
| **AC3** roster actions identify member **and firm**, retain input on refusal, enforce permission + last-owner, update from the authoritative roster | **build** firm-named confirmations + in-dialog refusals; the walls themselves **verify-only** (`0157:248`, `:346`; `0003:415`) |
| **AC4** removed/downgraded live session loses protected data/actions on the next authoritative response | **build** the second `caller_context` read at **all four** act sites. Server side **verify-only** (`authz.mjs:134-151`, `c5-stream-reauth-db.test.mjs:136`). The email column already self-heals through `act()`'s unconditional re-read (`hooks.ts:246`/`:251`) — assert it **separately** |
| **AC5** wide/narrow, keyboard-complete menus/dialogs, focus return, durable result, retry, live access change | **build** the narrow legs and the first walk that runs invite/revoke/role-change/remove to settlement |
| **AC6** full state family, 320px, 200% zoom, SR names, reduced motion, stable URL/Back, drafts, persistent outcome | **build** (i) narrow legs, (ii) field-level invalid on the invite form, (iii) **write down** that this journey has no partial/stale state rather than leaving it blank |
| **AC7** production-facing reads/commands under real least-privileged roles | **build** the `preview_invite` battery through `humanQuery` personas; record the honest ceiling — no browser→real-Postgres leg exists (`live-stack/playwright.live.config.ts:16` single-string `testMatch`; `:5-8` forbids the siblings on a real backend). A mock-backed walk is never AC7 evidence |
| **CB-AE2E-014 / -025 / -033** | REDESIGN — **re-measure on this ticket's acceptance**; the cited guards (`capabilities.ts:2`, `members-tables.tsx:41`, `settings-hub.tsx:17`) are existing instruments, not fresh evidence |
| **C-80** | **verify-only, no narrow migration owed** (`0157:84`/`:248`/`:346`/`:420`; `mdrw-rank-walls.test.mjs:399`/`:482`/`:551`). **Replace the old closing sentence** with the invariant: member doors keep taking `clara.firms` before the member row, because a sha-pinned posting core's `for key share` order derives from it |
| **C79.1 / C79.2** | partial. Least-privilege operation + append-only receipt exist, and "revocation wins first" is proven three independent ways (`0184:1731-1738`, `authz.mjs:134-151`, `0157:410`/`:468`). **Residual**: receipts are unreadable as access history — the ladder falls to `else 'documents'` (`0184:2078`) and `p_kinds` is closed. Not claimed as satisfied |

## 3. Slice (one branch)

**Migration `0209_preview_invite.sql`** — house header (copy `0193_accounting_plans.sql`'s shape; copy `0194`'s prestate idiom for splice bodies). Tables: **none**. Grants: `execute` on `clara.preview_invite(text)` to `clara_authenticated` only.

**Prestate pins — MEASURE on your migrated rig, never transcribe.** No body is recut, so these are **non-regression** pins taken from `pg_proc.prosrc` on the 193-migration rig: `clara.accept_invite(text,text,text)`, `clara.invite_member(text,text,text)`, `clara.revoke_invite(uuid,text)`, `clara.set_member_role(uuid,text,text)`, `clara.remove_member(uuid,text)`, `clara._jwt_email()`. Several are splices — `set_member_role` alone was emitted at `0005:707`, `0145:592` and `0157:248` — so a pin written from any creating file will not match. Also pin `to_regprocedure('clara.preview_invite(text)') is null` before, non-null after.

**Tail assertions:** the six sha256 above unchanged; `preview_invite` present with exactly one ACL entry (`clara_authenticated`), zero PUBLIC/agent/wake/runtime reach; `clara.firm_invites` still carries **zero** `clara_authenticated` grant; the three views' column censuses unmoved; no `token_hash` column reachable from the new function's output shape.

**Preintegration gate:** `packages/db/tests/preview-invite-preintegration-gate.mjs`, mirroring `mdrw-preintegration-gate.mjs` verbatim (sets `CLARA_ALLOW_MISSING_PREVIEW_INVITE=1`; a focused run leaves it unset and fails loudly). **`rig-meta.mjs`:** its own cohort array + roster entry (one granted function; "wholly present or wholly absent").

**Runtime:** nothing. No new module, no route, no e2e leg, no `action.yml` edit — say so in the report so the absence reads as a decision.

**Web.** (1) **`lib/firm/invite-preview.ts`** — the fail-closed reader for `preview_invite`, on `caller-context.ts`'s shape: declared column census, row validation, a typed outcome distinguishing *definite* statuses from *indefinite* (transport/unreadable) and from the no-oracle refusal. (2) `invite-accept-form.tsx` calls it at `:299-300`, **before** `set-password` renders; a new `previewing` stage carries it. Pending renders firm + role + masked address above the fields; revoked/expired/accepted render their own face with **no password form**; indefinite degrades as ruled. (3) The five `.reason` values map to P1–P4 exactly as the decisions bullet enumerates, each with its own next action and its own `.reason` diagnostic token; door-level CLRs keep their verbatim path (`:388-402`). (4) A settled **joined** stage before any navigation, rendering firm name + accepted role from the already-read `caller_context`, with an explicit Enter-workspace Button. (5) `members-panel.tsx`: pass `refusal` to both `MembersConfirmDialog` instances; one local `settleAndRefreshContext()` helper folded into the post-act settle of **all four** act sites (`:147`, `:194`, `:270`, `:294`) so capabilities re-derive from the new read. (6) `invite-dialog.tsx`: an in-dialog `DoorDialogRefusal` region + **field-level invalid** on the address (Field `data-invalid`/`aria-invalid`, appendix D row 28), distinct from the form-level banner. (7) Firm-named `Members.removeDialog.title`/`revokeDialog.title`. (8) `en.json`: keys under the existing `Invite`/`Members` namespaces only — preview block, four invite-outcome faces, P1–P4, joined stage, firm-named confirmations, one sentence stating there is no per-firm seat limit and no resend. Compositions stay inside the installed set (Table, Dialog, DropdownMenu, Button, Badge, Input, Label, Field); **do not install Alert Dialog** (catalog None/Conditional; the confirm dialog already guards mis-clicks). States on both faces: loading, successful-empty, no-results (**write down** that the roster has no filter, so none exists), partial/stale (**write down** that this journey has none), invalid/saving, denied/failed, cancelled/recovery (dismissal restores the row and the typed input); 320px, 640 CSS px, keyboard completion, focus return to the trigger, SR names, reduced motion, stable URL/Back (`replaceState` already erases the spent token, `:46-79`), preserved drafts.

**Docs.** `CONTEXT.md` (house `term / _Avoid_` shape, minimal): **Invitation**, **Membership / Roster**, **Role ladder and rank wall**, **Access history**; plus one cross-reference line under **Admission capacity** (`:203-205`) pointing at `docs/PRD.md:126` and stating the in-firm invite path has no capacity of its own. `apps/web/README.md`: the journey's two floors (roster bookkeeper+, invites admin+), the eleven typed courier codes, the preview-before-password window and its definite/indefinite rule, **no resend door by design**. `packages/db/tests/README.md`: name the `p4t1-*` and `mdrw-*` families. `packages/db/README.md`: C-80's lock-order invariant + 0209's deployment note. **Never `docs/PRD.md` or `docs/ARCHITECTURE.md`.**

**Shared-file one-liners (exact).** `packages/db/package.json` `"test"` script → one `--import ./tests/preview-invite-preintegration-gate.mjs` at the sorted position. `packages/db/tests/rig-meta.mjs` → one cohort. `apps/web/test/manifest.txt` → one line per new test file (alphabetical plain-string compare; unregistered files silently never run). `apps/web/messages/en.json` → keys inside existing namespaces. `apps/web/e2e/serve-built.mjs` → one import + one hook call for `members-lifecycle-mock.mjs`. `apps/web/e2e/e2e-fixture-ownership.test.ts` → add the mock to `LANE_MOCKS`, export its verb set, and declare `caller_context`, `firm_members_visible`, `firm_invites_visible` in `SHARED_RPC_VERBS` (`:962`) — this is a **CORE handover** (`serve-built.mjs:486`/`:505`/`:520`), exactly what that census exists to catch. The narrow `/invite/:token` leg renders the **confirm** stage only (no session), so it needs no `preview_invite` stub; if your walk drives past verification, declare `preview_invite` as that lane mock's own verb. **No** `tree.ts` edit. **No** `action.yml` edit. `apps/web/tests/firm-scope-db-pins.corpus.ts` → no entry: that corpus reviews **dynamic SQL inside migration files**, and 0209 contains no `execute format` (if you write one, sorted position + ≥40-character reason).

**Mechanism, fallback, and the proof that picks.** (a) Preferred: `preview_invite` supplies effective status post-session and drives the four invite-outcome faces. Fallback: where verifyOtp itself fails there is no session, only `InviteVerificationFailure`, and GoTrue returns the same `otp_expired` for a **used** and an **expired** token (`refresh-wave-2026-09-14/brief-622.md:29`) — then P1 says so honestly and the four-outcome guarantee holds post-session only. **Proof to produce:** the provider's actual `error.code`/`status` for (i) a consumed and (ii) an expired token, quoted in the report. (b) Preferred: `settleAndRefreshContext()` in all four post-act settles; fallback, if seam 12 still shows the capability object lagging, sequence the context read **before** re-deriving capabilities in the panel — never polling, never a `hooks.ts` change. Proof: seam 12 red first.

**Sibling deliveries to copy:** `0193_accounting_plans.sql` + `0194_periodic_adjustments.sql` (header, prestate, tail); `packages/db/tests/mdrw-preintegration-gate.mjs`, `mdrw-rank-walls.test.mjs`, `p4t1-fixtures.mjs:11-36`; `apps/web/e2e/journals-table-mock.mjs` and `documents-viewer-mock.mjs` (file-disjoint, scoped, hooked once); `refresh-wave-2026-09-14/reports/643-final.md` (report shape) and `reports/640-review-closure.md` — that reviewer demanded every finding close with a cell that would go red, REDESIGN evidence **re-measured** not copied, and a merge-order collision table. Write yours the same way.

## 4. TDD seams (red first)

1. `packages/db/tests/preview-invite.test.mjs` **p625.preview.recipient** — under `set role clara_authenticated` with the invitee's own JWT (`p4t1-fixtures.mjs:11-36`, never `rootQuery`): `preview_invite` returns `firm_name`, `role`, `status='pending'`, a **masked** email. Red today: the function does not exist.
2. **p625.preview.no_oracle** — a signed-in **different** email on a valid token, and an invented token, return **identical** refusal JSON (code, message, keys). Red on any divergence.
3. **p625.preview.status_faces** — revoked / expired / already-accepted invites each report their own effective status, mint nothing, and leave `clara.firm_invites` row counts unmoved.
4. **p625.preview.no_leak** — `token_hash` appears in no returned key or value; `clara_authenticated` still has zero table privilege on `clara.firm_invites`; `clara_runtime`/`clara_agent`/PUBLIC cannot execute `preview_invite` (expect `42501`).
5. **p625.doors.nonregression** — the six measured prestate sha256 are unchanged after 0209 applies; `accept_invite`'s JWT-email wall still precedes `_reserve_op` by the positional expression `p4t1-invite.test.mjs:263` embeds.
6. Runtime — **no new test**. Re-run `packages/runtime/tests/c5-stream-reauth-db.test.mjs` against `clara_625` and quote its counts.
7. `apps/web/components/invite-accept-form.test.tsx` **p625.web.preview_called** — the door is called **before the password fields exist**: with verification stubbed green, assert the `preview_invite` spy fired and the firm name is in the document while the password input is **not yet** rendered. Red today: nothing calls it, and `:300` goes straight to `set-password`.
8. **p625.web.preview_blocks** — a `revoked` / `expired` / `accepted` preview renders that face and **no password input** (`queryByLabelText(password)` null), and `updateUser` is never reachable; a *refused* preview renders the single no-oracle face; an *indefinite* preview (thrown read) still renders the password form with the preview block absent. Four cells, one per branch.
9. `apps/web/lib/firm/invite-preview.test.ts` **p625.lib.fail_closed** — a missing column, two rows, `[{}]`, and a non-ladder role each land on the indefinite/denied branch, never on "pending" (mirrors `caller-context`'s own census cells).
10. **p625.web.joined** — assert firm name and role are in the document **before** any navigation call fires (spy on `router.replace`; it must not have been called at assertion time).
11. **p625.web.faces** — all **five** `.reason` values render a distinguishable face: four distinct titles/next actions (P1–P4), `no-user` and `no-access-token` sharing **P3** while each still exposes its own `.reason` token. The collapsed `Invite.errorDescription` must no longer be the answer for any of them.
12. `apps/web/components/admin/members-walls.test.tsx` **p625.web.downgrade** — between two settled reads, flip `caller_context` from owner to bookkeeper; assert the role menu and the Invite entry are **gone** after the next settled act. Exercise it at **two** sites explicitly: `onPickRole` (`:194`) and a **refused** `submitInvite` (`:147`) — the two the panel would otherwise leave stale. Red today (`members-panel.tsx:135` is the only `context` reference).
13. **p625.web.email_mask** — a *separate* cell: the admin-only email column masks after the next `act()`-driven re-read, refused acts included (different mechanism; `members-tables.tsx:140`, `hooks.ts:246`/`:251`).
14. **p625.web.dialog_refusal** — query the **open dialog's own subtree**, not `document.body`, for the refusal text. Red today: nothing passes `refusal`. (`members-fixtures.ts:179-183` explains why the body query proves nothing.)
15. **p625.web.firm_named** — both destructive confirmations render the firm name from `caller_context`.
16. `apps/web/e2e/members-invite-walk.spec.ts` + `members-lifecycle-mock.mjs` (**mutable** roster/invite fixtures, five write verbs) — invite → pending row → revoke → role change → remove, each reaching a **persistent banner** with focus returned to its trigger; reduced motion; stable URL/Back.
17. `apps/web/e2e/responsive-shell-walk.spec.ts` — add `/settings/members` and `/invite/:token` to the narrow face loop (`:625-627`) so both get the 320px workbench floor (`:703`) and the 640 CSS px / 200% zoom leg (`:31`, `:714`), with no horizontal scroll.

## 5. Risks

1. **Recutting `accept_invite` or `invite_member` is more dangerous than it looks.** 0141 §K(5b) pins the JWT-email wall **positionally** on de-commented `prosrc`; `p4t1-invite.test.mjs:263` embeds a byte copy of that expression and `:340-399` is a six-cell mutant panel around `does not match this invite` and `_reserve_op`. This slice touches neither body — keep it that way.
2. **A door with no caller.** 0209 is justified only by the preview step at `:299-300`; if that wiring slips, the migration is dead weight and AC2's second half is unbuilt. Seam 7 is the one that must go red first.
3. **Lock order.** Any recut reversing firms-then-row is a deadlock regression against a sha-pinned posting core (`0194:1461-1470`, `0194:192-195`, `0195:396-400`).
4. **Existence oracle.** `preview_invite`'s refusal shapes must be identical, and the wrong-email face must not word itself in a way that re-opens the oracle the DB closed (0141 §B).
5. **`serve-built.mjs` is one server for every walk and the three member reads live in its CORE.** Logic in your lane module, one hook, every verb declared — including the three shared ones — or a sibling's cell reds for a reason nowhere in their diff.
6. **Cross-ticket merge:** `en.json`, `manifest.txt`, `rig-meta.mjs`, the `package.json` gate chain and `e2e-fixture-ownership.test.ts` are edited by eleven other branches this wave: one line each, at its sorted position. `capabilities.ts` is yours — do not change `firmCapabilities`' shape under #638/#654.
7. **Census suites red on files you never touched** (a new `app/**` file, a new lib file, a new test file, a new mocked RPC verb, a new SQL function in `sql-oracle.test.ts`). Budget one whole `apps/web` run.
8. **AC7 ceiling / REDESIGN rows.** `live-stack` does not reproduce production's narrow authenticator login (`live-stack/README.md`) — label everything LOCAL and write "hosted evidence pending". The three REDESIGN rows must be re-measured; copying their existing guards forward as "verified" is what those row headers forbid.

## 6. Effort and rig

**Effort: L** (upper end): a narrow migration with six measured non-regression pins, a DB battery + gate + cohort, a fail-closed reader and the preview step that calls it, four invite-outcome faces plus P1–P4, a joined stage, three dialog refusal wirings, two firm-named confirmations, a second panel read at four act sites, a mutable e2e lane with a CORE handover, one walk, two narrow faces, four CONTEXT terms, three READMEs.

**Rig:** worktree `C:\Users\zhant\Desktop\clara-wt\625`, branch `impl/625-membership-lifecycle`, PG `127.0.0.1:55501`, db `clara_625` (`export PGHOST=127.0.0.1 PGPORT=55501 PGUSER=postgres PGDATABASE=clara_625 CLARA_ALLOW_DESTRUCTIVE=1 CLARA_RIG_DB=1`; Node 22 via `export PATH="/c/Users/zhant/AppData/Local/pnpm:$PATH"`). Never set `CLARA_RIG_ALLOW_RESET=1`/`CLARA_RIG_ALLOW_ROLE_SWEEP=1`. Playwright: `CLARA_E2E_APP_ORIGIN=https://127.0.0.1:3230 CLARA_E2E_NEXT_PORT=3231 CLARA_E2E_RUNTIME_PORT=3232`.

**Before the final report, run and report counts for:** `pnpm typecheck`, `pnpm lint`; the DB battery with the 28-flag gate chain copied verbatim from `packages/db/package.json`, plus a **focused** run of `preview-invite.test.mjs` with the gate variable unset (zero skips — a skipped battery is not evidence); `operation-census.test.mjs` and `rig-isolation.test.mjs` (no reset flags); the **whole** `apps/web` unit suite once (`node scripts/run-tests.mjs`, ~5 min); `members-invite-walk` and `responsive-shell-walk` on your port triple; `c5-stream-reauth-db.test.mjs`. **Parts-parity / frozen check: not applicable** (nothing here is reachable from a frozen body) — say so rather than omitting it. Known Windows reds you must not "fix": #707, #693, and the `thread-live-clarify.test.tsx` whole-suite load flake (re-run in isolation; report both).

## Verifier findings not applied

One half of one finding: its alternative fix "correct *four* to *five* throughout" is **not** applied — DECISIONS §2 #625 fixes the invite-outcome face count at four, so the other half of that same fix (enumerate the mapping explicitly) is what ships, with `InviteVerificationFailure`'s five members mapped to P1–P4 above.
