# Brief: #654 — firm-wide knowledge defaults with preserved client exceptions

## Orchestrator decisions (binding)

- **Migration number: `0205`** (`packages/db/migrations/0205_firm_knowledge_defaults.sql`), ONE file. 0199–0204 / 0206–0209 belong to sibling branches and will be absent from your cluster — expected; do not depend on them. A released number is a gap, never renumbered (DECISIONS §1.2).
- **Rig row** (RIG.md): worktree `C:\Users\zhant\Desktop\clara-wt\654`, branch `impl/654-firm-defaults`, PG `127.0.0.1:55512`, db `clara_654`, 193 migrations at `4464e471`. **Playwright triple** (DECISIONS §1.9): `CLARA_E2E_APP_ORIGIN=https://127.0.0.1:3340 CLARA_E2E_NEXT_PORT=3341 CLARA_E2E_RUNTIME_PORT=3342`.
- **D8 — eligibility is a NEW relation, not `scope_default` (orchestrator ruling).** gap-654 Q5 recommended making `knowledge_keys.scope_default` load-bearing inside `clara._knowledge_floor`. **Refused**: `knowledge_keys` is append-only on UPDATE (`0192:185-186`), so existing rows cannot be re-defaulted, and `_knowledge_floor` (`0192:996-1008`) sits in the frozen chat lane's call path (`capture_knowledge_for` computes the floor at `0192:1187`, calls `_knowledge_capture_core` at `0192:1199`). Build **`clara.knowledge_key_firm_eligibility`**, fail-closed. **Zero recuts of any 0192 body** — not `_knowledge_capture_core`, `_knowledge_floor`, `capture_knowledge`, `list_client_knowledge` or `get_knowledge_pack` (DECISIONS §1.3, §2/#654).
- **Two NEW BEFORE INSERT triggers on `clara.knowledge_records`**, named so they fire *after* the existing `t_knowledge_records_authority` (`0192:596`) — Postgres fires same-event BEFORE triggers in alphabetical name order, so use `t_knowledge_records_firm_eligibility` then `t_knowledge_records_firm_evidence`, and assert that order off `pg_trigger` in the tail. Refusals, both CLR10 with `detail.reason`: `knowledge_scope_not_firm_defaultable`, `firm_scope_client_evidence`. Both fire only for `NEW.scope_kind = 'firm'`.
- **Eligible set**: seed the relation with `default_currency`, `reporting_framework`, `accounting_basis`; additionally admit by kind where `knowledge_keys.kind in ('preference','policy')`. Everything else is refused at firm scope — the five carried legacy assertion keys (`0192:206-217`), `turnover_band`, `financial_year_end_month`, `sst_regime`, `mpers_eligibility` (D8: client-identity facts are never firm defaults). **Measure the admitted set on your rig** and print it in the tail; the by-kind arm also admits `coa_seed_decision` (`0192:258-261`) — intended, state it.
- **Evidence wall is written against N, not one.** `uq_document_filing_active` is `(document_id, client_id) where retired_at is null` (`0007_document_pipeline.sql:93-94`), so one document may hold many live filings. Refuse a firm-scope record pinning a document with **any** live `clara.document_filings` row; an **unfiled firm document stays admissible** — 0192 reserves exactly that case at `0192:871-874` (`_knowledge_insert_revision`'s event-payload comment: `_tf_validate_domain_event` refuses an event carrying both a client and a document unless an active filing binds them, "and a knowledge source may legitimately be an unfiled firm document"). *gap-654 cites `0192:1027-1031` for this sentence; that range is `_knowledge_row_json`'s field-emission list and says nothing of the kind — re-measure on your rig before you quote it in a tail assertion.* The tail counts pre-existing violators across N filings and refuses to apply if any exist (none today — there is no firm-scope caller).
- **Two new reads, `clara_authenticated` only** — nothing to `clara_agent_ro` or the wake roles (`0192:1742-1745`): `clara.list_firm_knowledge()` (viewer floor aligned with `list_client_knowledge`, `0192:1316`) and `clara.get_knowledge_applicability(p_client uuid, p_knowledge_key text)` (viewer). `stable`, SECURITY DEFINER, `search_path = clara, pg_temp`, `plan_cache_mode = force_custom_plan`, PUBLIC revoked. **Never a masked view.** Envelopes elsewhere stay additive-only; any unioned row carries `authoritative: true` (DECISIONS §1.5).
- **Catalog ownership**: `knowledge_keys` / `knowledge_plan_item_map` are **#654's only** this wave (DECISIONS §1.5). Mint no new knowledge key. If a sibling files a map row through you, add it at the sorted position and re-baseline `kp.01`'s ten-entry `deepEqual` (`knowledge-onboarding-promotion.test.mjs:92-113`) **once**; expect none.
- **Entrance**: `/settings/knowledge`, section id **`knowledge`** (DECISIONS §1.8) — one row in `SETTINGS_SECTIONS` (`apps/web/lib/navigation/tree.ts:245-285`; `members` is #625's, `setup` is #648's). **You own the C13 firm-scope surface** (DECISIONS §1.7): #647 renders identity in its own route; #648/#649 call your doors and render nothing here.
- **Web entrance only.** A chat firm-scope capture is a **successor contract stanza**, never an in-place edit (settles gap-654 Q4). No `chatTurn` successor is implied; `claraWork_v4` carries the `observedRevisions({ knowledge_version })` contract (DECISIONS §1.1).
- **Re-evaluation engine: NOT built** (`docs/PRD.md:123` defers it to #658/#663; `0192:106-112`). Ship the human-review affordance instead — which clients hold an exception, which live Work cites the key — and record the AC3-vs-PRD contradiction under **"blueprint drift"**. Never edit `docs/PRD.md` or `docs/ARCHITECTURE.md`.
- **Do not touch**: the activity kind ladders (D13 — record a named residual), `apps/web/lib/firm/capabilities.ts` (#625 owns; read `firmCapabilities`, `:260`), `lib/parts/hooks.ts`, `components/common/state.tsx`'s `StateBanner` (#646 owns the Alert adoption, documents surface only), `client-workspace-overview.tsx` (#650), `packages/runtime/lib/knowledge.mjs` (frozen by closure).
- **Fallback, with the proof that picks it.** Prefer the two triggers. If you prove a BEFORE INSERT trigger cannot reach what the wall needs (measure on the rig whether the source pins land in `NEW.source` before insert — `_knowledge_capture_core` `0192:901-989`), the fallback is a **new** guard `clara._knowledge_firm_wall(...)` called from a **new** firm-scope door wrapper, `_knowledge_capture_core` still untouched. Ship the trigger-fire / `pg_proc` evidence for whichever you pick. A recut of `_knowledge_capture_core` or `_knowledge_floor` is a **stop-and-report**, not your call; if ever admitted it would owe a pre-image `sha256(prosrc)` pin measured on your rig plus a `0195:396-409` NON-REGRESSION pin.
- **Pins are measured, never transcribed.** You recut nothing, so you owe no sha pin — but you owe a measured PRESTATE §0: the four 0192 relations; the 13-row `knowledge_keys` census; `uq_knowledge_live` present and partial on `state='live'` (`0192:512`); `t_knowledge_records_authority` present (`0192:596`); the exact signatures of `list_client_knowledge`, `get_knowledge_pack`, `capture_knowledge_for`, `_knowledge_source_pins` — all off `pg_proc`/`pg_trigger`/`pg_index` on `clara_654`, never off a migration's text. Every path:line in this brief is a pointer to re-measure, not a quotation to copy. Idioms: `0194:171-196` (sha), `0198:130-170` (statement probes).

## 1. Current state

**db.** `0192_client_knowledge_records.sql` (2169 lines) is the whole implementation and is complete server-side. Two scopes, `client` the default (`0192:1114`, inside `capture_knowledge`'s signature opening at `:1112`); firm scope is admin+ whatever the key says (`_knowledge_floor` `0192:996-1008`, citing #603 Q22); `uq_knowledge_live` over `(scope_kind, coalesce(client_id, firm_id), knowledge_key, applies_when_digest) where state='live'` (`0192:512`) is what makes a client exception a first-class fact; both reads shadow a firm row **only** at the same key *and* the same applicability (`0192:1350-1363`, `:1510-1518`). Promotion demands `p_promote_firm_scope` + an admin human (`0192:1564`, `:1666-1675`) and pins **no source at all** — `'{}'::jsonb` into `_knowledge_capture_core` (`0192:1706-1711`). `_knowledge_source_pins`' document probe is `d.firm_id = p_firm` with no client conjunct (`0192:782-784`) — **the one real defect**. `scope_default` is a dead column: three hits repo-wide, all inside 0192 and not one of them a read (the column definition `0192:167`, two INSERT column lists `:208`, `:224`). `clara.knowledge_records` is referenced by no migration outside 0192. The plan-authority wall is already correct (`0193:457-458`, `:1474-1488`).

**runtime.** `packages/runtime/lib/knowledge.mjs` is **frozen by closure** (`frozen-workflows.json`, repo root; 281 entries, all `deployed:true`, zero `apps/**` keys). `chatTurn.v19.prompt.ts:130-235` reads the pack envelope field-by-field. `claraWork.v3.impl.ts:474` records `observedRevisions({ basis_digest: null })` verbatim though `knowledge_version` is already in the closed trace vocabulary (`lib/work-trace.mjs:283-286`).

**web.** Client register/detail only: `app/(firm)/clients/[clientId]/knowledge/page.tsx` + `[recordId]/page.tsx`, nav `tree.ts:306`, `:386`, href `tree.ts:420`. `components/registers/knowledge-panel.tsx` — four faces via `DataState`/`EmptyState`/`StateBanner` (`:36-46`), conflict banner `:158-162`. `components/registers/knowledge-shared.tsx` badges `:84-90`, applicability `:95-115`, provenance `:164-181`. `lib/registers/knowledge.ts` exports `loadClientKnowledge` (`:188`), `loadKnowledgeRecord` (`:197`), `loadKnowledgeHistory` (`:206`), `correctKnowledge` (`:217`), `withdrawKnowledge` (`:232`) — **no capture, no firm-scope helper**. `SETTINGS_SECTIONS` holds account/firm/members/compliance/vendorBindings (`tree.ts:245-285`); no knowledge section. Copy at `messages/en.json:2527`. **Neither human capture door has a caller anywhere** — your dialog is the estate's first manual-capture UI at any scope.

**tests.** `knowledge-records.test.mjs` (kn.01–kn.27), `knowledge-onboarding-promotion.test.mjs` (kp.01–kp.13), `knowledge-fixtures.mjs`, gate `knowledge-0192-preintegration-gate.mjs` in the 28-gate chain (`packages/db/package.json:18`), cohort census `rig-meta.mjs:1775-1797` + push `:2510`. Web: `knowledge-detail.test.tsx` (kd.01–kd.07); `e2e/knowledge-walk.spec.ts` (13 walks, incl. 320px and 200% zoom); `e2e/knowledge-mock.mjs` answers **five** verbs (`:290`, `:304`, `:316`, `:324`, `:344`), imported at `serve-built.mjs:66`, declared at `e2e-fixture-ownership.test.ts:325`. Personas: `humanQuery` (`rig-helpers.mjs:183`), `runAs` (`:262`). **No cell anywhere covers** revoked membership on either knowledge lane, `capture_knowledge_for` as a db cell, cross-client evidence, or a firm default read back for a second client.

## 2. Gaps / rows

| Row | Disposition |
|---|---|
| **AC1** | **partial → build**. Scope default + exception preservation verify-only (`0192:1114`, `:512`); **build** the eligibility wall, the evidence wall and a promotion act recording authority (promoter, role at the time, authored reason, applicability, effective window). |
| **AC2** | sentence 1 **verify-only** (`0192:388-490`, `:1013-1049`); **build** the firm-vs-exception pair (register + C13 pair via `get_knowledge_applicability`). "Where it applies in a subsequent Work" → **successor contract**, marked not-claimed on this branch. |
| **AC3** | clauses 1–2 **out of scope** on `docs/PRD.md:123` + `0192:106-112` (deferred to #658/#663, neither in this wave) — ship the human-review affordance + the contract. Clause 3 **build a cell** (a repeated successful treatment never overrides an explicit exception). |
| **AC4** | **build** all four: permission floors; revoked membership (`answerer_not_active` `0192:1691-1695`, `asserted_by_rank_insufficient` `0192:1192-1196` — no cell exists for either); cross-client evidence; independent overrides; plus the negative census "a firm preference is not a posting grant nor plan authority". |
| **AC5** | **build** — no walk exercises this journey; no firm register, no promote dialog exists. |
| **AC6** | **build** at DB+web boundaries under real least-privileged roles. **No Workflow clause**: no runtime module, no belt — say so. Hosted evidence pending. |
| CB-AE2E-007 | partial → the firm half is what you build; client half verify-only (#644). The shipped promotion pins no source (`0192:1706-1711`). |
| H-21 | partial → named residual: the Work context still never reads knowledge (`claraWork.v3.impl.ts:474`); carried by the successor contract. |
| H-52 | partial → applicability on the promotion act and its display is yours; the *asking* surface stays #648/#649's. |
| C-41 | **not this ticket** (#647 owns identity). Binding rule only: one composed control, never one button per DB function. |
| C-52 | **not this ticket** (#648/#649). Intersection = AC3's conflict question, which rides the same `clara.open_work_question` record (`0180:578-686`). |
| C48.5 / C83.24 | **not this ticket** — #648 closes them (DECISIONS §2/#648); you ship a pointer from `/settings/knowledge`, nothing more. |
| C79.3 | split: the **firm-setting placement half is yours**; the re-evaluation half is out of scope (`PRD:123`). |
| C79.6 | **verify-only at the firm boundary**: a promoted default keeps the trust of what it generalises; an extracted/inferred client row never becomes an asserted firm policy. |

## 3. Slice (one branch)

**Migration `0205_firm_knowledge_defaults.sql`** — house header + §0 PRESTATE (the measured probes above), in `0193_accounting_plans.sql` / `0194_periodic_adjustments.sql`'s shape.
*Table* `clara.knowledge_key_firm_eligibility(knowledge_key text primary key references clara.knowledge_keys, eligible_reason text not null, added_at)` — FORCE RLS, owner-only policies, **no app-role DML**, append-only + no-truncate on the `knowledge_keys` idiom (`0192:185-189`), `select` to `clara_authenticated` only; seeded with the three keys.
*Doors*: **none new for writing** — promotion/correction/withdrawal ride the shipped `capture_knowledge` / `correct_knowledge` / `withdraw_knowledge` / `promote_plan_answers_to_knowledge`. *Guards*: the two BEFORE INSERT triggers. *Reads*: `list_firm_knowledge()` (firm rows + per-row live-exception count) and `get_knowledge_applicability(p_client, p_knowledge_key)` (firm rule, client exception, **which is in force and why**, exception count, live Work citing the key).
*Grants* §H in 0192's shape; *§J fail-closed tail*: trigger firing order, eligibility census, zero pre-existing violators counted across N filings, grant census, `relacl` null, PUBLIC revoked. *Gate*: `packages/db/tests/knowledge-firm-defaults-preintegration-gate.mjs` on `knowledge-0192-preintegration-gate.mjs`'s shape. *rig-meta*: one new cohort const + one push.
**Lock order**: the new reads are `stable` and lock nothing — the cohort stays outside `accounting_plans → accounting_work → agent_tasks → agent_interruptions` (0192's only touch of `accounting_work` is a firm-congruence read, `0192:799-802`). **Calendar days**: `effective_from`/`effective_to` are `date` — resolve "today" **server-side in `Asia/Kuala_Lumpur`**.

**Runtime.** **No module, no belt, no World e2e leg, no `.github/actions/db-live-gates/action.yml` row** — AC6 is satisfied at DB+web boundaries; say so. If a helper becomes necessary it is a NEW module `packages/runtime/lib/knowledge-conflicts.mjs` (never an edit to the frozen `knowledge.mjs`) and it then owes a real-Postgres-World leg on `packages/runtime/tests/plan-occurrence-e2e.mjs`'s pattern plus one `action.yml` line after `work-cancel-e2e` — a scope change to report, not to take silently.

**Web.** (1) `/settings/knowledge` firm register — viewer+ reads, admin+ acts; rows reuse `components/registers/knowledge-shared.tsx` verbatim and name authority (promoter + role at the time), applicability, effective window, exception count, with links to the clients holding one. (2) **Promote dialog** on `knowledge-detail.tsx`, admin+ — shadcn `Dialog` (appendix D row 23: Title, initial focus, Escape/cancel, unsaved-input handling, focus restoration; Base UI triggers use `render`, not `asChild`), fields: **authored** reason (never inherited from the client row's basis), `applies_when`, effective window; a persistent statement that established client exceptions survive. (3) **Exception pair** in `knowledge-panel.tsx` — the firm rule muted beside the client row with an "overridden by this client's exception" line and a link to the register, fed by `get_knowledge_applicability` added to `lib/registers/knowledge.ts`. **An exception pair is NOT the conflict face** (`knowledge-panel.tsx:137-148` draws that line for legacy rows) — decided vs undecided.
Accepted compositions only — `Dialog`, `Badge`, `Table`, `Select`, `DataState`/`EmptyState`/`StateBanner`; no toast as the only receipt (appendix C §3). **Every state**: loading (skeleton, no placeholder zero); successful-empty vs filtered-no-results; partial/stale (labelled prior data, cleared the instant it is no longer permitted); invalid/saving (error beside its control, focus the first invalid, input preserved); denied/failed (inline verbatim CLR refusal with its code — the shape `knowledge-walk.spec.ts:172` pins); cancelled/recovery (Dialog cancel keeps the draft under the same user/firm/record/version; a lost response re-reads current state before a distinct resubmit and reuses the same `op_key`). 320px, 200% zoom, keyboard/focus return from the Dialog, screen-reader names, reduced motion, stable URL/Back, drafts never crossing clients.

**Docs.** `packages/db/README.md` — new section "Knowledge scope, firm defaults and exceptions" after "Operation-contract census". `packages/db/tests/README.md` — the battery + its gate. `apps/web/README.md:17` — `/settings/knowledge`. `CONTEXT.md` — **two** terms only, house term/_Avoid_ shape: *Knowledge promotion*, *Client knowledge exception*. `CONTEXT.md:131-133` (*Firm knowledge default*) already carries the cross-client rule and `:135-137` (*Accounting experience*) already carries AC3 clause 3 — do not restate either.

**Successor contract stanza** (report only, do not cut): `claraWork_v4` — (a) a knowledge-context step reading `clara.get_knowledge_pack(p_client, p_purpose, p_firm)` through a new non-frozen module with the never-null/never-throw envelope, feeding `observedRevisions({ knowledge_version })` into `claraWork.v3.impl.ts:465-474`; (b) a scoped conflict question through `clara.open_work_question`, naming both rows, both scopes and both applicability statements and picking no winner. State what an **expired** conflict question means — `0198:143-147` removed the `work_id is not null` predicate, so it sweeps on the chat-clarification clock while the conflict outlives it. Give tool name, `.strict()` zod schema, door call + argument order, refusal→message map, part kind.

## 4. TDD seams (red first)

`packages/db/tests/knowledge-firm-defaults.test.mjs`, real rig, **through `humanQuery` personas at the least privilege that should succeed** — never `rootQuery` for the assertion under test (DECISIONS §1.10):

1. **p654.eligibility.refuses_client_key** — firm-scope capture of `financial_year_end_month` (and of a carried legacy key) → CLR10 `knowledge_scope_not_firm_defaultable`; **no row lands**.
2. **p654.eligibility.admits_seeded / .admits_by_kind** — `default_currency` (seeded) and `reporting_framework` (policy kind) land; the admitted census equals the tail's printed set.
3. **p654.evidence.refuses_filed_document** — a firm-scope record pinning a document with one live filing → CLR10 `firm_scope_client_evidence`, and again at **N>1** live filings; row count unmoved. Reproduce the hole **first** (today the pin is accepted: `_knowledge_source_pins` checks firm congruence only, `0192:782-784`), then wall it.
4. **p654.evidence.admits_unfiled_firm_document** — an unfiled firm document is admitted, which is exactly the case 0192 reserves at `0192:871-874`.
5. **p654.exception.survives_promotion** — client A holds a live row at key+applicability, B does not; after promotion A still reads its own value through **both** `list_client_knowledge` and `get_knowledge_pack`, B reads the firm value, both reads agree.
6. **p654.exception.independent_overrides** — a second client's later exception disturbs neither the first's nor the firm row.
7. **p654.authority.floor** — bookkeeper → CLR04; admin succeeds and the record carries promoter, role at the time, authored reason, applicability, effective window.
8. **p654.authority.revoked_membership** — promoter deactivated between capture and promotion refused; `answerer_not_active` withheld by name (`0192:1691-1695`); `capture_knowledge_for` with a deactivated named human → CLR04 `asserted_by_rank_insufficient` (`0192:1192-1196` — **the first db cell for that door**).
9. **p654.trust.no_laundering** — an extracted/inferred client row cannot become an asserted firm **policy**, even as owner.
10. **p654.applicability.read_agrees** — the new read's "in force and why" agrees with what `list_client_knowledge` actually returns for that client.
11. **p654.census.not_a_posting_grant** — a `pg_proc.prosrc` scan proves no function outside the knowledge cohort reads `clara.knowledge_records`, and `create_accounting_plan` still refuses a knowledge record as `authority_ref` (`0193:1474-1488`).
12. **p654.pack.second_client** — the firm default reads back through `get_knowledge_pack` for a client with no row of its own, `authoritative: true`.

Web unit (each registered in `apps/web/test/manifest.txt`): `knowledge-firm-panel.test.tsx` (four faces; the exception pair renders **without** the conflict face); `knowledge-promote-dialog.test.tsx` (reason required; control absent below the admin floor; fresh `op_key`; CLR refusal verbatim with entered values preserved; effective-window default resolved in `Asia/Kuala_Lumpur`); `knowledge-exception.test.tsx`.
Playwright `apps/web/e2e/knowledge-firm-walk.spec.ts` + three new verbs on the existing `knowledge-mock.mjs` (promote, `list_firm_knowledge`, `get_knowledge_applicability`): promote from a client record → firm register shows it with its authority → the exception client still shows its own value beside the labelled firm rule → 320px → 200% zoom → keyboard/focus return from the Dialog → stable URL/Back → preserved draft → verbatim refusal.

## 5. Risks

- **The exception pair and the conflict face are one pixel apart and opposite in meaning** — getting it wrong is the defect #644's fix round 1 had to repair.
- **Merge risks.** `knowledge_keys` is append-only and `kp.01` is a ten-entry `deepEqual` (`knowledge-onboarding-promotion.test.mjs:92-113`) — you are the only catalog owner this wave; refuse a sibling's inline key. #647 lands on the same C13 register in its **own** route. #648 consumes your doors and renders nothing here — hand it the section id and the `FirmKnowledge` block. #646 wants the same reassessment engine; nobody builds it.
- **Census suites red on files you never touched** (a new `app/**` route, a changed href, a new SQL function, a new RPC verb). Budget one whole `apps/web` run.
- **Exact shared-file one-liners this ticket adds** — logic lives in your own modules:
  1. `apps/web/lib/navigation/tree.ts` — one `SETTINGS_SECTIONS` row `{ id: "knowledge", href: "/settings/knowledge", … minimumRole: "viewer" }` after `members` (`:262-269`, inside the array opening at `:245`) + one `SettingsSectionId` union member.
  2. `apps/web/messages/en.json` — one new top-level `FirmKnowledge` block + `Settings.sections.knowledge.{title,purpose}`; `ClientKnowledge` (`:2527`) additive only.
  3. `apps/web/lib/require-firm-scope.ts` — `/settings/knowledge` into `SCOPE_ENTRANCES` (`:253`), **not** `SCOPE_UNSCOPED_SURFACES` (`:288`) or `SCOPE_EXEMPT_SURFACES` (`:477`); an unregistered firm leaf is a scope hole. Run `tests/firm-scope-surfaces.test.ts` and `tests/firm-scope-fourth-entrance.test.ts`.
  4. `apps/web/test/manifest.txt` — one line per new test file, alphabetical plain-string sort (the runner does not directory-scan; an unregistered file silently never runs).
  5. `apps/web/e2e/e2e-fixture-ownership.test.ts:325` — declare the three new verbs on `knowledge-mock.mjs`; `LANE_MOCKS` (`:53-70`) and `serve-built.mjs:66` already carry the mock — **no new import**.
  6. `packages/db/tests/rig-meta.mjs` — one new cohort const beside `:1775-1797` + one `cohortFailures` push beside `:2510`.
  7. `packages/db/package.json:18` — one `--import ./tests/knowledge-firm-defaults-preintegration-gate.mjs` appended to the 28-gate chain.
  8. `apps/web/tests/firm-scope-db-pins.corpus.ts` — only if you add dynamic SQL; key order is load-bearing and its sha256 is over final file content, so sequence it at merge prep.
  9. `CONTEXT.md` — two terms. `.github/actions/db-live-gates/action.yml` — **no row**.
- **Copy these shapes**: `packages/db/migrations/0193_accounting_plans.sql` and `0194_periodic_adjustments.sql` (header, prestate, tail, gate, cohort); `packages/db/tests/knowledge-records.test.mjs` + `knowledge-fixtures.mjs` (battery); `apps/web/e2e/knowledge-walk.spec.ts` + `knowledge-mock.mjs` (walk/mock pair); `docs/plan/active/refresh-wave-2026-09-14/brief-640.md` / `brief-643.md`, and `reports/640-review-closure.md` — which tells you what the reviewer will demand: **behavioural cells, not `prosrc` assertions**; races actually raced; every number measured, never transcribed; Playwright honesty about per-run flakes; a merge-order collision census.
- Hosted evidence does not exist for this lane — write "hosted evidence pending". A skipped frontier-gated battery is not evidence.

## 6. Effort and rig

**M.** Rig: worktree `C:\Users\zhant\Desktop\clara-wt\654`, branch `impl/654-firm-defaults`, PG `127.0.0.1:55512` / `clara_654`; Playwright triple `3340/3341/3342`. Never set `CLARA_RIG_ALLOW_RESET=1` or `CLARA_RIG_ALLOW_ROLE_SWEEP=1`.

Before the final report, run and report counts for: the whole `apps/web` unit suite once (`node scripts/run-tests.mjs` from `apps/web`, ~5 min); `packages/db/tests/operation-census.test.mjs` and `rig-isolation.test.mjs` without the reset flags (your slice adds SQL functions); your own battery with the exact 28 gate flags from `packages/db/package.json:18`; `pnpm typecheck` and `pnpm lint` from the worktree root. **Parts-parity is not applicable** (nothing reachable from a frozen body changes) — still run `node scripts/check-frozen-workflows.mjs` once and report it green. Known Windows reds you must not fix: #707, #693.

## Verifier findings not applied

- Verifier note "`0192:206-217` should be `206-216` because line 217 is blank": **not applied** — measured on the file, `0192:217` is `from clara.client_fact_keys k;`, the INSERT's own last line, so `206-217` is the correct span for the five carried legacy assertion keys.
