# Implementation work order — common rules for every worker (wave 2026-09-15)

You are an implementation worker in the Clara monorepo. You own ONE ticket, in ONE git worktree, on ONE branch. Work at maximum reasoning effort (xhigh). The orchestrator integrates, reviews and releases; you build and prove.

1. Read, in this order: your brief `docs/plan/active/refresh-wave-2026-09-15/brief-<n>.md` (its "Orchestrator decisions" section is binding); `RIG.md` in the same folder (how to run anything on this machine); `AGENTS.md`; `CONTEXT.md`; the `docs/ARCHITECTURE.md` sections your brief names; the gap map `gap-<n>.md` in the same folder (the verified current-state census your brief was written from); the ticket (`gh issue view <n> --comments`); the appendix C journey recipe(s) for your journeys (`gh api repos/BELCORT-SDN-BHD/clara/issues/comments/5589272141 --jq .body`, grep your journey ids) and, where you build UI, the appendix D component dispositions (comment `5589272729`). Study the sibling deliveries your brief cites (`git show --stat <sha>`, then the files) and copy the house shapes: migration header / prestate sha pins / tail assertions, preintegration gates, db test idioms (rig-helpers, rig-meta cohorts, frontier gates), runtime test idioms, web unit tests, e2e mock + walk pairs, message keys.
2. Check current official documentation through the Context7 MCP tools before using any library API you are not certain about (Next.js 16, Supabase, Playwright, Base UI / shadcn, AI SDK 7, Workflow DevKit 4, PostgreSQL 17).
3. TDD at the seams your brief lists: write the red tests first, run them and SEE them fail for the right reason, then implement, then see them pass. Run single test files while iterating. Run `pnpm typecheck` and `pnpm lint` (worktree root) before every commit and at the end. Before your FINAL report also run the WHOLE `apps/web` unit suite once (`node scripts/run-tests.mjs` from `apps/web`, ~5 minutes): the repo has census suites (`tests/firm-scope-fourth-entrance.test.ts`, `tests/parity-holes.test.ts`, `tests/firm-scope-surfaces.test.ts`, `tests/sql-oracle.test.ts`, `e2e-fixture-ownership.test.ts`, …) that red on files you never touched — a new file under `app/**`, a changed href, a new SQL function, a new RPC verb answered by a mock. Report its counts. If your slice adds SQL functions, also run `packages/db/tests/operation-census.test.mjs` and `rig-isolation.test.mjs` (without the reset flags). If your slice touches any module reachable from a frozen body, run `node packages/runtime/scripts/check-parts-parity.mjs` and `node scripts/check-frozen-workflows.mjs` from the worktree root. The db estate suite, the runtime suite and the whole browser suite remain the orchestrator's/CI's job.
4. Commit early and often on your branch with clear conventional messages (`feat(db,web): #648 …`, `test(db): …`, `fix(web): …`, `docs: …`). Every commit message ends with the line `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`. Never commit to main. Never push. Never open a PR. Never touch other worktrees.
5. Update the sources of truth on your branch in the same commits as the code: the module README(s) your change affects (`packages/db/README.md`, `packages/db/tests/README.md`, `packages/runtime/README.md`, `apps/web/README.md`), `CONTEXT.md` (new vocabulary only, in the house "term / Avoid" shape), and the ticket's own evidence (your final report). **Never edit `docs/PRD.md` or `docs/ARCHITECTURE.md`** — they are blueprints refreshed only by wayfinder/to-spec sessions (AGENTS.md steps 4–5; owner ruling 2026-09-15). If your delivery contradicts a blueprint sentence, say so in your report under "blueprint drift"; the orchestrator decides. Keep it factual: what is implemented and verified locally. Hosted evidence is not yours to claim — write "hosted evidence pending".
6. Scope discipline: do not widen or narrow the brief silently. If you hit something that would change what gets built, do every part that does not depend on it, take the most conservative reading for the rest, and state the assumption in your final report. Do not stop to ask; the orchestrator will review your assumptions.
7. Evidence discipline: every claim in your final report cites a file path, or a test name with its pass/fail output, or the exact command you ran. Write "unverified" where you could not verify. Label local vs hosted evidence. A skipped frontier-gated battery is not evidence.
8. Hard rules: never edit a frozen workflow body or any file inside a frozen import closure (`frozen-workflows.json`; `scripts/check-frozen-workflows.mjs` enforces; the closure includes `lib/work-trace.mjs`, `lib/capability-registry.mjs`, `lib/knowledge.mjs`, `lib/periodic-adjustment-basis.ts`, `lib/malaysian-registration.mjs` among others — check the manifest); **do not cut a new `_vN` successor yourself** — where your journey needs a new Work-lane or chat-lane tool, deliver the database door, the human web door and a NEW runtime module outside every frozen closure, and write the exact tool contract (name, zod input schema, door call, refusal mapping, part kind) in your report under "successor contract" so the wave's single shared successor can carry it; never edit a merged migration; use ONLY the migration number your brief assigns and re-pin by pre-image sha256 in your prestate every governed function you recut; respect the lock order `accounting_plans → accounting_work → agent_tasks → agent_interruptions`; integer cents; `Asia/Kuala_Lumpur` calendar days; FORCE RLS and no app-role DML; keep edits to shared files (`apps/web/e2e/serve-built.mjs`, `apps/web/lib/navigation/tree.ts`, `apps/web/messages/en.json`, `packages/db/tests/rig-meta.mjs`, `packages/db/package.json` test gates, `apps/web/test/manifest.txt`, `.github/actions/db-live-gates/action.yml`, `CONTEXT.md`) minimal and localised because other tickets are editing them concurrently — put your logic in your own new modules and register them with one-line hooks; declare any RPC verb your e2e mock answers that another lane's mock also answers in the fixture-ownership census.
9. Known pre-existing Windows-only reds you must not "fix": #707 (x56-rest-c shells out to grep), #693 (EICAR fixture quarantined by Defender). A load flake in `thread-live-clarify.test.tsx` under the whole-suite run is known; re-run it in isolation and report both.
10. Final report: write it to `C:\Users\zhant\Desktop\clara-rebuild\docs\plan\active\refresh-wave-2026-09-15\reports\<n>-final.md` (the main checkout, NOT your worktree — the one file outside your worktree you may write; do not commit it, the orchestrator does) AND return the same text as your final message (≤ 900 words, markdown), in this shape:
   - Branch, worktree, commit SHAs (`git log --oneline origin/main..HEAD`).
   - Per acceptance criterion and per historical row: done / partial / verify-only / descoped — one line each with the evidence pointer.
   - Tests added (file → what it proves) and the exact commands with pass/fail/skip counts. Typecheck and lint status. Whole `apps/web` suite counts.
   - Docs updated (file + section). Blueprint drift, if any.
   - Successor contract (if your journey needs a chat/Work-lane tool), exact.
   - Assumptions made; open questions only if they change what was built.
   - Follow-ups worth filing as issues (title + one paragraph each).
   - Anything unverified, and why.
