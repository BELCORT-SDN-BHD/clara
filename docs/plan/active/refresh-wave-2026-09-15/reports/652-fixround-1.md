# #652 — review round 1, fix report

**Branch** `impl/652-accrual-adjustments` · **worktree** `C:\Users\zhant\Desktop\clara-wt\652` · rig PG `127.0.0.1:55510` / `clara_652` · Playwright `3320/3321/3322`. Review head was `b579e25a`; this round adds two commits.

```
abdb8716 fix(web): #652 review round 1 — drop the two unreachable accrual nav leaves, cover the empty list, keep the Plans ⌘K word
cebfcca5 fix(db,runtime,web): #652 review round 1 — the schedule runs inside the term it names, one honoured method rule, a typed op-key conflict
```

Every blocker/should/note is answered below. Nothing was dismissed; nothing was "fixed" by editing an expectation.

## Finding → what I did → evidence

| # | Finding | What I did | Evidence (red → green) |
|---|---|---|---|
| **F1** (SPEC, should) | `accrualNew` / `accrualDetail` are dead `CLIENT_LEAVES` rows — `resolveActive` matches `ACCOUNTING_ITEMS` on `rest[0]` and returns before `leafFor` | **Applied — removed**, mirroring `plans` (the exact precedent: a top-level accounting segment with `/new` and `/:id` below it and no leaf). Their two `AppShell.clientLeaf.*` keys went with them, and the form's bookkeeper floor now reads `hasNavigationAccess(scope, { minimumRole: "bookkeeper" })` — the same predicate the registry uses on every row — instead of borrowing a leaf to carry it | RED first: a new `tree.test.ts` cell walks `CLIENT_LEAVES` against a typed `Record<ClientLeafId, string>` sample-path table → `[{id:'accrualNew',got:null},{id:'accrualDetail',got:null}]` vs `[]`. GREEN after: `tree.test.ts` **25/25**, `routes.test.ts` **16/16**. The cell stays as a wall against the next dead leaf |
| **F2** (SPEC, should) | AC6's empty list state has no cell anywhere | **Applied** — new `apps/web/components/accruals/accruals-list.test.tsx` (+ one sorted line in `test/manifest.txt`): a successful EMPTY read renders its own sentence with no table shell; an UNRESOLVED read never renders it; one row renders term/legs/state | Cells GREEN 3/3. Proven load-bearing by mutation: `isEmpty={rows.length === 0}` → `isEmpty={false}` reds cell 1 (`the successful empty read has its own sentence`), restored → 3/3 |
| **F3** (SPEC, note) | the report's `rig-isolation.test.mjs` row quotes 31/29/1/1, which is the two files in ONE invocation | **Applied** — measured each file alone and corrected the table in `652-final.md`, with the slip named | `operation-census.test.mjs` alone → **10 / 10 pass**. `rig-isolation.test.mjs` alone → **21 / 19 pass / 1 fail (T10b) / 1 skip (T19)** |
| **A1** (ADV, should) | one stated term is frozen into the basis, so every occurrence posts a line describing the SAME term — false on a June/August posting of a July term; and a term wholly outside the window is accepted | **Applied, both halves.** (a) NEW `clara._assert_accrual_term_window` (payload-half, before `_reserve_op`) requires `effective_to` and brackets the window by the stated term; the relation carries the same law structurally — `effective_to` NOT NULL and `ck_accrual_adjustments_window_in_term` — so every occurrence posts a date INSIDE the term its line names. (b) the derived expense line now reads `one period of the accrual term <start> to <end>`, the sentence that is true of EVERY occurrence, in all three derivations (SQL, `accrual-basis.ts`, `lib/accruals/api.ts`). Mirrored locally in the form and in `localAccrualRefusal` so it is refused before admission | RED first on the pre-fix rig: `p652.term.window` → *"a closed term under an open-ended authority: expected SQLSTATE CLR10 but the call SUCCEEDED"*; `p652.basis.period_text` → expected `one period of the accrual term 2026-06-01 to 2026-07-31`, got the old wording. GREEN after re-applying 0207: both pass; whole battery **20/20**; `accrual-basis-unit` **18/18**; form cell *"a schedule that would post outside its own stated term is refused BEFORE the door"* 12/12; walk **14/14** |
| **A2** (ADV, should) | `method` is recorded and never applied: all four rules post identical cents while the form offers "The amount the previous period recorded" | **Applied — narrowed** (the reviewer's first option). The CHECK, `_accrual_methods()`, `ACCRUAL_METHODS` in `accrual-basis.ts` and `lib/accruals/api.ts`, and the form all hold exactly `stated_amount`; the form STATES the rule instead of offering a select; the `source_document_amount` arm and its three message keys are gone with the rules they served. The other three are named as a successor residual in the 0207 header, `CONTEXT.md`, the final report and the successor stanza | RED first: `p652.method.honoured` → *"the drafted selection rule stated_period_amount … expected CLR10 but the call SUCCEEDED"*. GREEN after: refusal carries `supported: ["stated_amount"]`, nothing written; `accrual-basis-unit` cell *"the method set is closed, and it is exactly the rule the schedule performs"* |
| **A3** (ADV, note) | the same-key-different-payload conflict is a bare CLR10 with no `detail` | **Applied** — both doors wrap `_reserve_op` and re-raise CLR10 with `detail {"reason":"op_key_conflict","field":"op_key"}`; 0004 raises exactly one CLR10 out of that body, so the wrapper cannot swallow anything else. Header vocabulary updated | RED proven by toggling the wrapper off, rolling 0207 back and re-applying: `p652.plan.nested_op` → *`expected detail.reason="op_key_conflict" … got null — message was: op_key reused with different args`*. Wrapper restored, rolled back and re-applied → **1/1 pass** |
| **A4** (ADV, note) | the `document_service_periods` "altered no foreign table" proof is a transcribed `<> 14` | **Applied** — §0 captures the count into `w652_foreign_pin` (the same idiom as the six body digests) and §E compares against it, so the assertion proves what it claims independent of the chain below | Migration re-applied from a true prestate; tail notice OK. The failure message now names both counts |
| **A5** (ADV, note) | three cells skip on a month end, so on one day in 31 the reversal/lock evidence is a counted skip | **Applied, by construction rather than by reshaping the day rule**: the battery's new `span()` helper ends every window at LAST month's month end, so the latest due date is in the past on every calendar day and "an accrual admitted, its reversal outstanding" always exists. The three `isMonthEnd()` guards, the helper itself and the World e2e's own `monthEndToday` skip are deleted | Battery **20 tests / 20 pass / 0 fail / 0 skipped** (was 17/17 with three conditional skips); `accrual-e2e.mjs` PASS 1 · 2 · 3+4 with no skip branch left |
| **A6** (ADV, note) | `accruals` was REMOVED from the shipped `plans` ⌘K row | **Applied — restored.** The word is back on `plans` and also on the new `accruals` row; ⌘K keywords are not a unique index in that registry ("work", "queue", "members" each sit on two rows) | RED first: new `routes.test.ts` cell *"#652's accruals row is ADDITIVE"* → *"the Plans row shipped answering 'accruals'"*. GREEN after: **16/16** |
| **A7** (ADV, note) | the reviewer's from-scratch cluster `rig652r` (55610, `clara_652r`) is left running | **No branch action.** I did the rollback/re-apply proofs on the ticket's own rig (`clara_652`, 55510) so every battery ran against the final 0207; `rig652r` is untouched and still available to the orchestrator | — |

## Counts (all local; hosted evidence still pending)

| Command | Result |
|---|---|
| `node --test --test-concurrency=1 $GATES tests/accrual-adjustments.test.mjs` (29 gates) | **20 / 20 pass / 0 fail / 0 skipped** |
| `node --test --test-concurrency=1 tests/operation-census.test.mjs` (alone) | **10 / 10 pass** |
| `node --test --test-concurrency=1 tests/rig-isolation.test.mjs` (alone, no reset flags) | **21 / 19 pass / 1 fail (T10b, rig state) / 1 skip (T19)** |
| `node --test tests/accrual-basis-unit.test.mjs` | **18 / 18 pass** |
| `node tests/accrual-e2e.mjs` (real Postgres/Workflow World, `RELAY_TEST_MODE=1`) | **PASS 1 · PASS 2 · PASS 3+4, exit 0** |
| `node --import ./test/bootstrap.mjs --import tsx --test components/accruals/*.test.tsx lib/work/accrual-draft.test.ts` | **36 / 36 pass** (12 + 3 + 21) |
| `node --import … --test lib/navigation/tree.test.ts lib/command/routes.test.ts` | **41 / 41 pass** |
| `node scripts/run-tests.mjs` (whole `apps/web`) | **3757 / 3755 pass / 0 fail / 2 skipped, exit 0** (pre-fix: 3751/3749) |
| `pnpm --filter @clara/web e2e accrual-walk` on 3320/3321/3322 | **14 passed (59 s), exit 0** |
| `pnpm typecheck` · `pnpm lint` (worktree root) | **exit 0** · **exit 0** |
| `node scripts/check-frozen-workflows.mjs` | **OK — 281 frozen files, 51 `"use workflow"` modules** |
| `node packages/runtime/scripts/check-parts-parity.mjs` | **OK, exit 0** (`work_accepted` still emittable from v18/v19 only) |
| migration rollback + re-apply (×3: A1/A2/A4, the A3 red toggle, the structural `effective_to`) | each: drop the relation + the 16 (now 17) `%accrual%` functions + the `schema_migrations` row → `PRESTATE: accrual functions 0 \| table null \| migration rows 0` → `pnpm db:migrate` → **1 new migration applied · 194 total**, prestate notice **"clean"**, tail notice **"OK"** |

One walk run reded first (`strict mode violation: getByText('2026-07-01 to 2026-07-31') resolved to 2 elements`) — my own mock change, not contention: the accrual's term and its authority window now coincide, so the detail renders that string twice. The cell asserts `toHaveCount(2)` and says why, which is itself the new law on screen. Both runs are reported; the second was 14/14 with nothing else on the host.

## What changed on the branch

`packages/db/migrations/0207_accrual_adjustments.sql` (+270/−…): header gains THE SIXTH MEASUREMENT and two refusal tokens, FOURTH MEASUREMENT rewritten; §0 `w652_foreign_pin`; §A `effective_to` NOT NULL + `ck_accrual_adjustments_window_in_term` + the one-rule method CHECK; §B `_accrual_methods()` narrowed, `_assert_accrual_term_window` added, the source-document arm removed; the derived line's wording; both doors call the wall and re-raise the reservation conflict; §E compares the foreign pin and exercises the wall and the wording. `packages/db/tests/accrual-adjustments{.test,-fixtures}.mjs` (three new cells, `span()`/`termed()`, the month-end guards gone). `packages/db/tests/rig-meta.mjs` (one cohort name). `packages/runtime/lib/accrual-basis.ts` + its unit cells + `tests/accrual-e2e.mjs`. `apps/web`: `lib/accruals/api.ts`, `lib/work/accrual-draft.ts`(+test), `components/accruals/{accrual-form,accruals-list,accrual-detail}.tsx`, the new `accruals-list.test.tsx`, `lib/navigation/tree.ts`(+test), `lib/command/routes.ts`(+test), `messages/en.json`, `test/manifest.txt`, `e2e/{accrual-mock.mjs,accrual-walk.spec.ts}`. Docs: `CONTEXT.md` (**Accrual adjustment** gains the window-inside-term sentence; **Calculation method** narrowed to the rule that is performed), `packages/db/README.md` (both new structural rules and the measured foreign pin). `652-final.md` updated in place: commit list, AC1/AC6 rows, tests-added table, every count, assumptions 3/6 (+ a new 7), the successor stanza (`effective_to` required, one method member, two new tokens), and the corrected rig-isolation row.

## What I deliberately left

- **Hosted evidence** is still pending (AC7 / C88.13) — not this round's to claim.
- **T10b on `rig-isolation.test.mjs`** stays red on this rig: `graphile_worker`'s PUBLIC-executable functions arrive with the WDK world bootstrap that `accrual-e2e.mjs` needs. Not 0207's (the reviewer measured the same file green on a from-scratch 0207 chain without the world). Follow-up 3 in the final report stands.
- **The unreachable `nonzero_total` arm** (0178:785-787) is still reported, not touched — it is another lane's body and the finding is in the final report's follow-ups.
- **#640 SHOULD-1** (a frequency change double-covering a period across alignments) is inherited and untouched, as the brief says.
- **The three withdrawn method rules** are a successor residual, not a silent deletion: they need a run-time reader of `clara.accrual_adjustments` and a per-occurrence basis, and `clara._plan_occurrence_basis` is #653's. Filed as a follow-up in `652-final.md`.
- **Ratification requested (orchestrator):** A1's wall is a *narrowing of the product surface* the brief did not spell out — an accrual can no longer be configured with an open-ended authority, and its window must sit inside the stated service period. I took it because the alternative is a ledger line that is false on every occurrence but one (measured), and because "a schedule that outruns the term a person stated" has no honest reading. If the owner wants open-ended accruals back, the honest shape is a nullable term with a per-occurrence period — a successor change, not a copy tweak.
