# #649 — fix round 1 (review round 1)

**Branch** `impl/649-client-onboarding` · **worktree** `C:\Users\zhant\Desktop\clara-wt\649` · rig PG **55508** / `clara_649` (194 migrations) · Playwright **3300/3301/3302**. All evidence **LOCAL**; hosted evidence pending.

```
13bd3e1e fix(web): the announcement follows the COMMIT, a failed identity read is not "nothing", and three notes
3aabf060 fix(web): e2e — the identity read is ANSWERED for every name, and the body is read through the shared cache
40e0b471 fix(db): 0204 — settle takes the CLIENT lock before the PLAN, and an unreadable fye month reads as absent
```
(on top of `baaf48fe`, the head the three lenses reviewed). Every commit carries `Co-Authored-By: Claude Fable 5.1`.

## Finding → what I did → evidence

| # | Sev | What I did | Red first → green |
|---|---|---|---|
| **B1** lock-order inversion in `settle_client_onboarding_facts` | blocker | **Applied.** The door now pre-reads the plan's `client_id` unlocked, takes `clara.clients … FOR UPDATE`, and only then `clara.onboarding_plans … FOR UPDATE` — the order `commit_client_onboarding` (0017:2764/:2768) and `cancel_client_onboarding` (0017:2852/:2853) already use, under 0037:2514-2536's single-total-order law. A labelled belt refuses CLR10 `plan_client_moved` if a future writer ever moves a plan between clients (unreachable today: no `update clara.onboarding_plans … set client_id` exists in `packages/db/migrations`). §B of the file's header now states the lock order as part of the door's contract. | New cell **`p649.settle.lock_order`** (real door through `humanQuery`, racing a root transaction that performs cancel/commit's own two acquisitions with a barrier). **RED** on `baaf48fe`: `expected '40P01' actual '40P01'` — *"neither did the transaction holding the client row"*. **GREEN** after: battery **11 tests, 11 pass, 0 fail**, and the cell also proves the write still lands (`fy_end_month=6, fy_end_day=30`) rather than "avoiding the deadlock by refusing". |
| **B2** the H-51 row was RED — the lane mock left `client_identity_candidates` unanswered | blocker | **Applied.** `client-create-mock.mjs` answers every name it does not own with the honest arity-0 shape `{name, arity: 0, candidates: []}`; the three fixture names keep the three arities. Declared in `e2e-fixture-ownership.test.ts` as this lane's one `unscopeable` handler with the reason in the mock's own source. **NOT** declared in `SHARED_RPC_VERBS` — see "left" below. | **RED** on `baaf48fe`, my own rig, ports 3300/3301/3302 (`pw-agentic-red.log`): `agentic-finish-walk` test 8 (H-51) failed — `TimeoutError: page.waitForURL` at `agentic-finish-walk.spec.ts:358`, with **exactly one** `client_identity_candidates` POST in the whole run and **no** `begin_client_onboarding` after it. **GREEN**: H-51 passes in **all three** post-fix runs (10.1s, 36.2s, 12.7s). |
| **STD-1** `CLIENT_RECORD_CHANGED` suppressed by a settle refusal | should | **Applied.** `clientRecordChanged({clientId})` now fires immediately after `commitClientOnboarding` resolves, inside the closure and before promote/settle; `act`'s `onOk` argument is gone. A refused commit still announces nothing (the line is unreachable then). | New cell **"a settle refusal AFTER a successful commit still announces CLIENT_RECORD_CHANGED exactly once"** in `onboarding-field-composition.test.tsx`. **RED**: `seen` was `[]`. **GREEN**: `onboarding-field-composition` **9/9**, plus `onboarding-checklist` (which owns the other half of the invariant) **23/23** together. The file also gained the real-`EventTarget` swap `onboarding-checklist.test.tsx` already makes — without it the assertion measures an instrument that cannot announce. |
| **S1** a FAILED identity read rendered as "nothing matches", refusal behind the modal | should | **Applied.** The read's OUTCOME (`answered` / `walled` / `unavailable`) is tracked separately from what it found. `tid("noCandidates")` and the arity-1 acknowledgement render only on `answered`; every refusal — the ≥2 wall, a check that could not run, and the birth door's own — travels into the dialog through `OnboardingDoorDialog`'s own `refusal` slot (which also takes focus), replacing the hand-rolled in-dialog banner. The page-level `StateBanner` stays for after the dialog is closed. | The existing failed-read cell now asserts **both** halves. **RED**: *"a read that could not run must never claim the name is free"*. **GREEN**: `add-client-candidates` **5/5** (the new assertion reads `data-slot="dialog-content"`, so "inside the dialog" is structural, not a body-wide text match), `add-client-control` **7/7**, `client-create-walk` **4/4**. |
| **S2** `_plan_fye_month`'s unbounded NUMBER branch → untyped 22003 | should | **Applied.** Both branches are now bounded to two digits **before** the cast (`^[0-9]{1,2}$`), so an unreadable month reads as ABSENT and the door refuses CLR10 `fy_end_month_unanswered` — which is what the file's own comment already promised. | New cell **`p649.settle.fye_unreadable`** over six shapes (wide number, 13, 0, object, array, word). **RED**: `expected SQLSTATE CLR10 but got 22003 — value "99999999999999999999" is out of range for type integer`. **GREEN**: all six refuse typed, and every client row is left `{null,null}`. |
| **S3** README claimed Add-client is the only entrance; ⌘K bypasses the read | should | **Applied as the reviewer's option (b)**: `apps/web/README.md` no longer claims exclusivity, and the palette bypass is named as a residual beside the direct-birth one; `649-final.md`'s AC1 row and follow-up list now carry it too. I did **not** wire the palette — see "left". | Independently re-measured: `grep -rn readClientIdentityCandidates apps/web --include=*.ts --include=*.tsx` returns only `lib/onboarding/identity.ts` and `components/firm/client-register-list.tsx`; `裁-37` (the palette arm) passes with zero `client_identity_candidates` requests. |
| **S4** private `readJson` drained the shared request stream | should | **Applied.** `client-create-mock.mjs` now imports `readCachedJson` + `matchVerb` from `mock-dispatch.mjs`; the local loop is gone, so `begin_client_onboarding`'s other claimant (hooked after this lane) can still read the body. | `e2e-fixture-ownership` **15/15**, both walks re-run (above). |
| **N3** collision arity taken from `candidates.length` | note | **Applied.** New `arityFromRefusal(err)` in `lib/onboarding/identity.ts` reads 0204's own `arity` off the typed detail; the face falls back to `rows.length || 2` only when it is absent. | `add-client-candidates` 5/5 (the ≥2 cell's fixture carries `arity: 2`), typecheck exit 0. |
| **N4** `openingStory` admitted `answered` for `carry_down_deferred` | note | **Applied.** Dropped, so the surface reads exactly the states `commit_client_onboarding` reads (0017:2814-2819); both keys' different state lists are now written down in the doc comment. | `client-onboarding-progress-opening-link` **5/5**. |
| **F2** report said "2 axe scans" | note | **Applied.** `649-final.md`'s AC6 row now says **3**, with the three line numbers. | `grep -n "await scan(" apps/web/e2e/client-create-walk.spec.ts` → `:111, :151, :184`. |
| **F1** the UI-21 altitude arm in `interview-walk.spec.ts` | should | **NOT applied — ratification requested** (the reviewer's option (a)), with a reason beyond "the rig lacks docker". | See "left", below. |
| **F3** the ≥2 refusal carries candidate ROWS, not bare ids | note | **NOT applied — ratification requested.** This is Assumption 1 in `649-final.md`; the shipped shape is deliberate and the brief's wording is the thing to amend if the orchestrator agrees. | `p649.identity.wall_two` asserts the row shape; the migration's §A header states the reasoning. |
| **N1** the reviewer's from-scratch cluster `rig649r` on 55608 | note | **Nothing to apply.** I did all of this round's DB work on my own assigned rig (55508 / `clara_649`). The 55608 cluster is still up; dropping it is the orchestrator's call at wave close (`pg_dropcluster --stop 17 rig649r`). | — |
| **N2** the two runtime World legs | note | **Re-run on this rig** (`clara_rt_test`, the template clone; the branch touches no runtime source, and `grep` finds no reference to either new door anywhere under `packages/runtime/`). | **Both green, both exit 0**, re-measured this round: `node tests/interview-e2e.mjs` → `PASS (a)`, `PASS (a-firm)`, **`PASS (positive): full 15-segment v2 drive → interview_complete, 16 items, no dupes, revision advanced`** — the very scenario the review could not reproduce on its from-scratch chain — and **`PASS (p649.interview.sst_park_today)`**, then `INTERVIEW E2E: ALL PASS`, `IV_EXIT=0`; `node tests/interview-kill-resume-e2e.mjs` → `PASS: SIGKILL mid-park → same-index re-park, byte-identical checkpoints, exactly-once drive to complete`, `KR_EXIT=0`. Log: `C:\Users\zhant\AppData\Local\Temp\claude\649fix\runtime-legs.log`. The review's N2 failure is therefore **not reproducible on this rig**; I am not calling it fixed either — nothing in this round touched the runtime. |

## Counts (all local, this rig)

| Command | Result |
|---|---|
| `pnpm db:migrate` after a **true prestate rollback** (three functions dropped, `client.onboarding_facts_settled` removed from `event_types` + `trigger_taxonomy` with the 20 rig-test `domain_events` rows it had raised, ledger row deleted → 193) | `#649 prestate: clean` · `#649 tail: OK` · `migrate: 1 new migration(s) applied · 194 total · target 127.0.0.1:55508/clara_649` |
| DB battery, 29 gate flags verbatim from `packages/db/package.json` | **11 tests, 11 pass, 0 fail, 0 skipped** (9 + the two new cells) |
| `operation-census` + `rig-isolation` (no reset flags) | **31 tests, 30 pass, 0 fail, 1 skipped** (destructive T19) |
| Whole `apps/web` suite (`node scripts/run-tests.mjs`) | **3749 tests, 3747 pass, 0 fail, 0 cancelled, 2 skipped**, exit 0 (3748 before + the one new cell; the 2 skips are the pre-existing live-provider auth cells) |
| `pnpm typecheck` (worktree root) | exit 0 |
| `pnpm lint` (worktree root) | exit 0 (eslint + token contrast + test-manifest + message-keys and both selftests) |
| Touched unit files | `add-client-candidates` 5/5 · `add-client-control` 7/7 · `onboarding-field-composition` 9/9 · `onboarding-checklist` 14/14 · `client-onboarding-progress-opening-link` 5/5 · `e2e-fixture-ownership` 15/15 |
| Frozen / parts-parity | **not re-run and not claimed**: this round touched no file under `packages/runtime` (`git diff --stat 40e0b471^..HEAD` names none), so the checks the brief makes conditional on that do not apply. `baaf48fe`'s own run is the standing evidence. |

### Playwright, three runs (all on 3300/3301/3302), and why the flip-flop is contention

| Run | State | `agentic-finish-walk` | `client-create-walk` |
|---|---|---|---|
| `pw-agentic-red.log` | **pre-fix** | 7 passed / 2 failed — **H-51 FAILED** (the B2 defect) + 裁-128 stuck on `/login` | not run |
| `pw-green-1.log` | post-mock-fix | 8/9 — **H-51 ok (10.1s)**; only 裁-27 failed, `apiRequestContext.post` 30 s timeout | **4/4** |
| `pw-green-2.log` | post-all-fixes, **run concurrently with the whole unit suite** | 5/9 — **H-51 ok (36.2s)**; 裁-27 flipped back to pass, 3/5/6/7/9 to fail | 2/4 — both failures `page.goto: net::ERR_ABORTED; maybe frame was detached?` on the FIRST `/login` navigation |
| `pw-green-3.log` | post-all-fixes, quieter host | 5/9 — **H-51 ok (12.7s)**, 裁-128 ok, H-30 ok; 1/3/4/5 failed on `toHaveURL` stuck at `/login` or a `beforeEach` timeout | **4/4** |

Every failure outside H-51 is a sign-in/navigation timeout, and the set that fails **changes between runs while the code does not** (裁-27 fails in run 1, passes in run 2; 5 and 6 pass in run 1, fail in run 2; 1 and 4 pass in runs 1-2, fail in run 3). Fifty-plus `node.exe` processes were live on this host throughout. I did not "fix" any of them.

## What I deliberately left

1. **F1 — the UI-21 altitude arm is NOT authored, and the reason is stronger than "no docker".** The reviewer's option (b) was to add the arm unexecuted, per that file's own "execution happens in the review/merge ceremony" convention. I did not, because `interview-walk.spec.ts` has **no fixture whose state the arm could rely on**: the three fixtures are shared, mutable DB rows, and the earlier arms consume them — arm 1 drives `COMPLETE` through every segment to "The interview is complete." (`:192-219`), `CANCEL` is cancelled by `:222`, and `RACE` is raced by two contexts at `:238`. An arm that types an answer in the rail needs an OPEN, unanswered park, so it needs a **fourth fixture provisioned by `e2e/live-stack/run-live-walk.mjs`** — a change to that runner, not "add the test code". Authored blind against an unknown post-arm-1 state, it would more likely red the merge ceremony than close UI-21. **Ratification requested**: keep UI-21 as the named residual with follow-up 1, or commission the runner change as its own ticket.
2. **F3 — ratification requested.** Assumption 1 (candidate ROWS in the `name_family_collision` detail) is unchanged; the orchestrator either ratifies it or amends `brief-649.md`'s "candidate ids" wording. I did not edit the brief.
3. **`client_identity_candidates` is NOT in `SHARED_RPC_VERBS`, and the review's instruction to add it is wrong** — proven, not argued: that census asserts every declared verb still has **two or more** real claimants (`e2e-fixture-ownership.test.ts:1044-1047`, *"declared shared but only N lane(s) answer it now — drop the declaration"*). This lane is its only claimant, so the declaration would fail on its own rule. The honest declaration is the `unscopeable` row I added instead, and the census is green: **15/15**.
4. **The ⌘K palette is not wired to the identity read.** `brief-649.md` §3 puts the candidate face on `AddClientControl` only, and closing the palette entrance means designing an arity-1 acknowledgement inside the palette — a product surface nobody ruled, on files this wave assigns to no one. Recorded as a residual in the README and in `649-final.md` instead (S3).
5. **`docs/PRD.md` / `docs/ARCHITECTURE.md` untouched**; the blueprint-drift note in `649-final.md` stands.
6. **#707 / #693 untouched**, and no Playwright failure named above was "fixed".
7. **The 55608 cluster is still running** (N1). Dropping it belongs to wave close.

## Files changed this round

`packages/db/migrations/0204_client_onboarding_facts.sql` · `packages/db/tests/client-onboarding-identity.test.mjs` · `apps/web/e2e/client-create-mock.mjs` · `apps/web/e2e/e2e-fixture-ownership.test.ts` · `apps/web/components/clara/OnboardingChecklistCard.tsx` · `apps/web/components/clara/onboarding-field-composition.test.tsx` · `apps/web/components/firm/client-register-list.tsx` · `apps/web/components/firm/add-client-candidates.test.tsx` · `apps/web/components/firm/client-home/client-onboarding-progress.tsx` · `apps/web/lib/onboarding/identity.ts` · `apps/web/README.md`. Outside the worktree and uncommitted, as instructed: `reports/649-final.md` (claims and counts corrected) and this file.
