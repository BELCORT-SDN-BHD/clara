{
  "ticket": 648,
  "head": "dcc7c94a",
  "closed": [
    "ADVERSARIAL F1 (blocker) — CLOSED. The write control is no longer gated on `isPending`: apps/web/components/firm-setup/firm-setup-checklist.tsx:566 gates on `isAnswerable(item)` and renders `firm-setup-change-<key>-action` (\"Change this answer\" / \"Answer this now\" for a deferral), the form prefills from the record via `answerDraftText` (apps/web/lib/firm-setup/types.ts:181), and a live-captured key gets a named line (`firm-setup-correct-on-register-<key>`) instead of a form the door would refuse. I raced the door myself rather than trusting the new cells (scratch probe, PG 55507/clara_648): `R3a defer -> answer: deferred -> answered | answer=\"MIA-9911\" | ghost deferred_reason=null`; `R3b answer -> answer: \"Rig & Partners PLT\" | answered_by is the corrector: true`; `R3c captured framework: knowledge_record_id set=true | second answer -> CLR10/knowledge_already_live`. 0203:883-886 also refuses a skip of an answered item with the words \"correct it rather than skipping it\", so the door itself names the path the surface now offers. Web side green: fs.web.10 (19/19 in the three firm-setup files) and live in the browser — `firmSetup.walk.answer` passed (12.0s) on a build made in that same run, exercising Change, prefill, form-closes-and-returns-focus, and change-back.",
    "ADVERSARIAL F2 (blocker) — CLOSED. The checklist now records the exact triple before the call (`SentCall`/`openAttempt`/`sentCall`, firm-setup-checklist.tsx:98-131) and re-sends it on a retry, closing the attempt on any governed refusal (`if (isDoorRefusal(err)) …current = null`) on all four write paths. Independently re-raced the reviewer's L1 on 55507: `R1a OLD behaviour (key + re-read revision) -> CLR10 \"op_key reused with different args\" detail=null`; `R1b NEW behaviour (key + the revision that was SENT) -> receipt replayed: \"mia\"`; `R1c receipts for that key = 1 | plan revision unchanged by the replay: true`. The bare CLR10 is mapped to its own face (classify at firm-setup-checklist.tsx:214-217 keys on `code===CLR10 && reason===null && message.includes(...)`; `reason` is null-by-construction for a detail-less refusal, apps/web/lib/wire.ts:216), rendered as `firm-setup-already-recorded` with `FirmSetup.form.alreadyRecorded`. fs.web.06 now rotates the revision in the fixture (the old frozen envelope is what made it vacuous) and fs.web.12 covers the face.",
    "ADVERSARIAL F3 (should) — CLOSED. `firmSetupOpKey()` is `crypto.randomUUID()` and the FNV derivation is deleted (apps/web/lib/firm-setup/api.ts:38-41); every call site passes a per-attempt key. Re-raced both dead ends on 55507 with the deleted derivation reimplemented verbatim in my probe: `R2a OLD derivation, revert to MIA-A -> CLR10 \"op_key reused with different args\" | item still: \"MIA-B\"` vs `R2b NEW per-attempt key, revert to MIA-A -> ACCEPTED | item now: \"MIA-A\"`; and on defer's reason, `R2c OLD derivation (tin), restate the FIRST reason -> CLR10 …` vs `R2d NEW per-attempt key, same reason -> ACCEPTED | state=deferred`. Pinned by fs.web.11 (three writes, three distinct keys, revisions [rev0, rotated-1, rotated-2]) and by the DB cell p648.opkey.attempt.",
    "STANDARDS S1 (should) — CLOSED. `const CONVERGED = { timeout: 30_000 }` (apps/web/e2e/firm-setup-walk.spec.ts:39-48) now covers the assertions after the second sign-in; no CAS or door logic touched. Confirmed live on a fresh build: `pnpm --filter @clara/web e2e -- firm-setup-walk` → 5 passed (2.5m), exit 0, with `firmSetup.walk.stale` at 15.5s — i.e. the convergence really was slow-but-correct, not missing.",
    "STANDARDS S2 (note) — CLOSED. The brief-item-17 loop is closed in writing at docs/plan/active/refresh-wave-2026-09-15/reports/648-final.md:26 (all twelve catalogue rows are text/long_text/choice/month/labelled_object, none takes file evidence, so #633's `(firm)/documents` leaf has nothing here to be the destination of). I re-verified the grep and the absent route myself.",
    "SPEC F1 (note) — CLOSED. The live walk neither reviewer could finish now runs end to end: all five cells passed (start 9.8s, answer 12.0s, stale 15.5s, responsive 9.8s, finish 29.6s), exit 0, against a build produced inside the same `e2e/run.mjs` invocation (log shows `[e2e] building @clara/web before starting the browser walk` → `Compiled successfully`; .next/BUILD_ID rewritten 09-17 02:33:32). The fix worker's stale-build diagnosis (`npx playwright test` serves whatever `.next/` already holds) is confirmed by construction.",
    "ADVERSARIAL N1 (note) — CLOSED. The from-scratch cluster is still up and was used: 127.0.0.1:55607 / clara_648r reports 194 migrations, and the ticket battery is 17/17 there as well as on the lane rig. Left running for the orchestrator.",
    "ADVERSARIAL N5 (note) — CLOSED. The SCOPE_ENTRANCES deviation is recorded as verified in 648-final.md:53 with the merge-prep warning; firm-scope-surfaces / fourth-entrance / parity-holes / sql-oracle / e2e-fixture-ownership are 103/103.",
    "ADVERSARIAL N4 (note) — CLOSED in its reachable half, and I verified the half rather than taking it: on the rig, `R3d after withdrawal: knowledge_record_id=null | item state=answered | confirmed_facts=[[\"reporting_framework\",\"withdrawn\"]]` — so the checklist's `isAnswerable` offers the answer form again (fs.web.10's second arm) and no dead end survives. The SQL half (stating the `superseded_at`-only intent in 0203 §F) is a recorded follow-up at 648-final.md:69, 0203 being byte-frozen this round.",
    "ADVERSARIAL N2 (note) — DEFERRED with a reason I checked, not re-looped: 0203_firm_setup.sql is byte-identical to the reviewed head (`git diff 6a3893e1..HEAD -- packages/db/migrations/0203_firm_setup.sql` empty), and the no-op reseed is unreachable from this surface — the seed control renders only under `notStarted` (firm-setup-checklist.tsx:420, 471-482) and now carries a per-attempt op key so a lost seed replays. Follow-up recorded at 648-final.md:67; orchestrator ratifies.",
    "ADVERSARIAL N3 (note) — DEFERRED with a reason I checked: both consumers guard first — firm-setup-checklist.tsx:415-417 returns the `firm-setup-no-plan` face before any counter renders, and apps/web/components/firm/firm-home/firm-setup-tile.tsx:41 returns null on `plan_id === null` before reading `env.counter`. Follow-up recorded at 648-final.md:68; orchestrator ratifies."
  ],
  "open": [],
  "new_blockers": [],
  "ran": [
    "git log --oneline -5 · git status --porcelain (clean) · git diff --stat 6a3893e1..HEAD -> 11 files, +662/-67 · git diff 6a3893e1..HEAD -- packages/db/migrations/0203_firm_setup.sql -> EMPTY (migration byte-unchanged, so no rollback/re-apply chain was owed) · final state: head dcc7c94a on impl/648-firm-setup, 9 commits ahead of origin/main, worktree clean, nothing pushed, no git worktree command run",
    "read the whole diff: apps/web/lib/firm-setup/api.ts, lib/firm-setup/types.ts, components/firm-setup/firm-setup-checklist.tsx, firm-setup-item-form.tsx, e2e/firm-setup-mock.mjs, e2e/firm-setup-walk.spec.ts, messages/en.json, packages/db/tests/firm-setup.test.mjs, packages/db/README.md, packages/db/tests/README.md, firm-setup-checklist.test.tsx",
    "pnpm typecheck (worktree root) -> TYPECHECK_EXIT=0",
    "pnpm lint (worktree root) -> LINT_EXIT=0 (eslint x4 projects + check-token-contrast + check-test-manifest + check-message-keys + frozen/citation/dynamic-SQL gates)",
    "packages/db on 55507/clara_648: node --test --test-concurrency=1 --import ./tests/firm-setup-preintegration-gate.mjs tests/firm-setup.test.mjs -> 17 tests / 17 pass / 0 fail (p648.answer.correct and p648.opkey.attempt live by name)",
    "the same file on the review's from-scratch rig 55607/clara_648r -> 17/17 pass; cluster confirmed up at 194 migrations",
    "packages/db: node --test tests/operation-census.test.mjs tests/rig-isolation.test.mjs (no reset flags) -> 31 tests / 30 pass / 0 fail / 1 skip (destructive T19)",
    "INDEPENDENT re-race of the reviewer's own sequences, scratch probe (scratchpad/648r1/recheck-probe.mjs, real doors via humanQuery on 55507/clara_648): R1a OLD lost-response retry -> CLR10 'op_key reused with different args' detail=null; R1b NEW retry (key + the revision that was SENT) -> receipt replayed; R1c receipts=1, plan revision unchanged; R2a OLD value-derived key revert -> CLR10, item still MIA-B; R2b per-attempt key -> ACCEPTED, item MIA-A; R2c OLD defer key restating a reason -> CLR10; R2d per-attempt key -> ACCEPTED; R3a defer->answer un-skips with no ghost reason; R3b answer->answer re-attributes to the corrector; R3c second capture of a live key -> CLR10/knowledge_already_live; R3d withdrawal leaves knowledge_record_id null while the tombstone stays in confirmed_facts",
    "apps/web: node --import ./test/bootstrap.mjs --import tsx --test components/firm-setup/{firm-setup-checklist,firm-setup-a11y,firm-setup-keyboard}.test.tsx -> 19/19 pass (was 16; fs.web.10/11/12 are the new ones)",
    "apps/web: the five closed-world censuses (firm-scope-surfaces, firm-scope-fourth-entrance, parity-holes, sql-oracle, e2e-fixture-ownership) -> 103/103 pass",
    "node scripts/check-frozen-workflows.mjs -> OK, 281 frozen files, 51 'use workflow' modules frozen+registered; node packages/runtime/scripts/check-parts-parity.mjs -> OK (reader superset of emittable)",
    "apps/web: pnpm --filter @clara/web e2e -- firm-setup-walk on CLARA_E2E_APP_ORIGIN=https://127.0.0.1:3290 / NEXT_PORT=3291 / RUNTIME_PORT=3292 (ports verified free first) -> BUILT in-run (BUILD_ID rewritten 09-17 02:33:32) then 5 passed (2.5m), E2E_EXIT=0: walk.start 9.8s, walk.answer 12.0s, walk.stale 15.5s, walk.responsive 9.8s, walk.finish 29.6s. Single run, no contention artefacts, no other lane's path in any stack.",
    "apps/web: node scripts/run-tests.mjs (whole suite) -> 3738 tests / 3736 pass / 0 fail / 2 skipped, SUITE_EXIT=0. The load flake the fix worker hit (components/work/work-detail.test.tsx) did not reproduce here — no regression anywhere in the suite.",
    "doc/source-of-truth checks: packages/db/README.md answer-door row now states the correction path; packages/db/tests/README.md names the two new cells; 648-final.md:26 (brief item 17), :53 (assumption 5 / SCOPE_ENTRANCES), :67-69 (the three 0203 follow-ups). Grepped apps/web *.md for stale op-key prose -> no matches, so nothing documents the deleted derivation.",
    "adversarial regression hunt on the fix itself (no defect found): group walk still filters isPending (no settled fact is re-asked); FirmSetupItemForm hides Skip when onSkip is undefined (:309); the form submits once from the review step so close-on-success cannot break the stepper; drafts are cleared only on acceptance (:199-202) so a correction prefills the record, not a stale draft; commit/answer attempts that re-send a superseded revision converge in one extra press via CLR06 stale, never a silent wrong write"
  ]
}