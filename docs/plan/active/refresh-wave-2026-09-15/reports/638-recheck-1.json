{
  "ticket": 638,
  "head": "a26e4dd0",
  "closed": [
    "SEC-1 (should, SPEC) — `already_settled` is now proven by shipped tests and the false citation is gone. `p638.claim.settled` runs for real (116 ms, 0 skipped) and asserts the three derived lines, one entry / one committed receipt / one claim row, payable+advance columns NULL, zero `staff_advance_applications`, and the settlement visible to a VIEWER through `list_staff_expense_claims`; `p638.refusals` gained the two arms pinning `constraint:'asset'` + `field:'claim.payment_account_code'`. I re-raced the reviewer's own door probe on a from-scratch chain: lines `[6200 dr48000, 6210 dr12500, 1150 cr60500]`, `payable/advance_acct/advance_id` null, ledger `[admitted,posted]`, allocations 0, viewer sees `already_settled`; 2010 and 2000 payment legs both refused CLR10 `invalid_claim` / `constraint:asset`. `grep -n 'World leg 1' 638-final.md` → no matches; AC2 now cites `p638.claim.settled`, AC8 reads 24 pass.",
    "F1 (blocker, adversarial) — same arm as SEC-1, same evidence; the blocker was the false AC2 evidence pointer plus zero regression protection, and both are answered (cell + corrected pointer). Nothing in the code moved for it, which matches the reviewer's own finding that the arm already worked.",
    "F2 (should) — the sealed year now refuses at ADMISSION. New world-half arm (o) in `clara._assert_claim_basis` (0206:877-903) raises CLR19 `write_into_closed_period` with `field:'claim.posting_date'` + `fiscal_year_id` + `fy_status`, using the wall's own FY-preference ordering. My own probe on the from-scratch chain: status `closed` → CLR19 at admission, status `closing` → CLR19 at admission, and in BOTH cases accounting_work 0→0, agent_tasks 0→0, claim rows 0→0, entries 0, receipts 0 — nothing durable is spent (the reviewer's P12 got a durable stranded row here). Over-refusal controls I added myself: a year with status `open` and one with status `reopened` still admit AND post (1 entry each), so the arm refuses the sealed year and nothing else. Read-check that it cannot shadow a legitimate permitted write: `clara._tf_period_wall` (0056:684-700) only honours permits whose `created_xact = pg_current_xact_id()` for purposes `close_entry`/`reopen_reversal` bound to a target entry id — unreachable from `wake_record_journal_entry`, so no permitted path is blocked. The commit wall is still asserted live by the cell and the admission-to-posting residual is named at 638-final.md:213.",
    "F3 (should) — two concurrent corrections now refuse by name. Door step 6a re-asks the world half UNDER the client rung (`pg_advisory_xact_lock(203005004, hashtext(client))`); the door is VOLATILE so the re-ask statement takes a fresh READ COMMITTED snapshot after the rung is granted, and the STABLE validator inherits it. I re-raced the reviewer's exact sequence with MY OWN third-session barrier (raw superuser connection holding the rung, waiters counted from `pg_locks`, no sleep, independent of the branch's new `withClientRungHeld` helper): outcomes `[ok, CLR10]`, loser detail `{reason:'correction_target_already_corrected', field:'claim.corrects_claim_id', claim_id:…}`, message \"that claim has already been corrected\" — the raw `23505 uq_staff_expense_claims_corrects` the reviewer got is gone. Data intact: 2 claim rows, `corrected_by_claim_id` names the winner once. `workErrorStatus` maps CLR10 → 400 (workRoutes.ts:613-615), so the route answers a typed refusal, not `{error:\"internal\"}`.",
    "F4 (should) — AC4's document half is asserted. `p638.correction.evidence` (178 ms) is real, and I re-ran its sequence myself: link born `attached_via='work_commit'` with `released_at` null; a second claim citing the same receipt while the entry stands is refused CLR13 `source_already_posted`; `reverse_entry` makes `t_entry_evidence_release` stamp `released_at` (link released, never deleted, still naming the document); the correcting claim cites the SAME receipt, posts, and holds the only live link.",
    "F5 (note) — both stale comments now say the measured six: 0206:97 (\"the census stays at the MEASURED SIX above\") and 0206:1483 (\"the MEASURED SIX of this file's header … NOT the four 0037's own text names\"). Comment-only; the from-scratch apply printed tail OK (2/7) again.",
    "F7 (note) — superseded and re-done properly. I did not reuse the fix worker's rolled-back/re-applied database: I DESTROYED and re-created cluster `rig638r` on 55603 with mkrig.sh (verified fresh: 3 default DBs, 16 default roles), created `clara_638r` and ran the whole chain from zero with the EDITED 0206. That is the from-scratch evidence the round needed."
  ],
  "open": [
    {
      "id": "F6",
      "why": "Unchanged and still the orchestrator's, not the branch's: 0206 §0 refuses to apply unless the `_subledger_on_approve` caller roster matches a byte-exact six-name string, and the census regex scans every clara body, so a sibling that merely NAMES the hook in any function source will trip 0206's prestate at integration. The fix worker correctly left the branch alone and recorded the recipe as follow-up 0 (638-final.md:176). I re-proved the prestate holds for THIS branch in isolation (fresh cluster, 0001→0198+0206, `#638 prestate: clean …`, tail OK 1/7…7/7), which says nothing about the merged 0199–0209 chain. Action stands: apply the merged chain from scratch once before release and re-measure/re-issue the roster string if §0 refuses — never weaken it."
    }
  ],
  "new_blockers": [],
  "ran": [
    "git (worktree 638): `git log --oneline -3` → tip **a26e4dd0**; `git status --porcelain` → EMPTY (branch untouched by me); `git log origin/main..HEAD` → 13 commits; `git diff ce11cc88..HEAD --stat` → 4 files, +354/−17, ALL in packages/db (0206_staff_expense_claims.sql, tests/staff-expense-claim.test.mjs, tests/staff-expense-claim-fixtures.mjs, tests/README.md) — nothing in apps/web or packages/runtime, as the report claims; both new commits carry the Co-Authored-By trailer (`--format=%(trailers:key=Co-Authored-By)`).",
    "FROM-SCRATCH CHAIN (migration changed, so re-built rather than rolled back): PowerShell → `wsl -u root -- bash …/mkrig.sh rig638r 55603` re-created the cluster; verified fresh via node/pg (databases `postgres,template0,template1`; `pg_roles` count 16). Then createdb `clara_638r` → `pnpm db:migrate` = **`migrate: 194 new migration(s) applied · 194 total`, exit 0**, carrying `#638 prestate: clean -- no claim surface exists, both purpose CHECKs carry their 0194 three values, the six non-regression bodies are at their pinned texts, the subledger-hook census is the measured six, and 0042 tail 20(a)/(b) stand` and `#638 tail OK (1/7)` … `(7/7)`. Re-run → `0 new migration(s) applied · 194 total` (idempotent). `pnpm db:seed` → 2 seed files. Battery on that chain → **24 pass / 0 fail / 0 skip**.",
    "LANE RIG BATTERY (127.0.0.1:55503/clara_638): `node --test --test-concurrency=1 tests/staff-expense-claim.test.mjs` → **24 pass / 0 fail / 0 skipped**, run twice. Per-cell durations prove the new cells really execute and are not gate-skipped: `p638.claim.settled` 116 ms, `p638.refusals …CLOSED fiscal year… at ADMISSION` 117 ms, `p638.correction.race` 180 ms, `p638.correction.evidence` 178 ms.",
    "INDEPENDENT PROBE re-racing the reviewer's sequences on 55603/clara_638r (scratchpad script, NOT committed, worktree left clean): P1 `already_settled` admit+post + both non-asset payment refusals; P2 sealed year at `closed` AND `closing` with before/after counts of accounting_work, agent_tasks, claims, entries, receipts; P2 controls at `open` and `reopened` (both admit and post); P3 correction race barriered by my own raw superuser session on rung 203005004 with waiters read from `pg_locks`; P4 the full document unwind. All four came back as the fix claims — full values quoted in the `closed` entries above.",
    "NEIGHBOUR DB BATTERIES (lane rig, one run, no reset flags): `node --test --test-concurrency=1 tests/periodic-adjustment.test.mjs tests/work-journal-post.test.mjs tests/operation-census.test.mjs tests/rig-isolation.test.mjs` → **82 tests, 81 pass, 0 fail, 1 skip** (T19 destructive, by design), 323 s, exit 0.",
    "RUNTIME: `node --import tsx --test tests/staff-expense-claim-unit.test.mjs` → **14 pass / 0 fail**. World e2e `PGDATABASE=clara_rt_test … node tests/staff-expense-claim-e2e.mjs` → **PASS 1..7, `STAFF EXPENSE CLAIM E2E: PASS`, exit 0, first attempt** — which also proves the rolled-back/re-applied 0206 on that database passes the runner's checksum drift guard.",
    "STATIC / FROZEN: `pnpm typecheck` → **exit 0** (apps/web + packages/runtime both Done); `pnpm lint` → **exit 0** (apps/web incl. check-message-keys selftest + the real gate green, and packages/reporting-render eslint); `node scripts/check-frozen-workflows.mjs` → OK, 281 frozen files, 51 \"use workflow\" modules; `node packages/runtime/scripts/check-parts-parity.mjs` → OK, reader ⊇ emittable.",
    "READ-CHECKS: the full `ce11cc88..HEAD` diff of the migration (arm (o), step 6a, the two comment fixes) and of the three test files; `clara._tf_period_wall` 0056:643-712 (permit semantics — why arm (o) cannot block a legitimate permitted write); the sibling `clara._assert_adjustment_relationships` arm 0194:975-983 (same CLR19 shape, so the house spelling is preserved); `workErrorStatus` (CLR19 → 400 at 0627, CLR10 → 400 at 0613) so both refusals leave the admission route typed; call sites of `_assert_claim_basis` (posting still asks world=false, so the wall stays the only closed-period law at commit).",
    "BLAST-RADIUS CHECKS: `grep -rl staff-expense-claim-fixtures` → imported ONLY by staff-expense-claim.test.mjs (so the new named re-export of #634's `evidenceDocument/docRef/linksForEntry/linksForDocument` cannot disturb another battery; `reverseEntry` still resolves — my own probe imported and used it). Playwright NOT re-run, with a reason rather than an omission: `staff-expense-claim-walk.spec.ts` is served by `staff-expense-claim-mock.mjs` via `apps/web/e2e/serve-built.mjs:63` and never reaches the database door, so a packages/db-only round cannot regress it; its 12/13 + solo-green residual from the cut still stands, as does \"hosted evidence pending\".",
    "REPORT CHECKS (docs/plan/active/refresh-wave-2026-09-15/reports/638-final.md): `grep -n 'World leg 1'` → no matches (the false pointer is gone); AC2 cites `p638.claim.settled` + the two refusal arms; AC4 cites `p638.correction.evidence` and `p638.correction.race`; AC8 reads 24 pass / 0 fail / 0 skip; the admission-to-posting residual is named at :213; follow-up 0 carries F6's integration recipe at :176; 638-fixround-1.md exists."
  ]
}