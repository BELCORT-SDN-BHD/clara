# #654 — fix round 1

**Branch** `impl/654-firm-defaults` · **worktree** `C:\Users\zhant\Desktop\clara-wt\654` · rigs PG `127.0.0.1:55512`/`clara_654` and `127.0.0.1:55612`/`clara_654r` (the reviewer's from-scratch chain) · head at review `4b69d2b2` → **head now `ba2c1cfe`**. All evidence LOCAL; **hosted evidence pending**.

```
ba2c1cfe fix(db):  0205's evidence census is the LIVE invariant, with history reported
0618b8f2 fix(web): the correct leg must change the value it asserts
e734b6b5 feat(web): Correct and Withdraw a firm rule, from the register that owns it
4c60a5f7 fix(db):  the cross-client evidence wall is two-way and covers both client-bearing pins
```

## Finding → what I did → evidence

| # | Finding | Sev | What I did | Evidence (red → green) |
|---|---|---|---|---|
| 1 | **654-ADV-1** — the evidence wall fires only on INSERT into `knowledge_records`, so filing a document *after* a firm rule cited it produces the contamination anyway, and the same trigger then refuses the retraction (a withdrawal carries the predecessor's pins verbatim), leaving the rule stuck live. The battery pinned the hole as intended behaviour at `:285-287`. | blocker | **Applied.** New `clara._tf_document_filing_firm_knowledge`, BEFORE INSERT OR UPDATE on `clara.document_filings`: refuses a filing whose document a **live** firm-scope record cites — CLR10 `document_cited_by_firm_default`, detail carrying `document_id`/`record_id`/`knowledge_key`, message naming the remedy (withdraw or re-source the firm rule first). `_tf_knowledge_firm_evidence` gained a **narrow retraction hatch**: `revision_kind in ('correction','withdrawal')` whose `source_document_id` AND `source_work_id` are both unchanged from `supersedes_id` is admitted. `:285-287` rewritten: the filing is now refused BY NAME, the refusal's remedy is exercised, and the original INSERT wall is re-proved afterwards. | RED, measured on 55512: `filing a document that a live firm-scope rule cites: expected SQLSTATE CLR10 but the call SUCCEEDED`; and `correct_knowledge` on a contaminated rule refused `firm_scope_client_evidence`. GREEN: `p654.evidence.admits_unfiled_firm_document` + `p654.evidence.retraction_survives_contamination` (revisions land `[1 capture superseded][2 correction superseded][3 withdrawal withdrawn]`). |
| 2 | **654-ADV-2** — `source_work_id` is a second client-bearing pin and is unwalled; a firm default may cite one client's `accounting_work` and that id reaches every other client's pack. | should | **Applied.** Second arm in `_tf_knowledge_firm_evidence`: any `source_work_id` on a firm-scope row is refused, CLR10 `firm_scope_client_work` with `work_id`/`client_id`. Absolute rather than conditional because `clara.accounting_work.client_id` is **NOT NULL** (measured off `information_schema`), so every Work is one client's. The migration header now states which pins the wall covers and why the extraction/region/field pins ride the document arm (`ck_knowledge_records_extraction_*`, `0192:461-470`). | RED: `firm capture pinning a client's accounting_work: expected SQLSTATE CLR10 but the call SUCCEEDED`. GREEN: `p654.evidence.refuses_client_work` — refused by name, no row lands, and the same pin on a CLIENT row still succeeds (the firm register's live-Work affordance derives from it). |
| 3 | **654-ADV-3** — DECISIONS §2/#654 binds the entrance to four things; Correct/Withdraw were not built and no other route reaches a firm record (`knowledgeRecordHref` needs a `clientId`; the only detail route is `/clients/[clientId]/knowledge/[recordId]`). A promoted rule could never be corrected or withdrawn. | should | **Applied, not descoped.** New `apps/web/components/registers/knowledge-firm-acts.tsx` mounts `correct_knowledge` / `withdraw_knowledge` on every live rule of the firm register through `ArApCounterpartyDoorDialog` + `toDialogRefusal` (the shape `knowledge-detail.tsx` uses), withheld below the admin floor with a sentence rather than a blank (裁-187), absent where the DB says `correctable=false`, each act followed by a re-read of `list_firm_knowledge`. The register's own `DataState` is now gated on "nothing known yet" so a refusal cannot blank the subtree and unmount the open dialog. New `FirmKnowledge.acts.*` message block; `apps/web/README.md` row updated. | RED: no `Correct`/`Withdraw` button existed on the register (kf.09/kf.10 could not find a trigger). GREEN: `knowledge-firm-panel.test.tsx` **11/11** (was 8) — kf.09 correct (reason required, door called with `p_record` = the record_id, fresh `op_key`), kf.10 withdraw (verbatim CLR04 inside the dialog, draft intact, register unblanked), kf.11 below-the-floor + non-correctable. Walk: 3 new legs, `27 passed / 0 failed`. |
| 4 | **SPEC F1** — the claraWork_v4 stanza's refusal map names `invalid_question_fields`, which `clara.open_work_question` never raises. | should | **Applied** (report-only; the stanza is a contract, not code). `654-final.md:68` now carries the real map, re-measured. | `grep -c invalid_question_fields 0180_work_questions.sql` → **0**; `grep -c "'invalid_fields'"` → **14**; the map at `0180:55-67` also yields `wrong_task_kind`, `work_unbound`, `question_already_pending`, `task_not_running`, `task_not_found`, all now in the stanza. |
| 5 | **654-ADV-4** — no idempotency, typed-conflict or race cell for the promotion act. | note | **Applied.** Three cells added exactly as the finding shapes them. | `p654.promote.replay_is_one_receipt` (identical envelope, 1 `clara.op_receipts` row, 1 knowledge row), `p654.promote.op_key_conflict` (`op_key reused with different args`, nothing moves), `p654.promote.race` (two raw connections, barrier = `uq_knowledge_live` itself with `pg_stat_activity.wait_event_type='Lock'` asserted before the winner commits; loser CLR10 `knowledge_already_live`, exactly one live row, no 40P01). |
| 6 | **SPEC F2** — no `emulateMedia({reducedMotion:'reduce'})` anywhere in the knowledge walks. | note | **Applied for this lane's spec only.** One leg on the promote dialog: open, focus, axe, Escape, focus return, all under reduced motion. | `knowledge-firm-walk.spec.ts` leg "reduced motion: the promote dialog still opens, takes focus and closes" — green in the 27-spec run. |
| 7 | **STANDARDS `unused-i18n-keys`** — `FirmKnowledge.exceptionOpen`, `exception.inForce`, `exception.clientOnly` defined, never referenced. | note | **Applied — deleted**, not wired. `exceptionOpen` duplicates the namespaced `openRegister` keys that are used; `inForce`/`clientOnly` describe states `KnowledgeExceptionPair` deliberately does not render (it shows only the decided override — a pair, never a second row). | 3 lines removed from `messages/en.json`; `pnpm lint` (which runs `check-message-keys` over 2994 static keys) exit 0. |
| 8 | **SPEC F3** — a lone `page.goto` ERR_ABORTED on the firm walk under full-suite load is host contention. | note | **No action**, as the finding asks. | My own 27-spec run had no such failure. |
| 9 | **654-ADV-5** — rig housekeeping. | note | **Done and recorded** (below). `rig654r` left up. | See "Rig work". |

## Counts

| Check | Result |
|---|---|
| `knowledge-firm-defaults.test.mjs` (29-gate chain, 55512/`clara_654`) | **20 cells — 20 pass / 0 fail / 0 skipped** (was 15) |
| …the same battery on 55612/`clara_654r` (the reviewer's from-scratch chain) | **20 pass / 0 fail / 0 skipped** |
| + `knowledge-records` + `knowledge-onboarding-promotion` | **60 / 60 / 0** (was 55/55) |
| + `operation-census` + `rig-isolation` (no reset flags) | **91 tests, 90 pass, 0 fail, 1 skipped** (destructive T19, by design) |
| `knowledge-firm-panel.test.tsx` | **11 / 11** (was 8) |
| Whole `apps/web` suite (`node scripts/run-tests.mjs`) | **3744 tests, 3741 pass, 1 fail, 2 skipped**, 306 s |
| …that 1 fail, re-run in isolation | `lib/clara/use-clara-thread-stop.test.ts` → **25 pass / 0 fail**. A file this branch never touches; reported as host contention, **not fixed** |
| Playwright `e2e knowledge` on 3340/3341/3342 | **27 passed / 0 failed** (1.6 m). First run: 26 passed / **1 failed** on my own new leg — it filled the reason but not the value it then asserted, a real cell defect fixed in `0618b8f2`, not a flake |
| `pnpm typecheck` / `pnpm lint` | exit 0 / exit 0 |
| `check-frozen-workflows.mjs` / `check-parts-parity.mjs` | exit 0 (281 frozen files, 51 `use workflow` modules) / exit 0 |

## The migration was rolled back and re-applied from a true prestate — twice, on two databases

`0205` is unmerged, so this is the ordinary loop. On 55512/`clara_654`: objects dropped, ledger row removed, prestate re-verified, `pnpm db:migrate` → `1 new migration(s) applied · 194 total`, with `#654 prestate: clean …` and `#654 tail: OK …` both printed. Repeated after the census change (`ba2c1cfe`), this time against a database that actually holds three superseded/withdrawn firm-scope revisions naming a client-bearing source — the new notice fired verbatim: *"3 superseded/withdrawn firm-scope revision(s) still NAME a client-bearing source. Reported, not refused…"*, prestate still clean, tail still OK. Then the same rollback/re-apply on 55612/`clara_654r`, the **from-scratch 194-migration chain the reviewer built**: clean prestate, OK tail, battery 20/20.

**One change I made that no finding asked for, and the orchestrator should look at it.** `§0(8)` and `§E T.4` counted *every* firm-scope revision citing a filed document. With the retraction hatch in place that is stricter than the wall and no longer a true statement about it — a withdrawal **is** admitted, on purpose, and once a rule is withdrawn, filing its document is the remedy the refusal points at. Both censuses now refuse on **LIVE** rows (what the three guards jointly enforce and what every read emits) and raise a **NOTICE** naming the historical count. Nothing is hidden; the strict number is still printed. Reverse it if you would rather the file refuse to apply against post-retraction history.

## Deliberately left

- **SPEC F2 in `#644`'s `knowledge-walk.spec.ts`.** The finding asks for the reduced-motion leg "once for both knowledge specs together". That file is #644's, not this ticket's; editing it would widen the diff into another ticket's surface for a note. Added to this lane's spec only; a follow-up can do #644's.
- **654-ADV-1's honest caveat about `p_source: {}`.** The promote dialog still pins no source (`apps/web/lib/registers/knowledge.ts:423`). That is the shipped promotion path's own behaviour (`0192:1706-1711`) and the brief's instruction, not a defect; the wall now holds against the door that *can* create a sourced record, which is the one that mattered.
- **The `t_document_filings_firm_knowledge` UPDATE arm is a belt, not the buckle**, and the migration says so: `0007`'s own `_tf_document_filing_update` (`0007:525-539`) admits exactly one transition, active → retired with actor and reason, and raises CLR17 on anything else, so un-retiring a filing is already impossible. The arm is scoped to precisely that transition so every other update still reaches 0007's trigger with its own error unchanged.
- **A brand-new WSL cluster for a fourth from-scratch chain.** Attempted on `rig654r` as a second database (`clara_654fx`) and it failed at `0154_binding_proposal_pr_1`'s tail — *"the clara role count moved from 14 to 18"* — because Postgres roles are **cluster-wide**, so a second database on a cluster that already carries `clara_654r`'s roles can never pass that census. Dropped `clara_654fx`; used `clara_654r` itself instead (see above). Not a defect in anything on this branch.
- **Hosted evidence** for any part of this slice. No hosted lane exists.

## Rig work (654-ADV-5)

- `55512/clara_654`: deleted **3** firm-scope rows carrying a client-bearing `source_work_id` (2 left by the reviewer's ADV-2 probes, 1 by my own red run) and retired **2** document filings left live by red-iteration runs that aborted before their cleanup, so 0205's prestate was satisfiable. Deletions used a session-local `session_replication_role='replica'`, never a global `alter table … disable trigger`.
- `55612/clara_654r`: deleted **1** reviewer probe row with a live client Work pin, then rolled back and re-applied 0205. Left **up**, as instructed — now carrying the fix-round 0205. Drop with `wsl -u root -- pg_dropcluster --stop 17 rig654r` when the wave closes.
- Both rigs now report **0** live firm-scope rows citing a filed document or pinning a Work.

## Sources of truth updated on the branch

`packages/db/README.md` (the wall is two-way, both pins, the live invariant), `packages/db/tests/README.md` (20 cells, the new fixtures), `apps/web/README.md` (correction/withdrawal happen on the firm register). `docs/PRD.md` / `docs/ARCHITECTURE.md` untouched. `654-final.md` updated in place: commit list, AC1/AC4/AC5 evidence, all counts, the corrected refusal map, and the now-built follow-up removed.
