{
  "ticket": 625,
  "head": "241acf74",
  "closed": [
    "ADVERSARIAL F1-preview-accept-wall-divergence (SHOULD) — CLOSED at the reviewer's own stated minimum (b). Re-raced the reviewer's probe independently on clara_625: clara.accept_invite's LIVE prosrc still carries `if clara.role_rank(inv.role) > coalesce(v_issuer_rank, -1) then raise ... 'invite exceeds the issuer''s rank -- re-issue by an owner' (CLR04)`, while clara.preview_invite's body contains no 'issuer' and no 'role_rank' at all — so the divergence the reviewer measured is real and unchanged. What the fix did about it is what (b) asked: the false sentence is GONE from C:\\Users\\zhant\\Desktop\\clara-wt\\625\\packages\\db\\migrations\\0209_preview_invite.sql:30-45 and from C:\\Users\\zhant\\Desktop\\clara-wt\\625\\apps\\web\\lib\\firm\\invite-preview.ts:12-22 (grep over the four files: zero hits for 'could not also ACCEPT' / 'nothing is previewable'; the only surviving 'previewable' is the unrelated `invite_not_previewable` refusal reason); the residual is NAMED in the 0209 header, packages/db/README.md (0209 note) and apps/web/README.md's Membership section; and it is PINNED by the new cell `p625.preview.issuer_rank` in packages/db/tests/preview-invite.test.mjs, which asserts preview status=pending/role=admin after the owner demotes the issuer, accept=CLR04 with that exact sentence, zero membership minted, the row still pending, and firm_invites_visible reporting pending too. Battery ran twice (before and after a true-prestate rollback+re-apply of 0209): 12 tests, 12 pass, 0 fail, 0 skipped both times. Option (a) is NOT applied and the worker's reason is sound and checkable: DECISIONS.md §2 '#625 membership lifecycle' fixes the set at 'Four invite faces from the typed InviteVerificationFailure', and brief-625 §3 binds this door's effective status to clara.firm_invites_visible's own expression (0209's tail asserts it fragment-for-fragment), so a fifth status would need the VIEW widened too — filed as follow-up 3 in reports/625-final.md.",
    "ADVERSARIAL F2-readme-eleven-courier-codes (note) — CLOSED. apps/web/README.md:48 now reads 'TEN typed courier codes', and apps/web/lib/members/doors.ts:310-335 declares exactly ten `InviteCourierCode`s in both the union and the `INVITE_COURIER_CODES` data array (no_session, cross_origin, invalid_request, unsupported_address, not_permitted, mail_not_configured, recipient_has_account, mail_unavailable, mail_failed, transport). Count matches.",
    "ADVERSARIAL F3-dead-tail-guard-T2 (note) — CLOSED, and independently measured. 0209 §C (T.2) now does `v_n := position('return jsonb_build_object(' in v_src); if v_n = 0 then raise ... CLR10; end if; v_ret := substr(v_src, v_n);` (v_n is declared at 0209:256, so the reuse is legal). Positive control on the rig: `position('zzz' in 'abc')`=0 and `substr('abc',0)`='abc', so the OLD `if v_ret = ''` shape raised NOTHING (dead, as the finding said) while the NEW shape raised with errcode CLR10. The amended §C then ran for real during a rollback+re-apply of 0209 on clara_625 and printed '#625 tail: OK …'.",
    "ADVERSARIAL F4-preview-step-never-runs-in-a-browser (note) — CLOSED. Two new cells in apps/web/e2e/members-invite-walk.spec.ts (:254 and :310) both passed in my own Playwright run on a quiet host (3.4s and 3.2s), inside a 5/5 green members-invite-walk. They are real browser legs, not jsdom: the harness log for the run records 2 × `POST /auth/v1/verify` and 2 × `POST /rest/v1/rpc/preview_invite`. The cells assert no password input before the act, the preview region carrying firm + 'Bookkeeper' + 'n***@larkin.test' and NOT the unmasked address, document order (compareDocumentPosition) putting the block before the password field, exactly one door POST whose Bearer payload decodes to the invitee's own sub/email, and a revoked preview rendering its own face with zero password inputs. The lane header and the spec both state this proves the client leg, not the door (the door stays the DB battery's claim) — honest, not overclaimed.",
    "ADVERSARIAL F5 / STANDARDS note-1 / SPEC F2 — SHARED_RPC_VERBS substitution (note ×3) — CLOSED FOR THIS LOOP as 'ratification requested'; the ORCHESTRATOR still owes a ruling (ratify CORE_RELATION_HANDOVERS as the substitute, or amend brief-625.md:66). No code change was made and none was needed: all three lenses independently confirmed the brief's literal instruction is mechanically impossible, and it is recorded as assumption 0 at reports/625-final.md:51. The second half (widening CORE_RELATION_HANDOVERS to the three pre-existing takeovers) is deferred to the repo-wide sweep ticket the census header proposes — correct call in a twelve-lane wave, since it would put a shared-file conflict in front of eleven siblings.",
    "ADVERSARIAL F6 / STANDARDS note-2 — dead verb-set export (note ×2) — CLOSED, verified by direct call, not by reading the comment. members-lifecycle-mock.mjs:222-223 now runs `if (!matchVerb(MEMBERS_LIFECYCLE_RPC_VERBS, verb)) return false;` before any branch, and preview_invite is declared in the set. My probe called the exported handler directly: an undeclared verb (/rest/v1/rpc/list_review_queue) → handled=false with the body stream NOT drained and no cache written; POST /auth/v1/token → false, not drained; a foreign persona's roster read → false, not drained. e2e/e2e-fixture-ownership.test.ts 16/16 and e2e/mock-dispatch.test.ts 6/6.",
    "ADVERSARIAL F7-preexisting-red-confirmed (note) — CLOSED as out of scope, confirmed by diff: `git diff 4464e471..HEAD -- apps/web/e2e/firm-navigation-walk.spec.ts` is empty, i.e. this branch never touches that file at all (not just since c6f18f8c). Already filed as follow-up 4 in 625-final.md.",
    "ADVERSARIAL F8-fresh-cluster-left-running (note) — CLOSED. The fix worker dropped rig625r; I re-created it for this re-check (mkrig.sh rig625r 55601 + createdb clara_625r) and dropped it again at the end (`wsl -u root -- pg_dropcluster --stop 17 rig625r`). pg_lsclusters now shows rig625/55501 online and no rig625r; no other lane's cluster was touched.",
    "SPEC F1 — responsive-shell-walk/members-invite-walk non-determinism (note) — CLOSED as host contention, no action against the branch. On a quiet host (0 chrome processes at start) my run was 8 passed / 1 skipped, PW_EXIT=0, with all five #625 cells green — the fix round's two reds on cells 1/3 (and the reviewer's reds on untouched #736/#732 cells) did not reproduce. Nothing was 'fixed' in response to them."
  ],
  "open": [],
  "new_blockers": [],
  "ran": [
    "Worktree state: C:\\Users\\zhant\\Desktop\\clara-wt\\625 — HEAD 241acf74 on impl/625-membership-lifecycle, `git status --porcelain` empty before and after my checks; `git diff --stat c6f18f8c..HEAD` = 9 files, +379/−19. I edited nothing, pushed nothing, ran no git worktree command, touched no other lane's worktree/cluster/ports.",
    "pnpm typecheck (worktree root) → exit 0 ('apps/web typecheck: Done', 'packages/runtime typecheck: Done').",
    "pnpm lint (worktree root) → LINT_EXIT=0; all four projects Done, including apps/web's eslint + check-token-contrast + check-test-manifest(+selftest) + check-message-keys(+selftest) and the root frozen/leak/dead-citation/dsn-pipe selftests.",
    "node scripts/check-frozen-workflows.mjs → 'freeze-lint: OK — 281 frozen file(s) verified', exit 0. node packages/runtime/scripts/check-parts-parity.mjs → 'parts-parity: OK — reader ⊇ emittable', exit 0. (Runtime is untouched by the diff; run anyway.)",
    "packages/db, 29 preintegration gates, PGPORT=55501/clara_625, CLARA_ALLOW_MISSING_PREVIEW_INVITE UNSET: preview-invite + p4t1-invite + p4t1-reads + p4t1-identity + p4t1-add-member-regression + mdrw-rank-walls + p4-4-role-rank-pin → 102 tests · 102 pass · 0 fail · 0 skipped (84s). The reviewer's 90 + the battery's now-12 = 102; the new cell is p625.preview.issuer_rank.",
    "packages/db, 29 gates, no reset/role-sweep flags: operation-census + rig-isolation → 31 tests · 30 pass · 0 fail · 1 skipped (destructive T19, as RIG.md requires).",
    "Migration re-race on the lane rig (the migration changed, so it was rolled back first): drop function clara.preview_invite(text) + delete the ledger row → measured prestate 193 migrations, 0 preview_invite functions; then `pnpm db:migrate` → '[notice] #625 prestate: clean -- clara.preview_invite does not exist; clara.firm_invites carries ZERO application-role grant …', '[notice] #625 tail: OK -- … ACL of exactly {clara_fn_owner, clara_authenticated} … EXACTLY four quoted keys … EXACTLY ONE copy of the shared no-oracle refusal … no write verb and no row lock of any kind … firm_invites still FORCES RLS … this file recuts nothing', 'migrate: 1 new migration(s) applied · 194 total', exit 0; immediate re-run → '0 new migration(s) applied · 194 total'; focused battery after the re-apply → 12/12, 0 skipped.",
    "Checksum ledger check: sha256 of packages/db/migrations/0209_preview_invite.sql with CRLF→LF = 52d6b46c6ded762d912c0b4af88f55d48aa04bd68accf9252ca9c37bdebad233, identical to clara.schema_migrations' stored checksum for 0209_preview_invite — the AMENDED file is the one applied, no checksum drift left behind on the rig (migrate.mjs:335 would otherwise refuse every later migration).",
    "Door-body comparison on clara_625 (the F1 race): clara.accept_invite prosrc carries the issuer-rank declare/select/raise (CLR04 'invite exceeds the issuer''s rank -- re-issue by an owner'); clara.preview_invite prosrc matches /issuer/i = false and /role_rank/i = false; its status expression is firm_invites_visible's `case when i.status = 'pend…`. Grants: EXECUTE only to clara_fn_owner + clara_authenticated; zero application-role grants on clara.firm_invites.",
    "F3 positive control on the rig: `select position('zzz' in 'abc'), substr('abc',0)` → 0 and 'abc'; a DO block with the OLD shape raised nothing (printed 'OLD guard did NOT fire; v_ret length=26'), the NEW shape raised with errcode CLR10.",
    "Playwright, my three ports only (CLARA_E2E_APP_ORIGIN=https://127.0.0.1:3230, NEXT_PORT=3231, RUNTIME_PORT=3232), started on a quiet host (0 chrome processes, no listener on 3230-3232): `pnpm --filter @clara/web e2e members-invite-walk signup-confirm-pending` → PW_EXIT=0, 8 passed / 1 skipped (4.1m). members-invite-walk 5/5 (:68 35.0s, :155 31.8s, :185 24.4s, :254 3.4s NEW, :310 3.2s NEW); signup-confirm-pending 3 passed / 1 skipped. Harness traffic in the log: 2 POST /auth/v1/verify, 2 POST /rest/v1/rpc/preview_invite, 3 POST /auth/v1/token, 3 POST /auth/v1/signup.",
    "Cross-lane check of the SHARED-file change the fix made (serve-built.mjs: the #625 hook moved AHEAD of the auth branches; it is the only app-harness file in the diff that other lanes share). Direct handler probe: signup-shaped /auth/v1/verify → handled=false, body read but CACHED on the request; serve-built.mjs:326 is `const readJson = readCachedJson;`, so the CORE branch reads the identical parsed object — no drained-stream hazard. Independently, `grep -rn verifyOtp app components lib` shows the ONLY browser caller is app/(entry)/invite/[token]/page.tsx (the signup confirm path 'no longer calls verifyOtp at all' — app/(entry)/auth/confirm/verify/handler.ts:24), so the CORE verify branch has no browser claimant to lose. signup-confirm-pending passing behind the reordered hook is the live half of this.",
    "apps/web unit re-runs on the files the fix touched or that read them: e2e/e2e-fixture-ownership.test.ts → 16/16; e2e/mock-dispatch.test.ts → 6/6; components/invite-accept-form.test.tsx + lib/firm/invite-preview.test.ts → 50/50. (The whole 3747-test suite was NOT re-run this loop: outside the DB migration, the fix's only non-test edits are comment-only — apps/web/lib/firm/invite-preview.ts — plus two READMEs, and the only two unit files that read the changed harness sources are the two above.)",
    "FROM-SCRATCH CHAIN: ATTEMPTED AND ABANDONED, NOT A BRANCH DEFECT. mkrig.sh rig625r 55601 → accepting connections; create database clara_625r → ok; `pnpm db:migrate` against 127.0.0.1:55601/clara_625r → FAILED after 56 migrations in ~50 minutes: 'migrate: FAIL — migration 0057_wave_e_registry_snapshots failed and was rolled back: canceling statement due to statement timeout'. 0057 sets its own `set local statement_timeout = '5min'` (0057:79) and is 152 migrations ahead of 0209, untouched by this branch; the host is carrying twelve lanes (49 node processes at the time). Substitute evidence for the changed migration is the true-prestate rollback + re-apply on clara_625 above (193→194, prestate + tail notices printed, idempotent on re-run, battery 12/12). The half-built cluster was dropped; rig625/55501 is online with 194 migrations and preview_invite present."
  ]
}