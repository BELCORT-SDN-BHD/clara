{
  "ticket": 652,
  "head": "f917c4ae",
  "closed": [
    "NB1"
  ],
  "open": [],
  "new_blockers": [],
  "ran": [
    "git log --oneline -8 / git status --porcelain (worktree C:\\Users\\zhant\\Desktop\\clara-wt\\652) -> head f917c4ae, ONE commit past recheck-1 head cebfcca5, worktree CLEAN, nothing pushed (12 commits ahead of origin/main); git diff cebfcca5..HEAD --stat -> 13 files, +433/-5; I edited nothing (git status still clean and HEAD still f917c4ae at the end of this loop)",
    "CHECKSUM PROOF the rig carries the tip's SQL: sha256sum packages/db/migrations/0207_accrual_adjustments.sql = 705567951c8624dbec53be84c45c238888293729111c6e6e722e6386241465d3 == the applied clara.schema_migrations checksum on clara_652 (194 rows), so every probe below ran against the branch-tip body with no drift",
    "NB1 / A-INDEPENDENT DOOR PROBE (my own script against the two real doors on clara_652, fixtures used only to build the world): L1 term+window 2026-07-01..2026-07-15 monthly/last_day_of_month -> REFUSED CLR10 {reason:accrual_schedule_yields_no_occurrence, field:day_rule, constraint:yields_occurrence, effective_from/to echoed}; L2 one-day term 2026-07-10..2026-07-10 -> same, field day_rule; L3 day_of_month=20 on a term ending the 15th -> same, field day_of_month. Footprint measured BEFORE and AFTER all three: plans 0 / revisions 0 / occurrences 0 / accruals 0, and clara.op_receipts holds NO row for any of the three keys -- the wall is payload-half, asked before _reserve_op. L4: the OBO door (clara_runtime) refuses the identical shape with the identical detail. The exact shape recheck-1 measured as silently accepted is now unreachable through either entrance",
    "NB1 / WALL-NOT-BAN, measured end to end: L5 the SAME half-month term 2026-07-01..2026-07-15 with day_rule=day_of_month, day_of_month=15 -> ACCEPTED, occurrence 2026-07-15 admitted at configuration time and clara.accounting_plan_occurrences holds ['2026-07-15','primary'] (a catch-up over the window then admits 0 because it is already there). So the refusal names a control the preparer can actually move",
    "NB1 / NO FALSE REFUSAL, NO FALSE ACCEPTANCE -- my own 3168-shape sweep, not the branch's cells: 3 frequencies x 4 day-rule/day combos x 24 window starts x 11 window lengths, comparing clara._accrual_schedule_yields(...) against the PLAN LANE'S OWN oracle exists(select 1 from clara._plan_due_events(from, freq, rule, dom, TRUE, from, to, 64) where leg='primary') -> 0 mismatches (1838 true / 1330 false). Spot checks: leap Feb 2028 window ending the 28th -> false, ending the 29th -> true; quarterly 2026-02-01..2026-03-31 -> true; a window opening after its own month's due day -> false. The wall's semantics are exactly 'a primary due date exists inside [effective_from, effective_to]', which is the condition for the plan to ever post",
    "NB1 / TS MIRRORS vs SQL, my own comparison over the same 3168 shapes: packages/runtime/lib/accrual-basis.ts accrualScheduleYields -> 0 mismatches; apps/web/lib/accruals/api.ts accrualScheduleYields (loaded through apps/web's own test bootstrap) -> 0 mismatches. localAccrualRefusal on the NB1 shape returns reason accrual_schedule_yields_no_occurrence / field day_rule / constraint yields_occurrence and returns null on both workable shapes; validateAccrualDraft returns exactly [{field:dayRule, code:scheduleYieldsNone}] on the NB1 draft, [] on the workable one, only [{dayOfMonth, dayOfMonthRange}] on a day 31 (no pile-on) and only the two required-date issues on empty dates (no pile-on)",
    "NB1 / SOUNDNESS OF THE NEW FUNCTIONS, read at source: clara._accrual_schedule_yields CALLS clara._plan_due_events (0193:845) and recuts nothing; both new functions are `revoke all ... from public`; both are marked immutable and clara._plan_due_events is itself immutable (pg_proc provolatile='i' for all three), so the immutable marking is consistent; _assert_accrual_schedule_yields deliberately falls THROUGH for an unknown frequency, unknown day rule, a day outside 1..28 and an inverted window -- I checked 0193's _assert_plan_schedule (0193:1572) and its admitted sets are exactly monthly/quarterly/annual, day_of_month/last_day_of_month, 1..28, so the fall-through list matches the validator that owns those shapes; both doors pass p_effective_from as the plan anchor to clara.create_accounting_plan, which is the same anchor the wall asks about",
    "BATTERY on clara_652 (29 --import preintegration gates): node --test --test-concurrency=1 $GATES tests/accrual-adjustments.test.mjs -> 21 tests / 21 pass / 0 fail / 0 SKIPPED, including the new cell `p652.schedule.yields` and all 20 pre-existing cells; tests/operation-census.test.mjs ALONE -> 10/10; tests/rig-isolation.test.mjs ALONE -> 21 tests / 19 pass / 1 fail (T10b) / 1 skip (T19). T10b's failure text names ONLY graphile_worker.* functions reachable by clara_agent_ro / clara_wake_* -- WDK world state from the runtime e2e, not 0207 (proved green below on the world-free chain). T18 ('every SECURITY DEFINER fn pins search_path + is owned by clara_fn_owner') PASSES, which covers the two new functions",
    "FROM-SCRATCH CHAIN (the migration changed, so I rebuilt the cluster): PowerShell wsl -u root -- bash .../mkrig.sh rig652r 55610 -> online; measured `select count(*) from pg_roles where rolname like 'clara%'` = 0 BEFORE migrating (cluster-wide roles are why recheck-1's first attempt died at 0154 -- recreating the cluster, not the database, is the fix); create database clara_652r; PGPORT=55610 pnpm db:migrate -> '194 new migration(s) applied · 194 total', exit 0, with '#652 prestate: clean -- no clara.accrual_adjustments relation and no accrual door exists' and the tail notice '#652 tail: OK ... the authority window is bracketed by the stated term, THE SCHEDULE MUST REACH AN ACCRUAL DATE INSIDE THAT WINDOW ... the six pinned 0193 bodies hash byte-identically to the digests taken before this file created anything, so NOTHING was recut; no foreign table was altered'; re-run -> '0 new migration(s) applied · 194 total'; pnpm db:seed -> 2 seed file(s)",
    "FROM-SCRATCH TESTS on clara_652r (world-free): battery with the 29 gates -> 21 tests / 21 pass / 0 fail / 0 skipped; tests/operation-census.test.mjs -> 10/10; tests/rig-isolation.test.mjs -> 21 tests / 20 pass / 0 FAIL / 1 skip, i.e. T10b is GREEN, re-confirming it is WDK graphile_worker rig state on 55510 and not 0207",
    "RUNTIME on clara_652: node --test tests/accrual-basis-unit.test.mjs -> 20 tests / 20 pass / 0 fail / 0 skipped (was 18/18 at recheck-1); node tests/accrual-e2e.mjs against the real Postgres/Workflow World (RELAY_TEST_MODE=1, WORKFLOW_POSTGRES_URL=postgres://postgres@127.0.0.1:55510/clara_652) -> PASS 1 (one commit, no engine, nothing posted) · PASS 2 (exit_after_commit crash replayed onto one entry/receipt/occurrence/accrual) · PASS 3+4 (the reversal waited for the primary, named it, survived a cancel and a human catch-up retry, and posted), exit 0",
    "WEB on my three ports: node --import ./test/bootstrap.mjs --import tsx --test accrual-form.test.tsx accruals-list.test.tsx accrual-draft.test.ts tree.test.ts routes.test.ts -> 79 tests / 79 pass / 0 fail. WHOLE SUITE node scripts/run-tests.mjs -> 3759 tests / 3756 pass / 1 FAIL / 2 skipped, exit 1. The single red is components/clara/thread-live-clarify.test.tsx 'with two clarify rounds live, ONLY the still-parked one carries a control' ('the one answer control never arrived within 400 settle passes'), a file NO #652 commit touches. SOLO RE-RUN of that file alone -> 2 tests / 2 pass / 0 fail. BOTH RUNS REPORTED: host contention on a twelve-lane host, not a regression; I changed nothing",
    "PLAYWRIGHT: pnpm --filter @clara/web e2e accrual-walk with CLARA_E2E_APP_ORIGIN=https://127.0.0.1:3320 NEXT_PORT=3321 RUNTIME_PORT=3322 -> 14 passed (2.7m), exit 0, GREEN ON THE FIRST RUN. My three ports were free before the run (netstat showed no listener on 3320/3321/3322) and I killed nothing belonging to another lane",
    "GATES: pnpm typecheck -> exit 0 (apps/web Done, packages/runtime Done). pnpm lint -> FIRST RUN exit 2 with 'ENOENT: no such file or directory, scandir apps/web/e2e/.artifacts' inside eslint's glob walk -- self-inflicted: I had the Playwright walk running concurrently and it creates/removes that directory mid-glob. SOLO RE-RUN after Playwright finished -> exit 0, with check-token-contrast (56 pairs, WCAG AA), check-test-manifest ('369 test file(s) ... every real test file on disk is present, exactly once, in alphabetical order') + its selftest, check-message-keys ('2916 static t(\"…\") key(s) in apps/web all resolve to a string in messages/en.json') + its selftest, and packages/db / packages/runtime / packages/reporting-render eslint all green. BOTH RUNS REPORTED. node scripts/check-frozen-workflows.mjs -> OK, 281 frozen files / 51 'use workflow' modules, unchanged. node packages/runtime/scripts/check-parts-parity.mjs -> OK exit 0, work_accepted still emittable from v18/v19 only",
    "STANDARDS on the round-2 commit: git diff cebfcca5..HEAD --name-only -> 13 files, none of workRoutes.ts / purpose-label.ts / accounting-work-list.tsx / work-list-filters.tsx / client-workspace-overview.tsx / lib/firm/capabilities.ts; git diff origin/main...HEAD -- docs/PRD.md docs/ARCHITECTURE.md -> EMPTY; grep -in 'create or replace' 0207 -> 2 hits, BOTH inside comments; the only `alter table` lines in 0207 are the two RLS statements on its own relation; the docs touched are CONTEXT.md and packages/db/README.md only -- an implementation ticket's own sources of truth. 652-final.md carries the round-2 evidence in place (AC1 row, the 21-cell battery line, assumption 8 flagging the narrowing FOR RATIFICATION, follow-up 6 naming the plan-lane residual)",
    "NOTE (not a blocker, no action required this loop, but relevant to the ratification the orchestrator must rule on): a stated term made only of days >= 29 that holds no month end now has NO admissible schedule at all. Measured at the door on clara_652 with term+window 2026-07-29..2026-07-30: last_day_of_month -> CLR10 accrual_schedule_yields_no_occurrence (field day_rule); day_of_month=29 -> CLR10 invalid_schedule/day_of_month/between_1_and_28 (0193's own CHECK); day_of_month=28 -> CLR10 accrual_schedule_yields_no_occurrence (field day_of_month). The same term extended to 2026-07-31 is ACCEPTED and materialises 2026-07-31. This is NOT a regression -- such a term could never post before either, it was silently accepted (the very NB1 defect) -- but the refusal's fix hint ('Choose a day that falls inside the service period') cannot be followed in that narrow case. If the wall is ratified, it is worth naming beside follow-up 6; if the owner prefers the advisory alternative the fix worker offered, this is the shape that argues for it",
    "RIG STATE LEFT FOR THE ORCHESTRATOR: cluster rig652r recreated clean on 127.0.0.1:55610, database clara_652r at frontier 0207 (194 migrations + 2 seed files, no WDK world), left RUNNING -- drop with `pg_dropcluster --stop 17 rig652r` when the wave closes. The ticket rig clara_652 on 55510 is unchanged apart from probe rows on two throwaway clients created by my own script (6 accruals/plans total). No other lane's cluster, port, worktree or listener was touched, and nothing was pushed"
  ]
}