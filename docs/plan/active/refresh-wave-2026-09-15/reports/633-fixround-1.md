# #633 — fix round 1

**Branch** `impl/633-document-intake` · worktree `C:\Users\zhant\Desktop\clara-wt\633` · rig PG17 `127.0.0.1:55502` / `clara_633` · Playwright 3240/3241/3242. **Reviewed head** `460c7369` → **new head** `da5c3f1a`. Worktree clean; nothing pushed. Zero SQL (unchanged).

**Four commits** (all carry `Co-Authored-By: Claude Fable 5.1`):

| SHA | What |
|---|---|
| `0fceae25` | web — the composer's Send gate pinned against the LIVE source; the settle-poll costs the ONE read its header promises; ask-once stated as measured; `consent_evidence` dropped from the classify control |
| `f217203a` | db — the evidence-link tenancy cell stops asserting on an empty table; two measured invariants join the batteries |
| `d5d1fec5` | runtime/ci — leg 1 proves the chain to the ADMISSION DOOR; the XLSX arm is real |
| `da5c3f1a` | docs — `apps/web/README.md` on the tick's read cost and the ask-once guarantee |

Branch total: **18 commits, 62 files, +7482/−118**.

## Finding → what I did → evidence

| # | Sev | Finding | What I did | Evidence |
|---|---|---|---|---|
| **STANDARDS-F1** | blocker | `useUploadQueue.test.ts:721-729` was tautological — it hand-wrote both arrays and compared them with a third literal, never reading `ComposerAttachmentControl.tsx:58`. Nothing mounted that component or exercised its `blocked` gate; `sendDisabled`'s upload-blocking arm was untested. | **Red first:** rewrote the cell to import `COMPOSER_IN_FLIGHT_STATES` → `SyntaxError: … does not provide an export named 'COMPOSER_IN_FLIGHT_STATES'`. Exported it from the component (same five strings, `ReadonlySet<QueueState>`). Then built BOTH halves the reviewer named: a new `components/clara/composer-attachment-control.test.tsx` mounts the real control over the real queue (in-flight ⇒ `blocked:true`, no part; settled+filed ⇒ `blocked:false`, one `attachment` part), and a new cell in `composer-attachment.test.tsx` mounts the real `ClaraThreadView` and asserts **Send's `disabled` attribute** with a control (a plain-text draft alone leaves Send open). | `useUploadQueue.test.ts` **22/22**. `composer-attachment-control.test.tsx` **2/2**; MUTANT `blocked: false` ⇒ `not ok 1`. `composer-attachment.test.tsx` **7/7**; MUTANT (drop `attachments.blocked` from `sendDisabled`) ⇒ `not ok 7`. `check-test-manifest`: 373 files, alphabetical. |
| **SPEC-F1 / 633-ADV-1** | blocker | World e2e leg 1's poll predicate was `(row) => row !== null`, so "a classify task exists" passed while every classify task was a permanent `firm_narrow_consent_inactive` failure. The file header, the CI step and the report all claimed "an admitted coding task". | Rewrote leg 1 into three arms, each able to go red alone. **(a) control:** an UNFILED upload's classify task settles `failed` with `error_code = 'firm_narrow_consent_inactive'` and the document keeps a null kind. **(b) the consented chain:** `ensureClassifyConsent` grants the client's `document_processing` consent through the real governed verbs, a new `fileToClient` helper performs the same `record_client_resolution`+`file_document` two-step the browser's queue performs after finalize, and the leg polls for classify `status === 'done'`, a non-null `document_kind`, and exactly one `document.classified`. The single `__claraModelForTest` override now answers BOTH lanes (`doGenerate` for the classify consumer's `generateObject`, `doStream` for text) — a text-only mock was why every classify attempt failed. **(c) the admission door:** a real MyInvois UBL invoice rides `structured_parse` → `local_facts` (local, no model, no vendor egress) → `document.invoice_facts_completed` → the autodraft consumer calling `clara.admit_autodraft_task` for THIS document's live filing, inside a sweep run it opened itself; the leg asserts the `sweep_run_items` row is keyed on that filing, that no run-less (one-click) item exists, and prints the door's verdict verbatim. | `node tests/intake-admission-e2e.mjs` on clara_633 → **exit 0, 7/7 legs**. Leg 1 now logs: `control refused firm_narrow_consent_inactive; consented chain classified as 'invoice'; admit_autodraft_task reached on its own for filing 3244bed8-d599-405a-a0d0-c0eab4504be0 with outcome 'skipped_lane' ({"clr":"CLR29","lane":"needs_you","reason":"lane_changed","reasons":["tier_a_fails","direction_unresolved","vendor_unresolved","no_consent"]}); zero request_autodraft in the automatic lane`. Arms (a) and (b) are each other's control — they run in one process against one firm and disagree. |
| **633-ADV-2** | should | Leg 7's `xlsxBytes()` tried `jszip` (not a dependency) and sent a 45-byte `PK\x03\x04` stub that `lib/scan.mjs` quarantined on every run, while the leg printed `outcome: admitted` and asserted only on registry rows. | Built a genuine stored-entry ZIP in the file (per-entry local header + data, central directory, EOCD; CRCs from `node:zlib`'s `crc32`) — no dependency. The leg now asserts `refusedAt === null`, a `document_id` on the receipt, and a `clara.document_intakes` row that is not `failed` and carries a null `failure_code`. | Verified the bytes against the estate's OWN reader before wiring: `scan.detectDocument` → `{"format":"xlsx","ext":"xlsx","mime":"…spreadsheetml.sheet","pages":1}`. Leg 7 now logs `xlsx bytes: 940 (504b0304), intake status: finalized` (was `outcome: admitted` after `intake FAILED … code=quarantined`). |
| **STANDARDS-F2 / 633-ADV-3** | should | `p633.grants.nonregression`'s cross-firm cell logged the `entry_evidence_links` population and then asserted two zeroes over an EMPTY table — it would pass with RLS removed. | Seeded a real link inside the cell: document → filing → drafted entry → one `attached_via='late_attachment'` row (the shape `ck_entry_evidence_links_receipt` admits without a receipt or a Work; #633 reads this relation and writes it never — rootQuery for fixture setup only). The non-vacuity control is now an **assertion** — firm A's own `humanQuery` persona reads the row — and runs BEFORE the foreign firm's zero, which is now checked by firm id AND by the exact row id. | `node --test tests/rig-docs-isolation-grants.test.mjs` → **12/12**, lane note `p633 seeded a real late_attachment evidence link 38f45248-… in firm A`. |
| **633-ADV-4** | should | `receipts.ts`'s header promised "FOUR READS AT MOUNT, ONE ON THE POLL" while `onTick` called `reload()`, re-running the whole derivation — up to four reads a tick, the heaviest the SECURITY INVOKER `list_unassigned_documents`. | Split the derivation: `loadIntakeReceipts` captures `{filedHere, unassignedById, me, meta}` and returns it; a new `refreshIntakeReceipts(previous, opts)` re-reads `document_intakes_visible` alone and rebuilds every row through the same pure `buildReceipts`. The workbench's tick takes the narrow path and pays the full derivation once more on the tick where the batch settles. **While fixing it I found the shipped bound cell was itself vacuous:** the poll waits 1.5 s before its first tick and `settle()` is a 0 ms hop, so it asserted a budget of ZERO ticks. The bounds are now a documented component option (the `useUploadQueue` idiom) and every budget assertion carries a `grew > 0` control. | `documents-workbench-refresh.test.tsx` **9/9** (was 7). New: "a tick re-reads the MASKED VIEW ALONE — the other three reads stay flat" and "the three slow reads ARE paid again, exactly once, on the tick where the batch settles". MUTANT (tick takes the full path) ⇒ `not ok 7`, `not ok 8`, `not ok 9`. |
| **633-ADV-5** | should | The leaf's header claimed `file_document` refuses ANY second attempt; the walk mock invented `CLR01 document_already_filed`, which no door produces. Measured: a same-client repeat is refused CLR10; a DIFFERENT client is accepted and leaves two live filings. | New db cell `p633.unassigned.second_attempt` measures all three outcomes and the consequence. Header rewritten to the measured guarantee (and to say the second-attribution behaviour is pinned in the db battery, not asserted on the leaf). The mock's refusal is now the estate's own code and words. `apps/web/README.md` carries the same correction. | `node --test tests/unassigned-intake-reuse.test.mjs` → **6/6** (was 5). The new cell asserts: one filing after the act; CLR10 + `/already .*filed/i` on the repeat, still one filing; two live filings after a different client; then `clara._enqueue_invoice_facts_core` → `document_processing_multi_client`. |
| **633-ADV-6** | note | The browser publishes `custody`/`byte_extraction` from `byFormat` — the first row for that format — on an assumption pinned nowhere. | New cell in `document-intake-capabilities.test.mjs`: for EVERY format, `count(distinct custody) = 1` and `count(distinct byte_extraction) = 1`, with a per-format control that the format publishes more than one kind. | `node --test tests/document-intake-capabilities.test.mjs` → **6/6** (was 5). |
| **633-ADV-7** | note | `document-kind-control.tsx:69` offered `consent_evidence`, which `set_document_kind` always refuses (CLR28). | **Red first** (`TypeError` — no such export), then exported `CLASSIFIABLE_DOCUMENT_KINDS` (the roster minus that one kind) and rendered the options from it. The cell asserts the exclusion AND that nothing else was quietly dropped. | `document-kind-labels.test.tsx` **5/5** (was 4). |
| **633-ADV-8** | note | Rig bookkeeping; reuse `rig633r` (55602 / `clara_633r`). | **No action, correctly.** The branch still ships zero SQL, so there is no migration to roll back and no prestate to re-pin; everything in this round ran on the ticket's own rig `clara_633`. | `git diff origin/main...HEAD -- packages/db/migrations` → empty. |
| **633-ADV-9** | note | RIG.md is owed one line: the #633 admission e2e bootstraps the Workflow world, which makes `rig-isolation.test.mjs`'s T10b red on that cluster afterwards. | **Left to the orchestrator, deliberately.** `docs/plan/active/refresh-wave-2026-09-15/RIG.md` is a shared wave document twelve branches would conflict on, and DECISIONS §1.6's shared-file list does not include it. Proposed line, verbatim: *"#633's `intake-admission-e2e.mjs` bootstraps the Workflow world on whatever cluster it touches, so `packages/db/tests/rig-isolation.test.mjs`'s T10b (`graphile_worker.*` EXECUTE reachable by `clara_agent_ro`/wake roles) reads red on that rig afterwards — rig state, not a branch defect."* | Confirmed by the reviewer's own control on a never-bootstrapped chain (30 pass / 0 fail on `clara_633r`). |

## Counts, re-run after the round

| Command | Result |
|---|---|
| `pnpm typecheck` (worktree root) | **exit 0** (apps/web + packages/runtime) |
| `pnpm lint` (worktree root) | **exit 0** — includes `check-frozen-workflows`, `check-parts-parity`, `check-token-contrast`, `check-test-manifest` (**373** files), `check-message-keys` |
| `node scripts/check-frozen-workflows.mjs` | OK — 281 frozen files, 51 `"use workflow"` modules |
| `node packages/runtime/scripts/check-parts-parity.mjs` | OK — reader ⊇ emittable |
| db, 5 batteries + the 28 preintegration gates (`clara_633`) | `tests 32 · pass 32 · fail 0 · skipped 0` (23.5 s) — was 30/30 |
| `node tests/intake-admission-e2e.mjs` (`clara_633`) | **exit 0, 7/7 legs** |
| whole `apps/web` suite (`node scripts/run-tests.mjs`) | `tests 3792 · suites 135 · pass 3789 · fail 1 · skipped 2` (212 s, exit 1) |
| the one red, in isolation | `components/clara/onboarding-checklist.test.tsx` → **14/14, fail 0**. A file this branch never touched; a load flake, reported both ways, not "fixed". |
| Playwright `documents-intake` (3240/3241/3242) | **12/12 passed (30.5 s)** |
| Playwright `chat-parity` (3240/3241/3242) | **7/7 passed (28.8 s)** |

Migration file: unchanged (there is none), so no rollback/re-apply was owed.

## What I deliberately left

1. **A Tier-A-complete autodraft fixture.** Leg 1(c) proves `clara.admit_autodraft_task` is reached automatically; the door answers `skipped_lane`/`lane_changed` because `_coding_lane_core` refuses this document (`tier_a_fails`, `direction_unresolved`, `vendor_unresolved`, `no_consent`, measured on clara_633). Proving an admitted CODING TASK needs counterparty resolution, a resolved direction and coding consent — the autodraft lane's own fixture, not this ticket's. Named in the e2e header, `packages/runtime/README.md`, the CI step comment and the final report's AC4 row. **This is the one place a reviewer's wording and the brief's differ:** the brief (line 172) asks for "`admit_autodraft_task` with zero `request_autodraft` calls", which is now proven; the final report's old AC4 row said "admitted coding task", which was not, and has been rewritten.
2. **`document-admin.tsx`'s classify Select still offers `consent_evidence`.** 633-ADV-7 names only `document-kind-control.tsx`; the detail surface's Select belongs to #624/#646 and DECISIONS §1.7 puts the correction act there, so editing it would be a silent scope widening into a concurrently-edited lane. Recorded as an observation.
3. **The RIG.md line** (633-ADV-9) — see the table; the exact sentence is there for the orchestrator to paste.
4. **`rig633r` / port 55602** was left untouched. The branch ships no SQL, so the from-scratch chain proves nothing new about this round's changes; everything here was measured on `clara_633`.

## Ratification requested

None. No finding asked the orchestrator to ratify an assumption, and none contradicted the brief or DECISIONS.
