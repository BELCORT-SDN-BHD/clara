Landed on `impl/649-client-onboarding` (worktree `clara-wt/649`, migration 0219, rig 55508/`clara_649`, Playwright 3300/3301/3302), 11 commits: `56d90892`/`471f04fb`/`ddff3d01`/`b18ee47d`/`baaf48fe` (base delivery — `clara.client_identity_candidates`, `settle_client_onboarding_facts`, candidate-rows refusal, the identity/fy-end web faces, the first browser walk), then two review rounds: round 1 `40e0b471`/`3aabf060`/`13bd3e1e` (settle door takes the CLIENT row lock before the plan lock; the e2e identity mock answers every name; the commit-success announcement no longer hides behind a settle failure), round 2 `eddaaf91`/`c5eccfab`/`22d283c3` (settle takes the client advisory rung *before* the client row, and the plan pre-read is firm-scoped so a foreign plan locks nothing).

`settle_client_onboarding_facts` writes canonical client records; fy-end goes through `set_client_fy_end` as month+day (D7) with the web `Field` asking the day explicitly (month-end shown only as a suggestion, never assumed). The interview's own day question and ask-only-missing (H-52) are contract-only this wave — they ship in `clientOnboarding_v5` at the integration cut, so AC2's interview half is delivered as a contract, not a running behaviour. Ambiguity: 0 candidates proceeds, 1 requires an explicit "different business" acknowledgement, ≥2 refuses with `name_family_collision` carrying full candidate rows (not bare ids). Opening books use the existing seed lane with an honest first-year-vs-deferred sentence; `p_promote_firm_scope` is never set. Hosted evidence pending.

**AC / historical rows**

| Row | State | Evidence |
|---|---|---|
| AC1 identity/ambiguity | Done on the register control; 2 named residuals | `p649.identity.*` cells; walks `add-client-candidates` 5/5, `client-create-walk` 4/4; residuals: direct birth door skips the wall, ⌘K dispatches with no identity read |
| AC2 ask-only-missing/canonical/Knowledge/resume | Partial by design | Settle door done (`p649.settle.*`, 9 cells); interview day-question/H-52 deferred to `clientOnboarding_v5` cut; Knowledge verify-only + new seam (5); resume verify-only (`interview-kill-resume-e2e` exit 0) |
| AC3 first-year vs deferred + link | Done | `client-onboarding-progress-opening-link` 5/5 |
| AC4 stale/concurrent/cancel/revocation | Verify-only; 3 defects found and fixed across 2 rounds | Round 1: settle inverted the family lock order (plan-then-client), causing real `40P01` both directions — fixed, order now client-row-first. Round 2: settle still met the client advisory rung only inside `set_client_fy_end`, i.e. after the plan lock — deadlocked against `approve_opening_seed`; fixed to rung→client-row→plan. Same round: the plan pre-read had no firm predicate, so a foreign plan queued on another tenant's client row before refusing (2453ms vs 51ms) — now firm-scoped (3ms vs 4ms) |
| AC5 reach Home, no blanket ritual | Partial, as briefed | `coa-seed-decision-state` 6/6; open-plan refusal intended, explained not removed |
| AC6 states/a11y/zoom/keyboard/drafts | Done on owned surfaces | `client-create-walk`: 320px, 200% zoom, reduced-motion, keyboard, focus-return, Back, 3 axe scans |
| AC7 least-privileged roles + World | Done | `interview-e2e.mjs` exit 0 on real WDK Postgres World, `humanQuery` personas throughout |
| UI-21 full-screen onboarding | Not re-measured — named residual | No docker/PostgREST live-stack runner on this rig; follow-up filed |
| H-52 SST number still asked | Live, unfixed — successor contract only | `p649.interview.sst_park_today` asserts the park is still announced today |
| COA before cancelled onboarding | Verify-only; owed count is hosted | `coa-template-pr-b` green; no repair migration yet |

**Review summary.** Adversarial found 2 blockers (B1 lock-order inversion; B2 the H-51 walk was red because the e2e mock didn't answer the identity check) and 4 shoulds (S1 failed-read rendered as "nothing matches" behind the modal; S2 untyped `22003` on an out-of-range fy month; S3 ⌘K bypasses the identity wall; S4 a private body reader draining the shared request stream), plus 4 notes — all closed in round 1 except S3, which was resolved by correcting the README's exclusivity claim and naming the ⌘K bypass as a residual rather than wiring it. Spec found F1 (UI-21 not authored, ratification requested), F2 (axe-scan count, note), F3 (candidate rows vs brief's "ids", ratification requested). Standards found STD-1 (a settle failure after a successful commit suppressed the `CLIENT_RECORD_CHANGED` announcement) — closed in round 1. Round-2 recheck confirms B1 and the new cross-tenant lock finding (NEW-1) both **closed**, verified independently against the reviewer's own probes and a from-scratch red reproduction; no new blockers.

**Ratifications applied (DECISIONS §3.1).** (1) UI-21 not authored — ratified as a named residual; follow-up issue for a docker-free path or a mock lane owning the rail. (2) `name_family_collision` carries candidate rows, not bare ids — ratified; brief-649 lines 51/74 corrected by this ruling. (3) Round 2's rung-first lock order (rung → client row → plan), a deviation from the review's literal client-row-first remedy — ratified: the literal order inverts against `set_client_fy_end` itself (rung before its own client update), while rung-first sits above every neighbour per 0037 §K. `clara.approve_wrong_client_correction` takes a client row before the rung — a pre-existing violation, filed as a follow-up, not this ticket's.

**Residuals/follow-ups.** ⌘K's identity-read bypass; the direct birth door has no arity wall; fy-end day absent from Knowledge (needs #654's `financial_year_end_day` key); `InterviewRunCard` still pre-`Field` (owner #633); COA-before-cancelled-onboarding affected-row count is hosted work; blueprint drift — `ARCHITECTURE.md:171`'s version pins omit `clientOnboarding→clientOnboarding_v4` today and go stale at the v5 cut.

**Successor contract.** `clientOnboarding_v5` + new `interview.v4.questions.ts`: H-52's `sst_no` gains `appliesTo` gating; a new `fye_day` segment (`required_for_commit: false`); a known-facts pre-read seeded from `get_knowledge_pack`. Registry repoint only (`registry.ts:264`,`:908`), no door call, no migration, no `WORK_ACCEPTED_PURPOSES` widening.

**Integration evidence:** <INTEGRATION_PLACEHOLDER>
