Landed on `impl/647-counterparty-identity` via 8 commits: `1f686612` (red battery+fixtures+gate) → `225e6562` (migration 0200: provenance, append-only correction history, four events, three human reads) → `410628e5` (routed identity detail + Identity section on Knowledge + two homeless doors wired) → `046e58d1` (faces, C77.6 cells, H-20, H-34 re-measured, leaf) → `63cb3ce8` (successor-contract carrier, non-frozen, unit-tested standalone) → `e7b905e4` (browser lane; found 3 real defects) → `850f2b65` (review round-1 fixes: lock order, pin trio, closed kind vocabulary) → `bda62a1d` (docs: tail note on the deeper source FKs).

Migration 0200 gives every alias/rename/identifier write a typed provenance (`recorded_via` NOT NULL — `human_ui`/`agent`/`seeding`/`legacy_unknown`, a trigger refusing `human_ui` without `jwt_sub()`), an append-only `counterparty_identity_revisions` history, and four `counterparty.*` domain events routed `context_update` with no consumer (D11, PRD:123 — writing stays out of scope this wave). Identity stays inside the counterparty relations — no move into `knowledge_records`, no identity knowledge keys. Three new reads are SECURITY DEFINER functions (never masked views); merge lineage renders from both sides; unmerge is a bounded discovery read only, no reversal door. Web: a registered Identity nav leaf reusing `knowledge-shared.tsx`, C13 shows source/history/conflict plus one versioned correction path. Local evidence: identity battery 19/19 + 150/150 with nine neighbours, `operation-census --strict` exit 0, runtime unit 5/5, whole `apps/web` 3748/3746 pass (0 fail, 2 skip, run alone), Playwright walk 17/17 (59.8s, run alone), typecheck/lint exit 0/exit 0 — all reconfirmed by an independent regression sweep in the re-check. **No World e2e leg was shipped, deliberately**: every identity door is `_human_ctx`-fronted and human-lane-only (gap-647 slice F struck), so there is no durable-execution claim to make. Hosted evidence pending.

| AC / row | State | Evidence |
|---|---|---|
| AC1 provenance, no agent-as-human lies | Done | 0200 §1–2, §7.1–7.3; round-1 closed source-pin coherence (two-way FK guards on extraction/region, typed `CLR10 source_extraction_not_of_document`/`source_region_not_of_extraction`) |
| AC2 rename keeps identity; merge lineage | Done (DB half verify-only) | `aging.ts`/`counterparty.ts` carry `recorded_counterparty_id`+`resolution`; walk cell "merge lineage … BOTH sides" |
| AC3 no general unmerge; bounded discovery | Done | `list_counterparty_merge_corrections`; surface states no un-merge exists |
| AC4 roles distinct; no netting/authority | Done | `p647.ac4.*`; walk cell "the CONFLICT face …" |
| AC5 source/history/conflict + correction path | Done | `counterparty_identity_revisions` append-only, forced RLS; round-1 fixed a measured deadlock (647-A1, blocker) |
| AC6 states/a11y/zoom/keyboard/URL/drafts | Done | walk 17/17, axe clean; round-1 added loading + partial/stale cells (SPEC-2) |
| AC7 production reads, least-privileged roles | Done locally | `humanQuery` personas; no World leg (see above) |
| H-09/H-20/H-34 | Done | round-1 closed H-20's free-text kind hole (647-A2, blocker: kind is now a closed 3-value select) |
| C08.3 vendor-binding ambiguity | Partial | stated against current identity, resolution act stays on `/settings/vendor-bindings`; follow-up filed |
| Activity kind ladder | Out of scope | DECISIONS D13 |

**Review summary.** Adversarial: two blockers (647-A1 revision/rename deadlock, measured up to 6/10 rounds `40P01`, now 0/10; 647-A2 free-text identifier kind admitting values the DB CHECK refuses), one should (647-A3 source-pin coherence), four notes — all closed in round 1, reconfirmed by the recheck's regression sweep (150/150, 108/108 web cells, e2e 17/17 solo). SPEC: the `ClientIdentifiers` third-namespace violation (closed, folded into `ArApCounterparty.clientIdentifiers`) and an AC6 loading/partial gap (closed). STANDARDS: same namespace issue plus one dead i18n key (closed). No fixround-vs-recheck contradiction; the recheck's positive control (re-adding the dropped FK) independently confirmed the FK removal is load-bearing.

**Ratifications applied (DECISIONS §3.1).** (1) No FK from `counterparty_identity_revisions` to `counterparties`; tenant congruence by a lock-free assertion inside the sole ungranted writer — ratified: "the FK's `FOR KEY SHARE` re-opened a measured deadlock between two shipped human doors." Follow-up: document `merge_counterparties`' lock order (0149 splice). (2) Only `KnowledgeSourceBlock` reused from `knowledge-shared.tsx` — ratified: "an alias has no kind/trust concept in 0200; calling those helpers would invent data."

**Residuals / follow-ups.** C08.3 vendor-binding ambiguity has no resolution face; four domain events have no consumer; `merge_counterparties`/`tick_seeding_proposal` still land `recorded_via='legacy_unknown'`; `merge_counterparties`' lock order needs documenting; the three door dialogs' shared draft rule is celled for only one of three; a lane-mock body-drain hazard was found and fixed locally, no repo-wide lint yet (shared with #633/#653); #707/#693 untouched.

**Successor contract.** The integration cut measured the same wall and left it uncut (`reports/successors-final.md` §3), confirming DECISIONS D11. `record_counterparty_alias` is written (carrier `packages/runtime/lib/counterparty-identity.ts`) but **not named in DECISIONS §1.1's `chatTurn_v20` cut list** and cannot ship as-is: it needs a new OBO door, `clara.add_counterparty_alias_for` (the `capture_knowledge_for` shape), which no branch has migrated. This is a gap the orchestrator must resolve before or at the integration cut, not a contract ready to carry.

**Integration evidence:** <INTEGRATION_PLACEHOLDER>
