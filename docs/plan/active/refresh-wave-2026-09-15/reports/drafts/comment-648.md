Landed on `impl/648-firm-setup` via 9 commits (35b538bc migration 0203 → 31834d62 `/settings/setup` checklist/form/tile → ccac8c3c unit+mock+walk → 95343260 docs → bda43ac8 re-pinned censuses → a4e0afde walk fixes → 6a3893e1 #654's two wall faces mapped ahead of 0205 → 446cf815 round-1 blocker fixes → dcc7c94a round-1 should fix).

Migration 0203 ships the firm setup catalogue, four human doors (answer/skip/reconcile-seed/commit) and `get_firm_setup`, plus a one-open-firm-plan index. Seeding is a reconciliation against the firm plan, never a blind insert. Web: `/settings/setup` (section id `setup`) + a firm-home tile; progress reads the catalogue's required set (so #625's `bookkeeper_email` can never block this commit); resume after reload and concurrent editing via CAS on plan version; optional education is mechanically skippable (though unseeded — see residuals). Firm identity facts stay as plan items per D10, never knowledge records; firm-level defaults route through `capture_knowledge(scope=>'firm')` subject to #654's eligibility wall, whose two faces are mapped here ahead of 0205 landing. This journey invokes no Workflow: no runtime module, no `db-live-gates/action.yml` row, no World e2e leg — stated, not skipped. Local evidence: DB battery 17/17 (on both the ticket rig and the reviewer's from-scratch cluster) + `operation-census`/`rig-isolation` 31/30/0/1; web unit 19/19; closed-world censuses 103/103; whole `apps/web` suite 3738/3736/0/2 clean; Playwright `firm-setup-walk` 5/5 built fresh. Three-axis review + one fix round, independently re-checked. **Hosted evidence pending** — no hosted probe was run.

| AC / Row | State | Evidence |
|---|---|---|
| AC1 real progress, resume, concurrent edit | Done | `p648.seed.reconcile/.empty`, `p648.answer.stale` (CLR06), full-reload resume walk |
| AC2 Field/bounded-set, skippable never a gate | Partial, named residual | walk + a11y cells; **no education content seeded** (`item_kind='education'` exists, zero rows) |
| AC3 scope/source/actor, conflicts/stale/refusal | Partial, named residual | `p648.capture.*`, correction path (round-1 fix, see below); **firm identity facts have no knowledge record** (D10, `p648.capture.ineligible`) |
| AC4 partial completion, workspace stays usable | Done | `p648.workspace.open` — bookkeeper reads clients/Work/activity/review-queue/knowledge while setup is open |
| AC5 saved/resumed/conflict/stale/denied/complete | Done | 5 walk cells |
| AC6 states/320px/200%/focus/SR/motion/URL/drafts | Done | `firmSetup.walk.responsive` + `.finish`, `fs.kbd.*`, `fs.a11y.*` |
| AC7 production, least-privileged roles | Done | 17 real-role DB cells, all via `humanQuery` |
| C48.5 correction path | Done, closed round 1 | `p648.answer.correct`, `fs.web.10` — settled/deferred facts get a correction control; a live-captured key points at the facts panel (door refuses `knowledge_already_live`) |
| C83.24 | Duplicate, closed with C48.5 | pointer left for #654 |

**Review summary.** Adversarial: 2 blockers (F1 nine of twelve facts were write-once with no correction path; F2 the lost-response retry provably couldn't work — an op key derived from the answer value, not the attempt) + 1 should (F3 same op-key defect on defer's reason) + 5 notes (from-scratch cluster reuse, no-op reconciliation rotating CAS, `get_firm_setup` painting progress for a nonexistent plan, withdrawn-default tombstone, `SCOPE_ENTRANCES` deviation confirmed correct) — all closed or recorded round 1. Standards: 1 should (CAS-convergence assertion ran on a 5s default against a ~32s real sequence) + 1 note (brief-item-17 loop not closed in writing) — both closed. Spec: 1 note — the walk had never actually been confirmed end to end by either reviewer, because `npx playwright test` serves whatever `.next/` already holds rather than building first; the fix round ran it correctly (fresh build, 5/5) and confirmed this diagnosis by construction. Recheck-1 independently re-raced every door-level claim from scratch and closed everything: `open: []`, `new_blockers: []`.

**Ratifications applied (DECISIONS §3.1).** None. No post-review ratification row names #648 in DECISIONS §3.1; three notes (no-op reconciliation CAS rotation, `get_firm_setup` painting progress for no plan, withdrawn-default tombstone SQL half) are explicitly deferred to the orchestrator as follow-ups rather than ratified in place, since fixing them would mean editing the byte-frozen 0203.

**Residuals / follow-ups.** The stale-Playwright-build trap (`npx playwright test` vs `pnpm --filter @clara/web e2e`) is this ticket's own discovery and worth a wave-wide doc callout or guard. Firm setup applicability predicates (`mpers_eligibility`, `tin` asked unconditionally, no predicate column on the catalogue). Optional education content unseeded (owner input, not code). Firm registration identity has no canonical home pending #647/#654. `uq_onboarding_plans_one_open_firm`'s predicate could be hardened to `(firm_id) where scope_kind='firm'`. The three deferred 0203 notes above (no-op CAS rotation, phantom-plan progress, withdrawn-default tombstone — SQL half only, the web-side dead end is already closed). `StateBanner` silently drops `data-testid` (closed prop list, hyphenated JSX attrs skip TypeScript's excess-property check). D13 activity-kind residual untouched. Foreign plan items from #625/#649 (`bookkeeper_email`, `first_client_onboarding`) are not rendered by `get_firm_setup`.

**Successor contract.** None owed. Firm-scope chat capture belongs to #654's `claraWork_v4` stanza; this slice ships only human doors and a human surface.

**Integration evidence:** <INTEGRATION_PLACEHOLDER>
