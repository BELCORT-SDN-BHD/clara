Landed on `impl/633-document-intake` via 18 commits (`06f53d0a`…`da5c3f1a`, including a fix round). **No migration** — this ticket ships zero SQL, by brief.

Builds the upload queue as a Data Table with real byte progress (`XMLHttpRequest` + `upload.onprogress`), durable receipt re-read from `readIntake`/`listProcessingTasksForDocument` after reload, local cancel (pre-custody) split from Work cancel (post-custody) by a `stopReason` discriminant, per-file retry, 20 `document_kind` labels + null as "Needs classification", and nine failure codes each with a next step. The firm intake leaf `(firm)/documents` sits over the already-granted `list_unassigned_documents` + `record_client_resolution` + `file_document`, with the "ask once" guarantee corrected to what the door actually holds (same-client repeat refused CLR10; a different client is accepted and produces a second live filing, which 0123 then refuses as `document_processing_multi_client`). Document→Work is rendered from `entry_evidence_links` directly. The fix round closed one blocker each in the standards and spec lenses — a tautological composer test that never mounted the real component, and a World e2e leg whose predicate treated a permanently-`failed` classify task as success — plus three shoulds (a vacuous cross-firm tenancy cell, a fabricated XLSX byte stub, a settle-poll re-reading four relations instead of the one its header promised). Local evidence: DB 32/32, operation-census+rig-isolation 29/31 (1 rig-state red — see below, 1 skip by design), World e2e 7/7 legs, Playwright `documents-intake-walk` 12/12 and `chat-parity-walk` 7/7, whole `apps/web` suite 3789/3792 (1 known flake, 14/14 alone; the re-check's own full run: 3790/3792, 0 fail), typecheck/lint exit 0. All twelve findings across three lenses closed on the re-check, including two independent mutation-tested proofs. Hosted evidence pending.

| AC / row | State | Evidence |
|---|---|---|
| AC1 upload/cancel/receipts | Done | real byte progress, `stopReason` cancel split, mount-time receipt recovery; format/size/safety walls verify-only |
| AC2 kind labels | Done | `kind-label.ts`, 20 phrases + null |
| AC3 classification surfaces | Partial, as briefed | null kind actionable on list/receipt rows; capability tiers from the registry; detail-panel verdict stays as #624 built it |
| AC4 automatic admission chain | Verify-only; residual named | World e2e leg 1, 3 arms (control refused / consented chain classifies / `admit_autodraft_task` REACHED, not ADMITTED — fixed in round 1 after spec/adversarial both found the old poll predicate treated `failed` as success) |
| AC5 firm intake leaf, ask-once | Done | leaf + `p633.unassigned.second_attempt` measuring all three real outcomes (round-1 fix; the walk mock previously invented a refusal code no door produces) |
| AC6 failure codes/next steps | Done | 9 `queueFailure.*` codes + unknown-code fallback |
| AC7 cancel/permission-loss/batch | Done | local vs Work cancel (round-1 added 2 real mount tests + mutation checks after a tautological cell was found); 40-file mixed batch |
| AC8 Data Table + Document→Work | Done | `entry_evidence_links` read direct |
| AC9 states/a11y/zoom/keyboard | Done, both surfaces | axe clean, 320px/200%, stable URL/Back |
| AC10 production command, real World | Done | `intake-admission-e2e.mjs` + CI step |
| C-37 real OFX+XLSX | Done (round-1 fix) | genuine stored-entry ZIP built in-file, replacing a 45-byte stub the runtime had silently quarantined |
| C-73 batch at scale | Partial | 40-file + 5-file World batch only; representative scale stays with #706/#760 |

**Review summary.** Standards: 1 blocker (tautological composer test, closed with a live export + 2 new mount tests, mutation-verified), 1 should (vacuous cross-firm tenancy cell, closed by seeding a real link). Spec: 1 blocker (World e2e leg 1's `!== null` predicate masked a permanently-failed classify task; closed by a real 3-arm leg with a named residual for the still-unproven coding-task admission). Adversarial: 1 blocker (same leg-1 finding), 4 shoulds (fabricated XLSX stub, vacuous tenancy cell, settle-poll over-reading, ask-once claim overstated), 4 notes (capability-tier assumption unpinned, a refusable kind still offered, rig bookkeeping, a RIG.md line owed). Re-check closed all twelve independently, with mutation probes reproducing 2 of the fix round's own claimed guards (correcting one: the fix report said a mutant would redden cell 7 too; it doesn't, only 8 and 9 do — substance unaffected).

**Ratifications applied (DECISIONS §3.1).** None. No post-review ratification row names #633; the fix round's own report states no assumption needed the orchestrator's ruling.

**Residuals / follow-ups.** A Tier-A-complete autodraft fixture is owed (leg 1(c) proves `admit_autodraft_task` is reached, not that a coding task is admitted — needs the autodraft lane's own counterparty-resolution/direction/consent fixture). `document-admin.tsx`'s classify Select still offers `consent_evidence` (deliberately left — that surface belongs to #624/#646). RIG.md is owed one line: this ticket's World e2e bootstraps the Workflow world, which reds `rig-isolation.test.mjs`'s T10b on that cluster afterwards (rig state, not a branch defect — confirmed on a never-bootstrapped control). Lane-mock private body-reader hazard found and fixed here (shared census follow-up with #647/#653). Poll-bound vacuous test-budget cell class found here (`useSettlePoll` waits 1.5s before its first tick; a repo-wide census is worth filing). `document_filings` has no list-form read for a set of documents. Playwright walks are not runnable honestly under twelve concurrent lanes (host-contention follow-up, shared).

**Successor contract.** None owed. Every act in this journey is a human web door over an already-granted read or existing door; the integration worker should not look for one.

**Integration evidence:** <INTEGRATION_PLACEHOLDER>
