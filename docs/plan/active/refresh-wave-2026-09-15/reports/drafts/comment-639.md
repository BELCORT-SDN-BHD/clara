Landed on `impl/639-asset-acquisition` via 11 commits (`2edaf5ca`…`693f8899`): migration 0216 — lane-agnostic fixed-asset acquisition birth, provenance and the runtime particulars door.

Migration 0216 adds a deferred, lane-agnostic birth trigger (`t_je_fa_acquisition_birth`, Option B) so any Work-lane entry touching an enrolled fixed-asset cost account births the register row before `t_je_fa_movement_belt` fires, storing `acquisition_document_id` at birth (write-once; the read is the authority). `_fa_asset_json` and `get_fixed_asset` are recut with measured `prosrc` pins. The dependent-particulars question reuses #629's mechanism; the human lane is complete, and a new `clara_runtime`-only `_for` overload with an OBO live-authority re-read (firm `for key share` + membership `for share`, mirroring 0195's lock order, added in fix round 1) lets `claraWork_v4` apply the answer with no migration at the cut. Credit-financed acquisitions are boundary-refused to the composer/intake doors by name; corrections are a derived, labelled predecessor/successor/co-acquired chain on a History tab, never a re-link. C7 ships list/detail across four `SectionTabs`; a real Postgres World e2e passes 5 legs including crash/replay and a live-authority recheck that refuses a demoted initiator. Local evidence: DB battery 94/94 (7 files) + ticket's own file 21/21 from scratch, operation-census/rig-isolation 30/31 (1 skip by design), runtime unit 8/8, World e2e PASS, apps/web whole suite 3735/3737 (2 skip), Playwright walk 8/8 on a quiet host. All three review lenses opened findings; every one is closed per the re-check, no open items, no new blockers. Hosted evidence pending.

| AC / row | State | Evidence |
|---|---|---|
| AC1 identity/source/date/cost/accounts | Done | `acquisition_document_id` (0216 §A); `p639.provenance.document(_lane)`; MYR-only stated boundary, not filled |
| AC2 atomic journal+register+lineage+receipt | Done | `p639.birth.work_lane` World e2e PASS 1+2; refusal cells leave no half-born row |
| AC3 one dependent versioned question, no journal on answer | Done (human); run half = successor contract | `p639.question.dependent`; `complete_fixed_asset_particulars_for` granted+proven |
| AC4 duplicate/restart/correction, no partial mismatch | Done | `p639.birth.idempotent`; World e2e PASS 3/4; `p639.correction.chain` + `.co_acquired` (fix round 1 recut, co-acquired vocabulary replaces false "successor" claim on siblings) |
| AC5 C7/Work/Journal/Document show acquisition apart from pending config | Done | `get_fixed_asset` `acquisition`/`particulars`/`history` blocks; route + 4 `SectionTabs` |
| AC6 atomic link; missing config blocks only dependent setup | Done | `p639.depreciation.independent` re-measured behaviourally (fix round 1) |
| AC7 refusals name the field without erasing the asset | Done (fix round 1 built the missing wiring) | `fa-refusal-field.ts` mirrors the runtime axis→control map into `apps/web`; `FaDoorDialog` focuses + `aria-invalid`; CLR40 belt classification pinned |
| AC8 C7 list/detail, keyboard + narrow recovery | Done | `fixed-asset-detail.test.tsx` (10); 320px/200%/roving-focus/focus-return walk cells |
| AC9 whole journey in a browser | Done (fix round 1 re-cut 2 cells) | 8-cell walk; residual-above-cost + viewer-denied cells replace a round-1 cell that submitted a valid form and never tested a non-owner role |
| AC10 production command, real roles, Workflow durability | Done | Every DB cell via persona; World e2e PASS locally |

**Review summary.** Adversarial: 1 blocker (locked-period cell asserted nothing), 2 should (co-acquired siblings mutually mislabelled "successor"; no raced concurrency cell / merge-order table), 4 note (OBO door's two-code comment overstated; 2 of 4 birth exclusions were string-search-only; live-authority read took no lock; review rig left running). Spec: 1 blocker (axis→control never wired into the human door; the walk's "invalid answer" cell submitted a valid form; no non-owner browser cell), 1 note (Playwright hang on a loaded host, inconclusive). Standards: 1 blocker (same locked-period gap), 1 should (source-document link went to the bare Documents tab). The re-check (head `693f8899`) confirms all nine finding-groups closed on both the lane rig and a from-scratch chain; `open: []`, `new_blockers: []`.

**Ratifications applied (DECISIONS §3.1).** Keeping `client_not_found` vs `obo_not_active` distinct on `complete_fixed_asset_particulars_for` is ratified: the runtime door is OBO-explicit, 0195's ladder separates the classes, and the browser-reachable door already folds both to CLR11. The `serve-built.mjs` `viewer@` rank-0 persona branch and `test/domInspect.ts`'s `getElementById` addition are ratified as needed to drive AC9's denied leg by role; integration keeps the hunk as a union with any competing persona branch from a sibling lane.

**Residuals / follow-ups.** CLR40 `fa_cost_adjustment_deferred` classifies as an invariant, not a refusal, in the frozen `claraWork` v1 errors table despite the belt offering a human remedy — one-row fix belongs in `claraWork_v4`. The belt (evaluates enrolment at `approved_at`) and the birth trigger (`fp.active` only) disagree on an entry approved in the same transaction that retires a profile — pre-existing, needs a side-choice. No opening-seed fixture exists for the K-family `scheduled_run` exclusion arm (belt-only evidence stands). No firm/class-level default depreciation policy exists yet (#654 territory). Blueprint drift recorded, not edited: ARCHITECTURE §3.5/PRD F3's "intrinsic because the hook runs at every approve path" claim was false since 0178, true again only because of this ticket's second instrument; corrected by a new test, not by editing the merged migration. The `_subledger_on_approve` six-name caller-census staleness is shared with #638. One whole-suite run was contaminated by concurrent worktree 654 activity — disclosed, not attributable, not reproduced on re-check.

**Successor contract.** `claraWork_v4` carries `apply_fixed_asset_particulars` (schema `faParticularsAnswerSchema`, no new part kind, no `WORK_ACCEPTED_PURPOSES` widening). The `clara_runtime` grant already exists (`0180:686`); no migration needed at the cut.

**Integration evidence:** <INTEGRATION_PLACEHOLDER>
