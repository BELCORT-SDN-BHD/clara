Landed on `impl/638-staff-claim` via 13 commits (`3bc75148`…`a26e4dd0`, migration 0206 + one round of review fixes, all in `packages/db`).

DECISIONS §2 #638 amended the brief mid-wave: the posting core's Work-purpose IN-list is closed and its validators raise `invalid_purpose` for a fourth value, so a staff expense claim ships as a **`journal_entry`-purpose Work**, not a new purpose. `clara.staff_expense_claims` (append-only status ledger, tenant-carrying composite FKs) is written inside the admission transaction by a new door `clara.admit_staff_expense_claim_work`, validated by a new `clara._assert_claim_basis` (claimant, incurred≤posting date, itemised lines, expense-class accounts, non-control payable leg, typed `settlement`), which then calls the unchanged `_admit_accounting_work_core`. The claimant is a staff-advance-enrolment handle, auto-enrolled on first claim, never a vendor. Posted state comes from a new AFTER INSERT trigger on `operation_receipts`; `clara.get_work_claim_origin` labels the claim on Work detail without a purpose value. This ticket also carries the wave's Work-purpose-vocabulary fix (4 surfaces) plus the `periodic_stock_adjustment`/`payroll_obligation` labels. Fix round 1 closed 1 blocker (a false AC2 evidence pointer — the `already_settled` arm worked but shipped with zero regression cell) and 3 shoulds (a claim into a sealed fiscal year admitted durably instead of refusing at admission; concurrent corrections escaping as a raw `23505`; AC4's document-release half unasserted), all in `packages/db` only. Local evidence: DB battery 24/24 (was 21, +3 in fix round, confirmed on a from-scratch chain), operation-census+rig-isolation+neighbours 81/82 (1 skip by design), runtime unit 14/14, World e2e PASS 7/7 legs (third attempt — first two reds were rig/host symptoms), Playwright walk 12/13 in-run (the one red is a shared `/login` stall, passes solo 1/1), whole `apps/web` suite 3747/3749 (0 fail after round 1; not re-run in the fix round since it touched no file outside `packages/db`), typecheck/lint exit 0. All findings across three lenses closed on the re-check bar one (F6, explicitly the orchestrator's, not the branch's — see below). Hosted evidence pending.

| AC / row | State | Evidence |
|---|---|---|
| AC1 claim basis, invent nothing | Done | `_assert_claim_basis` named refusals; `p638.claim.posts`, `p638.refusals` (11 arms) |
| AC2 three settlements distinguished | Done (round-1 fix) | `p638.claim.settled` added — the arm worked but had zero regression cell and a false evidence pointer before the fix |
| AC3 minimum versioned question | Done | refused before admission; `p638.items.continue` ×2, World PASS 6 |
| AC4 replay/correction unwind | Done (round-1 fix) | `p638.correction.evidence` (document release), `p638.correction.race` (typed `correction_target_already_corrected`, was raw 23505) |
| AC5 C1/C3/C6 surfaces | Done | `/clients/:id/accounting/claims`, `get_work_claim_origin` |
| AC6 itemised lineage | Done | register discloses items, waiting facts by name, correction chain |
| AC7 browser journey | Done, named run-level residual | 12/13 in-run, 13/13 across runs; shared `/login` stall not this branch's |
| AC8 real World, least-privileged | Done (local) | 24 pass DB; World e2e PASS 7 legs |
| C08.9 advance date ownership | Verify-only, no gap | date stays caller-stated by contract |
| C55.23 deterministic facts, no second human | Done | `p638.no_second_approval` |

**Review summary.** Adversarial: 1 blocker (F1, false AC2 pointer), 4 shoulds (F2 sealed-year admission, F3 raw-23505 correction race, F4 unasserted document unwind, plus SEC-1 duplicated by spec), 3 notes (stale "four" comment, merge-order hazard F6, cluster bookkeeping). Spec: 1 should (SEC-1, same as F1). Standards: 0 findings. Re-check closed F1–F5 and F7 with independent probes (including over-refusal controls proving the sealed-year arm blocks only sealed years); F6 stays **open by design** — it names a merge-order hazard for the integration worker (0206's prestate pins the `_subledger_on_approve` roster byte-exact; a sibling migration in 0199–0205 that merely names the hook could trip it), not a branch defect.

**Ratifications applied (DECISIONS §3.1).** None. No post-review ratification row names #638; the binding ruling for this ticket is DECISIONS §2 #638's own mid-wave amendment (no new purpose), which is a brief-level correction rather than a post-review ratification, and is fully reflected in what shipped.

**Residuals / follow-ups.** Blueprint drift, recorded not edited: PRD:114's implicit "amount-based" high-stakes gating already doesn't hold repo-wide (`is_high_stakes` also fires on opening balance/year-end/tax-affecting); the Work lane never calls it, but `book_staff_advance_application`'s drafted branch still carries the older forced checker. The `_subledger_on_approve` caller census is measured at six live callers, not the four `0037` text names — shared stale-pin follow-up with #639. F6's merge-order recipe is owed to the integration worker verbatim (re-measure and re-issue the roster string if 0206's prestate refuses after the full 0001→0209 chain — never weaken it). No Playwright coverage for `?tab=staffAdvances` (deliberately split out). A claim is labelled "Journal entry" on the Work list, not by claimant, until a list-level origin projection exists. Multi-advance allocation inside one claim is a scope cut. The `/login` stall costs every walk in the wave a red per run and needs one shared owner. `already_settled` has no runtime World e2e or Playwright leg (DB-door proof only, deliberate). A fiscal year sealing between admission and posting can still leave an admitted-never-posted claim (disclosed, deliberate — the register shows it honestly). #707/#693 untouched.

**Successor contract.** `chatTurn_v20` carries `start_staff_expense_claim_work` (tool + `.strict()` schema + door call spelled out in the report's own footer). No migration needed — the door already exists and is granted. Purpose stays `journal_entry`; **no `WORK_ACCEPTED_PURPOSES` widening**.

**Integration evidence:** <INTEGRATION_PLACEHOLDER>
