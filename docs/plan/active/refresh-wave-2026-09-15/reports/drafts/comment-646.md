Landed on `impl/646-document-correction` via 7 commits (`92fefff8`…`255559cb`, migration 0202 + one round of review fixes).

Per DECISIONS §2 #646 (D6), a correction is a `clara.document_fact_revisions` row plus the existing `filing_corrections` — **not** an `accounting_work` row; posted-effect integration stays #676's. A human fact revision appends a declarative `clara-fact-human:v1` extraction and never updates `document_regions`; a new narrow door `dismiss_orphaned_classification_question` closes the one classify question with no live filing, with no widening of `_active_document_filing`. Three routed views (`?document=<id>&tab=original|facts|accounting`), a revision Dialog, an impact Sheet for wrong-client context, and a persistent `DoorFeedback` Alert round out the web side. Fix round 1 closed one blocker (a fourth RPC-dispatch spelling made the lane invisible to the shared-verb census, so four real cross-lane verb shares went undeclared) and one should found independently by two lenses (a `'fact'` revision's ledger/receipt/audit all named the document-wide classification pointer instead of the `invoice_facts` reading it actually superseded), plus two spec shoulds (a vacuous a11y scan on the open Sheet; a vacuous "honest empty" cell). Local evidence: DB battery 16/16 (was 15, confirmed on a from-scratch chain), neighbours 138/138, operation-census 10/10, rig-isolation 20/21 (1 skip by design), whole `apps/web` suite 3727/3729 clean, Playwright walk 15/15 on a quiet host (contended-host reds confirmed not this branch), typecheck/lint exit 0. All findings across three lenses closed on the re-check. Hosted evidence pending.

| AC / row | State | Evidence |
|---|---|---|
| AC1 revision w/ source version, actor, reason | Done (round-1 fix) | `p646.fact.observes_facts_reading` — the revision now names the `invoice_facts` extraction it superseded, not the classification row |
| AC2 routed views; stale/duplicate/lost-response | Done | `p646.fact.stale` (CLR19 both versions + attempted value); `p646.replay.one_receipt` |
| AC3 reassess Work+Knowledge, narrow invalidation | Partial by name | classification invalidation narrow; automatic re-assessment deferred (PRD:123 → #658/#663); Work half partial — see residuals |
| AC4 posted effects | Partial, residual #676 | band states both Alerts per brief-646:64's literal "two persistent rows"; `p646.horn_a.no_work` — zero Work/receipt minted |
| AC5 six correction shapes | Done | fact append, kind stamp, refile+preview/propose/approve, orphan dismiss, dependents projection, replay-one-receipt |
| AC6 Dialog/Sheet/Alert | Done | wizard suspends to one overlay; `door-feedback.tsx`'s first import of `ui/alert.tsx`; focus-ring count unmoved (21) |
| AC7 journey states | Done, mock-backed (round-1 fix) | open-Sheet a11y scan now real (`.include`-scoped, cannot pass vacuously); orphan cell asserts the actual empty sentence |
| AC8 real roles, no Work minted | Done; no Workflow leg owed | `p646.fact.floor` viewer-refused; D6 makes this DB-only, proven not asserted |

**Review summary.** Standards: 1 blocker (census blind to a fourth RPC-dispatch spelling — closed by a rename + declaring all 5 real shares, including a fifth found during the fix). Spec: 2 shoulds (vacuous a11y-scan claim; vacuous empty-state claim, both closed) plus 1 note (host-contention Playwright reds, re-measured, not a defect). Adversarial: 1 should (646-A1, wrong observed-extraction pointer, closed and re-raced from scratch), 4 notes (a firm-term join predicate added; an activity-feed visibility gap documented not changed; a band-wording ratification request, declined — see below; cluster bookkeeping). Re-check independently reproduced the census blindness and its fix and closed everything; `open: []`, `new_blockers: []`.

**Ratifications applied (DECISIONS §3.1).** None listed for #646 in DECISIONS §3.1 by that exact wording, but the digest records the same ruling directly: the fix round requested ratification to make the band's "Accounting impact pending" Alert conditional on a live claim, and this was **not applied** — `brief-646.md:64` specifies two persistent rows and AC4's own wording says "separately," so making one conditional would change the brief rather than refine it. A conditional second row is left to #676.

**Residuals / follow-ups.** Posted-effect integration is #676's (also C48.4's close half). Automatic Knowledge/Work re-assessment deferred to #658/#663. A changed source can still be answered under the old Work-question version (`answer_work_question` compares only `question_version`+`basis_digest`, never `knowledge_version` — needs #654's `observedRevisions` plus a runtime re-ask). The activity-kind ladder (D13) still misfiles by prefix, shared across 6 tickets. C88.16 has no identified resolution (two clue-only candidates). A fact revision on a multi-filed or unfiled document is visible only in the firm activity feed (documented, not changed). The verb-dispatch census's blind spot is fixed for this lane only — a repo-wide census is worth filing. `next build` panicked three times under host contention (Windows, cleared on retry).

**Successor contract.** None shipped — explicit negative. No Work is minted, so no purpose-blind launcher can hand a correction to `claraWork_v3`; `source-correction-basis.ts` is not shipped and stays out of the frozen-workflows hash lock. One dependency: a future automatic consumer keys on `claraWork_v4`'s `observedRevisions({knowledge_version})`, owned by #654 — #646 ships only the read it would query, `clara.list_source_dependents(p_document)`.

**Integration evidence:** <INTEGRATION_PLACEHOLDER>
