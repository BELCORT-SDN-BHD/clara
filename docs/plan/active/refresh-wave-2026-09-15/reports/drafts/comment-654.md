Landed on `impl/654-firm-defaults` via 10 commits (`099acda4` red battery → `91f40dae` migration 0220 (eligibility catalog, cross-client evidence wall, two firm reads) → `8bd80bc2` `/settings/knowledge` + Promote dialog + firm-vs-exception pair → `4f0d7bb1` browser walk + two defects it caught → `4b69d2b2` census re-pins → fix round 1 (`4c60a5f7`/`e734b6b5`/`0618b8f2`/`ba2c1cfe`) → fix round 2 (`1251e374`)), 4 files changed in round 2 alone (+241/−8), all under `packages/db`.

Ships a new fail-closed `clara.knowledge_key_firm_eligibility` relation (seeded: `default_currency`, `reporting_framework`, `accounting_basis`, plus preference/policy-kind records), enforced by a BEFORE INSERT trigger (CLR10 `knowledge_scope_not_firm_defaultable`) and a private-evidence wall spanning `knowledge_records` and `document_filings` (a firm-scope record may not pin a document with any live client filing, and vice versa) — zero recuts of the 0192 bodies. `/settings/knowledge` entrance: firm register, Promote dialog, Correct/Withdraw (added round 1 to close a real gap — nine of twelve facts had no correction path at review), and the firm-rule-vs-client-exception pair on every client's C13. Re-evaluation engine explicitly not built per PRD:123; the human-review affordance ships instead. Round 2 closed a genuine concurrency hole round-1's recheck found: two BEFORE-row triggers each reading the other's table let two concurrent transactions both commit under READ COMMITTED; fixed with a shared advisory lock plus a `FOR KEY SHARE` row-lock ordering, proven deadlock-free by construction and stress-tested at 24 simultaneous rounds, zero deadlocks. Local evidence, final state: DB battery 21/21 (was 15 at review); combined with neighbours 61/61; filing-lane regression set 99/98/0/1-skip; web census suites 88/88; whole `apps/web` suite 3744/3742/0 fail clean re-run; Playwright `knowledge` 27/27. `pnpm typecheck`/`lint` exit 0. Three-axis review + two fix rounds, re-checked twice, closed with `open: []`, `new_blockers: []`. **Hosted evidence pending.**

| AC / Row | State | Evidence |
|---|---|---|
| AC1 eligibility + evidence walls, exceptions survive, promotion authority recorded | Done, including the round-2 concurrency close | `p654.eligibility.*`, `p654.evidence.race_capture_vs_filing` (round 2's 21st cell, all 4 arrival orders green), `p654.exception.survives_promotion`, `p654.authority.floor` |
| AC2 firm-vs-exception pair; "applies in a subsequent Work" | Partial — pair built, Work-facing half is a successor contract | `get_knowledge_applicability`, `ke.01`–`ke.04`; Work-facing half not claimed this branch |
| AC3 automatic re-evaluation (clauses 1–2); not a posting grant (clause 3) | Partial, by blueprint (PRD:123 defers 1–2) | Human-review affordance (`kf.07`, walk leg 1); `p654.census.not_a_posting_grant` |
| AC4 authority floors, revoked membership, cross-client evidence both directions/both pins/concurrent | Done | `p654.authority.revoked_membership`, `p654.evidence.*` incl. the round-2 race cell, `p654.exception.independent_overrides` |
| AC5 14 browser-walk legs incl. Correct/Withdraw, reduced motion, denied-below-floor | Done | `e2e/knowledge-firm-walk.spec.ts`, axe-checked every face |
| AC6 real Postgres, least-privileged roles, no Workflow clause | Done (stated, not skipped — no runtime module ships) | DB+web local; no World e2e owed |
| Correction path (round-1 blocker close) | Done | `knowledge-firm-acts.tsx`, `knowledge-firm-panel.test.tsx` 11/11 |
| Concurrent capture-vs-filing race (round-2 finding 654-RC1) | Done | shared advisory lock + row-lock ordering; recheck-2 confirmed on a virgin cluster, all 4 orders, 0 deadlocks |

**Review summary.** Adversarial: 1 blocker (654-ADV-1, evidence wall one-way) + 2 shoulds (`source_work_id` unwalled; no Correct/Withdraw surface) + 5 notes, closed round 1. Spec: successor-contract refusal map named a code never raised, plus a missing reduced-motion leg, closed round 1. Standards: 3 unused i18n keys, deleted. The round-1 **recheck** then surfaced 654-RC1 (a genuine concurrent-transaction hole in the just-fixed evidence wall) and 654-RATIFY-1 (the fix worker's own unrequested narrowing of the evidence census to LIVE rows). Round 2 closed 654-RC1 with the advisory-lock fix and left 654-RATIFY-1 for the orchestrator. Recheck-2 closed both: `open: []`, `new_blockers: []`.

**Ratifications applied (DECISIONS §3.1).** The firm-scope evidence census counting only LIVE violations (superseded/withdrawn revisions raise NOTICE instead of refusing) is **ratified** (§3.0 R5): "it is the invariant the three guards enforce and what every read emits."

**Residuals / follow-ups.** Both blueprint-drift rows recorded, not edited (PRD:123 re-evaluation engine not built; PRD:122 chat-capture half is a contract only). The `claraWork_v4` stanza is contract-only — nothing executes on this branch. No hosted lane exists for this slice. `rig654r`/55612 was stale after round 1 but recheck-2 re-applied it from scratch, so it is now current — flagged here since WAVE-DIGEST's cleanup census predates that re-apply. The promoter's role at the instant of a governed act is unrecorded (`firm_memberships` has no history, `audit_log` has no role column) — follow-up, naturally #625's lane. `knowledge_keys.scope_default` is now provably dead. D13's activity-kind residual is untouched.

**Successor contract.** `claraWork_v4`, written not cut: (a) a knowledge-context step feeding `observedRevisions({knowledge_version})` into the *existing* trace call at `claraWork.v3.impl.ts:465-474`, no new grant — generically named in DECISIONS §1.1; (b) tool `ask_knowledge_conflict` (**not named** in DECISIONS §1.1's own list — flag for the integration cut worker), uses `open_work_question`'s existing grant; expiry must re-open rather than resolve since 0198 removed the `work_id is not null` predicate from `expire_due_interruptions`.

**Integration evidence:** <INTEGRATION_PLACEHOLDER>
