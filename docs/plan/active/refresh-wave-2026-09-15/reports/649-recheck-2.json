{
  "ticket": 649,
  "head": "22d283c3",
  "closed": [
    "B1",
    "NEW-1"
  ],
  "open": [],
  "new_blockers": [],
  "ran": [
    "GIT STATE — worktree C:\\Users\\zhant\\Desktop\\clara-wt\\649 on impl/649-client-onboarding at 22d283c3; `git status --porcelain` EMPTY at start and at end, `git stash list` empty. I edited no file, ran no git/worktree write, pushed nothing.",
    "LIVE LOCK ORDER (my own C:\\Users\\zhant\\AppData\\Local\\Temp\\claude\\649rc2\\livebody.mjs, PGPORT=55508 clara_649) — settle's prosrc is BYTE-IDENTICAL to the committed C:\\Users\\zhant\\Desktop\\clara-wt\\649\\packages\\db\\migrations\\0204_client_onboarding_facts.sql (len 7752, file.includes(prosrc)=true). Comment-stripped order: rung 203005004@914 -> clara.clients FOR UPDATE@971 -> clara.onboarding_plans FOR UPDATE@1057; preReadFirmScoped=true; the now-unreachable `p.firm_id is distinct from c.firm` arm is ABSENT. Neighbours off the same catalog: set_client_fy_end rung@1127 -> update clients@2479; approve_opening_seed rung@1367 -> plan FOR UPDATE@1430; commit_client_onboarding clients@377 -> plans@565; cancel_client_onboarding clients@346 -> plans@413 (neither takes a rung). The shipped order sits above every one of them.",
    "B1 PROBE 1 — reviewer's C:\\Users\\zhant\\AppData\\Local\\Temp\\claude\\649rc\\advrung.mjs verbatim (55508/clara_649): `X result: acquired` (recheck-1 had `X result: 40P01`), settle returned its receipt, `client row: {\"fy_end_month\":6,\"fy_end_day\":30}`.",
    "B1 PROBE 2 — reviewer's 649rc\\advrung2.mjs verbatim: ALL SIX delays (0/40/80/150/300/600 ms) now `approve-side=acquired  settle=ok  client={\"fy_end_month\":6,\"fy_end_day\":30}`. Recheck-1 measured `settle=40P01` with `client={null,null}` at 150/300/600 ms.",
    "NEW-1 PROBE — reviewer's 649rc\\crosstenant2.mjs verbatim: CONTROL (firm A's row free) CLR11 plan_not_in_firm after 3 ms; TEST (firm A holds its OWN client row 2.5 s) CLR11 after 2 ms; the script's own verdict line prints `=> refused immediately: no foreign row lock was attempted`. Recheck-1 measured 51 ms / 2453 ms.",
    "DB BATTERY on the lane rig — packages/db/tests/client-onboarding-identity.test.mjs with the 29 preintegration gates verbatim from 649rc/gates.txt, PGPORT=55508 PGDATABASE=clara_649: 13 tests, 13 pass, 0 fail, 0 skipped (includes p649.settle.opening_rung_order and p649.settle.foreign_plan_takes_no_lock).",
    "FROM-SCRATCH CHAIN (the migration changed this round) — on 55608/clara_649r I rolled 0204 back for real via 649rc/rollback0204.mjs (3 functions dropped, event_types row + 1 taxonomy row + 7 domain_events removed, ledger row deleted -> 193 migrations, `0204 names left: 0`) and re-applied with `pnpm db:migrate`: `#649 prestate: clean`, `#649 tail: OK`, `applied 0204_client_onboarding_facts`, `migrate: 1 new migration(s) applied · 194 total · target 127.0.0.1:55608/clara_649r`, MIGRATE_EXIT=0. Battery on that freshly-applied chain: 13 tests, 13 pass, 0 fail, 0 skipped. (A second VIRGIN database on that cluster remains impossible for the cluster-wide role pin recheck-1 documented at 0154; clara_wave_b_ci was not touched.)",
    "THE CELLS ARE NOT VACUOUS — built the red prestate myself rather than trusting the fix report: `git show 13bd3e1e:packages/db/migrations/0204_client_onboarding_facts.sql` (47419 bytes, `diff -q` identical to the fix worker's extraction) spliced verbatim as clara_fn_owner into clara_649r_red, a clone of the freshly re-applied clara_649r on 55608. Markers on the spliced body: rungInBody=-1, planPreReadFirmScoped=false. Battery there: 13 tests, 11 pass, 2 FAIL — exactly `not ok 11 p649.settle.opening_rung_order` (expected/actual '40P01', \"…and neither did the transaction holding the rung\") and `not ok 12 p649.settle.foreign_plan_takes_no_lock` (expected/actual '55P03', \"an outsider queued on the owning firm's locks before being refused (1008ms) — that wait IS the oracle\"). Each new cell targets exactly one defect and nothing else. Clone dropped; 55608 back to clara_649r + clara_wave_b_ci.",
    "THE DEVIATION FROM B1's LITERAL REMEDY — verified, not accepted on trust. B1 prescribed `client ROW -> rung -> plan`; the worker shipped `rung -> client row -> plan`. 649fr2\\rungvsrow.mjs on 55508, whose adversary IS B1's prescribed shape (clara.clients row, then the rung): `set_client_fy_end (PRE-EXISTING)  door=40P01  adversary=acquired` and `settle_...facts (#649, rung-first)  door=40P01  adversary=acquired`. The prescribed order would have inverted settle against the very door it calls (set_client_fy_end takes the rung @1127 before it updates clara.clients @2479). Sound reason -> counts as closed for this loop; the orchestrator rules on the ratification.",
    "REGRESSION-HUNT, the one shape that could newly meet the rung-first door: a census of the live bodies finds exactly ONE that takes a clara.clients row before the client rung — clara.approve_wrong_client_correction (`clara.clients cl where cl.id=x.from_client … for update` @2940, `pg_advisory_xact_lock(203005004,hashtext(o.client_id::text))` @4505, both read off prosrc). The rungvsrow measurement above shows it inverts IDENTICALLY against the pre-existing set_client_fy_end, so it is another migration's pre-existing violation of 0037 SECTION K's rung-before-rows law, not something this fix created; 0037:2534-2538 discusses that door's rung only relative to its journal_entries locks, never relative to clara.clients. Left as the fix worker's follow-up 7, untouched. NOT filed as a blocker.",
    "REGRESSIONS — `pnpm typecheck` exit 0 (apps/web + packages/runtime); `pnpm lint` exit 0 (whole root chain incl. every selftest); `node scripts/check-frozen-workflows.mjs` OK (281 frozen files, 51 \"use workflow\" modules frozen+registered); `node packages/runtime/scripts/check-parts-parity.mjs` OK.",
    "WHOLE apps/web SUITE — `node scripts/run-tests.mjs`: 3750 tests, 3748 pass, 0 fail, 0 cancelled, 2 skipped, exit 0 (288.0 s). Matches the fix report's count exactly.",
    "DB CENSUS AFTER THE MIGRATION CHANGE — packages/db operation-census.test.mjs + rig-isolation.test.mjs with the same 29 gates: 31 tests, 30 pass, 0 fail, 1 skipped (T19 poison-role, destructive SKIP), exit 0.",
    "PLAYWRIGHT — `pnpm --filter @clara/web e2e client-create-walk` on MY ports only (CLARA_E2E_APP_ORIGIN=https://127.0.0.1:3300, NEXT 3301, RUNTIME 3302), the walk that drives the control c5eccfab changed: `4 passed (22.9s)`, PW_EXIT=0. No failure named another worktree, nothing flaked, so no isolation re-run was needed; afterwards 3300/3301/3302 hold TIME_WAIT sockets only, no LISTEN.",
    "WORLD LEGS, the reason they stand unre-run — `grep -rn \"settle_client_onboarding_facts|set_client_fy_end\" packages/runtime` returns ZERO hits across *.ts/*.mjs/*.js. The only non-db callers in the tree are apps/web/lib/onboarding/settle.ts, apps/web/lib/onboarding/fy-end.ts, apps/web/components/clara/OnboardingFyEndField.tsx and one test. The fix worker's reason holds.",
    "N2 MEASURED PRECISELY (read-only, nothing written) — 55508/clara_rt_test and 55608/clara_wave_b_ci BOTH still carry the pre-any-fix baaf48fe settle body (rungInBody=-1, preReadFirmScoped=false), while 55508/clara_649 and 55608/clara_649r carry eddaaf91/22d283c3's. Integration must re-apply 0204 on clara_wave_b_ci before this ticket's battery runs there, or cells 11 and 12 will red for the database's body, not the branch's. I touched neither.",
    "CITATIONS IN THE NEW COMMENTS, audited against the sources — packages/db/migrations/0037…:2515-2531 does say \"Taking them … BEFORE the open_items row locks, is what makes the extension deadlock-free rather than merely documented\", \"Advisory transaction locks are re-entrant, so the core re-taking them inside the composite is free\", and states the ladder \"firm (203005002) -> client (203005004)\" \"read as a PARTIAL order\"; 0042 carries \"THE RUNG BEFORE THE GUARD READS\" at :2002. No stale citation.",
    "DIFF REVIEW — `git diff 13bd3e1e..22d283c3 -- packages/db/migrations/0204_client_onboarding_facts.sql` is exactly the two fixes plus their comments: one added `perform pg_advisory_xact_lock(203005004, hashtext(v_client::text));` inside the existing `if v_client is not null` block, `and op.firm_id = c.firm` on the pre-read, `and firm_id = c.firm` on the locking read with `if not found` replacing `if not found or p.firm_id is distinct from c.firm`. No guard was dropped; the plan_client_moved belt is intact. c5eccfab adds one derived flag (`ambiguousAnswer = answered && arity >= 2`) to confirmDisabled in apps/web/components/firm/client-register-list.tsx; 22d283c3 is README-only."
  ]
}