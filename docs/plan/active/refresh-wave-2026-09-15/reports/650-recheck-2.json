{
  "ticket": 650,
  "head": "e9deacae",
  "closed": [
    "650-R1",
    "650-R2"
  ],
  "open": [],
  "new_blockers": [],
  "ran": [
    "worktree state: git log --oneline origin/main..HEAD -> 5 commits, head e9deacae (one new commit over the rechecked fed8b566); git status --porcelain -> clean BEFORE and AFTER every run; git diff fed8b566..HEAD --stat -> 5 files, +89/-11, ALL under apps/web (README.md, client-work-attention.tsx + .test.tsx, lib/work/client-work-pack.ts, messages/en.json); git diff --name-only fed8b566..HEAD -- packages/db/migrations/ -> EMPTY and -- packages/runtime -> EMPTY, so 0199 stays byte-identical to a948fb8b and no rollback / re-apply / from-scratch chain is owed. I edited nothing on the branch, ran no git worktree command, touched no other lane's files or ports, pushed nothing.",
    "650-R1 RACED (the reviewer's own sequence): `grep -n \"already narrowed to it\" apps/web/README.md` -> exit 1, NO MATCH (it was line 17 at fed8b566). The Client row now reads \"each count a link into that client's own Work list, narrowed as closely as that list's own axes allow (the recent-success drilldown is the same week over a different subject, which the tile discloses; see below)\", and a new paragraph at apps/web/README.md:35-45 states both narrowings the list cannot express. VERDICT: CLOSED.",
    "650-R1 — I did not take the new README paragraph on trust, I checked every factual claim in it against the SQL. (a) \"'retrying' is not a member of clara.list_accounting_work's status roster\": packages/db/migrations/0189*.sql:369-370 is the closed roster `('queued','running','awaiting_input','stopping','completed','refused','failed','cancelled','expired')` and 0189:110 defines Retrying as `status in ('queued','running') and attempts > 1` — a derived label, not a status. TRUE. (b) \"that door fences p_since/p_until on clara.accounting_work.created_at\": 0189:427-428 `and (p_since is null or w.created_at >= p_since) and (p_until is null or w.created_at < p_until)` over `from clara.accounting_work w`. TRUE, and it is the line the README cites. (c) \"p650.pack.recent_success_drilldown measures it in both directions\": packages/db/tests/client-work-pack.test.mjs:338, and it passed on this tip (below). (d) the tile really does disclose beside the count: client-work-attention.tsx:161-166. No claim in the new paragraph is unsupported.",
    "650-R2 RACED: the overstating arm is gone. client-work-attention.tsx:161-165 now renders `workAttentionWindowDates(pack) === null ? t(\"recentSuccessListUndated\") : t(\"recentSuccessListBasis\")`, and the new key (messages/en.json:554) names the wider population — \"the link opens every completed Work for this client — not only the last seven days\". Placement checked by reading the whole tile body: the <p> sits inside the `facet.count > 0` branch (count===0 returns the empty line at :121-123), so no dangling sentence under a zero. Two new cells pin it, a positive and a control (client-work-attention.test.tsx:206-229). VERDICT: CLOSED.",
    "650-R2 — the de-duplication the fix claims is real, not asserted: `grep -rn \"workAttentionWindowInstants|workAttentionWindowDates\"` over apps/web *.ts/*.tsx -> the predicate is DEFINED once (client-work-pack.ts:299) and read by exactly three callers — the href builder (:281), the instant rebuilder (:313) and the board (client-work-attention.tsx:54,162). `grep -rn \"isDateOnly|fromDate|toDate\"` over non-test apps/web source -> the fromDate/toDate + isDateOnly test survives in ONE place (client-work-pack.ts:302-304, inside that predicate); the other isDateOnly callers (lib/firm/activity.ts, lib/work/work-list-url-state.ts) are URL-state parsers, a different question. The href semantics are unchanged by the refactor (`dated ? from : null` became `dates?.from ?? null`, null on exactly the same condition), which the untouched cell \"an unreadable window drops the dates rather than guessing them\" re-proves.",
    "650-R2 — the fix worker's \"defensive arm, the door always publishes a window\" claim VERIFIED independently at packages/db/migrations/0199_client_work_pack.sql:288-292 (`v_today := (v_now at time zone 'Asia/Kuala_Lumpur')::date; v_from_date := v_today - 6`) and :423-428 (`'from_date', v_from_date::text, 'to_date', v_today::text, 'days', 7` built unconditionally into the return). A dateless window is a malformed body, not a state the door can emit — so the arm is polish, exactly as both the finding and the fix say, and the unit cell is the right altitude for it.",
    "apps/web focused battery (the four files the ticket owns): node --import ./test/bootstrap.mjs --import tsx --test lib/work/client-work-pack.test.ts lib/work/use-client-work-pack.test.ts components/firm/client-home/client-work-attention.test.tsx components/firm/client-workspace-overview.test.tsx -> 55 tests, 55 pass, 0 fail, 0 skipped, 4.75 s (53 at fed8b566; the two new ones are round 2's). Matches the fix round's count.",
    "apps/web WHOLE unit suite: node scripts/run-tests.mjs -> 3,762 tests, 135 suites, 3,759 pass, 1 FAIL, 2 skipped, 257.5 s, exit 1. The failure is `not ok 1279 - t728: a document already spoken for renders DISABLED with a reason and a link to the entry it backs` at apps/web/components/work/attach-evidence-dialog.test.tsx:472 (ERR_ASSERTION, expected true / actual undefined, log littered with React act() warnings — a timing-sensitive async dialog cell). It is NOT this branch's file: `git diff --name-only origin/main...HEAD | grep -c attach-evidence-dialog` -> 0. It is also NOT the failure the recheck-1 lens saw under load (that was #630's use-clara-thread-stop.test.ts), and the fix round's own whole-suite run was green — three runs, three different outcomes on files nobody is editing.",
    "apps/web ISOLATED re-run of that file: node --import ./test/bootstrap.mjs --import tsx --test components/work/attach-evidence-dialog.test.tsx -> 22 tests, 22 pass, 0 fail, 0 skipped, 9.45 s. BOTH runs reported; the failure vanished solo on a file the branch does not touch, so it is twelve-lane host contention, not a #650 regression. Nothing was 'fixed'.",
    "packages/db battery on rig 55509/clara_650 (not owed — the migration is byte-identical — run anyway as regression insurance), the exact 29 --import preintegration gates from packages/db/package.json: node --test --test-concurrency=1 <29 gates> tests/client-work-pack.test.mjs -> 13 tests, 13 pass, 0 fail, 0 skipped, 24.6 s, including `ok 5 - p650.pack.recent_success_drilldown — same week, different subject: the drilldown diverges in exactly two named ways`. No skips.",
    "worktree root deterministic gates: pnpm typecheck -> apps/web Done, packages/runtime Done, exit 0. pnpm lint -> exit 0 across all four workspaces, with check-message-keys at 2,932 keys (2,931 at fed8b566 + the one new key) and its selftest, check-test-manifest + selftest (no manifest row owed — no new test FILE), check-token-contrast. node scripts/check-frozen-workflows.mjs -> OK, 281 frozen files verified append-only vs origin/main, 51 'use workflow' modules frozen+registered, exit 0.",
    "apps/web Playwright on MY triple only (CLARA_E2E_APP_ORIGIN=https://127.0.0.1:3310, NEXT_PORT=3311, RUNTIME_PORT=3312; all three verified free with netstat before starting, no listener killed, no other lane's port touched): pnpm --filter @clara/web e2e home-board-walk -> 9 passed, 0 failed, 38.2 s, exit 0 — FIRST solo run, no re-run owed. Leg 6 (home.facets.drilldown) still asserts the board carries /dated by when each Work started, not when it posted/ before any click (home-board-walk.spec.ts:421) and still lands the recent-success click on \"Rates accrual\" while dropping \"Bank fee\" — round 2's conditional leaves the populated arm byte-identical, which this run proves rather than assumes.",
    "NON-BLOCKING OBSERVATION, pre-existing, NOT a new finding: `workAttentionWindowInstants` (client-work-pack.ts:310) is exported with ZERO callers anywhere in the repo — `grep -rn workAttentionWindowInstants` over all .ts/.tsx/.mjs/.md excluding .next and node_modules returns only its own definition line. It was already callerless at the rechecked head (`git grep -n workAttentionWindowInstants fed8b566 -- apps/web` -> the definition alone), so round 2 did not introduce it; round 2 only made it delegate to the new shared predicate. Its docblock (\"Exported so the board and its cells read the same conversion the list performs\") describes a reader that does not exist. Dead-export cleanup at the orchestrator's discretion; nothing in the ticket's behaviour depends on it.",
    "cross-check of the fix round's bookkeeping: docs/plan/active/refresh-wave-2026-09-15/reports/650-final.md really was updated in place — line 6 carries e9edeacae's commit line, lines 13-15 name both fix rounds and point at reports/650-fixround-1.md and -2.md, and the AC3 row (line 30) now carries the windowless arm, `recentSuccessListUndated` and the one-predicate note. Read only; I did not write to the main repo.",
    "NOT RUN, stated rather than claimed: (1) no from-scratch migrate/seed chain on rig650r/55609 — zero migration bytes changed since the head the reviewer already proved that chain on; (2) check-parts-parity — packages/runtime's diff is empty (recheck-1 ran it green regardless); (3) responsive-shell-walk — round 2 changed one conditional sentence inside an already-populated tile with no structural or element change, the recheck-1 lens ran it green 24/24 on fed8b566, and a second run on a host that just produced a contention failure would add a data point, not evidence; (4) no hosted evidence — every run above is LOCAL."
  ]
}