{
  "ticket": 639,
  "head": "693f88998d51d653ca3560ff6a57e7b88dfa18a2",
  "closed": [
    "STANDARDS F1 + ADV 639-A1 (blocker) — `p639.refusal.locked_period` now asserts. The `noteLane(...) + return` escape is gone from C:\\Users\\zhant\\Desktop\\clara-wt\\639\\packages\\db\\tests\\fixed-asset-acquisition.test.mjs:341-388; the cell SEEDS a closed `clara.fiscal_years` row on its own dedicated client (the `w623.post.closed-period` idiom the brief named), asserts the seeding was mandatory AND reads back `status='closed'`, then asserts CLR19 `write_into_closed_period` with entry / committed-receipt / register counts unmoved. Re-raced on BOTH rigs: the string 'cell recorded, not asserted' now appears 0 times in either battery log (was the reviewer's evidence of a zero-assertion pass). I also wrote my own CAUSAL control the review did not have — same client shape, same posting, year seeded OPEN instead of closed: `recheck639.control` measured `err=null`, +1 journal entry and +1 register row, while `recheck639.closed` measured CLR19/write_into_closed_period with nothing moved. So the refusal is caused by the closed status, not by the mere presence of a fiscal-year row. The reviewer's own probe (rev639.locked_period.SEEDED) also re-ran green on both rigs.",
    "STANDARDS F2 (should) — the source document deep-links. C:\\Users\\zhant\\Desktop\\clara-wt\\639\\apps\\web\\components\\registers\\fixed-asset-detail.tsx:208-225 now builds the href with the workbench's own URL state (`documentUrl(`/clients/${clientId}/documents`, applyDocumentParam(new URLSearchParams(), acquisition.document_id))`), exactly the helper the review named. Pinned by the new cell `detail.source_document`, which asserts the rendered href is `/clients/c1/documents?document=<uuid>` and that the link is named by the filename; it passed inside my whole apps/web run (3735/3737 pass, 0 fail).",
    "SPEC F1 (blocker) — axis -> CONTROL is wired into the HUMAN door, and the browser cells are real. New production module C:\\Users\\zhant\\Desktop\\clara-wt\\639\\apps\\web\\lib\\registers\\fa-refusal-field.ts (a deliberate mirror of the runtime map; I verified `apps/web` has no `@clara/runtime` dependency and no import of it — the four grep hits are all comments, and `lib/checkout/stripe-session.ts:48` is the standing precedent for the same mirror); `fa-row-actions.tsx` now carries the caller's standing refusal INTO the dialog (`refusal={toDialogRefusal(error)}`, `refusalFocusId={faRefusalControlId(idPrefix, error)}`) and `FaDoorDialog.tsx:83-108` focuses that control and marks it `aria-invalid`, guarded by `attempt === 0` so a fresh open never inherits a sibling's refusal. I checked the two axis maps are byte-equal in content (packages/runtime/lib/fixed-asset-acquisition.ts:144-154 vs the web copy) and that `FaParticularsFields` really renders `${idPrefix}-${suffix}` for all seven controls. The walk cell titled 'an invalid answer names the dependent CONTROL' now fills a residual ABOVE COST — the one invalid answer `particularsReadyToSubmit` cannot gate — and asserts the CLR37 message, the focus landing on `Residual value (RM)`, `aria-invalid=\"true\"` and the surviving draft; a new cell signs in as `viewer@example.test` and asserts CLR04 renders in the dialog, names NO field, and leaves the asset intact. Measured green: full walk run 3 on the assigned triple = 8 passed (33.0s), all eight cells listed by name. The brief's 'reuse unchanged' files are genuinely untouched (`git diff origin/main...HEAD` lists neither fa-particulars-fields.tsx nor work-question-panel/-form.tsx).",
    "SPEC F2 (note) — the walk hang does NOT reproduce, and the 8/8 figure is now mine as well as the worker's. Three runs on 3260/3261/3262, all reported: run 1 (host simultaneously carrying my from-scratch migration chain and a next build) = 7 passed, 1 failed — the new VIEWER cell, `page.goto: net::ERR_ABORTED; maybe frame was detached?` at `/login`, i.e. the sign-in navigation aborted, not an assertion; run 2 (that cell solo) = 1 passed (11.7s); run 3 (all eight, quiet host) = 8 passed (33.0s) with the axe cell at 6.2s, far inside even the default budget. Contention, re-run in isolation, both runs reported — no 'fix' applied.",
    "ADV 639-A2 (should) — two rows from ONE invoice no longer claim succession. 0201's history lateral now has an ORDERLESS `co_acquired_on_same_document`/`co_acquired` arm that excludes the row's own entry from `source_document` and compares `approved_at` STRICTLY. I re-ran the reviewer's own probe verbatim (rev639.history.siblings, adv639b.test.mjs) on BOTH the lane rig and the from-scratch chain: one document-lane entry with two debits on the enrolled cost account births 2 rows, and each names the other `relation=co_acquired link=co_acquired_on_same_document` — the word 'successor' is gone from both directions. The battery's own `p639.correction.co_acquired` cell asserts both directions plus `chain_open=false`, and `p639.correction.chain` still measures predecessor/successor, so the reversal lineage did not regress. The surface follows: `relationLabels` replaces the two-way ternary, `history.links.co_acquired_on_same_document` and `history.coAcquired` are in en.json (the only locale file), and `detail.co_acquired` pins that 'Successor' never renders for a sibling. RESIDUAL worth the orchestrator's eye, not a blocker: the strict `>` ordering in the `source_document` arm has no behavioural cell (correction.chain runs on the Work lane, where `v_doc` is null), and two entries on one document approved inside a single transaction would share `approved_at` and read as co-acquired rather than predecessor/successor.",
    "ADV 639-A3 (should) — the raced cell and the merge-order table both shipped. `p639.particulars.race` drives two real `clara_runtime` sessions across a COMMIT barrier and `p639.particulars.replay` covers op-key replay plus payload conflict; both pass on both rigs. I re-raced the reviewer's own probe: `race -> s1 ok=true err=null | s2 ok=false err=CLR37/fa_particulars_already_complete`, register carries the winner's 60 (not the loser's 36), journal count unmoved; `payload-conflict -> CLR10 'op_key reused with different args'`. The merge-order table is at C:\\Users\\zhant\\Desktop\\clara-rebuild\\docs\\plan\\active\\refresh-wave-2026-09-15\\reports\\639-final.md:106-122, listing 0199 (#650) and 0200 (#647) against all three things 0201 pins plus the regprocedure roster, and naming DECISIONS §1.3 as its authority rather than claiming to have read files it cannot reach. RESIDUAL: the race cell proves 'exactly one effect + typed refusal' but does not itself assert that session 2 BLOCKED (it would pass either way); the comment claims blocking.",
    "ADV 639-A4 (note) — recorded by the fix worker as deliberately-left with a stated reason, so it is closed for this loop and the orchestrator rules. The false claim is gone: 0201:770-782 now states the MEASURED behaviour and packages/db/README.md carries the same correction. The behaviour is unchanged and I re-measured it on the from-scratch chain: `ORACLE: unknown-client -> CLR11/client_not_found   another-firm's-client -> CLR04/obo_not_active`. Tenant isolation itself is still intact in the same probe (firm-B human -> CLR11, firm-B `clara_agent_ro` direct read -> 0 rows, cross-firm obo -> CLR04, no app role holds INSERT/UPDATE/DELETE on clara.fixed_assets).",
    "ADV 639-A5 (note) — partially applied with the remainder named, which the loop treats as closed. `scheduled_run` is now BEHAVIOURAL: `p639.depreciation.independent` runs a real depreciation period through the production command and asserts `assetCountOf(client) === 2` afterwards (green on the from-scratch chain). The K-family opening arm stays belt-only and says so in the cell and in 639-final.md:125-128 as a named residual, on the ground that no opening-seed fixture exists anywhere under packages/db/tests.",
    "ADV 639-A6 (note) — the live authority read is now taken under a lock. 0201 adds `perform 1 from clara.firms f where f.id = v_firm for key share;` before the membership select and `for share` on the select itself — the exact pair 0195:1792-1797 measured against `set_member_role`'s `for update` — and a new tail T.9b pins both by their EXECUTABLE text (the comment alone cannot satisfy it; I checked the substrings do not collide). I verified on the deployed catalog that `clara.complete_fixed_asset_particulars_for` carries both clauses, and the World e2e PASS 5 ('the particulars door re-reads LIVE authority, refuses a demoted initiator by name') and `p639.particulars.for_overload` both still pass, so no 40P01 or behaviour change came with it.",
    "ADV 639-A7 (note) — no branch action needed. I did not reuse the review's cluster: I recreated `rig639r` on 55604 from scratch with mkrig.sh and created a fresh `clara_639r`, which is the only safe way to re-run a from-scratch chain given 0154's cluster-global role-count pin. The cluster is left ONLINE at 127.0.0.1:55604 carrying `clara_639r` at 194/0201 for the next loop."
  ],
  "open": [],
  "new_blockers": [],
  "ran": [
    "wsl -u root -- bash .../mkrig.sh rig639r 55604 -> cluster DROPPED and recreated from scratch; 127.0.0.1:55604 accepting connections (PG 17)",
    "createdb clara_639r + PGPORT=55604 PGDATABASE=clara_639r pnpm db:migrate (from scratch, worktree root) -> FAILED TWICE at 0057_wave_e_registry_snapshots: 'canceling statement due to statement timeout'. 0057 sets its OWN `set local statement_timeout = '5min'` (0057:79) and it is an origin/main file this branch does not touch (`git diff --name-status origin/main...HEAD -- packages/db/migrations` = only `A 0201_fixed_asset_acquisition.sql`); the host was carrying a next build plus the whole apps/web unit suite at the time. HOST ARTIFACT, not a branch defect. The chain completed minutes later on a quieter host (the fixtures' own x41EnsureReady drove 0057->0201): clara.schema_migrations = 194 rows, max `0201_fixed_asset_acquisition`, applied_at 19:33:54Z, stored checksum 7b477abd810acca3f25e9f531e3163c6dda20c692a728499808c4572911cd141",
    "sha256 packages/db/migrations/0201_fixed_asset_acquisition.sql -> 7b477abd810a…1cd141 == the checksum applied on BOTH rigs == the pin in apps/web/tests/firm-scope-db-pins.corpus.ts:146. No drift.",
    "pnpm db:seed on 55604/clara_639r -> '2 seed file(s) applied', exit 0",
    "FROM-SCRATCH CHAIN, ticket battery (55604/clara_639r): node --test --test-concurrency=1 tests/fixed-asset-acquisition.test.mjs -> 21 tests, 21 pass, 0 fail, 0 skipped. Includes the three new cells (particulars.race, particulars.replay, correction.co_acquired) and the rewritten refusal.locked_period; grep 'cell recorded, not asserted' = 0 hits",
    "LANE RIG (55504/clara_639, applied 0201 checksum verified == file sha): 29-gate battery, 7 files (fixed-asset-acquisition, x41-wave-d-a-fa, x41-depreciation, x42-r7-fa-stamp, x41b0-surface, x41b2-surface, work-journal-post) -> 94 tests, 94 pass, 0 fail, 0 skipped (245s). Was 91/91 at the reviewed head; +3 cells.",
    "node --test tests/operation-census.test.mjs tests/rig-isolation.test.mjs (no reset flags) -> 31 tests, 30 pass, 0 fail, 1 skipped (T19 destructive, by design) — unchanged from the review baseline",
    "REVIEWER'S OWN PROBES, re-raced verbatim on BOTH rigs (scratchpad/rev639/adv639.test.mjs + adv639b.test.mjs) plus my new open-vs-closed control (scratchpad/recheck639/control639.test.mjs): 8 tests, 8 pass, 0 fail on 55504 AND on the from-scratch 55604. Measured: OPEN year -> err=null (+1 entry, +1 asset); CLOSED year -> CLR19/write_into_closed_period, nothing moved; race -> s1 ok / s2 CLR37 fa_particulars_already_complete; payload conflict -> CLR10 'op_key reused with different args'; two-cost-line invoice -> 2 rows, each `relation=co_acquired link=co_acquired_on_same_document`; firm-B human -> CLR11, firm-B agent direct read -> 0 rows, cross-firm obo -> CLR04; no app role holds direct DML on clara.fixed_assets",
    "packages/runtime: node --test tests/fixed-asset-acquisition-unit.test.mjs -> 8 tests, 8 pass (incl. fa.axis and the two clr40 classification cells)",
    "World e2e (55504/clara_rt_test, 194/0201, workflow schema bootstrapped, door verified to carry both lock clauses): node tests/fixed-asset-acquisition-e2e.mjs -> 'FIXED ASSET ACQUISITION E2E: PASS' with PASS 1+2, PASS 3, PASS 5 (live authority re-read, demoted initiator refused by name) and PASS 4 (crash after commit -> resume -> same asset id)",
    "pnpm typecheck (root) -> exit 0 (apps/web Done, packages/runtime Done)",
    "pnpm lint (root) -> exit 0",
    "node scripts/check-frozen-workflows.mjs -> OK, 281 frozen files verified, 51 'use workflow' modules frozen+registered; node packages/runtime/scripts/check-parts-parity.mjs -> OK",
    "whole apps/web unit suite: node scripts/run-tests.mjs -> 3737 tests, 135 suites, 3735 pass, 0 fail, 2 skipped, exit 0, zero `not ok` lines. No worktree-654 contamination this time (the review's earlier whole-suite run was contaminated; mine was not).",
    "Playwright walk on the assigned triple (CLARA_E2E_APP_ORIGIN=https://127.0.0.1:3260, NEXT 3261, RUNTIME 3262; all three ports confirmed free before the first run). RUN 1 (host also carrying the from-scratch chain + a next build): 7 passed, 1 failed — the VIEWER cell, `page.goto: net::ERR_ABORTED; maybe frame was detached?` at /login (no assertion reached). RUN 2 (that cell solo): 1 passed (11.7s). RUN 3 (all eight, quiet host): 8 passed (33.0s) — cells 1-8 by name, including 'an invalid answer names the dependent CONTROL…' (2.5s) and 'a VIEWER is refused by the door…' (2.4s); the axe cell took 6.2s.",
    "git -C C:/Users/zhant/Desktop/clara-wt/639 status --porcelain -> empty (I edited nothing); HEAD = 693f88998d51d653ca3560ff6a57e7b88dfa18a2; git diff aecfb34e..HEAD --stat -> 20 files, +794/-69; three fix-round commits 37f451a3 (db), ba637296 (web), 693f8899 (e2e)"
  ]
}