# #650 — fix round 1 (review verdicts: STANDARDS, SPEC, adversarial-migration-accounting-safety)

**Branch** `impl/650-client-home-work` · **worktree** `C:\Users\zhant\Desktop\clara-wt\650` · rig PG `127.0.0.1:55509` / `clara_650` · Playwright **3310/3311/3312**. Reviewed head `a948fb8b`; **one new commit** `fed8b566`. Every run below is **LOCAL**; hosted evidence pending.

```
fed8b566 fix(web,db,docs): #650 round-1 review — the recent-success drilldown says what it is dated by
```

## Finding → what I did → evidence

| Finding | What I did | Evidence |
|---|---|---|
| **650-B1** blocker — the recent-success tile and the list its count links to are dated by different instants | **Applied, with the disclosure rather than the de-scoping.** Reproduced it red on the ticket's own rig; kept the href's dates (see "deliberately left"); added the missing red-first DB cell; made the tile say what its list is dated by; corrected the header claim in `packages/db/README.md`; marked AC3's recent-success leg **partial by name** in `650-final.md`. | **RED**: new cell `p650.pack.recent_success_drilldown` asserting `tileIds == listIds` → `not ok`, `actual [2e33a202…, 8502f6bc…]` vs `expected [2e33a202…, c8f83a51…]` (`client-work-pack.test.mjs:341`). **GREEN** after re-stating it as the disclosed contract: `ok 5 … diverges in exactly two named ways`. The cell measures three fixtures on the rig: a Work admitted 30 MYT days ago and posted today (**in the tile, not in the list**), a Work admitted and completed today with no committed receipt (**in the list, not in the tile** — the population the door already names through `uncounted_completions`), and an ordinary same-day Work (**in both** — so the sets overlap; "in the ordinary case, disjoint" overstates it, and the correction is in the cell). UI: `client-work-attention.tsx` renders `recentSuccessListBasis` under the recent-success count only; `packages/db/README.md` gains the subject paragraph. |
| **650-S1** should — the only end-to-end proof of AC3 asserts the URL string against a hard-coded empty list | **Applied.** `EMPTY_WORK_PAGE` is gone; `home-board-walk.spec.ts` now answers `list_accounting_work` from `listWorkPage()`, which filters the **same fixture the pack mock is built from** through the door's own fences (`p_status` membership; half-open `[p_since, p_until)` over `created_at`). The fixture carries both divergence classes, and every drilldown leg asserts the population it lands on. | **RED demonstration**: with `listWorkPage` temporarily returning the old `{rows: []}`, `home.facets.drilldown` fails at `home-board-walk.spec.ts:405` — `expect(table().getByRole("link", { name: "Office rent" })).toBeVisible()` — `1 failed`, exit 1. **GREEN** with the fixture-driven handler: full spec `9 passed (37.1s)`, exit 0, first solo run. |
| **650-S2** should — 0199's header credits `ix_accounting_work_client` for the active scan and says both indexes were measured before the file was written | **Applied to `packages/db/README.md`; 0199 itself untouched.** Re-measured **myself**, as a bookkeeper with RLS in force on the ticket rig — I did not carry the reviewer's numbers across. | Cohort 1,317 `accounting_work` / 81 `operation_receipts`; probed client 102 active. ACTIVE → `Bitmap Heap Scan on accounting_work ← Bitmap Index Scan on uq_accounting_work_intent`, `Index Cond ((firm_id = clara.jwt_firm()) AND (client_id = …))`, Execution Time **0.451 ms**. RECENT SUCCESS → `Index Scan using ix_operation_receipts_client`, half-open MYT range in the Index Cond, **0.049 ms**; for a client that has committed receipts, `Bitmap Index Scan` on the same index, **0.083 ms**. No sequential scan either way, so the header's conclusion (no new index owed) stands; its attribution and its "measured before this file was written" clause are superseded in the README, which also records that the receipts cohort is small enough to make that leg structural rather than a load measurement. |
| **650-N1** note — the reviewer's from-scratch cluster `rig650r` (55609 / `clara_650r`) is still up | **Recorded, not acted on.** Dropping another agent's cluster is not mine to do, and the fix round needed no from-scratch chain (0199 is unchanged, so the prestate/tail it already passed still holds). | `pg_dropcluster --stop 17 rig650r` when the wave closes — the reviewer's own instruction, repeated here so it is not lost. |
| **650-N2** note — `stopping` Work falls through every facet | **Named residual in `650-final.md`** (the reviewer's first option). Widening the predicate is the second, and it is a brief change. **Ratification requested.** | Verified on my rig, not transcribed: the live CHECK is `status = ANY (ARRAY['queued','running','awaiting_input','stopping','completed','refused','failed','cancelled','expired'])`; the pack's predicates are `status in ('queued','running')` and the committed-receipt window. Including `stopping` in `active` **and** in `WORK_ATTENTION_ACTIVE_STATUSES` contradicts brief-650 line 8 and DECISIONS §2 #650 ("active = runnable/executing children"), so I did not do it. |
| **650-N3** note — the needs-you chip's active-client guard turns into a false negative on an onboarding/archived client | **Named residual in `650-final.md`.** Inherited from #629's shipped chip; the real fix belongs with whoever owns the review-queue splice. | Verified independently off `pg_proc` on rig 55509: the live `clara.list_review_queue(jsonb,jsonb,integer)` body joins `clara.clients wqc on wqc.id = wqw.client_id and wqc.status='active'` in its `work_question_rows` arm (line 215 of the live prosrc — it is a splice, so it is read off the catalog, not off 0180's text), while `clara.list_accounting_work` joins `clara.clients` with **no** status predicate (line 159 of its live body). The chip goes to 0 and the list does not. |
| **650-N4** note — a viewer is offered a link into a bookkeeper-floored list | **Applied, in the reviewer's cheaper form**: the link stays (a role can change, and hiding the destination hides the reason) and now carries `needsYouListFloor` when the pack came back `denied` — which is this board's only evidence that the caller is below the Work floor. | Two cells: "a DENIED pack makes the needs-you tile say that the list behind its number needs a bookkeeper role" (positive) and "a readable pack leaves the needs-you count unqualified" (control). Both green in `client-work-attention.test.tsx`. |
| **STANDARDS 1 / SPEC F1** note — `attempt` fakes ordinals with `=2`/`=3` and prints "21th run" from 21 on | **Applied.** `en.json` `ClientWorkAttention.attempt` is now `{n, selectordinal, one {#st run} two {#nd run} few {#rd run} other {#th run}}`. | New cell "the run ordinal is computed by the locale, not by three hand-written exceptions" over n ∈ {1,2,3,4,11,21,22,33}. **RED on the old key** (restored it temporarily): `error: 'attempts=21 must render "21st run"'`. **GREEN** on `selectordinal`. `check-message-keys` still passes (2,931 keys). |

## Counts

| Run | Result |
|---|---|
| `packages/db/tests/client-work-pack.test.mjs`, exact **29** preintegration gates, rig 55509/`clara_650` | **13 tests, 13 pass, 0 fail, 0 skipped**, exit 0 (was 12/12) |
| `apps/web`: `client-work-pack.test.ts` + `use-client-work-pack.test.ts` + `client-work-attention.test.tsx` + `client-workspace-overview.test.tsx` | **53 tests, 53 pass, 0 fail** (was 48) |
| Whole `apps/web` unit suite, `node scripts/run-tests.mjs` | **3,760 tests, 135 suites, 3,758 pass, 0 fail, 2 skipped**, 252 s, exit 0 (was 3,755/3,753 — exactly the five cells added here). The 2 skips are the live-provider auth cells. `thread-live-clarify.test.tsx` did not flake, so no isolation re-run was owed; #707 and #693 did not appear. |
| Playwright `home-board-walk` on 3310/3311/3312 | **9 passed, 0 failed**, 37.1 s, exit 0 — first solo run, no re-run needed |
| Playwright `home-board-walk --grep home.facets.drilldown`, **pre-fix empty-page mock** | **1 failed** at `:405`, exit 1 — the red demonstration for 650-S1 |
| `pnpm typecheck` (worktree root) | exit 0 (`apps/web` and `packages/runtime` both Done) |
| `pnpm lint` (worktree root) | exit 0 (all four workspaces incl. `check-test-manifest`, `check-message-keys` 2,931 keys, `check-token-contrast`) |
| `node scripts/check-frozen-workflows.mjs` | OK, 281 frozen files, 51 `use workflow` modules |
| `node packages/runtime/scripts/check-parts-parity.mjs` | OK (run though not applicable — `git diff --name-only origin/main...HEAD -- packages/runtime` is still empty) |

**Migration**: `packages/db/migrations/0199_client_work_pack.sql` is **byte-identical to the reviewed head** (`git diff a948fb8b..HEAD -- packages/db/migrations/` is empty), so no rollback and no re-apply were owed and none was performed.

## Files changed

`apps/web/components/firm/client-home/client-work-attention.tsx` (two disclosures) · `…/client-work-attention.test.tsx` (+5 cells) · `apps/web/messages/en.json` (`recentSuccessListBasis`, `needsYouListFloor`, `attempt` → `selectordinal`) · `apps/web/e2e/home-board-walk.spec.ts` (fixture-driven `list_accounting_work`, population assertions) · `packages/db/tests/client-work-pack.test.mjs` (+`p650.pack.recent_success_drilldown`, +`backdateWorkAdmission`, +`listWork`) · `packages/db/README.md` (both corrections). Outside the worktree and **not committed**: `reports/650-final.md` (AC3 row, db count, two residual rows, the re-measurement) and this file.

## What I deliberately left

1. **I did not drop `since`/`until` from the recent-success href** (650-B1's fix option 1). Brief §4 seam 12 binds that href to `status=completed` **plus** `since`/`until` via `businessDayStart`/`businessDayEnd`, and cell 12 in `client-work-pack.test.ts` is written to it — so option 1 contradicts the brief. It is also the weaker repair: a dateless `?status=completed` opens every completed Work this client has ever had, which is a superset so loose that "the number opens the list narrowed to that population" becomes less true, not more. The ticket's own idiom for a filter the list cannot express is the one I applied — name the closest expressible filter and **disclose** the rest, exactly as `retryNotFilterable` already does one line above.
2. **I did not edit `packages/db/migrations/0199_client_work_pack.sql`.** Migrations are append-only and the reviewer said so explicitly ("Do not edit 0199"); both corrected sentences live in `packages/db/README.md` instead. Consequence: 0199's header still reads "`ix_accounting_work_client` … serves the active scan" and "the browser rebuilds EXACTLY these instants … so the drilldown cannot mean a different week from the tile it came from". The second sentence remains **true of the week and false of the subject**, and the README says so in those words.
3. **I did not widen `active` to include `stopping`** — brief line 8 and DECISIONS §2 #650 define active as runnable/executing. **Ratification requested** if the orchestrator wants the fourth state carried.
4. **I did not touch `clara.list_review_queue`'s active-client guard** (650-N3): it is a 0016+0017+0180 splice owned elsewhere, and this wave recuts none of it.
5. **I did not drop the reviewer's `rig650r` cluster** (650-N1).
6. **A receipt-dated axis on `clara.list_accounting_work`** is the real fix for 650-B1 and is a **follow-up**, not this branch's: 0189 is recut by no ticket this wave (DECISIONS §1.3).

## Follow-up worth filing (new, from this round)

**Give `clara.list_accounting_work` a receipt-dated window.** The client home can count what POSTED in a period but can only hand the reader a list of what STARTED in it, because `accounting_work` carries no completion instant and the list door fences on `created_at`. Every future "what did we finish this week/month" surface will hit the same wall, and each will either disclose it (as #650 now does) or quietly imply something false. The shape is an optional `p_receipt_since`/`p_receipt_until` pair joining `clara.operation_receipts`, or a sibling door; either is a 0189 recut and therefore a ticket of its own.
