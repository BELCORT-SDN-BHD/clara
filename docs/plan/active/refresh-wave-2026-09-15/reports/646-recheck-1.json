{
  "ticket": 646,
  "head": "255559cb",
  "closed": [
    "STANDARDS F1 (blocker) — CLOSED. The census now SEES this lane: my `node -e` reproduction of the exact RPC_VERB_OPENER regex against apps/web/e2e/document-correction-mock.mjs returns 12 verbs (was 0), because the dispatch variable is now `verb` (document-correction-mock.mjs:323, 13 `verb === \"…\"` arms; the diff is a pure rename, every guard and body byte-unmoved). Running e2e-fixture-ownership.test.ts prints '72 distinct RPC verb(s) censused; 9 answered by more than one lane mock' and names all five of this lane's shares — get_document_state (3 claimants incl. work-list), get_document_extract, list_source_revisions, list_source_dependents, record_client_resolution — each declared in SHARED_RPC_VERBS (e2e-fixture-ownership.test.ts:973-1005 region) with matching claimant sets; 15/15 pass, INCLUDING test 15, the positive control that proves verbCollisions() still reds on an undeclared claimant. The three false comments are corrected (document-correction-mock.mjs:9-27, documents-viewer-mock.mjs:539-546, the declaration block itself), and the asymmetry with chat-parity-mock.mjs's unscoped arm is stated rather than glossed.",
    "SPEC F1 (should) — CLOSED. The axe scan on the OPEN Sheet exists at document-correction-walk.spec.ts:314-321, inside the 'C2 · AC6: the impact radius opens in a Sheet' cell and after the Sheet's visibility assertions, scoped `.include('[data-testid=\"correction-impact-sheet\"]')`. Non-vacuity is structural, not just asserted: that testid is on <SheetContent> (correction-impact-sheet.tsx:52) and neither components/ui/sheet.tsx nor that component uses forceMount, so Radix unmounts the node when closed and axe throws 'No elements found for include' — which is exactly the red the fix worker measured. That cell passed in my whole-file Playwright run. The overclaiming comment at correction-impact-sheet.test.tsx:9 now names the cell and the include scope instead of claiming a page-wide scan.",
    "SPEC F2 (should) — CLOSED. document-correction-walk.spec.ts:281 now asserts the successful-empty SENTENCE (`await expect(dependents).toContainText(\"Nothing recorded stands on this document's reading.\")`) immediately after the existing absence assertion at :280. The string is byte-equal to the catalog value messages/en.json:1923 `dependentsEmpty`, so it cannot pass against a differently-worded panel, and the cell (ORPHAN-QUESTION, spec line 263) passed in my walk run.",
    "ADVERSARIAL 646-A1 (should) — CLOSED, re-raced from scratch. I dropped and rebuilt the cluster (mkrig.sh rig646r 55605), created clara_646r and re-applied the whole chain with the AMENDED 0202: 'migrate: 194 new migration(s) applied · 194 total', #646 prestate notice 'clean' and tail notice 'OK' (so the recut's pinned pre-image sha and the 0197/0191 pins still hold on a chain built only from origin/main + this file), a second pass applied 0, seed applied 2. On that chain the INSTALLED clara.revise_document_fact body contains NO `obs.authoritative_extraction_id` at all — the ledger insert (:196), the audit payload (:202) and the _finish_op receipt (:214) all carry `obs.facts_extraction_id`, matching the envelope's `revises_extraction_id` (:165). Test-independent evidence from committed state after the batteries: `select … group by revision_kind, observed engine_kind, pointer engine_kind` gives 0 'fact' rows whose observed reading is not invoice_facts, and exactly one row in the reviewer's own scenario — document-wide pointer = doc_classify, observed reading = invoice_facts, same_row=false (revision 129946a8, observed 304ca5b4) — with the audit payload for that revision carrying the same 304ca5b4. The 'kind' writer is unmoved (5 kind rows still name the document-wide pointer as it stood, incl. 2 where it was still the invoice_facts row and 1 NULL for a never-extracted document). New cell p646.fact.observes_facts_reading is in the battery (EXPECTED_CELLS 15→16) and green on the fresh chain.",
    "ADVERSARIAL note (dependents work-join has no firm term) — CLOSED as applied. The installed clara.list_source_dependents on the fresh chain reads `join clara.accounting_work w on w.id = i.work_id and w.firm_id = c.firm`, beside the sibling arms' own `k.firm_id = c.firm` / `q.firm_id = c.firm`; p646.reads.scope and p646.dependents.projection green, neighbours 138/138. The suggested cross-firm Work fixture was not added and is recorded as a follow-up, not a blocker.",
    "ADVERSARIAL note (document.fact_revised invisible in client activity) — CLOSED as documented, not changed. packages/db/README.md's 0202 section now states the firm-feed-only consequence and why a per-filing event is #676's call; the note's own second option is what was taken.",
    "ADVERSARIAL note (band claims pending impact for never-posted documents) — CLOSED for this loop: recorded as 'contradicts the brief, not applied' with a sound reason (brief-646.md:64 specifies two persistent rows; AC4's wording is 'separately') and ratification requested. Orchestrator rules on it.",
    "ADVERSARIAL note (fresh cluster left running) — CLOSED, no action owed by the branch. I recreated rig646r myself for this re-check; it is online at 127.0.0.1:55605 with clara_646r at 194 migrations (max 0202_document_source_revision) and holds my probe data. Drop with `wsl -u root -- pg_dropcluster --stop 17 rig646r` when the wave closes. The ticket's own rig (55505/clara_646) was not touched by me.",
    "SPEC F3 (note, host contention) — CLOSED as re-measured, no code change. Both my runs are below; the failure set moved again and the failing cell is green alone."
  ],
  "open": [],
  "new_blockers": [],
  "ran": [
    "worktree /c/Users/zhant/Desktop/clara-wt/646, branch impl/646-document-correction, git status CLEAN, head 255559cb (3 fix commits on top of the reviewed 4ac11657; diff touches 9 files, +205/-45)",
    "apps/web: node --import ./test/bootstrap.mjs --import tsx --test e2e/e2e-fixture-ownership.test.ts -> 15 tests, 15 pass, 0 fail; census line '72 distinct RPC verb(s) censused; 9 answered by more than one lane mock' listing record_client_resolution, get_document_state (3), list_source_revisions, list_source_dependents, get_document_extract, list_review_queue, get_work_plan_origin, list_entry_links, list_spoken_for_documents",
    "apps/web: node -e reproduction of the exact RPC_VERB_OPENER regex against e2e/document-correction-mock.mjs -> 12 verbs matched (reviewer measured 0 at 4ac11657)",
    "PowerShell: wsl -u root -- bash .../refresh-wave-2026-09-15/mkrig.sh rig646r 55605 -> cluster dropped and recreated, online (this is a NEW cluster, not the reviewer's)",
    "node+pg: create database clara_646r on 55605 -> created; server PostgreSQL 17.11 (Ubuntu 17.11-1.pgdg26.04+2)",
    "worktree root, PGPORT=55605 PGDATABASE=clara_646r: pnpm db:migrate -> 'migrate: 194 new migration(s) applied · 194 total · target 127.0.0.1:55605/clara_646r' with #646 prestate 'clean' and tail 'OK' notices; re-run -> '0 new migration(s) applied · 194 total'; pnpm db:seed -> '2 seed file(s) applied'",
    "node+pg catalog read on clara_646r: pg_get_functiondef(clara.revise_document_fact) -> zero occurrences of obs.authoritative_extraction_id; ledger insert / audit / _finish_op all obs.facts_extraction_id. pg_get_functiondef(clara.list_source_dependents) -> 'join clara.accounting_work w on w.id = i.work_id and w.firm_id = c.firm'. schema_migrations -> {n:194, mx:'0202_document_source_revision'}",
    "packages/db on the FRESH chain (55605, all 29 --import gates from package.json, --test-concurrency=1): tests/rig-docs-source-revision.test.mjs -> 16 tests, 16 pass, 0 fail, 0 skip (was 15 at the reviewed head)",
    "packages/db on the fresh chain: the 9 neighbour files (rig-docs-correction, rig-docs-filings-provenance, dba-coding-lane-classification, document-capability-registry, document-fact-validation-belt, knowledge-records, work-question, work-question-reads, field-path-grammar) -> 138 tests, 138 pass, 0 fail",
    "packages/db on the fresh chain: operation-census + rig-isolation (no CLARA_RIG_ALLOW_RESET/ROLE_SWEEP) -> 31 tests, 30 pass, 0 fail, 1 skip (the destructive cell)",
    "node+pg independent committed-state probe on clara_646r: 'fact' rows whose observed reading is not invoice_facts -> 0; the classify-then-revise separated case -> 1 row (pointer doc_classify 2be2cd91, observed invoice_facts 304ca5b4, same_row=false), audit payload observed_extraction = 304ca5b4; 'kind' rows -> 5 doc_classify / 2 invoice_facts / 1 NULL, i.e. the document-wide pointer as it stood",
    "worktree root: pnpm typecheck -> exit 0",
    "worktree root: pnpm lint -> exit 0 (apps/web eslint + check-token-contrast + check-test-manifest + check-message-keys and both selftests; packages/db, runtime, reporting-render eslint Done)",
    "worktree root: node scripts/check-frozen-workflows.mjs -> exit 0, 281 frozen file(s), 51 'use workflow' modules registered",
    "worktree root: node packages/runtime/scripts/check-parts-parity.mjs -> exit 0, reader superset of emittable",
    "apps/web: node --import ./test/bootstrap.mjs --import tsx --test on the touched batteries (correction-impact-sheet, document-revision-dialog, documents-url-state, document-facts-table, documents-a11y, focus-ring-contract) -> 42 tests, 42 pass, 0 fail (focus-ring count unmoved)",
    "PLAYWRIGHT RUN A (ports 3270/3271/3272): pnpm --filter @clara/web e2e -- document-correction-walk.spec.ts -> 14 passed, 1 failed (6.2m). BOTH cells this round changed passed (9 ORPHAN-QUESTION, 10 the impact Sheet). The one failure is cell 14 'axe: each of the three routed views' — a 30s test timeout, not an assertion; its error-context page snapshot shows the app rendered normally. Host measured at 100% CPU with 50 node processes, 13.7GB free, from the other eleven lanes",
    "PLAYWRIGHT RUN B (isolation, same ports): the same cell ALONE, -g 'axe: each of the three routed views' -> 1 passed (17.2s) — green at little more than half the budget it blew in run A. Contention, not a defect: the failing set has now moved three times on this file (reviewer: cells 5/9/10/14 combined and cell 1 alone; fix worker: cells 1/2/3; me: cell 14, then green alone), and nothing was changed in response",
    "NOT re-run by me: the whole apps/web unit suite (3729 cells, ~10m). This round's web changes are two e2e files, one e2e-read gate (run, 15/15) and one comment in a unit test; the fix worker's own re-run was 3727 pass / 2 pre-existing skips, byte-identical to first delivery. Also not re-run: documents-viewer-walk.spec.ts (untouched by this branch; the fix worker measured it 22/0 alone)"
  ]
}