# Brief: #646 — routed document source correction (facts, kind, client) with honest downstream reassessment

## Orchestrator decisions (binding)

- **Migration number: `0202`** (`0202_document_source_revision.sql`), and only that number (DECISIONS §1.2). gap-646 proposes `0199`, which belongs to #650 (orchestrator ruling); a released number is a gap, never a renumber. One file, house header, prestate pins, tail assertions, preintegration gate module, `rig-meta.mjs` cohort rows.
- **D6 — the correction record is NOT an `accounting_work` row.** Identity + receipt live in a new `clara.document_fact_revisions` relation plus the existing `clara.filing_corrections` (`0007_document_pipeline.sql:310`). Do **not** widen `accounting_work.purpose` or `operation_receipts.purpose`; do **not** touch `_admit_accounting_work_core`, `_assert_journal_basis` (`0178:693-789`), `_record_journal_entry_core`, `ck_accounting_work_adjustment_basis` (`0194:244-247`), `_tf_accounting_work_immutable`, or the `0195:2165` arm (wave rule 1.3). gap-646's Horn B, and with it its slice items 2 and 3 (the `source_already_posted` opening across five sites), are **out of scope (orchestrator ruling)**. Posted-effect integration is **#676** — a named residual rendered with its link, never a silent drop.
- **Human fact revision = an appended declarative extraction**, engine_id `clara-fact-human:v1`, exactly the shape `set_document_kind` already uses for `clara-classify-human:v1` (`0169_set_document_kind_resolves_classification.sql:283-291`). Never `UPDATE clara.document_regions`. Use engine_kind `invoice_facts`, which the live CHECK `ck_document_extractions_engine_kind_0016` already admits (`0016_a21_compliance_watch.sql:181-182`) — **no CHECK widening, no new engine_kind**.
- **Orphan classification question** closes through a new narrow door `clara.dismiss_orphaned_classification_question`. Do **not** relax `clara._active_document_filing` (`0007:982`) — `resolve_open_question` / `dismiss_open_question` ride the same predicate (`0169:243-255`).
- **Views: `?document=<id>&tab=original|facts|accounting`** (DECISIONS §8). Route stays `/clients/[clientId]/documents`; no route segment; `?document=` deep links and `documents-viewer-walk.spec.ts` (#624's contract) stay green verbatim.
- **Dialog** for a bounded field/type change; **Sheet** for wrong-client impact context; inline **`ui/alert.tsx`** for refusals and persistent results **on the documents surface only, via `DoorFeedback`** (`door-feedback.tsx:20`). `components/common/state.tsx`'s `StateBanner` is **not** rebuilt — 138 referencing files and `tests/focus-ring-contract.test.ts:207` (DECISIONS §7).
- **Knowledge/Work reassessment is a read-only projection.** No stamp, no flag, no UPDATE on `clara.knowledge_records` — `_tf_knowledge_records_supersede_only` (`0192_client_knowledge_records.sql:521-566`, trigger `:598`) permits exactly one UPDATE shape. The engine is deferred to **#658/#663** (`docs/PRD.md:123`). Invalidate **only** the obsolete classification question.
- **Original client still sees the correction**: "transferred to another client on `<date>`, correction `<id>`" rendered from `get_document_state`'s `filings[].retired_at/correction_id` + `corrections[]` (`0191_document_capability_registry.sql:1197`, projection at `:1320-1332`). Bytes stay behind the existing no-existence-oracle fold (`0190_document_byte_door_v2.sql:216-226`) — unchanged.
- **No chat entrance.** #646 contributes **no tool to `chatTurn_v20`** and cuts **no successor** (wave rule 1). Its "successor contract" stanza is a negative contract plus one named dependency (below).
- **Knowledge catalog untouched** (rule 5): mint no `knowledge_keys`, no `knowledge_plan_item_map` rows. **Activity kind ladder untouched** (D13): record the named residual.
- **Shared files take one-line registrations only** (rule 6); logic lives in your own modules. New DB batteries call through `humanQuery` least-privileged personas, never `rootQuery` for the assertion under test (rule 10).
- **Playwright port triple**: `CLARA_E2E_APP_ORIGIN=https://127.0.0.1:3270`, `NEXT_PORT=3271`, `RUNTIME_PORT=3272`.
- **Rig row** (RIG.md): worktree `C:\Users\zhant\Desktop\clara-wt\646`, branch `impl/646-document-correction`, PG port `55505`, database `clara_646`, 193 migrations applied, PG 17.11.

## 1. Current state

**DB.** Kind revision has a real door: `clara.set_document_kind(uuid,text,text,text)` — bookkeeper floor, agent-identity wall, mandatory reason, op-key receipt, one `clara-classify-human:v1` extraction, narrow resolution of open `origin='classification'` questions under `FOR UPDATE`, retirement of kind-bound QUEUED tasks (`0169:152-317`). Client refile is mature: `preview_wrong_client_correction` (`0007:2438`) → `propose_wrong_client_correction` (`0125_f_a7_alpha2_judgement_recut.sql:450`) → `approve_wrong_client_correction` (`0125:490`; per-item reversing mirrors, `_subledger_on_approve` re-run `:645`, filing retired `:663`, `coding_tasks` + `notifications` `:668-679`). Provenance reads exist: `_assert_field_path` (`0191:554`), `get_document_state` (`0191:1197`), `get_document_for_human_read_v2` (`0190:176`). Facts are machine-only: `persist_invoice_facts` is granted to `clara_runtime` alone (`0009_coding_floor.sql:2916-2924`); the one human-reachable fact verb only re-runs the machine (`0026_lane_widen.sql:994`). The extraction/region writer `clara.persist_document_extraction(uuid,text,integer,jsonb,jsonb,text,text,text)` is spliced with `_assert_field_path` and pinned post-image `0230031f…` (`0191:659`); arithmetic validity is appended by DEFERRABLE constraint triggers (`0191:932`, `:1030`). `clara.filing_corrections` already carries maker/checker/status/plan_hash/books_version/timestamps (`0007:310-335`).

**Runtime.** No correction lane exists. `documentRoutes.ts:203` serves bytes only. `chatTurn_v19` adds two tools, neither about documents (`chatTurn.v19.tools.ts:5-9`); `claraWork_v3`'s roster is a three-key fixed literal (`claraWork.v3.tools.ts:4-13`). Both launchers are purpose-blind (`workRoutes.ts:490`; `reconciler-work.mjs:399-402`).

**Web.** One route, one parameter: `DOCUMENT_PARAM = "document"` (`lib/documents/url-state.ts:32`), push-to-open / Back-to-close (`url-state.ts:1-20`). `document-detail.tsx:122-185` stacks every panel in one column — no Tabs, no routed view. Kind change is an inline Select + reason Input + Button bar (`document-admin.tsx:67-100`); re-extraction and consent classification already use `DocumentsDoorDialog` (`:125-200`). `CorrectionWizard` renders the impact radius as a `<Table>` inside a `<Dialog>` and drops every attempted value on close (`correction-wizard.tsx:80`, `:115-145`). `DocumentFactsTable` is strictly read-only (`document-facts-table.tsx:70-150`). `ui/alert.tsx` has **zero importers** app-wide; `ui/sheet.tsx` and `ui/tabs.tsx` are production-installed. The published adjacent-view spelling is `?tab=` with a checked default (`registers-workbench.tsx:31-41`). Strings live under `"ClientDocuments"` (`messages/en.json:1626`).

**Tests.** `rig-docs-correction.test.mjs` (10 cells, real roles, `pg_blocking_pids` lock proof), `dba-coding-lane-classification.test.mjs:166-233`, `document-fact-validation-belt.test.mjs`, `knowledge-records.test.mjs`, #624's 22-cell `documents-viewer-walk.spec.ts`. **Nothing** drives fact revision, source-dependent reassessment, or the orphan question.

## 2. Gaps / rows

- **AC1** — **build**: the fact half has no human door, and no revision receipt records the source version the human was reading.
- **AC2** — **build**: no routed views (`url-state.ts:32` exports one parameter); attempted values are discarded (`correction-wizard.tsx:80`). The refusal half exists only for refile (`0125:529`, `:560`) — carry it, add stale-version for facts.
- **AC3** — **build the honest half**: classification invalidation exists and is already narrow (`0169:219-262`; `dba.5b` at `dba-coding-lane-classification.test.mjs:207`). Knowledge reassessment ships as a **read-only projection**; the automatic engine is **out of scope, authority `docs/PRD.md:123` → #658/#663** (orchestrator ruling). The "old question version" half has a preferred mechanism and a fallback — §4 cell 10.
- **AC4** — **partial-with-named-residual**: source-revision acceptance ships here; posted-effect integration is **out of scope, authority D6 + #676's own AC6** ("Consume accepted source-change/correction Work…"). The UI states both halves separately and links #676.
- **AC5** — **build**: unposted fact correction and retired-filing-with-orphan-question are new cells; reclassification and permitted/refused refile are **verify-only** (re-assert, do not rebuild); relevant-vs-unrelated extends `dba.5b`; lost-response recovery is proved on the new doors' op-key replay.
- **AC6** — **build**: routed views, Dialog, Sheet, Alert scoped to documents, addressable decisions.
- **AC7** — **build**: the existing walk is #624's *reading* surface; this ticket owns a correction walk.
- **AC8** — **build the DB half**; under D6 the journey is database/storage-only, so **no Workflow leg is owed** — state that explicitly rather than leaving it looking unmet. Hosted evidence: pending, never claimed.
- **UI-17** (page overlay) — **verify-only**, re-asserted under the new contract: the overlay keeps working inside the routed Original view and when a fact has been superseded.
- **H-22** (kind set leaves question open) — **verify-only regression to hold**: `0169`'s resolution survives the move into a Dialog (`dba.5b`, `dba.5c`).
- **ORPHAN-QUESTION** — **build** (the new narrow door); the dead end is named in the live body itself (`0169:236-255`).
- **C48.4** — **partial-with-named-residual**: the source half only. Close-correction semantics and close-readiness recalculation belong to **#676** (`0140_f_a4_pr_2a_prepayment_limb.sql:3747` records the named verb as NON-GOAL 10).
- **C88.16** — **unverified, discovery incomplete**: no "C-c"/F3 anchor resolves anywhere in the tree. Do not guess — record a reviewed no-identification with the two clue candidates (`0191:1197`; `docs/ARCHITECTURE.md` §7) and propose a follow-up.

## 3. Slice (one branch)

**Migration `0202_document_source_revision.sql`.**
*Prestate.* Reviewed **literal** pins, measured on YOUR migrated rig (`clara_646`) and written in as literals — `0196_firm_document_limits_preserving.sql:155-157`: "A literal, not a self-measurement." Pin (a) the **pre-image** `prosrc` sha of `clara.set_document_kind(uuid,text,text,text)` — there is no literal for it in the tree (0169 pinned the 0123 pre-image `611bc543…`, which is not the post-image), so **measure it from `pg_proc`**; (b) **non-regression** `clara._document_posting_entry(uuid,uuid)` = `8ba5e67f7bc92a809e5fbea04b635331c764a48263c1fef4e06f8efe67ebcfd0` (`0197_coding_lane_evidence_link.sql:221`, `:634`) — re-assert in prestate **and** tail; (c) **non-regression** `clara.persist_document_extraction(…)` = `0230031fe7d3f18332310fc19f39936b1408cb89ba597f77ce576017fea35905` (`0191:659`). Read `clara.control_witnesses` for every body you recut, the `0196:256-260` way.
*Tables.* `clara.document_fact_revisions` — `id, firm_id, client_id, document_id, revision_kind check in ('fact','kind'), field_path, prior_value jsonb, new_value jsonb, observed_extraction_id, observed_version_n, resulting_extraction_id, reason, recorded_by, recorded_at, op_key`. **Two kinds only, and no `correction_id` column.** Nothing in this branch ever writes a refile row: `revise_document_fact` writes `'fact'`, the `set_document_kind` recut writes `'kind'`, and 0125's wrong-client flow is not in the recut set (tail rule: no `create or replace` outside it) — a third CHECK value and its FK would be dead in every row this ticket writes. Wrong-client identity + receipt stay where they already live, `clara.filing_corrections` (`0007:310-335`); DECISIONS §2 #646 frames the two relations as parallel, not unified, so lineage is joined **read-side** (door 3), never denormalised into this table. FORCE RLS, composite `(client_id, firm_id)` FKs, zero app-role DML, append-only + no-truncate on the `operation_receipts` idiom (`0178:457-468`), human select policy on `firm_id = clara.jwt_firm()`.
*Write doors* (both: `_human_ctx` bookkeeper floor, agent-identity wall copied from `0169`, mandatory reason, `_reserve_op`/`_finish_op` receipt, typed `detail.reason`):
1. `clara.revise_document_fact(p_document, p_field_path, p_value jsonb, p_observed_version int, p_reason, p_op_key)` — appends ONE extraction (`clara-fact-human:v1`, engine_kind `invoice_facts`, `version_n = coalesce(max(version_n),0)+1` scoped to `(document, engine_id, engine_kind)`, `0169:283-291`) plus its regions, each through `clara._assert_field_path` (`0191:554`). Refusals: `stale_source_version` (CLR19) when the authoritative facts version has moved past `p_observed_version` — the attempted value is echoed back in `detail` so the UI can re-show it; out-of-grammar path CLR10; document not in this firm CLR11; live bank-statement binding; unsupported (format × kind) per the capability registry.
2. `clara.dismiss_orphaned_classification_question(p_question, p_reason, p_op_key)` — admits only `origin='classification' AND status='open' AND` **zero live filings** for that (document, client).
*Reads* (authorization only — the same bookkeeper floor and grant wall, **no `p_reason` parameter, no op-key, no receipt**: reason and idempotency are write-door properties, and no read in the tree carries them — `get_document_state` (`0191:1197`), `get_document_for_human_read_v2` (`0190:176`)):

3. `clara.list_source_revisions(p_document)` — the revision ledger **left-joined read-side to `clara.filing_corrections`** (`0007:310-335`) and to `get_document_state`'s retired filings (`0191:1320-1332`), so a wrong-client correction appears in one chronological lineage without any door ever writing a refile row.
4. `clara.list_source_dependents(p_document)` — **read-only** projection: `knowledge_records` standing on a superseded `source_extraction_id`/`source_region_id` (`0192:416-417`, `:495-497`), open `open_questions`, and open `agent_interruptions` whose Work names this document in `source_refs` (`0182_journal_work_evidence.sql:496-551`). Writes nothing.
**All four granted to `clara_authenticated` only** — zero `clara_runtime`/`clara_agent_ro`/PUBLIC, asserted in the tail the `0169:333-345` way.

*One recut, minimal.* `clara.set_document_kind` — **signature unchanged** (#633 mounts this control; a signature change breaks its call site). Body change only: stamp the observed authoritative extraction id + `version_n`, derived inside the transaction, onto the audit payload, the receipt and a `document_fact_revisions` row of kind `'kind'`. Re-assert SECURITY DEFINER + `clara_fn_owner` + `search_path` + exact ACL after the recut (`0169:100-104`, `:320-345`).
*Tail.* No `create or replace` outside the named set; both non-regression shas re-read and equal; **`accounting_work_purpose_check` and `operation_receipts_purpose_check` texts re-read and byte-identical** (this is Horn A's proof, and what keeps #646 out of the 0206→0208 spine); `open_questions`' CHECK still admits `origin='classification'` (`0169:121-129` idiom); grants exactly as declared. Ship `packages/db/tests/document-source-revision-preintegration-gate.mjs` and `DOCUMENT_SOURCE_REVISION_0202_*` cohort constants in `rig-meta.mjs` on the 0191/0192 shape (`rig-meta.mjs:1749-1791`).

**Runtime.** **No new module, no new route, no frozen edit, no successor.** Under D6 the journey never mints Work, so no purpose-blind launcher can hand a correction to frozen `claraWork_v3` — prove that (cell 8) rather than assert it. Being database/storage-only, the journey owes **no real-World e2e leg and no `.github/actions/db-live-gates/action.yml` edit**; say so in the report so the integration worker does not hunt for one. gap-646's `source-correction-basis.ts` is **out (orchestrator ruling: no chat entrance)**; note the cost that ruling avoids — once a frozen body imports such a module it is hash-locked forever (`check-frozen-workflows.mjs:452-454`, the trap still blocking #643's follow-up 1).

**Web.** `lib/documents/url-state.ts`: add `TAB_PARAM = "tab"` + a checked parser defaulting to `original`, mirroring `registers-workbench.tsx:31-41`; `?document=` semantics untouched. `document-detail.tsx:122-185` becomes three URL-driven `ui/tabs` views — **Original** (existing `DocumentPageOverlay` + byte read), **Facts** (`DocumentFactsTable` + a per-row Revise control), **Accounting** (`DocumentEntries` + `clara.entry_evidence_links` read **directly, never wrapped**, DECISIONS §7, + the correction band). Revise and kind-change both become `DocumentsDoorDialog`-shaped Dialogs (current value, field path, observed version, mandatory reason); the inline bar at `document-admin.tsx:67-100` goes. The wrong-client impact radius moves out of `correction-wizard.tsx:115-145` into `ui/sheet.tsx` on the `activity-event-sheet.tsx` pattern — Dialog holds the decision, Sheet the evidence context, both with Title, safe dismissal, focus restoration (appendix D rows 23/51). `DoorFeedback` renders `ui/alert.tsx` **on this surface only**. Correction band on every tab: two persistent rows, "source revision accepted" and "accounting impact pending (#676)" with the link. The original-client face renders the transferred-away sentence from `get_document_state`. Drafts survive tab switches and Back — `correction-wizard.tsx:80`'s `reset()` must not fire on a tab change. Cover every state: loading, successful-empty (no facts), no-results, partial/stale, invalid/saving, denied/failed, cancelled/recovery (op-key replay returns the original receipt); exact cents and dates; 320 px, 200 % zoom, keyboard and focus return, screen-reader names, reduced motion, stable URL + Back, preserved drafts. New strings into `messages/en.json` `"ClientDocuments"` (`:1626`), checked lookup tables only (`document-admin.tsx:26` idiom).

**Docs.** `packages/db/README.md` (0202 doors in the operation-contract census), `packages/db/tests/README.md` (new battery), `apps/web/README.md` (the three tab views). `CONTEXT.md` gains four terms in the house "term / Avoid" shape mirroring `CONTEXT.md:123-125`: **Source revision**, **Document filing**, **Wrong-client correction**, **Correction Work (deferred to #676)**. Never `docs/PRD.md` or `docs/ARCHITECTURE.md`; contradictions go under "blueprint drift".

**Successor contract stanza (required, and negative).** State: #646 contributes **no** `chatTurn_v20` tool and needs **no** `claraWork_v4` arm of its own; the integration worker must add neither. Name the one dependency: a future automatic reassessment keys on `claraWork_v4`'s `observedRevisions({knowledge_version})`, which **#654 owns**; #646 ships only the read-side projection such a consumer would query.

## 4. TDD seams (red first)

New battery `packages/db/tests/rig-docs-source-revision.test.mjs`, real chain, real role matrix, every assertion through `humanQuery` personas at the least privilege that should succeed (and one below it that must not).

1. **p646.fact.appends** — red: door absent. Green: one NEW `clara-fact-human:v1` extraction at `prior+1`; the prior extraction's regions byte-identical (proves no in-place UPDATE); one `document_fact_revisions` row with `observed_version_n`, actor, reason; one committed receipt.
2. **p646.fact.stale** — a revision quoting a superseded `p_observed_version` refuses CLR19 `stale_source_version`; nothing written; `detail` carries the attempted value back.
3. **p646.fact.grammar** — an out-of-grammar `field_path` refuses CLR10 from `_assert_field_path`; zero rows.
4. **p646.fact.validation_belt** — after the revision the highest-revision `document_fact_validations` row is **appended** and its terms are the NEW numbers (red if the belt measures the old extraction while the UI shows the new one).
5. **p646.kind.stamps_version** — the recut receipt/audit payload carries the observed extraction id + version; `dba.5b` (manual question survives) and `dba.5c` (filing stays in needs_you) still green unchanged.
6. **p646.orphan.dismiss** / **p646.orphan.narrow** — closes only at **zero live filings**; a question whose filing is merely "not the current one" still refuses (this cell goes red if the predicate drifts); `resolve_open_question` / `dismiss_open_question` unmoved.
7. **p646.replay.one_receipt** — op-key replay on both new doors returns the original receipt; concurrent duplicates under a barrier converge to one revision row.
8. **p646.horn_a.no_work** — after every door above: zero new `clara.accounting_work`, zero new `clara.agent_tasks`, no correction-purpose `operation_receipts`; both purpose CHECK texts re-read identical. This cell is the merge guard against the 0206→0208 spine.
9. **p646.dependents.projection** — knowledge records standing on the superseded extraction are **listed** and byte-unchanged (`state` unmoved, `superseded_by` still null); `_tf_knowledge_records_supersede_only` never fires.
10. **p646.question.version** — *the discriminating cell (preferred mechanism vs fallback)*. Answer an open work question whose Work names the revised document, using the pre-revision `question_version`. **Preferred:** it refuses (CLR13, the version / `basis_changed` arm at `0180_work_questions.sql:820`, `:836`) → pin that refusal and AC3's Work half is closed. **Fallback, only on a measured refusal-to-refuse:** paste the measurement verbatim, mark AC3's Work half **partial by name**, keep the projection and the classification invalidation, and file the follow-up. Never widen `clara.open_work_question`'s grant (`clara_runtime` + hook token, `0180:686`) to manufacture a version bump — that is a runtime-lane capability, not a human door's.
11. **p646.refile.original_client_view** — after `approve_wrong_client_correction`, the ORIGINAL client's persona still sees the retired filing + `correction_id` through `get_document_state`, while `get_document_for_human_read_v2` still folds to not-found (`0190:216-226`); in the same cell `list_source_revisions` shows that correction through the read-side join while `document_fact_revisions` holds **zero** rows for it (the proof that the two-kind CHECK is right).
12. **Neighbours, unmoved**: `rig-docs-correction.test.mjs`, `rig-docs-filings-provenance.test.mjs`, `dba-coding-lane-classification.test.mjs`, `document-capability-registry.test.mjs`, `document-fact-validation-belt.test.mjs`, `knowledge-records.test.mjs`, `work-question*.test.mjs`.

**Web unit.** `documents-url-state.test.tsx` (tab parse, checked default, malformed tab); `document-facts-table.test.tsx` (per-row Revise control; absent below the role floor); new `document-revision-dialog.test.tsx` (server field errors map to focusable controls; stale version re-renders the attempted value); governance a11y extended for the Sheet title + focus return; `tests/focus-ring-contract.test.ts` ring count **unmoved** — that is the proof Alert stayed scoped to documents.

**Playwright.** `apps/web/e2e/document-correction-walk.spec.ts` + `document-correction-mock.mjs` carry the whole AC7 ladder on this journey (C2 source correction; B3's Work-detail reading of the linked correction; C13's source/history/conflict face) across the three tabs, with axe per face, and `documents-viewer-walk.spec.ts` green verbatim.

## 5. Risks

1. **#633 overlap is the live merge risk** — it mounts #646's `set_document_kind` control in its list/receipt rows (a declared boundary crossing, DECISIONS §2 #633). Export the kind-change Dialog as a standalone component so #633 imports it rather than copying; do not restructure `document-state-panel.tsx` or `loaders.ts`.
2. **Pins transcribed instead of measured** — the `set_document_kind` pre-image has no literal in the tree; a transcribed one refuses on a correct database. Pins are **measured on the rig**, then written as reviewed literals.
3. **Message-pack growth flipping a timing cell elsewhere** (`643-final.md` records a ~3.5 % growth doing exactly this) — bisect by swapping `messages/en.json`, never by guessing.
4. **Alert leakage** beyond the documents surface moves `focus-ring-contract.test.ts:207` — if that count moves, the scoping is wrong; fix the scope, do not re-baseline.
5. **`apps/web/test/manifest.txt` is alphabetical plain-string** and the runner does not directory-scan: an unregistered test silently never runs.
6. **Windows pre-existing reds** #707 and #693 are not yours to fix.
7. **C88.16 stays unidentified** — record it as such; do not invent an anchor.

**Exact shared-file one-liners this ticket adds** (sorted position, no logic): `apps/web/test/manifest.txt` — one line per new web test between `:92` and `:101`; `apps/web/e2e/serve-built.mjs` — one import + one dispatch line for `document-correction-mock.mjs`, the `documents-viewer-mock.mjs` shape at `:34-37`; `apps/web/e2e/e2e-fixture-ownership.test.ts` — declare every RPC verb your mock answers, marking as **shared** any verb the documents-viewer lane also answers; `packages/db/tests/rig-meta.mjs` — the 0202 cohort constants; `packages/db/package.json` — one `--import ./tests/document-source-revision-preintegration-gate.mjs` in the gate chain; `apps/web/tests/firm-scope-db-pins.corpus.ts` — one entry at its sorted position with a ≥40-character reason if you add dynamic SQL (sequence this at merge prep, after the migration bytes are final); `apps/web/messages/en.json` — keys under `"ClientDocuments"`; `CONTEXT.md` — four terms. **No `tree.ts` change** (no new route or leaf) and **no `require-firm-scope.ts` change** (client-scoped surface). No new `needs-you.ts` row kind.

## 6. Effort and rig

**Effort: L.** gap-646 says XL, and was right under Horn B; D6 removes the purpose CHECK, the posting-core recut, the five-site `source_already_posted` opening and the whole Work model from this branch (orchestrator ruling). What is left is two doors plus two reads, one minimal governed recut, a three-view web refactor and four test seams.

**Rig:** worktree `C:\Users\zhant\Desktop\clara-wt\646`, branch `impl/646-document-correction`, PG `127.0.0.1:55505`/`clara_646`, port triple `3270/3271/3272`. Node 22 via the RIG.md PATH line on every call. Never set `CLARA_RIG_ALLOW_RESET=1` or `CLARA_RIG_ALLOW_ROLE_SWEEP=1`.

**Before the final report, run and report counts:** `pnpm typecheck` and `pnpm lint` (worktree root); the DB battery with the exact 28 `--import` gate flags copied verbatim from `packages/db/package.json`; the neighbour batteries; `packages/db/tests/operation-census.test.mjs` and `rig-isolation.test.mjs` (no reset flags — its destructive cell skips); the **whole** `apps/web` unit suite once (`node scripts/run-tests.mjs`, ~5 min) for the census suites that red on files you never touched; `document-correction-walk` and `documents-viewer-walk` under your port triple. Parts-parity and the frozen check are **not** applicable (no module reachable from a frozen body is touched) — say so rather than skipping silently. Hosted evidence: pending, not claimed.

**Named residuals the final report lists under "Follow-ups worth filing as issues"** (WORK-ORDER report shape, `WORK-ORDER.md:21`): posted-effect integration **#676** (AC4, C48.4); the automatic reassessment engine **#658/#663** (AC3, `docs/PRD.md:123`); the **D13 activity-kind-ladder misfiling** — untouched by decision, and easy to forget here because SYNTHESIS's shared-surface row for that ladder names only #625/#633/#647/#650, so name it explicitly; **C88.16** as a reviewed no-identification with its two clue candidates; and cell 10's fallback if it fires.

**Sibling deliveries to copy, by path:** `docs/plan/active/refresh-wave-2026-09-14/brief-640.md` and `brief-643.md` (brief shape, migration header / prestate pin / tail idiom, preintegration gate + `rig-meta.mjs` cohort, e2e mock + walk pairing); `packages/db/migrations/0193_accounting_plans.sql` and `0194_periodic_adjustments.sql` (FORCE RLS tables, op-key doors, typed refusals, tail assertions); `docs/plan/active/refresh-wave-2026-09-14/reports/640-review-closure.md` and `643-final.md` — what a reviewer later demanded: behavioural cells rather than `prosrc` assertions, re-measured evidence, exact pass/fail/skip counts, named residuals instead of quiet omissions.

## Verifier findings not applied

None — all three verifier findings (dead `client_refile` kind + `correction_id`; read doors inheriting the write-door reason/op-key preamble; the unoperationalised D13 residual) are applied above, and none contradicted DECISIONS.
