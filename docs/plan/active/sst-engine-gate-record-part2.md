# F-T1 SST engine — gate record, continued

**Split boundary:** `sst-engine-gate-record.md` reached the repo's 500-line ceiling; this file
holds only the 2026-08-23 owner-sitting dispositions for §6's four cards. Section numbering is
unchanged — this is `sst-engine-gate-record.md` §6, continued, never a new section series.

## 6 (continued) · Owner items — the 2026-08-23 sitting rulings

**Owner ruling 2026-08-23 (the sitting) — OQ-11, OQ-12, OQ-13 and OQ-14 are ALL RULED.** Each
item's text in `sst-engine-gate-record.md` §6 stands as written, original and unedited; these
are the dispositions, and each unblocks the PR named in its own row.

- **OQ-11 → RULED: recommendation (b), widened with a third arm.** Where `invoice.tax_breakdown`
  is a document-witnessed fact, the producer uses it directly — unchanged from (b). **Where it is
  absent, Clara MAY agentically derive the value fields**: the rate-bucket call (which
  First-Schedule item, which of 6%/8% vs 5%/10%) is her JUDGEMENT; the taxable base is
  deterministic arithmetic over WITNESSED numerals only, never a model-typed total. The admission
  gate is a MECHANICAL reconstruction identity — `base × rate = tax`, to the sen, checked against
  the document's own printed arithmetic — **never a model self-score** (laws 72/79; review law
  2). A row that passes lands `agent_derived`, carrying a receipt and the region evidence behind
  every numeral; a row that fails the identity refuses to `not_evaluable` and opens a question,
  exactly the fail-closed default already on file. **PR-6 unblocks.** Design mechanism recorded:
  `sst-engine-design-part2.md` §5.5.
- **OQ-12 → RULED: recommendation (a), with the fill mechanism specified.** The wall moves to **CN
  CREATION**: a service-tax-bearing `sales_credit_note` naming no originating invoice refuses at
  creation, on a new originating-invoice reference column; `apply_open_items` stays OPEN as the
  estate's named-pair path that satisfies reg 11(3)(j) (`unallocated_credit_forbidden` is NOT
  built). **Clara fills the reference by judgement, read from the CN document**; where the
  document is silent on which invoice it corrects, she opens a clarify card rather than guessing.
  **This RULING SUPERSEDES OQ-6** — both of OQ-6's options closed the compliant path, which is now
  moot. **PR-4's CN limb unblocks.** Design mechanism recorded: `sst-engine-design-part2.md` §5.5.
- **OQ-13 → RULED: recommendation (b), with the fill order specified.** Build the
  `sst_service_periods` sibling record — human-enterable, audited, append-with-supersede — keyed
  on the open item, sidestepping `open_items`' append-only wall without widening a live table's
  write surface. **Fill order: agent-first from document-witnessed performed dates; human
  fallback via an open question** where no document states the period. OQ-5's advisory estimate
  (option c) still stands on top of whichever fill produced the row. **PR-4's ADD COLUMN
  unblocks.** Design mechanism recorded: `sst-engine-design-part2.md` §5.5.
- **OQ-14 → RULED: F-T1 authors the `sst_threshold_schedule` ALTER.** Recorded as a dated
  amendment in `wave-f-contract.md` and a re-cut of `internet-lane-design.md`'s PR-3 row
  (`:431` area) to consume-not-author, both dated 2026-08-23. **PR-1's threshold limb re-enters**
  (Annex A.1's specification stands); F-A8/PR-3 drops the ALTER and consumes the table F-T1
  authors. The design-part2.md PR table (PR-1/PR-4/PR-6 rows) is trued to read RULED, not
  pending, at the same date.

**Consequence for §7's cross-item obligation 2:** `sst_threshold_schedule` no longer has two
claimants — F-T1 owns the ALTER outright, and the obligation is DISCHARGED by this ruling.
