# F-A7b gate record — the sitting's rulings (2026-08-27)

*The owner (Tao) sat on `fa7b-gate-questions.md` in the 磨合 session, 2026-08-27, one
question per turn with 大白话 briefings per the standing grill protocol. Every must-answer
item is ruled below. This record + the four gate docs (`fa7b-onboarding-survey.md` ·
`fa7b-onboarding-design.md` · `fa7b-gate-questions.md` · `fa7b-onboarding-annexes.md`)
constitute the closed gate; the design doc's NEEDS-DECISION labels resolve per this file.
The F-A7b train is now BUILD-AUTHORIZED per the design as amended here.*

## The five playbooks — APPROVED AS PROPOSED (Q1)

①-⑤ treatments ruled exactly as `fa7b-gate-questions.md` §1 proposes: ① gold path
document-tied on the deterministic parse, human-confirmed mapping · ② `management_account`
tie or keyed fallback, ALWAYS with the recorded unaudited acknowledgement · ③ bank-only =
NO seed, deferred activation + visible banner + chase list, the FY1 cost stated on screen
day one · ④ shoebox = as ③, and reconstruction is a NAMED SEPARATE ENGAGEMENT decision the
interview surfaces and stops at — never silently started · ⑤ mid-year = seed at the
predecessor's TB date, typed open question per gap period, the difference carried by name,
NEVER a plug (P-6's mid-year `p_as_of` prediction still owes its rig replay before ⑤
ships; fail-closed to ③ if it fails).

**Owner-ratified clarifications minuted with the same force:** (a) the HUMAN KEYED DOOR is
always open — an accountant may convert any insufficient-materials case into a keyed seed
by entering the complete, balanced opening themselves (`record_opening_keyed_resolution`
pins `method='human'`, per-line attribution; `approve_opening_seed` stays the admin's
reserved act) — the walls bind humans identically (no single-leg + plug, `obe_not_nil`);
(b) reverse-not-delete stands explained and re-confirmed — a mis-opened client is ARCHIVED,
never deleted (law 6).

## The design decisions

- **Q-D1 — RULED: ALL-PROPOSE.** Clara NEVER opens a client file unattended; every new
  client goes through the proposal card (the fail-closed default, chosen by the owner over
  the narrow hard-evidence auto-open). R8(a)'s "Clara proposes onboarding" is literally
  true. Widening to any auto-open arm is a future owner ruling, not a build decision.
- **Q-D2 — RULED: admin floor** on `accept_onboarding_proposal` (batch).
- **Q-D3 — RULED: seeding discipline** — the wake birth core seeds `contributors='{}'`,
  `review_maker`/`reviewed_at` NULL; no CoR of `commit_client_onboarding`; the loud-failure
  cell pair ships (design D-2) (batch).
- **Q-D4 — RULED: ride `firm_open_questions`**, `kind` extends with
  `'onboarding_proposed'`, both-direction census (batch).
- **Q-D5 — RULED: always ask** — `opening_position` is never derived; the normalizer only
  pre-fills the echo (batch).
- **Q-D6 — RULED: a close may NOT SEAL while the deferred-opening banner is up, and NO
  owner-override door ships.** Drawer-1 (absolute, no attestation path) is the mechanism.
  Accounting-correctness ruling under constraint 1, the owner's own.
- **Q-D7 — RULED: yes** — after the proposal is accepted and the client born, the held
  document auto-attributes unattended (law 79 judgement under the same B1-B9 walls);
  R8(a)'s "auto-attributes" stands literally (batch).
- **Q-D8 — RULED: the interview normalizer SHIPS in train 1**, on the `interactive_client`
  credential (one allowlist row, no new wake kind). Law 28's cross-model adversarial pass
  on the materials-into-model injection surface is MANDATORY before the train merges. The
  owner ruled with the full agentic walk-through on the record: the interview core's
  clarify/refuse-with-reason/echo-confirm loop and the mid-interview材料补交 path exist
  either way; Q-D8 only put Clara's eyes on the materials. Three human touchpoints remain
  by design: accept proposal (D1) · sign the consent doors · approve the opening (law 71).
- **Q-D9 — RULED: the FY1 basis-of-preparation note NAMES the opening's source and its
  unaudited/human-asserted status.** Schedules one note-template item in F-A5's report-spec
  territory; not built by this train.
- **Q-D10 — RULED (batch):** true the legal template's purpose table (`document_processing`
  is live, `0123:271-286`) and run the supplementary consent line for the three existing
  clients BEFORE F-A7b's acceptance walk.
- **Q-D11 — RULED (batch): map, no CHECK change** — ruled document-kind spellings map onto
  the live 20-value enum; UI shows the ruled label, stores the live kind.
- **Q-D12 — RULED (batch): do not extend `document_intakes.origin`** — the filing-less
  `documents` row remains the class marker.
- **Q-D13 — DISCHARGED before the sitting:** digest law 79's caveat was trued in PR #356
  on R1's ruling authority (`0125:184,209` cited); the rig-replay confirmation ships as a
  battery cell in the F-A7b train per the gate doc's own recommendation.

## What follows

The F-A7b train builds per `fa7b-onboarding-design.md` as amended here (NEEDS-VERB list
unchanged; D1's all-propose removes the unattended-open arm from `wake_propose_client_onboarding`'s
sibling scope). Sequencing per the 磨合 build plan (`mohe-grill-rulings-2026-08-27.md` Q9):
the train opens once P2's shell is underway; law-28's pass and the P-6 rig replay are its
named pre-merge obligations. Firm creation/tiers stay R8(b)'s separate gate; pricing
amounts stay R8(c)'s sitting.
