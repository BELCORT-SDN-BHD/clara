# docs/plan — index

Authored at the 2026-08-12 harness docs-tree refactor, when this directory split into
active/ (the wave currently in build) and `completed/` (every closed wave and slice, plus the
former REBUILD-PLAN.md's archived chronology). `research/` is untouched by this refactor and
keeps its own tree of per-wave cross-model research dossiers.

**The path-stability convention (made explicit 2026-08-18, at the ADR-0071 harness sweep):**
a document minted under active/ KEEPS its minted path when its wave closes — **status
lives in this index, never in the path** — because closed-wave paths are cited verbatim by
immutable artifacts (migrations `0064`/`0065`, hooks, test batteries cite
`docs/plan/active/wave-e-…` at file:line, and a migration's bytes can never be edited).
Documents authored AFTER a wave closes (ceremony as-runs, acceptance records) land in
`completed/` directly. The convention is FORWARD-ONLY from 2026-08-12: the wave contracts
and design docs already sitting in `completed/` were created at those paths by the harness
refactor itself (commit `1a66a61`), never renamed out of active/ — no history contradicts
this paragraph. This sentence is the reconciliation of PRD line 3's
active-then-completed wording with the citation-stability that the immutable estate
requires — the same authority split as the ζ squash-subject precedent (the directory is
the authority; frozen testimony goes stale honestly).

**Wave contracts of CLOSED waves are historical records of record: never re-grill them, cite
them.** If you're building against a closed wave's mechanism, read the file below for what was
ratified and why — don't re-open the question with the owner. Only active/ documents describe
work still open for negotiation.

Status key: **live** = current build target, changes expected · **historical** = closed, frozen,
cite-only · **superseded** = replaced/retired, kept only as a pointer to where its content went.

## active/ — Wave F's live build (ADR-0071 ruled 2026-08-18; live frontier — see `PROGRESS.md` Current posture)

| File | Status | Hook |
|---|---|---|
| `wave-f-contract.md` | **live** | **The Wave-F contract of record** (ADR-0071 execution): Track A the agentic core, F-A1..A9 (witness-pair extraction; agentic posting; bank, close, reporting, freeform-read, filing+interview, internet agency; metering — F-A1→F-A2 is the only hard chain, the rest fan out) ∥ Track B tax (F-T1..T4), plus **F-A10 — the retirement completion condition** (two architectures never enter Wave G). **AMENDED 2026-08-22 by the Track-A sitting (ADR-0074)**, in `[TA-2026-08-22]` blocks appended under each item so the delta stays readable: F-A3 gains the open-register verbs + the `bank_matching` purpose + the clock + the bank-rules retirement + the drawer-2 gate · F-A4 mints the calendar wake source and the minimal human close doors · **F-A5's "end to end" is rewritten open→evaluate→seal→render with ISSUE stated as a human act** · F-A6 gets the decided read surface + the DEFINER wrapper · F-A7 gets attribution-as-judgement (pending sign-off) + the narrow purpose + the promotion door · **F-A8 → F-T1 on the SST rate table** · F-A9 gets the one ledger + the brake census · F-A10's criterion widens with the one-architecture TEST. Sequencing gains four corrections incl. the single shared `finalize_close` D1 window. |
| `f-a1-witness-pair-design.md` | **historical** (delivered) | **The F-A1 design doc of record** (v3.1, 2026-08-18 night): the witness-pair mechanics under C1-C4 (cite-and-verify snap, atomic idempotent two-kind persist, `evaluate_witness_fact_state_v1` in the central evaluator freeze, resolver dispatch sparing ~30 call sites, the five-list egress wiring + the `witness_extraction` typed purpose, meter-never-cap), the D1-D12 decision register, and the PR-0..PR-4+3a build sequence. **§5 RULED in-session** (OQ-1 OpenAI-direct · OQ-2 ratified · cutover direct-release, dissent on file); **PR-0 re-shaped by owner ruling to a third NATIVE adversarial lane — RUN same night** (MERGEABLE-WITH-CONDITIONS; 3 blockers + 15 material folded, incl. B3's self-referential-withdrawal restatement and the §3.9 five-note 0017 cluster); Codex re-enters at future builds. |
| `f-a1-annexes.md` | **historical** (delivered) | F-A1's companion of record (split under the 500-line harness limit): Annex A the estate-as-built survey (data plane · verdict machinery · the twelve walls · runtime plane · reader lessons), Annex B the PR-0 finding-by-finding adjudication register, Annex C the §7 test-battery sketch. Wall and §7-cell references in the design doc resolve here. |
| `f-a2-agentic-posting-design.md` | **live** | **The F-A2 design doc of record** (v6.1, 2026-08-22 — PR-0 gate conditions folded; **owner rulings D34-D37** (chat parity in-train; OQ-2/3/5 RULED); **PR-1 build trues D38-D43** incl. the resolved B8 `facts_moved` rung; gate record below): the one unattended coder that replaces draft-only `autoDraft` across every document class (`journal_entry` generic included, superseding 7A-R7/ADR-063) — the `wake_post_entry` wrapper over an ungranted core over the SHARED approve core, the **four-tier gate ladder** (A raise — now incl. the three B9 locks · B the thirteen-rung all-evaluated receipt vector incl. **B15 `generic_on_directional_document`** · C `(errcode, reason)` pair conversion · D the deferred belts), acting identity on `entry_post_receipts`, N1/T3's receipt-keyed pin, the breeding excision (the 8th `_approve_entry_core` body), the pack's recomputed patterns block, the `posted` outcome chain (its own PR-1 file), and the trued train — **B12/B13 CUT on correctness grounds (GM-3); chat parity IN-TRAIN carrying GB-3's corrections (owner-ruled 2026-08-22, D34)**; PR-1 is THREE files in ONE D1 window. **§4 RULED:** OQ-1 (any amount, no thresholds — the agent's contrary recommendation on file as dissent) · **OQ-4 the three-exits shape** · **OQ-6 no category gate on the agent lane, with the human lane's gate STANDING** (ADR-0072). **OQ-2/3/5 RULED 2026-08-22 (D35-D37).** |
| `f-a2-annexes-1-estate.md` | **live** | F-A2 annex 1 of 3 (the 500-line split): **Annex A** the estate as-found and the seven findings at the bytes · **Annex B** the retirement checklist — artifact dispositions, the closed-world rosters and the `appliedStem`-gated cohort, CI/lint/partition obligations, the two missed censuses, the WB-R2 assertion sites, the 0040 marker dispositions, PR-1's three files + the explicit TEN-body D1 list, **the R-L1 claim-split**, and **Annex I (B4's three formulas — relocated here at v6.1)**. |
| `f-a2-annexes-2-mechanics.md` | **live** | F-A2 annex 2 of 3: **Annex C** the test battery (C.1-C.16, contract-blind cells marked) · **Annex D** the tier census predicted from `pg_trigger` for rig replay, the `interactive_client` decision record with its reader census, T3's mechanism, OQ-4's re-derivation trigger, the context-pack splice, and every wake-kind-keyed wall with a disposition. |
| `f-a2-annexes-3-record.md` | **live** | F-A2 annex 3 of 3: **E** the closed refusal vocabulary + `entry_post_receipts`' column list · **F** the `posted` outcome chain (five layers + six sites) · **G** the change log, incl. F33 (OQ-6's supplementary ruling) and **F34-F50, the PR-0 gate folds** · **H** the D1-D43 decision register (H.2 closed) · *(Annex I now lives in annexes-1.)* |
| `f-a2-pr0-gate-record.md` | **live** | **The F-A2 PR-0 gate record (2026-08-22)** — both legs run (8-lens independent judgement-logic review, every finding adversarially verified + the Codex cross-model pass, re-verified at the bytes): **3 blockers** (GB-1 the generic-on-directional hole · GB-2 the pre-stamp counterparty raise that kills the sales lane · GB-3 the unbuildable `interactive_client` limb) + **11 materials** (headline GM-1: B4-sales derived against a body superseded at `0022` — four independent confirmations) + nits, **the S1 seam and T3's pin holding every attack**, the **width ruling with its 2026-08-22 owner amendment** (B12/B13 cut · PR-1 three files/one window · chat parity back IN-TRAIN by owner ruling D34, severance dissent on file), the refuted-findings register, and §7's rig-replay obligations. **It is v5's fold spec** — every finding names its fold target. |
| `f-a2-statement-activation-spec.md` | **live** | **The F-A2 Window-B spec of record** — the bank-statement witness ACTIVATION, i.e. the bound follow-up F-A1 PR-4 shipped UNPOINTED: the router re-key (`bank_statement` arm's engine identity → the witness snapshot, `v_lane` UNMOVED per 0098's LANE DECISION) + the statement typed-consent re-key to `witness_extraction`, and the `statementFacts_v2` registry repoint. Carries §2 the migration + its whole-body prestate pin, **§5 the one-window ceremony recipe** (the machine stays STOPPED across BOTH the apply and the repoint deploy — the one gap with no DB-side guard), **§6 the per-client consent COVERAGE set difference** (a read that can say NO; a global count cannot), §7 the LANE DECISION and the precise scope of the coin-flip heal (routing-population effect, NOT an ancestor repair), §8 the battery + positive-read list, and §9's five adjudications. Built as `0102_f_a2_statement_activation.sql`. |
| `f-a2-annexes-4-build.md` | **live** | **F-A2 annex 4 of 4 — Annex J, the Track-A sitting's consequences for F-A2** (2026-08-22), held outside the design set because that set is at its line ceiling and under separate review. **J.1** the RECORD that 7A-R3 and E-R13's registered lift are dissolved with the rules machine (ruled BEFORE PR-1 merges, per ADR-0072①'s ban on a law lapsing by inertia; successor wall = the witness pair's nil-tax arm + the SST-registrant lock; RS's 22 no-tax invoices start posting unattended when F-A2 lands) · **J.2** TA-P12's REMOVE of the unattended `refused_budget` gate, the chat cap hotfix ahead of F-A9, and the **mandatory receipt-string rename** so an engine-protection refusal stops printing "insufficient budget" · **J.3** three neighbour rulings F-A2 must know (the ONE metering ledger it must record through; the shared `finalize_close` D1 window with task #17 and TA-P6; F-A3's wake-sibling verbs copying PR-1's idiom) · **J.4 R-OWNER — RULED 2026-08-22 (owner: option C, INTO PR-1)**: B15's second door, where a STATED but untestable supplier registration resolved `none` so the generic arm passed and GB-1's phantom-payment shape landed through D18, is closed **at the root** — `_document_direction`'s testability recut so **ONE held hard id suffices**, a stated id of an unrecorded kind yielding a new **`untestable`** evidence class, on **PR-1's D1 list with a `prosrc`-SHA pin**; the orchestrator's ledgered rider makes **B15 also refuse `generic_registration_untestable`**, leaving **D18 for direction-SILENT documents only**. The section names PR-1's four build consequences (D1 pin · only-widening closed world · the new refusal token · a both-polarities battery cell). |
| `bank-agency-survey.md` | **live** | **F-A3** · v2 · gated 2026-08-22 — the bank-agency ESTATE SURVEY (as-found), written to the F-A2 discipline: every claim carries a `file:line`, every unsettleable claim is carried as a PREDICTION for rig replay, and a body's live tip is found by lineage walk, never by name. |
| `bank-agency-design.md` | **live** | **F-A3** · **the design doc of record** · v2 · gated 2026-08-22 (record: `bank-agency-gate-record.md`) — the open-register bank verbs, the `bank_matching` egress purpose, the clock consumer, **F-A10's retirement clause for the BANK rules machine**, and **TA-P14's drawer-2 gate repair by measurement origin**. Width severed into PR-1a pure extraction · PR-1b agent limb · PR-1c egress · the clock PR (sequenced AFTER F-A4 mints the wake kind, R-L7). |
| `bank-agency-annexes-1-mechanics.md` | **live** | **F-A3** annexes 1 of 4 · v2 — **A** the verbs and their columns · **B** the full ladder, its tokens and its declared non-members · **C** locks and concurrency · **D** the clock. |
| `bank-agency-annexes-2-record.md` | **live** | **F-A3** annexes 2 of 4 · v2 — **H** the battery · **I** the retirement checklist · **K** the decision register · **M** the human doors and **N** the change log. |
| `bank-agency-annexes-3-build.md` | **live** | **F-A3** annexes 3 of 4 · v2 — **O** the build sequence, the two gates and the width ruling · **J** the D1 write-quiesce list per PR · **L** the predictions the rig replay must confirm or correct · **P** the owner questions and escalations · **Q** the registered risks. |
| `bank-agency-annexes-4-surfaces.md` | **live** | **F-A3** annexes 4 of 4 · v2 — **E** the `bank_matching` egress purpose (§3.7) · **F** the drawer-2 gate repair (§3.11) · **G** the read surface (§3.8). |
| `bank-agency-gate-record.md` | **live** | **F-A3 PR-0 gate record (2026-08-22)** — two lenses (BYTES: every estate claim re-derived at its cited `file:line`, the CoR lineage re-walked for every body the design plans to cut, the D1 list derived independently; RULINGS: fidelity to ADR-0074). **5 blockers / 6 materials**, headline: three live money bodies the design had declared CoR-free, and **the clock's execution path does not exist** (a `kind='wake'` task is born held and held→cancelled is the only legal move) — the cross-item finding that minted **gate G1**, RULED 2026-08-25, design of record `g1-wake-engine-design.md`. |
| `g1-wake-engine-survey.md` | **live** | **Gate G1 (cross-item: F-A3 · F-A4 · F-A5 · F-A7)** · the wake-execution-engine ESTATE SURVEY (as-found at frontier `0127`) — the stranded-row defect at the bytes, the proven spine-consumer idiom (`autodraft.mjs`) to mirror, the four named sources' half-shipped states, and the close_prep/TA-P11 tension the ruling overrides (§6, the honest record of the overruled mechanism (a)). |
| `g1-wake-engine-design.md` | **live** | **Gate G1** · **the design doc of record** · RULED 2026-08-25 (owner: mechanism (b) — one engine, the existing `kind='wake'` held projection gains its ONE consumer plus a settlement path). The engine's shape (a fourth registered spine consumer, two closed carrier shapes), credential flow (per-fire, unchanged `mint_wake_credential`/`assert_wake_allowed` seams), failure isolation (the estate's own dead-letter + `belt()` + DB-owned-cooldown primitives, composed, never a new breaker), the exact `_tf_agent_task_update`/`wakes_outbox` matrix delta, the rollout (bank_agent first, close_prep registered-but-disabled, filing/F-A5 register when their own runtime trains land), and the battery. |
| `g1-wake-engine-annexes.md` | **live** | **Gate G1** annex — **A** `clara.wake_engine_sources` + its owner-floor writer · **B** the exact trigger/CHECK deltas (full replacement bodies) · **C** the consumer's pseudocode against the real primitives · **D** the battery in full, incl. the single-engine blast-radius and per-source isolation drills · **E** the `clara_wake_bank_login` nine-step pool-wiring recipe (closes `bank-agency-annexes-3-build.md:58`'s M4 seam) · **F** the receipt-table proliferation, named non-blocking. |
| `close-key-1-survey.md` | **live** | **F-A4** · the close-key-① ESTATE SURVEY (as-found), findings **F1-F14**, censuses **C1-C16**, predictions **P1-P16**, read against TA-P1/P2/P4/P5/P6/P14. **States what IS, at the bytes; designs nothing.** |
| `close-key-1-design.md` | **live** | **F-A4** · **the design doc of record** · v2 · gated 2026-08-22 (record: `close-key-1-gate-record.md`), carrying **TA-P14's closing criterion**. **Mints the `close_prep` `agent_tasks` kind on the autodraft precedent** (its own local gate G4/D-27) — **and owns task #17 Fix A at PR-1b** (R-L9/GM-7, D-23). D-26 keeps the live ADJ oracle's admission and extracts an additive ungranted `_adjustment_run_due_core` beneath it (R-L11). **SUPERSEDED for F-A3/F-A5: gate G1 (2026-08-25) rules those two ride `kind='wake'` through `g1-wake-engine-design.md`'s engine, NOT this item's `close_prep` arm — close_prep itself stays as shipped, grandfathered, folded into the SAME engine as a second registered carrier shape (`g1-wake-engine-survey.md §6`).** |
| `close-key-1-annexes-1-mechanics.md` | **live** | **F-A4** annexes 1 of 2 · v2 — **A** the ladder and the decision tables in mechanism · **B** the two new evaluators · **C** the wake-kind census with a disposition per site · **D** the test battery. |
| `close-key-1-annexes-2-record.md` | **live** | **F-A4** annexes 2 of 2 · v2 — **E** the verb signatures, the closed refusal vocabulary and the new table shapes · **F** the numbered D1 list, **now TWO windows** · **G** the decision register and the owner-question grounds · **H** the change log · **I** the human-door spec. **Annex E.3's receipt-writer line carries the R-L16 re-wording.** |
| `close-key-1-gate-record.md` | **live** | **F-A4 PR-0 gate record (2026-08-22)** — two fresh-context lenses, both read-only on code, every cite resolved mechanically and printed back plus a full lineage walk. **3 blockers / 10 materials**, headline: rung B13's `adjustment_run_due` refuses a wake session so `wake_begin_close` would ABORT CLR03 while §7/D-14 forbade the only fix; two wrappers delegating "unchanged" into `_human_ctx`-opening bodies; **task #17 Fix A claimed by BOTH F-A4 and Track B**. **OQ-7/OQ-8/OQ-9 ruled 2026-08-22 by R-L12/R-L13/R-L11** (annotated in place). |
| `fa4-pr1c-codex-review-2026-08-27.md` | **live** | **F-A4 PR-1c** · the **Codex `gpt-5.6-sol` read-only adversarial review** of the close agent limb at frozen tip `a035c58`, one half of the double review the fix order synthesizes. Carries HIGH-1 (the law-71 reading over the five preparation wrappers — **later WITHDRAWN by the owner's ruling**, recorded in the fix order) and HIGH-2 (the receipt read-back that selected `verdict` and never compared it — confirmed and broadened by the native lane as F1, and fixed as FIX-1). Re-landed on its own train after the handoff PR it was first committed to carried only the handoff. |
| `fa4-pr1c-fix-order-2026-08-27.md` | **live** | **F-A4 PR-1c** · the conductor's **double-review synthesis and the fix order the build lane executed** — the native fresh-context lane's verdict (§Native) folded with the Codex pass above, both FIX-REQUIRED, into **FIX-1..12** with a verification protocol. Records the **owner's 2026-08-27 ruling on HIGH-1**: the gated design's reading stands, the reserved human acts are exactly the four professional-signature acts, and the five preparation wrappers' grants, allowlist rows and walls-census tests are correct as written. FIX-1's chosen form (fail-closed identity guard **plus** `(verdict, rung_digest)` in the receipt unique key) and FIX-2's three-way seam-census differential are the two items a later reader is most likely to need. |
| `fa4-pr2-design-2026-08-27.md` | **live** | **F-A4 PR-2a (the DB train; the runtime train is PR-2b)** · **the design doc of record** — wrapper 12's UNPARK plus the close-limb debt batch. Under evening ruling **R6** the unpark **shrinks to ONE core extraction** (`propose_adjustment_template`): signing stays a human ADMIN act, so `sign_adjustment_template` is untouched and an extracted sign core would be a permanent dead member. Carries the verb census at the live bodies, the new DB-owned term carrier `clara.document_service_periods` (the park's second blocker, re-measured: the OCR route needs a **third** live-body recut and is severed to a named follow-up), the evaluator with its deliberately single-member freeze closure, residual 1's rank-conjunct mirror with its consumer census, MED-8's supersede-churn guard, the migration's section plan, **the D1 note (one live body; the second declared and provably idle behind a disabled wake source)**, acceptance, an explicit NON-GOALS list, and five surfaced collisions, **all RESOLVED in §13** — plus the two CRITICAL input-contract findings the second review raised, **OWNER-RULED 2026-08-27**: **F1 extends the CARRIER** (whole-calendar-month straight-line, remainder in the final period, no pro-rata in v1) and **F2 lets Clara JUDGE the expense account** under three walls, on the maxim ***facts get anchored, judgements get receipted*** — the naming split (close-key-1-design.md:477 trued in the same PR), the human-only service-period door CONFIRMED AS LAW on hard-constraint-2 grounds, the FY-crossing rule confirmed as a SELF-HEALABLE refusal the clocked lane can clear itself, PROGRESS.md's truing handed back to the conductor, and MED-8's latitude carried to the design review as a named adversarial lens. **The R6 scope cut — one core extraction, not two — is conductor-ratified; a build lane must not restore the sign extraction.** Third round (N1-N10) adds the **congruence constraint validated AT PROPOSE** that earns the four-body D1 inventory (without it it is six), restates F2's wall 3 as **decline-and-re-propose** with `_adj_template_json` brought into scope for the visible half, and corrects this verb's ordinal to **wrapper 12** (0138 says 12 twice; §3.1 carries no ordinal at all). |
| `fa4-pr2-battery-2026-08-27.md` | **live** | **F-A4 PR-2a · Annex A — THE BATTERY.** Forty-five walls, each with its own cell AND its own mutant (the ten mutant-less rows closed at the 2026-08-27 review, F7; W29, the flipped park wall, got a real mutant with no exception), plus the fixtures that do not exist yet and the **armed-skip statement** naming the estate's four vacuity layers. Sharp ones: the receipt-honesty-under-multiplicity pair (W14/W32) whose mutants reproduce the shipped FIX-1 defect on two verbs · the F1 null-stability pair (W36/W37) that must be green **before** the ceremony window opens · twin-equivalence (W34) and the end-to-end books wall (W35, the prepaid account to exactly zero). |
| `fa4-pr2-derivations-2026-08-27.md` | **live** | **F-A4 PR-2a · annexes D-H, the DERIVATIONS.** **D** MED-8 recut to the ruled reading — strict superset over the `(check_key, item_key)` PAIR set, why the non-empty-new-pairs arm was killed (a rotation over overlapping subsets burns live proposals), and what each arm owes `settle_reason` (arm (1) CAN drop coverage, so it must name the dropped pairs) · **E** the receipt subject and op key, and the one recut deliberately not taken · **F** the five pre-rungs, with **F.1 recut to record the DISSOLUTION** of the P-align/P-carry fork (monthly is calendar-month: 0045:1881-1884 → 0041:1016-1018) and the honest courtesy-not-a-wall ceiling · **G** the mint-snapshot receipt collision · **H** F1's carrier surgery, including **H.3's six-reader congruence census** — the census that turns the four-body D1 claim from an assertion into a consequence. |
| `fa4-pr2-annexes-2026-08-27.md` | **live** | **F-A4 PR-2a** annexes **B-C** — **B** the measurements and debt-batch mechanics (the term-carrier blocker re-measured · the residual-1 consumer census, now complete with the four rig readers the first cut missed · the index/policy census strengthening · why two of the fix order's "comment truings" cannot land where it says, applied migrations being immutable) · **C** the named OCR follow-up. (Annexes **D-H** moved to `fa4-pr2-derivations-2026-08-27.md`.) |
| `fa4-pr2-design-part2-2026-08-27.md` | **live** | **F-A4 PR-2a** design, part 2 — the build envelope, split at the estate's design + part-2 convention when the 2026-08-27 review fold pushed part 1 past the line gate. **Section numbers run CONTINUOUSLY across the pair**: §9 the migration's section plan and **the D1 ceremony note** (ONE genuinely live body; two more declared and provably idle behind the `close_prep` wake source at `enabled = false`, read positively in the prestate) · §10 the battery pointer · §11 the twelve NON-GOALS, including that F1/F2 are NOT designed around · §12 the named OCR follow-up · **§13 the conductor's resolutions on all five surfaced collisions** plus the ratified R6 scope cut · §14 acceptance. |
| `fa4-pr2c-close-chat-design.md` | **live** | **F-A4 PR-2c (FS-7 echelon 1.5, 裁-99/裁-100)** · **the design doc of record** — the ATTENDED close-prep CHAT lane: a new sibling minter `mint_chat_close_credential` under the shared `interactive_client` kind (mandatory `on_behalf_of`, task-bound to a live `chat_turn`), and rung **A8** (`_assert_attended_close_floor`, CoR'd into `_close_wake_ctx` between the allowlist wall and the client pin) so an attended session can never exceed what its directing human could do directly. **Three owner corrections, all recorded in place**: (1) the deploy-shape premise ("no closePrep.v1 shipped") was stale — the workflow merged in #437 the day before authoring; the safety conclusion (zero live callers, no D1 window) holds on the STRONGER, correctly-cited mechanism (`wake_engine_sources.close_prep.enabled=false`, checked as a live apply-time tripwire, never a dated snapshot); (2) §4 cell 4's "plain interactive" arm and cell 6's "foreign fiscal year" arm were rewritten after a full rung-order audit (**Amendment #3**) found both unreachable as first drafted — the amendment records the MEASURED W-sequence/M-sequence tables as the permanent audit surface, two normative battery construction constraints, and the twelfth verb's floor ruling (`wake_run_depreciation_catchup` takes `wake_propose_close`'s bucket, 裁-100② extended); (3) `wake_mint_month_snapshot`'s floor (viewer/no-gap, always correct in the design text) is stated as the exact case-arm grouping so a build cannot re-bundle it with the bookkeeper-floor verbs by accident. PR A (DB only) ships the migration, the nine-cell battery + five-mutant panel, the additive PR-1c census arm, and three roster edits (`rig-meta.mjs`, `x42-s5-helpers.mjs`, and the previously-unlisted shared `wake-allowlist-roster.mjs` closed-world census, all stem-gated). PR B (runtime + web, stacked) is not yet opened. |
| `reporting-agency-survey.md` | **live** | **F-A5** · the reporting-agency ESTATE SURVEY · v2 — S2 re-cut (`evaluate_metric_v1` is itself JWT-bound) and three cites re-pointed. **Its banner carries the reason the set was staged as `fa5-agency-*` and renamed at landing.** |
| `reporting-agency-design.md` | **live** | **F-A5** · **the design doc of record** · v2 · gated 2026-08-22 (record: `reporting-agency-gate-record.md`) — **"end to end" rewritten open→evaluate→seal→render with ISSUE stated as a HUMAN act** (TA-P14 clause 5); the issue wall re-aimed at the DIRECTING human with a solo arm; `agent_prepared` receipts; the evaluate leg re-based on η's own `_metric_eval_node_v1` idiom after the gate found the frozen entrypoint had no lawful OBO middle. **The sandbox EXPORT path is severed to lane F-A5b (R-L15); this design keeps the sealed lane's `artifact_watermark` trio.** |
| `reporting-agency-annexes-1-mechanics.md` | **live** | **F-A5** annexes 1 of 2 · v2 — **A** the verb table, the closed refusal vocabulary, the receipt shape and the human-act doors · **B** the battery · **C** the censuses and roster pins F-A5 re-cuts extend-never-weaken. **C.3/C.4's receipt-writer line carries the R-L16 re-wording.** |
| `reporting-agency-annexes-2-record.md` | **live** | **F-A5** annexes 2 of 2 · v2 — **D** the decision register · **E** the predictions the rig replay must confirm or correct · **F** the change log · **G** the owner questions in full · **H** acceptance, registered risks and named non-goals. |
| `reporting-agency-gate-record.md` | **live** | **F-A5 PR-0 gate record (2026-08-22)** — two fresh-context lenses, every finding re-verified at the bytes by a verifier that did not author the design. **3 blockers / 7 materials / 1 nit; 2 refuted**, headline: the evaluate leg had no lawful path; `directed_by`/`prepared_by_agent` added but never written (a wall drawn, not armed); the definition-approval maker/checker wall REMOVED on the agent lane instead of re-aimed. **OQ-5 ruled by R-L14** and **the sandbox severance accepted by R-L15** (both annotated in place). |
| `freeform-read-survey.md` | **live** | **F-A6** · the audited-freeform-read ESTATE SURVEY — the estate as-found for the DECIDED read surface, read against `wave-f-contract.md` §F-A6 including its `[TA-2026-08-22]` amendment. |
| `freeform-read-design.md` | **live** | **F-A6** · **the design doc of record** · v2 · gated 2026-08-22 (record: `freeform-read-gate-record.md`) — server-side client scoping compiled into the query, HOME chat firm-wide, the **enumerated table list printed as a law-34 audit line (closes audit GAP5-5, HIGH)**, and the **one-arm/one-settle** receipt shape under SECURITY INVOKER: the receipt cannot be forged by the payload, any second call aborts the transaction, and a read with no settled receipt cannot COMMIT (R-L16). **Cross-client named read severed to lane F-A6 v2 (R-L17); XLSX/DOCX `structured_parse` excluded in v1 (R-L18).** |
| `freeform-read-annexes-1-mechanics.md` | **live** | **F-A6** annexes 1 of 2 · v2 — **A** the enumerated surface (the law-34 audit line) · **B** the RLS arms · **C** the receipt columns · **D** the refusal vocabulary · **E** the ops recipe and the censuses that move. |
| `freeform-read-annexes-2-record.md` | **live** | **F-A6** annexes 2 of 2 · v2 — **F** the battery (incl. R-L16's three required cells) · **G** the decision register · **H** the change log · **I** the consolidated predictions · **J** the owner questions, registered risks and named non-goals. |
| `freeform-read-gate-record.md` | **live** | **F-A6 PR-0 gate record (2026-08-22)** — a bytes lens (fourteen findings) and a rulings lens (two). **4 blockers / 5 materials**, headline: the two SECURITY DEFINER receipt writers granted to the SAME read role (write power by the back door); a relation census via dynamic EXPLAIN that cannot work in plpgsql; an arm→settle receipt the table cannot hold as two rows; `_freeform_read_core` granted and ungranted in three contradictory places. **GB-1 ruled by R-L16 and the cross-client severance accepted by R-L17** (both annotated in place). |
| `sandbox-export-survey.md` | **live** | **F-A5b** · the sandbox-export ESTATE SURVEY · v1 — findings **X1-X12** at the bytes (the watermark burn already exists at `layout.mjs:136`; `render_jobs` and `report_artifacts` cannot carry a run-less, multi-client export; the estate passes the artifact hash IN; the schema has **no recipient concept at all**), the censuses that move, predictions **P-1..P-6**, and the **UNVERIFIED register U1-U6** — two of the lane's dependencies do not exist yet. |
| `sandbox-export-design.md` | **live** | **F-A5b** · **the design doc of record** · **v2 · gate-folded 2026-08-23** (record: `sandbox-export-gate-record.md`) — the lane severed out of F-A5 by **R-L15**, building **TA-P10 C′**. `p_view` becomes `clara.sandbox_views` (immutable) + `clara.sandbox_exports` (lifecycle wall, hash IN at completion); the client set is **DERIVED at mint and fails closed**, and after the fold it binds to **the body's own blocks** (`basis_ref`) and not only to the caller's basis pointers; the basis is read under an **explicit same-firm predicate**; `firm_closure` covers clients at **any status**; an **empty** set refuses at both doors; the renderer keeps **its own watermark refusal**; §3.7's wall is scoped back to the receipt schema with **G-3** defined. **Both OWNER CARDS are RULED 2026-08-23** — the model-typed figure is replaced by a **substitution seam** (§3.6b), and Q4 gets a **firm-level disclosure authorization register** (part 2, §7) — and **law 28's cross-model pass is still owed, against v2**. The lane's DARK condition **LIFTS AT BUILD**: the owner ratified the `sandbox_watermark` trio the same sitting (§3.6a). §3.7 onward now in `sandbox-export-design-part2.md`. |
| `sandbox-export-design-part2.md` | **live** | **F-A5b** · design of record, §3.7-§7 · split off `sandbox-export-design.md` at the 500-line ceiling, 2026-08-23. Carries the RULED disposition of both owner cards (§7) and the discharged train dependencies (§6: the owner-ratified watermark trio, the substitution seam). Section numbering unchanged. |
| `sandbox-export-annexes.md` | **live** | **F-A5b** annexes · **v2 · gate-folded 2026-08-23** — **A** the surface (three relations, nine verbs, the typed refusal vocabulary incl. the fold's five new tokens) · **B** the battery (+B1.8-B1.13, B2.7, B3.6, B4.5; B3.5 and B4.1-B4.3 re-cut) · **C** decisions C-1..**C-24** · **D** predictions · **E** the six owner questions incl. a DRAFT `sandbox_watermark` trio in en/ms/zh · **F** risks (+**R-7**), non-goals, acceptance (**fixture corrected to BELCORT** — "RPR" was ROME PROPERTIES, never the synthetic sandbox firm) · **G** law 28's brief · **H** censuses · **I** the render entrance · **J** the human doors · **K** dependencies. |
| `sandbox-export-gate-record.md` | **live** | **F-A5b PR-0 gate record (2026-08-23)** — five lenses, refute-style adversarial verification per finding. **4 blockers / 6 materials / 1 nit confirmed; 5 refuted.** Headline: the coverage check bound to the caller's **basis pointers** rather than to the file's content (the ruled wall, encoded one argument out); the client-set derivation read basis rows with **no same-firm predicate** inside a `clara_fn_owner` core whose policy is `using (true)` — the `0083:102-108` class; the watermark wall sat only at the request door while `typstString` **coerces a missing string to `""`**; and the acceptance fixture named "RPR (the synthetic sandbox firm)", conflating ROME PROPERTIES with ROME PUBLIC ADVISORY. **Eight folded into v2 by this record; two are OWNER CARDS (§6) and are NOT closed.** |
| `card1-substitution-seam-design.md` | **historical** (delivered) | **F-A5b card 1** · **v3, the final design fold before build (2026-08-26)** · **the design doc of record, §1-§2** — implements the 2026-08-23 owner sitting's substitution-seam ruling (`sandbox-export-design.md` §3.6b): **§1** what/why plus the honest B-mapping, incl. the (a)/(b) asymmetry sentence (placeholder may cite any `'ok'` preview cell; a `cell` AST node may cite only a definition-backed one, BL-5) · **§2** stage (a) in full — the `kind='placeholder'` block (M3/M4/M5's explicit branched validation, N1's `if`-not-`elsif` precision), the widened `sandbox_export_payload` (M11: cited labels only), the renderer's typed malformed-shape refusal (BL-8) with the N4 three-`cell`-concepts disambiguation, and the claim/dispatch verbs incl. **BL-6's ALTER on `sandbox_exports`**, N7's exact lease-clamp formula, and N8's cross-client statement for stage (a). §3 onward in `card1-substitution-seam-design-part2.md`. **v3 folds all 7 remaining nits + all 9 battery gaps + CD-14 APPROVED**, complete gate report addressed. |
| `card1-substitution-seam-design-part2.md` | **historical** (delivered) | **F-A5b card 1** · v3 · design of record, **§3 only** — stage (b) in full: the `cell` AST primitive (definition-backed-only, BL-5; real temporality per its own definition, M7; N8's single-client statement); **M6's accounting-correctness ruling**; the evaluator-versioning plan corrected on four points (BL-1..BL-4) plus **N3** (`_validate_metric_ast_shape_v1` needs no `_v2` twin — a SECOND independent door closing §6's non-goal) and **N5** (`metric_units`' firm-predicate-free read is safe, stated against CD-7's framing); **M8 now states BOTH countings explicitly** (seven textual call sites, five kind-groups) rather than disputing a single number; **CD-14 APPROVED 2026-08-26**. |
| `card1-substitution-seam-design-part3.md` | **historical** (delivered) | **F-A5b card 1** · v3 · design of record, §4-§7. **§4** the walls that do not move — this build is **NOT DDL-free** (BL-1/BL-6's two ALTERs) · **§5** the full refusal-token table · **§6** named extension points, **§6 item 5 gains N3's second-door finding** (a future canonical `cell`-referencing definition path would need its own `_validate_metric_ast_shape_v2` too) · **§7** the migration + runtime + census move list, corrected for M1, M9, M10/M12, BL-7. |
| `card1-substitution-seam-annexes.md` | **historical** (delivered) | **F-A5b card 1** · v3, the final design fold — **A** the surface (two ALTERs; `evaluate_metric_v2`/`reap_exhausted_sandbox_exports` added; `interactive_client` deleted) · **B** the battery, **all 9 fold-relayed battery gaps now tagged and forced** (R-CD-3 both polarities, the deploy-flip both polarities, the `evaluator_entrypoint` stamp, `_normalize_metric_node_v1`'s byte-identical round-trip on a `cell` node, the M6 period/watermark mismatch cells, the `metric_primitives` CHECK both polarities, the widened lifecycle trigger's three properties incl. terminal-freeze-intact, the attempts-ceiling cell, the renderer's malformed-shape refusal), plus **N2's `client_set_exact` assertion on BOTH B1.4 and B1.5** · **C** decisions **CD-1..CD-16**, **CD-14 now APPROVED** · **D** predictions · **E** risks (**R-1 carried**; **R-CD-3 closed by structure**; **R-CD-5 DOWNGRADED per N9** — labels round-trip raw, no normalization to diverge, cell still owed) · **F** acceptance, gated on **precondition 0** (the deploy ceremony). |
| `freeform-read-v2-survey.md` | **live** | **F-A6 v2** · the cross-client-named-read ESTATE SURVEY · v2, gate-folded 2026-08-23 — findings **Y1-Y9**, headline: a document may be **legitimately filed to more than one client** (three estate walls exist because it is); `documents.client_id` was **DROPPED entirely at `0007:1102-1106`** (Y5, gate-fold corrected — not "frozen," and v1's S-1 arm on `documents` cannot apply as documented); `get_document_extract`'s live body is `0090_f_a1_walls.sql:1558-1684` (Y6, gate-fold corrected); the typed door's `unassigned` disjunct must not be inherited by a free SELECT. Predictions **Q-1..Q-6** (Q-2 now names its ≥100k-region bulk fixture); **UNVERIFIED register U1-U7** — everything this lane extends is still v1 design text. |
| `freeform-read-v2-design.md` | **live** | **F-A6 v2** · **the design doc of record** · v2, gate-folded 2026-08-23 (record: `freeform-read-v2-gate-record.md`) — the limb severed by **R-L17**, carrying **R-L18's** XLSX/DOCX deferral. One sibling verb with a **named client list** and no scope argument anywhere; a shared core made safe by *having no scope argument* (v1's GB-2 defect was the argument, not the sharing); a set-shaped pin compiler (`NULL` = firm-wide) giving one policy predicate for all three scopes; and **S-2′**, the one- and two-hop EXISTS joins to `document_filings` that reopen `document_extractions`/`document_regions`. Carries **two cheap requests on v1's shape** — cheap before v1 builds, 35 policy re-cuts after. **Gate fold:** the cross-client scope is now bound to the SAME verified receipt row `_freeform_admitted()` checks (arm-state forgery blocker closed); rung 1 normalises (dedupe+NULL) BEFORE evaluating; rung 2 carries no client-status conjunct; §3.4 gains a named currency/version/multi-filing payload-semantics hazard; §3.7 gains a content-borne-injection question; §6's D1 accounting now covers `CLR10`'s retirement unconditionally. **All four owner cards RULED 2026-08-23** (`freeform-read-v2-gate-record.md` §5 addendum): all seven live `engine_kind` values admitted with a row-level predicate + provenance label; the document-body egress purpose re-binds to v1's named-purpose apparatus, identity documents included (conductor's dissent on file); OQ-A's call-path extension RULED-BY-LEAD, R-1 extends. |
| `freeform-read-v2-annexes.md` | **live** | **F-A6 v2** annexes · gate-folded 2026-08-23 — **A** the whole diff against v1 (14 objects), the FOUR allowlist rows and why HOME gets one (now unconditionally true — no status exception), the token vocabulary · **B** the battery (the refile sequence, multi-filing in both directions, the unfiled document, the forgery structure re-proven — now incl. a direct-`set_config` forgery cell and a currency differential cell) · **C** the decision register (+ C-13 status-blind rung 2, C-14 receipt-row-bound arm state) · **D** the predictions · **E** five owner questions · **F** risks, non-goals, acceptance (+ R-8 content injection, R-9 D1-gated CLR10 retirement). |
| `freeform-read-v2-gate-record.md` | **live** | **F-A6 v2 PR-0 gate record (2026-08-23)** — five lenses (live-truth, accounting, security, law, build), 20 raw findings deduped to 18-to-verify + 2 nits. **2 blockers / 12 materials confirmed, 4 refuted.** One blocker (arm-state forgery) and nine materials FOLD into DESIGN v2 above; one blocker and three materials were **OWNER-RESERVED** as four crisp cards (the `engine_kind` seven-value world × 2 lenses, the document-body PDPA/egress purpose, OQ-A's call-path extension) — **ALL FOUR RULED 2026-08-23** (owner sitting), dispositions in this file's §5 addendum. Two confirmed nits recorded, not folded this pass. |
| `filing-and-interview-survey.md` | **live** | **F-A7** · the filing-verb + interview-layer ESTATE SURVEY · v1.2, reconciled to design v2 after the gate. **The design doc GOVERNS on any disagreement with this file.** |
| `filing-and-interview-design.md` | **live** | **F-A7** · **the design doc of record** · v2 · gated 2026-08-22 (record: `filing-and-interview-gate-record.md`), carrying **F-A10's retirement clause for the four `rule`-spelled matchers**. Severed into **five trains** (π additive · γ egress · α constitutional), with F-A7b split out; the four TA-P7 riders ship with it. **AM-8 widened at landing: Clara reverses her own posted misattribution herself and raises the question — only the cross-client re-home stays the human's.** **PR-ρ (the runtime train, UNAUTHORED) is gate G1's seam: rho becomes a CONSUMER CONFIG of `g1-wake-engine-design.md`'s engine — one registry row + its own workflow export — never its own spine consumer** (`g1-wake-engine-design.md §5`); `filing`'s DB shape (`0126`) already rides `kind='wake'`, so no carrier-shape conflict exists to resolve. |
| `filing-and-interview-annexes-1.md` | **live** | **F-A7** annexes 1 of 2 · v2 — **A** the verb catalog, the typed refusal vocabulary and the `filing` kind's allowlist · **B** the test battery · **C** the decision register · **D** the D1 detail and the census re-cut list · **E** the predictions a rig replay must confirm. |
| `filing-and-interview-annexes-2.md` | **live** | **F-A7** annexes 2 of 2 · v2 — the gate-1 fold's three REGISTERS, split out because they are registers and not paragraphs: **G** the live-tip register (AB-3) · **H** the two-value re-derivation census (AB-1, AB-2) · **I** the re-cut D1 surface and the severed build. |
| `filing-and-interview-gate-record.md` | **live** | **F-A7 PR-0 gate record (2026-08-22)** — two fresh-context lenses against v1.1, read at `main` `cfa0710`, frontier `0102`, every finding adversarially re-verified. **6 blockers / 8 materials**, headline: the two-value attribution predicate re-derived INLINE in **seven** live bodies (v1 had counted only calls to `assert_client_resolved`), a BEFORE INSERT trigger doing the same, eight bodies cited to superseded text, and **`chatTurn_v13` already claimed by F-A2's PR-2** (a constraint-9 version-chain collision). |
| `fa7b-onboarding-survey.md` | **live** | **F-A7b** · the CLIENT-onboarding ESTATE SURVEY for the R8(a) joint UI+backend gate · v1, 2026-08-27, read at `main` `a87cc71`, frontier `0136`. Findings **S1-S10**, gaps **G1-G14**, absences **A1-A10** (each with the search that would have found it), predictions **P-1..P-6**. Headline: the R8(a) chain is **four links of six** — "Clara proposes onboarding" and "the document auto-attributes" are the two that do not exist; **Gate O does not check that a contributor is HUMAN** and Annex K says it does (S2); the interview's `opening_position` is **two-valued** and four of the five playbooks fit neither (S3); `commit_client_onboarding`'s **third arm** activates a client on a merely deferred opening, silently (S4); `_assert_opening_tie` structurally refuses a bank-only opening (S6). Also records four **stale standing texts**, incl. that Annex K's e2e-de-pin residual is already discharged and re-doing it would be a regression. |
| `fa7b-onboarding-design.md` | **live** | **F-A7b** · **the design doc of record** · **v1 — the gate SAT 2026-08-27; every NEEDS-DECISION resolves per `fa7b-gate-record.md`.** The JOINT UI + backend design R8(a) ruled: §2 decomposes the Wave-G acceptance scenario into **thirteen numbered steps A1-A13 plus the negative step A14** (a ROME-family counterparty must never reach the proposal), each labelled EXISTS / NEEDS-UI / NEEDS-VERB / NEEDS-DECISION; §3 the UI on Q2's rail+thread-escalation and Q3's two-level IA — the `/needs-you` proposal card, the escalated interview thread, the materials fork, the two-act doors step, the auto-attribution close, each run through the remove-the-rail test; §4 the backend deltas **D-1..D-12** with proposed verb names and shapes. Scope boundary held: **CLIENT onboarding only — firm creation is R8(b)'s.** |
| `fa7b-gate-questions.md` | **live** | **F-A7b** · **the owner's must-answer set for the gate sitting.** §1 is the per-situation treatment proposal for **all five ruled playbooks** (① predecessor FS+GL · ② values-only management accounts · ③ bank statements only · ④ shoebox · ⑤ mid-year with a records gap) plus green-field — each stating what Clara **may construct**, must **request**, and must **NEVER fabricate** (law 22), its DB path, its end state, a recommendation and a fail-closed default. §2 is thirteen open design decisions **Q-D1..Q-D13**, each with recommendation + fail-closed default + **the cost stated rather than assumed away**. Q-D1 is the big one: **when may Clara open a client file unattended versus propose it** — reconciled with TA-P1 C and law 79 as a narrowing of WHEN, never WHETHER. |
| `fa7b-onboarding-annexes.md` | **live** | **F-A7b** annexes — **A** the six-PR build sequence with its two D1-δ windows and the Q9 DONE formula · **B** the four residual contract deltas D-9..D-12 · **C** six registered risks (incl. **R6**: Gate O's human wall rests on a seeding discipline, not a predicate) and the named non-goals. |
| `fa7b-gate-record.md` | **live** | **F-A7b · THE GATE RECORD — the sitting's rulings, 2026-08-27.** All five playbooks APPROVED as proposed; **Q-D1 RULED ALL-PROPOSE** (Clara never opens a client file unattended); **Q-D6 RULED no-seal-while-deferred, no override door**; **Q-D8 RULED the interview normalizer SHIPS** (interactive_client, law-28 pass mandatory); Q-D9 the FY1 basis note names the opening's source; the eight batch items (D2-D5, D7, D10-D12) ruled per recommendation; D13 discharged pre-sitting (#356). Plus two owner-ratified clarifications: the human KEYED door is always open, and reverse-not-delete re-confirmed. **F-A7b is BUILD-AUTHORIZED.** |
| `witness-identity-variant-survey.md` | **live** | **pi/F-A1-successor** · the candidate-parameterized `evaluate_witness_identity` variant, ESTATE SURVEY · v1, 2026-08-25 — the debt-sprint forward-obligation item (`PROGRESS.md` Backlog): `evaluate_witness_identity_v1`'s self-derived candidate binding, the 0126 B2/B3 ladder and the outcome-bearing re-derivation, the `R0ME`/homoglyph gap in `name_family_candidates`, and the evaluator-freeze law this design must not violate. |
| `witness-identity-variant-design.md` | **live** | **pi/F-A1-successor** · **the design doc of record** · v1, 2026-08-25 — `clara.evaluate_witness_identity_v2` (a caller-supplied candidate-array parameter, per-candidate verdict map, firm-guarded), the determinism argument, the candidates-mandatory runtime-prompt seam contract, the 0126 B3 wiring delta (path (i)), the deterministic homoglyph fold (`name_family_confusables` + `name_family_token_confusable`, widening `name_family_candidates` in place), and the migration/census shape. DESIGN ONLY — no product code, no migration ships from this branch. |
| `witness-identity-variant-annex.md` | **live** | **pi/F-A1-successor** annex — **A** the test battery (V1-V7 the evaluator, B1-B5 the homoglyph fold, W1-W4 end-to-end sequencing) · **B** the 52-row confusables seed set in full · **C** the seven-item decision register. |
| `internet-lane-survey.md` | **live** | **F-A8** · the internet-lane ESTATE SURVEY · **v3** — v2 re-cut six claims that were wrong at the bytes; v3 amends three more (**F6** the `fetch()` precedent is credential-bearing · **F7** an enumerated deny list is the wrong SHAPE · **F13** a one-directional identity predicate cannot certify a miss) and adds **§5, five estate facts measured on a live rig at the 0102 frontier**: no raw-bytes column anywhere (copy `report_artifacts`/`documents`), the evaluator-freeze family and its naming trigger, exactly five app roles, `_human_ctx`/CLR04, and `wake_fn_allowlist` = 15 rows. |
| `internet-lane-design.md` | **live** | **F-A8** · **the design doc of record** · **v3** · gated twice (record: `internet-lane-gate-record.md`) — Tier-1 closes to `fx_rates` + the SST rate and threshold rows, landing through an **audited owner one-click door, not a PR**; immutable+supersede, a backdate triggers an impact scan, **a missing row REFUSES**. **v3 folds the law-28 cross-model pass: the value is derived from an IMMUTABLE FETCH ARTIFACT persisted before any model access, citations are FK-bound to an artifact digest + span, a DB-owned Tier-1 endpoint registry replaces URLs, the network policy is per-hop HTTPS with an inverted address test and peer verification, FX is exact-day, and the owner signs a digest-bound card.** PR-1 now carries the evidence substrate; **PR-4 is gated on owner question OQ-A.** |
| `internet-lane-annexes.md` | **live** | **F-A8** annexes 1 of 2 · **v3** — the battery, the decision register (**+ D.1b, the law-28 walls IL-D17..IL-D34**), the DDL posture, the censuses, the rig-replay obligations, the change log, **Annex I** (the six cross-item sequencing obligations, moved out of design §6) and **Annex M** (the supersession-and-relocation ledger, S-1..S-18 — every v2 passage the v3 fold struck or moved, verbatim). |
| `internet-lane-annexes-2.md` | **live** | **F-A8** annexes 2 of 2 · **v3, DESIGN-NORMATIVE** — the law-28 fold: **Annex J** the per-finding table (twenty findings, all CONFIRMED at the bytes, none refuted, three narrowed) · **Annex K the walls, specified** (immutable artifacts · canonicalizers · the endpoint registry · artifact-bound extraction · per-hop network policy · the sterile GET-only client · exact-day FX · server-minted freshness and revalidation · the both-tier attempt ledger · the composite-FK digest chain · the tool-less reader · the digest-bound approval card · `p_url` closure · the query-architecture default · cache provenance) · **Annex L** cells C.11-C.16 (injection · per-hop · freshness · authenticity · sterile client · egress) · **Annex N** six owner questions, **OQ-A gating PR-4**. |
| `internet-lane-gate-record.md` | **live** | **F-A8 PR-0 gate record — BOTH legs (2026-08-22 and 2026-08-23)**. **Leg 2, the law-28 cross-model pass, is §10: twenty findings — fifteen blockers, four HIGH, one MEDIUM — all CONFIRMED at the bytes, none refuted; verdict BUILDABLE WITH THE LISTED WALLS, PR-2/PR-4 blocked until they are built.** Two advertised mechanisms did not exist: no stored bytes or digest, and no destination registry. Leg 1 (law 1, two fresh-context lenses): **6 blockers / 9 materials**, headline: **neither mechanical check bound the landed value to the sources**, so a model-chosen numeral could land (law 1); the threshold table's supersede ALTER is unbuildable as written (composite PK, no id); "Tier-2's structural insulation is an absence" is false; `entry_post_receipts` cited as a live `0037` precedent when it is F-A2's unbuilt table. |
| `metering-survey.md` | **live** | **F-A9** · the metering ESTATE SURVEY · v2 — the gate's byte lens re-derived every census here against the live migration sources; the two that were wrong are re-cut (the gate census was not closed-world — it missed a whole second limits table; the exhaustiveness claim was scoped to the wrong population). |
| `metering-design.md` | **live** | **F-A9** · **the design doc of record** · v2 · gated 2026-08-22 (record: `metering-gate-record.md`) — **TA-P13 = A** one ledger (`llm_usage_events` reshaped for any call kind, `client_id` + triggering actor added NOW), **TA-P12 = A** the brake census, **TA-P2 = A+** spend as a versioned deterministic evaluator. **`firm_id` becomes NULLABLE for platform-level calls — a NULL firm is a platform call, never an unmetered one (R-L10).** PR-0 is the chat-cap hotfix; PR-1A is the ledger reshape with no D1. |
| `metering-annexes.md` | **live** | **F-A9** annexes · v2 — **A** the decision register · **B** the build sequence, the D1 body list and the cross-item sequencing obligations · **C** the test-battery manifest · **D** risks and named non-goals · **E** the change log · plus the price machine. |
| `metering-gate-record.md` | **live** | **F-A9 PR-0 gate record (2026-08-22)** — a byte lens that re-derived every citation, census and count against the live sources rather than the documents' own cites (16 findings) and a rulings lens. **4 blockers / 6 materials**, headline: the brake census was not closed-world (three document-lane usage gates missed, including a per-UTC-day page budget); the spend evaluator's join was session-`TimeZone` dependent (law 1); the read view could not return rows for its only caller (SECURITY INVOKER over FORCE-RLS); the price door's two checks collapsed to one boolean. |
| `fix-queue-survey.md` | **live** | **F-T4** · the fix-queue ESTATE SURVEY (as-found), findings **F1-F33**, ten queue items with their owners stated first. Headline findings: drawer 1 returns `tie` on an EMPTY `bank_accounts` registry because the loop never iterates, and **the "bank-class COA account" any registry-vs-ledger predicate would key on is MINTED BY REGISTRATION** — which makes F-A3's arm 4 vacuous on the same population (cross-item **X-1**) · N5 is TWO-sided (the coding lane's raises carry no `fix` AND its mapper has no slot for one) · the 401/403 conjunct is copied three times with no shared helper · the ceremony DSN bridge was never in the repo at all. **No rig ran; every body-level claim is a prediction.** |
| `fix-queue-design.md` | **live** | **F-T4** · **the design doc of record** · v2, gate-folded 2026-08-23 (record: `fix-queue-gate-record.md`) — five PRs, **PR-1 (DSN bridge) SEVERED from this gate by owner ruling and building on its own**. Item A arm (a) redesigned to a MEASURED check (durable `is_bank_account` alone was a false-positive wall on every clean deactivation/remap) · item D's CHECK/CoR retargeted to the LIVE `ck_processing_task_error_code_f_a1` object, `request_reextraction` dropped from its D1 surface (it already admits unconditionally), and four frozen catch modules + three facts-lane writers added as the real D1 surface · N5's `refusal_remedies` key gains `direction` + `subsystem` and its grant is corrected to reach the agent lanes · §2.1's shared predicate states its security posture explicitly · **§8 is the claims accounting convention**, MPERS/CA 2016/LHDN citations fetched 2026-08-23; item C's employee row **UNBLOCKS — OQ-9 RULED 2026-08-23** (close-time scan + draft, `fix-queue-gate-record.md` M4 addendum). §8.3 onward, incl. §9-§10, now in `fix-queue-design-part2.md`. |
| `fix-queue-design-part2.md` | **live** | **F-T4** · design of record, §8.3-§10 · split off `fix-queue-design.md` at the 500-line ceiling, 2026-08-23. §8.5 carries OQ-9's RULED disposition (close-time scan + draft) and the registered auto-post widening trigger. Section numbering unchanged. |
| `fix-queue-annexes.md` | **live** | **F-T4** annexes · v2, gate-folded — **A** the batteries (incl. the redesigned arm-(a) cells, the direction/subsystem cells, the argv/disk cells) and the closed-world censuses (**C2 widened 3→6**; new **C11** for the frozen-module/facts-writer clamps) · **B** the decision register, now **D-1..D-25** (**D-13..D-25** are the fold's) · **C** **nine** owner questions (new **OQ-9**, item C's cut-off rule) · **D** the cross-item notes, registered risks (new **R-5**, `request_reextraction`'s pre-existing corrupt/encrypted gap) and the v2 change log. |
| `fix-queue-gate-record.md` | **live** | **F-T4 PR-0 gate record (2026-08-23, part of the joint Track-B gate)** — 19 confirmed (**6 blockers, 10 materials**) + 5 nits + 5 refuted, against 26 raw findings. 14 folded into v2 (design + annexes, above); item A's durable-flag predicate and item D's retired-CHECK/phantom-CoR/frozen-module gaps are the headline blockers. Two materials held open: **OQ-9** (item C's employee-claim cut-off rule, owner-reserved) and **PR-1's severance** (owner-ruled same day — PR-1 is out of this gate, building standalone). |
| `payroll-calendar-survey.md` | **live** | **F-T2** · the payroll deadline calendar's ESTATE SURVEY (as-found, v1 2026-08-23) — what exists for PCB/EPF/SOCSO/EIS/HRD today, the statutory re-verification with its URL + fetch date per fact, and the absences recorded as absences. Establishes the two facts the design turns on: `proactive` is allowlisted for **exactly one** function, `wake_record_notification` (`0002:558`; `0011:3903-3910` adds none, `0078:181` is interactive-only), and the deadline table's DDL is **not this item's to write**. |
| `payroll-calendar-design.md` | **live** | **F-T2** · **the design doc of record (v2, gate-folded 2026-08-23)** — the calendar contributes ROWS to a versioned, effective-dated `clara.statutory_deadlines` whose **DDL belongs to F-A4**, so **PR-1 is blocked on F-A4's PR-1c**. **It mints NO wake kind:** the chase rides `wake_record_notification` on the existing `proactive` credential, because the notice needs no capability that kind does not already have — the one-architecture test applied to a lane that could easily have invented a second. `working_day_basis = 'weekends_only'`, with the field present so that "public holidays are not handled" is a **visible** limitation rather than a silent one. **v2 folds eleven PR-0 gate corrections** (`payroll-calendar-gate-record.md`) — the applicability matrix made total, a second read named for rung B2, the coverage predicate's undated-filing arm period-bound, `effective_due` added to the notice payload — and reserved five owner cards (OC-1..OC-5) — **ALL FIVE RULED 2026-08-23** (`payroll-calendar-gate-record.md` addendum): the coverage predicate re-anchors on the payroll JV recognition fact, F-A4's clock spine owns period generation, a new `pays_agent_incentives` client fact gates CP58, the legal due date follows the Interpretation Acts' forward roll alongside `effective_due`, and B12 routes through F-A7's `wake_open_question` widening. |
| `payroll-calendar-annexes.md` | **live** | **F-T2** annexes (v2, gate-folded) — the row shapes (`obligation_code`, `authority` across `lhdn`/`kwsp`/`perkeso`/`hrdcorp` — four regulators, four channels, four penalty regimes), the battery, and **eight owner questions**: staff allowances (a PRD-named Wave-F behaviour with no contract item) · the Form E grace month · the CP21/22/22A/22B family · CP58 · a payroll-JV account-role convention · **the HRD Corp deadline conflict (15th vs the last day of the following month)** · **the weekend rule and whether BELCORT's own practice differs** · an EPF/SOCSO/EIS rate table as a fourth Tier-1 table. Decision register now runs to **D-27**. |
| `payroll-calendar-gate-record.md` | **live** | **F-T2 PR-0 gate record (2026-08-23)** — six lenses, 3 blockers / 19 materials / 5 nits confirmed, 6 refuted. Eleven distinct corrections folded into v2; **seven items were owner-reserved as five cards** (the `documents.financial_date` coverage anchor · the PCB/EPF/SOCSO/EIS period-basis anchor · CP58's missing fourth fact · the weekend-rule computation-of-time direction · `wake_open_question`'s missing door) — **ALL FIVE RULED 2026-08-23** (the sitting), dispositions after OC-7 in this file. |
| `tax-computation-survey.md` | **live** | **F-T3** · the draft tax computation's ESTATE + LAW survey (**v1.1**, 2026-08-23; §7's Tier-1 collision re-cut as **RULED — R-L25**, so S6 stops being a door dependency and the ICT/U1 posture becomes the model for the seeded-law family) — three findings reorder the item: the tax layer is **greenfield** (zero hits for `cp204`/`chargeable`/`add_back` anywhere) and Wave D's `ca_class`/`is_commercial_vehicle`/`is_new` trio is read by nothing; **the corpus contains no tax computation, no Form C, no CP204 and no asset register**, so there is no acceptance oracle; and the number path F-T3 must ride (`metric_definitions` → `evaluator_versions` → `metric_cells` → `report_runs`) exists in full but **has never carried a run**. Carries §6, the law re-fetched from LHDN and the ITA 1967 on **2026-08-23** with URL + fetch date per fact, the eight items that could NOT be verified (incl. the ICT 40/20 gazette), and the two places the prior research did not survive re-verification. |
| `tax-computation-design.md` | **live** | **F-T3** · **the design doc of record, §1-§7** (**v2, gate-folded 2026-08-23**) — the **severance**: Clara writes only a `code` from a closed, owner-signed treatment set that carries its own fraction and its own citation, and her proposal row **has no numeric column**, so a model structurally cannot emit a numeral into a statutory document. Twelve rungs from the sealed `close_receipts` to the tax charge and the CP204 schedule in **ONE frozen evaluator member** (**D-16**); ten new tables; a three-valued SME predicate that **refuses rather than defaulting to 24%**; a transparent entity **refuses an entity charge rather than computing zero**; field-addressed packs, not form replicas; **no submission verb of any kind**. **v2 folds the PR-0 gate's 11 blockers + 11 materials + 1 nit** (record: `tax-computation-gate-record.md`): the ladder is re-anchored on `snapshot->'pl_rows'` because `closing_position` is **balance-sheet-only** (**D-18**) and A.2 gains the sign rule it always needed; **R7/R8 are re-cut against the Act's own sequence** — the b/f business loss is the **s.43(2)** deduction floored at the business aggregate, the current-year loss is **s.44(2)**, and the s.44(6) cap sits on aggregate income (**D-19**); `tax_carryforwards` gives the loss inputs a DB-owned home whose absence **refuses** (**D-19**); **F-T3 mints no `client_fact_keys`** — `record_client_fact` has no valid time, a fail-closed dispatch and a `_human_ctx` gate that raises CLR04 in a migration, so `tin`/`ssm` read the existing `client_identifiers` and the dated facts land in `client_tax_attributes` (**D-3 re-cut, D-21, D-22**); a basis period that is not the sealed fiscal year **refuses by name** (**D-23**); the human-keyed apportionment wall gains an ARM-0 trigger and an `is_agent`-excluding door, the NOT-NULL CHECK the agent satisfies having been the only mechanism (**D-24**); **§3.1 finally gives the three agent writes a wake door** — wrapper, credential, allowlist, kind, receipt, on the `0078:90-107` shape, with no new kind needed and the analysis stated (**D-25**); the disposal value becomes the **statutory** Sch 3 figure, human-keyed, written by `_fa_on_approve` with the immutability allowlist widened, so **PR-3 replaces three live bodies, not one** (**D-7 re-cut, D-26**); and the frozen member moves to **PR-6, the last DDL-dependent PR**, with a closed eighteen-relation census. **v1.3, 2026-08-29 — the PR-0 REPLAY fold: the replay record is the MEASURED ground and OVERRIDES this set on collision.** Eleven deltas, ledgered in the mechanics annex **§M0**; the two that would otherwise have been built wrong are **D-7** (`uq_fixed_assets_id_firm_client` already exists — PR-3 must NOT add `uq_fa_id_tenant`) and **D-9** (the close belt enforces `closing_position` only, so `pl_rows` earns a twenty-second refusal string). D-16's ONE-member ruling **stands on a replaced rationale** (`pg_get_functiondef` renders neither the owner nor the ACL). Three new owner questions — **OQ-10/11/12** — all BUILT to their fail-closed defaults, as are OQ-7 and OQ-8. |
| `tax-computation-design-part2.md` | **live** | **F-T3** · **the design doc of record, §8-§13** (**v2**, split from part 1 at the gate fold to keep both inside the 500-line budget; nothing dropped) — the three artifacts and where the human wall stands (the pack's TIN reads `client_identifiers`, never a blank field); the **twenty-one-string refusal vocabulary** with each string's trigger and its `metric_cells.cell_status` mapping, plus the obligation the design never carried — `0058:261-262` makes `na_reason_version_id` a **hard CHECK** on every non-`ok` cell, so PR-1 seeds one `metric_na_reason_versions` row per string or the string can be raised but never persisted; the battery's shape and the seven cells the fold added; **the nine-PR ladder re-ordered** so every relation the frozen body names is created strictly before PR-6; sequencing (one shared surface **gone**, `fixed_assets` now three bodies, two research gates on U5 and U2); and what is explicitly not in v1, four rows longer than v1.2's. **v1.3, 2026-08-29** — the vocabulary is now **twenty-two ladder strings** (D-9 added `close_snapshot_missing_pl_rows`: the close belt enforces `closing_position` ONLY, so the key the whole ladder reads is unenforced) **plus two separately-counted RULING strings**, `s44_6_relief_unmodelled` (OQ-11) and `tax_issue_unavailable` (**裁-33**). **裁-33 rules there is NO golden bar: a tax computation goes to DRAFT only, never `issued`, and PR-7 is NOT built for beta** — `report_runs` keeps its `issued` value and the TRANSITION is walled by name. **PR-1 is BUILT**; PR-3 loses `uq_fa_id_tenant` and inherits OQ-10; PR-6's wrapper obligations are larger than v2 stated (D-10); §12's evaluator-roster claim was wrong at `main` in two of three terms and **F-T3 does not have to be last**. |
| `tax-computation-annexes.md` | **live** | **F-T3** annexes (**v2, gate-folded**) — **A** the basis-period model **plus the co-extensive-close wall**, the cents/rounding discipline **plus the `account_type` sign rule** (an income account's `movement_cents` is negative, so a literal read would have made an exempt-dividend exclusion *increase* adjusted income), and the worked ladder with **R7/R8 and R12 re-worked** (v1.2 printed a s.107C(10) increase of 50.55 where the recorded estimate gives 650.00) · **B** decision register **D-1..D-26** — **D-3 and D-7 RE-CUT** against live bodies that refuse them, **D-17's framing note CORRECTED** (constraint 12 was **vacated**, not reworded: cite `docs/product/PRD.md` §6 invariant 2(b)), and **D-18..D-26** for the nine gate corrections that changed a mechanism · **C** **fifteen** predictions — **P-3 re-cut** (its old discharge would have ticked PASS on the broken design, so it is now differential), **P-5 withdrawn** (it predicted a capability the live signature forecloses), **P-6's pin corrected**, **P-11..P-15** added · **D** the question register, **untouched: the fold decided no owner question and closed none** · **E** the belt's population, plus the three deliberately-absent rows it now keeps visible. |
| `tax-computation-annexes-2-mechanics.md` | **live** | **F-T3** mechanics annex (**new at the v2 gate fold**; nothing here is new scope) — **M1** the verb set: three granted wake wrappers on the `0078:90-107` shape, their ungranted cores, the wake-kind analysis measured against `0011:618-628`'s two CHECK families (`autodraft` for the client-scoped writes, `proactive` for the belt, **no new kind**), and the five human doors with their role floors · **M2** the surface DDL for all ten relations · **M3** the disposal machinery — the Sch 3 para 62 value is **not** the accounting proceeds, `disposed_on` is dropped because `disposed_at` already exists, `dispose_fixed_asset` writes no register row (`_fa_on_approve` does, on both the full and partial-supersede paths), and the closed immutability allowlist would have raised CLR13 on the **first** disposal in the estate · **M4** tenancy and RLS, re-scoped: **seven** client-scoped relations take `firm_id` + the composite tenant FKs, **six** platform-scoped ones take force-RLS + an owner-only policy and **no** `firm_id` (the `llm_price_table` precedent), and the tenant-congruence gap is invisible to every gate in the estate · **M5** the `law_review_due` belt · **M6** the **thirty-five-cell battery**, one per refusal string plus the structural walls. **v1.3, 2026-08-29 — this file now carries §M0, the PR-0 REPLAY LEDGER: all eleven deltas, what v2 claimed, what the rig measured, and where each is folded.** Also folded in place: M1.2's wake CHECKs re-derived from the live catalog (**seven** kinds, **six** pairing arms — the conclusion unchanged, the quoted text stale by four), M1.3's statutory door re-cited to the CoR'd core and re-framed as a role FLOOR, M2's `tax_treatment_codes` gaining `refuse`/`refusal_reason_key`/`requires_apportionment` and nullable signatures plus `tax_thresholds.value_int`, M3.4's measured finding that `ca_class` **freezes** once depreciation particulars are complete (→ OQ-10, and battery cell C11b is unbuildable as written), M4 dropping `uq_fa_id_tenant` and adding `tax_add_back_class_map` to class B, and M6 gaining cell **H2** (裁-33: no F-T3 relation carries a lifecycle state, proven with a positive control). |
| `tax-computation-gate-record.md` | **live** | **F-T3 PR-0 · the gate record** (2026-08-23) — five fresh-context lenses (live-truth · accounting · security · law · build) over design v1.2, every finding re-attacked by an independent verifier: **11 blockers · 11 materials · 1 nit CONFIRMED, 9 REFUTED**, the heaviest of the six Track-B sets. Carries what HELD (the severance idea, D-16's measured collapse, R-L25's posture, OQ-4's refusal), each blocker with its bytes and its fold target, the materials table, **§4's re-derivation of the SUSPECT-flagged tenancy finding** (it overstated the breadth — five relations are legitimately `firm_id`-free by house precedent — and mis-stated the CI mechanism, while being right and consequential on tenant congruence), the nit, **§6: no owner-reserved finding, and the standing cards D.1-D.3 / OQ-2 / OQ-3 / OQ-9 stand unchanged** (the gate sharpened OQ-1's argument: two of the eleven blockers are precisely the whole-ladder errors no wall test catches), the cross-item sequencing obligations, and §9's replay list. **§8 records a real gap: the gate result carried the refuted COUNT but not the refuted findings' text**, so they cannot be enumerated here. |
| `tax-computation-pr0-replay-2026-08-29.md` | **live** | **F-T3 PR-0 · the RIG REPLAY, measured at the live frontier** (2026-08-29, throwaway `postgres:17`, `0001`→`0147` from `main` `7e9180df`, destroyed after) — the file that turns the whole F-T3 set's source-read predictions into measurements. **Annex C's fifteen predictions discharged: nine CONFIRMED, three RE-CUT, two HALF-REFUTED, one withdrawal upheld.** Headlines: **D-16's *rationale* is half wrong and its *ruling* survives on a different measured argument** — an owner or ACL change does **not** move `pg_get_functiondef`'s hash (only `search_path`/volatility/secdef/body do), but the checker genuinely ignores `deployed` and closures genuinely **share** members, which is the real reason to register one · **P-14 half-refuted — `clara.fixed_assets` already carries `uq_fixed_assets_id_firm_client`, so PR-3 must NOT add `uq_fa_id_tenant`** · **P-11 confirmed both directions, plus a new finding: `ca_class` freezes under CLR13 once depreciation particulars are complete**, so capital allowances are unreachable for the existing asset population without a new door (**OQ-10**) · the snapshot builders are re-cited to **`0128`** (the `0056` body is superseded twice) and the type filter + `mv <> 0` + the `account_type` sign rule are all confirmed at the LIVE body · `client_fact_keys` is **five** rows, not four · **the close belt enforces `closing_position` only, so `pl_rows` is an UNENFORCED key** and a twenty-second refusal string is owed · **the merge order is corrected** (F-A5 PR-2 + the C-flip are done, F-A8 PR-1 has not merged, F-A5b card-1 and F-A4 PR-2a are unlisted claimants, and F-T3 need not be last). Carries the **v1.3 delta list PR-1 must fold**, **three new owner cards** (OQ-10 the CA-class door · OQ-11 the s.44(6) cap · OQ-12 CP204's missing period grain) beside the standing D.1-D.3, the 裁-21 COA dossier cross-reference with its four citation corrections, and **the PR-1 build brief — BUILD-READY, D1 NONE**. |
| `tax-computation-pr2-design.md` | **live** | **F-T3 · the COMPUTATION BUILD INCREMENT** (2026-08-30, replay-measured at `0155` on a throwaway `postgres:17`, 150 files, destroyed after) — the build layer beneath the v1.3 design set, covering the ladder's **PR-2 → PR-6**. Records what `0152` actually shipped (six platform relations, `relacl` NULL; **13 codes, all unsigned**; **24** reason rows enumerated by `cell_status`, so this increment seeds none), then the four things the build owes: **PR-4's severance** as three mechanisms rather than one NULL-shaped CHECK, plus **`requires_apportionment` as a fourth wall PR-1 already built** — a `COALESCE(apportionment_bp, 10000)` on a mixed-use motor code silently books the whole running cost as a private add-back, so the evaluator must refuse `mixed_account_needs_split` instead; **the signature door refusing a code whose `conflict` column is non-null** (C-1, the s.39(1)(k)-vs-(c) collision, is unresolved and load-bearing at signature); **§3.4's new finding — the estate's evaluators SELF-REFUSE `CLR10 evaluator_undeployed` until their closure is flipped**, measured in `evaluate_fs_pack_agent_v1` and `evaluate_metric_v2`, so F-T3 has **two** ceremony acts and their order is load-bearing; and **§8's argument that the ladder is the review instrument**, not a scheduling preference — one file makes the eighteen-relation census self-satisfying and puts a D1 window in the same file as a frozen registration. Carries the PR-3 D1 inventory (three bodies), the run wrapper's seven provenance families, **the GRANT SEQUENCE — `0152` granted nothing (`relacl` NULL on all six, one function minted), so PR-2 carries the FIRST `clara_authenticated` reach in all of F-T3 and nothing is wireable before it** — and a build-ready work order per PR. |
| `tax-prep-wake-design.md` | **live** | **F-T3 PR-9 · the `tax_prep` WAKE, design §1-§6** (裁-44, 2026-08-30, same replay) — the agentic half: after a close seals Clara drafts the ladder unasked, proposes each account's treatment, and a human signs. **§2 decides the CARRIER on measurement rather than on the ruling's wording**: `close.finalized` is a registered event whose `trigger_taxonomy` decision at the ACTIVE version is **`ignore`** ("the receipt row is the record; no router wake"), so the seal mints no wake — and the honest trigger is a **state**, re-asked, not a moment, because this lane's expected early outcome is a refusal that a human clears later. **`direct_queue`, `task_kind='tax_prep'`.** §3 names the **five extend-only CHECK widenings** at their measured live text plus two on the receipt carrier, `mint_wake_credential`'s **two** gates (the trap `0133` paid for), F-T3's own `_tax_wake_ctx` (so no D1 on F-A4's body), the **reuse of `agent_act_receipts`** (no new `agent_receipt_surfaces` row), the decision that **`tax_prep`'s `call_kind` does NOT ride 裁-49's car**, the due oracle, the hold, and the five allowlist verbs — **superseding the v1.3 set's `autodraft` kind assignment for PR-4/PR-6**. §4 the `taxPrep.v1` closure and its seven-arm classify decision; §6 puts the tax-draft card on the needs-you page's **second feed**, avoiding a CoR on the 18 666-char `list_review_queue`, with the card carrying **`cell_id`s, never numerals**. |
| `tax-prep-wake-annexes.md` | **live** | **F-T3 PR-9** annex — **§7** failure isolation (incl. why the `tax_prep` transition arm is byte-identical to `close_prep`'s, `queued→failed` leg included — the M4 defect the estate already paid for) · **§8** the sixteen-cell battery, RED-before per wall · **§9** what is explicitly NOT built (SST-02 and the CP204 reminders are **different shapes with no lane and no date**) · **§10** the build work order with the two-body D1 inventory and the frontend home per door · **§11** sequencing and **the FIFTH ceremony act 裁-40 does not yet have** — the evaluator deploy flip must precede the `tax_prep` switch, or every run refuses nightly to `max_attempts` and dead-letters · **§11.1** what `tax_prep` needs from the clock, stated **per carrier** because the four sources differ: `close_prep` and `tax_prep` share ONE `direct_queue` minting seam, `bank_agent` needs strictly more (its `bank.agent_due` is in **neither** `event_types` nor `trigger_taxonomy` today) — and `tax_prep` should not gate Wave G whatever the producer ruling, because its chain cannot land by then · **§12 — 裁-33 AND 裁-44 RESOLVED, and what P6's Tax tab actually is.** They constrain different verbs (裁-33 the terminal one, 裁-44 the initial one), so **Clara does everything up to the signature and nothing past it**. P6 builds a **proposal/receipt surface, not a form** — and the form-shaped residue is exactly the **nine** inputs Clara structurally cannot supply, each indexed by the seeded refusal that demands it, which doubles as the acceptance test: any field the tab asks for that is not in that table is either a 裁-44 gap or a constraint-2 breach. |
| `tax-prep-gate-record.md` | **live** | **F-T3 PR-2/PR-9 · the design lane's own adversarial pass** (2026-08-30) — a self-gate, explicitly **not** the independent review. **4 blockers, 8 materials, 4 refuted, 5 cards.** **GB-4 (from the lead's census): the card is required to cite law it cannot read** — 裁-44 demands every rung carry its statutory citation, and `0152` granted NOTHING (`relacl` NULL on all six relations, one function minted, no reader), so a card could name a code and not its `s.39(1)(l)`; the fold is a bookkeeper-floor READER projecting the citation, the signature state and the C-1 conflict into one row, never a table grant. Two blockers are the author's own: **GB-2 — `tax_prep_due()` cannot name a YA**, because nothing maps a fiscal year to one until PR-2's `tax_basis_periods` exists and deriving it from `ends_on` would be the runtime computing a figure under s.21A; **GB-3 — with all 13 codes unsigned, every wake is structurally a refusal**, so the belt must not fire at all. Both mint one principle: *the due oracle must not wake her for a state whose only possible outcome is a named refusal a human has to clear anyway.* Materials headline `bound_digests` needing `evaluator_version_id` in the key, an ordering wall so a draft cannot be written before the cells exist, and **GM-5's honest gap** (a deferred trigger's scope was read, not probed). **OQ-D is the card the owner most needs: 裁-40's fourth switch is very likely unreachable on the G1 ceremony's own date** — three sources are built and `tax_prep` needs five unbuilt PRs plus an unscheduled evaluator deploy; the recommendation gives it its own later sitting without amending 裁-44. |
| `sst-engine-survey.md` | **live** | **F-T1** · the SST-engine ESTATE SURVEY · v1 — what exists (the `0016` A2.1 compliance-watch belt, the `0037` AR allocation anchor, `0022`'s live output-tax ties, the write-only `opening_items` SST trio, the MyInvois `tax_breakdown` fact) and what does not (registration, taxable period, rate table, SST-02, bad-debt relief, reverse charge, dual-registrant separation — each a search result, not an assumption). **§3 is the statutory citation table, re-verified against RMCD/AGC on 2026-08-23**, every row graded BYTE / VERIFIED / SECONDARY / UNVERIFIED with its URL and fetch date. |
| `sst-engine-design.md` | **live** | **F-T1** · **the design doc of record, §1-§4** · **v2, gate-folded 2026-08-23** (record: `sst-engine-gate-record.md`) — the registration + taxable-period model (incl. both DG-variation axes), the versioned deterministic evaluator family (payment basis on real AR allocations, s.11(2), bad-debt relief, sales-tax accrual, scope/DA-SA), dual-registrant separation, and the SST-02 producer with its per-field mapping and NIL rule. **Four statutory findings correct the contract's own framing:** s.11(2)'s twelve-month clock runs from the **service-performed date**, not the invoice date (a date the estate does not have) · credit/debit notes are **reg 11**, deducted in **the note's period** · a **retroactive** 2026 rate order (P.U.(A) 125/2026) proves the rate table must key on service date · there are **five** designated areas and **Pulau 1 inverts** the DA rule. **OQ-4 is now RULED (owner, 2026-08-23): the GL carries the deferral** — `sst_output_deferred` is credited at invoice for every payment-basis service-tax registrant and transfers to `sst_output` on allocation or the twelve-month day, whichever comes first; the ruling lands as PR-4b, a new D1 window sequenced after F-A2 PR-1. **The gate fold folds fourteen findings here and into the companion files below; four owner cards OQ-11…OQ-14 — ALL RULED 2026-08-23** (dispositions: `sst-engine-gate-record-part2.md`; design mechanisms: `sst-engine-design-part2.md` §5.5). |
| `sst-engine-design-part2.md` | **live** | **F-T1** · **design of record, §5-§12** · **v2, gate-folded 2026-08-23** (the boundary moved at the fold — v1 split §1-§5/§6-§12, part 1 crossed the 500-line ceiling, but every existing `§5.x` citation still resolves, only the file changed) — Clara's judgement as one-click classification/scope questions filed to `compliance_watches` and never to `open_questions` · the walls · **§7.3 the should-have-charged detector, TA-P11's dissolved-7A-R3 residual, whose live specimen is RPR** · the nine-PR build sequence · acceptance (the positive path is synthetic-only and labelled so; no client in the estate is SST-registered) · risks and named non-goals. **The gate-folded mechanisms this part references live in `sst-engine-annexes-2.md` (Annex F).** |
| `sst-engine-annexes.md` | **live** | **F-T1** annexes · **v2, gate-folded 2026-08-23** — **A** mechanics (the ordered `sst_threshold_schedule` ALTER incl. the NIL-threshold and per-item-grain defects, the full SST-02/SST-02A field inventory, the apportionment rounding rule worked, s.11(2)'s two arms, reg 19's evidence list, reg 11's ten particulars, the `statutory_deadlines` seed rows) · **B** the decision register, now **D-1…D-21** · **D** **the owner's questions**, 大白话-framed with options, a recommendation and the cost of being wrong, now incl. **OQ-11…OQ-14** · **E** the change log. **Annex C (the rig predictions) and the fold's Annex F now live in `sst-engine-annexes-2.md`.** |
| `sst-engine-annexes-2.md` | **live** | **F-T1** annexes 2 of 2 · **v2, gate-folded 2026-08-23, design-normative** — **Annex C** the rig-replay predictions (C-1…C-16, moved here when part 1 crossed the line ceiling — every citation still resolves; nothing here was replayed) · **Annex F, the mechanisms the PR-0 gate fold specified** (not commentary — every number originates in a versioned deterministic evaluator over DB-owned inputs, every unknown falls to a NAMED `not_evaluable`): **F.1** §3.2's three input conventions · **F.2** the deferral interlock + writer census · **F.3** the brown-field opening position · **F.4** the differential control's two operands (**never inferred from an account pair**) · **F.5** the imported-services posting home · **F.6** the `list_review_queue` re-cut (PR-3) · **F.7** the `get_context_pack` splice (PR-7). |
| `sst-engine-gate-record.md` | **live** | **F-T1 PR-0 gate record (2026-08-23)** — five fresh-context lenses (accounting · live-truth · security · law · build), every finding then re-attacked by an independent verifier that did not raise it (re-graded four, **REFUTED eight**). **Verdict: the item's SHAPE holds** — the anchor, the arms, the rounding rule, the walls-as-refusals discipline and the statutory work are unusually good — **but §3.2's three inputs were each wrong at the bytes**, and the same-day OQ-4 ruling was folded into four sections and left stranded in three others. **2 blockers · 16 materials · 5 nits CONFIRMED · 8 REFUTED.** Fourteen findings fold into v2 (above); **four were reserved to the owner as OQ-11…OQ-14, each blocking a PR — ALL FOUR RULED 2026-08-23**, dispositions in `sst-engine-gate-record-part2.md`. |
| `sst-engine-gate-record-part2.md` | **live** | **F-T1** gate record, continued — the 2026-08-23 owner-sitting dispositions for §6's four cards (OQ-11..OQ-14), split off `sst-engine-gate-record.md` at the 500-line ceiling. Section numbering unchanged (this file's content IS §6, continued). |
| `track-a-sitting-1.md` | **historical** (minuted-in-0074) | **The Track-A sitting record, part 1 of 3 (2026-08-22)** — the agenda as PUT TO THE OWNER, preserved with its member tables, which are the consequence map the F-A3…F-A9 designs read (each row says what a given option turns a specific question into). Carries the reading guide, the agenda order table with every ruling, and **TA-P1** (the OPEN REGISTER — law 71's roster becomes a reservation) · **TA-P2** (three number-origins + the governed policy-table door) · **TA-P3** (one purpose per processing class; the firm-level narrow purpose; C6 to critical path) · **TA-P4** (mechanically-bound receipts; the DEFINER read wrapper) · **TA-P5** (the clock; law 21 narrowed to posting belts). Rulings are minuted in `docs/adr/0074-the-track-a-sitting.md`, which governs. |
| `track-a-sitting-2.md` | **historical** (minuted-in-0074) | **Sitting record part 2 of 3** — **TA-P6** (walls re-aim at the DIRECTING human; `agent_prepared`; the solo arm) · **TA-P7** (**attribution becomes the agent's JUDGEMENT under structural walls** — the constitutional amendment, with the orchestrator's DB-judged-corroboration dissent) · **TA-P8** (learned identifiers are context, never keys; the promotion door) · **TA-P9** (the DECIDED freeform read surface, closing audit GAP5-5) · **TA-P10** (sandbox exports with a byte-burned watermark; the covered-recipient cross-client test; aggregates narrative-only) · **TA-P11** (the one-architecture TEST; the bank rules machine retires whole, superseding WCC-R5's bank arm; 7A-R3 + E-R13 recorded dissolved). |
| `track-a-sitting-3.md` | **historical** (minuted-in-0074) | **Sitting record part 3 of 3, and the live half of the register** — **TA-P12** (the brake census; three gates REMOVED, the concurrency floors kept) · **TA-P13** (ONE metering ledger; `client_id` added now or never) · **TA-P14** (closed-loop DONE: repair your own gates, build the minimal human door, a durable proposal carrier, acceptance on real books, "end to end" rewritten, lost records treated as lost) — **plus the residue R-A … R-G**: the 60-day figure (the sitting's only standalone question), the 71 design-layer questions per project, the per-project human-act lists carrying **the 2026-08-22 test-data delegation** (the agent may perform law-71 human acts as the owner's delegate through the real audited doors, ledgered — e-filing excluded by its nature), the seven refused questions that never come back, the five conflict adjudications, the doc-staleness sweep, and **R-OWNER**, still open. |
| harness-audit-2026-08-23.md | **live** | **The harness drift + unrecorded-deferral audit (2026-08-23)**, pinned to `origin/main` at #291 and read via `git show <SHA>:<path>` throughout (the local tree was shared with concurrent lanes). **(A) the two-home claim does NOT hold** — ~18 real forward-looking obligations carry no `PROGRESS.md` row at all (mostly unruled owner-questions inside already-GATED v2 design/gate records for F-A3/F-A4/F-A8/F-A9/F-T3, plus three DR/incident follow-ups) and ~5 more sit in Lanes/Next/posture rather than Backlog or Known issues. **(B) the drift table** — 25 load-bearing harness claims re-derived at the bytes; the live ones were the CI runner count (2 → 4, trued in the same PR as this report) and the AGENTS.md constraint-12 reading (owner ruling pending); the rest CONFIRMED, incl. the PRD/ARCHITECTURE as-built caveat against `0018:57,62`, the lint-script comment, the db ledger, the registry pointers, harness-menu file existence, index ↔ active coverage and every MEMORY.md pointer. Closes with the ambiguities the repo alone cannot settle. |
| `harness-audit-rulings-2026-08-26.md` | **live** | **The owner's rulings on all nine cards raised by the 2026-08-26 harness-alignment batch** — minute style, one section per card (R1-R9): the judgement-confidence conjunct's future drop · the PRD two-tier reporting wording (SHAPE only, exact text awaits word-by-word review) · G1's owed ADR · three digest addenda (the #352 closed-wave-floor law, the evaluator two-halves ceremony, the four-runner CI expansion's CONFIRMATION closing the 08-23 audit's AMBIGUITY #2) · a new (not yet created) docs/ops/ceremony-practices.md · the F-A7b joint onboarding gate + the three-tier firm model (tier-3 self-serve LIVE AT BETA, conductor's dissent on file) + the pricing shape (amounts deferred to a sitting) · the `/ready` storage probe (磨合) and PITR (HELD again, beta-prep-triggered). `PROGRESS.md` and `frontend-handoff-addendum-2026-08-24.md` carry the resulting pointers; this file is the argument. |
| `r2-prd-two-tier-wording-draft-2026-08-27.md` | **historical** (superseded) | Draft wording approved verbatim by 裁-30 and landed in `docs/product/PRD.md`; kept as provenance. |
| `wave-f-lane-brief.md` | **live** | **The operating brief every Wave-F build lane reads before its first write** (landed from the sprint's session scratch so it survives the session). Grounding order · the throwaway-rig protocol and the **measured** rule that `pnpm db:migrate` SKIPS an `UNNUMBERED_*` file (a green migrate is not evidence your migration ran; keep the numbered copy on disk for the whole rig session or stage it outside the repo with `CLARA_MIGRATIONS_DIR`) · the authoring laws (prosrc-SHA prestate pins, behavioural walls, forced cells that never degrade to a note, three-valued evaluation) · the shared-surface announce protocol · **the three tenancy traps measured this sprint** (a NULL `firm_id` under `firm_id = jwt_firm()` is hidden from everyone; never infer a scope from a NULL; an owner-run plain view over an RLS+FORCE table leaks cross-tenant, and `security_invoker` breaks a nested ungranted shim instead of fixing it) · **the evaluator freeze bites at MERGE**, over every registered row, with undeletable rows · git and reporting discipline. |
| `freeform-read-law28-review.md` | **live** | **F-A6's law-28 adversarial pass (2026-08-23)** — the contract-obligated independent attack on the audited freeform read, run as a native fresh-context lane after Codex's cyber filter refused the prompt twice. **Verdict: BUILDABLE WITH THE LISTED WALLS.** Three blockers (the `pg_catalog` residual was accepted on the premise that the agent surface is *curated typed reads* — F-A6 IS raw SQL, so the acceptance's own ground fails · `query_to_xml` is a second execution engine that returns a whole table in one row past the caps while the plan names no relation · `cross_client_unavailable` has no producer, so the pin silently filters and the model gets a single-client answer to a two-client question), five HIGH (the payload can read the wake secret; the deadline is unenforceable inside one FETCH; an advisory lock blocks the firm's writers and `reset all` does not release it; the plan census cannot see catalog reads via functions), and what HOLDS — all 177 policies role-pinned, no dblink/fdw/pg_net, the single-statement wall real. |
| `internet-lane-law28-review.md` | **live** | **F-A8's law-28 leg-2 pass (2026-08-23)** — the prompt-injection and untrusted-content attack on the internet lane. **20 findings, ALL CONFIRMED, none refuted**; folded into design **v3** as IL-D17..IL-D34. The finding that re-cut the item: v2's "evidence" was a model-authored quote — no raw bytes and no digest were stored and no destination registry existed, so a model could cite a fetch that never happened and a hidden instruction could move a rate past the owner's approval card. |
| `wave-f-sprint-dag.md` | **historical** (executed) | **The sprint plan of record** — every remaining Wave-F PR as a dependency graph (adjacency list + mermaid), the critical path in hours with the chain named, the parallel build waves with per-lane base branch / rig port / shared-surface conflicts, the **58-position merge train** with a reason for each contested position, the **five consolidated ceremony windows** with the bodies each carries, the owner acts that survive scrutiny (and the four that do not), the top risks with mitigations, and an honest hour range. Written before the first build lane opened; the train order it fixes is the one `wave-f-sprint-handoff-2026-08-23.md` reproduces. |
| `wave-f-sprint-handoff-2026-08-23.md` | **historical** (executed) | **Wave F · the graph-engineered sprint handoff (written at the owner's 2026-08-23 pause):** estate state · every build lane by BRANCH with its content and next step · the merge-train order and ceremony windows · the rulings minted (owner + R-L19..R-L26) · the owner sitting cards · the standing rules learned · the resume path in commands. |
| `roadmap.md` | live | Wave F/G forward roadmap (re-scoped two-track at ADR-0071), the top-8 risks table, and the Phase-5 verification plan — carried from REBUILD-PLAN at its deletion (ADR-0069). |
| `mohe-session-handoff-2026-08-26.md` | **live** | **The 磨合-session opener — the Wave-F conductor's dated bridge into the state authority.** Clock-in read order · the world as MEASURED on 2026-08-26 with re-prove commands (live 131/`0136`, card-1 both stages lit, serving bundle = chatTurn_v13/autoDraft_v9 with v14 NOT serving) · the widened charter (three-tier firm model, F-A7b joint gate, pricing shape, storage probe, PRD two-tier wording task, G1 ADR + digest addenda) · the pre-flight checklist (v14 runtime deploy first) · the disciplines and standing owner cards. Written under `.claude/rules/handoffs.md`: no session-local identifier is a resume step. |
| `mohe-session-handoff-2026-08-27.md` | **live** | **The 磨合 mid-window handoff (2026-08-27 compaction point)** — the merged set to that hour (#362-#365), the in-flight lanes with tips and freeze notes (the p3-finale seam truings · f-a4/pr-1c under double review), and the resume paths in files and commands per `.claude/rules/handoffs.md`. |
| `frontend-sprint-handoff-2026-08-31.md` | **live** | **THE HANDOFF OF RECORD — the beta sprint's opening document for the next Claude Code session (裁-82 as amended by 裁-85), 2026-08-31.** Written for a reader with no session: clock-in order · the world as measured with re-prove commands (live 148/`0153`, repo `0155`, Fly v69 `chatTurn_v15`, `app.clarabook.com` still the Pages-deployed dashboard — Git-connected, so every push re-deploys it until disconnected) · the thirteen open PRs' dispositions as of 2026-08-31 (finish six, park six, one stays — **all thirteen are now CONCLUDED: the six finished, the six parked were ARCHIVED under 裁-123, and the one that stayed has merged**; the count is that day's measurement, not a live one) · the runway FS-0…FS-12 with sizes · the inherited laws (rung-0 at the live body, RED-before, the review ladder under 裁-84, the deferral-row law, the 3–4 heavy-lane cap) · the owner's own acts · the definition of beta-ready. |
| `frontend-sprint-handoff-2026-08-31-orders.md` | **live** | **The self-contained orders behind the handoff** (inheriting the P4 orders' §0 preamble): §A the lean ladder under 裁-86 (dispatch by fit · the Playwright e2e leg · one opus review) · §B the Codex MCP mounts, DORMANT under 裁-133 · **§C the lane laws** — SPLIT 2026-09-03 at the file's 500-line ceiling: the 2026-09-02 clauses moved byte-for-byte to the sibling row below and §C carries a pointer paragraph where they stood, while §C itself keeps the 09-03 clauses and every clause minted after (native lanes only 裁-133 · the ORIGIN half of PORT-ASSIGNMENT and its vacuous-needle corollary · ARTIFACT-IDENTITY, BINARY-SAFE-GREP and BARE-ROUTE · BACKGROUND-VERIFIER incl. the SATURATION half · PORTABLE-INSTRUMENT · MUTANT-PRODUCES-THE-VALUE · ONE-TEARDOWN as amended · the CONDUCTOR law's OVERTAKE and read-the-state sentences · MECHANICAL-KEEP-BOTH · ONE-AUTHOR · the image-not-closure deploy lock · the predicate-copy cell · STATE-BANNER and STATE-LINE — with the archived 09-02 set: the PROCESS LAW, PID-not-name · the fold-round mutant panel, its buffer-restore rule and "read a RED for its REASON" · PRINT-THE-THING and its `# fail 0` clause · the eight-item MERGE-FORWARD LAW · MERGED-TREE · GATE-SHAPE · ARMED-PR · FROZEN-TREE · ABSENCE-CENSUS · STATE-BANNER · TWO-CLOCK DATE — every lane inherits them beside `fe-train-plan-2026-08-30-orders-p4.md` §0.5) · FS-0 the live-catalog census that re-measures 裁-72 · FS-1…3 the P4 stack's remaining rounds with their bars restated · FS-4 the checkout/signup-gate train (Stripe Checkout → signed webhook → `firm_admissions` → `create_firm`; design gate + security review first; the DPA e-sign, the email-bound token, the rate wall's sitting) · FS-5 the interview-runner port (the four live runtime routes, the escalated-thread shape) · FS-6 #462/#454 · FS-7 report + close-prep chat tools as `chatTurn_v17` + F-A5b PR-3 · FS-8 P6-T shell + the honest-note sweep · FS-9 the third conformance pass · FS-10 P6-X amended (the Pages Git integration disconnected first) · FS-11 the reduced Wave G · FS-12 the harness duties incl. the PR-2 truings a docs-only PR could not touch. |
| `frontend-sprint-handoff-2026-08-31-orders-c-archive-2026-09-02.md` | **live** | **The §C ARCHIVE — the lane laws minted 2026-09-02, moved verbatim out of the orders file on 2026-09-03** when it reached the repo's 500-line document ceiling and the afternoon's clauses had nowhere to land. **They BIND exactly as they did in §C:** a lane that inherits the orders inherits this file with them, and §C carries a pointer paragraph where they stood. The set: the PROCESS LAW (kill by the PID you captured, never a name or a CommandLine substring) · the FOLD-ROUND DELIVERABLE · the MUTANT-PANEL RESTORE LAW (buffer-restore on an uncommitted tree, never `git restore`) · PRINT-THE-THING · the eight-item MERGE-FORWARD LAW · MERGED-TREE · GATE-SHAPE · ARMED-PR · the two standing riders. Byte-identical to the source (md5 `f183c4b7bb9095fb26f5ac10727351ad`, 6,733 bytes, proven on both sides of the move); nothing was re-worded, re-ordered or re-dated. Path-stable from here, per the convention at the top of this index. |
| `mohe-grill-rulings-2026-08-31.md` | **live** | **The fourth ledger — the 2026-08-31 DIRECTION and 裁-73…裁-93.** 裁-73 a beta firm is created the standard-SaaS way (Checkout → signed idempotent webhook → one admission → `create_firm`) · 裁-74 an unpaid signup parks on the holding page, never deleted, no reminder · 裁-75 裁-72 is re-measured on a live catalog before it is amended · 裁-76 the G1 clocks are post-beta, 裁-59 amended · 裁-77 reports get a chat opener + the PDF worker + the twelve close wrappers as chat tools — the only new backend the pivot admits · 裁-78 the interview runner is ported before the cutover · 裁-79 finish #462 + #454, park six · 裁-80 Track B paused, the Tax tab an honest shell · 裁-81 Stripe entirely Codex's, TEST first, keys never in the repo · 裁-82 Codex leads · 裁-83 the reduced Wave G stays · 裁-84 the review leg is a second fresh Codex read-only + the owner (dissent on file). **The second sitting (~13:00 MYT): 裁-85 the seat stays in the Claude Code session, lanes by the orchestrator-fable philosophy · 裁-86 the lean ladder — one fresh-context opus leg + a real-browser Playwright e2e leg on every frontend train, law 28 kept · 裁-87 Stripe's account-level objects through this session's connector, lanes build the code.** **The evening sitting (~19:50–20:40 MYT): 裁-88 TEST-mode beta, KYB at the launch sitting · 裁-89 G1=(B) the folded admission door, 裁-73 amended · 裁-90 the beta DPA draft delegated, lawyer-swap at launch · 裁-91 `stripe_events` as a redacted projection · 裁-92 the 6-digit-code confirmation · 裁-93 the package (remaining questions per recommendation; ADR-0077 signed; the CSRF deferral endorsed).** INFORM: 裁-69 was already executed at `0143`; native capacity is back; honest notes for paused lanes are lawful. |
| `mohe-grill-rulings-2026-09-01.md` | **live** | **The fifth ledger — 裁-95…100 (the 2026-09-01 morning sitting) + a session state bridge.** Direct continuation of the `-08-31` chain, same minute style; the bridge block is the compaction-safety asset the day's later sittings resumed from. |
| `mohe-grill-rulings-2026-09-01-pm.md` | **live** | **The sixth ledger — 裁-101…113 (the 2026-09-01 afternoon + evening sittings) + two state bridges.** Highlights: 裁-107(+b) the bidirectional seam↔door table lives in the PR BODY and door claims cite its rows · 裁-108 an UNNUMBERED migration never applies — the number claim at merge prep ARMS the tests (the four-step law) · 裁-109 the N1+N3 confirm-surface fixes are BETA-GATING (the owner's most-conservative pick) · 裁-111 the cross-family review leg SUSPENDED until beta live (law 28 time-boxed, not repealed) · 裁-112(a/b/c) the overclaim law family (construction-path scoping · model-as-oracle · duplicated-predicate) · 裁-113 the C-2 wiki-gate ruling: rewrite + CHECKED waiver, decided by EXECUTING the checker with a fabricated declaration. The evening state bridge is the post-compaction resume map. |
| `mohe-grill-rulings-2026-09-02.md` | **live** | **The seventh ledger — the pre-pause truing sitting + THE 2026-09-02 CHECKPOINT SITTING (裁-115…131).** The checkpoint section: 裁-115 frontend-first re-confirmed · 116 gate bundle A (tasks #14/#15/#16 + P6-3/4/5/6; P6-R idle-lane only) · 117 the four parity holes gate beta, one thread per altitude · 118 FS-7 echelon 2 PDF gates beta · 119 the Q-D6 wall before beta · 120 the extra opus security pass (delivered: `security-pass-2026-09-02.md`) · 121 cutover prerequisites (capability diff · OPS.x · `is_operator` at the reset) · 122 the Wave-G checklist re-walked with proofs · 123 disk + the parked backend queue ARCHIVED · 124 tokens kept, `.codex/config.toml` tracked, .agents/ ignored · 125 legal texts are agent templates for beta · 126 Stripe sandbox + key + Product/Price · 127 owner-batch post-beta · 128 the apply-standard-chart button · 129 the beta terms (separate kind, RM 5,000 floor, KL courts) · 130 inline clarify answer + composer attach IN · 131 Email OTP expiry 60 minutes for both arms, C4 amended.  裁-110 formally RESERVED (closing the 109→111 numbering jump; the cross-package test-guard proposal must be authored into the ledger before its owner sitting) · 裁-114 the PRD §6 truing (owner: both texts corrected — the credential law re-stated as "no service credential ever reaches a browser; apps/web's server-only Route Handlers are a second isolated holder", and invariant 16 split into 16(a) typed-consent document egress / 16(b) trace export). Context: the combined pause window (vhdx + the owner's Claude Code update) and the seven-lane drift-scan sweep behind the docs/harness-truing-2026-09-02 branch's PR. **Continued at 裁-132 by `mohe-grill-rulings-2026-09-02-pm.md`** — this file was split there at the 09-02 pm/night truing, at the 500-line ceiling; 裁-129…131 stayed here because that is where they landed (#506). |
| `mohe-grill-rulings-2026-09-02-pm.md` | **live** | **The eighth ledger — 裁-132…145, the afternoon and night of 2026-09-02** (moved byte-for-byte out of the seventh at its 500-line ceiling, as `-09-01-pm` continued `-09-01`). 132 streaming reply text is POST-BETA (settled-only thread + an honest progress indicator) · **133 NO Codex lane of any kind until beta live — native lanes only**, after three capacity/kill failures in ninety minutes · 134 the self-hosted concurrency cap, no spend, repo stays private (the orchestrator's recommendation) · **135 the owner OVERRIDES it: the repo goes PUBLIC and CI moves to GitHub-hosted runners**, executed in the ruled order (hosted migration → full-history secret scan → the fork-PR approval wall → the flip) and superseding 134 · 136 the sealed text hash must cover the burned watermark, changed NOW while nothing is sealed · 137 the wordmark renders lowercase glyphs, the name in prose is ClaraBook · 138 P6-3's four owner-visible look changes all ACCEPTED, ruled on measured before/after screenshots · 139 a firm member is REFUSED at checkout before Stripe is called · **140 how a sprint ruling reaches the ADR system: a digest row + an "amended by" line, never a new ADR** · 141 the ⌘K "Do" palette's allowlist shape + a required drift guard (amends 裁-37). **Extended 2026-09-03 by #535 with the §9 sitting's first three: 143 the OPERATOR tier and its hard cap into PRD §2/§4 · 144 billing's tier tranche to the backlog, completed before 上市 and not before beta live · 145 PRD §9.3's signup gate names FIVE items, the email-bound token retired.** **Continued at 裁-146 by `mohe-grill-rulings-2026-09-03.md`** — split there at this file's own 500-line ceiling. |
| `mohe-grill-rulings-2026-09-03.md` | **live** | **The ninth ledger — 裁-146…150, and NO LONGER the newest (continued at 裁-151 by `mohe-grill-rulings-2026-09-04.md`) — 裁-146 onward, the 2026-09-03 afternoon sitting** (opened at the eighth's 500-line ceiling, exactly as the eighth was opened at the seventh's; 裁-143/144/145 stay in the eighth because that is where they landed, #535, even though they were ruled on 2026-09-03). **146 Supabase Auth's SMTP is pointed at Resend — signup confirmation and password reset ride Supabase's own mailer over CUSTOM SMTP into Resend, invitations ride the Resend API from the server-only invite route, the sender is a no-reply mailbox on the one Resend-verified domain, and the cap is the Resend plan's rather than the default mailer's 2/hour (team-only, no SLA, "not meant for production").** Its operative half is a LAUNCH GATE: Wave-G's "Mail" line certifies only after a real signup confirmation is sent to and RECEIVED at a NON-team address through the custom SMTP — never on a settings screenshot. Carries the sitting's dashboard readings at their true weight (the verified sending domain and the SMTP configuration MEASURED; the rate-limit raise and the template re-confirm REPORTED without read-back, 裁-112) and corroborates 裁-145's `btw` half on the confirmation call site. |
| `mohe-grill-rulings-2026-09-04.md` | **live** | **The TENTH ledger — NO LONGER the newest (continued at 裁-186 by `mohe-grill-rulings-2026-09-04-pm.md`) — 裁-151…185**, opened at the ninth's 500-line ceiling as every earlier file was. Thirty-five rulings across one night: the **23-decision ceremony grill** (裁-151…173, one question per turn through `AskUserQuestion`, the recommendation first and the cost stated) · the two the P6-X suite classification forced (**裁-175** the XML `blob:`-open exposure to a Known-issues row, **裁-176** each of the twelve ④ suites ruled before #540 merges, with all twelve dispositions in a table) · the rulings the ceremonies produced as they ran (**174** FS-10 opens tonight · **177** the reset runs with no dump and no restore-proof · **178** the two leaked role passwords are NOT rotated · **179** the six lane DSNs go to `uselibpqcompat=true&sslmode=require`, verify-full first after beta, after a probe found four of them in PLAINTEXT · **180** the real Rome/BEE corpus is the walk's data · **181** the onboarding interview should PROPOSE and still ask the human to confirm · **182**/**183** the client-consent letter and the grant, walked through the audited door because the surface is dark · **184** walk the bank, the close and the reports tonight) · and **裁-185, GO for a CLOSED beta**. **EIGHT went AGAINST the lead's recommendation**, each carrying its dissent line. The TEXT OF RECORD for 裁-151…177 is one file each under `docs/plan/completed/owner-rulings-151-177-2026-09-03/`, which governs on divergence. |
| `mohe-grill-rulings-2026-09-04-pm.md` | **live** | **The ELEVENTH ledger, and the NEWEST — 裁-186…197, the repair session (2026-09-04 from ≈09:00 MYT, into the small hours of 2026-09-05)**, opened at the tenth's ceiling (480 lines). **191** (≈12:30) two arguable document kinds — tax correspondence, agreement/contract — are codeable in the close gate's codeability table (a data ruling) · **192** (≈12:40) the browser smoke on the built app becomes a REQUIRED per-PR CI gate beside the per-train acceptance walk (amends 裁-86; the two known checkout-walk flakes fixed at the cause first) · **193** (≈16:45) the chart of accounts may be applied only AFTER `commit_client_onboarding`, settling 裁-23 Q5's ambiguity (AGAINST the recommendation, no dissent filed; the lead's mechanism note puts the refusal inside `apply_coa_template`, which never consulted the plan) · **194** (2026-09-05 ≈02:50) the 裁-149 clause-2 PREMISE correction is accepted — the leader always had an error listener and a reconnect loop, so it was never crash-loud, and it stays byte-untouched · **195** (≈02:50) REQUEUE-ONCE: a human's real ANSWER to a coder-opened `sweep_refusal` question un-parks the registry and re-mints the draft, a dismissal does not · **196** (≈02:50, all four taken) boot refusal on `sslmode=no-verify`, a dead non-runtime DB lane fails `/ready` (AGAINST the recommendation, dissent filed, with the lead's implementation reading recorded), the sales-lane switch keeps `p_reason`, and the SST threshold schedule gains a firm-user read · **197** (≈02:50, all three taken) three product tickets queue after the nine, ordered (iii) → (ii) → (i). Opening rulings: **186** client AI consent becomes a firm-level declaration made once at the DPA stage, every client of the firm consented automatically (AGAINST the recommendation, dissent filed) · **187** every attestation ceremony and maker-checker wall abolished, basic RBAC the only human gate (bookkeeper approves and posts any amount including own drafts, close is admin+, members and legal signatures owner-only), automatic receipts kept at zero ceremony · **188** the wall-removal DB lane runs this session, after the P0 block · **189** the runtime v75 and web Worker deploy ceremonies are run by the lead as the owner's delegate, receipted · **190** native lanes only for this session. 186 and 187 are minuted as ADR-0078; this file is the text of record and governs on any divergence. |
| `beta-handover-2026-09-04.md` | **live** | **THE HANDOVER OF RECORD — the document the next session opens on (裁-185, 裁-150).** Written for a reader with no session: (A) the posture in plain language — beta LIVE and CLOSED, the Worker version and the deleted Pages fallback, runtime v71 with #533 merged-but-not-serving, the re-cut books pins · (B) the honest milestone tally of the beta-live walk, **6 PASS · 1 PARTIAL · 2 FAIL · 2 NOT REACHED of the ELEVEN enumerated milestones**, with the agentic section scored by 裁-164's own verdict weight · (C) **the complete Backlog and Known-issues list — P0 (14 rows, before the first EXTERNAL applicant) and P1**, each row carrying what was measured, why it matters, the fix shape, a size guess, an owner, a ruling number and a tier · (D) the harness notes for the next session (clock-in order, the ledger chain, the 500-line WRITE-BLOCKING ceiling, `PROGRESS.md`'s single author, the DSN bridge and its destroyed sleeper, native lanes only, the frozen-workflow rule, the EXPIRED test-data authority) · (E) the ordered pick-list. |
| `beta-handover-2026-09-04-part2.md` | **live** | **The handover's part 2** — the **22 P2 hygiene rows** from the beta-live walk (each seen on screen, in a console or in a gate result — including the two close-gate observations, the vacuous no-statements pass and the still-offered Finalize over two UNKNOWN gates), and **§C.4, the CARRIED registry C-17…C-76**: every Backlog and Known-issues row that was open before the launch night, re-expressed by area with an owner, a next step and a tier, closing two by measurement. The pre-truing text of both `PROGRESS.md` sections is archived verbatim in `docs/plan/completed/progress-archive-2026-08-part8.md`. **Continues in `beta-handover-2026-09-04-part3.md`.** |
| `beta-handover-2026-09-04-part3.md` | **live** | **The handover's part 3**, opened at part 2's 500-line ceiling: **§C.4's continuation, C-77…C-88** — twelve rows carrying the fourteen still-open items a fresh-context review measured MISSING from the first cut (ten as rows, four as named dispositions) (the 09-01-pm standing follow-up ledger's ten items, P4-7's magiclink arm, the FS-0 census's eight NO-HOME dispositions, `revoke_invite`'s lock order, the δ and η residuals, the 2026-08-23 harness audit's ~18 unrecorded obligations, the wiki dynamic-SQL comment gate, F-A7's γ residuals, the 2026-08-29 dawn-review successors, the Mobbin pass, and four small carried sets) — plus **the note on what deliberately STAYED archived** (the `__Host-clara-auth` HTTPS line CLOSED by measurement; the worktree-isolation and junction-safe LAWS moved to the handover's §D; the dangling doc paths inert) and **§C.5, the seventeen documentation truings the rulings ordered that the final truing did NOT execute**, each naming its file and what the line must say. |
| `security-pass-2026-09-02.md` | **live** | **The 裁-120 security pass (opus, fresh context, own rig 0001→0161)** over PR #493's nine objects, the runtime authz + chat routes, and the confirm surface as shipped. NO BLOCKER; nine doors held under its own battery (cross-registration, foreign/consumed/unpaid payment, second-firm mint, concurrent double claim, agent identity, append-only, the per-address OTP keying); eight MATERIAL — A-M1 `open_checkout_intent` not idempotent (a double click = two Stripe sessions), A-M2 the JWT-email wall was a null check, A-M3 the OTP-wall route contract must be ONE endpoint with `attempt_id` never on the wire, A-M4 a paid applicant who joined another firm is stranded with no operator read, A-M5 `livemode` stored never read, B-M1 the rail re-points to a colleague's newest shared thread, B-M2 thread↔client mismatch renders under the wrong client, B-M3 SSE never re-authorises for 30 min — and an 11-line FS-10 cutover checklist. A-M1/2/4 fold into #493, A-M3/5 + B-M3 into C-5, B-M1/2 into the parity-holes lane (裁-117). |
| `clarabook-conformance-pass-3-2026-09-02.md` | **historical** (signed off 2026-09-03) | **FS-9 — the third ClaraBook conformance pass, SIGNED OFF 2026-09-03 @ `5d70b8dd` as P6's entry gate (裁-9).** The head banner carries the verdict and the per-line table — **19 lines MET · 3 OWED (owner) · 2 deferred by ruling · 3 carried open (non-gating) · 2 not FS-9's (FS-10/FS-11)** — every verdict re-measured on that tip with its instrument named beside it, and the table **supersedes the inline `dfe9406c` marks in §3/§5 wherever they differ** (#519 landed between the two tips). The re-fetch condition is discharged in the banner: the design authority's `main` is still `a7709883` and its PR #2 is still open. **What the sign-off does NOT close, and says so:** 裁-64② and R3 §9's two recut PRs to the authority repo (the owner's, a launch-sitting item) · the DS-07 control-height question, raised to an owner DECISION — which artifact is authoritative — because it has no home in any other harness file · and three non-gating carried lines (DS-09, the ⌘K by-name client jump, DS-15's unnumbered token recuts). All four owe a `PROGRESS.md` row under ADR-0075 §6; that is the sign-off's one carried obligation. Cite-only below the banner: §1 consumed (zero raw-colour violations, byte-identical fonts, the contrast gate green on every pair the gate file declares and next-intl coverage across the components tree — **count the tree, never this row**: both figures were true at the record's own pin and neither is re-measured here (the denominators grow with every train), the two-level IA, page-level client-switch clearing, all 26 part kinds, the door layer, the credential wall) · §2 diverged by ruling (Q2/Q3/Q5/R7/裁-95/R3/裁-64③/裁-2 4c/裁-117, and **裁-137** — the wordmark renders lowercase glyphs while the name in prose is ClaraBook) · §3 owed by train (P6-3's token/a11y debts + five new a11y items, P6-4's narrowed money-parser gap, **P6-5 CLOSED (#519)**, **P6-6 CLOSED (#514)**, tasks #15/#16 strengthened then closed, client-name chrome, **error boundaries + password recovery DONE (#507)**, the control-height question now the banner's owner-decision line) · §4–§7 unruled violations, the three FS-9 acceptance lines (**DONE — #514**), six findings the adversarial pass REFUTED, the could-not-verify list. Written while the 裁-116/117/128 citations were on this branch only — the lane's refusal to launder them is kept as a positive control. |
| `dashboard-web-capability-diff-2026-09-02.md` | **live** | **The dashboard→web capability diff (裁-121①) — the FS-10 cutover precondition.** All 18 `apps/dashboard` routes, door by door, against `apps/web`: ~85 doors/reads SAME (several relocated per the two-level IA) · 7 DROPPED-UNRECORDED (`remap_bank_account_coa` uncalled · adjustment templates' `p_replaces`/`p_schedule` hardcoded null · inline clarify answering relocated out of the thread · no composer attachment · the onboarding plan's revision history unread · the document-tied opening-balance parse path unreachable · the chat session list/switcher) — **裁-130 rules the clarify answer + the attachment IN for beta, the rest post-beta with Backlog rows, the switcher by 裁-117** · 1 SUPERSEDED-PENDING-BUILD (`create_firm` → `claim_paid_firm`, C-5/Lane B) · 2 HONEST-NOTE · 3 DASHBOARD-ONLY-BY-DESIGN · the five flat receipt-card kinds as recorded design debt. |
| `archive-parked-lanes-2026-09-02.md` | **live** | **The parked backend queue ARCHIVED (裁-123) — the re-integration map.** #447 #448 #452 #456 #449 #460: each round WIP-committed to a pushed ref, the verify-bar notes posted as a resume comment, the PR CLOSED, the worktree removed; plus the #482 correction, two dead lanes' refs, the preview-stand scripts, and the four things that could not be removed (two OS-locked worktree shells, one locked scratch file, the still-running 08-31 session's job dir — NOT retired). Re-integration is post-beta, one lane each, from the PR comment. |
| `mohe-grill-rulings-2026-08-27.md` | **live** | **The 磨合 OPENING grill — RULED IN FULL (Q1-Q9 + Q-A…Q-F), the align-before-code charter.** The production app is `apps/web` in THIS repo; the Codex `clarabook-frontend` output is DESIGN LAW + prototype evidence · rail-first with thread escalation, never a modal · the two-level IA with client-switch as a security event · the ClaraBook brand system RATIFIED (42/42) · i18n three layers (statutory BM+EN day one) · desktop-first + a mobile decision corridor · WCAG 2.1 AA with three CI gates · catalog 18 live + 4 in one batched wire bump · the per-journey DONE formula + the P0-P6 order. |
| `mohe-grill-rulings-2026-08-27-evening.md` | **live** | **The evening batch, R1-R7** — ClaraBook brand + the Ledger Fold mark adopted (Clara stays the agent persona) · identity-canvas on entry pages only · the focus ring unified on shadcn (§9 founder amendment; recut PR owed to clarabook-frontend) · primitives build-on-demand · Mobbin MCP into `.mcp.json` riding the next code PR · the admin-floor draft-vs-sign law · F-A7b onboarding = in-thread interview. Each ruling names its execution owner and window. |
| `clarabook-resource-audit-2026-08-28.md` | **live** | **The ClaraBook handoff resource audit — consumption VERIFIED at the bytes (2026-08-28).** Two-pass lane over the handoff repo's prescriptive docs vs the apps/web tree: **consumed with unusually high discipline** (tokens byte-cited · Emil motion per contract §7 · 8 skills tracked · both MCPs · WCAG gate set); all divergences on file as rulings. Dispositions: the Clara **mascot** gap recorded → P6 + owner batch; the `ClaraBook*` naming API ruled as-conducted NOT BINDING. Its one batched owner question — **WCAG 2.2 target-size (2.5.8)**, mandated twice by the source contract, untested, arguably dropped by Q7's 2.1 bar — was **RULED 2026-08-28 (裁-13): ADOPT AT P6 as a real a11y CI gate** in `apps/web/test/a11yRules.ts`, honouring the contract's `--target-min` documented-exception mechanism, with Q7's 2.1 AA bar unchanged. The §4 row is CONSUMED-AT-P6, not open (heading trued 2026-08-29). |
| `mohe-grill-rulings-2026-08-28.md` | **live** | **The night batch, 裁-1…裁-22** — the 70% focus-ring alpha + Button treatment · the entry-face trio (white card on identity-canvas · the waiting page as the FOURTH entry face · the `--input` token recut initiated in the clarabook repo) · the three-tier polish model (conformance now, flow polish + identity flourishes at P6) · the MCP grounding pair adopted (Mobbin/shadcn live; flow-video pass to backlog) · P5 clarified = F-A7b's gate, ran early, CLOSED · port wave = ruling A (cross-ref) · the FULL-PRODUCT assurance frame + the new `.claude/rules/db-migrations.md` frontend-home rule. **裁-12…裁-21 are the ten-item sitting** (`create_account_set_v1` retired · WCAG 2.2 target-size at P6 · the mascot · the `security_barrier` pass · hash-only bearer tokens · seeding proposals into the firm inbox as a ninth `row_kind` · the signer≠proposer wall + Clara as binding PROPOSER · **裁-19 the merge re-home + un-merge door** · the sweep-receipt card · a firm-level standard SME chart of accounts), and **裁-22** makes agent proposal bases DB-resolved citations. The closing block is the pre-beta queue this sitting minted. **EXTENDED 2026-08-29 with 裁-23…裁-28, the morning sitting**: 裁-23 the COA template's twelve (research-derived, owner review waived; Q2 overruled; Q6/Q8/Q11 widened) + the maintenance model · 裁-24 the merge re-home ruled PHYSICAL in the append-only shape, a HYBRID with the read layer kept and the orchestrator's dissent on file · 裁-25 the binding-proposal gate's eight (G6 OVERRULED — the post-time re-check restored; G7 WIDENED — the expiry sweep rides; five PRs) · 裁-26 admission tokens bind to an email at issue (P4 UI tranche) · 裁-27 "Amend resolution" on a resolved onboarding item (P6) · **裁-28 the pricing amounts NOT ruled — the owner brings his own plan; a cost-floor + market-band brief is owed before P4's checkout wiring.** The file closed at its 500-line cap; the ledger continues in `mohe-grill-rulings-2026-08-29.md`. |
| `mohe-grill-rulings-2026-08-29.md` | **live** | **The second ledger, 裁-29…裁-43** — 裁-29 the backend residuals re-censused item by item (F-A9 PR-1A was STALE; runtime has zero callers of `wake_freeform_read`; G1 sources `enabled=false` on live; the formal seal chain never exercised) and **ALL EIGHT 🔴/⚠️ items PULLED INTO the sprint before Wave G** (G1 flips · F-A6 PR-2 · reports chain + F-A5b PR-3 · F-T3 · F-A9 PR-1B · F-A4 PR-2b · F-T1 · F-T2 rows; F-A8 / F-A6 v2 / F-T4's remainder beta-era) · 裁-30 the R2 PRD two-tier wording approved verbatim and landed. **THE 08-29 EVENING SITTING (裁-31…裁-34):** Wave G's acceptance evidence **RE-SCOPED to the desktop corpus as-is** — the oracle gaps are not chased, the criterion is "every flow walks, reports issue, the period rolls forward", and the RPR series pick becomes a conductor act (裁-31) · **the effective proposer is the DIRECTING HUMAN** — he may not sign what he directed, except in a solo firm with PRD §2's self-approval attestation on the card (裁-32) · **F-T3 gets NO golden bar: draft only, never `issued`**, PR-7 unbuilt for beta (裁-33) · Track B's frontend home = **a Tax tab + a firm-level deadline feed + one compliance-register line, all in P6** (裁-34). **THE 08-30 ~02:00 SITTING (裁-35…裁-43):** 裁-19's re-home WRITE door **declined — S0**, the re-measured census being 23 bodies plus a doubled sealed metric (裁-35, with the owner's asked-for definition of *fail-closed* recorded) · the tier-3 security gate = **DPA e-sign + a rate wall, no trial quota** (裁-36) · ⌘K "Do" **into P6 behind a live allowlist check** (裁-37) · **F-T3's five and F-T2's eight all per recommendation** (裁-38, 裁-39) · **the three clock switches open TOGETHER at the G1 ceremony**, so PR-4 is BUILT (裁-40) · `client_identifiers` gains a uniqueness wall that **names duplicates and refuses, never dedupes** (裁-41) · **裁-42 THE BILLING MODEL** (supersedes R8c's shape; amounts still open) · R9(c) after beta + BELCORT's operator flag onto the Wave-G checklist (裁-43). |
| `mohe-grill-rulings-2026-08-30.md` | **live** | **The third ledger, 裁-45…裁-72 — the 08-30 NOON + EVENING sittings.** 裁-45 `0155`'s live duplicates ride the Wave-G reset (route a) · 裁-46 a revoked binding re-opens only through a NEW admin door `reset_binding_revocation` with a reason, receipted (PR-3) · 裁-47 the solo self-sign is CONFIRMED narrow — Clara's directed path only, never a human's own manual propose-then-sign · 裁-48 the dead `0028` postverify is RETIRED · 裁-49 G1's `call_kind` values + `login_pool` truing ride G1 PR-2 · **the billing cards: 裁-50 RM via Clara's own rate table, AMOUNTS STILL UNSET ("用 RM，不过先不定价") · 裁-51 no `payments_only` role · 裁-52 `onboarding` stays, "Draft" is a label · 裁-53 BELCORT exempt by plan flag · 裁-54 Stripe Tax · 裁-55 read-only after grace, never delete for non-payment · 裁-56 ARCHIVE = EXPORT PACKAGE THEN DELETE** (the orchestrator's dissent on file; the fail-closed defaults — sealed hash-receipted package, owner confirms the hash, 30-day cooling window, an audited delete door that never disables a trigger, refusals for operator clients and open obligations; beta-era build). **EXTENDED 2026-08-30 evening (~20:10–21:30 MYT) with 裁-57…裁-71**: 裁-57 beta is a PAID launch, no invited-free tier · **裁-58 THE AMOUNTS RULED FINAL: RM0, free until the pricing sitting** (a same-sitting mis-click superseded) · 裁-59 Wave G's three switches (`bank_agent`/`close_prep`/binding-expiry), `tax_prep` gets its own later sitting · 裁-60 the P6 cutover PULLED FORWARD onto the beta critical path · 裁-61 `/ready`'s storage-write probe becomes a hard readiness failure · 裁-62 beta ships NO issued tax artifacts — the tax module is INERT at launch (every treatment refuses unsigned) · 裁-63 Wave G's acceptance = the desktop corpus as-is, final, MBB-1 CLOSED · 裁-64 the four FE-plan cards (OQ-1/2/3/7) accepted as recommended · 裁-65 the P4-7 magiclink arm for existing-account invite accepts, riding P4-4 · 裁-66 裁-48 REVERSED on the corrected premise — the `0028` postverify is KEPT, live and passing · 裁-67 `0155` (裁-41) deploys to live AFTER the Wave-G factory reset · 裁-68 the tier-3 self-serve gate = exactly three walls + Stripe checkout success · 裁-69 agent proposal bases carry DB-resolved citations as one contract for both doors · 裁-70 the tax family's IA in apps/web (a client "Tax" tab + a firm-level deadline feed) · 裁-71 ten delegation defaults confirmed, incl. three items landing in PROGRESS Known issues · **裁-72 the P6 cutover scope widens to ALL 110 verbs** (the 81 cutover-owed + the 29 orphans) before the domain switch, organised as domain trains P6-C1…C7 in criticality order, each shipping dated NotBuilt cards (orchestrator's dissent on file: ~8+ frontend trains, beta moves by weeks); 裁-60's cutover trigger becomes "when C7 lands". |
| `mohe-owner-batch-2026-08-29.md` | **live** | *(row re-filed here 2026-08-30 — the file lives in `docs/plan/active/`, but this row had been sitting in the `completed/` table below.)* **BOTH BATCHES ANSWERED — 裁-23…裁-28 (morning), 裁-31…裁-43 (evening + 08-30) — with only the pricing AMOUNTS still open.** Each item is marked RULED in place. **First batch (items 1-18):** what landed overnight (Wave C 11/11 · the pre-beta backend queue · the three gate-OPEN design sets), the orchestrator's autonomy-mandate INFORMs (裁-15 widened to the catalog-derived ELEVEN barrier views after a demonstrated cross-tenant leak · 裁-18a's refusal in the ruled words + the 18a-before-18b ordering · raw model `sightings` dropped · the post-#414 sweep RED from a shared `seedAdmission` fixture that followed head, fixed frontier-aware by #415), the owner's own (T11 N2 · 裁-18b's eight · 裁-19's seven incl. the M9 live defect · 裁-21's twelve · the pricing sitting) and the record-only ledger. **SECOND batch (items 19-42), minted by the alignment audit and the night's gates:** Wave G's oracle evidence (裁-31) · F-T3's acceptance oracle (裁-33, and again at item 28) · the 裁-18b independent gate, **acted not ruled** — it ran and merged as #422 · the tier-3 security gate (裁-36) · ⌘K "Do" (裁-37) · Track B's IA home (裁-34) · R9(c) (裁-43) · F-T3's OQ-7/8/10/11/12 (裁-38) · the engine-source enable act (裁-40) · `client_identifiers` uniqueness (裁-41) · **the billing model in place of the pricing amounts (裁-42)**. **Seven items stay INFORM** — which app serves `app.clarabook.com` today (the OLD `apps/dashboard`, until P6's cutover) · the stale F-A8 lane row · 裁-18b's O1/O2/O3 delegation rulings · 裁-21 PR-a's three (the provisional equity variants, citations-only tax fields, and the CORE add-back families + the MPERS 4.2 addendum that landed as #424) · F-A9 PR-1B's both-arms REMOVE and its five-body D1 · a P6 sweep-panel check · **two backlog rows the reviews minted** (`wake_open_firm_question` can still bypass Door 2's authorisation on the FIRST question of a kind; 99 `unique_violation` handlers of which one relabels every violation as `binding_conflict`; `_approve_entry_core`'s stale budget prose; and the `0031` postverify **already red at the `0147` frontier**) · and 裁-19 PR-1's measured ~15% aging cost with its **P1** (a merge made before the deploy carries no carrier row). |
| `mohe-owner-batch-2026-08-31.md` | **live** | Owner-batch additions 72–98 from 2026-08-30/31; unruled unless marked, with 74/90/95 closed by fact or recorded disposition and 91b/94/96/97 visibly open. |
| `sprint-session-state-2026-08-31-afternoon.md` | superseded | **SUPERSEDED → the evening state bridge in `mohe-grill-rulings-2026-09-01-pm.md`.** The dated bridge for the 2026-08-31 afternoon compaction — written for a reader with no session or transcript: the five PRs that landed (+ runtime v70), the three in-flight fix rounds with their on-disk order files, the day's three defect classes, **FS-4's design-gate verdict in full (3 BLOCKER / 14 MATERIAL / 9 NIT — recorded in no PR body)**, the sharpened owner list, and the one-paragraph resume. `PROGRESS.md` stays the state authority; this file carries what compaction would otherwise have destroyed. |
| `billing-model-brief-2026-08-30.md` | **live** | **裁-42's billing model as DESIGN INPUT** for the billing design set (survey → design → gate) that **precedes P4's checkout tranche**. §1 is the model in the owner's own seven sections plus the configurability law: a base subscription (including seats, Active Client slots and an AI allowance) · paid seats (owner/admin/bookkeeper; viewer and payments-only free — **capacity, not people**) · **an extra firm-wide SHARED AI allowance per paid seat**, never a private quota · Active Client slots (capacity, unbound to a client; **draft clients free, slot-less and capped**) · archived-client retention (archiving frees the slot; the archive month keeps the active fee; **never both fees at once**) with **scheduled-for-deletion billing until PURGED** · AI overage = usage − allowance floored at 0, monthly expiry, **the service never auto-stops** · proration of additions **including their allowance**, removals next cycle, **never an auto-archive to cut capacity**, and an invoice showing every line. **THE LAW: every price, included quantity, allowance and ratio is CONFIGURABLE — nothing hard-coded.** §2 names the DB-owned artefacts it implies (an effective-dated configuration relation holding all nine values · the audited client state machine `draft`→`active`→`archived`→`scheduled_for_deletion`→`purged` · a frozen monthly rollup evaluator over `llm_usage_events` · Stripe objects **generated from** the configuration, never authored beside it) under constraint 2 — a billing figure is an authoritative number. §3 carries the three questions it leaves the owner: **the amounts** · **what "AI usage" is denominated in** (RM at list price vs tokens; rec RM, fail-closed = store both) · **grace on an unpaid invoice**, which §1 never addressed. |
| `billing-survey.md` | **live** | **裁-42** · the billing ESTATE SURVEY (as-found, 2026-08-30) — every claim a **catalog MEASUREMENT** on a throwaway rig at main's own chain (142 migrations → frontier `0147` → seed, container destroyed) or a Read of a cited `file:line`; migration-source reads are named PREDICTIONS. **Four findings the brief could not know. (1)** 裁-42②'s **`payments_only` role DOES NOT EXIST** — the CHECK is closed at `viewer|bookkeeper|admin|owner` (`0002:215`) and `role_rank` returns NULL outside it, so a new label is either silently `viewer` (full read of the books) or unable to act at all. **(2)** 裁-42⑤/⑥'s transitions **have no implementation**: exactly two statements in the tree write `clients.status`, the live graph is two edges out of `onboarding`, and **`active` and `archived` are both DEAD ENDS** — no archive, reactivate, delete-schedule or purge door exists. **(3)** 裁-42⑩'s three draft caps **ALREADY STAND**, measured — `file_document` (CLR11), the `mint_wake_credential`/`_tf_agent_task_insert` pair (CLR10, two independent walls) and `_assert_client_operational` inside `_draft_entry_core` (CLR10, WB-R1) — so the design builds none. **(4)** **`llm_price_table` is the VENDOR's USD COST**, USD-only by CHECK (`0110:497`), not Clara's MYR price, and **no MYR or FX relation exists anywhere** (`fx_rates` and `sst_rate_schedule` both measured absent — correcting the brief's own cite). Plus: `firms.tier_code`/`firm_tiers` measured absent (P4 asks 9-11 unbuilt), Stripe measured absent in code and config, `is_operator` measured unset on every firm, `evaluator_versions.firm_id` NULLABLE so a **platform-scoped** evaluator is already expressible, and a closed-world catalog scan finding **zero** billing prior art. |
| `billing-design.md` | **live** | **裁-42** · **the design doc of record** · v1 (2026-08-30), **gate OPEN**. Keeps the owner's own ①-⑩ numbering. **`clara.billing_plans`** (platform, effective-dated on `llm_price_table`'s measured posture — one `clara_fn_owner` policy, no app-role grant, DEFINER read) holds **every** configurable as a **column**, `0` legal throughout, incl. `paid_roles text[]` and `overage_multiplier_bp`; **`clara.billing_usage_rates`** is a SECOND relation carrying Clara's own **MYR** list price per engine, because reusing the USD vendor table would **bill at cost** and need an FX rate law 18 forecloses. **`amounts_ruled` is a FLAG separate from the amounts** — the evaluator computes, `issue_invoice` REFUSES — which makes 裁-42's *"until the amounts land, nothing charges"* a wall, and reconciles `0`-is-legal with P4's *"never RM0, which reads as free"*. Seats and slots are **capacity** on an effective-dated append-only `firm_subscriptions` (totals, not extras; anchor day 1-28), so proration and next-cycle removals fall out. **The paid/free boundary is `paid_roles`, NEVER `role_rank`** — a permission ladder is not a price list. `clara.evaluate_firm_billing_v1` is a **frozen platform-scoped evaluator** (cycle as a PARAMETER — no bare clock; explicit UTC; **rounding at the LINE, never the total**, so the invoice reconciles by addition) producing an eleven-rung `invoice_lines` set Stripe **mirrors**: the overage is pushed as our own computed invoice item, **never Stripe metered usage**, because that would make Stripe originate an authoritative number. §3.12 is the anti-cap wall, proved by a **positive set-equality census**, not an absence. |
| `billing-annexes.md` | **live** | **裁-42** annexes · **A** the decision register **D1-D12** (headline **D2**: why the MYR rate table is a second relation and not a reuse of `llm_price_table`; **D3** why the seat boundary is data not `role_rank`; **D6** why `purge_client` is designed-now-built-later against law 6's no-delete-verb rule; **D10** why "unruled" is a flag not a zero; **D12** why Stripe never meters) · **B** the DDL sketch and the **eleven-rung** table, with `retention_days` stated exactly (it reconciles **four** clauses of 裁-42⑤/⑥) and a worked proration cell whose every quantity differs so a transposition cannot pass · **C** the Stripe object map against the **live official docs (2026-08-30)** — licensed prices generated from the config, a one-off invoice item for the overage, and the webhook→**one idempotent door** table where no webhook ever writes a book · **D** the twelve-cell battery outline (incl. the anti-cap census, the five-status × three-wall positive read, allowance expiry, and a **two-`TimeZone` determinism** cell) and the PR ladder with **PR-1's four-body D1 list** · **E** risks and named non-goals. |
| `billing-gate-record.md` | **live** | **裁-42 gate card set (2026-08-30)** — §1 what the set SETTLED, §2 **eight owner cards** each with 大白话 context, a recommendation, its cost and a **fail-closed default**, §3 the seven attacks the independent pass owes. The cards: **OQ-1** the AI denomination (rec **RM via Clara's own rate table**, not the USD vendor table; fail-closed = **store BOTH** tokens and RM so no month is ever recomputed from data not kept) · **OQ-2** whether `payments_only` is minted (rec **no** — it is an RBAC change wearing a billing hat, and `paid_roles` makes it one array element later) · **OQ-3** the owner's word "draft" vs the live `onboarding` (rec keep the schema, display "Draft" — *the card exists because he is owed the sentence*) · **OQ-4** whether the operator firm is billed (rec **exempt via a PLAN flag**, not `is_operator`, which is measured unset and therefore inert) · **OQ-5** SST (rec **Stripe Tax**, with the reason it is not a law-1 breach: Clara's own sales invoice is not a client's book) · **OQ-6** unpaid invoices (rec **read-only after N days, never delete** — a professional must always get his books out; fail-closed = **nothing happens, suspension is not built**) · **OQ-7** retention per client or per GB (rec **per client, flat**) · **OQ-8** **the amounts** — 裁-28's, still open, with the market bands landed and the cost floor still owed. |
| `billing-model-owner-spec-2026-09-03.md` | **live** | **裁-42 · the owner's 2026-09-03 billing specification, filed VERBATIM as a design INPUT** (not a design of record) — the owner's own re-send of the 08-30 brief's §1 model, quoted byte-for-byte (md5 `62bb956d31d7b8bd8ddca27a14d444fe`), posed while ruling B3 (PRD §8's billing scope cell has zero code). Header carries a clause-by-clause measured delta against `billing-model-brief-2026-08-30.md` §1 — two items differ (the Draft-client restriction's wording, and an explicit Deleted/Purged build-sequencing note), everything else identical in substance — plus **裁-144** (owner, 2026-09-03): the design-of-record's tier tranche (`billing-design.md` §5 — the rest of PR-1, all of PR-2, an AI usage ledger) is BACKLOG, due before the official paid launch, not before beta live. **To be grilled (constraint 6) into a design doc before any build**; beta scope is unchanged (裁-57/裁-58/裁-126). |
| `checkout-gate-survey.md` | **live** | **FS-4 / R8** · the self-serve checkout + tenant-creation ESTATE SURVEY (rung 0, 2026-08-31) — every door read at the **LIVE body** on a throwaway `postgres:17` at frontier `0155`, never a migration's first `CREATE`. Findings **F1-F11**, absences **A1-A7**, predictions **P1-P6**. Headline: **nothing in the product mints a `clara.firm_admissions` row** — a whole-repo census finds exactly two writers, the seed (`0002_core_seed.sql:55-59`) and four test fixtures — so 裁-73 is not a choice between two working paths, it is the construction of the only one. **F1** `create_firm` (`0147:497`, `prosrc` sha12 `59fa533d9c03`) never reads `_jwt_email()`: 裁-26's email wall is unbuilt and the token is a pure bearer credential · **F3** `firm_registration_requests.status` admits exactly `open\|approved\|rejected`, so the ruled shape's "PAID" has no column · **F5** one firm per person is ALREADY a DB wall (`uq_membership_active_user` + `users_email_key`), which is 裁-36's limb ① enforced · **F6** `op_receipts.firm_id` is NOT NULL, so no pre-firm door may use `_reserve_op` · **F7** `audit_log.firm_id` and `domain_events.firm_id` are both NOT NULL, so **the pre-firm half of the journey cannot be audited at all** and today writes nothing · **F9** `pgcrypto` is absent (no `gen_random_bytes`) · **F10** no `inet`/`cidr` column exists anywhere in `clara`. §5.3 is the measurement that decides the CSRF remedy: the installed @supabase/ssr 0.12.5 **already hard-codes `flowType:"pkce"` in both client factories** (`dist/main/createBrowserClient.js:44`, `createServerClient.js:37`) and writes the verifier cookie — the binding material exists and is simply never consulted. §6 carries the one mechanical fact the webhook turns on: `express.json()` is mounted at `packages/runtime/src/index.ts:55` and a router mounted after it is **silently** unable to verify a Stripe signature. |
| `checkout-gate-design.md` | **live** | **FS-4 / 裁-89 · 73 · 74 · 68 · 36 · 64①** · **the design doc of record, part 1 · v3, amended to the owner's G1 ruling** — **裁-89 folds the admission door into ONE transaction.** Working the fold out produced a consequence the ruling did not have to promise: **the folded door needs no admission token at all** (a single transaction has no gap to carry a credential across), so it calls `_create_firm_core` directly exactly as `approve_firm_registration` already does — **`clara.firm_admissions` is untouched, `create_firm` is NOT re-cut, and the train's D1 write-quiesce window disappears**, restoring 裁-73's own "unchanged, no D1 window" prediction that v2 had to report as a divergence. `reconcile_paid_registrations` retires unbuilt (its state is unreachable), 裁-26's email-bound token is superseded rather than built (the credential it would bind no longer exists), and the fold does **NOT** remove the double payment — that is settled at ⑦, before the door runs. **Also amended to the same sitting's other rulings: 裁-92 replaces the confirmation LINK with a 6-DIGIT EMAILED CODE** — the owner chose cross-device signup over the cryptographic binding, which is a deliberate overrule of G10's own recommendation. §3 designs it honestly: the login-CSRF vector disappears *with the link* (there is none to send a victim), the binding drops to "the address is the person's own" and §3.3 walls the one caller-supplied value left (**the address may never come from a URL** — else the attacker picks it and the whole class returns), and a six-digit space makes the rate wall **mandatory** — four walls C1–C4, with C4 (the 24-hour default expiry, shortened to 10 minutes) named as the one act with **no in-repo enforcement** rather than implied to be enforced. Everything below is v2/v3 and stands. **FS-4 / 裁-73 · 74 · 68 · 26 · 36 · 64①** · · v1 (2026-08-31); **the gate has since CLOSED (裁-93, ADR-0077 signed — see `checkout-gate-gate-record.md`); this clause narrates v1's genesis, not current state.** The shape end to end, the ten-state machine with its four "illegal by construction" database properties, **the login-CSRF binding recommendation**, the rate-wall sitting, the partial-failure matrix and the build sequence. **Recommends Supabase's native PKCE exchange over a hand-rolled nonce** — the platform already runs PKCE in both clients, so the fix is the email template plus `exchangeCodeForSession` replacing `verifyOtp`, with **no `token_hash` fallback arm** so a mis-configured template breaks loudly instead of silently unbinding; it also closes the recorded sibling finding (`token_hash` in ingress logs) that a nonce cannot. 裁-36/裁-64①'s rate wall is written with **both options** and recommends a **peppered digest** over a stored IP (identical wall, no personal data at rest), with the trusted-proxy-header trap named and the applicant's own retries exempt so 裁-74's resume arm cannot self-DoS. **v2 (fix round):** the independent review proved the v1 door **stranded the paying customer** — it required an unconsumed payment while consuming it on the first call, so the retry it called "rotation" was unreachable and what was specified was strict-refuse under rotation's name; part 2 §1.3 carries the repair and states what was wrong rather than quietly correcting it. The partial-failure matrix, re-walked against the repaired door, proves **no path strands a paying customer and no path mints two firms**, across thirteen death points. §6.1 reports the one measured contradiction with 裁-73 rather than resolving it. One D1 item: the `create_firm` recut. **§1.1 is a transport WALL, not a preference**, and v2 names its subject: the train adds **exactly three** server entries (the confirm verify POST, `POST /checkout`, the /checkout/success POST), each a route.ts HTTP-method export; steps ③ and ④ call their doors from the CLIENT over PostgREST as the built signup form already does, and **④'s transport is decided rather than left unnamed** — an unnamed server-side step under a no-Server-Actions heading is where a build lane reaches for an action. **The argument is re-ordered onto its sound leg:** Next's own guidance is that page-level auth cannot protect Server Actions (which needs no instrument at all), and the census's real value here is the **forced declaration** — a route leaf must be named in `SCOPE_UNSCOPED_SURFACES` with a written reason, while an action file is enumerated by nothing and declares nothing. *(v1 justified it by the census's firm-scope blindness; that was the weaker argument and not operative here, since none of this train's surfaces are firm-scoped — the customer has no firm until ⑧.)* Measured across main and all four open web branches: zero `"use server"` modules, no `template.tsx` — the train declines to open the first one rather than repairing a hole. |
| `checkout-gate-design-part2.md` | **live** | **FS-4** · the design of record, **part 2** — **the database objects**. **v3 §1.3 is rewritten to 裁-89's single transaction**, and §1.3.0 answers the one genuinely open question the fold raised — *does the admission row survive?* **No**, with the reasoning written out: there is no gap for a credential to cross; the estate precedent already exists (`approve_firm_registration` creates a firm without ever touching `firm_admissions`); and keeping the row as an "audit artifact" would mean writing a **fake receipt**, because `token_hash` is NOT NULL and UNIQUE (`0147:323-324`) so it would carry a hash of nothing. A table states what each retired guarantee is replaced by. **§1.4 is now "there is no D1 item"**. **§1.2 is 裁-91's REDACTED PROJECTION**: reconciliation fields only, **no `customer_details` PII in the database at all** — the webhook verifies the signature over the raw body, projects an allow-listed set, and **discards the raw event**, so Stripe stays the system of record for what Stripe saw. That dissolves G11's PDPA problem structurally rather than building an erasure door into an append-only table, and it states its own limit: **the allow-list is the wall, `ck_stripe_events_no_pii` is the mistake-net** (a CHECK cannot cheaply see arbitrary nesting), the same posture the estate states for the pinned-ids guard. Four `UNNUMBERED_*` migrations: the DPA pair + rate events; `stripe_events`/`stripe_event_problems`/`stripe_object_map` + `record_stripe_event` + `apply_stripe_events`; `checkout_intents`/`firm_registration_payments` + the three `firm_admissions` columns + `claim_paid_admission`/`open_checkout_intent`/`close_paid_registration`; and the `create_firm` recut carrying 裁-26's email wall with a **deliberately identical refusal string** (a distinct one is an oracle on whose email a token belongs to). **`stripe_events` has no `applied_at` on purpose** — "applied" is derived from the payment row, so the append-only trigger is unconditional. The webhook's principal is a new `clara_stripe_webhook` NOLOGIN + `_login` pair on the estate's measured idiom, granted EXECUTE on **exactly two functions and nothing else**. §5 is the battery: sixteen walls, each with a REFUSE cell, a POSITIVE control and the **named mutant** that must redden it — including W-A's spy-not-empty-table assertion and W-H's second-browser-context cell, which is the leg that actually proves the binding. |
| `checkout-gate-design-part3.md` | **live** | **FS-4** · the design of record, **part 3** — the webhook route contract, the `apps/web` surfaces, the environment, and **the acceptance battery**. **v3:** W-D/W-K/W-P rewrite against the folded door (**W-K's mutant moves onto the `FOR UPDATE`, because under 裁-89 the row lock IS the wall**), and **seven cells retire with their subjects** — W-E·W-E2·W-F·W-F2·W-F3 (the recut and the `firm_admissions` columns that are no longer added), W-P2 (`reconcile_paid_registrations`). **§4.3 is the retirement ledger**, stating for each what covers the property now, because a cell dropped without its subject is a deleted proof; and **W-E3 is ADDED** to assert positively that `firm_admissions` ends the battery with the same columns, indexes and row count as a freshly seeded rig — "we no longer touch that table" is exactly the absence this estate does not take on trust. **N3 becomes a NEGATIVE assertion** (no server entry exists beyond the three named), since that is what the mutant tests. **裁-92 then reshapes the confirmation cells**: W-Q retires (no link, so no `confirmation_url` to validate) and W-H/W-H2 are rewritten as **W-H…W-H6** — the address-never-from-a-URL wall with the realistic wrong implementation as its mutant (`searchParams`), three distinguishable refusal cards, C1 attempts-per-address asserted **by a spy before `verifyOtp` is reached**, C2 attempts-per-origin whose mutant is *dropping the origin limb* (the one that proves C1 alone cannot stop a spray), an unsettled-attempt-still-counts cell, and a MUST-NOT-RED control recording that **single-use is Supabase's property, not ours**. §2.1 carries the wall's objects; the e2e's happy path now confirms **in a second browser context**, because cross-device working is the thing 裁-92 bought. The review proved four of v1's mutants **non-discriminating on a rig** — chief among them **W-L**, the cell the whole *"two firms stay impossible"* argument rests on: `_create_firm_core` carries TWO guards (an `exists(...)` pre-check and a `unique_violation` catch on `uq_membership_active_user`), so deleting the pre-check alone produced the same refusal, the same errcode and zero leaked firms — **the cell stayed green under its own mutant.** It now carries a **two-mutant panel**: m1 (delete the check AND drop the index) MUST go red; m2 (delete the check alone) is a **MUST-NOT-RED control**, which is the positive assertion that the index is the real wall. W-A splits into two cells because the try/catch mutant yields 500 and never exercises the spy limb; W-B additionally asserts `recorded:false` because the mutant leaves row counts unchanged; W-H's mutant becomes the realistic wrong implementation (resolve the verifier from a server-side store, so the exchange SUCCEEDS for a verifier-less browser) rather than one that reddens the positive control; W-C records that no discriminating mutant exists and the positive control IS the cell. Plus W-O2, a MUST-NOT-RED control pinning the measured truth that **the webhook DSN is equivalent in power to the Stripe signing secret**. **§4.2 inverts W-R** after a second review pass: v1 asserted a whole-repo COUNT while §1.1 states a TRAIN-scoped property, and the two come apart both ways (false GREEN if a train step is implemented inside a `page.tsx` body; false RED on any unrelated special file). The cell now asserts, per server entry, that it is a route leaf **by the census's own imported `LEAF` regex** and that it is **registered in `SCOPE_UNSCOPED_SURFACES` with a reason**, with a MUST-NOT-RED control that an unrelated new page/route does not redden it. The global check demotes to a **derived ROSTER** — every non-LEAF file under `apps/web/app`, seven named members today (app/layout.tsx, app/not-found.tsx and the five route-group/client layouts), because **the blind spot already has seven residents and v1 counted only the two that happen to be zero**; the assertion message carries the remedy so the lane that adds an `error.tsx` cannot go green by bumping a number. And the directive check becomes a **parse fact** — the census's own `stripComments(…, {blankStrings:true})` on the module's FIRST statement, not a grep, because a grep misses `'use server'` in single quotes and matches the literal in a comment; the planting demo re-runs with both quote styles plus two decoys. Homed at apps/web/tests/checkout-transport.test.ts, **required in `apps/web/test/manifest.txt`** (that gate rides `lint` on every PR) and may not be `.skip`-ed. |
| `checkout-gate-gate-record.md` | **live** | **FS-4 gate record — the record of the gate that RAN, now carrying its rulings.** **G1 RULED (B) 2026-08-31 evening (裁-89): fold the two doors into one.** **G13 RULED: beta runs the whole journey on Stripe TEST mode**, KYB and live-mode deferred to the pricing + launch sitting — whose design consequence is that `payment_method_collection` becomes **config-driven from the plan row** (`'if_required'` at RM0, `'always'` once priced), correcting v2's flat `'always'` which would have asked a real beta customer for a card in test mode. **G8 WITHDRAWN by 裁-89** — it asked how long an admission token may live and the fold removed the token; kept rather than deleted, because a question that vanishes without explanation is worse than one that is answered. **G7 item 2 retires** (the `create_firm` recut was the divergence; the fold restores the ruling) and **gains an INFORM at item 4: 裁-26's email-bound token is superseded**, its purpose served by deleting the credential rather than binding it. **裁-90 rules G5** (we draft the beta DPA; the owner and a lawyer swap it at launch — and the swap is a **version bump, not a schema change**, which M8's pinned `dpa_version` already makes safe mid-flow, so the v2 fail-closed empty-table default is replaced by a real beta row plus an honest note). **裁-91 rules G11** per recommendation (the redacted projection). **裁-92 rules G10 AGAINST its recommendation** — flagged in place, because 裁-93 rules the rest "per recommendation" and applying that phrase to G10 would silently reinstate PKCE. **裁-93 rules everything remaining per recommendation, signs ADR-0077 and endorses the CSRF deferral — so the ledger is CLOSED.** The Wave-G template line is superseded a second time: the template now emits `{{ .Token }}` and **no link at all**, which moots item 85's prefetch problem entirely. Originally: **TEN questions plus two declarations**, each 大白话 where it helps, each with a recommendation, the cost of every option and a fail-closed default. **G1 (restated in v2, because the review changed what is being asked)** — the specified door stranded the paying customer, so the stated default was not a working option; the real choice is now **a repaired rotation vs folding the two doors into one**, and the fold is recommended (it deletes three failure modes at once — the stranding class, M5's unreachable closer and M7's double-payment window — because they all live in the seam between two calls). Carries a correction to this record's own earlier pitch: the token never reaches the BROWSER either way; the fold's real gain is that a live bearer credential never enters the application tier · **G2** where Stripe's Product/Price come from when `billing_plans` does not exist (rec: land the minimal table + one beta row; hand-authoring is forbidden by 裁-42) · **G3** the webhook's home (rec: the runtime — `apps/web` holds no service credential today and that is worth keeping) · **G4** the 裁-36/裁-64① sitting's **two options** — the address vs a peppered digest (rec: digest) — plus **G4b** the shared-office NAT case · **G5 the finding to read first: the DPA text 裁-68① requires does not exist.** The three `docs/ops/legal/` drafts are an OpenAI-DPA brief, a letter a firm sends its own clients, and an internal PDPA memo — **none is a Clara↔firm agreement**; the default is that `dpa_documents` ships EMPTY and no self-serve firm can be created at all, which is fail-closed by construction · **G6** the pre-firm audit gap · **G7** now **three** measured divergences from 裁-73's text, not one — the missing `paid` status, **the `create_firm` recut the ruling priced at "unchanged, no D1 window"**, and the webhook-mints-the-admission move · **G8** the admission expiry · **G9 CLOSED by 裁-54** ("no tax line before registration") · **G10 NEW — PKCE costs CROSS-DEVICE SIGNUP**: the confirmation mail must be opened on the device that signed up, which for a Microsoft-365 customer base is a real fraction of signups; 裁-68③ asked for a binding, not for this, and the owner had not been shown it. The cost belongs to ANY browser binding, a hand-rolled nonce included; the third option is Supabase's 6-digit code typed into the original tab, which keeps cross-device AND the binding · **G11 NEW — PDPA retention/erasure for `stripe_events`**: G4 puts a whole sitting to the owner about storing an IP because it is personal data, while the design stores Stripe's full payload (email, name, address, phone, tax ids) in a table whose UPDATE, DELETE and TRUNCATE all raise — **no erasure path exists by construction**; recommendation is to store a redacted subset · **G12 NEW — the double-payment residual**: two in-flight sessions can both complete; the design now writes one payment row and records the second as a visible `duplicate_payment` problem, but the refund path is unbuilt and is a named precondition on the pricing sitting. |
| `counterparty-merge-survey.md` | **live** | **裁-19** · the merge re-home + un-merge ESTATE SURVEY (as-found, 2026-08-29) — findings **M1-M14**, censuses **C1-C9**, predictions **P1-P5**, every claim either a `file:line` at `36c2fd50` or **rig-measured at frontier `0142`** from the live catalog, with the behavioural half driven through the REAL granted doors in a bookkeeper session. **Four findings are new to this survey and appear in neither 裁-19's framing nor T8's review record (#397): (M3)** `_statement_core` canonicalises its ARGUMENT but filters items on the RAW column, so the merged party's balance is in **NEITHER** statement — measured RM3,800.00 on aging against RM600.00 on every statement anyone can open · **(M9)** `list_open_items_by_counterparty` passes the **firm id** where `p_client` is expected (`0038:8006`), so a granted `clara_authenticated` door consumed by bank matching has returned `[]` for every counterparty since `0038` · **(M10)** `merged_into` cannot be cleared by anyone, superuser included (`_tf_counterparty_update_0011`, live tip `0040:6230`) · **(M13)** a post-merge cross-party `apply_open_items` is **ADMITTED** where the pre-merge one refuses `cross_counterparty_application`. Also measured: the append-only wall holds against a superuser (M5), and a merge does **not** move `ar_control_tie`'s digest (M4). |
| `counterparty-merge-design.md` | **live** | **裁-19** · **the design doc of record** · v1 (2026-08-29), **gate OPEN**. Delivers the ruling's OUTCOME — aging consolidates, the statement is whole, one party's money reads as one party's — through a **canonicalising READ layer** (`_aging_core` · `_statement_core` · `list_open_items_by_counterparty`), and **dissents from the ruling's stated MECHANISM** at **D-01**: `open_items` is append-only against a superuser (M5) and an append-only-legal "move" is a new item pair that lands in the **current** aging bucket (`0037:713-715`), so a physical re-home would falsify the very report the item exists to fix, or need a shared wall weakened (constraint 14). The dissent is priced on one page and escalated as **OQ-1**, which **gates the item**; §3.2 records what ships if the owner overrules. Plus the **un-merge door** at an **admin, human-only** floor keyed on a new `clara.counterparty_merges` row — six named refusal rungs (**U1-U6**) covering the entanglements an un-merge cannot honestly split, **a named refusal instead of a partial reversal**, and three things it can never restore (a terminal-`retired` coding rule, an alias it did not create, post-merge postings). **The wall is entanglement, not age** (D-09). Seven owner questions, each with a recommendation and a fail-closed default. |
| `counterparty-merge-annexes.md` | **live** | **裁-19** annexes — **A** the migration mechanics (the three read recuts with their anchors and postchecks · `clara.counterparty_merges` in full · the ONE added branch in `_tf_counterparty_update_0011` with its byte-preservation proof · U4's predicate · the un-merge's load-bearing transaction order, which puts the identity clear FIRST because `_tf_coding_rule_insert_0016` demands a live canonical vendor) · **B** the live `prosrc` sha256 pin table (26 bodies at frontier `0142`), the two closed-world censuses (**32** `_canonical_counterparty` callers — `_aging_core` absent, which IS finding M2; **42** bodies naming `open_items`), and the numbered **D1 inventory, two windows** · **C** the battery: both directions per rung, Gate-A exact-cent tie-outs, the two-session merge-vs-allocate race with blocking PROVEN, and M9's positive control — the cell whose absence let a dead door live since `0038` · **D** the decision register **D-01…D-12** with the owner-question grounds · **E** the frontend home and **every `ArApCounterparty` string this item re-trues, verbatim**, including the two that invert (`merge.consequence`'s "there is no un-merge door" and `mergePreview.whatChanges1`'s "this cannot be undone"). |
| `counterparty-merge-gate-record.md` | **live** | **裁-19 PR-0 gate record — the SEVEN OWNER QUESTIONS ARE RULED 2026-08-29 (裁-24, §2.1); the gate itself has NOT run.** **OQ-1 came back PHYSICAL in the append-only shape**, with the orchestrator's dissent on file: for every OPEN item in an UNFROZEN period the merge APPENDS a re-home pair (old row superseded, new row under the survivor at the ORIGINAL date, back-pointing), the un-merge appends the reverse pair, and frozen years fold in the READ layer only — so **D-01 becomes a HYBRID and the set is AMENDED, not superseded**; **OQ-4 WIDENED** (Clara may PROPOSE an un-merge, the admin signs). The build may not start. Twelve lenses to attack, with the design naming its own most likely blocker (**L5**: `_aging_core`'s fail-closed branch on an unresolvable party raises out of `ar_aging`, and `ar_aging` sits under `_control_tie_core` → `_evaluate_one_gate` → the close). **OQ-1 outranks the gate** and should be ruled first, because a "physical move" ruling supersedes the design set rather than amending it. Also carries the eight obligations the gate must fold regardless of findings — chief among them a **closed-world census of post-merge entanglement sources** derived from the live catalog rather than from the design's imagination (the evidence for whether U1-U6 are complete), and **P1/P2 measured on the live estate** so a merge that is already un-reversible is stated in Known issues rather than discovered by a user. |
| `frontend-handoff-addendum-2026-08-24.md` | **live** | **Extends `frontend-handoff-2026-08-23.md` — the Wave-G OS surface pulled forward (owner ruling 2026-08-24):** ⌘K Ask/Do/Go + ActionPanels, the proactive cross-scope inbox, and the plan-as-document close/onboarding surface now build in this session rather than waiting for a later wave, because the backend verb inventory is effectively frozen. Every claim carries a `file:line` or is marked UNVERIFIED. Flags that `PROGRESS.md`'s Wave-G frontend lane row still read "HANDOFF LANDED 2026-08-23" as of this file's authoring — a later clock-out sweep should true it. |
| `frontend-handoff-2026-08-23.md` | **live** | **Wave G · frontend — the Codex session handoff: settled decisions, contracts, API surface, journey inventory, setup, skills, grill-first list, merge plan.** §0 is closed (location: the new **apps/web** package in this repo · Cloudflare Workers via `@opennextjs/cloudflare` · Supabase cookie auth, invite-only first · Resend with no client data in an email · Tailwind + shadcn adopted · build against the LIVE project under ADR-0075 · crude Track-A doors replaced IN PLACE with the same verb). §3 is the non-negotiable backend contract set (typed `parts[]` with fail-closed rendering · hydrate-never-trust · the two-lane wire where governance never transits the runtime · money as `bigint` cents the UI never sums · receipts · one door per human act · sandbox-watermark-in-the-bytes). §4 is the live API surface with call sites; §5 the 26-journey inventory with BUILT / CRUDE-DOOR-COMING / DESIGNED / UNBUILT and the draft-edit verb map; §8 the grill-first list; §10 the UNVERIFIED claims, named rather than buried. **TRUED 2026-08-31:** §8 was ruled by `mohe-grill-rulings-2026-08-27.md`; `verb-coverage-census-2026-08-28.md` supersedes §5 as roster authority. |
| `codex-frontend-handoff-errata-2026-08-27.md` | **live** | **Integration errata for the clarabook-frontend build** (a separate Codex session's frontend work against a snapshot of this repo, now merged and archived to design-history) — its manifests were honest about their own staleness (audit base `d4b6cd2`, 36 commits behind `main`); this page trues seven rows, confirmed by the 2026-08-26 adversarial scan and independently re-derived here at `file:line`: **(i)** `clara.begin_close` is LIVE and human-capability-gated since `0120`, not prohibited · **(ii)** the `parts[]` union is 18 live types, not 21 — `kb_rule_proposal`/`rule_post_receipt`/`bank_rule_proposal` retired · **(iii)** bank agency: 13 wake verbs live (`0121`) + the 11-function rules loop permanently retired (`0129`) · **(iv)** filing/attribution LIVE (`0123`-`0126`), not DESIGNED · **(v)** the freeform read surface (`0131`) is live and absent from the stale manifests entirely · **(vi)** two report-download paths now exist (`0127` archive doors, `0132` sandbox export) · **(vii)** the brand-guideline PDF package (path output/pdf/clarabook-brand-guideline-package-v1.0 in THAT repo, not this one) IS present on clarabook-frontend `main` and verifies 42/42 clean — an earlier absence finding was a Windows long-path checkout truncation, corrected same-day; carries the short-path/`core.longpaths`/CRLF-strip re-clone practice. |
| `verb-coverage-census-2026-08-28.md` | superseded | **SUPERSEDED 2026-08-31 by `verb-coverage-census-2026-08-31.md` (FS-0, 裁-75) — kept for the STALE-NOT-BUILT law it minted and the 0138 history.** The two-way verb-coverage census (2026-08-28) — the roster authority the port wave worked from. Run to answer the owner's standing question with a measurement rather than an assurance: a throwaway rig at the `0138` frontier, the **live catalog read directly** (never migration-text greps — revokes make text unreliable), one coordinating lane + three domain lanes, every orphan claim spot-verified against review laws 2/3. **Direction 2 (frontend → backend): 85/85, ZERO stale wiring** — P3's verb-census-first law held under full audit. **Direction 1 (backend → frontend): 250 items → 60 UI-wired · 2 STALE-NOT-BUILT · ~79 deliberately non-UI · 81 CUTOVER-OWED · 29 ORPHAN** (with a stated ±2 imprecision in the metrics lane's c/d split, reported rather than smoothed). **Mints the STALE-NOT-BUILT class** — an honest "not built yet" note is a DATED CLAIM, and two went false when their subject's train merged with nothing updating them; **law: every NotBuiltNote names the verb/train it waits on, and truing it is part of that train's MERGE.** Headline product-level find: **nothing in the entire product can open a fiscal year today.** Its three owed dispositions were answered by the owner's ruling A (2026-08-28) — see `port-wave-plan-2026-08-28.md`. |
| `verb-coverage-census-2026-08-31.md` | **live** | **The two-way verb-coverage census (2026-08-31) — the FS-0 re-measurement (裁-75) and the ROSTER AUTHORITY.** A throwaway rig at the **`0155`** frontier (live DB `0153`; the four `0154` merged-not-live names stated, never smoothed), the live catalog read directly. **Direction 2: 224/224, ZERO stale wiring** (171 `callDoor` verbs + 53 `getRows` relations). **Direction 1: 280 items → 178 UI-wired · 13 in-flight in the open P4 PRs (proven by added-line diffs) · 1 planned by ruling (`create_firm` → FS-4) · 64 deliberately non-UI (cited) · 15 row-bound · 8 NO HOME with dispositions.** The 08-28 headline is dead — fiscal years open, the close cluster, counterparty hygiene, autodraft and reextraction are all live in `apps/web`. **裁-72's P6-C1…C7 programme is DISCHARGED** (amendment under 裁-72 in `mohe-grill-rulings-2026-08-30.md`); the P6-X exit gate re-runs this census at the cutover tip. Carries its own instrument notes (the nested-generic extractor hazard; `relkind::text`; wrapper-wired nuance). |
| `port-wave-plan-2026-08-28.md` | **live** | **THE PORT WAVE — the plan of record for owner ruling A (2026-08-28, "beta is a live launch; every full-product userflow lives in the agentic UI").** Ruling A collapses the census's disposition sheet: the 81 cutover-owed AND the journey-mapped orphans all get `apps/web` homes, and `apps/dashboard` retires when the wave completes. **§1** what ruling A settles and what it opens · **§2** the roster reconciled at NAME level — **115 distinct door names** across the census's two lists (87 cutover-owed after its own `withdraw_draft` double-count, 29 orphans, 1 overlap), of which 109 land in trains, 3 route to P4 (`create_firm` · `users_visible` · `set_firm_high_stakes_threshold`) and 3 are owner-excepted; the 81-vs-87 headline gap is stated, not smoothed, and closed by the exit-gate re-run · **§3 the structural finding** — four shared `apps/web` files are the whole wave's merge risk, and one of them (`apps/web/package.json`'s 68-path single-line test enumeration) **fails SILENTLY**, so a T0 seam PR lands a per-line test manifest with a count-control gate, a needs-you row-kind affordance registry, the final tab arrays with `NotBuiltNote` placeholders, and eleven pre-landed i18n namespaces; the near-duplicate door dialogs are a DELIBERATE house pattern and stay duplicated · **§4** the eleven trains, every one of the 109 names assigned exactly once, sized against measured P3 lanes · **§5** agentic presentation per train (needs-you rows · workbench panels · door dialogs · which two trains earn a P6 Clara-raised card) · **§6** the wave graph: T0 alone, then three build waves of 3-4 concurrent lanes, then the wire bump, then the exit gates and the cutover. Companion: `port-wave-plan-2026-08-28-part2.md`. Roster authority: `verb-coverage-census-2026-08-28.md`. **Status note (2026-09-02): the 11/11 build trains landed by 2026-08-30; the §9 exit gates and the P6-X cutover itself are carried forward as FS-10/11 (`frontend-sprint-handoff-2026-08-31-orders.md`), not yet run — the wave stays open on those legs only.** |
| `port-wave-plan-2026-08-28-part2.md` | **live** | **THE PORT WAVE, part 2.** **§7 the ladder** — rung 0 is a verb census at the LIVE bodies (the discipline that produced zero wrong-verb defects across five P3 lanes), then Q9's five-rung DONE formula, then the battery (every new surface owes an a11y file; every door dialog owes a keyboard file — the instrument that once found six permanently-unopenable doors five code reviews missed), then independent review → fix round → **same-reviewer** re-verification, because a fix round is itself judgement logic · **§8** the P6 four-part wire bump (catalog 22 → 26 in ONE `chatTurn_v16` bump — TRUED 2026-08-30, was 18 → 22 / `v15` — bundle-proven in-VM after deploy) and the **ceremony-grade cutover PR**, which classifies each of `apps/dashboard`'s 61 test suites as superseded/migrated/retired/owner-ruled BEFORE the delete — a "superseded" that names no equivalent is not evidence · **§9** the two exit gates: a fresh two-way census (pass = zero cutover-owed, zero un-dispositioned orphan, direction 2 still 100%) and a conformance re-audit that re-derives `apps/web/lib/command/routes.ts` from the live app tree and sweeps every `NotBuiltNote` for a claim that went false — plus **§9.3's NAMED NOTE: for T1 and T9 the Q9 rung-5 walk is FIRST EXECUTION, not confirmation** (zero `fiscal_years` rows; zero `reporting_periods`/`period_snapshots`), so a failure there is escalated as a backend discovery, never worked around in the UI · **§10** effort ≈ **12.3 P3-lane equivalents** with the calibration shown, plus the dependency table (**no backend dependency exists** — every door is live at `0138`) · **§11** non-goals · **§12** the seven questions with their recommendations, and the **CONDUCTOR ADOPTIONS (2026-08-28) — all seven adopted as written**, two annotated: a confirmed `create_account_set_v1` retirement goes to the OWNER for visibility in the next batch (exceptions never go silent), and T8 reports its `apply_open_items`/`unallocate_group` census outcome to the conductor BY NAME (the wave's weakest provenance) · **§13** the Mobbin grounding debt: four new flows here plus P4's three unmet, one lane for all seven. **Status note (2026-09-02): builds done 2026-08-30; §9's exit-gate re-run and the §8 ceremony-grade cutover ride FS-10/11, not yet run.** |
| `p4-design-2026-08-27.md` | **live** | **P4's design of record — signup · operator approval · firm tiers · in-firm invites/RBAC** (design only; the review pass follows). Carries the **verb census** against the live DB frontier and the **identity gap** it found: nothing propagates `auth.users` into `clara.users`, so a person can complete the P2 invite flow and land with `jwt_firm()` NULL — every read empty, every write `CLR04`. Also: `create_firm`'s LIVE body is `0017`, not `0004`, and returns `plan_id` too · one active membership per user is a GLOBAL invariant · zero firms carry `is_operator`, so the operator console needs a ceremony before it can be smoke-tested. Eleven **BACKEND-ASKS** with door shapes, the five journeys, the per-journey DONE rows, the battery plan, NON-GOALS, and ten owner questions each carrying a recommendation — including two measured collisions: **R3's translucent focus ring fails SC 1.4.11 on every ground (2.31-2.36 vs a 3:1 bar), not just R2's cream**, and form-field boundaries fail it today at 1.728 on plain white. |
| `p4-design-2026-08-27-annex.md` | **live** | **P4's evidence companion** — the byte-verified quotes behind the design's three load-bearing DB claims, the enumerated negative searches behind its nine gaps (so a reader re-runs them rather than trusting a sentence), the full contrast runs measured with the shipped gate's own exported functions incl. the **six-ground alpha sweep** (65% fails on `accent` at 2.970; 70% clears all six with ≥0.270 margin), and the proposed door signatures with refusal codes and receipts — including the pre/post-condition splits for the three extracted cores (`_create_firm_core`, `_add_member_core`, `_claim_identity_core`) naming exactly which guard sits in the core and which at each entrance. |
| `p4-design-2026-08-27-annex-2.md` | **live** | **P4's frontend-execution companion (annex 2)** — routes and the scope check's three entrances (`(full)` and the runtime API route are SIBLINGS of `(firm)`, so a check in the firm layout alone leaves both reachable by a no-membership session), the primitives to vendor under R4, the `PAIR_SPECS` rows plus the constraint that the ring alpha is ruled BEFORE they land, the per-file battery, and R3's four-part failure population — plus a gate defect found in passing: both existing focus-ring pairs measure the SOLID token while nine components render the translucent idiom, so the gate is green on a treatment that is not there. |
| `p4-mobbin-grounding-2026-08-28.md` | **live** | **Closes P4's §11 ("Mobbin grounding — UNMET")** — real-world references for the four P4 journeys (signup-to-holding, operator approval queue, email-keyed invite/RBAC, read-only tier display), each with 3-6 cited screens/flows and takeaways mapped to EXISTING Clara components only (`StateBanner`, `SectionHeader`, `PageShell`/`PageHeader`, `Table`, `Dialog`, `Select`, `Badge` — no new primitive introduced). Names anti-patterns to reject (bulk approve/deny, a "Delivered" badge the DB can't back, a feature-gating checklist, self-serve plan buttons) and seven flags for the design/owner not resolved here, incl. reject-reason required-vs-optional, the missing dropdown-menu primitive, and that `.mcp.json` still has no `mobbin` server entry despite R5's approval. |
| `wave-g-e2e-corpus-design.md` | **live** | **The Wave-G corpus's CONTRACT OF RECORD for what the 2026-08-20 sitting ruled** (ADR-0072 ⑤), and a design for what it did not. Ruled and binding: **§3.0's two-tier reshape** (an ORACLE tier — BEE across two FYs, ROME SECRETARY and ROME PROPERTIES as single terminal periods — plus an **open-intake REALITY tier**, because two of the three designated clients are terminal-period books for companies in strike-off and can never supply a second consecutive FY) · **§10's eleven OD rulings** (custody FULL PERMISSION with the IC copy excluded · **no second principal**, so B3's distinct-checker arm ships named-unexercised · a **whole clean product DB on the live project**, sandbox and fixtures not re-created · the **UX floor precedes the run**) · and **§5's step-4 G1 amendment** — *"standing rules earn autopost"* is dead, F-A2's agent judgement is the authority. **TRUED 2026-08-31 (裁-63): missing corpus evidence is marked 资料缺失 and never awaited; MBB-1 is CLOSED.** Still design: the slot matrix's axes, the golden-standard package, the run script, the vacuous-green rule and the edge-case lens (now incl. **EC-14, the strike-off/terminal period**). |
| `wave-g-corpus-oracle-assessment.md` | **live** | The corpus's evidence companion (2026-08-20, read-only, nothing copied or modified): per-client verdicts against §4's seven-item package, **the named gaps now CLOSED as acceptance blockers by 裁-63 (marked 资料缺失, never awaited)** (BEE has no GL and no TB for either FY; RPR is missing two months of bank statements and has no accounts at a period end; neither RS nor RPR names a producer), what the assessment CONFIRMED (BEE's bar verified against the client's own papers; its bank ties to both balance sheets to the sen; RS and RPR genuinely greenfield), the exclusions and fixture sets (the IC copy · 68 byte-duplicates · the ~32 USD invoices, retained as the FX-lite fixture), the OCR-hardest classes, and the personal-data inventory behind OD-4. |
| `../ops/wave-g-setup-checklist.md` | **live** | **The Wave-G SETUP checklist (2026-08-30 evening sitting)** — owner-facing, one page, every line naming the file/command that proves it: the Resend account posture (`sending_access` domain-restricted key, message storage OFF, restricted team log access — 裁-65) · the four env vars (`SUPABASE_SERVICE_ROLE_KEY`/`RESEND_API_KEY`/`INVITE_MAIL_FROM`/`CLARA_PUBLIC_ORIGINS`, fail-closed when unset) · the invite link's `?ct=` query-value redaction at the edge/access log · Supabase Auth's tier-3 self-serve signup switch (裁-57) · the Stripe account + Stripe Tax posture and the TEST-mode non-zero-price walk (裁-58) · BELCORT's `is_operator` flag at the G1 three-switch ceremony (裁-59, `g1-operator-firm-ceremony.md`) · the RPR overlapping-series pick recorded in the as-run (裁-63) · the full DB backup before the factory reset, and `0155` applied AFTER it (裁-67) · the 资料缺失 marking for the missing owner evidence (裁-63) · Playwright real-browser coverage of the invite/members keyboard paths. |
| `coa-template-survey.md` | historical (delivered) | **裁-21** · the firm-level standard chart of accounts, **ESTATE SURVEY (as-found)** — findings **F1-F10**, and a **LIVE RIG REPLAY at frontier `0142`** (137/137 migrations + both seeds on a throwaway `postgres:17`) whose ledger records **two REFUTED predictions**. Headline **F1**: the interview already asks *"Apply the standard LHDN-aligned MPERS Chart of Accounts seed?"*, `requiredForCommit: true`, and `coa_seed_decision` has **ZERO consumers** — a shipped promise the product cannot keep. **F2a** the `upsert_account` chain has **FOUR** members (`upsert_account` → `_upsert_account_core` ← `_agent_upsert_account_core` ← `wake_upsert_account`), all four shas pinned; **F2b** the live core carries a `CLR37` rung **in no migration file** — the survey's own proof of the read-the-catalog law. **F5** `client_fact_keys` holds **five** keys, not two, and **`trade_nature`** (a validated enum the close already reads fail-closed) is a stronger trim axis than the optional format-only `msic`; **F10** `chart_templates`/`chart_template_versions` ALREADY EXIST and are **dataviz** specs — a real naming collision, plus the `firm_id IS NULL` scope idiom R-L26 forbids. |
| `coa-template-design.md` | historical (delivered) | **裁-21** · **the design doc of record** (gate CLOSED 2026-08-29 — record below; **BUILT: #462 governed COA apply + #479 claiming 0156, both shipped 2026-08-31 — FS-6 closed**; D-14's tax column stays deferred to F-T3 PR-4, Track B paused). Four relations under a `coa_` prefix (`coa_templates` · `coa_template_families` — **the trim unit** · `coa_template_accounts` · `coa_template_adoptions`, one relation two states per TA-P11), an **explicit `scope` column** with a POSITIVE cross-firm visibility cell, versioning as a **copy not a reference** (a template edit is structurally incapable of touching an applied chart), `apply_coa_template`'s nine-rung ladder whose **rung 5 REFUSES a non-empty chart** rather than additively merging, the trim on `interactive_client` writing **zero** `coa_accounts` rows, **裁-22's law applied to a FACT citation** (the basis resolves against `client_facts`, not `document_regions`), **D-10 "template first, balances after"** on four independent grounds incl. `opening_tb_targets`' FK, drift as a **READ never a wall**, **D-14** deferring the tax column to F-T3's PR-4 while cutting the families tax-aware NOW — and a **D1 write-quiesce inventory of EMPTY**, because F-A3 PR-1a's `_upsert_account_core` is already the seam. |
| `coa-template-annexes.md` | historical (delivered) | **裁-21** annexes — **A** the source ladder re-fetched 2026-08-29 (**no official Malaysian chart of accounts exists**; MPERS ¶4.2/¶5.5 minimum line items rank 1, SSM's MBRS/SSMxT rank 2 but only a 2022 draft was reachable, LHDN prescribes none, MSIC keys at Section/Division never the 5-digit leaf) · **B** the family cut (core · by-industry · the eight tax-split families · the entity-type equity swap) · **C** the 20-cell battery, every wall proven by a REFUSAL with its adversarial twin · **D** the frontend homes and six cross-train obligations (T2 · T4 · T11 · F-A7b PR-c's `clientOnboarding_v4` · 裁-22 · F-T3 PR-4) · **E** the nine non-goals · **F** the full DDL · **G** the four-PR build sequence with PR-0's seven replay obligations. |
| `coa-template-gate-record.md` | historical (delivered) | **裁-21's gate — CLOSED, ALL TWELVE RULED 2026-08-29 (裁-23)**, each written onto its own question: the template is **RESEARCH-DERIVED and the owner waived his review of the draft** (so a research lane precedes PR-0 and it ships published), **Q2 OVERRULED** (neither legacy numbering convention), **Q6/Q8/Q11 WIDENED** (Clara asks first when the industry is unknown; the add-back list and the statutory-payable names both come from the research), the rest as recommended, plus the **COA maintenance model** at §1a. *(As put, pre-ruling:)* Twelve owner questions, each answerable in one line by a Malaysian accounting-firm principal, each with a recommendation, the cost of the alternative, and a fail-closed default so a deferral never blocks the build: **Q1** do you already have a standard chart (the big one — hand it over and it becomes the template) · **Q2** numbering · **Q3** who applies vs who publishes · **Q4** taking over a client with existing books, whose chart wins · **Q5** auto-apply at creation (recommended against; it would cost a D1 window and remove the review moment) · **Q6** what Clara proposes when the industry is unknown · **Q7** add a `trade_nature` interview question · **Q8** tax-sensitive expenses in their own accounts · **Q9** re-word the LHDN claim · **Q10** entity-type equity swap · **Q11** statutory-payable names (not researched, stated) · **Q12** MSIC 2008 vs 2025. §4 carries six build obligations that need no ruling. |
| `binding-proposal-survey.md` | **live** | **裁-18b** · the Clara vendor-binding PROPOSAL door's ESTATE SURVEY (as-found) · v1, 2026-08-29, read at `main` `36c2fd50` with a **rig replay at frontier `0142`** (137 migrations; every body pinned by prosrc sha256). Findings **S1-S10**, censuses **C1-C14**, absences **A1-A6**, predictions **P-1..P-6**. Headline: the binding machinery is complete on the HUMAN axis and has **zero agent reach** (88 allowlist rows over 7 kinds, not one naming a binding verb); every content field is **already DB-derived**, so this door persists no model-typed fact at all; `created_by` carries **no FK** and `clara.agent_user_id()` is a real `is_agent` user row, so 裁-18a's wall passes by construction. Three live gaps recorded: the **post-time re-check no longer exists** (`execute_rule_post` retired at `0118`; nothing writes `vendor_binding_resolutions.phase='post'`), **N open proposals are admissible** for one pair, and no verb ever writes the `declined` status the CHECK admits. |
| `binding-proposal-design.md` | **live** | **裁-18b** · **the design doc of record** · v1, 2026-08-29 — **the gate is CLOSED-WITH-FOLD** — 裁-25 ruled all eight (`binding-proposal-gate-record.md`). The door is `clara.wake_propose_vendor_identity_binding` on the **existing `filing` kind plus `interactive`** for 裁-18c's human ask — **no new wake kind, no new role, no `wake_credentials` CHECK change, no G1 engine source, no egress authorization**; a companion read `wake_list_binding_candidates` keeps the trigger from probing by refusal. **The whole item **SUPERSEDED — the item replaces TWO audited writer bodies** (`sign_vendor_identity_binding` under 裁-32's effective-proposer wall, and `propose_vendor_identity_binding` under the conductor's decline-suppression ruling), so PR-1 now carries a D1 window; the pre-ruling text below claimed it replaces ZERO audited writer bodies:** the frozen derivation is left byte-exact (its `content_hash` covers the evidence array) and extended by a **non-hashed sibling**, `_derive_vendor_binding_basis`. Thirteen walls **W1-W13**, incl. a bidirectional honesty CHECK (`proposed_by_agent = (created_by = clara.agent_user_id())`), a partial unique index closing the duplicate-open-proposal gap with **no body change**, and a 裁-22 basis floor that **forbids a model-supplied `sightings`** outright. |
| `binding-proposal-annexes.md` | **live** | **裁-18b** annexes — **A** the verbs, columns, index and the complete allowlist/grant delta · **B** `clara.binding_agent_receipts` and its mapping onto the 19-column receipt contract as the **ninth** registered member (neither existing table can host it: `agent_filing_receipts`' filed-iff-clean CHECK and `agent_act_receipts`' mandatory `wake_task_id`, both measured) · **C** the battery — wake authority proven **both directions with a real credential through the executor role** on all seven kinds, the thirteen walls, the frozen-derivation pins, the sign path over an agent-created row incl. the solo-firm case · **D** the frontend delta · **E** seven risks and six named non-goals · **G** the cross-lane ledger (裁-18a's wall must be an ACTOR comparison, not "the proposer must be human", or every single-admin firm is stranded) · **J** the D1 inventory, empty of writers — **SUPERSEDED: THREE writer bodies now (the 2026-08-30 fold round added `_tf_vendor_identity_binding_update`, whose freeze list could not see the maker/checker columns PR-1 adds); annex A.5's "unchanged surfaces" list is dead with it.** **TRUED 2026-08-30 (fold N-2)** — each superseded claim now carries a dated block naming what shipped instead, and the three lifecycle verb contracts the annex never specified live in Annex K. |
| `binding-proposal-annexes-k-fold-verb-contracts.md` | **live** | **裁-18b Annex K** — the verb contracts `binding-proposal-annexes.md` never carried, written from the shipped migration rather than from a draft, so PR-2/PR-3/PR-4 build from a true contract. **K.0** the ONE lock protocol every lifecycle writer now follows (unlocked pair read → `clara._binding_lock_pair` advisory key → row `FOR UPDATE`) and the three protocols it replaced, one of which let a `decline` committing inside an open transaction race a concurrent `propose` and land a LIVE binding on a pair a human had just refused · **K.1** `decline_vendor_identity_binding` · **K.2** `reset_binding_decline`, which lifts a DECLINE only — a revoked pair refuses `binding_revoked_reset_requires_ruling` pending the owner's ruling on what ceremony should lift one · **K.3** `_expire_stale_proposals`. Each with signature, role floor, refusal tokens, status transition, lock order, audit payload and domain event. Split from the parent file to keep it under the repo's per-file line cap. |
| `binding-proposal-gate-record.md` | **live** | **裁-18b's gate — CLOSED, ALL EIGHT RULED 2026-08-29 (裁-25):** G1/G3/G4/G8 as recommended · **G2 closed by fact** (`0143` shipped the document-SET resolver) · **G5's premise changed** (裁-17's ninth `row_kind` is live at `0146`, so the tenth ships) · **G6 OVERRULED** — the post-time binding re-check is restored inside 裁-18, its own PR and its own D1 window · **G7 WIDENED** — the `decline` verb AND the expiry sweep both ride. **The item is now FIVE PRs / two D1 windows minimum.** *(As put, pre-ruling:)* **eight owner questions, each with the 大白话 briefing, the cost of every arm and the lane's recommendation.** **G1** the wake kind and trigger (rec: `filing`+`interactive`, event + human ask, no sweep) · **G2** 裁-22's shared resolver needs a **document-SET** signature because this door's basis spans three documents — the hardest cross-lane dependency · **G3** no fingerprint count in `_coding_lane_core` · **G4** the receipt-registry key for a pre-beta item · **G5** the tenth `row_kind` sequenced after 裁-17's ninth · **G6** the vanished post-time re-check — record or restore · **G7** a `decline` verb rides, an expiry sweep does not · **G8** confirm the one-open-proposal rule changes the HUMAN door's behaviour. **G1, G2 and G4 gate the first PR.** |
| `mohe-alignment-audit-2026-08-29.md` | reference | **The 磨合 alignment audit (2026-08-29, read-only at `origin/main` `7e9180df`)** — four census lenses (backend doors ↔ frontend calls · user flows · the agentic vision · design system + provided resources) plus a backend residual sweep, every candidate gap then put through **two independent adversarial passes whose only job was to refute it**: **28 refuted, 15 survived**, and §3 lists the refutations so the coverage is visible, not just the residue. Headline: **no BLOCKING-BETA gap** (the one nominee was downgraded on a mitigation the original finding missed, with the dissent recorded rather than suppressed). **8 must-before-beta**, three of them owner rulings no lane can absorb — **MBB-1** Wave G's oracle-evidence gaps (highest latency in the queue; zero engineering path, non-zero external turnaround) · **MBB-2** F-T3's acceptance oracle OQ-1, whose unruled default fails OPEN · **MBB-3** 裁-18b build-authorized on owner Q&A without the independent design gate 14 of 16 sibling sets ran. The four agent-buildable ones — **MBB-4** the live `chatTurn_v14` emitting four part kinds neither frontend could render · **MBB-5** the ⌘K manifest stale in both directions · **MBB-6** the dated "built in P3" copy · **MBB-8** the unexecuted Q4 colour lint — plus **P-2** the Mobbin MCP stanza and the **P-3/P-4** doc truings were landed by the web/alignment-fixes PR of 2026-08-29 **in `apps/web` only (TRUED 2026-08-30)** — `apps/dashboard`, the app `app.clarabook.com` still serves, was not touched: its union (`apps/dashboard/app/shared/parts.ts`) still has no `entry_posted`/`question_opened`/`bank_act`/`bank_pack` member, so all four still render there as the `FALLBACK_UNSUPPORTED_PREFIX` "Unsupported part:" chip (`apps/dashboard/app/chat/parts.tsx:131`) until the P6 cutover retires it; whether to patch `apps/dashboard` meanwhile is an owner question batched, not decided here; **MBB-7** (the identifier-promotion duplicate-open wall · the spreading `trigger_id` looseness) and **P-1/P-5, R-1/R-2/R-3** remain open. §4 is the backend residual queue with SILENT-RISK rows first (M9's always-empty `list_open_items_by_counterparty` at the top), §5 states the instrument limits honestly — read-only, no docker, no rig, no live DB, ~100 lanes active during the pass. |
| `binding-proposal-pr0-gate-2026-08-29.md` | **live** | **裁-18b's PR-0 INDEPENDENT DESIGN GATE (2026-08-29)** — the one design set of sixteen that was build-authorised on owner Q&A alone; this is the lens review + per-finding adversarial verification every sibling ran, merged with the Codex cross-model pass into ONE ranked list (`[N]`/`[C]`/`[N+C]` attribution) against the LIVE lineage at frontier `0147` (the set was authored at `0142`). **Verdict FOLD REQUIRED — 1 CRITICAL, 8 blockers, 9 materials, 4 nits.** Headline **C1** (Codex): a poisoned approval corpus binds future invoices to the WRONG VENDOR — `_binding_f3_holds` corroborates on an unanchored **name substring** (`0028:246-326`), runtime matches F1 by `starts_with` (`0030:427`), and law 79's collision guard `name_family_is_ambiguous` (`0103:781`) is **never called in the vendor-binding estate**; the ROME family is the estate's own fixture set. Then **B1** the `interactive` path defeats signer≠proposer (law 69 / `0084:123`; **RULED 裁-32**), **B2** the refusal receipt the design specifies cannot be written because every wall raises, **B3** the design's `_resolve_proposal_basis` signature is wrong in order AND in the third argument's type, **B4/B5** decline+revoke suppression and the propose-vs-sign race **plus** a permanent per-vendor deadlock (nothing ever expires a `proposed` row), **B6** the 14-day predicate is spoofable, **B7** the basis passes on one irrelevant region, **B8** "`_approve_entry_core` is replaced" must mean SPLICE (a retype from `0037` restores the breeding block F-A2 retired) and the port source is **`0046`, not `0029`**. Carries 裁-32 + the conductor's five technical rulings; four owner questions (retroactive identity census · refusal receipts · expiry-vs-revocation at post time · the engine rollout). |
| `binding-proposal-pr0-gate-annex-2026-08-29.md` | **live** | **裁-18b PR-0 gate ANNEX** — the evidence the ranked list compresses: the six re-verified Codex claims with **two corrections** (`name_family_is_ambiguous` is at `0103:781` not `:755`; the post-time port source is `0046:1364-1420`, not `0029` — porting `0029` re-introduces the pre-LCP F1 equality test); C1's live bytes; the two-tier receipt contract in `0126`'s own words; **`0029`'s fourteen-step post-time control** as the contract PR-3 owes; nine refuted candidates; and **§F, the two refutations this pass got WRONG** (the T2 "ask Clara" path, refuted from a narrow ruling while missing standing law 69; and the eligibility predicate quoted at length as a robust wall when it counts journal entries on caller-controlled `posting_date`) — kept in full, because a gate record that erases its own errors cannot show where the second reviewer earned his keep. |
| `mobbin-grounding-wave-2026-08-28.md` | **live** | **Mobbin grounding for the port wave's four NEW flows** — T2 opening balances/carry-down tie-out, T7 coding-lane triage, T8 counterparty merge, T11 in-thread onboarding checklist card. Sibling of p4-mobbin-grounding-2026-08-28.md (kept separate, not merged into it, to stay under the 500-line split limit — the two ground disjoint trains, on their own not-yet-merged branches); jointly closes port-wave-plan-2026-08-28-part2.md's §13. 2-3 cited screens per flow, takeaways mapped to EXISTING components only (`Table`, `StateBanner`, `SectionHeader`/`SectionTabs`, `Dialog`, `Badge`, the `apps/web/components/parts/PartSummaryCard.tsx` catalog — no new primitive). Named anti-patterns: a drag-and-drop lane board (T7, implies a client-composed door call), a bulk approve/dismiss bar (T7, no plural door exists — the same shape the P4 sibling already flagged, now seen twice), a field-level radio-picker merge UI (T8, the door takes no field reconciliation), a side-panel/Canvas checklist (T11, exactly what R7's ruling supersedes). Five flags for the conductor/owner not resolved here. |
| `fe-train-plan-2026-08-30.md` | **live** | **THE FRONTEND TRAIN PLAN for P4 + P6 — the consolidated P6 scope doc the alignment audit found missing.** Measured at `main` `94afbbef`. **§0 carries FIVE truings taken before any train was scoped: ① `chatTurn_v15` is TAKEN and added NO part kind** — F-A6 PR-2 consumed the number on 2026-08-29 (Fly v69) and its own frozen header says `ClaraPartV15 IS ClaraPartV14`, so **P6's four-card bump is `chatTurn_v16`**, everywhere it was written as v15; **②** the catalog is **22, not 18** (MBB-4 closed — the v14 four render), so the bump lands **26**; **③** 裁-1's 70% ring, 裁-2's identity-canvas + `--input` recut and 裁-13's target-size gate are **all UNEXECUTED at the tip**, measured (ten focus-ring carriers still at 50% alpha, not 70; `--input` still `#c7c5bd`; no `(entry)` route group); **④ THE P4 TRANCHE IS MEASURABLY ZERO AND THE SHIPPED INVITE FLOW IS BROKEN** — all sixteen P4 door/relation names return zero hits across `apps/web`, and `apps/web/components/invite-accept-form.tsx` makes **no `callDoor` call and never names `accept_invite`**: it runs `verifyOtp` → `updateUser` → a success redirect, so an invitee gets a valid session with **no `clara.users` row, no membership, and an invite still `pending`** while the UI reports success. `clara.accept_invite` (live body `0145:694`) is the ONLY caller of `_claim_identity_core`/`_add_member_core` — the only path that mints a membership for a real person. **Blocks beta**, and it reorders the plan: **P4-1 is now the invite repair**, ahead of the scope spine. **⑤** MBB-5/6/8 + P-2/P-3 landed; P-1 and P-5 remain, both now ruled. **§1** the measured state — 19 routes, 148 test files, three green a11y gates (27 contrast pairs) with the fourth owed, the nine live-but-unwired P4 doors at their **live** bodies (`invite_member` → `0147:372`, `create_firm` → `0147:497`, not `0141`/`0145`). **§2** P4 = **six** build trains partitioned by FILE OWNERSHIP so they run concurrently (P4-1 the invite repair and P4-2 the scope spine both fork immediately — the two halves of the same identity gap), one DB order (裁-26 + 裁-36), and **two surfaces named, homed and deliberately NOT built** — 裁-46's door does not exist, and billing is billing-PR-4 behind three unbuilt DB PRs **and conditional on whether beta charges at all**. **§3** P6 = six build trains + a backend-gated Track-B train (裁-34, whose tab 裁-44 fixes as a **proposal/receipt surface, never a form**) + the hygiene ride-along + the ceremony-grade cutover. **§4** the residual dependency table with what each train does when its door is absent. **§5** the entry gate (裁-9's third conformance pass + 裁-44's ONE agentic-defaults list) and the four-proof exit gate — whose census must run against a **LIVE catalog on a throwaway rig**, since the standing one is pinned to `0138` and seven verb families at `0149`–`0155` have never been measured. **§6** seven open questions, headed by the one that adds or removes a whole train: **does beta charge at all?** **TRUED 2026-08-31 dawn:** #450 P4-1 and #459 P6-2 merged; the row's zero-P4/broken-invite and hardcoded-title measurements are superseded. |
| `fe-train-plan-2026-08-30-orders-p4.md` | **live** | **The P4 work orders — self-contained per `.claude/rules/handoffs.md`.** **§0 is the shared preamble every order inherits in full**: the reading list, rung-0 **at the LIVE body** (two P4 doors were replaced at `0147`), worktree/junction mechanics with no `pnpm install`, the four verify commands, the test-manifest enumeration rule, **the five instrument laws** (`clickButton` over `fireEvent` inside a portalled dialog · a DISCRIMINATING post-condition · three distinguishable states · RED-before per wall), the named-skills rule (the eight vendored Emil skills + `shadcn`/`impeccable` + the Mobbin/shadcn MCPs), and the `SendMessage` report shape. Then **P4-1 THE INVITE REPAIR** — the beta blocker, deliberately narrow (one door call, no route moves), whose acceptance cell is **"after a successful acceptance, a MEMBERSHIP EXISTS"** asserted on the membership read rather than on the redirect, with a mandatory RED-before proof (delete the call, the cell must go red — a green one is asserting today's broken flow) · **P4-2** the scope spine (`requireFirmScope()`, ONE implementation, THREE entrances — the (full) group and the runtime route are SIBLINGS of (firm) — with both deliberate exemptions and their reasons) · **P4-3** the (entry) group, signup and the fourth entry face (ten cream text rows, **no composited focus rows** — the halo is still at 50%) · **P4-4** members/roles/invites and the mail courier, whose negative test is the one that matters (**no mail when the door refused**) · **P4-5** the operator queue, with its rung-5 walk honestly DEFERRED to the Wave-G `is_operator` ceremony · **P4-6** nav/⌘K/hub · **P4-D** the DB half, which does not start until its IP-wall design sitting closes. |
| `fe-train-plan-2026-08-30-orders-p6.md` | **live** | **The P6 work orders**, each inheriting the P4 file's §0 preamble. **P6-1** `chatTurn_v16` — a NEW frozen closure beside byte-untouched v1..v15, registry repointed, **proven by a built-bundle grep and an in-VM bundle grep, never by the deploy's own success** · **P6-2** the card wave 22 → 26, transcribed field-for-field from the **merged** runtime (the reader is never the declarer), plus 裁-20's sweep-card upgrade (no wire change), 裁-44's tax-draft card as a **named placeholder whose shape belongs to the `ft3-taxprep-design` lane, not this one**, and the ten hardcoded English summary titles at `PartRenderer.tsx:160-163` routed through next-intl (裁-3 tier (a): a conformance item is fixed as found, and this one would red the Q5 string lint the day it lands) · **P6-3** the a11y + token finish: 裁-13's target-size gate with `--target-min` exceptions, 裁-1's 70% ring over a re-run ten-carrier census (the robust query is the COLOUR TOKEN, not the width), the six composited rows in annex 2 §F's non-negotiable order, the base-outline fallback that stops R3 extending the failure app-wide, and 裁-2 4c's `--input` recut · **P6-4** ONE shared signed money input replacing four implementations across eleven call sites, with the key-by-key RM50.00 test the Wave-A defect earned · **P6-5** ⌘K "Do" behind a **per-action live** allowlist read (裁-37), 裁-27's amend-as-a-new-row, the seventh firm-question kind, 裁-17's deep-links · **P6-6** mascot/Ledger Fold/copy pass/entry-face finish · **P6-T** Track B's IA only, with three backend-gated ride-alongs — and 裁-44 fixing the tab's SHAPE before its schedule: Clara drafts R1–R10 and CP204 unasked with a statutory citation and her reasoning per rung, a human signs, so it is a **proposal/receipt surface and never an input grid** · **P6-R** the hygiene ride-along, ONE PR for both counterparty debts because both land on one component (`counterparty_aliases_visible` with its three absence pins and six stale comments to re-point, plus `counterparty_merges` and the `merge_id` receipt key `MergeCounterpartiesResult` never declared — the seam two scheduled PRs depend on) · **P6-X** the cutover, classifying all 61 dashboard suites BEFORE the delete and repointing before deleting. |

## active/ — Wave E's citation-stable historical set (CLOSED 2026-08-16; pinned here PERMANENTLY by the path-stability convention above — `packages/db/migrations/0064_wave_e_theta_close_plan.sql`, `0065_wave_e_epsilon_reporting.sql`, `scripts/hooks/pinned-ids-guard-checks.mjs`, and the `x55`/`x56`/`x57`/`er9-corpus-fixtures` test batteries cite these ten paths verbatim by filename and can never be repointed)

| File | Status | Hook |
|---|---|---|
| `wave-e-contract.md` | historical | The Wave E contract of record, E-R1..E-R14 (ADR-065) — ratified, never re-grilled (digest law 67); cite-only now the wave is ceremonied. **ADR-0071 supersedes specific clauses in place** (E-R5/E-R18 approver, E-R11 key ①, E-R13 absorbed, #25) — the file's bytes stay; the ADR names the changes. |
| `wave-e-design-skeleton.md` | historical | Campaign design skeleton, §1–§2.8 (part 1 of 4) — as-built; the ceremony as-runs are the operative record. |
| `wave-e-design-skeleton-part2.md` | historical | Skeleton continued, §2.9–§2.12 (the E-R6 close-model / E-R12 client-facts hookup). |
| `wave-e-design-skeleton-part3.md` | historical | Skeleton continued, §3–§6. |
| `wave-e-design-skeleton-part4.md` | historical | Skeleton conclusion. |
| `wave-e-design-reporting.md` | historical | The FS/reporting-engine design half (part 1 of 2). |
| `wave-e-design-reporting-part2.md` | historical | Reporting design continued. |
| `wave-e-acceptance-matrix.md` | historical | The acceptance matrix (part 1 of 2) — the five `completed/` ceremony as-runs are now the acceptance evidence of record. |
| `wave-e-acceptance-matrix-part2.md` | historical | Matrix continued: Section F and the cross-section sweeps. |
| `wave-e-delta-handoff-2026-08-13.md` | historical | Clock-out checkpoint for δ final-finding closure — **discharged by the night run**; kept as the rulings' source of record. |

*(These ten stay physically in active/ forever — see the path-stability convention above. Their ceremony as-run records, which physically live in `completed/` already, are listed there under "Wave E → F ceremony as-run chronology", not here.)*

## `completed/` — closed waves and slices

### Phase 3 (Slices 3–6)

| File | Status | Hook |
|---|---|---|
| `slice3-event-spine-contract.md` | historical | Slice 3: `domain_events` + outbox + relay contract. |
| `slice4-durable-runtime-contract.md` | historical | Slice 4: the durable WDK runtime-skeleton contract. |
| `slice5-document-pipeline-contract.md` | historical | Slice 5: upload → OCR → persist-unassigned → assign contract. |
| `slice5-migration-0007-design.md` | historical | Slice 5's migration `0007` design. |
| `slice5-as-built-amendments.md` | historical | Slice 5 as-built deviations from its contract. |
| `slice6-thin-e2e-contract.md` | historical | Slice 6 (GATE 3 thin end-to-end demo) contract. |
| `slice6-migration-0009-design.md` | historical | Slice 6's migration `0009` design. |
| `slice6-delegated-decisions.md` | historical | Slice 6 decisions delegated to build time. |
| `slice6-as-built-amendments.md` | historical | Slice 6 as-built deviations. |

### Wave A / A2 / A2.1 (CLOSED, ADR-022..030)

| File | Status | Hook |
|---|---|---|
| `wave-a-daily-loop-contract.md` | historical | Wave A: the daily coding loop contract. |
| `wave-a-migration-0011-design.md` | historical | Wave A's migration `0011` design. |
| `wave-a-as-built-amendments.md` | historical | Wave A as-built deviations. |
| `wave-a2-ar-myinvois-contract.md` | historical | Wave A2: AR / MyInvois UBL / SST 3-leg / CN-DN / purchase-only autopost contract. |
| `wave-a2-migration-0015-design.md` | historical | Wave A2's migration `0015` design. |
| `wave-a2.1-contract.md` | historical | Wave A2.1: the eval-finding ledger + ADR-026 deferrals, WA21-R13. |
| `wave-a2.1-migration-0016-design.md` | historical | Wave A2.1's migration `0016` design. |

### Wave B (CLOSED on intent, ADR-044..046)

| File | Status | Hook |
|---|---|---|
| `wave-b-contract.md` | historical | Wave B: knowledge + onboarding contract, WB-R1..R18 (ADR-032). |
| `wave-b-migration-0017-design.md` | historical | Wave B's migration `0017` design (part 1 of 3). |
| `wave-b-migration-0017-design-part2.md` | historical | Migration `0017` design continued. |
| `wave-b-migration-0017-design-part3.md` | historical | Migration `0017` design concluded. |
| `wave-b-migration-0018-design.md` | historical | Migration `0018` design (Gate-K domain). |
| `wave-b-migration-0019-design.md` | historical | Migration `0019` design (the wiki authority boundary). |
| `wave-b-migration-0020-design.md` | historical | Migration `0020` design (typed consent). |

### Wave C (CLOSED, ADR-051..054)

| File | Status | Hook |
|---|---|---|
| `wave-c-contract.md` | historical | Wave C: money movement contract, WC-R1..R12 (ADR-051). |
| `wave-c-a-subledger-design.md` | historical | C-a: AR/AP open-item subledger + allocation design, WCA-R1..R9. |
| `wave-c-b-bank-design.md` | historical | C-b: bank statement ingest + matching design (part 1 of 3). |
| `wave-c-b-bank-design-part2.md` | historical | C-b design continued. |
| `wave-c-b-bank-design-part3.md` | historical | C-b design concluded. |
| `wave-c-c-tieout-design.md` | historical | C-c: tie-out / aging / learn-loop design, WCC-R1..R8 (part 1 of 2). |
| `wave-c-c-tieout-design-part2.md` | historical | C-c design continued — the round record + ratified deviations. |

### Wave D (COMPLETE, ADR-055..059)

| File | Status | Hook |
|---|---|---|
| `wave-d-contract.md` | historical | Wave D: assets + adjustments contract, WD-R1..R15 (ADR-055). |
| `wave-d-a-fa-design.md` | historical | D-a: fixed-asset register design (part 1 of 2). |
| `wave-d-a-fa-design-part2.md` | historical | D-a design continued — the round-2 record. |
| `wave-d-b-design.md` | historical | D-b: adjustments + advances DESIGN-TIME record — banner: describes a monolith that never shipped as designed; see `wave-d-b-asbuilt.md` for the real shape. |
| `wave-d-b-design-abi.md` | historical | D-b's builder ABI (signatures, DDL blocks, op-key matrix) — DESIGN-TIME only, same banner. |
| `wave-d-b-design-part2.md` | historical | D-b design ladder record continued. |
| `wave-d-b-design-part3.md` | historical | D-b design ladder record concluded. |
| `wave-d-b-asbuilt.md` | historical | D-b AS-BUILT record — the real four-slice split (`0042`/`0043`/`0044`/`0045`), ADR-058/059. |
| `wave-d-b-asbuilt-part2.md` | historical | D-b as-built ladder record continued. |

### §7-A — the unattended sales drafter (CLOSED, ADR-063/064)

| File | Status | Hook |
|---|---|---|
| `wave-7a-contract.md` | historical | §7-A contract, 7A-R1..R12 (ADR-063). |
| `wave-7a-design-skeleton.md` | historical | §7-A design skeleton v2. |
| `wave-7a-acceptance-h1.md` | historical | §7-A acceptance, half 1 — 22 real ROME SECRETARY invoices. |
| `wave-7a-acceptance-h2.md` | historical | §7-A acceptance, half 2 — sandbox unattended posts + the nine-controls receipt battery. |

### The extraction slice + settlement program (CLOSED, ADR-047/048)

| File | Status | Hook |
|---|---|---|
| `extraction-slice-contract.md` | historical | Re-extract verb + governed high-stakes verb + two-reader-corroboration-last contract. |
| `extraction-slice-x7-field-record.md` | historical | The reader's field story on the 7 real affected documents. |

### Autopost vendor binding

| File | Status | Hook |
|---|---|---|
| `autopost-vendor-binding-design.md` | historical | The autopost vendor-binding design (part 1 of 2). |
| `autopost-vendor-binding-design-part2.md` | historical | Vendor-binding design continued. |

### The currency defect (task #24 — reader half SHIPPED, PR #133; override half deferred)

| File | Status | Hook |
|---|---|---|
| `currency-defect-design.md` | historical | The currency-reader design + measured defect (part 1 of 2). Archival note in-file: nothing here re-opens a shipped decision. |
| `currency-defect-design-part2.md` | historical | The remedy decision — reader now, override deferred behind a named trigger (part 2 of 2). |

### Wave E — first strike + closed lane records

| File | Status | Hook |
|---|---|---|
| `wave-e-f6f9-acceptance.md` | historical | The F6–F9 fix-batch acceptance record (ADR-066). |
| `wave-e-lane-alpha-acceptance.md` | historical | Lane α as-run acceptance record (`0055`, PR #226). |
| `wave-e-lane-beta-acceptance.md` | historical | Lane β as-run acceptance record (`0056`, PR #228). |
| `wave-e-lane-gamma-acceptance.md` | historical | Lane γ as-run ceremony record: `0057` applied quiesce-free, E5 taken live, the trap re-proven (PR #231). |

### Wave E → F ceremony as-run chronology

| File | Status | Hook |
|---|---|---|
| `wave-e-delta-ceremony-asrun.md` | historical | The 0058-0063 live ceremony as run: positive reads, the two field findings (SUSET, pooler-pid), deviations with grounds, the no-print DSN bridge. |
| `wave-e-theta-epsilon-ceremony-asrun.md` | historical | The 0064-0072 live ceremony as run (θ+ε together): 9/9 clean, backup-first, the Windows pnpm-shim trap, the probe-defect lesson, the live #43 gate. |
| `wave-e-final-ceremony-asrun.md` | historical | The 0077-0084 live ceremony as run (η wake wrappers + ζ render family + B4): D1 write-quiesce, the chatTurn v11 deploy, the freeze deploy-lock (140/140), positive reads throughout. |
| `b3-reopen-ceremony-asrun.md` | historical | The 0085-0086 live ceremony as run (B3): backup-first, D1 quiesce, ALL-PASS positive reads (81/`0086`, the segregated 5-arg body, the 4-arg form gone), two probe instrument defects owned in-line. |
| `masb-wording-ceremony-asrun.md` | historical | The 0087-0088 live ceremony as run (#43): backup-first, no quiesce, ALL-PASS reads (83/`0088`; en 5/5 + zh 5/5 verified — E-R14 OPEN; ms 4/5 per the sign-off), the owner's three dispositions of record. |
| `f-a1-pr1-ceremony-asrun.md` | historical | The 0089-0095 live ceremony as run (F-A1 PR-1, 2026-08-19): backup-first, D1 with the 110s staleness wait (the 0023-lineage in-file quiesce guard's first live FIRING), 28/28 positive reads (90/`0095`), evaluator deploy flip 2→4 + `--lock-deployed`, the sslmode deviation + dsn-pipe harness gap recorded, the live statement-pair coin-flip measured (15 docs/24 pairs). |
| `f-a1-pr3-ceremony-asrun.md` | historical | The F-A1 PR-3 (witness-pair cutover) ceremony as run (2026-08-20): migrations `0096`/`0097` live; runtime v65 verified live pre-window by in-VM bundle grep; the v5 recipe's new pre-quiesce tripwire step. |
| `f-a1-corpus-measurement.md` | historical | The F-A1 PR-3 gating corpus measurement, as run (2026-08-20): live post-cutover model calls against 33 of the widened 64-document BELCORT invoice-kind population (owner-stopped tail, audited claim→fail door). Headline: 0/33 corroborated under the witness regime vs 28/92 legacy — two named binding conjuncts, neither a bug; 0/69 model-call failures; D12 wrong-party gate PASSES at 0 false positives. |
| `wave-f-w1-ceremony-asrun.md` | historical | The Wave-F W1 window as run (2026-08-24): four train cars to live 103/`0108`; the two CI mechanism fixes (#314/0103); the post-W1 D-a cascade context. *(Index row added at the 2026-08-26 clock-out — the file predates it.)* |
| `wave-f-w2w3-ceremony-asrun.md` | historical | The W2+W3 combined window as run (2026-08-25): ten migrations, live 112/`0117` → 122/`0127`; Track A backend fully live; backup-first, 11-session reap, 9/9 probes. *(Index row added at the 2026-08-26 clock-out.)* |
| `mohe-0139-0140-apply-asrun.md` | historical | **The 0139+0140 apply as run (2026-08-28, D1 WINDOW): live 133/`0138` → 135/`0140`.** Sweep ALL-GREEN first · backup banked (23.1 MB, hc-ping) · all five refuse-conditions read positively (tripwire 7/7 byte-match — no live drift) · ~4-min quiesce with a zero-non-idle read · both tails green (the park inverted, allowlist 12→13 measured, R6 scope cut proven) · `/ready` 200. The limb lands live-inert; the evaluator stays `deployed=false` until PR-2b. Carries the guessed-signature instrument note (law 2 applied to one's own probe). |
| `mohe-0142-0146-apply-asrun.md` | historical | **The combined 0142–0146 apply as run (2026-08-29, ONE ~51-s D1 window 03:17Z): live 136/`0141` → 141/`0146`.** F-A7b PR-a · 裁-22 DB-resolved proposal bases · hardening A (the barrier over ELEVEN masked views + the signer≠proposer wall) · P4 tranche-2 · 裁-17's ninth row_kind. The five refuse-conditions read positively (sweep ALL-GREEN on `b2b0eca5` · backup banked 23.2 MB at head 136 · tripwire 45/45 identical as sorted sets · in-flight zero with controls, incl. the live `serializable` proconfig pin CONFIRMED on the approve doors · TLS by construction); post-reads on live identical to the rehearsal rig apart from the frozen-schema counts; two instrument notes (the backup poll vs ANSI colour; a post-splice sha that moved with the renumber's comment bytes). |
| `mohe-0148-0153-apply-asrun.md` | historical | **The 0148–0153 apply as-run (2026-08-30, the overnight merge train: six files, one ceremony, TWO D1 windows): live 142/`0147` → 148/`0153`.** ELEVEN live writer bodies replaced (0148: 2 · 0149: 4 · 0151: 5), nine witnesses byte-identical; rehearsal rig at 0147 (six files in one migrate call, second apply 0 new, mechanical RECUT/WITNESS body check) · tripwire 64/64 identical · backup banked 23.3 MB · in-flight zero with controls. **Window 1 was REFUSED at `0151` by the file's OWN quiesce guard** (a runtime heartbeat younger than 90 s — the apply came 13 s after the stop; correct behaviour, the kit's pacing was the defect); `0148`–`0150` had applied, live consistent at 0150; window 2 paced on a positive heartbeat-age read and applied `0151`–`0153`. Post-reads on live identical to the rehearsal apart from the REPORTED rows; `/ready` 200 after both windows; the manual sweep dispatched after. Two read-instrument defects fixed before live was read (`reason_key`; psql CRLF on a last-field sha). |
| `mohe-0147-apply-asrun.md` | historical | **The `0147` apply as-run (2026-08-29, hardening B · 裁-16 hash-only bearer tokens): APPLIED, live 141/`0146` → 142/`0147`**, one D1 window of 47 s stop-to-ready (08:46:55Z → `/ready` 200 at 08:47:26Z). Two live writer bodies replaced (`create_firm`, `invite_member`) with `_create_firm_core` byte-identical across the window; all four live admission rows backfilled to a 32-byte hash, the plaintext column GONE, `scrubbed 0` (live carried no plaintext receipt). Five gates first — a rehearsal rig at the live frontier (two read-instrument defects found and fixed BEFORE live was read: `"char"` casts, and `_create_firm_core`'s real `(uuid,text)` signature) · tripwire 29/29 identical rig-vs-live · backup banked · in-flight zero · the populated-upgrade drill 8/8 on two independent rigs. Also records **the sweep RED that preceded the window** (a shared `seedAdmission` fixture following HEAD; PR #415 made it frontier-aware; product code never wrong) and the 裁-28 pricing-brief read (`llm_usage_events`, four rows on live — a thin cost floor that says so). |
| `mohe-0141-apply-asrun.md` | historical | **The 0141 apply as run (2026-08-28, D1 window ~37 s): live 135/`0140` → 136/`0141`.** P4 DB tranche-1 (identity claim · in-firm invites · roster/invite reads); the five refuse-conditions read positively (sweep ALL-GREEN · backup banked · rig-vs-live tripwire 12/12 · in-flight zero with controls · TLS both polarities); two instrument notes (the `fly machine status` two-`State`-lines parse, the `polcmd "char"` post-read). |
| `mohe-0137-0138-apply-asrun.md` | historical | **The 0137+0138 apply as run (2026-08-27, WINDOWLESS): live 131/`0136` → 133/`0138`.** No D1 window, by derivation borne out at the tails — `0137` creates three masked views only; `0138` is additive (three tables · twelve wrappers · the settle door · the deferred Tier-C trigger) with only two READ-oracle bodies recut. The close agent limb lands live-inert behind `enabled=false`. |
| `wave-f-w4-ceremony-asrun.md` | historical | **The Wave-F closing window as run (2026-08-26): nine migrations, live 122/`0127` → 131/`0136`; the BL-3 flip — card-1 stage (b) LIT (freeze 7/7).** Carries: the RED first sweep (stale closed-wave floor vs 0129's retirement) → #352's succession-aware floors → the ALL-GREEN second sweep incl. wave-e contract drills' first true run · backup banked first (22.8MB, hc-ping) · the stale-DONE-line watcher lesson · tripwire ALL-PASS · 11-session reap, 0 non-idle · 8/8 positive-read probes · `/ready` 200 + six leases · both sleepers destroyed. |
| `f-a2-window-ab-ceremony-asrun.md` | historical | **The F-A2 opener train's activation, as run (2026-08-21) — and the corpus re-measure in the same file.** Windows A and B were **COMBINED**, with the grounds recorded (a fully-merged train makes a split create a stall gap, not separate risk; both flips independently reviewed; machine STOPPED across both). Live 92/`0097` → **97/`0102`**, runtime v65 → **v66**. Carries: the backup banked first · the pre-window PROCESS reads **and the `$`-expansion instrument failure the positive control caught** · the tripwire ALL-PASS plus one abort taken BEFORE any stop · the 5-migration apply · the probe 20/21 with the one red owned as a **probe defect** (B3 precedent) · the evaluator flip 4/4→5/5 · **the `0102` coverage probe SAYING NO as designed**, naming the synthetic sandbox firm, adjudicated ACCEPTED · v66's in-VM bundle greps, `/ready` 200, zombie sweep 0 · **the re-measure: 12/20 vs 0/20 like-for-like, the denominator rule verbatim, prediction 14/20 with every miss named, lock 3 firing on the one genuine registrant, type_code 19×`01` + 1×`03` correct** · five findings for the next round · the deploy-lock of both manifests. |

### The former REBUILD-PLAN.md + the PROGRESS archive

| File | Status | Hook |
|---|---|---|
| `rebuild-plan-history.md` | historical | The former REBUILD-PLAN.md's verbatim Phase-3 + Phase-4 dated STATUS chronology, archived at this refactor. |
| `progress-archive-2026-08.md` | historical | PROGRESS.md's split-out (the outgrow law, 2026-08-18): the sixteen terminal Wave-E lane rows, the session log through 2026-08-16, the superseded posture bullets, the tombstones, and the 2026-08-20 backlog-disposition REASONING — all verbatim. **Itself split 2026-08-21** when it passed 500 lines; see part 2. |
| `progress-archive-2026-08-part2.md` | historical | The archive's own outgrow split (2026-08-21, the same SPLIT-never-prune law applied one level up): the 2026-08-18/19 F-A1 build-night session-log entries, the 2026-08-20 night's entry, and the two Known issues resolved at or before the combined Window A+B — BEE's opening-TB discrepancy, and the statement-pair coin flip whose ACTIVATION heal-point `0102` finally made real. Verbatim. **Itself split 2026-08-26** when it too passed 500 lines; see part 3. |
| `progress-archive-2026-08-part3.md` | historical | The archive's second outgrow split (2026-08-26, the same law applied again): the four ceremony as-run posture bullets (W2+W3 through W4) plus their superseded records, moved verbatim from `PROGRESS.md` at the W4 clock-out. |
| `progress-archive-2026-08-part4.md` | historical | The archive's third outgrow split (2026-08-30, opened when the 磨合 sprint's own entries pushed `PROGRESS.md` back to its 500-line cap): the **2026-08-22 · 08-23 · 08-24 · 08-25 session-log entries** (the Track-A sitting + v2 design landing · the alignment grill and split pass · the train night + W1 · the W2+W3 combined close) and the **F-A7 gamma residuals R1/R2/R3** — all moved **verbatim**, none summarised on the way in, and all three gamma residuals still STANDING (`PROGRESS.md` keeps the pointer, so law 82's "a deferral belongs in the record" is satisfied by the pair). |
| `progress-archive-2026-08-part5.md` | historical | The archive's fourth outgrow split (2026-08-30 evening, opened when the day's evening sitting — 裁-57…72 — pushed `PROGRESS.md` back to its cap): the **2026-08-29…08-30-noon session-log entries** and the **`Next` items 2/3/5 DONE narrative** (the #43 sitting + BEE FY2025 close dispositions and golden-bar figures · the F-A2 build's merge history · the owner's digest sign-off's full landed-follow-up list) — moved **verbatim**, each `Next` item's still-open forward obligation left inline in `PROGRESS.md`. |
| `progress-archive-2026-08-part6.md` | historical | The archive's fifth outgrow split (2026-08-31, opened at the #454 merge round): the 裁-22 / F-A6-PR-2-runtime / R4-R5-R7-digest Backlog-block DONE bullets — 29 lines moved **verbatim** per the archive law. |
| `progress-archive-2026-08-part7.md` | historical | The archive's sixth outgrow split (2026-09-02, opened at the pre-pause truing sweep): the superseded 2026-08-31 beta-pivot posture banner, the 2026-08-30-noon-through-W4-closing historical `Current posture` bullets, five more now-historical posture bullets (the Track-A sitting pointer · render deployment · hard constraint 12 · harness hardening · the 2026-08-30-evening sitting digest), and the two oldest `Session log` entries (2026-08-31 morning + 2026-08-30 evening) — moved **verbatim** per the archive law; the new 2026-09-02 pause-window banner (naming the 15-merge 09-01 day, 裁-95…113, and the COMBINED pause window) replaces the demoted one in `PROGRESS.md`. |
| `progress-archive-2026-09-part3.md` | historical | The 2026-09 archive's third part (**2026-09-06**, opened at the report-disposition truing because `PROGRESS.md` could not take the trued Backlog and Known-issues text inside the 500-line ceiling): **both entries of the `Session log`** as they stood before that truing — 2026-09-04 "beta went live" (23 lines, 2304 bytes, md5 `759c32130a33e2584574fcdbd506c6ea`) and 2026-09-04→05 "the repair session" (28 lines, 2767 bytes, md5 `fb1f9181f13d71d3444fa62474f73088`) — each moved **byte-for-byte** with its md5 computed on both sides and verified present here before `PROGRESS.md` lost it. The repair-session entry's "NOTHING DEPLOYED" claim is superseded by the 2026-09-05 ceremony; the supersession note is in this file's preamble, never in the moved text. *(Parts 1 and 2 of the 2026-09 archive are not yet indexed here — a pre-existing gap this PR did not open.)* |
| `progress-archive-2026-08-part8.md` | historical | The archive's seventh outgrow split (**2026-09-04**, opened at the FINAL clock-out truing because part 7 could not take two more sections): the **whole `Backlog` section and the whole `Known issues` section of `PROGRESS.md`** as they stood immediately before that truing, moved **byte-for-byte** with md5s computed on both sides and verified present here before `PROGRESS.md` lost them. Every still-open row is carried — with an id, an owner, a next step, a size guess and a tier — in `docs/plan/active/beta-handover-2026-09-04.md` and its part 2, so this file is the PROVENANCE, not the queue. |
| `fs10-cutover-asrun-2026-09-03-part1.md` · `-part2.md` · `-part3.md` | historical | **The FS-10 P6-X cutover.** Parts 1–2 are the ceremony's step TEMPLATE (S1…S27, 49 boxes) written before it opened; **part 3 is the cutover AS IT HAPPENED and governs**: the Workers build in WSL, the Worker `clara-web` created, eight versions (with version H found carrying FIVE of six secrets — read, not assumed — and version **I `c5b1e051…`** promoted instead), the SSE streaming proof (TTFB 1.83 s of a 6.05 s total), the domain moved off Pages at ≈00:31 MYT 2026-09-04, the real-origin re-walk, the `mailer_otp_length` = 8 defect only a real walk could find, and the Pages project's deletion (2,078 deployments across five runs, read back absent). |
| `fs11-wave-g-asrun-2026-09-03-part1.md` … `-part6.md` | historical | **The FS-11 Wave-G factory reset.** Parts 1–3 are the step TEMPLATE; **parts 4–6 are the AS-RUN** — part 4 steps 1→14 (the staged `DROP SCHEMA` after a single transaction ran out of lock slots, the 159-migration re-apply through `0154`'s absolute role census, the circuit-breaker outage traced to the re-migration silently flipping every ceremonied role to NOLOGIN, the 裁-178 password-echo incident, the 裁-179 TLS finding that four production lane DSNs were running in PLAINTEXT, and BELCORT re-minted through the product's own self-serve door), part 5 step 15 and the interview/coding/chat half of the product walk, part 6 the bank lane, the close, the reports and steps 17→19. |
| `launch-sitting-record-2026-09-04-part1.md` · `-part2.md` · `-part3.md` | historical | **The beta launch sitting.** Parts 1–2 are the sitting's PREP template (written before the walk, and carrying the contradictions it flagged); **part 3 is the sitting AS IT HAPPENED and governs**: the 裁-184 walk's closing verdict, the honest milestone tally with a receipt on every row, **裁-185's three options as they were put and the owner's verbatim GO**, and — stated rather than glossed — what the sitting did NOT do, including 裁-171's item-by-item reading, which the walk consumed and which is owed to the next session. |
| `launch-sitting-prep-2026-09-03-part1.md` … `-part3.md` | historical | The launch-sitting PREP pack (2026-09-03): the go/no-go gates, the twenty knowingly-open items, and the contradictions to carry into the sitting. Superseded in substance by the launch-sitting record's part 3 and by `beta-handover-2026-09-04.md`. |
| `owner-decisions-2026-09-03-part1.md` · `-part2.md` | historical | The 23 owner ceremony decisions pack (2026-09-03) — the INPUT to the grill that produced 裁-151…173. Each decision is stated with its options, its recommendation and its cost; the RULINGS are in `docs/plan/active/mohe-grill-rulings-2026-09-04.md` and in the ruling texts below. |
| `fs10-cutover-prep-2026-09-03-part1.md` … `-part3.md` | historical | The FS-10 cutover PREP pack (2026-09-03) — the survey the as-run template was cut from. |
| `fs11-wave-g-reset-prep-2026-09-03-part1.md` … `-part3.md` | historical | The FS-11 Wave-G reset PREP pack (2026-09-03) — the survey the as-run template was cut from, including its own NOT-IN-REPO list (no FS-11 runbook exists; the runtime quiesce is in no checklist; there is exactly one reset mechanism). |
| `corpus-manifest-2026-09-04.md` | historical | **The desktop corpus inventory the repo lacked** (FS-11 step 15.2, 裁-180): RPR 117 files · RS 87 · BEE 289 · decrypted RPR 9, with the bank-statement **series pick made BY MEASUREMENT** (`clara-rpr-decrypted` is the only Apr–Jul series both complete and readable) and the **four 资料缺失 marks**, each probed by name and not found. |
| `completed/owner-rulings-151-177-2026-09-03/` | historical | **The TEXTS OF RECORD for 裁-151…177**, one ruling-NNN.md file per ruling, filed verbatim at the final clock-out truing. **These govern on any divergence with the ledger entries** in `docs/plan/active/mohe-grill-rulings-2026-09-04.md`. (裁-178…185 have no separate file: the ledger is their text of record.) |
| `repair-session-2026-09-04-orders.md` | historical | **CLOSED.** The eight QUEUED work orders of the 2026-09-04→06 repair session (DB-D, CI-E2E, L18, L10, CONSENT, DB-C, REPORTING, L17), filed byte-verbatim from the session scratchpad in the order they were issued; their queue positions are `PROGRESS.md`'s Backlog (Q-01 = DB-D, Q-02 = CONSENT-186, Q-03 = DB-C, Q-04 = L10, Q-05 = L17, Q-06 = REPORTING-H15, Q-07 = CI-E2E-192; L18 is unnumbered; Q-08/Q-09 have no order filed here). Each order inherits `repair-session-2026-09-04-house-rules.md`; the live queue and its order are `PROGRESS.md`'s Backlog, which governs. |
| `repair-session-2026-09-04-house-rules.md` | historical | **CLOSED.** The common house rules every build lane of the repair session inherited, plus the migration-renumber tool the lead ran at merge time. The tool is a rename plus basename-reference rewrite that never touches a migration's own bytes, so reviewed barrier hashes hold. Filed byte-verbatim from the session scratchpad. |
| `repair-session-2026-09-04-register.md` | historical | **CLOSED.** The lead's condensed register of the #541 beta handover's 81 items as triaged on 2026-09-05 — id, status, size, anchor, cause, fix, needs and open questions, grouped by area. Filed byte-verbatim from the session scratchpad; the live queue and its order are `PROGRESS.md`'s Backlog, which governs. |
| `repair-session-2026-09-04-roster.md` | historical | **CLOSED.** The orchestrator's dated lane roster for the 2026-09-04→06 repair session — merged PRs, the serial merge chain, rulings 裁-191…201 as first recorded, follow-up rows, and the ceremony log — filed byte-verbatim from the session scratchpad. The rulings of record are `mohe-grill-rulings-2026-09-04-pm.md` and `README-rulings-2026-09.md`, which govern on divergence. |
| `report-disposition-2026-09-06-r1-owner-flaws.md` | historical | **CLOSED.** The per-item disposition of the owner's own UIUX flaws list — 36 items, each split from the file's compound lines, each dispositioned against a line opened on `main` at `fc39c361`. 19 FIXED · 13 PARTIAL · 1 OPEN-VALID · 1 WRONG · 1 DRIFT · 1 OWNER-ACT. Filed byte-verbatim between md5-stamped markers. The truings it drove are in `PROGRESS.md` and the handover part 3's Errata, which govern. |
| `report-disposition-2026-09-06-r2-issue-541.md` | historical | **CLOSED.** The per-item disposition of GitHub issue **#541**, the authenticated production e2e audit (CB-AE2E-001…036), including the §8 Beta Exit Pack verdict — 6 of 16 satisfiable, 8 not, and **two pack items now unsatisfiable BY RULING** (JOURNAL-01 against 裁-187, CONSENT-01 against 裁-186) and needing a re-cut before they can be graded. 20 FIXED · 6 PARTIAL · 5 OPEN-VALID · 2 OBSOLETE · 2 OWNER-ACT · 1 WRONG. Filed byte-verbatim between md5-stamped markers. |
| `report-disposition-2026-09-06-r3-handover-h.md` | historical | **CLOSED.** The per-item disposition of the beta handover's own walk rows, **H-01 … H-56** — 27 FIXED · 8 PARTIAL · 11 OPEN-VALID · 8 OWNER-ACT · 1 DRIFT · 1 OBSOLETE, none STALE and none WRONG. Its §(b) is the docs-sync list this record's own PR executed against `PROGRESS.md` and the handover. Filed byte-verbatim between md5-stamped markers. |
| `report-disposition-2026-09-06-r4-handover-c.md` | historical | **CLOSED.** The per-item disposition of the beta handover's CARRIED registry, **C-01 … C-88** — 3 FIXED · 9 PARTIAL · 4 DRIFT · 1 STALE · 3 WRONG · 1 OBSOLETE · 8 OWNER-ACT · 59 CARRIES. Filed byte-verbatim between md5-stamped markers. |
| `pr232-body-notes.md` | historical | PR #232's body-notes material (the harness grand refactor) — re-homed at the 2026-08-18 sweep from the repo root, where it sat as *_PR-BODY-NOTES.md*; bytes untouched. |

## `docs/plan/` root

| File | Status | Hook |
|---|---|---|
| `research/` | reference | Supporting cross-model research dossiers, organized per wave/slice. Not restructured in this pass — cite the specific dossier inside it, not this line. Dossiers are listed here only when a live plan document depends on the answer; the tree is otherwise browsed, not indexed. |
| `research/wave-g/agui-fit-2026-08-22.md` | reference | **The AG-UI / CopilotKit fit assessment** (2026-08-22, owner-facing, written against official docs with a URL + access date on every external claim, per law 75). Verdict: AG-UI is a transport plus a front-end event convention, **not accounting authority** — what it standardises, Clara already does more strictly. **Recommendation B, relayed and accepted: adopt NO dependency**, and steal three ideas on the existing AI-SDK SSE stream — an `activity` progress frame · a non-authoritative session working-state (scope and progress, zero amounts, lint-enforced) · expandable tool-call evidence rows (cheapest, hits auditability hardest, the data is already on the wire). Grounds: the Vercel AI SDK adapter is unpublished on npm and targets `ai@4` while we run 7.0.31; AG-UI's HITL **ends the run and the client restarts it**, which would put a second approval path through the runtime (forbidden — governance never transits the runtime); generative UI streams model-written tool args into components (PRD §6.1); the core package is pre-1.0 with weekly churn; the dashboard is a static Cloudflare Pages export with no server. **Placement: the Wave-G OS surface** (the evidence-row idea may ride chat parity). Frozen-workflow discipline is NOT the obstacle — any translation would sit in the non-frozen `streamRoute.ts`. |
| `research/card1-substitution-seam-survey.md` | reference | **F-A5b card 1's estate survey, part 1 of 2** — ground for `active/card1-substitution-seam-design.md`. Findings **S1-S23**, every claim carrying a `file:line` the instrument printed: the render entrance (`sandbox_exports`' worker/leader verb split, S1-S13 — **no CLAIM verb ships in 0132**, a registered gap this design's §2.6 closes) and `metric_cells`/the evaluator framework (S14-S23 — a cell is permanently immutable once inserted, S15-S18; supersession/model-proposal/human-approval are schema-reserved but doubly walled shut, S17; the evaluator freeze census re-derives every member's body hash LIVE from the catalog, S21-S23). Read from *the f-a5b/pr-1 branch* at tip `ee76f70` for every `0132.sql:` citation (F-A5b PR-1 is not yet merged); everything else from `main`. |
| `research/card1-substitution-seam-survey-part2.md` | reference | **F-A5b card 1's estate survey, part 2 of 2** — findings **S24-S48**: the block/basis machinery in 0132 (S24-S30, incl. the `v_has_free_text` widening rule a `placeholder` block must NOT trip) · the expression-engine precedents (S31-S38 — the closed eleven-primitive AST validator, the two-phase structural-then-semantic validation shape, and the headline finding that no general-purpose "caller submits a formula" engine exists anywhere in the estate) · the renderer's runtime/privilege (S39-S44 — `clara_runtime` holds no table grant on `metric_cells`; a widened `sandbox_export_payload` is the only lawful path to a cell's value) · the freshness/consistency wall (S45-S48 — the S46 pin-rule: resolve by the exact pinned `cell_id`, never re-derive) · the closed-world move list (migrations/runtime/tests/docs) and the open design questions **D1-D5**, all resolved by the design doc's decisions **CD-1..CD-9**. |
| `research/coa-template-research-2026-08-29.md` | reference | **裁-21's CHART-OF-ACCOUNTS RESEARCH DOSSIER (2026-08-29)** — the lane 裁-23/Q1 ruled must run before PR-0, and **the rigour that stands in for the review the owner waived**. Answers **Q1-Q2, Q7-Q8, Q10-Q12** with evidence; four parallel passes (standards frame · LHDN add-backs with Public Ruling citations · a **nine-product** Malaysian software catalog · MSIC 2008 sections/divisions), every claim carrying its URL and fetch date, and every fallback from an unreachable primary source **stated next to the claim, never silently**. Headlines: **§2 numbering lands on 4-digit blocks** — earned on four legs (Bukku/Sage UBS/NCL converge · QuickBooks' own suggested ranges · the textbook GAAP convention · it fits `ck_coa_account_code_0009`'s FIRST branch), while the majority-installed **AutoCount/SQL Account/QNE `XXX-0000` form is rejected as structurally ROME PROPERTIES' `300-000` shape** the ruling forecloses · **§3 the add-back list grows to ten** (past the design's eight), each with its Public Ruling or ITA section · §4 statutory-payable naming on the mainstream "XX Payable" form, **HRDF flagged as the weakest evidence** · §5 equity by entity type, three sets · §6 industry families keyed at Section/Division · **§7 names what could NOT be verified** (SSMxT's live element list still unreachable; HRDF's default account name; no dedicated PR for s.39(1)(c)). Draft: **101 accounts**, inside the 80-150 band mainstream defaults occupy. Machine-readable companion: `research/coa-template-2026-08-29.json`. |
| `research/pricing-market-survey-2026-08-29.md` | reference | **The MARKET half of the pricing brief 裁-28 owes the owner** (R8(c) ruled the shape — base monthly tier per firm + metered LLM overage; **amounts stay the owner's**, and this file is **evidence, not a recommendation**). Prices pulled live 2026-08-29: §1 Malaysian SME accounting software RM50-180/month · §2 firm-facing tools (Dext ≈RM75-110/client/month; Karbon per-seat) · §3 the AI-bookkeeping entrants (US$60-700+/company/month; SEA's closest proof point, AI Account, ≈RM270/month for unlimited client companies) · §4 what Malaysian firms charge THEIR clients (RM300-800/month) with the 10-20%-of-fee rule of thumb that **bounds the tool-cost ceiling at ~RM30-160/client/month** · §5 official Anthropic/OpenAI list prices. Concludes with **two evidence bands** — base tier ≈**RM300-900/month per firm**, per-client-company ≈**RM50-250/month** — and says plainly that both must still be reconciled against the **cost-floor half** (BELCORT's live per-call spend) before any amount is ruled. Also records what could not be found (Xero Malaysia's own MYR pricing page 503'd on every attempt). |
| REBUILD-PLAN.md | **superseded (deleted)** | Retired at this refactor. Its dated chronology → `completed/rebuild-plan-history.md`; its `coding_kind` roadmap table → `../ARCHITECTURE.md` ("Roadmaps" section); its CURRENT posture → `PROGRESS.md`; Wave F/G + Risks + Phase 5 → `active/roadmap.md`. |
