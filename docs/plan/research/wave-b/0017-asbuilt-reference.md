# Wave B — 0017 as-built reference (extraction fan-out, 2026-07-23)

> Exact as-built surfaces the 0017 design binds to. Produced by the six-reader extraction
> fan-out; shapes are verbatim-as-extracted — re-verify against the migration file before
> relying on one in code. Companion to docs/plan/wave-b-migration-0017-design.md.

## x:constraints+tables

The Wave B surfaces live in packages/db/migrations 0003–0016 with a consistent evolution idiom: inline/system-named CHECKs from early migrations are swapped by later ones via drop-by-definition DO-blocks (0014 idiom) or by-name drops with version-suffixed re-adds (_0016), plus tail assertions on pg_get_constraintdef and pg_proc.prosrc. Key as-built facts the 0017 design must absorb: clients.status is a bare inline two-value CHECK with NO shared enumeration guard (each consumer inlines status='active', and only some do); fixed_assets is schema-only since 0003 (zero writers, zero immutability); there are NO ar_items/ap_items tables — open items are journal_lines.counterparty_id against account_class in ('payable','receivable') control accounts, so WB-R11's typed SST columns need a new home; document_kind ALREADY contains 'opening_balance_doc' (and 'management_account'/'knowledge_artifact') but no prior-GL kind, and the kind vocabulary is duplicated inside classify_document's body; is_opening_balance=true is hard-refused by the generic draft writer and automatically makes an entry high-stakes (distinct-checker CLR05), and the SST turnover evaluator excludes is_opening_balance entries.

### clients DDL + status CHECK
- location: packages/db/migrations/0003_books_core.sql:34-41
- shape: create table clara.clients (id uuid pk default gen_random_uuid(), firm_id uuid not null references clara.firms(id), name text not null, status text not null default 'active' check (status in ('active','archived')), created_at timestamptz not null default now()); create unique index uq_clients_firm_name on clara.clients (firm_id, lower(name)); -- plus 0007:59 alter table clara.clients add constraint uq_clients_id_firm unique (id, firm_id)
- wave_b_touch: SWAP (WB-R1): the status CHECK is system-named inline — 0017 must drop it by definition (0014 DO-block idiom, run as migration superuser NOT clara_fn_owner) and re-add named e.g. clients_status_check_0017 with ('active','archived','onboarding'). Activation flips exactly at onboarding commit.

### client status guard sites (no shared guard exists)
- location: 0007:1386, 0007:2447, 0011:1180, 0011:1237, 0014:91, 0016:866
- shape: Inline equality only, e.g. 0016:866 `for cl in select c.id from clara.clients c where c.status='active' order by c.id loop` (evaluate_sst_watches_all); 0007:1386 `if not exists (select 1 from clara.clients where id=p_client and firm_id=c.firm and status='active')` (file_document); same shape at 0007:2447 (move filing), 0011:1180/1237 (wake/consent lanes)
- wave_b_touch: NEW SIBLING + AUDIT (WB-R1): no shared client-enumeration guard fn exists — 0017/runtime must introduce one and route every operational consumer (belts, queue, coding lanes, SST watch, autodraft enumerators, classify) through it, negative-tested per consumer. The inline `='active'` sites auto-exclude 'onboarding', but consumers that never re-check client status (sweep enumerates document_filings, queue rows) are the gap to close.

### open_questions table (scope_kind + scope/terminal CHECKs)
- location: packages/db/migrations/0011_daily_loop.sql:796-841
- shape: scope_kind text not null check (scope_kind in ('document','vendor','client')); scope_id uuid not null; constraint ck_open_questions_scope check ((scope_kind='document' and scope_id=document_id and document_id is not null and counterparty_id is null) or (scope_kind='vendor' and scope_id=counterparty_id and counterparty_id is not null and document_id is null) or (scope_kind='client' and scope_id=client_id and document_id is null and counterparty_id is null)); constraint ck_open_questions_terminal check ((status='open' and resolved_by is null and resolved_at is null and resolution_text is null) or (status in ('resolved','dismissed') and resolved_by is not null and resolved_at is not null and nullif(btrim(resolution_text),'') is not null)); status in ('open','resolved','dismissed'); opener_kind in ('human','wake')
- wave_b_touch: scope_kind: likely none (interview must-asks live on the plan object per WB-R1, never open_questions). If an onboarding scope were ever added it is a swap of the inline scope_kind CHECK + ck_open_questions_scope together.

### open_questions_origin_check_0016 (latest origin CHECK)
- location: packages/db/migrations/0016_a21_compliance_watch.sql:202-204 (swap pattern 189-201)
- shape: alter table clara.open_questions add constraint open_questions_origin_check_0016 check (origin in ('clarify_promotion','rule_proposal','rule_conflict','sweep_refusal','manual','classification')); -- preceded by DO-block: find conname via pg_get_constraintdef(con.oid) ilike '%origin%' and ilike '%clarify_promotion%', raise CLR10 if absent, execute format drop constraint %I
- wave_b_touch: SWAP (B3, 0017): drop open_questions_origin_check_0016 by name, add _0017 with the widened set (+'onboarding' if ratified). Tail-assert pg_get_constraintdef like '%onboarding%' per the 0016 tail idiom (0016:5300-5304).

### _open_question_core inline origin whitelist (duplicate of the table CHECK)
- location: packages/db/migrations/0011_daily_loop.sql:1902-1959 (whitelist at 1911)
- shape: create function clara._open_question_core(p_actor uuid,p_firm uuid,p_obo uuid,p_wake_kind text,p_opener_kind text,p_client uuid,p_scope_kind text,p_scope_id uuid,p_question text,p_origin text) returns uuid ... `or p_origin not in ('clarify_promotion','rule_proposal','rule_conflict','sweep_refusal','manual') then raise ... CLR10`. NOTE: 0016 did NOT widen this — classify_document inserts into open_questions directly (0016:3278-3288), bypassing the core
- wave_b_touch: EXTEND-or-BYPASS: an 'onboarding' origin written through the core needs the inline whitelist widened (CREATE OR REPLACE); the 0016 precedent is a direct insert inside the new writer instead. Pick one deliberately; the whitelist/CHECK duplication is a standing trap.

### _open_question_blocks (the CLR26 gate)
- location: packages/db/migrations/0012_consent_optional_and_rule_proposal.sql:88-109
- shape: create or replace function clara._open_question_blocks(p_client uuid,p_filing uuid,p_counterparty uuid) returns table(question_id uuid,scope_kind text) ... where q.client_id=p_client and q.status='open' and q.origin<>'rule_proposal' and (q.scope_kind='client' or (q.scope_kind='document' and q.document_id=v_document) or (q.scope_kind='vendor' and ...canonical...)); ordered document<vendor<client
- wave_b_touch: NONE-BUT-CONSTRAINS (WB-R1): interview must-asks must NEVER enter this gate — they live on the plan-as-document object and block only the commit. If an 'onboarding' origin lands in open_questions, decide explicitly whether it joins the rule_proposal exclusion; silence means it BLOCKS coding on that client.

### coa_accounts special_acc_type CHECK + expense-typing sibling (latest)
- location: packages/db/migrations/0016_a21_compliance_watch.sql:121-125 (base DDL 0003:47-57)
- shape: alter table clara.coa_accounts drop constraint coa_accounts_special_acc_type_check; alter table clara.coa_accounts add constraint coa_accounts_special_acc_type_check check (special_acc_type is null or special_acc_type in ('rounding','sst_output','sst_purchase_cost')); alter table clara.coa_accounts add constraint ck_coa_sst_purchase_cost_expense check (special_acc_type is distinct from 'sst_purchase_cost' or account_type='expense'); -- also account_code check (account_code ~ '^[0-9]{4,8}$'), account_type in ('asset','liability','equity','income','expense'), ck_coa_account_class (0015:199-200): account_class is null or account_class in ('payable','receivable')
- wave_b_touch: SWAP (P8): drop/re-add coa_accounts_special_acc_type_check adding the OBE marker (e.g. 'opening_balance_equity') + the retained-earnings marker decision; add a typing sibling CHECK à la ck_coa_sst_purchase_cost_expense pinning OBE to account_type='equity'.

### uq_coa_special
- location: packages/db/migrations/0003_books_core.sql:58-59
- shape: create unique index uq_coa_special on clara.coa_accounts (client_id, special_acc_type) where (special_acc_type is not null);
- wave_b_touch: NONE: per-(client, VALUE) uniqueness already admits exactly one OBE marker per client (same reasoning 0016 recorded for sst_purchase_cost at 0016:119-120). No index change.

### coding_rules DDL + latest constraint set
- location: 0011:753-794 (base), 0015:289-306 (rule_type/tier), 0016:69-113 (evidence_class/status/terminal)
- shape: coding_rules_rule_type_check: rule_type in ('vendor_account','autopost') [0015:289-290]. ck_coding_rules_tier: (rule_type='vendor_account' and amount_cap_cents is null and frequency_window is null and window_max_posts is null and expires_at is null and direction is null) or (rule_type='autopost' and amount_cap_cents is not null and amount_cap_cents>0 and frequency_window is not null and window_max_posts is not null and window_max_posts>0 and expires_at is not null and direction in ('purchase','sales')) [0015:301-306]. coding_rules_status_check_0016: status in ('proposed','live','declined','retired','suspended_pending_resignature') [0016:97-98]. ck_coding_rules_terminal (0016:102-113): five branches — proposed (no signed/retired/declined fields), live (signed_by+signed_at, not retired/declined), suspended_pending_resignature (signed fields present, not retired/declined), declined (declined_by/at + nonblank decline_reason, not retired), retired (retired_at + nonblank retire_reason). Also: origin text not null check (origin in ('proposed','authored')) [0011:763]; content_hash ~'^[0-9a-f]{64}$'; uq_coding_rules_one_live_vendor on (client_id,counterparty_id,rule_type) where status='live' [0011:791-792]; ck_coding_rules_evidence_class [0016:69-81]
- wave_b_touch: REFERENCE, mostly none (WB-R2): the tick ceremony mints ordinary vendor_account rules through the existing propose/sign lane (sign_coding_rule maps unique_violation on uq_coding_rules_one_live_vendor to CLR27 duplicate_live, 0016:3118-3127). The seeding-proposal batch is a NEW object — the C-11 candidate tier stays dead; per P10 no '@1.000' metric rides the rule, so no coding_rules column changes expected; if a 'seeded' origin value is wanted it is an inline-CHECK swap (system-named, 0011:763).

### t_coding_rules_insert_0016 / _tf_coding_rule_insert_0016 (vendor-binding BEFORE INSERT trigger)
- location: packages/db/migrations/0016_a21_compliance_watch.sql:3139-3159
- shape: create function clara._tf_coding_rule_insert_0016() returns trigger ... if new.rule_type='vendor_account' then if not exists(select 1 from clara.counterparties cpv where cpv.id=new.counterparty_id and cpv.kind='vendor' and cpv.merged_into is null and cpv.retired_at is null) then raise 'a vendor_account rule requires a live canonical vendor counterparty' CLR27 {"reason":"vendor_required"}; if exists(select 1 from clara.coa_accounts a where a.client_id=new.client_id and a.account_code=new.account_code and coalesce(a.account_class,'') in ('payable','receivable')) then raise 'a vendor_account rule cannot target a control-class account' CLR27 {"reason":"control_account"}; create trigger t_coding_rules_insert_0016 before insert on clara.coding_rules for each row
- wave_b_touch: NONE-BUT-CONSTRAINS (WB-R2): every seeded rule insert passes this floor — mass counterparty birthing MUST precede rule minting (live canonical vendor, not merged/retired), and prior-GL mappings targeting control-class accounts must be refused/re-mapped at proposal time, not at tick.

### documents_document_kind_check (latest, 18 kinds)
- location: packages/db/migrations/0014_consent_evidence_documents.sql:64-69 (swap idiom 49-62)
- shape: alter table clara.documents add constraint documents_document_kind_check check (document_kind is null or document_kind in ('invoice','receipt','credit_note','debit_note','bank_statement','payment_voucher','claim_form','payroll_summary','tax_correspondence','ssm_company_doc','agreement_contract','e_invoice_xml','management_account','opening_balance_doc','knowledge_artifact','handwritten_note','consent_evidence','other'));
- wave_b_touch: 'opening_balance_doc' ALREADY EXISTS (WB-R15 tie target needs no widening); 'management_account' and 'knowledge_artifact' also exist. A prior-GL kind is ABSENT — 0017 must either add one (swap by name; run as migration role) or ride 'management_account'/'other'. If added, the vocabulary is DUPLICATED in classify_document's body (0016:3202-3206) — that fn must be re-created too, with a prosrc tail assert.

### classify_document kind whitelist + facts-gate routing
- location: 0016:3175-3208 (whitelist 3202-3206); _enqueue_invoice_facts_core gate 0016:3388-3430
- shape: classify_document(p_document uuid, p_kind text, p_confidence numeric, p_engine_id text, p_op_key text) returns jsonb — engine must like 'clara-classify-%', 'clara-classify-human:v1' reserved for set_document_kind; p_kind validated against the same 18-value list verbatim; consent_evidence protected both directions (CLR28); <0.8 leaves kind untouched + opens origin='classification' question per active filing. Facts gate: NULL kind → hold; kind in ('invoice','credit_note','debit_note') → invoice_facts; else terminal failed task error_code='skipped_kind' (workflow NULL)
- wave_b_touch: EXTEND if a prior-GL kind is added (new fn body + freeze-style prosrc assertion); otherwise none — prior-GL docs classified as 'management_account'/'other' terminal-skip invoice_facts by design, and B5's parse arrives as its own lane per the 0015/0016 lane<->engine CHECK pattern (ck_processing_task_lane_0016 / ck_processing_task_lane_engine_0016, 0016:132-154).

### fixed_assets FULL DDL (untouched since 0003 — schema only)
- location: packages/db/migrations/0003_books_core.sql:155-182 (trigger 456-457; RLS 504-517; grants 522-525)
- shape: create table clara.fixed_assets (id uuid pk default gen_random_uuid(), firm_id uuid not null, client_id uuid not null references clara.clients(id), description text not null, acquired_date date, cost_cents bigint check (cost_cents > 0), residual_cents bigint default 0 check (residual_cents >= 0), useful_life_months int check (useful_life_months > 0), depreciation_method text not null default 'straight_line' check (depreciation_method in ('straight_line')), asset_account_code text, accum_depr_account_code text, depr_expense_account_code text, acquisition_entry_id uuid references clara.journal_entries(id), disposed_at date, accumulated_depreciation_cents bigint not null default 0 check (accumulated_depreciation_cents >= 0), depreciation_start_date date, baseline_as_of date, status text not null default 'active' check (status in ('active','disposed')), created_at timestamptz not null default now(), constraint ck_fa_residual check (residual_cents is null or cost_cents is null or residual_cents <= cost_cents), constraint fk_fa_asset_acc foreign key (client_id, asset_account_code) references clara.coa_accounts (client_id, account_code), constraint fk_fa_accum_acc foreign key (client_id, accum_depr_account_code) references clara.coa_accounts (client_id, account_code), constraint fk_fa_depr_acc foreign key (client_id, depr_expense_account_code) references clara.coa_accounts (client_id, account_code)); only trigger: t_fa_stamp before insert (firm stamp from client); standard 3-policy RLS + select-only grants; ZERO audited writers, ZERO immutability/update triggers, no updated_at
- wave_b_touch: EXTEND heavily (B4/P8): audited writer family (create/baseline/dispose), immutability or column-delta discipline, per-asset cost/accum/NBV tie assertion pinned to acquisition_entry_id linkage, the non-straight-line decision at carry-down (widen depreciation_method CHECK vs refuse per asset — inline CHECK swap), baseline fields (baseline_as_of, accumulated_depreciation_cents, depreciation_start_date) already exist as targets. Nullable cost_cents/acquired_date mean books-grade NOT NULL discipline must arrive as CHECKs on the writer path or new constraints with pre-asserts.

### journal_entries OB surface: is_opening_balance + basis/pair CHECKs + status + coding_kind (latest)
- location: 0003:101-132 (base), 0007:1012-1017 (status), 0015:217-224 (coding_kind + checked_via_rule_id), 0016:50-51 (closing_transfer)
- shape: is_opening_balance boolean not null default false [0003:112]; constraint ck_je_doc_pair check ((document_id is null) = (source_doc_sha256 is null)); constraint ck_je_basis check (document_id is not null or (memo is not null and btrim(memo) <> '')); ck_journal_entries_status: status in ('draft','approved','withdrawn') [0007:1013-1014]; ck_je_document_filing_pair: (document_id is null)=(filing_id is null) [0007:857-859]; ck_je_coding_kind (latest 0015:218-220): coding_kind is null or coding_kind in ('supplier_bill','sales_invoice','sales_credit_note'); ck_je_match_fingerprint_shape (latest 0011:642-649): decision in ('registration_match','name_match_unregistered','birth','alias_match'); uq_je_one_approved_reversal on (reversal_of) where status='approved' and reversal_of is not null [0003:131-132]; NOTE there is NO entry_kind column — the discriminators are origin in ('manual','document','agent','reversal') + coding_kind + the boolean flags. The generic draft writer hard-codes is_opening_balance=false: 'is_opening_balance never trusted from a generic draft (v2 §E)' [0004:186-189]
- wave_b_touch: NEW SIBLING (B4): the OB writer family is a new audited human-lane writer that is the ONLY setter of is_opening_balance=true (per-item, document-bound to the opening_balance_doc sha or the WB-R15 keyed-in provenance extension), plus the supersede verb posting at the governed opening date and re-asserting the tie atomically. Approved-entry immutability (t_je_immutable, 0003:468-469) + uq_je_one_approved_reversal mean supersede must be a reversal-pair-shaped lane, never an UPDATE. A coding_kind or flags marker for OB items is a CHECK swap decision.

### is_high_stakes (latest, 0009 replace)
- location: packages/db/migrations/0009_coding_floor.sql:1513-1521
- shape: create or replace function clara.is_high_stakes(p_entry uuid) returns boolean language sql stable security definer set search_path = clara, pg_temp as $$ select je.is_opening_balance or je.is_year_end or je.tax_affecting or (je.flags ? 'amount_override') or coalesce((select sum(debit_cents) from clara.journal_lines where entry_id = je.id), 0) >= f.high_stakes_amount_cents from clara.journal_entries je join clara.firms f on f.id = je.firm_id where je.id = p_entry; $$
- wave_b_touch: NONE-BUT-CONSTRAINS (WB-R4): every OB entry is automatically high-stakes ⇒ the distinct-checker law (CLR05) binds per entry. The atomic set approval must record per-entry approval rows satisfying maker≠checker (self_approval_attestation is the solo-firm variant, 0003:118); the one-checker-signs-once ceremony wraps, it does not bypass.

### SST turnover evaluator OB exclusion
- location: packages/db/migrations/0016_a21_compliance_watch.sql:550-552,600
- shape: baseline branch: `where e.client_id=p_client and e.status='approved' and e.is_opening_balance`; rolling window: `... and e.status='approved' and not e.is_opening_balance` (plus the is_year_end AND closing_transfer exclusion per 0016:216-224)
- wave_b_touch: NONE-BUT-CONSTRAINS: carry-down postings MUST carry is_opening_balance=true or they distort the live RPR watch (currently OVERDUE, RM 1,310,276.40 crossing June-2025). This is the structural reason the OB writer family, not draft_entry, posts carry-down.

### AR/AP open-item representation (NO ar_items/ap_items tables exist)
- location: 0009:881 (journal_lines.counterparty_id), 0015:198-200 (ck_coa_account_class), 0016:1328-1338 (control-leg counterparty stamping)
- shape: alter table clara.journal_lines add column counterparty_id uuid; [0009:881] — open items are approved journal_lines against coa_accounts.account_class in ('payable','receivable') control accounts with the counterparty stamped on the control leg at approve (0016:1328-1338). ck_coa_account_class: account_class is null or account_class in ('payable','receivable'). No subledger/open-item/settlement table exists anywhere in 0001-0016
- wave_b_touch: NEW SIBLING (WB-R11/WB-R12): the carried-open-item SST facts (portion, rate, basis) have NO existing landing columns — 0017 must create the typed home (either open-item carry tables keyed to the per-item OB entries, or typed columns on the OB rows). journal_lines are immutable after approval (t_jl_immutable 0003:470-471), so the facts must be written at insert. WB-R12 uncleared bank items ride the same per-item OB entry granularity for Wave C matching.

### counterparties DDL (latest kind set) + canonical-liveness columns
- location: 0009:812-845 (base), 0015:160 (kind widened), 0011:607-616 (merged_into/retired_at)
- shape: kind check latest: constraint counterparties_kind_check check (kind in ('vendor','customer')) [0015:160]. Normalization: ck_counterparties_name_normalized: name_normalized = lower(regexp_replace(name, '[^a-zA-Z0-9]', '', 'g')); ck_counterparties_registration_normalized (null-pair or normalized+nonblank). Uniques: uq_counterparties_client_registration on (client_id, registration_normalized) where registration_normalized is not null; uq_counterparties_client_unregistered_name on (client_id, name_normalized) where registration_normalized is null. 0011 adds merged_into uuid + retired_at timestamptz + fk_counterparties_merged_into (self, per firm/client) + CHECK (both null, or both set and merged_into<>id); immutability trigger 0011:945-949 allows only merged_into/retired_at/updated_at once merged
- wave_b_touch: REFERENCE (B5 mass birthing): seeding births counterparties through the existing writers honoring the two partial uniques and normalization CHECKs; alias collisions surface via uq_counterparty_aliases_live_name. Kind must be 'vendor' for rules to pass the 0016 insert trigger.

### counterparty_aliases DDL
- location: packages/db/migrations/0011_daily_loop.sql:651-672
- shape: create table clara.counterparty_aliases (id uuid pk, firm_id uuid not null, client_id uuid not null, counterparty_id uuid not null, alias_normalized text not null check (btrim(alias_normalized)<>''), alias_display text not null check (btrim(alias_display)<>''), origin text not null check (origin in ('former_name','trade_name','human')), created_by uuid not null references clara.users(id), created_at timestamptz not null default now(), retired_at timestamptz, unique(id,firm_id,client_id), fk (counterparty_id,firm_id,client_id) → counterparties(id,firm_id,client_id), constraint ck_counterparty_aliases_normalized check (alias_normalized=lower(regexp_replace(alias_display,'[^a-zA-Z0-9]','','g')))); create unique index uq_counterparty_aliases_live_name on (client_id,alias_normalized) where retired_at is null
- wave_b_touch: EXTEND-maybe: seeding-derived aliases either ride origin='human' (the admin ticks) or the inline origin CHECK gains a 'seeded' value (system-named inline — drop-by-definition swap). One live alias per (client, normalized name) is the dedupe floor for prior-GL name variants.

### create_firm (idempotency = admission token, NOT op_receipts)
- location: packages/db/migrations/0004_governed_fns.sql:315-341
- shape: create function clara.create_firm(p_name text, p_admission_token uuid, p_op_key text) returns jsonb — header comment: 'create_firm accepts p_op_key for signature stability; its idempotency is the single-use admission token (a retry finds the token consumed -> CLR04), so it does not use op_receipts (documented — like mint/revoke)'. Body: consumes firm_admissions token (update ... set consumed_at=now() where token=... and consumed_at is null; not found → CLR04 'invalid or consumed admission token'); refuses agent identity (CLR04) and existing active membership (CLR10)
- wave_b_touch: EXTEND/WRAP (WB-R1): the durable firm-interview needs a receipt-idempotent wrap — today a same-op retry after commit RAISES CLR04 on the consumed token instead of replaying a receipt. Either a wrapping writer that adopts _reserve_op/_finish_op around the token consumption, or a documented replay contract on the interview run's checkpoint.

### upsert_account (latest signature — the CoA seed writer)
- location: packages/db/migrations/0009_coding_floor.sql:1460-1492
- shape: create function clara.upsert_account(p_client uuid, p_code text, p_name text, p_type text, p_special_acc_type text default null, p_op_key text default null, p_account_class text default null) returns jsonb — bookkeeper+, op_key mandatory (raises CLR10 on null despite the default), _reserve_op-hashed over (client,code,name,type,special,account_class), on conflict updates name/type/special/class + is_active=true
- wave_b_touch: REFERENCE (B3): the LHDN-verified CoA seed + F12-11 marker fixes ride this writer (it already carries special_acc_type and account_class params, so the OBE marker lands through it once the CHECK admits the value); a bulk-seed sibling is optional, not required by shape.

### THE constraint-swap + assertion pattern (0014/0016 idiom — 0017 must reuse)
- location: 0014:46-69 (drop-by-definition), 0016:121-123 & 202-204 (by-name/version-suffix), 0016:135-148 (pre-assert data), 0016:5163-5214 (prosrc asserts), 0016:5262-5304 (constraintdef asserts)
- shape: (a) named constraint: `alter table X drop constraint <name>; alter table X add constraint <name or name_0016> check (...)`. (b) system/inline-named: DO-block `select con.conname into v_con from pg_constraint con join pg_class c ... join pg_namespace n ... where n.nspname='clara' and c.relname='<t>' and con.contype='c' and pg_get_constraintdef(con.oid) ilike '%<token>%'; if v_con is null then raise exception '...not found' using errcode='CLR10'; execute format('alter table clara.<t> drop constraint %I', v_con);` then add explicitly-named successor. (c) pre-assert existing rows conform BEFORE the tighter CHECK lands (0016:135-148). (d) tail asserts: `select pg_get_constraintdef(con.oid) into v_def ... where con.conname='...'; if v_def is null or v_def not like '%value%' then raise ... CLR10` and for replaced fns `select p.prosrc into v_src from pg_proc p where p.oid='clara.fn(sig)'::regprocedure; if position('marker literal' in v_src)=0 then raise ... CLR10`. (e) table alters run as the migration (superuser) role, NOT under `set role clara_fn_owner` (0014:46-48); new tables + writers ARE created under clara_fn_owner (0011/0015/0016 idiom, zero direct app-role grants, tail-asserted)
- wave_b_touch: 0017 applies (a) to open_questions_origin_check_0016→_0017, coa_accounts_special_acc_type_check, documents_document_kind_check; (b) to clients.status and any inline CHECK (coding_rules.origin, counterparty_aliases.origin, fixed_assets.depreciation_method); (c) before every tightening; (d) for every swap + every re-created fn (classify_document if the vocabulary widens); (e) for role sequencing of the wiki/registry/batch tables.

### hazards
- No shared client-enumeration guard exists as-built — status='active' is inlined at only SOME entry points (0007:1386, 0007:2447, 0011:1180, 0011:1237, 0016:866); consumers that enumerate document_filings/queue/coding rows never re-check client status, so WB-R1's structural exclusion needs a new shared guard plus a per-consumer audit + negative test, not just the CHECK widening.
- Origin-vocabulary duplication: open_questions has BOTH a table CHECK (open_questions_origin_check_0016) and an inline whitelist inside _open_question_core (0011:1911) — 0016 widened only the CHECK and bypassed the core with a direct insert; widening for 'onboarding' must touch both or deliberately repeat the bypass, and must decide whether the new origin joins _open_question_blocks' rule_proposal exclusion (silence = it BLOCKS coding CLR26).
- is_opening_balance=true auto-elevates to high-stakes (is_high_stakes, 0009:1515) ⇒ CLR05 distinct-checker binds per OB entry; WB-R4's one-checker atomic set approval must still record per-entry maker≠checker approvals (solo-firm via self_approval_attestation), and the generic draft writer hard-codes is_opening_balance=false — the OB writer family is the only lawful setter.
- Approved entries are immutable (t_je_immutable) and reversal is capped by uq_je_one_approved_reversal (partial unique on reversal_of where approved) — the opening supersede verb must be a reversal-pair-shaped lane posting at the governed opening date; an UPDATE-based correction is structurally impossible (0016's A8 note documents the same for closing_transfer marking).
- The SST turnover evaluator excludes is_opening_balance approved entries (0016:550-552) — carry-down amounts posted WITHOUT the flag would corrupt the live RPR overdue watch (RM 1,310,276.40 crossing June-2025).
- NO ar_items/ap_items tables exist: open items are counterparty-stamped control-account journal_lines (account_class payable/receivable); journal_lines are immutable post-approval, so WB-R11's typed SST columns need a NEW home written at insert time — this is a 0017 design decision, not an ALTER of an existing table.
- Seeding order-of-operations: t_coding_rules_insert_0016 refuses any vendor_account rule whose counterparty is not a live canonical VENDOR (merged_into null, retired_at null) or whose account is control-class — mass counterparty birthing must complete (and merges resolve) BEFORE tick-minting; uq_coding_rules_one_live_vendor makes a second live rule per (client, counterparty, rule_type) raise CLR27 duplicate_live at sign.
- Constraint-swap role discipline: table ALTERs must run as the migration superuser (0014:46-48 — clara_fn_owner does not own the tables), while new tables/writers are created under `set role clara_fn_owner`; clients.status, coding_rules.origin, counterparty_aliases.origin and fixed_assets.depreciation_method CHECKs are system-named inline — drop by pg_get_constraintdef definition, never by guessed name.
- document_kind 'opening_balance_doc' already exists but nothing consumes it (the facts gate terminal-skips it as skipped_kind — correct); a prior-GL kind is ABSENT and the 18-kind vocabulary is duplicated verbatim inside classify_document (0016:3202-3206) — adding a kind means swapping the table CHECK AND re-creating the fn with a prosrc tail assertion; 'clara-classify-human:v1' stays reserved and consent_evidence stays protected (CLR28).
- fixed_assets has zero writers, zero immutability and nullable cost/dates — books-grade discipline is greenfield; the three (client_id, account_code) FKs into coa_accounts and ck_fa_residual are the only floors; the acquisition_entry_id→journal_entries FK exists but nothing enforces the P8 cost/accum/NBV tie yet — that assertion is new 0017 surface.
- Replay/receipt discipline: every writer follows _reserve_op(p_op_key, payload-hash)/_finish_op — create_firm is the documented exception (token-consumption idempotency, retry RAISES CLR04), so the WB-R1 durable firm interview cannot naively retry it; WB-R4's byte-identical receipt replay + second-semantic-seed-RAISES maps onto the existing op_receipts hash-mismatch behavior plus a serializable lock on the new seed-registry row.
- Advisory-lock namespace: _open_question_core already claims pg_advisory_xact_lock keys 203005003/203005004 (vendor/client scope) — new Wave B lock keys must not collide, and the open-question document path locks the active filing FOR UPDATE to serialize against approve_entry's FOR SHARE (any onboarding-lane writer touching filings inherits that ordering).

## x:fns+writers

The as-built fn plane is a strict CoR discipline over 16 migrations: every governed writer rides _reserve_op/_finish_op receipts, dual-lane prologues (_human_ctx vs wake_context+assert_wake_allowed), SECURITY DEFINER with `set search_path=clara,pg_temp`, revoke-all-from-public plus an explicit per-role grant matrix, and a migration-tail prosrc/ACL self-check block. The latest bodies Wave B touches live in 0016 (get_context_pack v3, list_review_queue, _draft_entry_core, _approve_entry_core), 0009 (draft_entry/wake_draft_entry wrappers, upsert_account 7-arg, account_code grammar), 0005 (create_firm/create_client/add_member event-emitting CoRs), 0004 (trial_balance, _human_ctx, receipts core), 0011 (wake_context, allowlist rows, the ACL tuple matrix), 0012 (_open_question_blocks). Two structural asymmetries matter most for 0017: _draft_entry_core blacklists only status='archived' (an 'onboarding' status would be postable today), and _approve_entry_core's sighting mint fires on any counterparty-stamped debit leg (carried AR/AP opening entries would seed the sighting pool, violating WB-R2).

### get_context_pack (v3, pack_schema_version 3)
- location: packages/db/migrations/0016_a21_compliance_watch.sql:4262-4351
- shape: create or replace function clara.get_context_pack(p_client uuid,p_purpose text) returns jsonb language plpgsql stable security definer set search_path=clara,pg_temp. Key build order (exact): 'pack_schema_version',3 · 'purpose' · 'generated_at',now() · 'books_version' (max(domain_events.seq) per firm, coalesce 0) · 'client' {id,name,status} · 'firm' {id,name,high_stakes_amount_cents} · 'coa' [] ({account_code,name,account_type,special_acc_type,is_active} order by account_code) · 'trial_balance' [] (to_jsonb over clara.trial_balance(cl.id)) · 'recent_entries' [] ({entry,lines[]} — status<>'withdrawn', order posting_date desc, limit 50) · 'documents' [] (document_filings join documents, df.retired_at is null, order filed_at desc) · 'resolutions' [] (superseded_at is null) · 'approval_history' [] (approved_at not null, limit 25) · 'sst_registration_watch' [] (per open compliance_watch; ends with 'basis','db_computed_screening_estimate' and 'permitted_use','surface_and_request_professional_review_only'). Final FROM: clara.clients cl where cl.id=p_client and cl.firm_id=v_firm (NO status filter).
- wave_b_touch: v4 CoR: bump 'pack_schema_version',4; append the wiki block PURPOSE-GATED (renders only for the new v7 purpose literal — P5 additive-but-dark for every pre-v7 consumer) carrying the wiki last-projected marker vs books_version (WB-R3) under the ≤6-page/≤12KB budgeted-CTE (WB-R8); keep every v3 key byte-identical; ship a matching 0017 tail prosrc assert.

### get_context_pack dual-lane prologue (ADR-015 idiom)
- location: packages/db/migrations/0016_a21_compliance_watch.sql:4266-4288
- shape: if coalesce(current_setting('clara.wake_secret',true),'')<>'' then select * into w from clara.wake_context(); if w.credential_id is null then raise exception 'no valid agent read context' using errcode='CLR03'; end if; if w.wake_kind not in ('interactive','proactive') then perform clara.assert_wake_allowed(w.wake_kind,'get_context_pack'); end if; if w.client_id is not null and p_client<>w.client_id then return null; end if; v_firm:=w.firm_id; else c:=clara._human_ctx(clara.role_rank('viewer')); v_firm:=c.firm; end if;
- wave_b_touch: Copy verbatim into the v4 CoR and into any new agent-readable read surface (dated TB, plan reads); the wake-secret PRESENCE is the lane marker — never invent a new lane discriminator.

### 0016 tail prosrc self-check pattern (7g)
- location: packages/db/migrations/0016_a21_compliance_watch.sql:5199-5215
- shape: select p.prosrc into v_src from pg_proc p where p.oid='clara.get_context_pack(uuid,text)'::regprocedure; if position('''pack_schema_version'',3' in v_src)=0 or position('sst_registration_watch' in v_src)=0 or position('surface_and_request_professional_review_only' in v_src)=0 then raise exception '0016 get_context_pack missing the v3 watch block' using errcode='CLR10'; end if;  — same idiom for list_review_queue: position('compliance_watch')/('stale_evaluator')/('coding_kind'). Plus the granted-fn-writes-forbidden-table scan idiom at 5218-5230 (aclexplode over proacl + prosrc ilike '%insert into clara.sst_threshold_schedule%').
- wave_b_touch: 0017 replicates both idioms: assert '''pack_schema_version'',4' + the v7 purpose literal + the wiki block key; and the WB-R6 dependency audit = the aclexplode+prosrc-scan proving NO gate/bound/floor/autopost fn references wiki tables.

### list_review_queue
- location: packages/db/migrations/0016_a21_compliance_watch.sql:4558-4729
- shape: create or replace function clara.list_review_queue(p_scope jsonb,p_cursor jsonb,p_limit int default 50) returns jsonb ... security definer. _human_ctx(role_rank('viewer')). Scope: only key 'client_id' admitted, must exist in c.firm else CLR10. Limit clamp: p_limit:=least(greatest(coalesce(p_limit,50),1),500). Cursor: {'tuple':[5]} validated as int/uuid/(text)/timestamptz/uuid else CLR10. Five union CTEs (draft_rows/filing_rows/question_rows/task_rows/compliance_rows), each emitting the SAME 22-column row ending (...,vendor_group, coding_kind, watch_id, tier); compliance_rows: section_rank 1 when state in ('crossed','overdue') else 2, row_kind 'compliance_watch', tier=cw.state. sort_tuple = array[section_rank::text, client_id::text, vendor_group, created_at::text, id::text]; page = sort_tuple>v_cursor order by sort_tuple limit p_limit. Envelope: watermark (max domain_events.seq::text) · counts {ready,needs_review,needs_you,open_drafts,open_questions,open_tasks,compliance_watches} (integers only) · sweep {open_run,last_finalized_at,last_ack_at} · compliance {stale_evaluator (newest compliance_eval_runs receipt >48h old or absent ⇒ true), clients[]} · rows[] · next_cursor {tuple}. No clients.status predicate anywhere.
- wave_b_touch: CoR: add a lint_rows sibling CTE emitting row_kind='lint_finding' (P18 — identity/dedupe key, hydration card; the unknown-kind placeholder is banned), an integer counts key (e.g. lint_findings — never monetary), and the WB-R1 exclusion join (clients.status='active') across every CTE. CoR discipline: additive keys only; sort tuple + cursor grammar + all 0011/0016 row shapes unchanged; update the 0017 tail assert.

### _draft_entry_core (19-param dual-lane core)
- location: packages/db/migrations/0016_a21_compliance_watch.sql:3970-4250 (insert at 4079-4090; client gate at 3995-4002; lineage 0004:127, 0004:186-189)
- shape: create or replace function clara._draft_entry_core(p_actor uuid, p_firm uuid, p_obo uuid, p_wake_kind text, p_is_human boolean, p_client uuid, p_resolution uuid, p_posting_date date, p_memo text, p_lines jsonb, p_document uuid, p_sha256 text, p_flags jsonb, p_op_key text, p_books_version bigint, p_proposed_counterparty jsonb, p_evidence jsonb, p_coding jsonb, p_coding_kind text) returns jsonb — ungranted, revoke-all. Client gate (EXACT): select firm_id,status into v_client_firm,v_client_status from clara.clients where id=p_client; CLR11 if wrong firm; `if v_client_status='archived' then raise exception 'client is archived -- no new postings' using errcode='CLR10';` — a BLACKLIST, nothing else checked. Insert hardcodes is_opening_balance false: values(...,p_resolution,false, coalesce((p_flags->>'is_year_end')::boolean,false), ...) — 0004:189 comment: 'is_opening_balance never trusted from a generic draft (v2 §E)'. Also: assert_client_resolved(p_client,p_resolution,p_document); coding_kind whitelist ('supplier_bill','sales_invoice','sales_credit_note'); closing_transfer human-only (CLR03 for wake lane); receipt via _finish_op(p_firm,'draft_entry',p_op_key,...).
- wave_b_touch: SIBLING, not widening: the OB writer family (B4) is a new human-lane audited core that sets is_opening_balance=true, posts at the governed opening date, per-item, OBE-plug, WB-R11 SST typed columns, WB-R12 per-item bank entries — the generic core keeps its hardcoded false forever. ALSO must-touch: the archived-only blacklist means status='onboarding' would be POSTABLE — the shared client-enumeration guard (WB-R1) must flip this to an allowlist (status='active') here and be negative-tested per consumer.

### draft_entry (granted human wrapper) + wake_draft_entry (agent wrapper)
- location: packages/db/migrations/0009_coding_floor.sql:1414-1458
- shape: create function clara.draft_entry(p_client uuid, p_resolution uuid, p_posting_date date, p_memo text, p_lines jsonb, p_document uuid default null, p_sha256 text default null, p_flags jsonb default '{}', p_op_key text default null, p_proposed_counterparty jsonb default null, p_evidence jsonb default null) returns jsonb — body: c:=clara._human_ctx(clara.role_rank('bookkeeper')); return clara._draft_entry_core(c.actor,c.firm,null,null,true,...); granted to clara_authenticated. wake_draft_entry adds p_books_version bigint (mandatory, CLR10 if null) + p_coding/p_coding_kind; prologue: wake_context() → CLR03 → assert_wake_allowed(w.wake_kind,'wake_draft_entry') → _draft_entry_core(clara.agent_user_id(),w.firm_id,w.on_behalf_of,w.wake_kind,false,...); granted to clara_wake_interactive only.
- wave_b_touch: The OB writer family siblings the HUMAN wrapper shape exactly (own fn name → own audit fn label → own op_receipts fn key); NO wake sibling ever (WB-R1/B4 human-lane) — zero wake_fn_allowlist rows, zero agent-role grants for any OB/seeding/carry-down verb.

### trial_balance
- location: packages/db/migrations/0004_governed_fns.sql:730-741 (never CoR'd since)
- shape: create function clara.trial_balance(p_client uuid) returns table(account_code text, name text, debit_cents bigint, credit_cents bigint) language sql stable security invoker set search_path = clara, pg_temp as $$ select a.account_code, a.name, coalesce(sum(jl.debit_cents) filter (where je.status = 'approved'), 0)::bigint, coalesce(sum(jl.credit_cents) filter (where je.status = 'approved'), 0)::bigint from clara.coa_accounts a left join clara.journal_lines jl on jl.client_id = a.client_id and jl.account_code = a.account_code left join clara.journal_entries je on je.id = jl.entry_id where a.client_id = p_client group by a.account_code, a.name order by a.account_code; $$ — SECURITY INVOKER (RLS-scoped), no date bound, ungranted-to-public.
- wave_b_touch: New dated SIBLING (e.g. trial_balance_as_of(p_client uuid, p_as_of date)) for WB-R15 tie-out + the WB-R5 daily opening-TB tie watch; copy the invoker + filter shape and add `je.posting_date <= p_as_of`; do NOT overload the 1-arg name (pack v3 calls it and the 0016 tail asserts the pack body).

### create_firm + firm_admissions (admission-token mechanics)
- location: packages/db/migrations/0005_event_spine.sql:596-620 (latest body); table 0002_foundation.sql:255-260; grant 0004:768
- shape: create or replace function clara.create_firm(p_name text, p_admission_token uuid, p_op_key text) returns jsonb — NO op_receipts by design (0004:315-317 comment: 'its idempotency is the single-use admission token (a retry finds the token consumed -> CLR04)'). Body: jwt_sub actor; agent identity cannot own a firm (CLR04); `update clara.firm_admissions set consumed_at = now() where token = p_admission_token and consumed_at is null; if not found then raise exception 'invalid or consumed admission token' using errcode = 'CLR04';`; single-firm rule; insert firms + owner membership; _append_event('firm.created'); returns {firm_id} directly (no _finish_op). Table: firm_admissions(token uuid primary key, note text, consumed_at timestamptz, created_at).
- wave_b_touch: WB-R1 receipt-idempotent wrap for the durable firm interview (Gate F): a lost-response retry after success currently CLR04s (token consumed) instead of replaying — the wrap must record a replayable receipt (e.g. op_receipts keyed on the token, or a wrapping commit fn) WITHOUT letting a token double-consume; the durable run's commit step needs exactly-once semantics across runtime kills.

### create_client
- location: packages/db/migrations/0005_event_spine.sql:623-639 (latest body)
- shape: create or replace function clara.create_client(p_name text, p_op_key text) returns jsonb — c := clara._human_ctx(clara.role_rank('admin')); op_key mandatory (CLR10); _reserve_op(c.firm,'create_client',p_op_key,clara._hash(jsonb_build_object('n', p_name))); insert into clara.clients(firm_id, name) — status defaults 'active'; unique_violation → 'a client with that name already exists' (uq_clients_firm_name on (firm_id, lower(name))); _append_event('client.created'); _finish_op receipt {client_id}.
- wave_b_touch: B3: CoR or sibling that births the client with status='onboarding' (real id/CoA/identifier FKs for dry-run validation) + a NEW audited activation verb flipping 'onboarding'→'active' exactly at the onboarding commit (event + audit + receipt); interview must-asks live on the plan object, never in open_questions.

### clients.status CHECK
- location: packages/db/migrations/0003_books_core.sql:38 (inline, system-named clients_status_check; never altered since)
- shape: status     text        not null default 'active' check (status in ('active','archived'))
- wave_b_touch: 0017 swap: drop (by the 0016:189-201 find-by-definition idiom, since it is system-named) and re-add NAMED (e.g. clients_status_check_0017) with 'onboarding' added; every operational consumer then needs the shared status='active' guard because most gates are firm-scoped or archived-blacklist, not active-allowlist.

### add_member
- location: packages/db/migrations/0005_event_spine.sql:677-704 (latest body)
- shape: create or replace function clara.add_member(p_firm uuid, p_user uuid, p_role text, p_op_key text) returns jsonb — _human_ctx(role_rank('admin')); p_firm must equal c.firm (CLR11); _reserve_op hash {'u','r'}; `perform 1 from clara.firms where id = c.firm for update; -- serialize per-firm (v2 §F/F18)`; role_rank(p_role) not null; agent identity can NEVER be a member (HIGH 11); one-active-firm-per-user rule; event 'member.added' emitted LAST (C4: the counter carries no FK so firms-lock→counter ordering cannot deadlock); receipt {membership_id}.
- wave_b_touch: None-but-constrains: the firm-interview durable run calls it as-is; any Wave B fn combining the firms FOR UPDATE lock with event emission must keep the same lock-then-counter-last ordering.

### upsert_account (latest, 7-arg) + account_code grammar + special_acc_type CHECK
- location: packages/db/migrations/0009_coding_floor.sql:1460-1505; grammar 0009:758-761; special CHECK 0016:121-125; uq_coa_special 0003:58-59
- shape: create function clara.upsert_account(p_client uuid, p_code text, p_name text, p_type text, p_special_acc_type text default null, p_op_key text default null, p_account_class text default null) returns jsonb — _human_ctx(bookkeeper); op_key mandatory; blocks type/class change when journal_lines exist (CLR10); on conflict (client_id,account_code) do update ... is_active=true; granted clara_authenticated. Constraints: account_code CHECK (0009): `account_code ~ '^[0-9]{4,8}$|^[0-9]{3}-[0-9A-Z]{2,4}$'`. special_acc_type CHECK (0016, named coa_accounts_special_acc_type_check): `special_acc_type is null or special_acc_type in ('rounding','sst_output','sst_purchase_cost')` + companion `ck_coa_sst_purchase_cost_expense check (special_acc_type is distinct from 'sst_purchase_cost' or account_type='expense')`. uq_coa_special: unique (client_id, special_acc_type) where special_acc_type is not null — one-per-client per VALUE for free.
- wave_b_touch: P8: 0017 swaps coa_accounts_special_acc_type_check to add the OBE marker (+ the retained-earnings marker decision) with a type-binding companion CHECK per the sst_purchase_cost pattern; the B3 LHDN-verified CoA seed (incl. F12-11 marker fixes) rides upsert_account and MUST conform to the account_code grammar (dashed 'NNN-XXXX' or 4-8 digits) or swap it again.

### _approve_entry_core side-effect block (sighting mint + auto-proposal)
- location: packages/db/migrations/0016_a21_compliance_watch.sql:1220-1536 (mint 1456-1479; proposal loop 1481-1518; CLR26 gate 1342-1353; human wrapper approve_entry 0015:1536, 4-arg grant approve_entry(uuid,uuid,text,text))
- shape: create or replace function clara._approve_entry_core(p_ctx jsonb, p_entry uuid, p_expected_revision uuid, p_attestation text, p_op_key text) returns jsonb. Gate before status flip: advisory locks pg_advisory_xact_lock(203005003, hashtext(client:counterparty)) + (203005004, hashtext(client)); then `select * into v_question from clara._open_question_blocks(e.client_id,e.filing_id,v_counterparty) limit 1; if found then raise ... errcode='CLR26'`. Mint (H2 carve-out — human-checked only): `if v_counterparty is not null and e.reversal_of is null and v_checked_via_rule is null then insert into clara.rule_sightings(firm_id,client_id,counterparty_id,account_code,entry_id,side) select distinct ... where l.entry_id=p_entry and l.debit_cents>0 and a.is_active` (side='debit', NO account_class filter) + the income-class credit insert (side='credit', a.account_type='income'); on conflict uq_rule_sightings_mapping do nothing. Proposal loop: 3 distinct human-checked debit sightings on a NON-control account (coalesce(am.account_class,'') not in ('payable','receivable')) for a canonical VENDOR ⇒ coding_rules 'proposed' + an open_questions row origin='rule_proposal' + event 'kb_rule.proposed'. High-stakes: CLR05 distinct-checker / attestation ladder at 1425-1443.
- wave_b_touch: The WB-R4 batch set-approval verb must NOT reuse this core unguarded: a carried AR/AP opening item with a counterparty stamped on its control line WOULD mint a debit sighting (the mint has no control-class filter) — violating WB-R2's zero-sightings-from-prior-GL and the empty S/P pool. Either a dedicated OB-approval path (no sighting block, no proposal loop) or an explicit `not e.is_opening_balance` carve-out; the CLR05 high-stakes law (OB ⇒ always high-stakes) must still be satisfied per entry inside the one serializable set-approval.

### op_receipts + _reserve_op/_finish_op (idempotency spine)
- location: packages/db/migrations/0002_foundation.sql:295-303; 0004_governed_fns.sql:46-68
- shape: create table clara.op_receipts (firm_id uuid not null, fn text not null, op_key text not null, request_hash bytea not null, result jsonb, created_at timestamptz not null default now(), primary key (firm_id, fn, op_key)); — _reserve_op: insert ... on conflict (firm_id, fn, op_key) do nothing; if found then return null (proceed); else re-read: `if v_hash is distinct from p_req_hash then raise exception 'op_key reused with different args' using errcode = 'CLR10';` return coalesce(v_result, jsonb_build_object('pending', true)). _finish_op: update result, return it. Writers reserve-before-effect; the receipt row rolls back with a failed txn.
- wave_b_touch: Every new Wave B writer (wiki ingest writer, OB family, activation verb, tick-list signature mint, batch approval, supersede verb) rides this exactly. WB-R4's 'same-op retry replays byte-identically' = this pattern; the 'second SEMANTIC seed RAISES' is NOT op_receipts — it needs the seeded-once registry's own uniqueness. Note the {'pending':true} replay for a concurrently-in-flight op_key — the failure-after-N-items resume gate must define behavior around it.

### _human_ctx / wake_context / assert_wake_allowed (prologue idioms)
- location: 0004_governed_fns.sql:299-309 (_human_ctx); 0011_daily_loop.sql:1133-1153 (wake_context latest); 0004:114-121 (assert_wake_allowed); 0011:3922-3941 (_agent_read_admitted helper, agent_ro-granted)
- shape: _human_ctx(p_min_rank int, out actor uuid, out firm uuid): jwt_sub() null ⇒ CLR04; jwt_firm() null ⇒ CLR04; coalesce(actor_role_rank(),-1) < p_min_rank ⇒ CLR04. wake_context() returns table(credential_id,wake_kind,firm_id,on_behalf_of,client_id): sha256 of clara.wake_secret GUC vs wake_credentials.secret_hash, revoked_at/consumed_at null, expires_at>statement_timestamp(), on_behalf_of must be an active bookkeeper+ member. assert_wake_allowed: `perform 1 from clara.wake_fn_allowlist where wake_kind = p_wake_kind and function_name = p_fn; if not found ⇒ CLR03`. mint_wake_credential (0011:1156-1197, runtime-only): autodraft kind REQUIRES `clients ... status='active'` (0011:1180) — already excludes non-active clients at mint time.
- wave_b_touch: New human verbs open with _human_ctx(role_rank(...)); new agent reads with the wake prologue or _agent_read_admitted; the interview family (durable runs) is human-lane. mint_wake_credential's status='active' pin is one leg of the WB-R1 shared guard already in place — the negative-test matrix should cite it.

### wake_fn_allowlist row pattern
- location: 0002_foundation.sql:247-251 (table: wake_kind text, function_name text, pk both); seeds 0002:553-559 and 0011:3903-3910
- shape: insert into clara.wake_fn_allowlist (wake_kind, function_name) values ('interactive','wake_draft_entry'),('interactive','wake_record_client_resolution'),('interactive','wake_ingest_document'),('interactive','wake_record_notification'),('proactive','wake_record_notification') on conflict do nothing;  -- 0011 adds: ('autodraft','wake_draft_entry'),('autodraft','get_document_extract'),('autodraft','get_context_pack'),('autodraft','get_draft_review'),('autodraft','coding_lane'),('autodraft','wake_open_question'),('interactive','wake_open_question')
- wave_b_touch: 0017 adds rows ONLY if a new wake-callable read arrives; NONE of the OB/seeding/carry-down/interview writers ever get a row (human-only per WB-R1/B4); the lint belt + wiki projection consumers run as clara_runtime, not wake.

### _open_question_blocks + open_questions.origin CHECK
- location: 0012_consent_optional_and_rule_proposal.sql:88-109 (latest fn body); table 0011_daily_loop.sql:796-834; origin CHECK 0016:202-204
- shape: _open_question_blocks(p_client uuid,p_filing uuid,p_counterparty uuid) returns table(question_id uuid,scope_kind text) — blocks when an OPEN question matches: `q.origin<>'rule_proposal'  -- 0012(B): a proposal is advisory, never a gate` AND (scope_kind='client' OR (scope_kind='document' and q.document_id=v_document) OR (scope_kind='vendor' and canonical counterparty matches)); ordered document→vendor→client. Ungranted (ACL matrix row all-false). Current origin CHECK (NAMED): `alter table clara.open_questions add constraint open_questions_origin_check_0016 check (origin in ('clarify_promotion','rule_proposal','rule_conflict','sweep_refusal','manual','classification'));`
- wave_b_touch: B3's origin widening = 0017 drops open_questions_origin_check_0016 BY NAME and re-adds _0017 with the new origin(s) (P18 lint findings may also need one if they mint questions — else they get their own table). WB-R1 HARD RULE: onboarding must-asks must NEVER be open_questions rows — a scope_kind='client' open row would CLR26-block every approval for that client at commit time; they live on the plan-as-document object and gate ONLY the commit verb.

### ACL grant matrix row pattern + the grants discipline
- location: 0011_daily_loop.sql:4229-4287 (tuple matrix); 0016:4742-4757 (revoke-all sweep + explicit grants); 0016:4734-4740 (the agent-gains-zero rule)
- shape: for v_acl in select * from (values ('clara.coding_lane(uuid,uuid)',true,true,false,false), ('clara.list_review_queue(jsonb,jsonb,integer)',true,false,false,false), ... ) x(signature,authenticated,agent,wake,runtime) loop if pg_catalog.has_function_privilege('clara_authenticated',v_acl.signature,'execute') is distinct from v_acl.authenticated or ... 'clara_agent_ro' ... or ... 'clara_wake_interactive' ... or ... 'clara_runtime' ... then raise exception '0011 row-by-row grant assertion failed for %',v_acl.signature; end loop. 0016 discipline: `revoke execute on all functions in schema clara from public;` then explicit grants — human writers → clara_authenticated; evaluator/classify → clara_runtime ONLY; 'THE AGENT ROLE GAINS ZERO EXECUTE ANYWHERE IN THIS MIGRATION (tail-asserted like 0015)'.
- wave_b_touch: 0017 extends the tuple matrix with every new fn (wiki writers/query, OB family, interview verbs, lint fns, seeding ceremony, activation, supersede, dated TB) and keeps: agent-role-zero-new-execute tail assert, runtime-only for the belt/projection consumers, authenticated-only for human verbs, all-false for internal cores; cross-firm SECURITY DEFINER probes are a named rig-confined close gate.

### SST evaluator turnover-base exclusions (carry-down blast radius)
- location: 0016_a21_compliance_watch.sql:544-554 (coverage), 599-603 (counted CTE); is_high_stakes 0009:1513-1521
- shape: v_has_ob:=exists(... e.status='approved' and e.is_opening_balance); ... v_cov:=not v_has_ob and v_first_posting is not null and v_first_posting<=(v_stat_month-interval '11 months')::date;  -- counted CTE filter: where e.client_id=p_client and e.status='approved' and not e.is_opening_balance and e.posting_date<=v_today and not (e.is_year_end and e.closing_transfer). is_high_stakes: je.is_opening_balance or je.is_year_end or je.tax_affecting or (je.flags ? 'amount_override') or sum(debit_cents) >= f.high_stakes_amount_cents.
- wave_b_touch: None-but-constrains, hard: the OB writer family MUST stamp is_opening_balance=true so carry-down never pollutes the RPR-proven watch figures (the evaluator excludes OB from turnover and flips coverage_complete=false, surfacing missing-history correctly); every OB entry is automatically high-stakes ⇒ the WB-R4 approval protocol must satisfy CLR05 per entry; the supersede verb's correction entries must carry the same marker.

### hazards
- _draft_entry_core's client gate is a BLACKLIST (only status='archived' raises, 0016:4000-4002): adding 'onboarding' to the clients CHECK makes onboarding clients immediately postable through draft_entry, wake_draft_entry, and the autodraft sweep unless the shared guard flips to an allowlist (status='active') — WB-R1 demands per-consumer negative tests; sites already allowlisting: mint_wake_credential autodraft (0011:1180), assign_document (0007:1386/2447), evaluate_sst_watches_all (0016:866), record_client_resolution-adjacent (0009:2311), consent writers (0012:54, 0014:91). list_review_queue and get_context_pack have NO status predicate at all.
- _approve_entry_core's sighting mint has NO account_class filter on the debit insert — any counterparty-stamped approved entry mints sightings, so carried AR/AP opening items approved through the normal core would seed the sighting pool (violates WB-R2's zero-sightings-from-prior-GL and the deliberately-empty S/P pool). The WB-R4 batch verb needs its own approval path or an explicit not-is_opening_balance carve-out; same for the mass counterparty birthing in B5.
- OB entries are structurally high-stakes (is_opening_balance ⇒ is_high_stakes, 0009:1515) ⇒ the CLR05 distinct-checker/attestation ladder binds per entry — the WB-R4 one-checker serializable set-approval must thread maker≠checker + the solo-attestation variant through every item, inside one transaction, while holding the seed-registry row lock.
- A scope_kind='client' open_questions row CLR26-blocks EVERY approval for that client (approve takes pg_advisory_xact_lock(203005004, hashtext(client)) then calls _open_question_blocks) — onboarding must-asks entering open_questions would deadlock the commit ceremony against itself; they must live only on the plan-as-document object (WB-R1/P19).
- get_context_pack v3's body is pinned by the 0016 tail prosrc assert (position('''pack_schema_version'',3'...) — the 0017 v4 CoR must ship its own tail assert AND the rollback preflight must account for frozen pre-v7 consumers (P5: v4 additive-but-dark; the wiki block renders only for the v7 purpose literal; non-terminal-run check before repoint).
- create_firm deliberately has NO op_receipt — a lost-response retry after a successful commit raises CLR04 ('invalid or consumed admission token') instead of replaying; the WB-R1 receipt-wrap must add replay without enabling token double-consumption, and the durable interview's commit step must be exactly-once across runtime kills (Gate F kill-mid-interview).
- op_receipts replays {'pending':true} for an op_key whose transaction is still in flight (result only set at _finish_op) — the failure-after-N-items resume gate and any chunked seeding writer must define semantics around a pending receipt; receipts roll back with their transaction, so per-item txns vs the WB-R4 single serializable txn is a real design fork.
- The trial_balance the pack embeds is SECURITY INVOKER while the pack is DEFINER (owner's using(true) RLS) — a dated sibling must copy the invoker + exact status='approved' filter; never CoR the 1-arg fn's shape (the pack body and its prosrc assert reference it).
- Constraint-swap idioms differ by lineage: open_questions origin is now NAMED (open_questions_origin_check_0016 — drop by name), clients.status is system-named inline (needs the 0016:189-201 find-by-definition dance), coa special_acc_type is named coa_accounts_special_acc_type_check; uq_coa_special gives one-per-client-per-VALUE for free for the new OBE/retained-earnings markers, and the sst_purchase_cost pattern shows the companion type-binding CHECK to copy.
- account_code grammar CHECK (0009:761) = '^[0-9]{4,8}$|^[0-9]{3}-[0-9A-Z]{2,4}$' — the LHDN CoA seed and any OBE/equity account codes must conform or 0017 swaps it (coa_accounts_account_code_check).
- Approval-order/locking in _approve_entry_core: filing FOR SHARE → row FOR UPDATE → advisory locks (203005003 counterparty-scoped, 203005004 client-scoped) → _open_question_blocks → status flip → sightings; any Wave B verb touching the same entries (supersede, batch approval, lint re-check belt) must respect this ordering to avoid deadlocks with live coding; the lint belt is per-client by ruling (WB-R8 — never firm-wide locks).
- The 0016 grants pattern is a firm-wide `revoke execute on all functions in schema clara from public` followed by explicit re-grants — 0017 must re-verify every retained same-arity CoR keeps its as-built ACL (tail-asserted), keep the agent-role-gains-zero-execute rule, and register every new fn in the 0011-style tuple matrix; the WB-R6 dependency audit reuses the 0016:5218 aclexplode+prosrc scan to prove no granted authority fn reads wiki tables.
- SST evaluator turnover excludes `not e.is_opening_balance` and `not (e.is_year_end and e.closing_transfer)` — OB carry-down entries missing the marker would silently inflate the RPR watch's confirmed figures (currently proven to the sen); coverage_complete flips false the moment ANY OB entry exists, which is correct surfacing but must be expected in the Gate K eval.

## x:events+consumers

The event spine is a per-firm-monotonic append-only log (clara.domain_events, seq allocated by firm_event_seq as the txn-tail lock) with a single ungranted emission point (_append_event) called LAST inside every audited writer, an empty-payload pg_notify('clara_events'), and a versioned routing taxonomy behind a guarded singleton pointer. Consumers are cloned from one template (sst_watch/facts_gate, Wave A2.1): own name, own hashtext advisory lock, own dedicated LISTEN-clara_events clara_runtime session, discoverWork/writeCheckpoint/dead-letter primitives reused verbatim from lib/relay.mjs, effect+checkpoint in one txn, MAX_ATTEMPTS=5 then checkpoint-skip, warn-only /ready health that touches only pre-migration tables. The daily SST belt (reconciler-sst.mjs) encodes the per-client locking lesson — one statement per client so the firm_event_seq row lock is never held across a sweep — with a receipt-last _all wrapper writing an append-only run receipt that backs a 48h stale flag, gated by a pure finite-guarded due-fn in the leader loop. Wave B's wiki-projection consumer, lint belt, and interview wake surface each have an exact as-built sibling to clone; the process currently holds ~26 Supavisor sessions (9 dedicated + 17 pooled), so each new LISTEN consumer must ride the WB-R18 headroom check.

### domain_events + event_types + firm_event_seq (the spine tables)
- location: packages/db/migrations/0005_event_spine.sql:58-94
- shape: create table clara.domain_events (firm_id uuid not null /*NO FK (C4)*/, seq bigint not null, id uuid not null unique default gen_random_uuid(), event_type text not null references clara.event_types(name), client_id uuid, actor uuid, on_behalf_of uuid, via_wake_kind text, entry_id uuid, document_id uuid, resolution_id uuid, payload jsonb not null default '{}', created_at timestamptz not null default now(), primary key (firm_id, seq)); event_types(name pk, client_scoped boolean not null, description); firm_event_seq(firm_id uuid pk /*NO FK*/, n bigint not null default 0). Append-only + validate triggers; _tf_validate_domain_event enforces firm-level types carry null client + ref ids belong to firm/client (CLR10).
- wave_b_touch: Extend by sibling rows only: 0017 registers the new wiki/lint/interview/carry-down event types into event_types + active taxonomy (additive-pair idiom). Events must carry enough IMMUTABLE metadata (storage key, content hash, page identity) in payload/document_id to rebuild the wiki index by replay (P17). Never touch the table DDL.

### clara._append_event (the single emission point)
- location: packages/db/migrations/0005_event_spine.sql:476-491
- shape: create function clara._append_event(p_firm uuid, p_type text, p_client uuid, p_actor uuid, p_obo uuid, p_wake_kind text, p_entry uuid, p_document uuid, p_resolution uuid, p_payload jsonb) returns bigint — insert into firm_event_seq ... on conflict do update set n=n+1 returning n (create-or-increment + ROW LOCK in one stmt); insert domain_events; perform pg_notify('clara_events',''); called LAST in every writer txn (the txn-tail lock). Ungranted; definer-context only.
- wave_b_touch: Every new 0017 audited writer (wiki ingest writer, lint-finding writer, OB/carry-down writers, interview commit) calls it LAST — never mid-body (C4 lock-order law). The 0016 evaluator precedent: emit only on real transitions (created/re_armed/tier_change at 0016:740,813,823), never on no-change evaluations, so converged belt passes take NO firm_event_seq lock.

### additive-pair taxonomy registration idiom
- location: packages/db/migrations/0016_a21_compliance_watch.sql:421-433 (same idiom 0011:3885-3901, 0009:1159-1168)
- shape: with added(name,client_scoped,description,decision,note) as (values ('compliance.watch_transition',true,'...','notification',null::text), ('document.classified',true,'...','ignore','facts-gate re-enqueue is a separate registered consumer')), inserted_types as (insert into clara.event_types(...) select ... returning name) insert into clara.trigger_taxonomy(version,event_type,decision,note) select a.version,x.name,x.decision,x.note from added x join inserted_types i on i.name=x.name cross join clara.taxonomy_active a; — no new version, no repoint. 0009 tail asserts FULL coverage (every event_type has an active-taxonomy row).
- wave_b_touch: 0017 registers wiki/lint event types this exact way. Decision choice is the design fork: 'ignore' + a registered runtime consumer (the document.classified/facts_gate precedent — right for the wiki projection), 'notification' only for genuinely human-surfaced transitions (each such event mints a held agent_task + wakes_outbox row via the drain — a chatty type would flood the held outbox).

### relay primitives (reused verbatim by every registered consumer)
- location: packages/runtime/lib/relay.mjs:152-288 (setRuntimeRole 152, acquireLeaderLock 164 'pg_advisory_lock(hashtext($1)::bigint)' session-level, discoverWork 208, deadLetterEvent 264, writeCheckpoint 279)
- shape: discoverWork: `select s.firm_id, s.n as head_seq, coalesce(c.last_seq,0) as last_seq from clara.firm_event_seq s left join clara.relay_checkpoints c on c.consumer=$1 and c.firm_id=s.firm_id where s.n > coalesce(c.last_seq,0)` — O(firms), never a log scan; LEFT JOIN bootstraps a new firm at lastSeq 0. writeCheckpoint: upsert `set last_seq = greatest(relay_checkpoints.last_seq, excluded.last_seq)` — monotonic, never regresses. relay_checkpoints pk (consumer text, firm_id); relay_dead_letters pk (consumer, event_id) with update-allowlist trigger (status/attempt_count/resolved_at only).
- wave_b_touch: The wiki-projection consumer imports these UNCHANGED (the sst_watch/facts_gate precedent — new consumer name string is the only namespace needed: its own checkpoint rows, dead-letter lane, advisory lock all key off it). No DB change required for a new consumer.

### the registered-consumer template (sst_watch — clone source for wiki_projection)
- location: packages/runtime/lib/sst-watch.mjs:29-332 (consts 29-36, runEffectTxn 110-130, processSstWatchFirm 136-173, runSstWatchCycle 177-200, redrive 211-239, CONSUMERS 245-247, health 254-270, loop 280-332)
- shape: SST_WATCH_CONSUMER='sst_watch'; SST_WATCH_EVENT_TYPE='entry.approved' (single subscribed type; ALL others coalesce into one checkpoint advance via batchMax). Effect + writeCheckpoint in ONE txn; dead-letter in its OWN txn (attempt_count survives rollback — the matcher idiom); MAX_ATTEMPTS=5 (env CLARA_SST_WATCH_MAX_ATTEMPTS) then checkpoint-skip; blocked=true retries next cycle; round-robin maxBatchesPerFirm=4, batchSize=100; POLL_INTERVAL_MS=2000 + waitForNudge; reconnect backoff 500→5000ms; op_key idiom 'sstwatch:<client>:<seq>'. CONSUMERS registry entry {name, identity:'runtime-role', redrive}. Loop: makeRuntimeClient() → connect → set role clara_runtime → acquireLeaderLock(name) → listen clara_events.
- wave_b_touch: New sibling module (wiki-projection consumer): clone this file shape with its own name/event-type set. If it subscribes to MULTIPLE event types (wiki pages project from several writers), extend the single-EVENT_TYPE filter to a Set — the only structural deviation from the template. The op_key embeds the event seq for exactly-once effect receipts.

### terminal-receipt semantics (facts_gate — DB owns the retry policy)
- location: packages/runtime/lib/facts-gate.mjs:22-28,47-54,110-131
- shape: enqueue_invoice_facts returns jsonb {document_id, status, task_id?}; terminal-by-design statuses (skipped_kind / skipped_consent_evidence / classify_low_confidence / skipped_type / already_completed / failed) are LOGGED verbatim and CHECKPOINTED — NEVER retried by the consumer ('the DB's attempt caps own the retry policy'); only a genuine THROW dead-letters. Takes ONLY p_document, no op_key — internally idempotent on the document's task trail.
- wave_b_touch: The wiki-projection writer fn follows this contract: a jsonb receipt with typed terminal statuses (e.g. already_projected / superseded / held_consent) that the consumer checkpoints without retry; only infrastructure throws dead-letter. P17's projection receipts land here.

### consumer cold-start (no checkpoint seeding exists anywhere)
- location: packages/runtime/lib/relay.mjs:200-227 (LEFT JOIN lastSeq 0) + packages/runtime/lib/reconciler-sst.mjs:5-8 + leader.mjs:52-58 (lastSstRun=0 first-cycle-at-boot)
- shape: grep-verified: NO migration or script seeds relay_checkpoints — a brand-new consumer surfaces every firm at last_seq 0 and REPLAYS the whole log. 0016 absorbed this two ways: (a) the effect is idempotent recomputation from the books (replay is harmless), (b) 'The first-cycle-at-boot sweep is what catches PRE-EXISTING crossings right after the 0016 deploy ceremony (the event-driven spine only re-evaluates a client on a NEW approval — the belt covers everyone)'.
- wave_b_touch: Design decision 0017 must make explicitly: either the wiki projection tolerates full-log replay (idempotent projection receipts keyed on event seq/content hash) or the ceremony seeds `insert into relay_checkpoints (consumer, firm_id, last_seq) select 'wiki_projection', firm_id, n from firm_event_seq` at deploy — plus a backfill/belt pass for pre-0017 documents (the belt-covers-everyone pattern). Pre-0017 events carry no wiki metadata, so seeding-at-head + deterministic backfill is the likely shape.

### clara.evaluate_sst_watch (the exception-isolated per-client evaluator — clone contract for the lint evaluator)
- location: packages/db/migrations/0016_a21_compliance_watch.sql:471-472,494-506,843-852 (revoke 853; runtime GROUP grant at tail)
- shape: create function clara.evaluate_sst_watch(p_client uuid, p_op_key text) returns jsonb language plpgsql security definer set search_path=clara,pg_temp — whole body wrapped `begin ... exception when others then return jsonb_build_object('client_id',p_client,'status','failed','changed',false,'error',sqlerrm,'sqlstate',sqlstate); end;` — NEVER raises to a caller; unknown client → {'status','skipped'}; op_key validated+audited but NOT op_receipts-reserved ('re-evaluation is idempotent recomputation from the books'); audits only when v_changed; returns {client_id,status:'ok'|'skipped'|'failed',changed,groups[]}. Granted to the clara_runtime GROUP (plain group-role call, no login dance).
- wave_b_touch: The lint evaluator (per-client hygiene pass: contradictions, staleness, orphans, caps, the WB-R5 opening-TB tie watch) clones this signature and contract exactly: (client, op_key) → jsonb receipt, exception-isolated, idempotent recomputation, runtime-GROUP-granted, emits events only on finding transitions (P18 open→superseded/resolved/recheck).

### evaluate_sst_watches_all + compliance_eval_runs (the belt receipt)
- location: packages/db/migrations/0016_a21_compliance_watch.sql:857-891 (table 376-387; append-only trigger 396-397)
- shape: create function clara.evaluate_sst_watches_all(p_op_key text) returns jsonb — loops `select c.id from clara.clients c where c.status='active' order by c.id`, calls evaluate_sst_watch(cl.id, p_op_key||':'||cl.id), counts failures WITHOUT raising, then inserts ONE clara.compliance_eval_runs row (started_at, completed_at, clients_examined, clients_changed, clients_failed, through_event_seq=max(domain_events.seq), schedule_note, error_note) and returns {run_id, clients_examined, clients_changed, clients_failed}. The receipt backs list_review_queue's stale_evaluator flag (>48h or absent ⇒ stale, 0016:4699-4702).
- wave_b_touch: The lint belt gets a sibling `lint_all`-style wrapper + its own append-only run-receipt table + a stale flag in the queue envelope. CRITICAL: the runtime NEVER calls the _all wrapper as the sweep itself — see the per-client belt lesson below; the wrapper is the receipt writer called ONCE after convergence.

### the per-client belt cadence (reconciler-sst.mjs — the firm-wide-stall lesson)
- location: packages/runtime/lib/reconciler-sst.mjs:10-45 (lesson header), 71-85 (per-client loop), 87-100 (receipt LAST), 101-108 (CADENCE LAW)
- shape: One statement — one implicit txn — one short-lived firm_event_seq lock — PER CLIENT: `select clara.evaluate_sst_watch($1,$2)` per active client id, op_key 'sstsweep:<iso>:<client>'. Why: an all-clients DB call holds the firm's event-seq ROW LOCK for the whole sweep and every concurrent writer emitting any domain event blocks behind it (worst on first post-deploy cycle). Receipt call (evaluate_sst_watches_all) runs ONCE, LAST, after convergence — a converged pass takes NO lock. CADENCE LAW: sstOk gates the leader's daily timer; false ONLY on whole-belt failure (discovery threw / receipt threw); a per-client failure never re-arms the belt (it would re-run every ~2s cycle → unbounded receipt growth).
- wave_b_touch: The lint belt (WB-R8 'lint daily on the per-client belt — never firm-wide locks') is a direct clone: runtime-side per-client iteration, receipt-last, whole-belt-only failure gating, poisoned clients visible in the run receipt. Same module-split precedent (own reconciler-lint.mjs re-exported from reconciler.mjs).

### leader-loop cadence gating (the due-fn idiom)
- location: packages/runtime/lib/leader.mjs:37-58 (SST_RECONCILE_MS finite-guard + sstReconcileDue), 78-79 (lastRun=0), 100-108 (due→sweep→advance-on-ok)
- shape: const SST_RECONCILE_MS = Number.isFinite(env) && env>0 ? env : 24*3600000 (finite-guarded: junk env can never silently disable the belt); pure `export function sstReconcileDue(lastRunMs, nowMs, intervalMs)`; `let lastSstRun = 0` ⇒ first cycle after boot runs immediately; `if (sstDue && swept.sstOk) lastSstRun = Date.now()` — a failed belt retries next cycle. The belt runs inside runReconcilerSweep on the LEADER connection (phase 3 after routing+drain).
- wave_b_touch: Add lintReconcileDue + lastLintRun as exact siblings (same finite-guard, same first-cycle-at-boot, same ok-gated advance) wired into runReconcilerSweep, OR give lint its own consumer loop — the leader-phase pattern is the as-built precedent and costs no new session.

### wake_fn_allowlist + assert_wake_allowed (wake authority)
- location: packages/db/migrations/0002_foundation.sql:247-251 (table), 553-559 (seed); 0004_governed_fns.sql:114-121 (assert); 0007:1098-1100 (drops wake_ingest_document, adds generated fn_name); 0011:3903-3910 (autodraft rows); 0016:5102-5107 (negative assert)
- shape: create table clara.wake_fn_allowlist (wake_kind text not null, function_name text not null, primary key (wake_kind, function_name)); assert_wake_allowed(p_wake_kind,p_fn) raises CLR03 when no row. LIVE rows today: interactive→{wake_draft_entry, wake_record_client_resolution, wake_record_notification, wake_open_question}; proactive→{wake_record_notification}; autodraft→{wake_draft_entry, get_document_extract, get_context_pack, get_draft_review, coding_lane, wake_open_question}. 0016 tail: `if exists(select 1 from clara.wake_fn_allowlist where function_name in ('evaluate_sst_watch',...)) then raise '0016 a new fn leaked into the wake allowlist'`.
- wave_b_touch: Interview workflows' wake surface: any new wake-callable fn (or a new wake_kind for the interview lane) is an APPEND insert in 0017 + an assert_wake_allowed(w.wake_kind,'<fn>') prologue in the entry fn (the get_context_pack 0016:4281-4283 pattern: kinds beyond interactive/proactive go through the assert). 0017 MUST repeat the negative tail-assert for every new lint/wiki/carry-down writer (none may leak into the allowlist), and the WB-R6 dependency audit rides the same tail.

### notification fns (proactive surfacing writers)
- location: packages/db/migrations/0005_event_spine.sql:819-844 (_record_notification_core), 0004_governed_fns.sql:654-690 (wake_record_notification), 0003_books_core.sql:184-192 (notifications table); best-effort usage precedent 0016:2605-2613
- shape: _record_notification_core(p_actor uuid, p_firm uuid, p_obo uuid, p_wake_kind text, p_client uuid, p_kind text, p_payload jsonb, p_op_key text) returns jsonb — op-reserved (_reserve_op/_finish_op firm-scoped idempotency), inserts notifications row, audits, emits 'notification.recorded' (taxonomy: ignore). wake_record_notification(p_kind, p_payload, p_client default null, p_op_key default null): proactive = single-use consume-FIRST (atomic conditional update, CLR03 on reuse) with op_key replay exception. 0016 idiom for system notices: `begin perform clara._record_notification_core(r.signed_by, e.firm_id, null, null, r.client_id, 'autopost_rule_suspended', jsonb…, 'autopost-suspend:'||r.id) ; exception when others then null; end;` — deterministic op_key = exactly-once, wrapped best-effort.
- wave_b_touch: P18's exactly-once lint-finding notification = _record_notification_core with the finding's identity/dedupe key as op_key (the autopost-suspend precedent). Interview proactive nudges ride wake_record_notification under the proactive single-use contract.

### wake_intents → drain → agent_tasks + wakes_outbox (the proactive projection)
- location: packages/runtime/lib/drain.mjs:41-146; wake-bound decisions relay.mjs:38-45; tables 0006_runtime_core.sql:97-228
- shape: WAKE_BOUND_DECISIONS=['internal_task','notification','background_review']; NON_WAKE=['context_update','ignore']. Drain: one txn per batch, `for update skip locked` on pending intents, project UNIFORMLY into a held agent_tasks row (kind='wake', on conflict do nothing on partial-unique origin_intent_id) + a held wakes_outbox row (condition derived from decision by trigger), surviving-row identity mismatch THROWS (rolls the batch back), intent flips pending→consumed last in-txn. Everything held — project-only, no autonomous LLM run (ruling 2).
- wave_b_touch: None-but-constrains: a Wave B event type registered as 'notification' automatically mints held task+outbox rows per event. The lint belt should therefore emit transition-only events (the compliance.watch_transition precedent: created/re_armed/tier_change only) so the held outbox stays bounded; per-page wiki projection events must be 'ignore' with the registered consumer doing the work.

### list_review_queue row_kind CoR (the queue surfacing seam)
- location: packages/db/migrations/0016_a21_compliance_watch.sql:4548-4744 (compliance_rows CTE 4650-4663, all_rows union 4664-4667, counts 4674-4681, compliance envelope 4699-4714, watermark 4691)
- shape: The 0016 CoR adds ONE sibling CTE: `compliance_rows as (select case when cw.state in ('crossed','overdue') then 1 else 2 end section_rank, 'compliance_watch'::text row_kind, case … 'needs_you' else 'needs_review' end section, …, cw.id watch_id, cw.state tier from clara.compliance_watches cw where … state<>'resolved')`, unions it into all_rows, adds a counts filter (`count(*) filter(where row_kind='compliance_watch')`), and a top-level envelope block with the stale flag. Every prior row shape gains only NULL-defaulted columns (watch_id, tier, coding_kind); 'Sort tuple, cursor grammar and every 0011 row shape are otherwise unchanged (additive keys only)'. Header comment 4548-4557 is the CoR law statement.
- wave_b_touch: P18's lint-finding row_kind is the next sibling CTE (severity → section_rank 1/2), a counts filter, and possibly an envelope summary — additive keys only, cursor grammar untouched. The dashboard needs the matching hydration card (P18: 'the queue's unknown-kind placeholder is not an acceptable surface'), so the row_kind string is a cross-repo contract.

### get_context_pack pack_schema_version 3 (the pack CoR + projection-lag surfacing seam)
- location: packages/db/migrations/0016_a21_compliance_watch.sql:4262-4351 (dual-lane prologue 4269-4288, books_version 4292-4293, sst block 4329-4348)
- shape: ONE-statement snapshot: 'books_version' = (select coalesce(max(de.seq),0) …) read in the SAME snapshot as every figure (the token IS the edition of the numbers); dual-lane prologue keyed on the clara.wake_secret GUC presence (wake_context() hash+liveness is the boundary, CLR03); the new block is an ARRAY with explicit framing keys: 'basis','db_computed_screening_estimate' and 'permitted_use','surface_and_request_professional_review_only'. pack_schema_version bumped 2→3.
- wave_b_touch: Pack v4: the wiki block renders ONLY for the new v7 purpose literal (WB-R3/P5 purpose-gated — pre-v7 consumers never see it), carries the wiki's last-projected event-seq marker beside books_version (projection lag SURFACED, freshness token stays authoritative), budgeted ≤6 pages/≤12 KB by relevance rank (the budgeted-CTE pattern), and each page carries its provenance citation. The basis/permitted_use framing-key idiom is the template for the wiki block's 'informs, never decides' framing.

### consumer registration + supervision + session budget (startWorld)
- location: packages/runtime/plugins/startWorld.ts:42-217 (fatal 75-79, A2.1 block + budget walk 176-204, dedicated heartbeat 206-213); pools.mjs:29-31 (pooled budget), 338-354 (makeRuntimeClient fail-closed)
- shape: Every loop: `const x = startXLoop({...}); x.done.then(() => fatal('x loop'), (e) => fatal('x loop', e)); sup.stops.push(x.stop);` — any unexpected settle exits the process non-zero (crash-only, Fly restarts). Budget comment (verbatim): 'the process now holds NINE dedicated LISTEN/persistent clients — control + leader + matcher + autodraft + local_facts + rule_post + (A2.1) sst_watch + facts_gate + classify — ON TOP OF the pooled budgets in lib/pools.mjs (5 runtime + 5 read + 2 write + 5 engine = 17). Grand total ≈ 26 sessions against the Supavisor session ceiling; the integrator MUST confirm headroom before deploy.' The 'world' heartbeat is a DEDICATED timer (10s), never the relay leader (ND5).
- wave_b_touch: The wiki-projection consumer registers here as loop #10 (+1 dedicated session ⇒ ≈27; WB-R18: Supavisor headroom check FIRST, ~26/60 today) with the fatal-watch + stops pattern; the lint belt costs zero sessions if it rides the leader's reconciler sweep. The budget comment MUST be rewritten to the new count (the walk-the-code discipline).

### /ready integration (warn-only consumer health)
- location: packages/runtime/lib/health.mjs:85-269 (per-consumer warn blocks 184-208; fail set 265-268); sst-watch.mjs:249-270 (the health-fn template)
- shape: /ready FAILS only on: db unreachable, world heartbeat stale (>30s), control heartbeat stale, taxonomy pointer missing. Every consumer is WARN-only ('a stalled X must NEVER take chat traffic down — the matcher/autodraft law'): try { const sh = await sstWatchHealth(c); checks.sstWatch = {ok:true,...sh}; if dead>0 warn; if lag>1000 warn } catch { warn 'unavailable' }. Health fns query ONLY spine tables that exist since 0005 (firm_event_seq / relay_checkpoints / relay_dead_letters) so /ready never breaks in the deploy-before-migrate window: lag = sum(greatest(s.n - coalesce(c.last_seq,0),0)) over the LEFT JOIN.
- wave_b_touch: wikiProjectionHealth clones the SQL shape (consumer name swapped) + one warn-only block in checkReadiness; lint staleness surfaces through the queue's stale flag (DB-side), not /ready. The pre-migration-safety property is mandatory: the 0017 runtime image deploys against a 16-migration DB during the ceremony.

### LISTEN channel discipline (the session-per-consumer law)
- location: packages/runtime/lib/listen.mjs:1-38 (isConnErr + waitForNudge); channel census: leader.mjs:90, matcher.mjs:465, autodraft.mjs:351, local-facts.mjs:226, rule-post.mjs:292, sst-watch.mjs:297, facts-gate.mjs:285, classify.mjs:303 — ALL `listen clara_events`; control.mjs:195 `listen clara_runtime_ctl` (pg_notify emitters: 0005:489 clara_events; 0006:856,887 clara_runtime_ctl)
- shape: Exactly TWO channels exist. Every spine consumer holds its own dedicated session LISTENing clara_events; the nudge is EMPTY-payload (N1/C7 — the channel is database-global, any role may LISTEN, so it must carry ZERO information; 'the relay treats any nudge as poll everything'). waitForNudge(client, ms, stopRef) = NOTIFY-or-timeout-or-stop, single-shot; the POLL is the delivery guarantee (a dropped NOTIFY can never strand work). isConnErr classifies 57P01/08xxx/ECONNRESET/EPIPE ⇒ discard + reconnect with 500→5000ms backoff.
- wave_b_touch: The wiki-projection consumer LISTENs clara_events — NEVER a new channel and never a payload-carrying notify. Interview durable runs ride the existing WDK world + control channel; no new LISTEN surface.

### projection replay/rebuild discipline (the as-built precedents)
- location: 0016:332+730-735 (evaluated_through_event_seq stamped per evaluation), 0016:878 (through_event_seq on run receipts), relay.mjs:14-18 ('Crash ⇒ the batch replays ⇒ at-least-once delivery, exactly-once effect'), 0016:4691 (queue watermark = max(seq)), docs/plan/wave-b-contract.md:105-109 (P17)
- shape: Three rebuildability properties as-built: (1) every derived state row stamps the event-seq it was computed through (compliance_watches.evaluated_through_event_seq, compliance_eval_runs.through_event_seq); (2) every consumer effect is either idempotent recomputation from base tables (evaluate_sst_watch) or ON-CONFLICT-keyed (wake_intents unique(event_id), agent_tasks unique origin_intent_id) so at-least-once replay collapses to exactly-once; (3) read surfaces carry the head marker (books_version / watermark) in the same snapshot as the data. P17 (contract, binding): 'events carry enough immutable metadata to rebuild the index by replay (never re-synthesis)'.
- wave_b_touch: The wiki projection must satisfy all three: wiki index rows stamp projected_through_seq (rebuild = truncate index + replay events, no LLM call); page-synthesis events carry the immutable Storage key + content hash (upload→verify→publish states, orphan repair per P17); the pack's wiki block exposes last-projected-seq vs books_version (WB-R3 lag surfacing).

### compliance_watches + compliance_watch_events (the durable watch-case state-machine template)
- location: packages/db/migrations/0016_a21_compliance_watch.sql:298-372 (one-open partial unique 353-354; resolved CHECK 343-347)
- shape: State machine as data: `state check (state in ('monitored','early_warning','crossed','overdue','resolved'))`; overlays never erase the condition (acknowledged_by/at, snoozed_until, next_rearm_cents/at as DATA); `create unique index uq_compliance_watches_one_open on clara.compliance_watches(client_id,service_group,watch_kind) where state<>'resolved'` — ONE open episode; ck_compliance_watches_resolved forces typed conclusion + evidence + actor + timestamp all-or-nothing; append-only compliance_watch_events(event_kind check in ('created','tier_change','acknowledged','snoozed','re_armed','resolved','evaluation'), state_before/after, figures jsonb, actor, rationale).
- wave_b_touch: P18 lint findings are the direct analog: identity/dedupe key → the one-open partial unique index; open→superseded/resolved/recheck transitions → the events trail + typed resolution verb with the all-or-nothing resolved CHECK; ack/snooze human-lane writers (0016:1047-1219 ack/snooze/resolve_compliance_watch, bookkeeper+, agent hard-refused CLR03) are the verb templates.

### 0016 tail assertion battery (the migration self-audit idiom)
- location: packages/db/migrations/0016_a21_compliance_watch.sql:4992-5331 (grant matrix 5090-5100, allowlist leak 5102-5107, must-not prosrc greps 5109-5119, seed/shape asserts 5232+)
- shape: The migration ends with a do-block battery: has_function_privilege checks per role per new fn (agent/wake roles must have ZERO execute on every new writer), the wake-allowlist leak check, prosrc position() greps asserting must-NOT couplings ('NO watch logic inside _approve_entry_core'), and seed-count/shape assertions — the migration aborts atomically if any invariant fails.
- wave_b_touch: 0017 must ship the same battery extended with the WB-R6 authority-boundary audit: no gate/bound/floor/autopost fn's prosrc references any wiki table (dependency-audited in-migration), zero agent/wake EXECUTE on wiki/lint/carry-down writers, no leak into wake_fn_allowlist except the deliberate interview rows, and the PUBLIC-execute revoke sweep at the tail (the 0005 N13 law — every new fn is PUBLIC-executable until it).

### hazards
- firm_event_seq is a per-firm ROW LOCK held until commit: any Wave B belt or writer that touches many clients in ONE transaction (a single all-clients lint call, a bulk wiki backfill in one txn) stalls every concurrent event-emitting writer firm-wide — the exact 0016 lesson; iterate one-statement-per-client in the runtime, receipt-call last after convergence.
- _append_event must be the LAST call in every writer txn (the txn-tail lock order, C4): a new 0017 writer that emits mid-body then takes other row locks can form a deadlock cycle with approve_entry/add_member; multi-event writers call it repeatedly at the tail (consecutive seqs under the held counter lock).
- No checkpoint seeding exists anywhere: a new consumer name cold-starts at last_seq 0 and replays the ENTIRE per-firm log. The wiki projection must either be replay-safe from seq 0 (pre-0017 events carry no wiki metadata — likely wasteful/wrong) or the ceremony must seed relay_checkpoints at head + run a deterministic backfill belt; decide explicitly in the design, don't inherit silently.
- Session budget: 9 dedicated LISTEN/persistent sessions + 17 pooled ≈ 26 against the Supavisor ceiling today; each new spine consumer is +1 dedicated session. WB-R18 makes the headroom check the FIRST ceremony step — and the startWorld.ts budget comment must be re-walked, not incremented blindly.
- The clara_events channel is database-global and any role may LISTEN (empirically proven, N1/C7): nudges must carry ZERO payload — never put a wiki page id, client id, or finding key in pg_notify.
- Taxonomy full-coverage is an enforced invariant (0009 tail asserts it; an uncovered type dead-letters at the router every cycle): every new 0017 event type MUST go through the additive-pair CTE against taxonomy_active in the same migration.
- A 'notification' taxonomy decision mints a held agent_task + wakes_outbox row PER EVENT via the drain: registering a chatty per-page or per-evaluation event type as 'notification' floods the held outbox; emit transition-only events (the created/re_armed/tier_change precedent) and keep bulk projection types 'ignore' with a registered consumer.
- Consumer health fns must query only tables that exist BEFORE 0017 (the sst-watch precedent queries 0005-era spine tables only): the v25 image deploys against the 16-migration DB during the ceremony window, and /ready must not break there.
- Exactly-once is per-layer, not global: the spine gives at-least-once delivery; the effect must supply idempotence (seq-embedded op_key, ON CONFLICT keys, or internally-idempotent fns). Terminal-by-design receipt statuses are checkpointed and NEVER consumer-retried — the DB's attempt caps own retry policy; conflating the two reintroduces retry storms.
- Dead-letter attempt counts must be written in their OWN transaction (the matcher idiom) or the effect-txn rollback erases them and MAX_ATTEMPTS never trips; redrive requires an existing dead-letter row FOR UPDATE (X5a) and must never falsely resolve.
- Checkpoints are monotonic by greatest() — any hand-written checkpoint path that plain-UPDATEs can regress or silently no-op on new firms (C2); reuse writeCheckpoint verbatim.
- Replacing writer bodies while a live runtime runs lets an in-flight PL/pgSQL execution finish on its STARTING body (D1, 0005 header): the 0017 ceremony needs the same write-quiesce → atomic apply → runtime v25 ordering as 0016.
- PUBLIC-lockdown (N13): every function 0017 creates is PUBLIC-executable until the tail `revoke execute on all functions in schema clara from public` sweep; named grants go AFTER the sweep; the tail assertion battery (grant matrix + allowlist-leak + WB-R6 prosrc dependency audit) is the enforcement point.
- The wake allowlist is a belt ON TOP of role grants — 0016's negative assert pattern (no new fn leaks into wake_fn_allowlist) must be repeated for every lint/wiki/carry-down writer, while interview wake rows are deliberate append inserts; new wake kinds must also thread assert_wake_allowed in every entry fn they may call (the 0011 autodraft precedent: read fns need allowlist rows too).
- Belt cadence gating: the ok-flag must go false ONLY on whole-belt failure — gating on per-client failure re-runs the belt every ~2s leader cycle (unbounded receipt growth, sustained load); poisoned clients stay visible in the run receipt instead.
- Cadence env knobs must be finite-guarded (Number.isFinite && >0 else default): a NaN interval makes the due-check permanently false and silently disables a daily belt — the one failure mode the leader.mjs comments call out twice.

## x:runtime+freeze

Runtime versioning is enforced by a three-layer freeze system: (1) frozen-workflows.json (53 entries today: {version:1, workflows:{path:{sha256,note}}}), append-only vs origin/main, re-baselined ONLY locally via `pnpm freeze:update` (--update refused under CI); (2) scripts/check-frozen-workflows.mjs which golden-hashes every @frozen file + its transitive RELATIVE import closure, mandates @frozen+registration for every "use workflow" file, rejects import-escapes (workspace/alias specifiers), and structurally parses workflows/registry.ts for version monotonicity + enqueue-site provenance (fail-closed); (3) the registry itself, where each class points at its newest _vN export and all prior versions stay exported for parked runs. A chatTurn v7 / autoDraft v3 is a byte-copy of a SIX-file closure (.ts/.impl/.tools/.infra/.prompt/.errors), renamed exports with agreeing _v7/.v7 suffixes, registry repoint, and 12 appended manifest entries (53→65). The pack purpose literal lives in three as-built places per family ("chat" in loadContextStepV6 + the chat tool default; "coding" in the draft wrapper's server-side read and as the autoDraft tool default) — but critically, the tools' purpose is a MODEL-SUPPLIED free string, so a purpose-literal-only wiki gate is not structural against frozen v4–v6. Consumers run as clara_runtime on dedicated LISTEN clients (rule_post alone does a per-effect `reset role` login-direct dance); chat/autodraft lanes run steps on withRuntime and mint per-attempt OBO wake credentials inside the step (never crossing a step boundary), reads via clara_agent_ro + txn-local wake_secret, writes via clara_wake_interactive.

### workflows/registry.ts — current full shape
- location: packages/runtime/workflows/registry.ts:21-53
- shape: export const workflows = { closeExample: closeExampleV1, chatTurn: chatTurn_v6, documentIngest: documentIngest_v1, invoiceFacts: invoiceFacts_v1, autoDraft: autoDraft_v2 } as const;  plus kept re-exports: export { chatTurn_v1 }; { chatTurn_v2 }; { chatTurn_v3 }; { chatTurn_v4 }; { chatTurn_v5 }; { autoDraft_v1 };  and export const workflowNames: string[] = Object.keys(workflows). Header law: enqueue sites import from HERE; repoint on behavioural change; never rename/delete an export with in-flight runs (policy (c)).
- wave_b_touch: Repoint chatTurn: chatTurn_v6→chatTurn_v7 and autoDraft: autoDraft_v2→autoDraft_v3; ADD chatTurn_v6 + autoDraft_v2 to the kept re-export block; add the NEW interview class entry/entries (e.g. firmInterview: firmInterview_v1) — new classes are permitted by the monotonicity check, but once added a class can NEVER be removed (REGISTRY-CLASS-REMOVED), so name the family deliberately.

### Registry identifier/module version convention (fail-closed parse)
- location: scripts/freeze-lint-checks.mjs:30-38,101-135
- shape: identifierVersion: /_?[vV](\d+)$/ on the identifier; specVersion: /\.v(\d+)(?:\.[cm]?[jt]sx?)?$/i on the module spec. Each `class: identifier` entry MUST be a plain identifier imported from a RELATIVE module, carry a parseable _vN suffix, and the identifier version must AGREE with the module's .vN suffix — else REGISTRY-UNPARSEABLE / REGISTRY-VERSION-MISMATCH (violation, never a skip). checkRegistryMonotonicity: class version may only stay EQUAL or INCREASE vs origin/main; removed class = REGISTRY-CLASS-REMOVED reject.
- wave_b_touch: Constrains naming: the new exports must be exactly chatTurn_v7 in chatTurn.v7.ts and autoDraft_v3 in autoDraft.v3.ts (relative imports); interview workflows must also carry _v1 suffixes in .v1.ts modules or the registry parse fails closed.

### Enqueue-site provenance law
- location: scripts/freeze-lint-enqueue.mjs:19-20 (+ startWorld.ts:117-135 as the compliant pattern)
- shape: ENQUEUE_MODULES = new Set(["workflow/api", "@workflow/core/runtime", "@workflow/core/runtime/start"]); ENQUEUE_CALLABLES = new Set(["start"]). Every start() call in packages/runtime (tests + registry excluded) must receive a reference whose import provenance traces to workflows/registry.ts; dynamic import of workflow/api, re-exporting start, or extracting the callable (const go = api.start) are each hard rejects.
- wave_b_touch: The interview enqueue sites (HTTP route or consumer) must be written as `start(workflows.<interviewClass>, [{...}])` with `workflows` imported from ../workflows/registry.js — exactly the startWorld.ts enqueueChatTurn/enqueueAutoDraft pattern.

### Freeze manifest + freeze:update process
- location: frozen-workflows.json (53 entries) · scripts/check-frozen-workflows.mjs:47-52,298-321,401-434 · package.json scripts
- shape: Manifest: {"version":1, "workflows": {"<repo-rel path>": {"sha256": "<LF-normalised sha256>", "note": ""}}} — currently 53 entries (all chatTurn v1–v6 + autoDraft v1–v2 closures, closeExample.v1, documentIngest, invoiceFacts, steps.ts; NOTE invoiceFacts.v1.azure.mjs/.services.mjs are OUTSIDE the freeze surface — injected services). `pnpm freeze:update` = node scripts/check-frozen-workflows.mjs --update (REFUSED under CI); CI runs verify-only with append-only-vs-origin/main as THE durable protection: changed hash = REHASHED-VS-BASE, dropped entry = REMOVED-VS-BASE, both hard rejects. Every "use workflow" file under packages/ must be @frozen AND registered (H2); the transitive relative-import closure is hashed; first-party workspace/alias imports from a frozen file = IMPORT-ESCAPE reject.
- wave_b_touch: Append-only bump 53→65: 6 chatTurn.v7.* + 6 autoDraft.v3.* entries added via local `pnpm freeze:update` then committed; any interview .v1 workflow closure adds its own entries the same way. Zero existing hashes may change — even a whitespace touch to a v1–v6 file fails CI.

### chatTurn.v6 closure — EXACT file inventory (what a v7 copy must include)
- location: packages/runtime/workflows/chatTurn.v6.{ts,impl.ts,tools.ts,infra.ts,prompt.ts,errors.ts}
- shape: SIX files, all manifest-registered: chatTurn.v6.ts (main: "use workflow" + @frozen; export async function chatTurn_v6(input:{taskId:string}): Promise<{taskId;outcome;segments}>; MAX_SEGMENTS=12; pushPart je_review/refusal replay-dedup; park via createHook<{kind:"answer"|"expired"|"cancelled";answer?}>({token}) + await hook) · chatTurn.v6.impl.ts ("use step" bodies: claimRunStep, loadTaskStepV6, loadContextStepV6, runModelSegmentStepV6 (streamText, stopWhen:[stepCountIs(8), hasToolCall("clarify"), stoppedOnSuccessfulDraft], getWritable stream), mintHookTokenStep (`clarify:${randomUUID()}` memoized), openInterruptionStep (clara.open_interruption), checkpointStep (clara.checkpoint_turn), markRunningStep, settleStep (clara.settle_chat_turn), closeStreamStep) · chatTurn.v6.tools.ts (buildToolsV6 + runDraftJournalEntry → clara.wake_draft_entry 14-arg call; get_context_pack/trial_balance/list_journal_entries/read_document/list_unassigned_documents/clarify) · chatTurn.v6.infra.ts (pools() from globalThis.__claraPools — NEVER imports pools.mjs; ClaraPools type incl. mintWakeCredentialObo; readScoped/writeScoped/safeRead; resolveModel with __claraModelForTest override) · chatTurn.v6.prompt.ts (SYSTEM_PROMPT_V6, CLARIFY_FRAMING, DRAFT_TOOL, toTypedParts_v6, findClarifyCall, hasCodingIntent, ClaraPart types) · chatTurn.v6.errors.ts (refusalFromDbError, runtimeRefusal, sessionUnboundRefusal, codingIncompleteRefusal, readToolRefusalMessage, isAuthorityOrOracleError — the CLR→typed-refusal map).
- wave_b_touch: Copy all six to .v7 siblings, rename every internal import to the .v7.js specifiers (relative), rename exports (chatTurn_v7, SYSTEM_PROMPT_V7, buildToolsV7, loadContextStepV7...), keep @frozen headers; the wiki-block framing + citation guidance lands in v7.prompt; the new purpose literal lands in v7.impl + v7.tools.

### autoDraft.v2 closure — EXACT file inventory (what a v3 copy must include)
- location: packages/runtime/workflows/autoDraft.v2.{ts,impl.ts,tools.ts,infra.ts,prompt.ts,errors.ts}
- shape: SIX files, all manifest-registered: autoDraft.v2.ts ("use workflow"; export async function autoDraft_v2(input:{taskId:string}): Promise<{taskId;outcome}> — claim (begin_autodraft_task) → recoverAutoDraftStep → runAutoDraftModelStep → settleAutoDraftStep; noop_existing on CLR29 double_coded; openSweepQuestionStep for question-shaped refusals) · .impl.ts (steps; begin_autodraft_task CAS + getWorkflowMetadata().workflowRunId) · .tools.ts (draft wrapper + get_context_pack tool) · .infra.ts (globalThis pools; autodraft credential = 5-arg client-pinned mint_wake_credential('autodraft', firm, null, ttl, client) — OBO NOBODY, system origin) · .prompt.ts (SYSTEM_PROMPT_AUTODRAFT_V2, isQuestionShaped, toAutoDraftOutcome; line 71 documents purpose "coding") · .errors.ts (refusalFromDbError, noFilingRefusal, noDraftRefusal, readToolRefusalMessage).
- wave_b_touch: Copy all six to .v3 siblings (WB-R7: ships wiki-aware in the SAME ceremony); the sweep's wiki framing + the W2 sweep-probe surface goes in v3.prompt; the new purpose literal in v3.tools; registry repoints autoDraft: autoDraft_v3.

### Pack purpose literals as-built (the P5 gate anchor)
- location: chatTurn.v6.impl.ts:111 · chatTurn.v6.ts:79-81 · chatTurn.v6.tools.ts:108,236-240 · autoDraft.v2.tools.ts:97,203-207
- shape: loadContextStepV6 (impl:111): c.query("select clara.get_context_pack($1, $2) as pack", [clientId, "chat"]) — injected into the system prompt as `Client context pack (books_version is the freshness token):\n${JSON.stringify(ctx.contextPack)}` (v6.ts:79-81). Chat tool (tools:236-240): inputSchema z.object({ purpose: z.string().optional() }); execute passes [clientId, purpose ?? "chat"]. Draft wrapper server-side authoritative read (tools:108): [clientId, "coding"]. autoDraft tool default (v2.tools:207): purpose ?? "coding"; draft wrapper (v2.tools:97): "coding". DB side (0016_a21_compliance_watch.sql:4262-4268,4291): get_context_pack(p_client uuid, p_purpose text) SECURITY DEFINER STABLE — validates purpose non-blank (CLR10) and ECHOES it ('pack_schema_version',3,'purpose',p_purpose) but does NOT branch on it today.
- wave_b_touch: v7/v3 swap the literals to the new WB-R6/A2 purpose (all four call sites: v7 loadContextStep, v7 chat-tool default, v7 draft wrapper, v3 tool default + wrapper), and 0017 makes get_context_pack purpose-BRANCHING so the wiki block renders only for the new literal (pack_schema_version bump). See hazard: the tool purpose is model-suppliable.

### New-family registration + durable/park surface (what the interview workflows ride)
- location: plugins/startWorld.ts:20-21,83-88,110-142 · chatTurn.v6.ts:119-143 · chatTurn.v6.impl.ts:250-284 · lib/control.mjs:28,82-111,238
- shape: Registration: a new .v1.ts file with a `"use workflow"` prologue + @frozen, exported and added to the registry `workflows` object; the Nitro build (workflow/nitro compiler module) compiles the directives; the embedded worker starts via `(await import("workflow/runtime")).getWorld().start?.()` gated on CLARA_START_WORLD==="1". Enqueue: `start(workflows.<class>, [args])` from "workflow/api". Park: token = mintHookTokenStep (`clarify:${randomUUID()}`, memoized step so replay-stable) → createHook<{kind:...}>({token}) → clara.open_interruption(taskId, hookToken, question_jsonb, null) opened ATOMICALLY in a step → `await hook` = zero-compute park (survives kills/redeploys; run resumed by run id). Resume: lib/control.mjs leases clara.agent_interruptions (update ... delivered_at) and calls resumeHook(hook_token, payload) from workflow/api; markRunningStep flips awaiting_input→running. Durable business state: checkpointStep → clara.checkpoint_turn($task,$segment,$tokens,$parts::jsonb) per segment; "use step" bodies are engine-memoized on replay.
- wave_b_touch: New sibling family: the interview workflows reuse this exact surface (hook-token-per-question steps, open_interruption or a typed sibling lane, per-step checkpoints holding the P19 plan-as-document state until commit); gate O's ≥48h park and kill-mid-interview resume are exactly the createHook + step-memoization guarantees.

### Rollback preflight (§4.9 — BLIND REVERT FORBIDDEN)
- location: packages/runtime/README.md:233-244
- shape: "Before any `fly releases`/rollback, confirm the target image exports every workflow name+version that has non-terminal runs:  select name, count(*) from workflow.workflow_runs where status not in ('completed','failed','cancelled') group by name;  If the target image lacks a workflow that still has parked/running runs, a revert would strand them — quiesce/drain those runs first, or do not roll back to it."
- wave_b_touch: None-but-constrains: the v25 ceremony must run this preflight before AND after (WB-R17 rig gate: "v25 upgrade with parked runs + rollback preflight (non-terminal-run check)"); with the interview family parking ≥48h, non-terminal interview runs will be the NORM, so a v25→v24 revert is structurally forbidden once the first interview parks (v24 lacks the interview exports).

### DB roles in play — consumers vs chat lanes vs OBO mint
- location: lib/pools.mjs:57,228-241,252-282,293-336,345-359 · lib/sst-watch.mjs:24-27 · lib/rule-post.mjs:48-56 · plugins/startWorld.ts:57-64
- shape: THREE logins/roles (N10: connect as login, SET ROLE immediately): clara_runtime_login→SET ROLE clara_runtime (runtime pool max 5: admission, settle, mint, checkpoints, drain, heartbeats, reconciler); clara_agent_read_login→SET ROLE clara_agent_ro + default_transaction_read_only=on (read pool max 5); clara_wake_write_login→SET ROLE clara_wake_interactive, NOT read-only (write pool max 2). CONSUMER LANES (matcher/autodraft/local_facts/rule_post/sst_watch/facts_gate/classify + control + leader) each hold a DEDICATED makeRuntimeClient() (clara_runtime_login) with setRuntimeRole → clara_runtime, own advisory lock + LISTEN clara_events; rule_post is the one exception — per-effect `reset role` to the raw clara_runtime_login (login-direct EXECUTE on execute_rule_post) then `set role clara_runtime` back. CHAT LANE OBO mint: mintWakeCredentialObo(firmId, oboUserId, ttl='5 minutes') = withRuntime → select credential_id, secret from clara.mint_wake_credential('interactive', $firm, $oboUserId, $ttl::interval); DB rejects below-bookkeeper OBO with CLR10; secret bound TXN-LOCALLY via select set_config('clara.wake_secret',$1,true) inside withReadWakeScoped (rollback-ended) or withWriteWakeScoped (commits); secret NEVER crosses a WDK step boundary. autoDraft mint: 5-arg client-pinned mint_wake_credential('autodraft', firm, null, ttl, client). Session budget: 17 pooled (5+5+2+5 engine) + 9 dedicated ≈ 26 vs Supavisor ceiling.
- wave_b_touch: The Wave B lint-belt + wiki-projection consumers are NEW SIBLINGS on the sst-watch pattern (own .mjs, own advisory lock, dedicated clara_runtime LISTEN client, own dead-letter, startXLoop in startWorld.ts) at +1 session each — WB-R18 mandates the Supavisor headroom check FIRST (~26/60). Interview steps mint OBO per attempt exactly like readScoped/writeScoped; the wiki writers' grants decide whether the projection consumer is a plain group-role call (evaluate_sst_watch precedent) or a login-direct dance (execute_rule_post precedent).

### World-off default + shared-engine safety
- location: plugins/startWorld.ts:43-46 · packages/runtime/README.md:45-50,170-186
- shape: if (process.env.CLARA_START_WORLD !== "1") { console.log("...world NOT started...skeleton mode"); return; } — the world runs ONLY in the deployed Fly app; the engine schemas are `workflow` + `graphile_worker` (bootstrap bin, idempotent); single always-on non-HA machine (single-leader engine, never scale >1).
- wave_b_touch: None-but-constrains: the v25 ceremony deploys through the same single-machine world; rig-confined interview fault gates (kill/resume, ≥48h park) run against a throwaway world, never the shared project.

### hazards
- MODEL-SUPPLIED PURPOSE STRING (the P5 gate's soft spot): in every frozen closure v2–v6 the get_context_pack TOOL takes `purpose: z.string().optional()` straight from the model (chatTurn.v6.tools.ts:236-240, default "chat"; autoDraft.v2.tools.ts:203-207, default "coding"). If 0017 gates the wiki block on the purpose literal ALONE, a prompt-injected model running under a frozen v4–v6 closure can pass the v7 literal and see the wiki block — violating WB-R6(2) 'pre-v7 consumers never see the wiki block' as a STRUCTURAL claim. The DB-side gate needs a second discriminator old closures cannot supply (e.g. an allowlisted purpose set per pack_schema_version, a new fn name, or a wake-credential/kind check), or the tool schema in v7 must pin purpose to a literal enum AND the DB must reject the v7 literal from any caller context that isn't v7 — decide explicitly, don't inherit the free-string pass-through into v7.
- FREEZE APPEND-ONLY MECHANICS: `pnpm freeze:update` is refused under CI — the 12 new manifest entries (53→65, plus interview closures) must be re-baselined locally and committed; ANY byte change to an existing v1–v6/autoDraft.v1–v2 file (even whitespace, even a comment) = REHASHED-VS-BASE hard CI reject. The v7/v3 copies must be true copies, not refactors of shared code.
- NAMING IS LOAD-BEARING: exports must be exactly chatTurn_v7 / autoDraft_v3 in modules named .v7.ts / .v3.ts, imported RELATIVELY into the registry — the structural parser fails closed on any suffix mismatch, alias, or non-relative import (REGISTRY-UNPARSEABLE / REGISTRY-VERSION-MISMATCH). Interview workflows need _v1-suffixed identifiers in .v1.ts modules from day one.
- A REGISTRY CLASS IS FOREVER: adding the interview class(es) is monotonicity-legal, but REGISTRY-CLASS-REMOVED makes removal impossible while the freeze-lint stands — choose the class names (one `interview` class vs separate `firmInterview`/`clientInterview`) as a ratified design decision, not ad hoc.
- ROLLBACK BECOMES STRUCTURALLY BLOCKED: once the first interview parks (≥48h by design, gate O), workflow.workflow_runs will hold non-terminal interview runs and the §4.9 preflight forbids reverting to v24 (which lacks the interview exports). The ceremony plan must treat v25 as a forward-only deploy with quiesce/drain as the only escape hatch.
- ALL FIRST-PARTY IMPORTS IN THE NEW CLOSURES MUST BE RELATIVE (IMPORT-ESCAPE reject) and infrastructure must come from globalThis injection (__claraPools / __claraModelForTest — the AB-16 precedent), never an import of pools.mjs, or tuning gets hash-locked into the frozen closure.
- ENQUEUE PROVENANCE: interview enqueue sites must statically import { start } from "workflow/api" and pass workflows.<class> from the registry — dynamic import, re-export, or extracting the callable each fail CI (fail-closed).
- WAKE SECRETS NEVER CROSS A STEP BOUNDARY and TTL is ~5 minutes: an interview that parks for 48h must mint fresh OBO credentials inside each post-resume step; nothing credential-shaped may live in workflow state/checkpoints (P19 keeps interview outputs in run checkpoints + the plan object — keep secrets out of both).
- HOOK TOKEN REPLAY: mint each hook token in its own memoized "use step" (the mintHookTokenStep pattern) — a token minted outside a step changes on WDK replay and strands the interruption row; a multi-question interview needs a new memoized token per question.
- SESSION CEILING: every new consumer lane (lint belt, wiki projection) is +1 dedicated Supavisor session on top of ≈26 today; WB-R18 makes the headroom check the ceremony's FIRST step — and each lane needs its own advisory lock + dead-letter lane (the sst-watch sibling pattern), never a shared leader.
- CONSUMER AUTHORITY PATTERN CHOICE: plain group-role call (evaluate_sst_watch granted to clara_runtime — no role dance) vs login-direct (execute_rule_post: EXECUTE granted to the clara_runtime_login shell only, per-effect `reset role`). 0017's wiki-writer/lint grants must pick one deliberately; a mismatch surfaces as 42501 at runtime, not at CI.
- PROMPT-ONLY BUMPS STILL COST A FULL CLOSURE: v5→v6 was prompt-only yet shipped all six files + 6 manifest entries; budget the same for v7/v3 — there is no 'small' version bump under this freeze design.
- steps.ts is frozen and shared by closeExample.v1 — do NOT add interview steps to it; each interview version owns its own impl module, or a later interview change would try to mutate a frozen shared file.

## x:storage+documents

The storage + document pipeline is fully runtime-owned and content-addressed: a single private Supabase bucket `firm-docs` written ONLY by the Fly runtime under a dedicated NOLOGIN custom role (`clara_storage_docs`, INSERT+SELECT only, no UPDATE/DELETE — write-once, delete-never), with the canonical key grammar `firms/{firm_id}/docs/{sha256}.{ext}` triple-enforced (documents CHECK, intakes CHECK, runtime safeKey regex, Storage RLS policy). Intake is a durable DB state machine (create→claim→received→verifying→verified/duplicate→finalized/adopted) with malware/quarantine scanning BEFORE canonical storage; the 0016 kind gate routes pdf/image NULL-kind docs to a local classify lane (`classify_document`, single audited verdict writer, ≥0.8 sets `documents.document_kind`, <0.8 opens an origin='classification' review question), and any non-invoice-shaped kind — including `opening_balance_doc` and `management_account`, the WB-R15 tie-out kinds — gets a terminal `skipped_kind` receipt: OCR/structured_parse text+regions exist but NO typed facts writer consumes them, and spreadsheet mimes never even reach the classify lane (kind stays NULL unless a human calls set_document_kind). Provenance is filing-bound (`assert_provenance`/`_active_document_filing`: active non-retired filing + sha match + bytes_verified_at, CLR02), and `get_document_extract` carries the budgeted-CTE running-window truncation idiom WB-R8 adopts.

### ck_documents_storage_path_v2 (documents key grammar)
- location: packages/db/migrations/0007_document_pipeline.sql:53
- shape: alter table clara.documents add constraint ck_documents_storage_path_v2 check ( storage_path ~ ('^firms/' || firm_id::text || '/docs/' || sha256 || '[.][a-z0-9]{1,12}$') ) not valid;  -- same grammar on intakes: ck_document_intakes_storage_key (0007:142-144): storage_key ~ ('^firms/'||firm_id::text||'/docs/'||sha256||'[.][a-z0-9]{1,12}$')
- wave_b_touch: none-but-constrains: wiki page objects can NOT ride documents.storage_path (docs/ prefix + sha-named, one row per sha). P17 needs a NEW sibling key family (e.g. firms/{firm_id}/wiki/…) carried on the new wiki_pages index table with its own CHECK; documents rows stay reserved for evidence bytes.

### safeKey — runtime storage module key validator
- location: packages/runtime/lib/storage.mjs:16-22
- shape: function safeKey(key){ if(!/^firms\/[0-9a-f-]{36}\/docs\/[0-9a-f]{64}\.[a-z0-9]{1,12}$/i.test(value)) throw new StorageError("storage_error","canonical storage key is invalid"); }  — every putCanonical/hashCanonical/verifyCanonical/downloadCanonical path funnels through it
- wave_b_touch: extend/new sibling: wiki puts need either a widened validator or (cleaner) a sibling key-family fn (safeWikiKey) + dedicated put/verify wrappers; must stay content-hash-addressed per P17 (deterministic immutable keys + content hashes, upload→verify→publish states).

### Storage bucket + custody role + RLS policies (the ceremony artifact)
- location: packages/db/deploy/storage-provision.sql:60-83
- shape: private bucket `firm-docs`; role clara_storage_docs NOLOGIN NOINHERIT, granted to authenticator; grant select, insert on storage.objects (NO update/delete — "write-once, read-back verified, delete-never"); create policy clara_storage_docs_insert … with check (bucket_id='firm-docs' and name ~ '^firms/[0-9a-f]{8}-…-[0-9a-f]{12}/docs/[0-9a-f]{64}\.[a-z0-9]{1,12}$'); identical SELECT policy. Runtime env: CLARA_STORAGE_URL (= /storage/v1/object/firm-docs base, packages/runtime/README.md:83), CLARA_STORAGE_ROLE=clara_storage_docs, CLARA_STORAGE_ROLE_JWT (custom-role JWT; anon/authenticated/service_role refused, storage.mjs:40-62)
- wave_b_touch: extend: the wiki artifact protocol needs a new policy pair (INSERT+SELECT only, same bucket + role recommended) whose name-regex admits the wiki key family. Same-bucket keeps the DR rclone `firm-docs` byte mirror covering wiki objects for free; a new bucket would need new backup wiring + zero-501 proof. Ceremony-only (local rig has no storage schema).

### putCanonical / verifyCanonical (the only storage writers)
- location: packages/runtime/lib/storage.mjs:81-97,120-124
- shape: putCanonical(filePath,key,mime): POST objectUrl with headers {authorization:Bearer jwt, apikey:jwt, content-type:mime, "x-upsert":"false"}; 409 → {created:false,existed:true}; verifyCanonical(key,expectedSha256) re-downloads and sha256-compares (checksum_mismatch). Browser NEVER holds a storage credential (apps/dashboard/app/shared/intake.ts:2, reviewApi.ts:194-199); human byte reads go through the runtime route GET /api/documents/:id/bytes (packages/runtime/src/documentRoutes.ts:50, JWT + membership-validated, download-then-stream with sha verify)
- wave_b_touch: reuse pattern: wiki page publish = put(immutable versioned key) → verify(content hash) → publish state flip in DB; x-upsert:false + no-UPDATE policy makes overwrite structurally impossible, so "every edit reversible" (WB-R9) = new object per version + index pointer swap; orphan repair must treat 409-existed as idempotent success.

### documents.document_kind CHECK (17 kinds as-built)
- location: packages/db/migrations/0014_consent_evidence_documents.sql:64-65 (supersedes 0007:33-37)
- shape: constraint documents_document_kind_check check (document_kind is null or document_kind in ('invoice','receipt','credit_note','debit_note','bank_statement','payment_voucher','claim_form','payroll_summary','tax_correspondence','ssm_company_doc','agreement_contract','e_invoice_xml','management_account','opening_balance_doc','knowledge_artifact','handwritten_note','consent_evidence','other'))  — 0014 did a lookup-drop + re-add of 0007's check (the constraint-swap precedent)
- wave_b_touch: none-but-constrains: `opening_balance_doc` (WB-R15 tie target), `management_account` (WB-R16 RPR incremental), and `knowledge_artifact` (wiki source material) already exist — 0017 adds NO new kind for these. The 0014 swap idiom is the exact pattern for 0017's open_questions.origin widening (B3). classify fns already accept all 17 (0016:3202-3207).

### document_filings (the filing object provenance binds to)
- location: packages/db/migrations/0007_document_pipeline.sql:63-98
- shape: clara.document_filings(id, firm_id, document_id, client_id, filed_at, filed_by, resolution_id, basis check in ('legacy-0007','human','rule','correction','seed-0007'), retired_at/by/reason, correction_id, revision_token); create unique index uq_document_filing_active on (document_id, client_id) where retired_at is null; active→retired only, identity immutable (t_document_filings_update 0007:523-540)
- wave_b_touch: none-but-constrains: the wiki_pages index FK-ing documents should cite (document_id, firm_id) composite FK per the 0007 same-firm pattern (uq_documents_id_firm 0007:58); page provenance citations should record filing context where client-scoped. The WB-R15 tie-out doc must be FILED to the client before entries can cite it (assert_provenance requires the active filing).

### _tf_check_provenance + _active_document_filing + assert_provenance (source_doc_sha256 validation)
- location: packages/db/migrations/0007_document_pipeline.sql:965-1009
- shape: _tf_check_provenance (deferred constraint trigger on journal_entries): if new.document_id is not null → must exist documents d JOIN document_filings f on f.id=new.filing_id and f.document_id=d.id where d.sha256=new.source_doc_sha256 and firm match and f.client_id=new.client_id, else CLR02 'filing-bound provenance mismatch'. _active_document_filing(p_document uuid, p_sha256 text, p_client uuid, p_lock boolean default false) returns uuid — requires retired_at is null AND d.sha256=p_sha256 AND d.bytes_verified_at is not null, optional FOR SHARE OF f; CLR02 on miss. assert_provenance(p_document,p_sha256,p_client) wraps it. Plus ck_je_document_filing_pair check ((document_id is null)=(filing_id is null)) (0007:857-859)
- wave_b_touch: extend: the OB writer family (B4) posts WITH document provenance against the opening_balance_doc via the same assert path; the WB-R15 keyed-in FALLBACK cannot ride it (no document) — the "typed non-document provenance extension" is a new sibling lane and must respect ck_je_document_filing_pair (both null) while carrying the entering-staff attribution in typed columns, not by loosening CLR02.

### Intake writer chain (runtime-only EXECUTE) + status machine
- location: packages/db/migrations/0007_document_pipeline.sql:2780-2799 (grants), 109-111 + 573-590 (machine)
- shape: grant execute … create_document_intake(uuid,text,uuid,text,text,bigint,text,timestamptz,text), claim_document_intake_upload(uuid,text,text,int,text), mark_document_intake_received(uuid,text,text,text,text,text), begin_document_intake_verification(uuid,text), verify_document_intake(uuid,text,int,text), fail_document_intake(uuid,text,text), finalize_document_intake(uuid,text,text,jsonb,int,text,uuid,uuid,text), upgrade_legacy_document(…), claim_document_processing_task(uuid,text,boolean), persist_document_extraction(uuid,text,int,jsonb,jsonb,text,text,text), … to clara_runtime. Status: uploading→received→verifying→(verified|duplicate)→(finalized|adopted) | failed; terminal immutable CLR13; failure_code in ('too_large','bad_type','limit','checksum_mismatch','storage_error','expired','malware_detected','quarantined','internal')
- wave_b_touch: reuse as-is: prior-GL/TB documents arrive through this exact chain (WB-R15 document-primary). Wiki deterministic ingest (WB-R10) is a NEW consumer downstream of finalized documents — no intake change; B5's prior-GL parse rides the same intake + a new reader over extractions.

### Runtime intake flow: spool → detect → scan(quarantine) → canonical put → verify → DB finalize → task enqueue
- location: packages/runtime/lib/intake.mjs:260-423 (finalize), 147-153 (laneSnapshot), 282-288 (scan-before-storage)
- shape: finalizeDocumentIntake: detectDocument(path) → declared-MIME-vs-signature check (bad_type 415) → scanFile(path) [scan.mjs: clamd malware_detected; encrypted-PDF/XML-entity/ZIP-anomaly → quarantined — all BEFORE canonical Storage; spool removed on those codes, intake.mjs:415-417] → key=`firms/${meta.firmId}/docs/${meta.sha256}.${detected.ext}` (line 285) → putCanonical → verifyCanonical → claim/received/verifying/verified DB chain → finalize_document_intake → laneSnapshot enqueue: xlsx/docx/csv/tsv→'structured_parse' (clara- engine), xml→'structured_parse' (+ separate local_facts pass), else 'ocr' (azure)
- wave_b_touch: none-but-constrains: an xlsx prior-GL gets structured_parse text but NEVER a classify task (classify enqueue exists only in the pdf/image branch of the facts gate) → its document_kind stays NULL unless a human runs set_document_kind. Wave B must either widen classify enqueue to structured mimes or make the onboarding flow stamp the kind via set_document_kind — decide explicitly in the 0017 design.

### _enqueue_invoice_facts_core — the document-kind gate (0016)
- location: packages/db/migrations/0016_a21_compliance_watch.sql:3379-3488
- shape: pdf/image: kind NULL → lane='classify', engine='clara-classify-llm:v1' (re-fires facts on document.classified via facts-gate.mjs consumer, FACTS_GATE_EVENT_TYPE line 38); kind in ('invoice','credit_note','debit_note') → invoice_facts azure-di:prebuilt-invoice:2024-11-30; ANY OTHER kind (incl. opening_balance_doc / management_account / knowledge_artifact) → terminal failed task error_code='skipped_kind' receipt (attempt_count 0, idempotent); done-classify-but-NULL-kind → 'classify_low_confidence' hold (never loops). xml mime → local_facts clara-myinvois:v1 (rule-classified, no classifier wait); other mime → 'skipped_type'. Only invoice_facts consumes the page budget
- wave_b_touch: none-but-constrains (answer to the P17/WB-R15 question): prior-GL/TB uploads ride document_kind='opening_balance_doc' (or 'management_account'), and AS-BUILT nothing consumes them after OCR/structured_parse — the facts gate skips them by design. B4/B5's tie-out + seeding readers are NEW consumers over document_extractions/document_regions (or a new dated-TB read fn); do NOT widen invoice_facts. The skipped_kind receipt is the existing terminal — leave it.

### classify_document + set_document_kind (the classify lane writers)
- location: packages/db/migrations/0016_a21_compliance_watch.sql:3175-3301 (classifier), 3308+ (human)
- shape: clara.classify_document(p_document uuid, p_kind text, p_confidence numeric, p_engine_id text, p_op_key text) returns jsonb — engine must match 'clara-classify-%' ('clara-classify-human:v1' reserved/refused); doc row FOR UPDATE; verdict persisted as document_extractions row engine_kind='doc_classify' (NO regions); ≥0.8 AND no human verdict → set documents.document_kind + emit 'document.classified'; human precedence via envelope->>'source'='human'; <0.8 → open_questions(origin='classification') per active filing, NO event; consent_evidence blocked both ways CLR28; GRANT clara_runtime ONLY. set_document_kind(p_document uuid, p_kind text, p_reason text, p_op_key text) — bookkeeper+, agent identity refused CLR03, writes the human doc_classify row
- wave_b_touch: none-but-constrains: wiki deterministic ingest keyed on document_kind must tolerate kind ARRIVING LATE (classify is async) and kind CHANGING (classifier mis-stamp overwrite ≥0.8; human override any time) — the P17 projection must re-project on document.classified replay, and events must carry immutable metadata sufficient to rebuild the index without re-synthesis.

### get_document_extract — the budgeted-CTE max_chars pattern (WB-R8's idiom)
- location: packages/db/migrations/0011_daily_loop.sql:3232-3323 (live body; first shipped 0009:2613)
- shape: clara.get_document_extract(p_document uuid, p_client uuid default null, p_max_chars int default 20000) returns jsonb, STABLE SECURITY DEFINER. Budget clamp (0011:3236): v_budget int := least(greatest(coalesce(p_max_chars,20000),0),100000). THE IDIOM (0011:3283-3287): budgeted as ( select p.*, greatest(0, least(length(content), v_budget - coalesce(sum(length(content)) over(order by ord rows between unbounded preceding and 1 preceding),0)))::int as take_n from pieces p ) — pieces ordered by ord ('0:envelope' before '1:regions'); each piece emits left(content, take_n). Dual-lane auth: wake_secret GUC present → wake_context() (CLR03 on invalid; allowlist for non-interactive kinds; client-pin returns null) else _human_ctx(viewer). Result carries 'max_chars', v_budget
- wave_b_touch: adopt: the pack wiki block (≤6 pages AND ≤12 KB by relevance rank, WB-R8) reuses this exact running-sum window truncation with ord = relevance rank (first-listed-wins); budget values must be NAMED CONFIG (retunable by ADR), unlike this fn's inline literals — 0017 should surface them as a config row/fn, not copy the hardcoded 20000/100000.

### get_document_extract ACL + wake allowlist
- location: packages/db/migrations/0011_daily_loop.sql:4075-4082, 3903-3910
- shape: grant execute on … get_document_extract(uuid,uuid,int) … to clara_authenticated, clara_agent_ro; wake_fn_allowlist rows: ('autodraft','get_document_extract') (+ get_context_pack, get_draft_review, coding_lane, wake_draft_entry, wake_open_question); interactive/proactive bypass the allowlist in-body (0011:3253-3255)
- wave_b_touch: extend: new Wave B read fns (dated-TB read, wiki query) follow this exact dual-lane + allowlist pattern; any new wake kind (lint belt, wiki projection) needs explicit allowlist rows; per WB-R6 the wiki QUERY fn is a read surface, and NO gate/bound/floor/autopost fn may read wiki tables (dependency-audited).

### Extractions read surface — role grants as-built
- location: packages/db/migrations/0008_runtime_read_surface.sql:36-57 + 0007:2740-2746, 782-791
- shape: clara_runtime: GRANT SELECT on document_extractions, document_regions, clients (0008:36-38), document_intakes, document_processing_tasks, document_ingest_reservations (0008:48-50), client_identifiers, client_aliases (0007:2746) — all under using(true) policies ("RLS is NOT the tenant boundary for clara_runtime; SQL hard-scopes firm"). clara_agent_ro: SELECT on document_filings, document_extractions, document_regions (0007:2740-2741) under wake_firm() RLS policies (0007:782-791). clara_authenticated: same three under jwt_firm() RLS. All mutation via named DEFINER writers only
- wave_b_touch: extend: the wiki projection/ingest consumer (clara_runtime) can already read extractions+regions for deterministic ingest — no new grant needed there. New wiki tables need the full 0007 posture replicated: FORCE RLS + owner policy + jwt_firm()/wake_firm() read policies + explicit runtime grants; the audited wiki page writer is a new DEFINER fn family (query pure, file-back dispatches the ingest writer per P17).

### document_extractions / lane + engine_kind CHECKs (post-0016)
- location: packages/db/migrations/0007_document_pipeline.sql:183-201 (DDL); 0016:133-134, 141-154, 181-182 (widened)
- shape: document_extractions(id, firm_id, document_id, engine_id, engine_kind check in ('ocr','structured_parse','invoice_facts','doc_classify'), version_n, superseded_by (exactly-once, CLR08 trigger 0007:663-676), status in ('done','failed'), page_count, envelope jsonb, extracted_at); unique (document_id, engine_id, version_n). Tasks lane check: ('ocr','structured_parse','none','invoice_facts','local_facts','classify'); lane↔engine binding: ocr/invoice_facts→'azure-%', structured_parse/local_facts/none→'clara-%', classify→'clara-classify-%'
- wave_b_touch: none-but-constrains: if Wave B persists a typed prior-GL/TB parse it needs either a new engine_kind (CHECK swap, 0016's widening precedent) or to stay OUT of document_extractions and land parsed TB lines in a Wave-B-owned table citing the extraction row — the latter keeps the AB-3 attribution set closed. Extraction facts are immutable + supersede-once; regions append-only.

### _seed_verified_document (deterministic doc-seed precedent)
- location: packages/db/migrations/0007_document_pipeline.sql:1567-1616
- shape: clara._seed_verified_document(p_firm uuid, p_client uuid, p_sha256 text, p_filename text, p_mime text, p_bytes bigint, p_storage_path text, p_uploaded_by uuid default null, p_page_count int default 1, p_document_kind text default null, p_financial_date date default null, p_resolution uuid default null) returns jsonb — idempotent on (firm_id,sha256) conflict; mints resolution+filing (basis 'seed-0007'/'rule'), emits client.resolved/document.filed/document.ingested
- wave_b_touch: none (rig/ceremony fixture) — but it is the existing precedent for an idempotent, event-emitting document registration; Wave B's onboarding must NOT clone it for production paths (real docs ride the intake chain); useful for rig-confined fault gates.

### document.classified event registration + facts-gate consumer (the projection-consumer precedent)
- location: packages/db/migrations/0016_a21_compliance_watch.sql:424-426; packages/runtime/lib/facts-gate.mjs:6-38; packages/runtime/lib/classify.mjs:31-37
- shape: event_types += ('document.classified', true, …) with taxonomy decision 'ignore' ("the facts-gate re-enqueue is a separate registered consumer"); runtime consumer walks per-firm events, own txn per event, checkpoint-advance; CLASSIFY_CONSUMER='classify' own advisory-lock key, CLASSIFY_ENGINE_ID='clara-classify-llm:v1', no DB terminal-fail writer (stranded-requeue re-drives)
- wave_b_touch: new sibling: the WB-R3 wiki-projection consumer + the lint belt are new registered consumers on this exact pattern (taxonomy 'ignore' + named runtime consumer + per-client checkpointing, never firm-wide locks per WB-R8); P17 projection receipts + the last-projected marker vs books_version surface lag in the pack.

### hazards
- Storage custody is structurally write-once (x-upsert:false, no UPDATE/DELETE grants or policies): wiki page 'edits' MUST be new immutable versioned objects + a DB index pointer swap — any design assuming object overwrite fails at the Storage RLS layer. Orphan repair must treat put-409 (existed:true) as idempotent success.
- The key grammar is enforced in FOUR places that must move in lockstep for the wiki family: documents CHECK (N/A for wiki), intakes CHECK (N/A), runtime safeKey regex (storage.mjs:16), and the Supabase Storage RLS policy name-regex (storage-provision.sql:71/79). The Storage policy is a CEREMONY artifact (Supabase SQL editor, not a migration — local rig has no storage schema): the 0017 ceremony checklist must include the policy extension or every wiki put 403s in production while passing the rig.
- DR coverage: the daily backup mirrors the `firm-docs` bucket (write-once firm-docs-mirror prefix, rclone 1.74.4). Same-bucket wiki keys ride it for free; a separate bucket silently escapes backup until wired + re-proven (zero-501).
- Prior-GL kind reality (WB-R15): pdf/image prior-GL docs get classified (classifier CAN output opening_balance_doc/management_account) but then hit the facts gate's terminal 'skipped_kind' — text/regions exist, no typed facts. SPREADSHEET prior-GL (xlsx/csv) NEVER reaches the classify lane (classify enqueue exists only in the pdf/image mime branch of _enqueue_invoice_facts_core) — its kind stays NULL unless a human calls set_document_kind. The B4 tie-out design must pin how the tie-target doc's kind gets stamped (human attestation at upload vs widening classify to structured mimes) — silent assumption here breaks Gate K.
- document_kind is MUTABLE-late: classify is async, a ≥0.8 classifier verdict overwrites a prior classifier stamp (mis-stamp backfill path), and a human override wins any time. Deterministic wiki ingest / projection keyed on kind must re-project on replay (P17: events carry immutable metadata; rebuild the index by replay, never re-synthesis) rather than snapshotting kind at first sight.
- assert_provenance/_active_document_filing require an ACTIVE (non-retired) filing + sha match + bytes_verified_at, and ck_je_document_filing_pair forces document_id/filing_id both-null-or-both-set: the WB-R15 keyed-in fallback is a NEW typed non-document provenance lane, not a loosening of CLR02; the OB writer must file the tie-out doc to the client BEFORE posting entries that cite it. Filing retirement (wrong-client correction) also retires the tie citation path — the WB-R5 lint tie watch must handle a retired filing under the opening entries.
- consent_evidence is a protected class in BOTH directions (CLR28, 0014): wiki/seeding ingest must exclude consent-evidence documents from any synthesis or facts reading — the classifier gate already refuses, but a new Wave B reader over document_extractions bypasses classify_document and must re-assert the exclusion itself.
- Concurrency/locking: classify_document takes the documents row FOR UPDATE for the whole verdict write (both classification writers serialize); _active_document_filing FOR SHARE on the filing under posting; intake terminal states are immutable (CLR13/16) with a narrow verified→adopted race valve gated by a GUC. New Wave B writers touching documents/filings must respect this lock ordering (document row before filing) to avoid deadlocks with the classify lane.
- The budgeted-CTE idiom truncates by cumulative running sum over `ord` — order IS the budget policy. WB-R8's 'by relevance rank' means the wiki block's ord key must be the relevance rank, and budget VALUES must land as named config (the as-built fn hardcodes 20000/100000 inline — do not copy that; WB-R8 requires retunable-by-ADR named config).
- Runtime RLS posture: clara_runtime policies are using(true) — 'RLS is NOT the tenant boundary for clara_runtime; SQL hard-scopes firm' (0008 header). Every new Wave B runtime reader (wiki ingest, TB parse) must hard-scope firm_id in SQL; a forgotten predicate is a silent cross-firm read, exactly the cross-firm SECURITY DEFINER probe the WB-R17 rig gates test.
- open_questions.origin CHECK currently ends at 'manual','classification' (0016:204): B3's origin widening is a constraint SWAP — use 0014's lookup-drop+re-add idiom (constraint name found by pg_get_constraintdef ilike), never a blind drop by assumed name.
- Wake authority: any new wake kind (lint belt, wiki projection) needs explicit wake_fn_allowlist rows (0011:3903 pattern) and the taxonomy 'ignore' + registered-consumer pattern for new event types; interactive/proactive bypass is in-body — copy the get_document_extract prologue exactly (ADR-015 GUC-presence marker) or the agent lane breaks silently.

## x:dashboard

The dashboard has TWO distinct card systems Wave B extends: (1) the chat transcript catalog — a compile-time-gated registry (ClaraPart union in shared/parts.ts + PART_CATALOG in chat/partCatalog.ts + render branches in chat/parts.tsx + a parity/reachability test) where an unregistered wire type fails typecheck and an unknown runtime type renders a visible "Unsupported part:" chip; and (2) the queue surface — an envelope mapper (toReviewQueue/toQueueRow in shared/reviewTypes.ts) feeding a plain if-chain dispatch in QueueDetail.tsx whose fallback is the "no inline detail surface yet" honest-minimal panel that P18 explicitly rules unacceptable for lint findings. The ComplianceWatchCard (0016) is the complete precedent for landing a new queue row kind: union member + additive-nullable QueueRow keys + counts key + gated tile + QueueRowView title/accessory + QueueDetail branch + pure-model file + card component + governed writers with fresh op_key — but it hydrates by fragile envelope-matching (regex-parsing question_text) only because 0016 shipped no read fn, a wart the lint card should not replicate. The rules workbench (/rules) is autopost-specific; the WB-R2 tick-list ceremony's real precedents are BatchApprove (N independent per-item calls, per-row outcomes, one refusal never poisons the batch) plus the per-rule signature act signCodingRule from KbRuleProposalCard; the takeover-pack upload rides the existing documents_tab intake transport (begin→PUT→finalize→poll, 20MB cap, concurrency 2) with document_kind already a free string so `opening_balance_doc` needs no dashboard type change.

### QueueRowKind union
- location: apps/dashboard/app/shared/reviewTypes.ts:28
- shape: export type QueueRowKind = "draft" | "uncoded_filing" | "open_question" | "coding_task" | "compliance_watch";  (QueueRow.row_kind is typed `QueueRowKind | string` at :33 so unknown kinds pass through un-crashed)
- wave_b_touch: extend — add "lint_finding" (P18) and any onboarding row kind; row_kind strings flow through even un-extended, but the union drives titleFor/detail branches

### toQueueRow envelope mapper + additive-key precedent
- location: apps/dashboard/app/shared/reviewTypes.ts:93-120 (default at :96; 0016 additive keys :53-57, mapped :116-118)
- shape: row_kind: s(o.row_kind) ?? "draft", … // 0016 additive keys — a pre-0016 envelope degrades each to null: coding_kind: s(o.coding_kind), watch_id: s(o.watch_id), tier: s(o.tier)
- wave_b_touch: extend — add an additive nullable `finding_id` (and any onboarding ids) following the exact watch_id precedent so a pre-0017 envelope degrades to null; note the hazard that a MISSING row_kind defaults to "draft"

### QueueCounts + gated count tile
- location: apps/dashboard/app/shared/reviewTypes.ts:60-64,143-151 · apps/dashboard/app/queue/page.tsx:158-160
- shape: QueueCounts = { ready; needs_review; needs_you; open_drafts; open_questions; open_tasks; compliance_watches } — tile gated: {queue.counts.compliance_watches > 0 || queue.compliance.clients.length > 0 ? <Tile n=… label="compliance"/> : null} (comment: a permanent "0 compliance" tile would be noise, not news)
- wave_b_touch: extend — add a lint-findings count key (mapper defaults 0) and gate its tile the same way; pre-0017 envelopes must not render a permanent zero tile

### QueueDetail dispatch + the unknown-kind placeholder
- location: apps/dashboard/app/queue/QueueDetail.tsx:47-91 (kind branches :55-78; fallback :79-90)
- shape: if (row.row_kind === "open_question" && row.question_id) → OpenQuestionCard; "compliance_watch" && row.watch_id → matchComplianceClient(…) → ComplianceWatchCard; "draft" → DocReviewCard|DiffCard; "uncoded_filing" → LaneSummary; fallback: <p>Work this item from the documents workspace — no inline detail surface yet.</p>
- wave_b_touch: extend — add a `lint_finding` branch to a new hydration card (P18: "the queue's unknown-kind placeholder is not an acceptable surface"); NOTE this if-chain has NO registry/parity gate (unlike the chat catalog) — a 0017 row kind without a branch silently lands in the fallback; consider a queue-kind parity test mirroring partCatalog.test.tsx

### QueueRowView titleFor + per-kind accessory
- location: apps/dashboard/app/queue/QueueRowView.tsx:22-31 (default :29) and :67-71 (compliance tier band)
- shape: switch (row.row_kind) { case "open_question": …; case "compliance_watch": return row.question_text ?? "SST registration watch"; default: return `${row.row_kind} · ${shortId(row.id)}`; } — accessory precedent: {row.row_kind === "compliance_watch" ? tierBand(row.tier) chip : null}
- wave_b_touch: extend — add a lint_finding case (title from the finding text) and, if lint findings carry severity, an accessory chip per the tier-band precedent

### SECTION_ORDER + the 0017 sort-tuple candidate
- location: apps/dashboard/app/queue/model.ts:85-99 (note at :87-91) · isSelectable :106-108
- shape: export const SECTION_ORDER: QueueSectionKey[] = ["needs_you", "needs_review"]; // WA21-R14/ADR-031 … (Envelope wrinkle: needs_you-lane DRAFTS are rank 2 in the 0016 sort tuple — DB alignment is a 0017 candidate; the UI hoists them per page.) — isSelectable: row.row_kind === "draft" && !row.high_stakes && !!row.entry_id
- wave_b_touch: none-but-constrains — 0017 is the named candidate to align the DB sort tuple with the needs_you-first order (open owner ruling "queue section-order"); new row kinds must slot into the same two sections and stay non-selectable (draft-only isSelectable already excludes them)

### ComplianceWatchCard precedent — full file inventory for landing a new queue card
- location: apps/dashboard/app/shared/cards/ComplianceWatchCard.tsx:31-41 · shared/cards/complianceWatch.ts:44-68 · shared/reviewTypes.ts:71-82,152-155 · shared/reviewApi.ts:128-141 · queue/QueueDetail.tsx:58-69 · queue/page.tsx:160,162,169-171,223 · shared/cards/ComplianceWatchCard.test.tsx
- shape: Card props: { token: string | null; row: QueueRow; client: ComplianceClient | null; watchId: string; onChanged: () => void } — hydrates WITHOUT a network read (`const loader = useCallback((): Promise<ComplianceClient | null> => Promise.resolve(client), [client])`) because "no get_compliance_watch fn exists in 0016"; matched via parseServiceGroup: /\(([^)]*)\)\s*$/.exec(questionText) + matchComplianceClient(clients, {clientId, serviceGroup, tier}); writers: ack_compliance_watch(p_watch,p_rationale,p_op_key) / snooze_…(p_watch,p_until,p_rationale,p_op_key) / resolve_…(p_watch,p_conclusion,p_evidence,p_op_key)
- wave_b_touch: new sibling — the lint-finding card copies the file inventory (pure model .ts + Card .tsx + .test + writers + envelope keys + detail branch + row accessory + page tile/banner) but should ship a real `get_lint_finding(p_finding)` read fn instead of the envelope-match wart; the P18 lifecycle (open→superseded/resolved/recheck + a resolution verb) maps onto the ack/snooze/resolve writer trio pattern with fresh op_key each call

### useCard shared card lifecycle
- location: apps/dashboard/app/shared/cards/cardHooks.ts:25-73
- shape: export function useCard<T>(token: string | null, loader: (token: string) => Promise<T>): CardState<T>  — hydrate on mount, act(fn, onOk?) re-derives after EVERY action (no optimistic UI), CLR classified from the PostgREST envelope: `if (pe.clr) setClr({ code: pe.clr, reason: pe.reason ?? null })`; loader MUST be useCallback-stable over primitive ids or reload loops
- wave_b_touch: none-but-constrains — every Wave B card (lint finding, interview surfaces, tick-list rows) must ride this hook; refusals render verbatim through clr

### ClaraPart wire union (canonical, dashboard-side)
- location: apps/dashboard/app/shared/parts.ts:83-100 (header law :1-13)
- shape: export type ClaraPart = text | tool_call | tool_result | tool_error | clarify | clarify_closed | AttachmentPart | JeReviewPart | refusal | DocReviewPart | DiffPart | SweepReceiptPart | KbRuleProposalPart | OpenQuestionPart | RulePostReceiptPart  — "9 existing + 5 Wave-A + 1 Wave-A2 members"; ALL new-style parts are IDENTIFIER-ONLY (card hydrates via pinned read fn); the runtime's ClaraPart unions live in FROZEN chatTurn*.prompt.ts files — parity is pinned by fixture tests, not imports
- wave_b_touch: extend — new members for the interview cards / plan-as-document view (e.g. an identifier-only plan/interview part) land HERE; the runtime can only emit them from chatTurn_v7 (WB-R18) since the frozen v6 union cannot change; keep identifier-only + hydrate-on-mount

### PART_CATALOG registry + compile-time exhaustiveness
- location: apps/dashboard/app/chat/partCatalog.ts:25-122 (asserts :134-139) · STATUS_RESOLVER_TYPES :16
- shape: export const PART_CATALOG = { text: {renderBranch: true, fixtures:[…]}, … rule_post_receipt: {…} } satisfies Record<string, CatalogEntry>;  type AllCovered = [PartType] extends [CoveredType] ? true : ["UNCATEGORISED_PART_TYPE", Exclude<PartType, CoveredType>]; type NoExtra = [CoveredType] extends [PartType] ? true : ["CATALOG_TYPE_NOT_ON_WIRE", …]; const _covered: AllCovered = true;
- wave_b_touch: extend — each new ClaraPart member REQUIRES a catalog entry with ≥1 reachability fixture in the same change or `pnpm typecheck` fails (AllCovered), and a catalog key without a union member fails NoExtra — union + catalog + render branch must land as one atomic PR

### TranscriptParts render branches + fail-closed unknown-part chip
- location: apps/dashboard/app/chat/parts.tsx:148-251 (fallback const :128, chip :243-247) · parity gate chat/partCatalog.test.tsx:30-51
- shape: export const FALLBACK_UNSUPPORTED_PREFIX = "Unsupported part: "; — after all branches: if (isStatusResolverType(p.type)) return null; return <span className={styles.unsupportedChip}>{FALLBACK_UNSUPPORTED_PREFIX}{(p as {type?:string}).type ?? "?"}</span>; test asserts every registered type renders non-empty and NEVER hits the fallback, and an unknown type DOES
- wave_b_touch: extend — add a render branch per new part type (keyed `<Card key={`kind:${id}:${i}`} token={token ?? null} part={p} />`); a belt test naming the new types (the wave-a2 test at partCatalog.test.tsx:83-88 is the template)

### Rules workbench as-built (/rules — autopost-specific)
- location: apps/dashboard/app/rules/page.tsx:23-124 (ProposeForm :128-186) · rules/AutopostRulePanel.tsx:24-110 (run/narrow belt :36-57) · rules/model.ts:30-65
- shape: Lists AutopostRule only (listAutopostRules(p_scope)); acts: signAutopostRule(p_rule,p_op_key) admin+ / retireAutopostRule(p_rule,p_reason,p_op_key) / proposeAutopostRule(p_proposal jsonb,p_op_key); typed HTTP-200 refusal → narrowRuleWrite → thrown error-shaped CLR27 (reviewApi.ts:148-192); "Bounds are frozen once live — widening … means retiring this rule and signing a fresh successor"
- wave_b_touch: new sibling, not an in-place extension — the WB-R2 tick-list ceremony reviews a SEEDING-PROPOSAL BATCH of vendor_account rules (not autopost rules), so it is a new surface (or a distinct /rules section); each tick mints ONE real per-rule signature via the sign_coding_rule act (reviewApi.ts:97-99, the KbRuleProposalCard path at shared/cards/KbRuleProposalCard.tsx:77), NOT sign_autopost_rule; provenance/frequency metrics render from the proposal object (P10: no "@1.000" idiom, authority = the human signature alone)

### BatchApprove — the batch-ceremony interaction precedent
- location: apps/dashboard/app/queue/BatchApprove.tsx:57-78 (doctrine header :3-9)
- shape: "on confirm fires N INDIVIDUAL approve_routine_entry calls — each with the entry's own current revision token and a FRESH op_key … one refusal never poisons the batch (each call is independent)"; per-row Outcome map rendered honestly; per-row opt-out checkboxes pre-confirm
- wave_b_touch: extend/copy — the tick-list ceremony UI follows this exact pattern: per-mapping tick (opt-in rather than opt-out), N independent per-tick signature calls each with fresh op_key, per-row outcome rendering; unticked mappings stay open proposals (no bulk-commit transaction on the UI side — WB-R4's atomic set approval is the CARRY-DOWN checker path, a different surface)

### reviewApi wire-client idiom (reads + governed writers)
- location: apps/dashboard/app/shared/reviewApi.ts:15 (opKey), :23-31 (listReviewQueue), :148-167 (typed refusal), :217-219 (pgrestConfigured)
- shape: const opKey = () => crypto.randomUUID(); — every writer: await rpc("fn_name", { p_x: …, p_op_key: opKey() }, token); reads are firm-scoped DEFINER fns via rpc(); typed HTTP-200 refusals: narrowRuleWrite(out) → {status:"refused",reason} thrown as PgrestError with clr="CLR27"; governance NEVER transits the runtime (human PostgREST lane) except document bytes (fetchDocumentBytes :202-213)
- wave_b_touch: extend — all Wave B reads (get_lint_finding, plan/tick-list reads) and writers (resolve-lint verb, tick signatures, interview acts) are new fns in this file following p_* args + fresh op_key + CLR envelope; anything refusing as typed-200 must ride the narrowRuleWrite pattern so refusal never looks like success

### Intake transport (takeover-pack upload lane)
- location: apps/dashboard/app/shared/intake.ts:28 (IntakeOrigin), :99-100 (caps), :107-162 (begin/PUT/finalize) · documents/useUploadQueue.ts:19 (CONCURRENCY=2), :60-98 (runOne, 60×1s poll)
- shape: export type IntakeOrigin = "chat" | "documents_tab"; export const MAX_FILE_BYTES = 20 * 1024 * 1024; — begin POST /api/intake/documents (same-origin Next proxy, Bearer JWT) → PUT ${runtimeBase()}/api/intake/documents/:id/bytes (Bearer upload_token, streamed) → finalize POST …/finalize → poll document_intakes_visible on the JWT lane until finalized/adopted
- wave_b_touch: none-but-constrains (probably) — the WB-R15 prior-year TB / management-accounts upload rides origin "documents_tab" as-is; hazards: 20MB per-file cap and the 60s poll timeout must fit a real takeover pack, and if the design wants a distinct `takeover` origin the IntakeOrigin union + runtime route validation both change (a bigger touch — prefer riding documents_tab)

### Document filing surfaces (tie-target + document_kind)
- location: apps/dashboard/app/documents/api.ts:27 (document_kind), :48 (ClientRow), :125-127 (listClients), :157-199 (record→file two-step) · documents/page.tsx:52-60 (unassigned FIFO anti-join)
- shape: DocumentRow.document_kind: string | null (free string — no enum in the UI); ClientRow = { id: string; name: string | null; status: string }; fileToClient = record_client_resolution(p_client,p_subject_kind:'document',p_subject,p_confidence:1.0,p_method:'human',p_evidence:{source},p_op_key) → file_document(p_document,p_client,p_resolution,p_op_key)
- wave_b_touch: extend lightly — `opening_balance_doc` (WB-R15) needs NO dashboard type change (document_kind is a free string) but the onboarding/carry-down surfaces will want to badge it; WB-R1 hazard: listClients selects status already and every client dropdown (documents file-to-client :91, queue scope, rules propose) will LIST `onboarding` clients — the takeover upload MUST be able to file to them (real FKs for dry-run), so pickers should badge rather than filter non-active clients; the DB-side exclusion guard, not the UI, keeps them out of operational consumers

### Compliance envelope block (the no-read-fn hydration pattern)
- location: apps/dashboard/app/shared/reviewTypes.ts:69-82 (types), :122-135,152-155 (mappers) · shared/cards/complianceWatch.ts:1-6 (why)
- shape: export type ReviewCompliance = { stale_evaluator: boolean; clients: ComplianceClient[] }; // "an absent block degrades to {stale_evaluator:false, clients:[]}" — exists because "the card hydrates WITHOUT a network read (no get_compliance_watch fn exists in 0016)"
- wave_b_touch: constrains — 0017's design choice: either add a lint block to the list_review_queue envelope (this pattern) or ship get_lint_finding(p_finding) per-row hydration; the as-built wart (matchComplianceClient keyed on a regex over question_text) argues for the read fn; whichever is chosen, the mapper must keep the absent-block degrade so pre-0017 dashboards never crash

### hazards
- toQueueRow defaults a MISSING row_kind to "draft" (reviewTypes.ts:96) — a malformed 0017 lint row without row_kind would land in the draft detail branch (entry_id null → DiffCard branch skipped, falls to placeholder, but it would also render as a draft-titled row); 0017's queue fn must always emit row_kind
- The queue detail if-chain has NO parity/registry gate — unlike the chat catalog (compile-time AllCovered + parity test), adding a 0017 row kind without a QueueDetail branch silently produces the honest-minimal placeholder P18 forbids; add a queue-kind parity test (mirror partCatalog.test.tsx) or the lint card can regress unnoticed
- ClaraPart exhaustiveness is a two-way compile gate (partCatalog.ts:134-139): the new interview/plan part member, its PART_CATALOG entry (with fixture), and its TranscriptParts branch must land in ONE change or typecheck fails either direction; and the runtime can only EMIT the new part from chatTurn_v7 — the frozen v6 prompt files cannot be edited (Appendix A), so dashboard-first landing renders nothing until v25 ships (additive-but-dark is fine; plan for it)
- Do not replicate the compliance hydration wart: matchComplianceClient keys on client_id + a regex parse of question_text ('/\(([^)]*)\)\s*$/') because 0016 shipped no get_compliance_watch — P18's lint card should get a real get_lint_finding read fn and an additive nullable finding_id on QueueRow (the watch_id precedent, reviewTypes.ts:53-57), so a pre-0017 envelope degrades to null instead of crashing
- SECTION_ORDER needs_you-first is a UI hoist over a mismatched 0016 DB sort tuple — model.ts:90 names DB alignment 'a 0017 candidate' and the owner's queue section-order ruling is still open; 0017 should close both together or the per-page hoist stays load-bearing
- WB-R1 UI leak: listClients already selects status and every client picker (documents file-to-client, queue scope, rules propose) will list `onboarding` clients — the exclusion guard is DB-side by design, and the takeover upload MUST target onboarding clients (real FKs for dry-run), so pickers must badge, not filter; but check no dashboard surface lets an onboarding client look operationally live (e.g. appearing in queue scope with zero rows is honest; appearing in the rules propose form is arguably not)
- Counts-tile noise precedent: page.tsx:158-160 gates the compliance tile on nonzero — a lint tile must be gated identically, and the QueueCounts mapper must default the new key to 0 for pre-0017 envelopes
- Batch semantics differ by ceremony: the tick-list (WB-R2) follows BatchApprove's N-independent-calls-with-fresh-op_key pattern (one refusal never poisons the batch), while carry-down set approval (WB-R4) is ONE serializable DB transaction signed once — the dashboard must not blur them (no 'approve all' single call for ticks; no per-item calls for the carry-down set)
- Typed HTTP-200 refusals: any new Wave B writer that refuses as a typed return (the 0016 ADV-R2#4 pattern) must ride narrowRuleWrite/ruleWriteRefusedError (reviewApi.ts:148-167) or a refusal fires onChanged() and reads as success
- Upload ceiling: MAX_FILE_BYTES = 20MB, CONCURRENCY = 2, and a 60×1s adoption poll (useUploadQueue.ts:78-92) bound the takeover pack — a large prior-GL export or scanned TB bundle that exceeds these needs an owner ruling or chunked delivery, and the intake poll timeout produces a scary-but-false 'Timed out' on slow scans
- All new cards must keep the useCard discipline (cardHooks.ts): identifier-only part → hydrate on mount → act() re-derives after EVERY action, refusals render verbatim from the CLR envelope — no optimistic UI anywhere in the tick-list or interview surfaces
- The compliance banner + stale_evaluator badge (queue/page.tsx:162,169-171) are per-snapshot from the SAME envelope as the rows — the lint 'exactly-once notification' (P18) is a DB/notification concern, not a dashboard one; the dashboard only ever re-renders snapshot state (never synthesizes a notification)
