# The extraction slice — CLOSE receipts (2026-07-28)

Contract v1.0 (ADR-047). Everything below is live-verified, not asserted. Deployment:
**23 migrations (`0023_extraction_slice_x5`) · runtime v32** — two quiesced ceremonies
(0022 solo; 0023 combined with the v32 image), both §0-guard-proven, all postverifies
green (0023: 7/7; 0022 re-run head-conditional at 0023: 7/7), manifest 80/80
deploy-locked (#121).

## The gates

| gate | state |
|---|---|
| **XG1** (amended) | **CLOSED** — `request_reextraction('509e788d')` proved the verb + supersede chain twice (v6, v7): `total_excl_tax 435,560.40` emitted (typed-collapsed — the corpus's first agreed fact), `tax_total` correctly ABSENT (the printed dash), rounding correctly REFUSED (`sign_unknown`), `vendor_registration` emitted with the registry-matching key (`2024010477561593602x` = `registration_normalized`, the resolver's own join = true). Gate P's SST 3-leg close retargeted by owner ruling to the first genuinely SST-charging real bill |
| **XG2** (amended) | **CLOSED** — the component tie rig-proven EXACT on the LAI LOU MEI figures (94.30+3.77+5.66+0.02=103.75); live unsigned-rounding documents refuse to the human lane by design (as-built law (iv)) |
| **XG3** | **OPEN → the runway program** — the vendor blocker is SOLVED on live evidence; the Tier-A blocker is structurally open but **zero live documents are genuinely two-reader yet** (the honest zero-flip). The first qualifying document closes it — task #14/#15 |
| **XG4** | **CLOSED** — 29 pre-existing extractions byte-stable through every deploy; the XML/structured branch and supplier floor byte-untouched, postverify-pinned |
| **XG5** | **CLOSED** — anchor outcomes changed ONLY at 0023: the dark guard held through the v30/v31 deploys; before/after: head 0022 corr 0/51 → head 0023 corr 0/51 with the RULE replaced and `X4 DARK GUARD` verified absent from live prosrc |

## What the slice actually changed on live

`request_reextraction` (bookkeeper, 3 production calls, 22-doc yield sweep) ·
`set_firm_high_stakes_threshold` (owner) · the corrected component sales tie · the
X2 totals reader (v6) + X6 vendor-identity reader (v7), both through 3-round Codex
ladders · corroboration = **explicit two-reader agreement** (confidence GONE; typed-only
self-consistency REFUSED — the K1 wrong-money path) · the anchor lane open on the
corrected identity with `customer_unresolved`/`floor_lost` resurfaced · negative
net/tax refused at the write boundary · interview_v2 aboard the same image (own
receipts: PR #120).

The autonomous lane's honest posture: **authority granted to a population of size
zero today** — it opens only to documents that print arithmetic two independent
readers confirm. That is the designed boundary, not a defect; the receipt to watch on
the first qualifying document is `envelope.totals_reader.fields.<path>.outcome`.
