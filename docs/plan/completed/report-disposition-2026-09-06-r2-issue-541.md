# Report disposition record — R2-cb-ae2e-541

The per-item disposition of GitHub issue #541, the authenticated production e2e audit (CB-AE2E-001…036), as measured **2026-09-06 on `main` at `fc39c361`** by a
read-only opus-5 lane under 裁-190. The owner's decisions are recorded in `PROGRESS.md`; this is a
CLOSED record, filed verbatim so the measurement can be re-read against the tree it was taken on.
**ONE CLAUSE BELOW WAS OVERTAKEN BY A LATER READ: CB-AE2E-005's "the 10-minute log window was never
measured" describes the state BEFORE the ceremony lane took it** — 2026-09-05, `fly logs -a
clara-runtime`, machine `48ee715b763048`, a 48 m 30 s window (17:08:15Z→17:56:45Z), 100 lines, zero
matches for the retired function, reported to the lead at 17:56:52Z and now carried in `PROGRESS.md`.
Both statements are true of their own moment. The verbatim block is byte-identical to the source and
is not edited; this sentence is the supersession note.

<!-- begin verbatim: R2-cb-ae2e-541.md · md5 afa67957290d332e5a06a853d603d8a3 -->
# R2 — disposition of GitHub issue #541 (CB-AE2E-001…036)

**HEAD:** `fc39c361` (confirmed) · **Written:** `Sun Sep 6 01:44 MPST 2026` (`date`, host clock)
**Serving baseline assumed by every FIXED row:** runtime **v75** (`registry.fly.io/clara-runtime:v75-gate-0351f022`,
image id `img_wd57v5d3lej9p38o`), web Worker **`90c1a5d0`** at 100 %, DB frontier **171 / `0176`** — all three
from the 2026-09-05 ceremony as-run. #560 landed after the web build and is e2e-only, so it changes no serving byte.

**Method.** Every FIXED/PARTIAL row was read on `main` at `fc39c361`, not inferred from a PR title. Where I could
not find a thing, the row says where I looked. Two clauses I could NOT verify are called out in §(c).

---

| id | P | 标题 (zh, short) | Disposition | Evidence (PR + file:line / as-run / ruling) | Acceptance clause met? | What remains / restated form | Queue | 推荐 |
|---|---|---|---|---|---|---|---|---|
| CB-AE2E-001 | P0 | Beta Terms 没有独立接受证据 | **OWNER-ACT** | Nothing landed. `apps/web/components/entry/signup-dpa-form.tsx:54-60` states it in its own header: the terms text exists at `docs/ops/legal/clara-beta-terms.md` but `clara.dpa_documents` has **no `kind` column**, so a Terms signature has nowhere to live. No `kind` in `packages/db/migrations/0158_checkout_gate_c1_dpa.sql` on the documents table. | **No.** No second receipt, no two-receipt checkout gate. | The XL migration (kind on documents+signatures, re-keyed currency/uniqueness, kind-carrying `sign_dpa`, both checkout consumers) plus the owner sitting that decides whether checkout requires BOTH kinds and whether a Terms body with 27 lawyer markers may be seeded. | **Q-09** | 先开 sitting，别先写迁移 — 两个问题没答，迁移会白写。 |
| CB-AE2E-002 | P0 | DPA 仍是 placeholder | **OWNER-ACT** | `docs/ops/legal/clara-beta-dpa.md:6,39,64,121` — v1 is still "a conspicuous placeholder" by the document's own words. Handover row **H-36**. No deploy-time placeholder detector found anywhere in `.github/workflows/ci.yml`. | **No.** Neither half: no final bytes, no pipeline token check. | Legal owner freezes final bytes → hash pins → add the placeholder/`[verify]` release-gate cell. The gate cell is ~0.2 units and can land BEFORE the bytes. | unqueued (裁-125 lawyer pass) | 律师那关是上市前的事；**但那个「检测到 placeholder 就 fail deploy」的闸门现在就能建**，先建它。 |
| CB-AE2E-003 | P0 | Stripe sandbox 没有 runtime hard gate | **FIXED** | #544. `apps/web/lib/checkout/stripe-session.ts:126-144` `stripeKeyClassRefusal` (refuses in BOTH directions; unset mode = refusal); called at **`:381`, before the `doFetch` at `:392`** and before the timer; startup arm `reportStripeKeyClassAtStartup` at `:476-489`, self-invoked `:491`. | **Yes — "live key refused before any network call".** Read the ordering myself: gate at 381, network at 392. Startup arm LOGS, does not throw, and the file says why (Workers populates `process.env` per request). | The audit also asked for a **deploy preflight that calls Stripe** and a **build manifest recording mode** — neither exists. `/api/build-info` reports web sha + runtime_url only. | unqueued | 核心那道墙已经好了。剩下两条是「锦上添花」，可以放到上市前那批。 |
| CB-AE2E-004 | P0 | Opening approval 生产不可能完成且静默失败 | **FIXED** | Two halves, both landed and both **applied live**. DB: `packages/db/migrations/0171_opening_approval_isolation_pin.sql:98-101` pins `default_transaction_isolation='serializable'` on both approval bodies, with a prestate assert (`:72`) and a tail census (`:110-114`); as-run part 1 line **153** records `0171` applied at 16:13:05Z. Web: `apps/web/lib/parts/single-fire-guard.ts:48-58` — `runOnce` now returns `{ran, value}`; `apps/web/components/clara/OnboardingDoorDialog.tsx:142-144` closes only on `closeOnConfirmedOk(outcome)`. | **Yes on both named clauses.** "Dialog stays open on refusal": proven by a **derived** census in `apps/web/components/common/door-dialog-outcome.test.tsx:238-266` that walks the tree for importers of the guard rather than a hand list. "proconfig serializable": the tail census in `0171` fails the migration unless 2 of 2 bodies carry it. | The end-to-end clause — **"a clean client finalizes entirely through the UI, no DB bridge"** — has not been walked since the deploy. The mechanism is there; the journey is unproven. | unqueued (walk only) | 建议下次开个干净 client 走一遍 UI 到 finalize，**证明**它通，而不是推断它通。 |
| CB-AE2E-005 | P1 | Runtime v74 仍调用已删除 autopost function | **FIXED** | v75 built from `0351f022` and released **by image id** (as-run §5.2, `img_wd57v5d3lej9p38o`) — an identity proof, not a tag match. Source census on `main`: `reconcile_autopost_rules` survives in `packages/runtime/lib/reconciler.mjs:505,518` and `leader.mjs:53` **as comments only**, plus a regression guard at `packages/runtime/tests/reconcile-belt-isolation-unit.test.mjs:380` that fails if a caller is re-added. No executable call. | **NOT MEASURED.** The clause is "≥10 min without the retired-function log". I grepped both as-run files for `logs` / `fly logs` / `autopost` / `does not exist` — **zero hits**. §5.3 records `fly status`, `/ready`, `/api/build-info`, a smoke walk. **No log window was ever read.** | Take the 10-minute log read now. It costs one command and it is the only thing standing between "the code is right" and "the storm stopped" — an absence claim needs a measured window (the register's own words). | unqueued | 现在就去读 10 分钟日志。这是**唯一**一条只差一个命令就能收掉的 P0/P1。 |
| CB-AE2E-006 | P1 | OTP 缺可发现路径，resend 是 dead end | **PARTIAL** | Landed (#544): `apps/web/components/entry/signup-account-form.tsx:324-328` renders a primary `<Link href="/auth/confirm">`, and it renders on **both** arms (fresh + duplicate) so the screen stays a non-oracle (`:312-322`). NOT landed: `:328-334` says in its own comment that `lib/registration/confirmation-resend.ts` "unconditionally answers `unavailable` and no /resend route exists"; the copy was retuned to point at `/login` instead of promising a send. No `apps/web/app/api/auth-wall/` directory exists. | **Half.** "New user needs no hand-typed URL" — yes. **"expired → resend → new OTP succeeds" — no**, the resend was never built. "No enumeration" — held by the flattened duplicate arm. | Build `POST /api/auth-wall/resend` over the existing `claim/settle_confirmation_attempt` pair, sharing the signup rate wall, then widen `showResend`. One open question the register flags: does a resend spend the SAME rate-wall budget as a wrong guess? | unqueued | Resend 是新用户最容易卡死的地方（code 过期就只能重注册）。建议把它排进下一批 web lane。 |
| CB-AE2E-007 | P1 | DPA signed state 重登后不 hydrate | **PARTIAL** | DB half landed and applied: `packages/db/migrations/0174_web_reads_and_small_doors.sql:206` creates `clara.get_own_dpa_signature()`, `:224-225` revokes public and grants `clara_authenticated`. **Web half NOT wired** — I grepped `apps/web` for `get_own_dpa_signature` and `alreadySigned`: **no hits**. The only signed-state read is `apps/web/lib/registration/dpa-doors.ts:88-97`, which learns "already signed" only from the door's own `replay` flag AFTER a submit. #552's body confirms it was a DB-only PR. | **No.** A re-login still asks for consent and only then answers "already signed" — exactly the reported behaviour. | One server-side loader call + an `alreadySigned` prop into `SignupDpaForm`, seeding its render. Small (≈0.3). One open question: a signature against a SUPERSEDED version — re-sign, or pass? | unqueued | 门已经开好了，就差 web 那一根线。这条最便宜（半天不到），建议先做。 |
| CB-AE2E-008 | P1 | 结构化答案渲染 `[object Object]` | **FIXED** | #546. `apps/web/lib/onboarding/answer-format.ts` is the typed formatter (per-`item_key` table + ordered `key: value` fallback that is never `JSON.stringify` — `:377`); consumed by `apps/web/lib/interview/thread.ts:5` and `apps/web/lib/onboarding/resolution-history.ts:25`, so the checklist AND the thread share one presenter. | **Yes.** "Unit + browser test forbid `[object Object]`": `answer-format.test.ts:45,106`, `interview/thread.test.ts:41`, `resolution-history.test.ts:46` all assert `doesNotMatch(/\[object Object\]/)`. | Nothing. SSM 新旧号的字段拆分 rides the per-item table. | — | 收掉。 |
| CB-AE2E-009 | P1 | Interview facts 不 promotion 到 canonical truth | **OPEN-VALID** | Nothing landed. Grepped every migration for `project_onboarding_captures`: **not found in `packages/db/migrations/`**. `commit_client_onboarding` still only activates the client (register anchor `0017_wave_b.sql:2824`). This is handover **H-21** and PROGRESS calls it "the P1 row that alone unblocks four separate symptoms". | **No.** Entity/MSIC still `—`; SSM/TIN/bank re-typed. | New `clara.project_onboarding_captures(client, plan, op_key)` called by commit, mapping ssm/tin → `add_client_identifier`, entity/MSIC → `record_client_fact` with `basis_kind='interview_carryover'`. Open: the interview captures an FY **month**, the door needs month+day. | **Q-01** | 这条是四个症状的**同一个根**（重复输入 / Bank 空 / Knowledge 空 / CLR23）。排第一位是对的。 |
| CB-AE2E-010 | P1 | Typed consent Grant + Activate 没有产品 surface | **OBSOLETE (reshaped)** | **裁-186** (ledger `docs/plan/active/mohe-grill-rulings-2026-09-04-pm.md:25-62`, ADR-0078 decision 1) replaces the per-client Consent Center with **one firm-level declaration signed at the DPA stage**; the onboarding commit's successor door auto-mints each client's consent and activates every purpose, citing it. The READ half landed: `0174` ships `clara.client_egress_state(uuid)`. No `apps/web` caller for any egress verb (grepped `client_egress_state` / `grant_client_egress` / `activate_client_egress` — zero hits). | **Superseded.** "Grant alone still blocks / Activate opens exact purpose / revoke blocks" survive as mechanism; the per-client evidence rung is **relaxed by ruling**. | Build 裁-186's shape: a declaration row on the DPA page, an admissible evidence kind, the commit-time auto-mint, and a compliance register showing each client's consent state + evidence kind. | **Q-02** | 注意 owner 自己 ruled 的时候**逆了建议**，dissent 已存档一次。上市前律师那关很可能把它翻回来 — 先按 (b) 建，但把 evidence kind 记清楚，以后能升级。 |
| CB-AE2E-011 | P1 | Bank statement end-to-end 仍不可完成 | **PARTIAL** | Landed: the hand-key path — `apps/web/lib/bank/doors.ts:77-87` now carries `institution_code` + `account_number` **as a required pair** (H-06), so `enter_bank_statement` can succeed at all; #545 shipped `statementFacts_v3` (period basis + witness→roster normalisation + terminal settle); `0175` shipped `_stmt_institution_code`. NOT landed: the AI path is broken by a **NEW P0 the ceremony measured** — the classify lane settles 1.4–5.8 s BEFORE its OCR extraction persists, so the classifier reads an empty string (PROGRESS.md Backlog, "CLASSIFY/OCR ORDERING RACE"; as-run part 1 §5.1a). | **No.** "One statement all the way through UI to recon" is not reachable. Maybank=MBB / Alliance=ALB mapping now has a resolver but no real corpus proof. | Fix the ordering race (gate classify on extraction `done`, with a cell that plants a classify task ahead of its extraction and asserts it WAITS); then the de-sensitised Maybank/Alliance corpus; then stage diagnostics. | unqueued (the race is a new P0) | 这条现在的瓶颈**不是 UI，是那个 race**。修 race 之前做 corpus 是白做。 |
| CB-AE2E-012 | P1 | Sales lane 是 DB-only 开关，CLR23 无恢复 | **PARTIAL** | Landed: `autoDraft_v10` (`packages/runtime/workflows/autoDraft.v10.errors.ts`) replaces the substring 23505 mask with an exact constraint-name map, so an alias collision no longer surfaces as an unanswerable CLR23; `0176` kind-scopes the alias unique and mints the owner-floored `set_firm_sales_lane_activation` wrapper (`0176:65-87,210`). NOT landed: **no web surface at all** — I grepped `apps/web` case-insensitively for `saleslane` / `sales_lane`: **zero hits**. | **Half.** "Ambiguous → CLR23 → resolve → one draft, idempotent" is not user-reachable; the masked-refusal half is fixed. | The owner status/activation panel over the new wrapper + `firm_sales_lane_visible`, and the requeue-once lane (**F-02**, 裁-195, ≈0.5). | **F-02** + unqueued UI | 后端已经干净了，缺的是那块面板。建议和 F-02 一起做，一趟走完。 |
| CB-AE2E-013 | P1 | Bookkeeper 自建自批 routine journal | **OBSOLETE** | **裁-187** (ledger `:64-122`, ADR-0078 decision 2, permanent): every attestation ceremony and **every maker-checker wall is abolished**; RBAC is the only human gate, and bookkeeper "approves and posts ANY amount, own drafts included". Ruled AGAINST the recommendation, dissent filed once. The web side already complies: #548 collapsed to one Approve (`apps/web/components/journals/drafts-queue-panel.tsx:360-379`) and #550 **removed the Change-threshold control entirely** (grepped `apps/web` for `set_firm_high_stakes` / "Change threshold": no component hits). | **Reversed.** The audit wanted a typed `same_actor` refusal; the ruling forbids one. | The DB is now **stricter than the ruling**: `_approve_entry_core`'s high-stakes segregation rungs (`0037_wave_c_a_subledger.sql:1992`) still stand, and no `017x` migration removes them (grepped). The wall-removal lane is 裁-188's. | **Q-03** | 提醒一句：现在 **UI 已经没有 threshold 开关，但 DB 的墙还在** — 超过门槛的分录还是会被拒，而用户已经没有地方改门槛了。Q-03 要么快做，要么这中间是个死角。 |
| CB-AE2E-014 | P1 | Bookkeeper 能看到 Owner-only Admin controls | **FIXED** | #550. `apps/web/lib/firm/capabilities.ts` is one capability object whose floors are **parsed out of the migration they cite** — `apps/web/lib/firm/capabilities.test.ts:84-92` fails if a claimed floor disagrees with the migration line. Consumed by `apps/web/components/admin/members-panel.tsx:129-136` (fail-closed on an unreadable read) and threaded to the row menu at `:186` (`canManageMembers`). Nav: `apps/web/lib/firm/navigation.ts:2` imports it. | **Yes on the DOM clause** for the two named surfaces (threshold control deleted outright; member role/remove gated). "Direct URL typed 403" and "Owner unaffected" are asserted by the capability tests, not by a browser walk. | A DOM/AX-tree sweep of the remaining admin surfaces would close it fully. The DB wall is untouched, as required. | unqueued | 收掉主体。要更稳的话，等 Q-07 那个 browser gate 上了，加一格 bookkeeper 的 DOM 断言。 |
| CB-AE2E-015 | P1 | Close 的 Clara 权限 refusal 文案错误 | **OPEN-VALID** | Still true on `main`. `packages/runtime/workflows/chatTurn.v15.freeform.ts:130` — `FREEFORM_ORACLE_MESSAGE = readToolRefusalMessage({ code: "42501" })`, i.e. **every** oracle-safe refusal renders the 42501 session-authority sentence. No `chatTurn.v18.*` exists (`ls packages/runtime/workflows` shows v16 and v17 only), and workflow bodies are immutable (constraint 9), so the fix must be a new frozen closure. | **No.** An owner with an active client still gets "an active bookkeeper-or-higher firm session is required". | `chatTurn_v18` defining the oracle string locally (honest about the class, never naming the refused table), plus a migration granting + policying the descriptive close tables so the read stops being refused in the first place. | **Q-04** | 这条最伤信任 — Clara **对着 owner 说他不是 owner**。文案那半其实很小，可以先单独出 v18 收掉。 |
| CB-AE2E-016 | P1 | Close abandon 后缺 restart/recovery | **FIXED** | #549. `apps/web/components/close/CloseDoors.tsx:91-95` — `canBeginClose` now admits `absent` **or** `abandoned` **or** `finalized`, mirroring `_begin_close_core`'s own predicate rather than inventing a client rule; `:103-104` `isRestartOfAbandonedClose` labels the trigger **Restart** only for the abandoned case (a finalized-then-reopened year is not a virgin restart, and the file says so at `:100-102`). Begin-dialog copy now states the write freeze; gate tokens get a label map. | **Yes** for `not_started / in_progress / abandoned / finalized` each having one primary CTA that survives a refresh (state comes from `get_close_plan`, not local state). | The audit also named a `blocked` state; today "blocked" is rendered as gate rows on the in-progress run rather than as its own state. Cosmetic against the ask. | — | 收掉。 |
| CB-AE2E-017 | P1 | Reports 没有任何模板或 artifact | **OPEN-VALID** | Nothing landed. No `publish_house_style_version` re-cut in `016*`/`017*` (grepped), and `packages/db/seeds/` holds only `0001_smoke_seed.sql` and `0002_core_seed.sql`. The structural blocker stands: `publish_house_style_version` requires every manifest value to be a flat sha256 string while the renderer reads `fonts` as a nested array, so **a manifest satisfying one refuses the other**. | **No.** `report_templates` = 0 remains the production fact. | Migration re-cutting the validator to accept the nested `fonts` array (distinct refusal token), then a data act publishing style → template → chart template → spec through the real audited doors. Statutory stays behind the verified-wording gate. | **Q-06** | 这是 beta 里唯一「完全没有闭环」的功能域。建议先发一个 **management** 模板 — statutory 的措辞还没过律师，别碰。 |
| CB-AE2E-018 | P1 | Activity 看不到真实审计事件 | **PARTIAL** | DB landed and applied: `0174` ships `clara.firm_timeline_visible` + `clara.list_firm_timeline(bigint,int)` and repoints the `f_a4` receipt shim (#552). Web landed **on Firm Home only**: `apps/web/components/firm/firm-home/firm-timeline-section.tsx` reads it and tells the three failure arms apart (not-deployed vs refused vs failed). **The `/activity` page itself was NOT rewired** — `apps/web/app/(firm)/activity/page.tsx:27-35` still renders a `NotBuiltNote` for the timeline, with a comment saying the read "is a DATABASE gap". That comment is now **stale**: the read exists. | **Partly.** Acts are visible on Home; the page a person goes to for history still says not-built. Whether journal approve/reverse, member invite/accept and opening finalize each appear with actor/time/object/result is not proven — the view drops the payload wholesale and prints `event_types.description`. | Point `/activity` at `list_firm_timeline`, delete the not-built note and its stale copy string, and prove the three named act families actually emit rows. Four of the nine receipt shims are still typed-empty stubs. | unqueued | **一个尴尬的现状**：Home 有时间线，Activity 页面还写着「没建」。这条建议紧接着 Q-01 做，一天以内。 |
| CB-AE2E-019 | P1 | Authenticated shell 窄窗/200% zoom 不可用 | **FIXED** | #553. `apps/web/app/(firm)/layout.tsx:114` — the firm rail is `hidden … lg:flex`; `:144-145` renders `<FirmNavDrawer />` in an `lg:hidden` header; the Clara rail becomes an overlay below the breakpoint. | **Yes, and proven in a real browser.** `apps/web/e2e/responsive-shell-walk.spec.ts` walks **640×720 (= 1280×720 at 200 %, and the file explains why that equivalence is exact, `:23-31`)**: workbench ≥ **320 CSS px** (`:60,116,265`), no page-level horizontal scroll (`:51-52,119`), drawer opens **and closes by keyboard with focus returned to the toggle** (`:143-196`), Escape closes the overlay rail (`:285-303`). | Nothing on the named clause. AXE was **not** run in the deploy ceremony (as-run: the MCP browser has none) — a11y beyond this walk rests on each train's own Playwright leg. | — | 这条做得最扎实 — 三条验收全部有浏览器证据。收掉。 |
| CB-AE2E-020 | P2 | Opening "Ties" 语义误导 | **FIXED** | #549. `apps/web/components/registers/opening-dryrun-strip.tsx:53-63` — `openingTieGates` renders **four** named gates in the assert's own order: `targetsPresent` (`targets.length > 0`), `allMapped` (`unmapped_labels.length === 0`), `allTie`, `obeNil` (`obe_net_cents === 0`), each carrying the DB's own reason token (`tie_mismatch` / `obe_not_nil`, `:46-49`). | **Yes.** The three gates the audit asked to be split out are split, and named with the DB's words rather than the UI's. | Nothing. | — | 收掉。 |
| CB-AE2E-021 | P2 | Approved / Posted / routine 用词不一致 | **FIXED** | #548. `apps/web/components/journals/drafts-queue-panel.tsx:360-379` — ONE primary Approve, routed by `row.high_stakes` to the guarded or the attesting door, with the DB left as arbiter (a stale flag gets a verbatim CLR05, then a reload). `:414-430` records that the second "Approve (routine)" button **is gone** and why; both doors stay exported and tested for a future batch surface. Posted legend + badge rename landed with it. | **Yes** on "one primary approval action" and "routine is a policy detail, not a duplicate button". | Nothing named. | — | 收掉。 |
| CB-AE2E-022 | P2 | 内部函数/migration/roadmap 文案暴露 | **PARTIAL** | The four **named** leaks are gone: `grep -c` over `apps/web/messages/en.json` for `record_client_resolution` / `draft_entry` / `clara_set_firm_high_stakes_threshold` / `firm-altitude` returns **0**; `altitude` returns **0**; no `0NNN_migration` tokens remain. A message-key lint now ships (`apps/web/scripts/check-message-keys.mjs`, wired into `apps/web/package.json:13`). | **Partly.** The reported strings are clean; the class is not closed. | **35 strings still name a `clara.*` verb**, and two still carry session-internal tokens (`en.json:214` cites 裁-80; `:280` cites `PROGRESS.md`). **Two of those strings are now factually WRONG** (see §c): the Activity note says the build "does not call `clara.list_firm_timeline`" (Home does), and the chat note says `chat_sessions has no archived_at column` (`0174` added it). | **Q-05** | 文案 sweep 排最后是对的，**但那两条已经说谎的字符串该单独提早修** — 用户会照着它做判断。 |
| CB-AE2E-023 | P2 | committed 后仍显示 Commit/Cancel | **FIXED** | #546. `apps/web/components/clara/OnboardingChecklistCard.tsx:96-99` adds a third **`settled`** variant for a plan that is committed or cancelled — a receipt with the item list collapsed and no door that could only refuse; `:122-131` explains why the settled arm cannot and does not compute the commit gate. | **Yes.** Mutable controls hidden on a server-reported committed status; receipt shown. | Nothing named. | — | 收掉。 |
| CB-AE2E-024 | P2 | Clients list 没有 Add Client | **FIXED** | #546. `apps/web/components/firm/client-register-list.tsx:68` (the labelled fix), `:102` resolves `beginClientOnboarding` through `findDoAction`, `:119-121` renders the primary trigger. `:107` fails **closed with a warning** when the authority read is unreadable, rather than rendering a control that can only refuse. | **Yes.** The control is on the register, reusing the palette path, so the rail is no longer the only entry. | Nothing named. | — | 收掉。 |
| CB-AE2E-025 | P2 | Members 信息架构与日期不一致 | **FIXED** | #550. The duplicate heading is gone — `apps/web/messages/en.json:324` is now `"Everyone with access"`, and `apps/web/components/admin/members-dates.test.tsx:114` pins the rendered pair as exactly `["Everyone with access", "Pending invites"]`. Dates go through the shared business-date formatter rather than an ISO string slice, with the test file named for the defect. | **Yes** on both halves (heading pairing; no more previous-day UTC slice at 00:00–08:00 MYT). | The audit also asked that mail and UI share one locale/timezone formatter; only the UI half is proven here. | — | 收掉。 |
| CB-AE2E-026 | P2 | Needs you 使用内部 marker | **FIXED** | #550. `apps/web/components/firm/needs-you-row.tsx` now renders all four: WHAT (`:107`, client name merged from the register, falling back to a short id — never a guessed name), WHY (`:110-111` `reviewQueueWhyKey` + `whyChips`), WHEN (`:114-115` `aged_since` as an instant **and** a plain age), and the period under a per-kind label (`:119`) instead of one bare word meaning three dates. | **Yes.** "发生了什么 / 为什么需要你 / 下一步 / 截止时间" all present from columns the RPC already shipped. | The "Sweep runs" definition sentence — I did not verify its placement. | — | 收掉。 |
| CB-AE2E-027 | P2 | Reports recipient 显示 raw UUID | **FIXED** | #549. `apps/web/lib/members/use-member-names.ts` resolves a user id to `display_name` once per firm scope; `apps/web/components/reports/ExportRecipientsPanel.tsx:19,91` consumes it and the free-text uuid input became a **picker**. `:84-91` states the deliberate gap honestly: the roster read floors at viewer while the write floors at admin+, so a viewer sees an empty picker and the DB is still the wall. | **Yes.** Display name/email, uuid demoted. | Nothing named. | — | 收掉。 |
| CB-AE2E-028 | P2 | Close hold 显示 raw user UUID | **FIXED** | #549, same mechanism. `apps/web/components/close/ClosePrepHoldPanel.tsx` is in the `useMemberNames` consumer set (grepped: it, `firm-activity-feed.tsx`, `ArtifactRow.tsx`, `StatutoryReportsPanel.tsx`, `ExportRecipientsPanel.tsx`, plus the shared `common/member-name.tsx`). Fallback is a shortened raw id, never a guessed name. | **Yes.** | Nothing named. | — | 收掉。 |
| CB-AE2E-029 | P2 | Registers 空数据 vacuous tie | **FIXED** | #549. `apps/web/components/registers/fa-register-tie-banner.tsx:27` `notEvaluated = data.accounts.length === 0` — three-valued off **a fact the DB itself returned** (`:64`: "the DB's own report that it compared nothing"); `:69,72` paint it neutral with a `notEvaluated` message instead of a green pass; `:78` suppresses the difference columns in that arm. Test: `fa-register-tie.test.tsx:13`. | **Yes.** `0=0` now reads "not evaluated", never a green tie. | The DB still asserts `v_tie = true` from an empty walk (`0041:4260`); the honesty is the UI's. The open question — should the DB return the third state — is unanswered. | — | 收掉。UI 已经诚实；DB 那半是以后的事。 |
| CB-AE2E-030 | P2 | Knowledge 在 onboarding 后仍为空 | **OPEN-VALID** | The surface exists (`apps/web/app/(firm)/clients/[clientId]/knowledge/page.tsx`, `apps/web/components/registers/knowledge-panel.tsx`) but has nothing to show, because **CB-009's projection never landed** — no `project_onboarding_captures` anywhere in the migrations. Same root cause, downstream symptom. | **No.** No source, verified status, version or last-confirmed-by, because there are no canonical facts to carry them. | Blocked on Q-01. Once captures project, the panel needs the provenance columns rendered (and must not copy interview prose). | **Q-01** (blocked) | 别单独排它 — Q-01 一做完，这条自己就活了。 |
| CB-AE2E-031 | P3 | 品牌大小写不一致 | **WRONG** | Not reproduced. `apps/web/components/entry/brand-lockup.tsx:35-39` — **裁-137** (owner, 2026-09-02) ruled the case question: §8's "lowercase" governs the **wordmark glyphs**, R1's "ClaraBook" governs **prose**; the visual lowercase is a `text-transform` with the DOM text still `ClaraBook`. The register's own census found all 51 lowercase hits to be filenames, hostnames or repo names. | n/a — no defect. | Optional only: convert the twelve prose strings that hardcode the literal brand to ICU interpolation of `Brand.productName`, replacing an enumerated roster with a no-literal rule. The census covered `apps/web` only, **not the hosted mail templates** — if the tester saw it in an email, that surface is unmeasured. | — | 不用修。但值得问一句 auditor：**他到底在哪一屏看到的？** 如果是邮件，那块我们没量过。 |
| CB-AE2E-032 | P3 | Tax 页面把路线图当产品 | **FIXED** | #557. `apps/web/components/tax/TaxWorkbenchPage.tsx:17-25` records the removal: the three cards previously named lane ids, three migration numbers, an owner-ruling id and two raw SQL signatures; they are now written "about the product rather than as a citation of the lane that is paused". `:64` adds a rank-gated live control (the turnover classification) beside a non-gated watch. | **Yes.** No internal build plan on a beta user's screen; the never-files promise survives as product behaviour. | **F-01**: `clara.sst_threshold_schedule` has no `clara_authenticated` grant, so the new control cannot offer the statutory thresholds (裁-196 d). | **F-01** | 收掉主体。F-01 是个小 grant，顺手做。 |
| CB-AE2E-033 | P3 | Admin section 对非 Admin 名称误导 | **FIXED** | #550. `apps/web/lib/firm/navigation.ts:156` — the `admin` entry's message key swaps to `firm` when the caller does not administer, so a bookkeeper sees the "Firm" label over the reads they are allowed, exactly the rename the audit asked for. Route floor stays at `viewer` (`:76`) because the read half is legitimately theirs; the write floor is `admin` (`:83`). | **Yes** — capability-based rename, with the roster/compliance reads still reachable. | Nothing named. | — | 收掉。 |
| CB-AE2E-034 | P3 | Bank reconciliation empty state 重复 | **FIXED** | #549. `apps/web/components/bank/read-state.tsx:44,60,97` — an optional `emptyCopy` prop defaulting to the old shared string, so each of the eight distinct absences now carries its own key under its own section namespace instead of one six-word sentence. | **Yes** on per-absence copy; the duplicated stacked sentence in the reconciliation card collapses with it. | Nothing named. | — | 收掉。 |
| CB-AE2E-035 | P3 | Release provenance 不可唯一反查 | **FIXED** | #558 + #552. Runtime: `packages/runtime/src/buildInfoRoutes.ts:37-52` serves `GET /api/build-info`, session-gated, over `lib/build-info.mjs` + `clara.build_frontier()` (`0174`). Web: `apps/web/app/api/build-info/route.ts:41-45` returns its own baked `git_sha` (null when unresolved) and `runtime_url`. **Proven live** in as-run §5.3: `git_sha 0351f022…`, `image_ref registry.fly.io/clara-runtime:v75-gate-0351f022`, `machine_id`, 11 workflow names, `frontier {171, 0176_counterparty_alias_kind_scope}`. | **Yes.** Web SHA, runtime image ref and migration frontier are all reachable and were read from production. | The audit also listed classifier/interview/report **template versions** — not in the payload. Note the classifier engine id is pinned at `:v1` in three places and cannot move without a DB lane (**F-04**). | **F-04** (id bump) | 收掉。「靠 Fly v74 猜源码」这个时代结束了。 |
| CB-AE2E-036 | P3 | Browser E2E 不是 required CI gate | **OPEN-VALID (ruled in)** | Still true: `grep -n "playwright\|web-e2e\|smoke" .github/workflows/ci.yml` returns **one** hit, a comment at line 5. No Playwright job exists. But the ASK is now **ruled law** — **裁-192** (ledger `:168-189`) amends 裁-86 and makes one required `web-e2e-smoke` job the floor, with the per-train acceptance walk kept beside it. | **No.** The gate does not exist. | 裁-192 attaches a precondition: **two known flakes in `apps/web/e2e/checkout-gate-walk.spec.ts` are fixed at their cause first** (the axe contrast arm sampling a transitioning button; the `reachDpaStep` navigation race), and the fixing lane **measures before it edits**. PROGRESS also carries three harness rules the gate must adopt, including the **selector-luck** class found 2026-09-05. | **Q-07** | 别急着开闸 — **带着已知 flake 的必过闸门会卡住每一个 PR**。先修那两个 flake，这是 owner 自己 ruled 的顺序。 |

---

## (a) Counts

**By disposition (36 total):**

| disposition | n |
|---|---:|
| FIXED | 20 |
| PARTIAL | 6 |
| OPEN-VALID | 5 |
| OBSOLETE | 2 |
| OWNER-ACT | 2 |
| WRONG | 1 |
| STALE | 0 |
| DRIFT | 0 |

**By severity × disposition:**

| | FIXED | PARTIAL | OPEN-VALID | OBSOLETE | OWNER-ACT | WRONG | total |
|---|---:|---:|---:|---:|---:|---:|---:|
| **P0** | 2 | 0 | 0 | 0 | 2 | 0 | **4** |
| **P1** | 5 | 5 | 3 | 2 | 0 | 0 | **15** |
| **P2** | 9 | 1 | 1 | 0 | 0 | 0 | **11** |
| **P3** | 4 | 0 | 1 | 0 | 0 | 1 | **6** |
| **total** | **20** | **6** | **5** | **2** | **2** | **1** | **36** |

FIXED items: 003 · 004 · 005 · 008 · 014 · 016 · 019 · 020 · 021 · 023 · 024 · 025 · 026 · 027 · 028 · 029 · 032 · 033 · 034 · 035.
PARTIAL: 006 · 007 · 011 · 012 · 018 · 022. OPEN-VALID: 009 · 015 · 017 · 030 · 036. OBSOLETE: 010 · 013.
OWNER-ACT: 001 · 002. WRONG: 031.

## (b) §8 / §9 verdict — what of the Beta Exit Pack is now satisfiable

**Satisfiable today, 6 of 16:** OPEN-01 (the `0171` pin is live and the dialog holds on refusal) · MEMBER-01 (passed in the audit and untouched since) · ROLE-01 (the capability object hides the controls; the DB wall stands) · AUTO-01 (v75 serves the clean source — **but the 10-minute log read was never taken**, so the evidence line is unwritten) · A11Y-01 (proven in a browser at 640 CSS px; **AXE was not run**) · OPS-01 (`/api/build-info` returned sha + image ref + frontier from production).
**Not satisfiable, 8:** ENTRY-01 (no second legal receipt) · ENTRY-02 (no resend) · ONB-01 (no captures projection) · DOC-01 (the classify/OCR race) · SALES-01 (no surface, no requeue) · RPT-01 (zero templates) · SEC-01 (no cross-tenant suite was ever built or run) · CLOSE-01 (restart landed, but the close-prep chat still misreports the reason).
**Two pack items are now unsatisfiable BY RULING and must be re-cut before they can be graded:** JOURNAL-01's "a different checker approves" contradicts 裁-187, and CONSENT-01's per-client evidence chain contradicts 裁-186.
**§9 exit gate: still NO.** "0 open P0, 0 open P1" is false on both counts — two P0s are the owner's (Terms, DPA bytes), and the ceremony **minted two new P0s** (the classify/OCR ordering race; #558's recall harness cannot run inside the image it ships in). Eight P1-class register rows remain unclosed.

## (c) Items I was NOT sure about, and why

1. **CB-005's core clause.** The code is unambiguously clean and the image identity is proven by Fly image id. But I searched both as-run files for `logs`, `fly logs`, `autopost`, `does not exist` and found **nothing** — no log window was read. I report the fix as landed and the acceptance clause as **unmeasured**, not as met. Absence of the log in the source is not evidence about the running process.
2. **CB-018's completeness.** `list_firm_timeline` exists and Home renders it, but I did not verify that journal approve/reverse, member invite/accept and opening finalize each actually **emit** `domain_events` rows with a description. The view drops the payload wholesale, so "actor/time/object/result" depends entirely on `event_types.description` being populated for those act families. Unverified.
3. **Two message strings that are now false.** `apps/web/messages/en.json:214` and the Activity/chat notes assert absences that `0174` filled (`list_firm_timeline` is called by Home; `chat_sessions.archived_at` exists). This is copy DRIFT inside an item I graded PARTIAL (CB-022) rather than its own row — flagging it because a user reading it is told the product lacks something it has.
4. **CB-013's residual.** I could not find any `017x` migration removing the high-stakes segregation rungs, so the DB wall appears to outlive the UI control that configured it. If a firm's threshold is non-zero today, an above-threshold approval will still refuse with no way to change the threshold. I did not read live `firms.high_stakes_amount_cents` to see whether any firm is actually exposed.
5. **CB-031's blind spot.** The brand census covered `apps/web` only. If the auditor saw lowercase branding in a **hosted mail template**, that surface was never measured and the WRONG verdict would not cover it.

## (d) 三个最重要的 owner 决定（大白话）

**第一，两个 P0 现在只等你，不等工程。** Terms（服务条款）和 DPA 的最终文本，全世界只有你和律师能定。工程这边把闸门都建好了，但**签的是什么字，没人替你决定**。这两条不结，#541 这个 umbrella issue 按它自己的规矩就永远不能关。建议：先开 CB-001 那个 sitting（要回答两个问题：checkout 是不是必须两份都签？Terms 那 27 个律师待填项能不能先种下去），律师那关可以晚一点。

**第二，这份审计的验收清单，有两条已经被你自己的 ruling 作废了，得重写。** 审计要求「分录必须换一个人批」（JOURNAL-01）和「每个客户单独签授权书」（CONSENT-01）；但 裁-187 废掉了所有 maker-checker，裁-186 改成了 firm 层一次性声明。**照原清单永远过不了，因为产品已经不是那个产品了。** 请拍板：是把这两条按新规则重写，还是回头认为当时那两个 ruling 走远了。顺带一提，两次 ruling 你都是**逆着建议**选的，dissent 都存了档 — 律师那关很可能会把 裁-186 翻回来。

**第三，下一步先做哪个。** 现在有三件事在抢同一批人：**(甲)** Q-01 客户访谈资料入库 —— 一修就同时治好四个症状（重复输入、Bank 空、Knowledge 空、CLR23），性价比最高；**(乙)** 那个新发现的 classify/OCR 抢跑 P0 —— 不修，银行对账单和 AI 认单这条路整条是死的，而且 #558 那次改错了地方（改的是提示词，真正的原因是**分类跑在 OCR 前面**）；**(丙)** Q-06 发第一个报表模板 —— Reports 现在是唯一一个**完全没有闭环**的功能域。我的建议顺序是 **乙 → 甲 → 丙**：先把跑错的那条修好（它已经骗过我们一次），再做最省力的那条，报表放最后，因为它的前置卡点是一个 validator 和 renderer 互相打架的老问题，要单独一段时间。
<!-- end verbatim: R2-cb-ae2e-541.md -->
