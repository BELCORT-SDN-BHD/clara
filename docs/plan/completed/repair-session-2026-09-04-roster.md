# Repair session 2026-09-04 — lane roster (closed)

The orchestrator's dated lane roster for the 2026-09-04→06 repair session — merged PRs, the
serial merge chain, rulings 裁-191…201 as first recorded, follow-up rows surfaced by lanes and
reviews, and the ceremony log — filed here byte-verbatim from the session scratchpad
(`lane-roster.md`). **A CLOSED record.** The rulings of record are
[`mohe-grill-rulings-2026-09-04-pm.md`](../active/mohe-grill-rulings-2026-09-04-pm.md) and
[`README-rulings-2026-09.md`](../../adr/README-rulings-2026-09.md), which govern on any
divergence from what is stated below; `PROGRESS.md` is the state authority for what shipped.

<!-- begin verbatim: lane-roster.md · md5 126dd5cc3fa46d5363097d85e3f27e58 -->
# Lane roster — repair session 2026-09-04 (orchestrator's tracker; PROGRESS.md is the authority at clock-out)

**11:20 MYT: every running agent hit the account session limit (resets 13:40 MYT); the owner ran /login at ≈11:22;
all nine cut agents were sent RESUME messages at ≈11:26 (resume-from-transcript; each told to re-read its worktree
before trusting its last command).** review-546 deliberately NOT resumed until L2 pushes its correction.

## Merged
| PR | branch | what |
|---|---|---|
| #543 `dbaf9056` | docs/rulings-186-190-2026-09-04 | 裁-186…190 + ADR-0078 + truings (review-543: 3 majors folded, verified) |
| #544 `e9dbf094` | web/entry-checkout-fixes-541 | L1: confirm link, pending copy, password policy, Stripe key-class gate, customer_email (review-544: blocker enumeration oracle folded, verified) |

**SECOND session-limit cut at ≈12:58 MYT (reset 15:10); all eleven cut agents resumed ≈15:52 with the same re-read-your-tree instruction.** #548 MERGED `144f0a8f` (journals table) at ≈15:52. #554 (docs 裁-191/192) folded by the orchestrator itself after review-554 (law 85 inline amended-by, "BECOMES … flakes first, not built yet", corrected flake coordinates, tense on the 12/8 count) — `c213ab93`, re-verify requested. Remaining chain: #546 (R1/R2 push pending) → #547 (r2 push pending) → #549 (merge-time items 4–7) → #550 → #553 → #555 → #551/#552 (number at merge) → #554 whenever verified (docs, no conflict).

**#554 MERGED `0f2f44de` (docs 裁-191/192) at ≈16:05.** Lesson (memory-worthy): with the "head must be up to date with base" rule, a docs merge in the MIDDLE of a code PR's CI cycle forces that PR to re-update and re-run — merge docs PRs only between code cycles, or accept the extra cycle. #546 re-updated at ≈16:06 (head after #554); new wait armed.

**16:10 state:** #547 r2 VERIFIED, R5 (portal Escape containment) sent → merge after #546 · #555 (documents, `ff60959e`) reviewed: MIME gate + report-only CSP CLEAR; 2 majors (overlay canvas pinned to inline px so polygons drift on scrollbar/resize → delete `pdf-page-render.ts:76-77`; pdfjs SSR chunk contradicts the "never in the server graph" comment → `ssr:false` wrapper) + 8 nits; the lane's tree was mid-merge with main (UU on the two shared e2e files) — resolution: ONE derived lane-mock roster (#548's shape) with the documents mock added. review-555's merge correction: `proxy.ts` does NOT conflict with #553; the real conflict is #549 (six documents files — keep both: #549's dialog plumbing wins, #555's `diagnostic` prop survives).

**#546 MERGED `aa5fef4a` (onboarding) at ≈16:20.** Main now: #543 · #544 · #545 · #548 · #554 · #546. #547 (`cef11861` = R5) updated from main, CI armed, review-547 verifying R5 → merge next → then signal L7 for #549's merge-time push.

**#547 MERGED `d1b12f9b` (chat) at ≈16:46.** Main: #543 · #544 · #545 · #548 · #554 · #546 · #547. L7 signalled for #549's merge-time push (items 4–7 + nits + merge-forward sweep) → then review-549 re-verify → merge. Then #550 → #553 → #555 → #557 → #551/#552 (number) → #556 → L9.

**17:00 state:** #553 VERIFIED (`5d3ae95e`; 5 nits ride its merge-time push) · #555 VERIFIED (`217b48b8`; the N1 assertion held locally at `91e1f92c`, rides its merge-time push with the carve-out deletion + the #549 keep-both) · #549 merge-time push IN PROGRESS (L7) · #550 merge-forward IN PROGRESS (L5) · #551 repairs + 裁-193 re-cut IN PROGRESS (DB-A) · #552 fold r2 + sweep IN PROGRESS (DB-B) · review-556 / review-557 RUNNING · L9 building. Merge order after #549: #550 → #553 → #555 → #557 → #551 · #552 (number) → #556 → L9.

**17:15 state (serial mode — no new BUILD lane until the open PRs land; reviews/folds ARE landing work):** #549 `a9511216` CI green but DIRTY vs main (L7's merge-time push running) · #550 `af883696` BLOCKED (awaits review-550 re-verify, after #549) · #552 FOLDED `2149fde9` (CI 9/9 + fresh-rig sweep 4031/3937/0) → review-552 re-verify SENT · #556 `f9957672` review-556 CLEAR WITH NITS → fold order SENT to L8 (op_key guard + replay cell; LAND the firm_limits lane-fields read beside the door; cite 0062; two retry cells; body re-cut); finding 7 (requeue scope) requested in full · #557 `643255a7` review-557 running (addendum sent) · #558 L9 `78714074` CI GREEN (meta-gate) → review-558 DISPATCHED (opus xhigh) while L9 finishes its report · #551 `a975c1e4` CI RED on db-estate/db-live-gates (DB-A repairing; rig `dba7-1c17ae6d` up) · #553/#555 verified, holding for signal.
- **Owner questions accumulated (bring at the checkpoint, not one by one):** (i) 裁-149 clause 2's premise was WRONG as written (L9: the leader session DOES have an error listener + reconnect loop at `relay.mjs:139-153`, `lib/leader.mjs:177-188`; the correction is in ARCHITECTURE §4.3 — review-558 verifying); (ii) L8's requeue-once: after two failures the registry is `parked` and admission refuses unconditionally — is answering the coder's question the human act that un-parks? L8 recommends NO re-mint on the dismissal arm; (iii) L8's H-19 wrapper carries `p_reason` beyond the order's three args (the inner verb requires a non-blank reason; the alternative was inventing one into an append-only audit log) — lead accepts, owner may overrule; (iv) `sst_threshold_schedule` grant gap (L16); (v) L9: should any NON-runtime lane fail `/ready` rather than WARN (a dead write pool means no coding act can succeed; L9 warns because 503-ing the runtime for a lane whose ceremony has not run is what the lazy-pool posture prevents) — lead recommends WARN stays for beta; (vi) L9: should production REFUSE a DSN carrying `sslmode=no-verify` rather than warn — lead recommends REFUSE (fail closed), consistent with `docs/ops/dsn-bridge.md`'s standing "never no-verify" law; the cost is a machine that refuses to boot on a misconfigured DSN, which is the point.
- **17:22:** L9 confirms `/api/build-info` needs NO code change when #552 lands (`clara_runtime` call, 42883 → `frontier: null` + reason today; cells cover both arms + the SQL text). #558 touches four shared `apps/web` files (new `app/api/build-info/route.ts`; one line each in `lib/require-firm-scope.ts`, `next.config.ts`, `test/manifest.txt`; `tests/firm-scope-surfaces.test.ts` entrance count 3→4) → merges LAST in the web chain as planned. L15 (#553): R1–R5 committed locally `714f55f3`, push HELD for the signal — R1 was worse than the nit (the fixture was a live Tailwind candidate and MINTED the forbidden bare side-translate into the production sheet; fixed by assembling the string; the guard's allowlist omitted `=` so it was VACUOUS — mutant R1b added, now red); R2 deleted the EXEMPT machinery entirely (zero offenders); a third announcer-class site from #548 fixed + censused (three total). Verify: 2430 pass, e2e 85 passed/8 skipped.
- **17:20:** #552 r2 `3451247a` (five-arg header, commit trail, barrier re-stamped; CI + sweep running) → review-552 re-pointed · #557 review-557 CHANGES REQUIRED (BLOCKER heading order on the onboarding board — card above the moved h1, no axe on that arm; MAJOR fy_end_source paired with the client's standing pair; MAJOR 裁-44 input-grid guard deleted; 4–5 pending) → fold batch 1 SENT to L16 · #558 L9 complete: engine-id `:v2` bump NOT shipped (would strand every classify task: `enqueue_document_processing` hard-codes `:v1` at `0123:1128`, `classify_document` exact-match at `0016:3225-3228`, `0102:447` byte-pins — needs a DB lane + in-flight transition posture → DB-D row); `/api/build-info` degrades to `frontier: null` until #552's `build_frontier` exists.
- **17:25:** review-552 CLEAR WITH NITS on `3451247a` (comment-only SQL delta from the swept `2149fde9`; both barrier hashes recomputed and match; CI run 33857065505 pending on db-estate/db-live-gates — re-read the green before merge). It flags a CROSS-PR NUMBERING HAZARD with #551 — text requested; numbering HELD until it lands.
- **17:30 NUMBERING PLAN (review-552, measured):** the only collision is a BARRIER-MAP key — #551's dba4 has two bare `execute v_next;` (lines 120, 208; `sqlOracle.ts:433-441`'s unresolved-dynamic-SQL shape) and #551 touches no `apps/web` file, so numbered dba4 reds `main` via `viewDefinitions` in ANY order → #551 must register dba4 in `firm-scope-db-pins.test.ts` (reason ≥40 chars + content sha256) before merge; same check on the 裁-193 `apply_coa_template` CoR (not yet pushed at `a975c1e4`). No shared object, no sort dependency, no absolute census moved (#551 mints no role/view). ORDER: **#551 first → 0165…0172** (dba1…dba8) → **#552 → 0173 web-reads, 0174 statement** (D1 file last) → **#556 → 0175** (counterparty alias kind) — re-sort barrier-map keys at each renumber; `rig-meta.mjs` + `x42-s5-helpers.mjs` are keep-both merge surfaces for whoever is second. Whoever numbers re-runs the estate suite ONCE on the numbered chain (裁-108; CI skips unnumbered).
- **17:33:** #550 `af883696` MERGE-FORWARD DONE by L5 — CI run 33855854857 9/9 + meta-gate SUCCESS; manifest sorted union 274→273 (the union re-added `threshold-money-door.test.tsx`, which the branch DELETES under 裁-187 — dropped by hand; the manifest gate checks only disk→manifest, so a blind union ships a red test run: LESSON); MERGE-FORWARD LAW all clean (en.json 0 value mismatches). Awaiting review-550's re-verify → then MERGE #550 (may precede #549; L7 told to re-merge main before its push). Barrier map at #552's renumber = THREE keys (dba4 0168, web-reads 0173, statement 0174).
- **17:38 #550 MERGED `c21f26c3`** (review-550 CLEAR: all 11 findings fixed at the cause; the lead independently verified the merged `en.json` — 12 main keys absent from the merged file are ALL #550's own deletions at merge-base `d1b12f9b`, ZERO dropped carriers). Main: #543 · #544 · #545 · #548 · #554 · #546 · #547 · #550. L7 told to merge `c21f26c3` before its #549 push. review-557's full list delivered (BLOCKER heading order; MAJOR 3 vacuous-relaxation of the 裁-44 guard with a false comment; MAJOR 4 raw DB role on the front page; MAJOR 5 the uncompilable merge note; drifted journals fixture; N1–N9) → batch 2 SENT to L16 incl. the real `clientName` threading after merge-forward; N7 decided: gate the classification CONTROL at bookkeeper+ mirroring the door's floor. Remaining order: #549 → #553 → #555 → #557 → #551 (0165–0172) → #552 (0173/0174) → #556 (0175) → #558.
- **17:45 review-558 CLEAR WITH NITS** on `78714074`: 裁-149 clause 2's premise CONFIRMED wrong (leader listener + reconnect at `relay.mjs:139-153`, `leader.mjs:177-188`; failover holds via `client.end()` releasing the session advisory lock; doc §4.3 accurate; non-swallow pinned by a cell) → OWNER must be told at the checkpoint; build-info fail-soft → #558 may merge before #552; freeze clean; parity fingerprints byte-verified; pg-pool emits `'error'` only after `_remove` so the listener over-swallows nothing. MAJOR-1 (real): the lane probe is a THIRD sequential 5 s `bounded` stage in `/ready` (`health.mjs:343` after `:99`/`:286`) inside fly's 5 s timeout — a black-holed lane makes the check time out instead of warning → fold SENT to L9 (probe off the request path or one overall budget + worst-case latency cell); MAJOR-2/3 + nits requested.
- **17:48 #558 full fold SENT to L9.** Lead-owned carry-outs: (a) `docs/adr/README-rulings-2026-09.md:24` row 90 still says the leader "stays crash-loud" + clause 4 asks for an unwritable mutant → one-line ERRATUM citing ARCHITECTURE §4.3 in the clock-out docs PR, and the owner told his clause-2 premise was wrong when he ruled; (b) **RUNTIME DEPLOY GATE:** #558's classify calibration text pushes documents to ≥0.85 across `classify_document`'s ≥0.8 auto-accept gate with NO before/after → before the runtime image ships, run `scripts/measure-classify-recall.mjs` in `live`/`replay` against the REAL corpus, prompt sha recorded, result in the ceremony receipt; (c) `SCOPE_ENTRANCES.length` pinned 4 by #558 (main 3) — #558 lands before any sibling adding an entrance (none today); (d) the TLS boot assert is bypassable BY DESIGN when no DSN pins a CA (`tls-ca.mjs:194` warns) — narrower than the order's "refuse if absent", argued at `:26-35` (an unconditional assert bricks the image-first deploy); pairs with owner question (vi).
- **20:25 RESUMED after the THIRD session-limit cut of the day (≈17:35→20:10 reset; six lanes cut: L7, L16, L8, L9, DB-A, DB-B; all resumed with git-status-first).** DB-A status: `a975c1e4` pushed; 3 local CI repairs (dba4 barrier entry DONE, sha `c6f3b99a…`, 320-char reason, 22/22; dba3 + codeability fixtures on 0038's `MBB`, drift guard 21/21); 8 rig copies already deleted; the 15-fail run was a REUSED db (not comparable) — fresh main-chain control rig8 = 3 host pg_dump fails only. NEW **dba9** approved (`apply_coa_template` open-plan refusal, static body) → **#551 = 9 files → 0165–0173; #552 → 0174 web-reads / 0175 statement; #556 → 0176.** #552 CI on `3451247a` = 9/9 + meta-gate GREEN (run 33857065505).
- **20:35 #549 merge-time push DONE `808daaee`** (MERGEABLE, up to date with `c21f26c3`; 150 files; typecheck 0, test 2563/2563, e2e 75 passed; sweep clean with positive controls; CI run 33872628119 IN PROGRESS) → review-549 re-verify SENT. Follow-up row: **the table primitive's keyboard-scroll fix (item 6, `components/ui/table.tsx` tabIndex) has NO browser coverage** — mutant P removed it and the full leg stayed green (no walk face renders a scrolling read-only table; axe applies `scrollable-region-focusable` only to a region that scrolls) → needs mock verbs + a walk arm: route to the CI-E2E-192 lane. L7 also caught a merge regression: main's 裁-187 cell re-opened the commit dialog after a refusal (wrong under CB-AE2E-004) — re-cut, mutant N.
- **20:45 #549 review-549 CLEAR at `808daaee`** (nothing to fold; CI run 33872628119 watched by monitor b5ld8hnzc → merge on green). **#558 fold pushed `5224a932`** — the lane probe moved OFF the request path (background interval via `storage-probe.mjs`'s machinery, synchronous cached read, cold = `pending`), `POOL_ROLES` minted, replay REFUSES contaminated fixtures, TLS-roster superset cell → review-558 re-verify SENT.
- **20:55 #552 `2a9ec1fc`** (comment fold `a1a86353` + main merge): #550's `receipt-kinds.test.ts` gate (a SECOND file-scanning gate — greps every migration for each shim's definition) went RED on the merged tree and was RIGHT: #552 repoints f_a4 so `agent_act` is WIRED → the Activity page's honest-coverage copy moves 4/9 → 5/9 arms (would have understated). review-552 final re-verify SENT (count enumerated against the live roster). LESSON: every file-scanning gate on main is a merge surface for an UNNUMBERED migration — the list is now THREE (firm-scope-db-pins barrier map, runtime `bank_institutions` drift guard, receipt-kinds shim census). #558 fold r2 SENT (wedged-cycle warning, cycle/interval/overlap cells, inFlight nit, DSN-leak payload assertion restored e2e).
- **21:00 #549 MERGED `90b59cc1`** (CI 33872628119 9/9 + meta-gate, read from the job list; review-549 CLEAR). Main: #543 · #544 · #545 · #548 · #554 · #546 · #547 · #550 · #549 (NINE). Signals SENT: L15 (#553 merge-forward + push) and L13 (#555 merge-forward + carve-out deletion + push); L16 told to merge-forward from `90b59cc1` (census 15). Checkpoint artifact republished 20:45 (label "Checkpoint 20:45"). Remaining order: #553 → #555 → #557 → #551 (0165–0173) → #552 (0174/0175) → #556 (0176) → #558.
- **21:05 #551 pushed `8381b42b`** (9 migrations; 裁-193 = dba6 re-cut committed-only + NEW dba9 wall with zero `execute`; a THIRD CI cause was the lane's own new §4.11 dr-verify cell reporting a difference of VINTAGE as drift on CI's unnumbered-skipping chain → migration-STEM witness with STRICT semantics, `checkCanary`'s shape; fresh rig 4032/3938/0; D1 = `apply_coa_template` + `set_document_kind` + `_gate_outstanding_items`; MERGE-TIME: re-key the dba4 barrier entry to its numbered name) → review-551 final re-verify SENT. LESSON (witness-gate class on a dr-verify probe): a cell that pins an object on the DR target must witness the migration STEM first, else CI's unnumbered-skipping chain reads vintage as drift.
- **21:12 review-551 CLEAR WITH TWO NITS on `8381b42b`** (every claim read; CI run 33874926715 in progress) → nit fold SENT to DB-A (stale "FAILS not SKIPS" comment + body l.179; a dba9 behavioural fixture + `dba.9c` cancelled-plan cell). Lead's read of the nit fold at merge suffices (comment + one cell). Product tickets proposed to the owner 21:00 (await a word): provisional streaming text in the rail (≈0.7), real readers for the 9 ids-only part kinds (≈1), chatTurn tool expansion for documents/fixed assets/depreciation/recurring/close/onboarding (≈1.5).
- **21:20 #558 r2 pushed `78403870`** (stalled-wedge warning; four knob cells; inFlight fix; e2e DSN-leak assertion through the REAL prober; §4.3 handover line) → review-558 final re-verify SENT. #556: CI red is in `db-estate` (job 101021516256); the cell is being grepped from the job log (bg b7njflvpf); L8 silent after two pings — if the grep names the cell and L8 stays silent, resume L8 with the cell named. Renumber tool ready: `scratchpad/renumber-migrations.mjs <worktree> <first> <order>` (renames + rewrites basename refs; content untouched so barrier hashes hold; refuses number collisions). Merge-time recipe for a DB PR: merge main into the branch → run the tool → `pnpm --filter @clara/web test` (barrier keys re-sorted) → one estate run on the numbered chain (rig) → commit "db: claim NNNN–NNNN at merge" → push → CI → merge.
- **21:35 #556 CI red DIAGNOSED by the lead from the job log** (L8 silent to two pings → resumed with the cell named): `packages/db/tests/debt-human-read-surfaces.test.mjs:271` debt-BAR1 pins a closed-world list of FOURTEEN naming `firm_sales_lane_visible` unconditionally; CI's chain skips the UNNUMBERED file → the view is absent → red. Witness-gate class (裁-108). Fix: witness the cohort (`to_regclass`), thirteen otherwise; #552 edits the SAME cell (witness-gated, passed CI) → fifteen after both, keep-both at L8's merge-forward.
- **21:40:** #553 merge-forward pushed `b5ab8b39` (2601 pass; e2e 95; a REAL defect fixed beyond the merge: ⌘K palette labels drifted from the 裁-187 "Firm" rename — palette now consumes `visibleFirmNavigation`'s output; three cells) → review-553 re-verify SENT. #555 merge-forward pushed `c951c1fb` (2654 pass; e2e 83; carve-out deleted; TWO real mock defects fixed: verb-per-line census; `readJson` CONSUMES the stream so a parse-then-fall-through starves later lanes → parse only inside a matched verb + hook ordered ahead of L7's) → review-555 re-verify SENT. **Follow-up row (CI-E2E-192 lane):** `bank-close-registers-mock.mjs:204-246` still consumes the body for foreign verbs — house rule needed (parse-after-match, or a body-cloning `readJson`). review-558 CLEAR on `78403870` with ONE MINOR (stall clock keyed on loop start → false "not settled in 14400000ms" after one blown cycle) → r3 fold SENT to L9. Merge order next: #553 (after review-553 + CI) → #555 (`update-branch`, CI) → #557 → #551 → #552 → #556 → #558.
- **21:48 review-553 CLEAR on `b5ab8b39`** (R1–R5 confirmed against the rebuilt sheet; union census 14 real; the palette fix correct at the cause, no rank leak). Nit → L17 copy-sweep row: `app/(firm)/clients/[clientId]/layout.tsx` comment says "7 of them client-altitude", the merged tree has 9. Merge #553 on the meta-gate (monitor b5g60s097).
- **21:52 review-555 CLEAR on `c951c1fb`** (N1 discriminating; carve-out deletion NOT vacuous — real table containers scanned; three keep-both merges with both carriers; the hand-spliced ownership test parses and fires five cells). Hook ORDER judged a latent trap (positional) — the structural fix (memoised `readJson` on the request + a census cell enforcing parse-after-match) is now items (iii)/(iv) in `order-CI-E2E-192.md` (with the table-primitive browser arm). Merge #555 on the meta-gate after #553 (monitor b5g60s097 covers both runs).
- **21:58 #556 fold done `24286a37`, CI GREEN** (L8 had found the debt-BAR1 witness cause itself; three-state proof; numbered-chain estate 4035/3941/0; the retry cells use `toAutoDraftOutcome` — the review's `parseAutoDraftOutcome` was a misnaming; zero l8 rigs remain) → review-556 re-verify SENT. At L8's merge-forward after #552: keep-both on `debt-human-read-surfaces.test.mjs:271` (two witnesses → 15).
- **22:05 #551 nit fold `bb58c5eb` READ BY THE LEAD** (comment trued: never skips, fails on every negative path but the vintage arm; dba9 tail walks absent/open/cancelled/committed and rolls back; `dba.9c` drives the real door: open → `onboarding_plan_open`, cancelled → admitted, committed outranks) — #551 READY for renumber. **ORDER CHANGE (lead):** #557's fold is the longest, and the DB PRs touch no web UI file (#551: `firm-scope-db-pins.test.ts`; #552: `receipt-kinds` copy) → after #553/#555 land, merge **#551 → #552 → #556 → #558**, then #557 LAST (it merge-forwards over them cheaply). Renumber recipe: in the lane's clean worktree, merge origin/main → `node scratchpad/renumber-migrations.mjs <wt> 165 '^UNNUMBERED_dba(\d+)_'` → `pnpm --filter @clara/web test` → commit "db: claim 0165–0173 at merge (#551)" → push → CI (db-estate now APPLIES the numbered files = the numbered-chain proof) → merge.
- **22:12 #553 MERGED `80e42bf7`** (CI 9/9 from the job list). Main = TEN PRs. #555 `gh pr update-branch` → `429313c1`, CI running (monitor armed) → merge on green. Renumber tool DRY-RUN PASSED on a node-copied scratch of #551's tree: dba1…dba9 → `0165_document_kind_codeability` … `0173_apply_coa_template_refuses_open_plan`; the only basename reference rewritten is the dba4 barrier key in `firm-scope-db-pins.test.ts` (DB cohorts key on stems/`to_regprocedure`, unaffected).
- **22:18 review-556 CLEAR on `24286a37`** (guard + replay cell pins the audit-row count; the read is the house pattern — `ak.28` pins exactly four lane fields, `ak.26` cross-firm blindness on a POPULATED table, PUBLIC false; debt-BAR1 a true three-state pin; CI run 33875905029 success). #556 READY; L8 is pre-reading #552's version of the debt-BAR1 cell for the keep-both (two witnesses in ONE cell, fifteen when both present; re-proves as 0176 after merging forward). All three DB PRs + #558 (r3 pending) now wait only on #555 landing and the serial renumber/merge.
- **22:25 #558 r3 pushed `ac1d5d17`** (stall reference = `lastSettledAt || loopStartedAt`; settle-then-blow cell, non-vacuous; mutant reds cell 18 alone) → review-558 quick check SENT. #555's old run on `c951c1fb` was CANCELLED as superseded (expected); the live run is on `429313c1` (monitor bcmmbnb86).
- **22:32 review-558 CLEAR on `ac1d5d17`** (reference fixed; the new cell non-vacuous; no CI run existed for that sha at read time — the post-DB `update-branch` triggers one; read its job list before merging). ALL SEVEN open PRs now have a CLEAR: #555 (CI on `429313c1` watched) → #551 → #552 → #556 → #558 → #557 (folding).
- **22:36 #558 `ac1d5d17` is DIRTY vs main** (#549/#553 moved `manifest.txt` / possibly `firm-scope-surfaces.test.ts`) — that is WHY no CI run existed (GitHub cannot form the merge ref) → L9 told to merge-forward from `80e42bf7` and push once; a later `update-branch` after the DB PRs is expected clean. LESSON: "no run for this sha" on a PR = check `mergeStateStatus` first — DIRTY suppresses the pull_request run.
- **22:40 #557 fold pushed `46b79f29`, CI GREEN** (blocker + 4 majors + 9 nits; N7 = gate the CONTROL, read stays open, `canClassifyTurnover` mirrors `0016:916`; focus-ring census FIFTEEN as a union — the auto-merge had list 15 / count 14; two extra uncovered claims got cells) → review-557 re-verify SENT. Merges LAST after an `update-branch` over #553 + the DB PRs.
- **22:45 review-557 CLEAR at `db9dd175`** (L16 had already merged #553's main; heading order measured H1-first with axe `[]`; MAJOR 2 fixture discriminates; the 裁-44 guard needs both halves; N7 shape right; census 15 list = count; no dropped carrier). **ALL SEVEN OPEN PRs CLEAR.** Landing order: #555 (CI watched) → #551 → #552 → #556 → #558 → #557.
- **CLOCK CORRECTION (read from `date` at 21:34 MYT):** the stamps "21:48 … 22:45" in the lines above were the lead's own estimates and run ~1 h FAST; true order is unchanged. From here every stamp is read from `date` first. (The known lesson: the lead's clock drifts — stamp from `date`/gh.) #555's live run 33878094899 started 21:26 MYT; the monitor's 45-min window covers it.
- **02:58 (09-05, from `date`):** #555 MERGED `6bad969b` → main = ELEVEN. Post-cut census: L9 pushed its merge `8d2e1061` (CI 33908751780 running; BLOCKED = awaiting approval) → review-558 merge-commit read SENT; L16 pushed `09101d66` (locator fix) → review-557 delta read SENT, but DIRTY vs main (#555) → L16 told to merge `6bad969b` once more; L8 tree clean (composed cell presumably stashed) — report pending; zero rigs up. **#551 RENUMBER (lead):** main merged clean into `dba-close-gates`; 9 files → 0165–0173; only `firm-scope-db-pins.test.ts` re-keyed; no stale `UNNUMBERED_dba` refs; web suite 2691/2692 — the ONE red was `pdf-worker-asset.test.ts` "Cannot find module pdfjs-dist" = STALE INSTALL after the lockfile-changing merge (house rule added) → re-install + re-run in bg (busp0p0u7); 9 R + 1 M staged; commit + push on green. Artifact REPUBLISHED at a NEW url (the old one died with the account change): https://claude.ai/code/artifact/1a58df51-1696-41fe-a5a5-9bfbcf127257. 裁-194…197 recorded above (196(b) with the lead's dissent + implementation reading).
- **03:15 (09-05):** review-557 CLEAR on the `09101d66` delta (two test files; both h1s named, `toHaveCount(2)`); review-558 CLEAR on the `8d2e1061` merge (only #558-owned lines; the entrance `4` is the vacuity control beside a derived both-ways set — right shape); L8's keep-both is banked on local branch `l8/keepboth-552-HOLD` (`8e8d3985`) with a five-state proof on a 0176 chain (13/14/14/15/foreign-red) — plus TWO more #552 collision files: `packages/db/tests/rig-meta.mjs` (different offsets, should auto-merge — verify) and `packages/db/package.json` (guaranteed one-line conflict on the `test` script's `--import` gates → keep both). L9 ALSO hit the stale-install trap after merging #555 (TS2307 pdfjs-dist) — the house rule is now confirmed twice. **LANDING ORDER (final): #551 (push on green suite) → #558 (update-branch, CI 33908751780 already running on 8d2e1061) → #552 (DB-B merge-forward + renumber 0174/0175 + owed doc clause) → #556 (L8 keep-both, 0176) → #557 (L16 merging 6bad969b now; final update-branch).** Strictly serial: a merge mid-cycle costs the next PR a fresh 25-min CI.
- **03:25 (09-05): #551 RENUMBERED AND PUSHED by the lead — `aa52999e`** (0165–0173; web suite 2692/2692 after re-install; commit trailer per the CURRENT attribution rule = Co-Authored-By only, no Claude-Session line). CI running → merge on the meta-gate. Numbers now CLAIMED: the next DB merge (#552) starts at 0174.
- **03:40 (09-05): docs PR #559 OPEN** (裁-193…197 + row-90 erratum; ledger 197→277, register 36→46 rows 100–104, log 54→76; CI docs-only green) → review-554 single-lane read SENT. Lands LAST; after #558 merges, one-line reword (§4.3 "owed" → "landed by #558") + update-branch. #558 CI GREEN on `8d2e1061` (run 33908751780) — held until #551 merges so #551's running numbered-chain CI is not knocked BEHIND (strict-cascade lesson: senior/long first).
- **03:50 (09-05): POSSIBLE REGRESSION ON MAIN OR IN #557** — L16's merged tree (`1d9455ac` = main `6bad969b` + #557) fails `documents-viewer-walk.spec.ts:179` deterministically (overlay `svg` width 0 vs canvas 125.5). L16 swapped main's mock in and it still failed → the MOCK is cleared, the TREE is not (absence-of-cause-in-the-mock ≠ cause-on-main). L13 measuring on PURE `6bad969b`; L16 censusing its shared-style diff. Outcomes: red on main → #555's MAJOR-1 did not hold → small L13 follow-up PR before the deploy; green on main → #557's shared CSS reaches the overlay → L16 scopes it before its final update-branch. #557 is LAST in the chain either way, so the chain is not blocked. **#559 docs fold in progress** (items 1/3/4 now; item 2 after #558).
- **03:40 (09-05, from `date` 03:30 + ~10): #551 RED on the numbered chain** — run 33910144663 `migrate` applied 0165…0172 then `0173 … failed and was rolled back: dba9 tail (5d): the most-recent read did not pick the COMMITTED plan` (raw job log, job 101144436237; `gh run view --log-failed` returned nothing usable — use `gh api …/jobs/<id>/logs`). LESSON (the now()-tie class): a fixture planted inside ONE transaction gives every row the same `now()`, so a "most recent" read is a tie broken by physical order — passes on one rig, fails on CI; AND a rung built on "most recent" is nondeterministic in production under the same tie. Fix order to DB-A: the rung refuses on ANY `state='open'` plan (deterministic), fixture with `clock_timestamp()`, re-prove in CI's shape, merge main before the push. **#558 MERGED `2060c762`** (nothing was knocked behind: #551's run was already dead) → main = TWELVE. Docs lane signalled for #559 item 2. L9 told: lane complete; 裁-196 (a)+(b) follow-up order later.
- **03:50 (09-05): #551 run 33910144663 final = `db-estate` SUCCESS, `db-live-gates` FAILURE on the SAME commit** — the two jobs each migrated from scratch and the 5d tie fell both ways: the cleanest proof of the coin flip (DB-A confirmed: `created_at` = transaction `now()`, tie broken by `gen_random_uuid()` `id desc`; the RUNG at `0173:205-207` shares the ordering). Fix CONFIRMED to DB-A: fixture backdate + fail-safe tie-break `(state='open') desc` in all three reads + a tie arm 5e / cell `dba.9d` that PROVES the tie refuses. LESSON: one commit, two jobs, opposite verdicts on one assertion = nondeterminism, never "flake" — find the tie.
- **03:58 (09-05): #559 item 2 done `90703b0a`** (§4.3 verified on main; main merged; docs-only 5 files; lint 0) + one discharge clause at the ledger :224 ordered. review-554 confirms the final head at land time. Lands LAST.
- **04:05 (09-05): overlay red RESOLVED — pure main `6bad969b` is red 2/2 with identical widths; the FIX HOLDS** (three aria-hidden svgs; index 0 = a breakpoint-hidden lucide menu at width 0; index 1 = the overlay at 125.5 = the canvas host); the defect is the SPEC's document-order `querySelector` at `documents-viewer-walk.spec.ts:198` (selector luck — passed on L13's tree, failed on main with identical code). L13 ordered: a small e2e-only PR (scoped selector + polygon assertion + mutant) off `2060c762`, landing after #556 and BEFORE #557's final update-branch. L16 holds at `1d9455ac`. Selector-luck class added to `order-CI-E2E-192.md` as item (v). #559 final `1693693c` (ledger 280). Chain: #551 (DB-A fix pending) → #552 → #556 → L13 e2e PR → #557 → #559.
- **04:20 (09-05): PR #560 open `8b116288`** (L13, e2e-only: scoped overlay selector in one const, polygon guard ≥1, and a SECOND defect fixed — the post-resize poll accepted a NULL width because the overlay unmounts its svg for a frame during reflow; mutant reds on the guard; leg 103/0/7) → review-555 SENT. DB-A: repair `94100a74` + main merged `e2ef9e71` — mechanism is now EXISTS-form (any open plan refuses), accepted with three conditions (semantics stated; `dba.9d` older-open+newer-committed = REFUSED; the human door that clears a stale open plan named or a Known-issue row); push after its suite.
- **04:30 (09-05): #560 review-555 CLEAR** (one const feeds poll + measurement; the null-width poll cannot pass on the empty state; mutant deterministic on the polygon guard; the reviewer owns the earlier miss) → CI 33913749261 watched (bu0v0oq8j) → merges on green, BEFORE #551 if DB-A has not pushed (DB-A local at `e2ef9e71` + 2 dirty, suite running; it re-fetches main before its push).
- **04:40 (09-05): LEAD ERROR, contained — two crossed orders on 0173's mechanism** ("tie-break confirmed" → "exists accepted" → "keep tie-break") made DB-A re-cut twice. Settled FINAL: push the current head unchanged; review-551 verifies the tree. **What is IN the tree (DB-A, `615de8c2`): the TIE-BREAK form** — recency read + `(state='open') desc` + `id desc` (total order); `dba.9d` = THE TIE cell (same `created_at`, committed holds the higher uuid → refuses); mutant 3/3 on 9d, 9a–9c must-not-red; the exists cut `017f6142` stays in history superseded; the human path recorded (`cancel_client_onboarding` `0017:2843`). Lead said nothing further — the tie-break ships. LESSON: when a lane is mid-build on a wall, ONE mechanism order, then silence until its report — never a second message that revisits the first. Known-issue row (owner-visible, pre-existing): `cancel_client_onboarding` (`0017:2843`, admin+) ARCHIVES the client (`:2866`) and `apply_coa_template` gates on client-in-firm not status, so a chart can still be applied to an archived client.
- **04:55 (09-05): #560 MERGED `2f736758`** (CI 9/9; review-555 CLEAR) → main = THIRTEEN. DB-A told to merge `2f736758` before its push. Remaining: #551 (DB-A push pending) → #552 → #556 → #557 → #559.
- **15:45 (09-05) CHECKPOINT — ONE SILENT LANE FOUND:** DB-A finished the 0173 repair at 04:19 (`04ee2ff7`, 8 commits ahead, unpushed), waited on its suite, and never reported for 11 h; rig `clara-dbaU-52681` up 12 h. Resumed with "read the suite result, push on green, tear down"; a PUSH DETECTOR monitor (bmyp2x9s7, 60-min timeout) watches #551's head. LESSON (the silent-wait class): a lane that says "waiting on the suite before pushing" gets (a) a push/head-move Monitor armed by the lead immediately and (b) an interim-ping rule (launch + 10-min); the lead's own session sat idle 11 h because no event arrived — silence is not a signal, so ARM a detector on every "waiting on X" from a lane. Artifact republished (same new URL). Owner: no new rulings needed; one known-issue surfaced (cancel archives the client; chart still applies).
- **15:50 (09-05): #551 PUSHED `04ee2ff7`** (suite had been green since 04:27: 4034/3940/0; rigs all down; main `2f736758` already merged in; body applied; the TREE = tie-break form) → review-551 delta read SENT; CI run 33953432103 watched (monitor). The push detector fired as designed.
- **15:58 (09-05): #551 MERGED `d28b6a75`** (0165–0173 on main; CI 33953432103 9/9; review-551 CLEAR) → main = FOURTEEN. DB-B SIGNALLED for #552's merge-forward (push detector armed, bmyp… successor). Then: lead renumbers 0174/0175 → CI → merge → L8 signal (#556, 0176) → #557 update-branch → #559.
- **16:35 (09-05): #552 RENUMBERED AND PUSHED by the lead — `55048c7a`** (0174 web-reads / 0175 statement; hashes recomputed = barrier entries; the barrier map RE-ORDERED — DB-B's "position does not move" was wrong: numbered, web-reads sorts before the statement file — and SPLIT into `apps/web/tests/firm-scope-db-pins.corpus.ts` because the test stood at 573 lines over the 500 hook; test 498 lines; eslint 0, tsc 0, web 2697/2697). The renumber tool rewrote a basename INSIDE 0175 (a comment) — restored to the reviewed bytes so the barrier hash holds (TOOL FIX owed: skip `packages/db/migrations/*.sql` content). CI 33955533932 watched; review-552 final delta read SENT. L8 pre-signalled (barrier map moved; compose both witnesses; package.json keep-both; 0176 is the lead's renumber).
- **16:50 (09-05): #552 MERGED `5007bbcc`** (0174/0175 on main; CI 33955533932 9/9; review-552 CLEAR) → main = FIFTEEN. L8 SIGNALLED for #556's merge-forward (push detector armed). Remaining: #556 (renumber 0176 mine) → #557 update-branch → #559. Then clock-out + the deploy ceremony (DB 0165–0176 with SIX D1 bodies: #551's `apply_coa_template`/`set_document_kind`/`_gate_outstanding_items` + #552's three; runtime image gated on the classify recall run; Worker last).
- **17:10 (09-05): #556 RENUMBERED AND PUSHED by the lead — `c751fc5c`** (0176; byte-identical; comment-only rewrites; web 2697/2697; L8's estate proofs 4086/0 on both chains; the tool's migration-bytes guard held). CI 33956697262 watched; review-556 final delta read SENT. After #556: `gh pr update-branch 557` → CI → merge; then #559 `update-branch` → merge; then clock-out (PROGRESS.md truing PR, graph re-index) and the deploy-ceremony runsheet lane.
- **17:25 (09-05): review-556 CLEAR on the final delta** (gates keep-both; debt-BAR1 one cell two witnesses 13/14/14/15; rename pure; no barrier owed). **PR #561 open** (PROGRESS truing: 305→395 + archive part1 107; 18 PRs measured; three "landing" placeholders #556/#557/#559) → review-554 single-lane read SENT; lands after #559 with the three shas filled. Ceremony-runsheet lane running (read-only).
- **17:40 (09-05): ceremony runsheet DRAFTED** (`scratchpad/ceremony-runsheet-2026-09-05.md`, 348 lines; read-only). Confirmed from the migration headers: SIX D1 bodies (0169 `set_document_kind`, 0172 `_gate_outstanding_items`, 0173 `apply_coa_template`, 0174 `_tf_chat_session_update` + `_tf_counterparty_update_0011`, 0175 `_persist_statement_core_v2`); 0176 wants a LOCK window (index rebuild on `counterparty_aliases`), not D1; `chatRoutes.ts:168-171` documents the archived_at 500 until 0174 applies. **OWNER QUESTION (new, needed before runtime v75 ships): the numeric recall floor for the H-04 classify gate** — lead's recommendation: a NON-REGRESSION floor on the real corpus (per-kind recall ≥ the current prompt's, and no new false auto-accept at ≥0.8), an absolute number later. Staleness found: `packages/runtime/README.md` vs #558; H-43/H-48 rows (code shipped; operator flip remains); no routine Worker-redeploy runbook; `clara-backup` machine id pre-reset → all routed into #561.
- **17:55 (09-05): #556 MERGED `9ed75f49`** (0176; CI 33956697262 9/9; review-556 CLEAR) → main = SIXTEEN, frontier 0165–0176 COMPLETE on main. **#557 `update-branch` → `edd1f8b2`** (clean), CI 33957663798 watched; L16 asked for the 裁-86 acceptance walk on that exact tree. Then: #559 → #561 (shas: #556 `9ed75f49`, #557 ?, #559 ?) → hand sweep `gh workflow run ci.yml` on final main (runsheet 0.2) → graph re-index → ceremony order.
- **18:05 (09-05): #561 additions pushed `1a8aa25d`** (PROGRESS 427; dsn-bridge roster 7 + two pre-existing broken backticks fixed; the 0.8 `CONFIDENCE_GATE` is the per-row bar, NOT the recall floor — a decision still owed) → review-554 delta read SENT. L8 record: 0176 = lock window (ACCESS EXCLUSIVE per statement on `counterparty_aliases`/`counterparties`), no D1; the walk's `error_code internal · tokens 0` on the two beta admissions is UNEXPLAINED by H-17's fix — read `agent_tasks.last_refusal` at the ceremony (already on the deploy-time list).
- **18:12 (09-05): #557 MERGED `127c4513`** → main = EIGHTEEN; every code PR of the session is on main. Merged on the existing acceptance evidence (three full walks; product code identical between `1d9455ac` and `edd1f8b2`; CI 9/9; review CLEAR) because L16 went silent on its final-tree walk (the second "the task will wake me" loss today — lane rule: never wait on a wake-up; read the file). #559 `update-branch` → `cb61628b`, docs CI 33965034852 watched. Owner rulings 裁-198 (ceremony tonight on chain + sweep green) and 裁-199 (non-regression recall floor) taken; the ceremony ORDER is written (`scratchpad/order-CEREMONY-0905.md`). Session URL changed → trailers updated in the house rules. Next: #559 merge → #561 GO (shas 9ed75f49 / 127c4513 / #559) → sweep → re-index → ceremony lane.
- **18:20 (09-05): #559 MERGED `f57f6af4`** → main = NINETEEN (裁-193…197 + the row-90 erratum on main). **裁-200** (owner, ≈18:15, (a) of 3): the owner's uncommitted `AGENTS.md` one-liner ("Ask the owner first **using /grillwithdocs**") is folded into #561 as repo law; after #561 merges the lead restores the local file to main's content (identical text, now committed) and fast-forwards the main checkout for the graph re-index (project id `C-Users-zhant-Desktop-clara-rebuild` is keyed by that path — a worktree would mint a second project). Docs lane GO'd for #561's final push (shas 9ed75f49 / 127c4513 / f57f6af4; 裁-198/199; the AGENTS.md line); push detector armed. Then: review-554 delta → update-branch → merge #561 → restore+pull → re-index → hand sweep → ceremony lane on green (裁-198).
- **21:05 (09-05): #561 final push `b2713952`** (shas filled; 裁-198/199 rows 105–106; the "no recall floor" row replaced; AGENTS.md /grillwithdocs line = 裁-200; new trailer) → review-554 delta read SENT; docs CI 33967583564 watched. **HAND SWEEP DISPATCHED on `f57f6af4`** (the last CODE-carrying tip; #561 is docs-only) = run 33967641251, 13 jobs, watched with a 55-min timeout line (runsheet 0.2). The lead's session idled ≈2.5 h (18:20→20:56) because a Monitor's harness timeout produced NO event → every monitor now echoes its own TIMEOUT line. Next: merge #561 → restore local AGENTS.md (content = main's) → ff-pull → `index_repository` → on the sweep's green, tell the owner the window opens (be at the keyboard) → dispatch the ceremony lane with `order-CEREMONY-0905.md`.
- **21:15 (09-05): THE CHAIN HAS LANDED — #561 MERGED `0351f022`** (docs CI green; review-554 CLEAR after the body-URL fold) → main = NINETEEN session PRs (#543…#561), ZERO open. Local main checkout restored (AGENTS.md = main's sentence, 裁-200) and fast-forwarded to `0351f022`, clean. Graph RE-INDEXED (full: 29,296 nodes / 93,651 edges). Artifact republished "Chain landed 21:15". CLOCK-OUT items done: PROGRESS trued (#561), harness sync (#559/#561), memory (lesson file + 11 items), graph. REMAINING IN MOTION: the hand sweep 33967641251 on `f57f6af4` (watched, 55-min line) → on 13/13 green: message the owner (window opening; be at the keyboard) → dispatch the ceremony lane (opus xhigh) with `order-CEREMONY-0905.md` + the runsheet.
- **21:35 (09-05): PR #562 open** (裁-200 recorded; docs-only; CI green; head `a68fceb7`) → review-554 SENT; lands AFTER the ceremony as-run PR. Ceremony lane `ceremony-0905` (opus xhigh) in §0 preflight (pinging every 10 min); the sweep 33967641251 still running (watched, 55-min line).
- **21:45 (09-05) ceremony §0 interim:** live frontier 159 / `0164` (as PROGRESS said), freeze `{ok, 7, 8}` unchanged, 19 roles, PG 17.6, bridge via sleeper `784e4d0c996938` verify-full. ESCALATION: ONE non-terminal `workflow.workflow_runs` row — `wrun_01M1MNTV64Z681KNQ6QVM4HEDH` `clientOnboarding_v4` running since 2026-09-03T14:22Z, zero progress (a launch-night orphan). RULED PROCEED (its export is unchanged by this session; the deploy neither strands nor rescues it); the lane records its subject for a Known-issues row; the OWNER decides its disposition later (cancel vs leave).
- **21:55 (09-05) ceremony §0 TABLE:** main `0351f022` clean, 0 open PRs; sweep 10/13 green so far (closed-wave-drills + db-estate running); live 159/0164, freeze `{ok,7,8}`, 19 roles; backup BANKED (`2026-09-05T13-15-28-593Z`, 17.8 MB bundle, ping success; the runbook's machine id `d895470c6024e8` is CURRENT — the runsheet's "pre-reset" warning was unfounded); Fly v74 = the v71 image (nobody deployed); `/ready` old shape, held_outbox 8; pg client v16 only (no impact on the ordered path); runtime build exit 0; freeze OK. **OWNER ACTION NEEDED for §6: `wrangler login` in WSL (the FS-10 OAuth lapsed; no token on the host).** The recall gate has NO manifest → lane derives one from HUMAN-set kinds (approved with derivation review + per-kind counts); OPENAI key stays on the machine (harness runs where the secret lives). 0.6: `autodraft_attempts.last_refusal` on the ONE parked row = `{"code":"CLR23","message":"The counterparty could not be resolved as proposed."}` — the masked shape #556 replaces (read, not asserted as cause); two `agent_tasks` failed/internal for client `7a045c7f…` (ROME SECRETARY).
- **22:05 (09-05) MATERIAL: the scoreable recall corpus is FOUR human-labelled documents** (3 `bank_statement` + 1 `invoice`, all firm `04daf86c…`; the live prompt scored 0/3 on the bank statements at confidence ≤0.05 = the H-04 defect; the 500+ desktop corpus is NOT ingested). 裁-199's "non-regression" is trivially met on bank_statement; the "zero confident-and-wrong" half on n=4 catches only a catastrophe. Working plan (a): run on the four, denominator in the first line, verdict "no catastrophic regression proven; calibration unproven; next 20 human-labelled docs form the baseline". OWNER to be asked during the window whether n=4 satisfies 裁-199 tonight (alternatives: hold the image; or temporarily raise the auto-accept gate to 0.95 — a DB write outside this ceremony). Pre-window tripwire captured (six body shas + 17 witnesses incl. `_approve_entry_core d5ab4afc` = the 0148 as-run's value); `counterparty_aliases` has ZERO rows → 0176's index rebuild is trivial; no heartbeat guard in the twelve files.
- **At deploy time (lead, as delegate, via the DSN bridge):** read `agent_tasks.last_refusal` on the two beta-walk admissions — L8's honest caveat: the walk recorded `failed · error_code internal · tokens 0` (model never called), which is NOT the CLR23-mask signature; H-17's fix is proven as a code defect, not proven as what fired. Also: whoever claims #556's number re-runs the estate suite once on a NUMBERED chain (CI skips unnumbered files, 裁-108).

## Open PRs (merge chain is SERIAL: `gh pr update-branch` → CI → squash-merge). **RULED ORDER: #546 → #547 → #548 → #549** (#549 conflicts with all three: `OnboardingDoorDialog.tsx` keep-both with #549's close semantics winning; `focus-ring-contract.test.ts` census → 14 with both carriers; `e2e-fixture-ownership.test.ts` / `serve-built.mjs` / `drafts-queue-panel.tsx` / `manifest.txt` union). #545 MERGED `a2d098f2`.
| PR | branch | lane | state |
|---|---|---|---|
| #545 | runtime/statement-facts-v3 | L4 | review-545 FOLD VERIFIED (F1 codeless-fault regression fixed, F2 claim trued); updated from main `caa9d235`; CI run 33832193650 IN PROGRESS; armed wait task buzpi8p3c → merge on green |
| #548 | web/journals-table-541 | L6 | review-548 CHANGES REQUIRED (blocker: legend rbac sentence pre-裁-188; 2 majors: mock claim + ownership census; 5 nits) — FOLD IN PROGRESS (3 dirty files at the cut; resumed) |
| #547 | web/chat-panel-541 | L3 | CI green; review-547 restarted after the cut |
| #546 | web/onboarding-fixes-541 | L2 | CI green on `1ce501de`; lane was re-running a CORRECTION (3 dirty files) — resumed to push; then re-dispatch review-546 |
| #549 | web/bank-close-registers-541 | L7 | opened just before the cut; CI running (db-estate pending); lane resumed to send its report; then review-549 |

| #551 | db/close-gates-document-kinds | DB-A | delivered 12:04 (fresh-rig estate 4028/3934 pass/0 fail; 7 UNNUMBERED files, dba1 first; ONE D1 body `set_document_kind`); review-551 running; **owner call in the PR body: the 20-row kind codeability table (`tax_correspondence` the likely flip)**; H-29 turns on 裁-23 Q5 (after begin vs after commit — DB-A built "after begin") |

**Fold state of the four web PRs (all CHANGES REQUIRED → folding):** #546 (3 majors: cancelled-arm chart panel, MPERS basis unread, vacuous backward-index cell) · #547 (4 majors: numeric strings on cards, fabricated created_at, New-while-resolving, mock/server divergence fence) · #548 r2 (ownership census last-handler blind spot, derived LANE_MOCKS) · #549 (7 majors now incl. shared refusal object across coexisting dialogs, one shared close predicate + per-family refusal cells, act() seam cell, honest export copy, hoisted member names, verb-opener census, FA refresh cells; 2 merge-time items).

## Building (no PR yet)
| lane | branch | worktree | state at the cut |
|---|---|---|---|
| L5 lane-web-firm | web/firm-needs-you-activity-admin-541 | l5-web-firm | 34 dirty files, "re-census every door's live body"; resumed |
| DB-A lane-db-a | db/close-gates-document-kinds | dba-close-gates | 13 dirty + 1 unpushed commit; rig `dba2-aad5fae9` up; "re-running the estate suite"; resumed |
| DB-B lane-db-b | db/web-reads-and-doors-541 | dbb-reads | 2 dirty; rig `dbb-5d48ffc7` up; "migration 1 clean, now the statement-lane migration"; resumed |
| L13 lane-web-documents | web/documents-viewer-mime-541 | l13-web-documents | just started; resumed |
| L15 lane-web-shell | web/shell-responsive-favicon-cmdk-541 | l15-web-shell | just installed; resumed |

## Queued orders (scratchpad `order-*.md`, dispatch as slots free)
- L16 web-home-build (firm home + client home from `design-home-spec.md`; tax tab) — after L5 merges (consumes its needs-you row) or in parallel with file discipline
- L17 web-copy-sweep — LAST, after every other web lane merges
- L8 runtime-autoDraft-v10 (H-17 exact constraint map + kind-scoped alias unique + H-19 owner-floor wrapper)
- L10 runtime-chatTurn-v18 (H-07 close relations + H-08 oracle wording) — needs the DB grant half
- L9 runtime-ops (H-48 per-lane probe + C-04 listener, H-43 CA copy, CB-035 build-info via DB-B's build_frontier, H-04 classifier harness)
- DB-C walls (裁-188) after the P0 block; the consent lane (裁-186); reporting H-15; CB-001 Terms kind (owner sitting); H-47 preflight + runbooks; CB-036 CI e2e job (needs a ruling vs 裁-86)
- Deploys (裁-189): runtime v75 from main after L4+DB lanes merge; the web Worker after the web lanes merge

## Rulings taken after the ledger PR merged (write into the -pm ledger at clock-out)
- **裁-191** (owner, 2026-09-04 ≈12:30 MYT, AskUserQuestion option (a) of 3, 「宁可误报、不可漏报」): in the document-kind codeability table (PR #551) `tax_correspondence` and `agreement_contract` are BOTH codeable — a Notice of Assessment and an HP/finance-lease contract create bookable liabilities; acknowledgement letters and supply contracts show as uncoded until dismissed, which no longer blocks a one-click close under 裁-187. Table = 12 codeable / 8 not. Recommendation followed.

- **裁-192** (owner, 2026-09-04 ≈12:40 MYT, AskUserQuestion option (a) of 3 after one 「不懂，再解释」 re-brief): the browser smoke (Playwright on the built app, mocked backend) becomes a REQUIRED per-PR CI gate — a ~10-minute `web-e2e-smoke` job under the `ci` meta-gate; unexpected console/network errors red it. AMENDS 裁-86 (the walk was an acceptance instrument, not a gate; the per-train acceptance walk STAYS beside it). CB-AE2E-036. Recommendation followed.

- **裁-193** (owner, 2026-09-04 ≈16:45 MYT, AskUserQuestion option (b) of 2, AGAINST the recommendation (a)): the chart of accounts may be applied only AFTER `commit_client_onboarding` — 裁-23 Q5's "after the client is created" = after commit. Consequence: #551's dba6 re-cut to committed-only + a `seed_decision_plan_state` key for the card's copy; #546's settled receipt already hosts the apply control post-commit. H-29 closes as "the card says decided-applies-after-commit". Dissent: none filed (a taste call, no accounting risk either way).

- 裁-193 MECHANISM (lead's call, house law PRD §6 "enforced in the DB, not the UI"): review-551 measured that `apply_coa_template` (`0156:726-910`) never consults the onboarding plan, so "the door refuses on an open plan" was a GAP. DB-A adds the refusal INSIDE the door (a CoR on a granted writer → prestate pin + D1 quiesce) and makes dba6 two CTEs (committed-only `dec`; a separate open-plan lookup feeding only `seed_decision_plan_state`). #551's D1 list grows by `apply_coa_template`.

- **裁-194** (owner, 2026-09-05 ≈02:50 MYT, AskUserQuestion (a) of 2): the 裁-149 clause-2 PREMISE CORRECTION is ACCEPTED — the leader session always had an error listener + reconnect loop (`relay.mjs:139-153`, `leader.mjs:177-188`), was never crash-loud, and failover holds via the released session advisory lock; `docs/ARCHITECTURE.md` §4.3 is the record; the rulings register row 90 gets a one-line erratum at clock-out; the leader stays byte-untouched. Recommendation followed.
- **裁-195** (owner, 2026-09-05 ≈02:50 MYT, (a) of 3): REQUEUE-ONCE — a human's real ANSWER to a coder-opened `sweep_refusal` question IS the human act that un-parks the twice-failed registry and re-mints the draft; a DISMISSAL does not re-mint. Lane: copy `readmit_autodraft_after_withdrawal` (0117:173-345, reserve the op key AFTER the delegation); eligibility = `origin='sweep_refusal'` + `scope_kind='document'` + parked registry + terminal task; op-key `'q-requeue:'||event`; runtime arm beside `admitWithdrawalEvent`; four cells incl. a `'manual'`-origin question must NOT re-mint. ≈0.5 unit. Recommendation followed.
- **裁-196** (owner, 2026-09-05 ≈02:50 MYT, multi-select, ALL FOUR taken): (a) production REFUSES to boot on a DSN carrying `sslmode=no-verify` (was warn); (b) **a dead NON-runtime DB lane FAILS `/ready`** — AGAINST the lead's beta recommendation; DISSENT recorded: a 503 on a lane whose ceremony has not run would take chat down for a lane nobody uses yet. IMPLEMENTATION READING (lead, to keep the ruling safe): only a CONFIGURED lane (DSN present) that is unreachable fails readiness; an UNCONFIGURED lazy lane stays `skipped` and never fails; `pending` (unmeasured) never fails; a `stalled` probe loop never fails (an instrument fault is not a lane fault). (c) the sales-lane switch keeps `p_reason`; (d) `clara.sst_threshold_schedule` gains a firm-user read (grant or definer read) so the Tax tab's classification control offers the statutory thresholds. (a)+(b) = one runtime lane (L9 follow-up, ≈0.5); (d) = the next DB lane (DB-D).
- **裁-197** (owner, 2026-09-05 ≈02:50 MYT, multi-select, ALL THREE taken): three product tickets ENTER THE QUEUE after the nine: (i) provisional streaming text in the rail (≈0.7; grey provisional stream replaced by the DB-persisted message — not a durable artifact, so constraint 2 holds); (ii) real readers for the nine ids-only part kinds (≈1: je_review, doc_review, diff, open_question, bank_recon_receipt, fixed_asset, depreciation_run_receipt, adjustment_run_receipt, staff_advance); (iii) chatTurn tools + cards for the five gaps (≈1.5: send a document into intake, start an onboarding interview, add a fixed asset / set depreciation method, recurring adjustments, start a period close). Order among the three: (iii) → (ii) → (i) unless the owner says otherwise (the owner listed (iii) first).

- 裁-196 ORDER NOTES (L9, for the follow-up lane): (a) refuse-on-no-verify is a throw in `assertLaneDsnTlsPosture` (`packages/runtime/lib/tls-ca.mjs`) where `weakMode` is already computed — SCOPE DECISION (lead): refuse on an EXPLICIT `sslmode=no-verify`/`disable`/`allow`/`prefer`/`require` (anything that does not verify the chain) in production ONLY when a CA is pinned or the DSN is a `*.supabase.*`/non-loopback host; a DSN with NO `sslmode` on a loopback host (a local rig with `RELAY_TEST_MODE` unset) stays a WARN — the ruling targets skipping certificate checks against a real pooler, not local rigs; state the rule in the module header + a cell per arm. (b) configured-but-dead lands in `checkReadiness` (`lib/health.mjs`): `lane.skipped` = unconfigured (never fails), `laneProbe.pending` (never fails), `laneProbe.stalled` (never fails); the FREEFORM lane is eager in `assertProductionPoolConfig` so it is always "configured" in production and would newly be able to 503 the runtime — ACCEPTED under 裁-196(b) (the owner chose it; freeform is a real lane with a real DSN), but the order must say so and a cell must pin that a dead freeform lane fails readiness while a `skipped` lazy lane does not.

- **裁-198** (owner, 2026-09-05 ≈17:55 MYT, AskUserQuestion (a) of 3): the DB ceremony (0165–0176 onto the live project, ONE write-quiesce window with the runtime machine stopped, ≈15–30 min, backup first, per-step rollback) OPENS as soon as the chain lands and the hand-dispatched 13-job sweep on the final main is green — tonight. Recommendation followed.
- **裁-199** (owner, 2026-09-05 ≈17:58 MYT, (a) of 3, after one 大白话 re-brief — the classifier decides the document's KIND, not its client): the H-04 classify gate's floor for runtime v75 is NON-REGRESSION on the real corpus — per-kind recall with the new prompt ≥ the live prompt's, AND zero new "confidence ≥ 0.8 but wrong" cases (one such case blocks the image). No absolute number until a baseline exists. Recommendation followed. Both rulings go into the ledger/register with #561's sha-fill push (after #559 merges, so the ledger carries 193–197 first).

- **裁-201** (owner, 2026-09-05 ≈22:20 MYT, AskUserQuestion (b) of 3, after a 大白话 re-brief that the 0.8 gate is the DB's lock and the confidence number is the LLM's own self-report): deploy runtime v75 tonight with the auto-accept gate UNCHANGED at 0.8; the 裁-199 recall run proceeds on the four human-labelled documents (3 bank_statement + 1 invoice), denominator recorded first; a confident-and-wrong case still blocks the image. **AGAINST the lead's recommendation** (tighten to 0.95 until 20 human-labelled documents form a baseline, because #558's prompt instructs the model to self-report ≥0.85 — a self-graded score behind the DB's only lock). DISSENT recorded; executed. Mitigation kept: the as-run states "calibration unproven"; the baseline re-run is a Backlog row.
- **SWEEP 33967641251 on `f57f6af4`: SUCCESS 13/13** (read from the job list, 22:2x MYT) → 裁-198's gate met → **OPEN sent to the ceremony lane at ≈22:25 MYT 2026-09-05.**

- **22:35 (09-05) ceremony rulings batch:** stranded run = the E2E audit fixture (Pine & Co E2E / "Clara E2E Audit 2026-09-04"; plan reached `committed` 21 min after the run started; the lane's first timestamps were 8 h off — `timestamp without time zone` parsed as local on a UTC+8 host; corrected numbers only); authenticated smoke legs §4.2/§6.2 = the LEAD drives in the owner's browser (a wall is not a defective face); §5 SPLIT approved (build-only push → one-off machine runs the recall harness with an explicit command, `--rm`, single-leader constraint kept → release on PASS); the runner's history-integrity dry run PASSED (159/159 checksums, 12 pending in order, checksums recorded); tripwire = six body shas + 17 witnesses; `/api/build-info` on v74 = empty 200 (the catch-all) — after v75 it must be JSON.
- **LESSON (ceremony lane, 22:35):** a `timestamp without time zone` column read through node-postgres on a UTC+8 host shifts by 8 h — read as TEXT and do the arithmetic server-side; calibrate the reading against a neighbouring `timestamptz` event (the plan created 30 s before the run).

- **00:08 MYT 09-06 — WINDOW OPEN.** §1 done: runtime `48ee715b763048` STOPPED 16:05:03Z; zero `clara_%` sessions in ANY state; one `statement_facts` task `running` (`8923e85a…`, doc `8978b8d5`) whose run FAILED 39 h ago = an H-03 orphan (pre-#545) → ACK'd PROCEED without touching it (no active call; 0175 does not read that table); Known-issue row. §2 acknowledged at ≈00:10 MYT.

- **00:15 MYT 09-06 — LEAD ERROR, being contained:** the checkpoint census sent `curl …/health` to the STOPPED runtime and got HTTP 200 → either the lane is already at §4, or Fly AUTO-STARTED the machine on inbound HTTP, breaking the quiesce. Lane told to read `fly status` now, re-stop + re-verify if §2 has not begun, or report the runner's position if mid-flight. LESSON: during a write-quiesce window, NOBODY sends HTTP to the app — Fly's proxy wakes a stopped machine; liveness is read with `fly status`, never with curl. Write it into the ceremony recipe (§1) and the runsheet.

- **00:22 MYT 09-06:** the lead's Fly watch was BLIND from the start — this shell's `flyctl` has no token (the ceremony lane's WSL flyctl does), so the monitor's grep matched nothing and printed nothing; stopped. The lead has NO independent liveness instrument for the runtime that is safe during the window (curl wakes the machine); the lane's `fly status` reads + its 10-min pings are the only instrument. LESSON: a monitor must print its FIRST read unconditionally (a positive control) — a watcher that has never emitted a line is not known to work.

- **00:30 MYT 09-06 — DB CEREMONY DONE.** Window 16:05:03Z→16:14:49Z (9m46s). `node packages/db/scripts/migrate.mjs` (the runsheet's `pnpm db:migrate` form fails under dsn-pipe on Windows: `spawn pnpm ENOENT` — the first attempt touched nothing, frontier re-read 159 before retrying). 12 applied 16:12:41–16:13:26Z; 171 / `0176`; all 12 ledger checksums = the pre-window pins; 26 notices all OK; six bodies recut (`_gate_outstanding_items` 2df52b8f→162571b8, `_persist_statement_core_v2` d931067a→6f694ac0, `_tf_chat_session_update` 89a66964→6118109f, `_tf_counterparty_update_0011` cfb2313b→b4ad0083, `apply_coa_template` 26ddd7f3→09d03a71, `set_document_kind` 611bc543→74bb929e); 17 witnesses identical; catalog 1068→1080 fn / 280→284 rel; RLS forced on both new tables; `dr_canary_subjects` 0 rows; the 0176 index re-keyed; grants as the tails claim; codeability seed 20/12; roles 19; freeze ok/7/8. Restart 16:14:47Z, `/ready` true (old shape), world baseline identical (held_outbox 8). The lead's 00:15 curl = the restart, not an auto-start. **§4.2 smoke → the OWNER runs it** (the Chrome bridge is disconnected); §5.0/5.1 proceed in parallel; §5.2 release waits on smoke + gate.

- **00:45 MYT 09-06 — §5.0 DONE:** image `registry.fly.io/clara-runtime:v75-gate-0351f022` (`sha256:9d904928…`, 207 MB, ~5 min remote build), NOTHING released (releases v74 unchanged; one machine on the old image). Non-fatal: three "failed to read input source map" ERROR-styled lines for `@ai-sdk/openai|gateway|provider-utils` (missing `.map` files); build context 1.6 GB / 68,573 files → `.dockerignore` follow-up row. §5.1 GO sent. Auto-start ruled out by config (`auto_start_machines = false`, `min_machines_running = 1`) + timeline. SMOKE pending: Playwright at the login page (autofilled by the browser profile — the lead does NOT press Sign in; the owner does); `wrangler login` command given (via `/opt/node/bin/npx wrangler login` from `apps/web` under WSL user `runner`).

- **00:56 MYT 09-06 — SMOKE §4.2 PASS ×4** (lead-driven in Playwright; the owner pressed Sign in themselves; the Chrome extension bridge is disconnected since the account change): firm home · Journals (all reads 200 incl. the re-cut `coa_chart_state`) · a document (six reads 200; `list_uncoded_filings`/`list_coding_lanes` re-cut bodies 200) · a chat turn (POST 202 → stream 200 → "books_version 201"); 0 console errors. Enter-to-send absent on the deployed Worker (pre-#547) — expected until §6. §5.2 unblocked on the gate. Playwright page kept open (signed in) for §6.2.

- **01:05 MYT 09-06 — GATE BLOCKED by TWO SHIPPING DEFECTS in #558's harness** (found only by running it INSIDE the image): (i) `packages/runtime/Dockerfile` omits `workflows/` → `lib/classify.mjs:28` import breaks every standalone script in the image (server unaffected: nitro inlines it); (ii) `measure-classify-recall.mjs:290` builds a bare `pg.Client` with no `SET ROLE` → 42501 through every lane login (by design: `roles-bootstrap.sql:190` INHERIT FALSE, SET TRUE); (iii) the Supabase session pooler does NOT forward `PGOPTIONS` startup options. RULED option (a): run the harness on the `--rm` one-off with the MIGRATION DSN env-to-env from the sleeper pipe (identical read; no file/sftp/argv; reaped on the trap) — the 5.1a safety proof passed (zero node processes on the one-off; PID 1 = `/fly/init`, so "PID 1 = sleep" was the weaker check). Provenance captured: current prompt model `gpt-5.6-terra`, system prompt sha256 `191776db…`, gate ≥ 0.8. LESSON: an ops script shipped in an image is proven IN the image before merge. Follow-up PR row: Dockerfile COPY workflows/ + SET ROLE (or pools.mjs checkout) in the harness + a `dsn-bridge.md` line on PGOPTIONS.

- **01:12 MYT 09-06 — RECALL GATE RAN (option a) and PASSES 裁-199:** n=3 bank_statement / n=1 invoice; BOTH prompts (baseline sha `98fc27ad…`, current sha `191776db…`, model `gpt-5.6-terra`) score 100 % at-gate, 0 off-diagonal → no regression, zero confident-and-wrong. **THE HOLE (false-PASS shape, recorded):** the baseline arm did NOT reproduce H-04 — live scored the same three statements "other" at ≤0.05 on 09-03 — so the run shows v75 is not WORSE on any kind and does NOT show it is better; the control never reproduced the defect. Candidates (unmeasured): baseline file ≠ the deployed v71 prompt; live lane's model/params ≠ the harness default; n=4 variance → Known-issue + follow-up row (read the three 09-03 `document_extractions` verdicts' engine/model/params + the v71 image's prompt). GO §5.2 sent (裁-199 met, 裁-201 orders the release). Four conditions verified (dsn_len=113 only; NODEPROCS=0; reaped 16:35:49Z; key untouched).

- **01:25 MYT 09-06 — ROOT CAUSE OF H-04 FOUND (the ceremony lane, measured server-side): the classify lane FINISHED BEFORE the OCR extraction was persisted on all four documents (5.72 s / 3.14 s / 1.36 s / 5.81 s late; one extraction each, version 1; `readExtractionText` needs status 'done') → the classifier was handed an EMPTY STRING and answered "other" at ≤0.05 — the correct answer to that question. NOT a prompt defect; #558's sharpening addressed a cause that was not the cause (baseline file = the deployed prompt, since only #558 touched `lib/classify-llm.mjs` after `344f7ad8`; model = the same default `gpt-5.6-terra`). DISPOSITION: v75 ships on the gate (safe, not the fix); H-04 stays OPEN re-described; NEW P0 ticket "classify/OCR ordering race" — the dispatcher must gate classify on extraction 'done' (or re-run on completion), reproduction = the four rows, cell = a classify task planted ahead of its extraction must WAIT. This goes to the HEAD of the next-tickets list (before L18/chat tickets): every uploaded document currently loses this race.

- **01:42 MYT 09-06 — RUNTIME v75 LIVE** (released BY IMAGE `v75-gate-0351f022` = the gated artefact, image id `img_wd57v5d3lej9p38o` on both; v75 created 16:39:45Z; one machine; `/ready` true in the NEW shape — seven lanes settled on the first poll: runtime/read/write/freeform/stripe_webhook/auth_wall ok, `bank` skipped `dsn_not_configured` → H-47's "three skipped" note is STALE; `/api/build-info` 401 unauth = the route exists). §5.3 lead reads PASS: build-info JSON (git_sha `0351f022…`, frontier {171, 0176}); chat sessions 200 on v75. GO §6 sent (standalone wrangler in WSL; clone-and-build as FS-10 S7). Rollback for §5 = `fly releases rollback` to v74's image.

- **01:52 MYT 09-06 — §6 built + uploaded:** WSL clone at `0351f022` (linux workerd), `cf:build` exit 0 16:47:21Z, `worker.js` 2,278 B (= FS-10's thin entry), version `90c1a5d0-f808-4b88-bd28-d2395d9bc26a` uploaded 16:50:15Z with SIX secret names (the FS-10 H-defect check passed via `versions view`); vars are FOUR not three (truing); the first build REFUSED without `NEXT_PUBLIC_SUPABASE_ANON_KEY` (the publishable-key gate working); leak check: names of the three server secrets absent from client assets, value-greps SKIPPED (not a pass) because those values are not in the source; `NEXT_PUBLIC_CLARA_RUNTIME_URL` empty by design. PROMOTE sent. Method note for the runbook: `deployments list` (not `versions list`) names the version at 100 %.

- **02:00 MYT 09-06 — ALL THREE ARMS DEPLOYED + SMOKE §6.2 PASS ×7.** Worker `90c1a5d0-f808-4b88-bd28-d2395d9bc26a` promoted 16:52:13Z at 100 % (bytes proven by content-hashed chunk names; `/favicon.ico` 200); firm home board + client board + Journals + Bank header pair + Close gates (two honest FAILs live) + Documents overlay (bytes via proxy, pdf worker same-origin) + ENTER-TO-SEND (202 → reply) + sessions 200 + sign-out; 0 console errors. Axe not run in the lead's walk (stated). §7 GO sent (as-run PR + PROGRESS truings; then sleeper destroyed + backup-app census). Remaining after §7: review-554 single-lane read of the as-run PR → merge → #562 merge → final artifact + memory → owner report.

- **02:30 MYT 09-06 — #563 (the as-run) folded `c069829a`:** review-554's one blocker (archive part2 claimed byte-for-byte but the block carried three annotations) fixed — the lead VERIFIED by its own instrument that main's `PROGRESS.md:9-88` (md5 `e109f798…`, 80 lines) appears verbatim in the archive at line 30; the supersession note is in the preamble; the earlier mistake is on the record. CI 33980390741 watched → merge → #562 update-branch → merge → owner report. LESSON (the lane's own words): "my hash was internally consistent and measured the wrong text" — a byte-for-byte claim is proven by hashing the SOURCE range and finding it verbatim in the destination, never by hashing the destination alone.

- **02:40 MYT 09-06 — #563 MERGED `d948b537`** (the as-run: two parts, D-1…D-9, the Worker redeploy runbook, ceremony-practices + dsn-bridge corrections, PROGRESS trued with the two P0 rows). #562 `update-branch` → `e9b1f027`, docs CI 33980537166 watched → merge = the session's LAST PR. Then: ff-pull the main checkout, final artifact touch, owner report. Ceremony lane released.

- **01:20 MYT 09-06 (from `date`) — SESSION CLOSED.** #562 MERGED `fc39c361` = the last PR; 21 PRs (#543–#563), 0 open; main checkout returned to `main` (the ceremony lane had left it on its as-run branch — already merged — not a worktree) and fast-forwarded to `fc39c361`, clean. CLOCK CORRECTION: the roster's "02:xx MYT" stamps for the ceremony were the lead's mis-added UTC+8 (16:5xZ = 00:5x MYT); the artifact and this line carry `date`-read times; the as-run carries Z times. Clock-out: PROGRESS trued (#561/#563/#562), harness sync (#559/#561/#562), memory (13 items), graph re-indexed at the code-final tip (no code merged since), artifact final. NEXT SESSION opens on the owner's ask with the P0 "classify/OCR ordering race" at the head of the queue, then the harness-in-image fix, then the queue as PROGRESS lists it.

## Follow-up rows surfaced by lanes/reviews (for the register + PROGRESS)
- L4/review-545: `_stmt_header_norm` vs `0098` totals asymmetry (DB-B 7b); `period_start/period_end` plausibility bound (none, `0038:380-409`); `v3.behavior.mjs:464` hard-codes status "done" on a RETURNED failure (pre-existing); `f4-egress-release-hold.test.mjs:177` stale message; `registry.ts` at 648 lines
- L1: the resend backend (`POST /api/auth-wall/resend` sharing the C1/C2 wall) — the confirm face's button still refuses; H-40 HIBP owner decision confirmed false on the project
- L3: no export tool in chatTurn_v17 AND `complete_sandbox_export`/`fail_sandbox_export` have zero runtime callers — H-16 is a copy fix + a render-worker gap, not a v18 tool alone; `adoptConsent` copy re-cut belongs to 裁-188's lane; switcher for colleagues' shared threads + archive shape = owner questions
- L6: no human-facing journal number (a migration if wanted); the clarification SUBMIT button "Answer" duplicate name (L3's test file)
- L2: H-29 needs 裁-23 Q5 clarified (after begin_ vs after commit_) — DB-A is widening the CTE
- review-544: `tests/server-boundary.test.ts` is static analysis, not a bundler wall
- review-548 r2 (non-blocking, pre-existing, another lane's file): `apps/web/e2e/fs4-checkout-mock.mjs:100-106` claims the whole `/rest/v1/rpc/` prefix and RECORDS every other lane's door call into `state.doorCalls` (the array `checkout-gate-walk.spec.ts:334` asserts empty) before falling through — held closed only by `workers: 1` / `fullyParallel: false`; goes live the day `workers` is raised. Fix in the CI-E2E lane (裁-192) since that lane touches the runner config.
- L15/#553: `app/globals.css` is at 615 lines (568 before) — above the 500-line write hook; the next writer splits first. Owner calls in the PR body: the 16 px favicon crop (shipped as the mark as it ships; no inverted tile), the rail's narrow default CLOSED (an open rail made the shell inoperable at 640 px), a 200 ms slide-out on a narrow first load (the server cannot know the viewport).
- L3/#547: `identity-finish.spec.ts`'s chat stub must keep the row it mints — any lane stubbing chat sessions hits the same once creates are confirmed by re-reading the list.
- L13 finding (SERIOUS a11y, cross-cutting): `components/ui/table.tsx:10`'s scroll container has no `tabIndex` → every read-only table is keyboard-unscrollable (axe `scrollable-region-focusable`); routed to L7's merge-time push on #549 (tabIndex + name + cell); L13 carries a named one-violation carve-out until then; L16 to be told. Also fixed by L13 in its files: `aria-selected` on a `role="button"` row (CRITICAL `aria-allowed-attr`) → `aria-current`.
- Owner steer 12:55 (「chatrail 不能 cover 完？」): the rail carries the DISPOSING verbs — `order-L18-chat-actions.md` (je_review Approve/Reject on the card, attach on the clarify card, Settle on the bank proposal, open questions answerable in the rail); dispatch after #547/#549/#552 merge.
- #552 decisions recorded in the fold: chat archive is LIST-LEVEL only (a running SSE and late turns unaffected) and author-only/one-way for beta; un-archive + a departed colleague's shared thread = owner questions. The counterparty-identifiers door can move a party from the unregistered-name unique to the registration unique (law 59's name-only guard still refuses) — named in its header.
- PR #554 (docs: 裁-191/192) open, single-lane review running.
- **DEPLOY ORDER HAZARD (DB-B #552):** the runtime image carrying `chatRoutes.ts`'s `archived_at` list filter must NOT ship before the web-reads migration is numbered + applied (every session list would 500). So: merge #552 → number → DB ceremony (D1 quiesce on THREE bodies: `_tf_chat_session_update`, `_tf_counterparty_update_0011`, `_persist_statement_core_v2` — quiesce the statement_facts lane for the second file) → THEN runtime v75. Numbering: web-reads LOWER (after 0163/0164/0145), statement HIGHER (after 0098); re-sort the two barrier-map keys in `apps/web/tests/firm-scope-db-pins.test.ts` at renumbering (hash is content-only).
- DB-B not done (routed): `counterparty_aliases_visible` already exists (`0145:960-964`) — the alias reader gap is WEB-side (the registers counterparty panel; L17 or a registers lane); the three wire-able receipt shims (f_a2/f_a3/f_a5) still stubs; the raw `domain_events` grant lets a rank-0 viewer read payloads over PostgREST (wr.9 pins it) — a wall change for DB-C/DB-D; `period_basis` column deferred (shape in the body).
- Lesson (memory at clock-out): a SECURITY INVOKER read must inline its floor with the GRANTED resolvers (`jwt_sub`, `jwt_firm`, `actor_role_rank`, `role_rank(text)`) — calling the internal `_human_ctx` under invoker binds the EXECUTE check to the caller and raises 42501 on every call (review-552 r2 on #552's item-10 fold). Never grant `_human_ctx` to fix it.
- KNOWN ISSUE (review-551 on #551 item 11): NO door can close/dismiss an open question whose document filing was RETIRED — `retire_document_filing` never touches `open_questions`, and `resolve`/`dismiss_open_question` are walled by `_active_document_filing`; dba5 now makes the same orphan block `set_document_kind` too (correct refusal, pre-existing dead end widened by one door). Needs a small door (`dismiss_orphaned_question` or a lift inside `retire_document_filing`) — a DB-D/DB-C item.
- dba8's two load-bearing shapes (where drawer 1 steps aside): an account deactivated mid-FY with zero statements; prior-year statements + a covering reconciliation (`has_statements` has no FY scope). Recorded for the close runbook.
- L16/#557 (`643255a7`, CI green): needs ONE merge-forward after #550 (`clientName={client.name}` at `client-home/client-needs-you.tsx`); the focus-ring census is a UNION count (13 main + #549's banner + #557's carrier → 15 by then) — never take-either; DB gap: `clara.sst_threshold_schedule` has no `clara_authenticated` grant, so the Tax tab's turnover-classification control can only offer the client's own watch groups (a DB-D item: a SELECT grant or a definer read).
- Not yet dispatched but ordered: DB-D H-21 projection (`order-DB-D-h21-projection.md`), CONSENT (裁-186), L9 ops, L10 chatTurn v18, L17 copy sweep (+ the in-dialog refusal threading for 13 wrapper families + V16Cards uuids + clarify-card attach control), DB-C walls, REPORTING H-15, CI-E2E (裁-192).
<!-- end verbatim: lane-roster.md -->
