# Report disposition record — R1-owner-flaws

The per-item disposition of the owner's own UIUX flaws list (36 items, handed 2026-09-04), as measured **2026-09-06 on `main` at `fc39c361`** by a
read-only opus-5 lane under 裁-190. The owner's decisions are recorded in `PROGRESS.md`; this is a
CLOSED record, filed verbatim so the measurement can be re-read against the tree it was taken on.
Every citation in it resolves on that commit. Nothing here is edited after filing.

<!-- begin verbatim: R1-owner-flaws.md · md5 54716882890878a546382e5b9af2c40b -->
# REPORT 1 — the owner's own UIUX flaws list: per-item disposition

- **Source:** `C:\Users\zhant\Desktop\MY OWN UIUX Flaws.txt` (64 lines, handed 2026-09-04)
- **Measured against `main` at `fc39c361`** — every "FIXED" below was READ in the file cited, not taken from a PR title.
- **Date of this pass:** 2026-09-06 (`date` → `Sun Sep 6 01:43:43 MPST 2026`)
- **Serving posture:** every code PR of the repair session (#543…#558) is in runtime **v75** + web Worker **`90c1a5d0`**, deployed 2026-09-05 from `0351f022`; the DB frontier is **171 / `0176`**. #560 landed after the Worker cut but is e2e-only, and #561–#563 are docs. So a fix cited below in `apps/web`, `packages/runtime` or an applied migration **reaches a user today**.
- 36 items: the file's compound lines are split, so several lines yield two rows.

| # | 原话 (verbatim, trimmed) | 一句大白话 | Disposition | Evidence (PR + file:line, or ruling) | What remains / restated form | Queue | 推荐 |
|---|---|---|---|---|---|---|---|
| 1 | 包括那个6 digit and 没有uerflow去continue的 | 收到六位数验证码那一步, 没有路走下去 | FIXED | #544 · `apps/web/components/entry/signup-account-form.tsx:324` renders `<Link href="/auth/confirm">` in the check-email fork; the code form is `apps/web/app/(entry)/auth/confirm/page.tsx` | — | closed | close |
| 2 | card component and其他东西 | 卡片组件和别的东西也有问题 | DRIFT | The tree changed shape: #546/#547/#550/#555/#557 rewrote the onboarding, chat-part, needs-you, document and home cards. `apps/web/components/ui/card.tsx` (the shadcn primitive) was not itself touched | **Restated:** "which card, on which screen, still looks wrong?" — this cannot be dispositioned as written | unqueued | restate |
| 3 | 那个needs review 的界面(text only) | 待办审阅那个界面只有一堆字 | FIXED | #550 · `apps/web/components/firm/needs-you-row.tsx` now renders WHAT/WHY/NEXT/WHEN — client name at `:107`, tier chip `:143`, why line `:155`, `aged_since` + plain age `:114-115,:183` | — | closed | close |
| 4 | 确定我们有利用shadcn(the skill and its mcp)? | 我们到底有没有真的在用 shadcn | PARTIAL | shadcn IS in use: `apps/web/components.json:3` (`style: base-nova`), 15 vendored primitives under `apps/web/components/ui/`. But the journals table was built on the in-house `apps/web/components/common/data-table-card.tsx`, not shadcn's data-table (a deliberate call — no TanStack dependency added) | The register's own census says most of the sixteen primitives this program wants are absent. Adding them is a lane nobody has costed | unqueued | owner decides |
| 5 | 我们的uiux 有没有跟着shadcn mcp and its skill and the vercel agent sdk and the emil animation skills to 设计and work? | 我们是不是照着这几套工具在设计 | PARTIAL | Vercel AI SDK: yes — `packages/runtime/package.json:18` (`"ai": "7.0.77"`, `@ai-sdk/openai` at `:16`). Design tokens + motion: yes — `apps/web/app/globals.css` carries 152 tokens, motion durations/easings at `:356-361`, a reduced-motion block at `:397`. shadcn: see #4 | **No motion library ships** (no framer-motion in either package.json); animation is CSS tokens only. Whether that satisfies "emil animation" is a taste call nobody has ruled | unqueued | owner decides |
| 6 | GO MOBBIN MCP to find the best exemplar of AI AGENTIC product of our niche | 去 Mobbin 找同类 AI 产品的最佳范例来学 | PARTIAL | Done for the two homes: #557's title names the Mobbin-informed spec, and the grounding wave is `docs/plan/active/mobbin-grounding-wave-2026-08-28.md`. The built result is `apps/web/components/firm/firm-home/firm-home-board.tsx` + the seven `client-home/` sections | Not re-run for chat, documents or onboarding — those three trains built from the register's anchors, not from a fresh exemplar study | unqueued | keep |
| 7 | e2e for all uiux upgrage and changed. | 所有改过的界面都要有 e2e | PARTIAL | 15 Playwright walk specs on main under `apps/web/e2e/` — one per train (`home-board-walk`, `journals-table-walk`, `documents-viewer-walk`, `chat-parity-walk`, `interview-walk`, `responsive-shell-walk`, …), each run by its lane under 裁-86 | **No REQUIRED CI browser job exists** — the browser gate is local-only, so a regression can merge unseen | **Q-07** (裁-192 · CB-AE2E-036) | keep |
| 8 | 补齐所有缺乏的UIUX like tax features | 税务那一块的界面要补上 | PARTIAL | #557 · `apps/web/components/tax/TaxWorkbenchPage.tsx:44-46` — the tab now issues one real read (`loadClientSstWatch`) and mounts `SstPanel` + `TurnoverClassificationPanel` (wiring the live `set_turnover_classification`), copy rewritten out of build-log prose | The computation panel is still an honest not-built note (no computation/CP204/Form C object exists). The statutory thresholds cannot be offered: `clara.sst_threshold_schedule` has no `clara_authenticated` grant | **F-01** (thresholds); computation unqueued | keep |
| 9 | ...and GI clock | 那个 GI 时钟也要补 | OPEN-VALID | Searched `apps/web/components/` and `apps/web/lib/`: **not found** — no GI clock component, route or read anywhere. The one DB clock is `clara.statutory_deadlines` (`0139`), live-empty and ungranted; `apps/web/components/firm/firm-home/firm-home-board.tsx:32` says exactly that in a source note | **Blocked on a definition.** "GI" resolves to nothing in the repo. Size guess M (register). A year-end strip is cheap and needs no backend; a statutory calendar needs seeds, a grant and a due-date oracle `0139` deliberately did not ship | unqueued — needs owner ruling | owner decides |
| 10 | ...and all agent chat generative ui | 聊天里 agent 的生成式界面全部补齐 | PARTIAL | #547 · `apps/web/components/parts/PartRenderer.tsx:186` (tool_call status chip), `:267-268` (typed `bank_act` / `bank_pack` cards). 24 of 26 catalog kinds now have a render arm | **Nine kinds still render ids only** — `apps/web/components/parts/PartSummaryCard.tsx:8-11` names them (je_review, doc_review, diff, open_question, bank_recon_receipt, fixed_asset, depreciation_run_receipt, adjustment_run_receipt, staff_advance). Five agent capabilities have no tool or card at all | **Q-10** (tools, ≈1.5) · **Q-11** (readers, ≈1) | keep |
| 11 | 找出所有类似Flaws 谢谢 | 把所有同类毛病都找出来 | FIXED | Done: the session's triage register holds ~100 anchored items across nine areas, unified from this file + issue **#541** (36 defects) + the handover's rows; every row carries a code anchor, a cause and a fix shape | — | closed | close |
| 12 | RESPECT the Design token and deisgn system while DOING all component and animation and motion design | 做组件和动效时要守住设计 token 和设计系统 | FIXED (honored) | Enforced mechanically, not by promise: `apps/web/package.json:13` runs `check-token-contrast.mjs` inside `pnpm lint`; the token set and the motion scale live in `apps/web/app/globals.css:356-361` with a reduced-motion guard at `:397` | Standing instruction, not a ticket. `apps/web/app/globals.css` is over the 500-line write hook and the next writer must split it (**F-06**) | F-06 | keep |
| 13 | autodraft 不是 agent 自动的吗??? | autodraft 不是应该 agent 自己做吗 | FIXED | #556 · `packages/runtime/workflows/autoDraft.v10.ts` (registry repointed; `packages/runtime/tests/h17-autodraft-v10-constraint-map.test.mjs:207` pins it) + migration `0176_counterparty_alias_kind_scope.sql`, **applied** (frontier 171/`0176`). v9 self-refused because a kind-blind alias unique's 23505 was masked as CLR23; v10 maps by exact constraint name | — | closed | close |
| 14 | after 6 digit uixu 没有地方给我去tap the digit | 没有地方让我输入那六位数 | FIXED | Same fix as #1 — #544 · `apps/web/components/entry/signup-account-form.tsx:324`. The code form prefills the address from this browser's own sessionStorage, so only six digits are typed | — | closed | close |
| 15 | and 给我proceedfirm ceretae | 也没让我继续去建事务所 | WRONG (premise) | The firm-create step was always reachable: `apps/web/app/(entry)/auth/confirm/verify/handler.ts:98-104` redirects a confirmed caller to `/signup`, and `apps/web/components/entry/signup-step.tsx:2-3` mounts `SignupDpaForm` then `SignupFirmForm`. What was genuinely missing was only the route TO the code form (#14) | — | closed | close |
| 16 | documents: needs your comfrimation 没有sync after user pressed "comfirm and filed"? and its indeed "Filed to this client" | 按了确认归档后, 那个"需要你确认"没有消失 | FIXED | Both halves. Web: #555 · `apps/web/components/documents/documents-workbench.tsx:62-66` — a refresh EPOCH re-reads the filed cell AND the candidates cell (it used to reload the filed cell only). DB: #551 · `0169_set_document_kind_resolves_classification.sql`, **applied** — `set_document_kind` now resolves `origin='classification'` questions in the same transaction | — | closed | close |
| 17 | "page overlay viewer" havnt buildt?? i want it now | 页面叠加查看器还没建, 我现在就要 | FIXED | #555 · `apps/web/components/documents/document-page-overlay.tsx` (403 lines) — pdfjs-to-canvas for PDFs via `document-pdf-page.tsx`, a plain img for raster, one absolutely-positioned polygon per `page_polygon` locator, malformed geometry skipped rather than guessed | — | closed | close |
| 18 | no more looooog extraction evident in UIUX / extraction text also render as nice doc or md renderer like to read | 抽取出来的文字要排得好看, 不要一大坨 | FIXED | #555 · `apps/web/components/documents/document-extract-panel.tsx:18-19` imports `partitionRegions` + `prettyEnvelope` and mounts `DocumentFactsTable` — three tiers now: a facts table, layout text grouped per page, and the raw envelope behind a disclosure. It used to print up to 20 000 chars of raw JSON in a `<pre>` | — | closed | close |
| 19 | AI agentic 交互with generative UI, 会智能和user 互动, 理清or 出卡片 and socrates way and only 在背后 db 放对的资料 | 开户访谈要像真的 AI 一样一问一答出卡片, 背后把资料写对 | PARTIAL | Cards and typed answers landed: #546 · `apps/web/lib/onboarding/answer-format.ts` (384 lines, a per-item_key table for the nine object shapes, never `JSON.stringify`) + `OnboardingChecklistCard.tsx` + `InterviewRunCard.tsx` progress sync | **The "背后 db 放对的资料" half is NOT built.** `commit_client_onboarding` still projects no interview capture into the client record — identifiers, FY end, facts and bank accounts are all dropped (H-21, XL). The interview is also a fixed frozen script, not adaptive (H-52) | **Q-01** (H-21); H-52 unqueued | keep |
| 20 | onboarding 的卡往下后commit or cancel 后, 上面的东西都还在? | 提交或取消之后, 上面那张卡还留着 | FIXED | #546 · `apps/web/components/clara/OnboardingSettledCard.tsx` — a third settled variant mounted by the same `Loaded` switch in `OnboardingChecklistCard.tsx`, rendering a receipt from columns already read, with the live Commit/Cancel triggers gone | — | closed | close |
| 21 | since client is onboarding 状态没有什么可以看, 直接fullscreen in chat onboarding? or 第一时间zoom in the onboarding chat | 客户还在开户时, 直接把开户对话放大或全屏 | FIXED (as an opt-in) | #546/#557 · `apps/web/components/firm/client-workspace-overview.tsx:81-112` — `ContinueOnboardingCard`, mounted at `:173` only when `status === "onboarding"`, resolving the rail's OWN thread by a read (never a create) and linking to the existing full-screen route; falls back to opening the rail in place when no thread is found | **Deliberately no auto-redirect** — the header at `:77-79` records why: a redirect makes the client's other eight tabs unreachable for the whole of onboarding. If the owner wants forced fullscreen, that is a new ruling | closed unless re-ruled | owner decides |
| 22 | after onboarding , onboarding 的card 都还在?? | 开户完成后那些卡还留着 | FIXED | Same fix as #20 — `OnboardingSettledCard.tsx`. Plus #546's `CLIENT_RECORD_CHANGED` bus so the workspace header stops reading "Status Onboarding" after commit (H-50, `client-workspace-overview.tsx:114-118`) | — | closed | close |
| 23 | Client workspace's home tab — 糟糕透顶 | 客户工作区的首页做得很烂 | FIXED | #557 · `apps/web/components/firm/client-workspace-overview.tsx` rebuilt as a seven-section situation board in ACCOUNTING order (`:10-14`), over the seven new modules in `apps/web/components/firm/client-home/` — identity band, onboarding progress, needs-you, docs backlog, bank, close, last activity. Each section reads for itself so one failed read cannot blank the board | — | closed | close |
| 24 | chatpanel 没有clear/add/delete thread 的功能? | 聊天面板不能新建/清空/删除对话 | PARTIAL | #547 · `apps/web/components/clara/ClaraThreadMenu.tsx` — NEW at `:78`, SWITCH at `:83`. CLEAR is ruled never-build (`:19-22`: the transcript is the audit record, reverse-not-delete) | **ARCHIVE is now buildable and is not built.** The menu still declares it a backend gap at `:23` ("the table has no `archived_at`") — that claim went **stale on 2026-09-05**: `0174_web_reads_and_small_doors.sql:578` adds the column and `:92` mints `clara.archive_chat_session`, both applied. Wiring the control is small | unqueued | keep |
| 25 | chatpanel — after onboarding 后东西都还在? | 开户后聊天里的东西还留着 | FIXED | Same fix as #20/#22 — the checklist card mounted inside `ClaraThreadView` shares the settled face, so the rail thread shows a receipt rather than live Commit/Cancel triggers | — | closed | close |
| 26 | 聊天界面不能生成或提供可下载的 export 文件 | 聊天里没法生成或给我下载文件 | PARTIAL | Only the LIE was fixed. #549 re-cut the copy — `apps/web/messages/en.json:1653` now says plainly that there is no way to request a sandbox export in this build, instead of telling the user to ask Clara. Measured: the `chatTurn_v10…v17` builder chain names no export, sandbox or download tool | **The capability itself does not exist.** A v18 export tool plus the F-A5b PDF render worker are two separate lanes, neither costed into the current queue. Register size: L | unqueued | owner decides |
| 27 | 没有generative chat uiux?? | 聊天里根本没有生成式界面 | PARTIAL | Same evidence as #10 — 24 of 26 kinds render, tool_call now carries a status chip (`PartRenderer.tsx:186`), bank act/pack are typed cards (`:267-268`) | Nine ids-only kinds + five missing tools, as #10 | **Q-10 · Q-11** | keep |
| 28 | 「上傳授權信」和「標為 consent evidence」網頁上有按鈕, 但最後一步「發放同意」(grant_client_egress) 網頁上沒有按鈕 | 客户 AI 授权的最后一步在网页上没有按钮, 真的 beta 客户开不了 AI | PARTIAL | **The owner's code reading was correct and is still correct.** The READ verb landed: #552 · `packages/db/migrations/0174_web_reads_and_small_doors.sql:271` creates `clara.client_egress_state(uuid)`, granted to `clara_authenticated` at `:366`, applied. **Measured on main: ZERO call sites** — `grep -rn "client_egress_state\|grant_client_egress\|clientEgress" apps/web/` returns nothing | **No web surface exists.** 裁-186 re-shaped the ask: consent becomes a FIRM-level declaration at the DPA stage, so the panel to build is the firm declaration + a per-client state readout, not a per-client grant button | **Q-02** (裁-186; size L) | keep — this is the one that blocks a real beta firm |
| 29 | UIUX 全部集中在DPA那个阶段一次过处理, firm同意后, 所有client 在firm 的级数下全部认同 | 同意书集中在 DPA 那一步一次签完, 事务所签了全部客户就算同意 | OWNER-ACT (already ruled) | This line BECAME the ruling: **裁-186** (`docs/plan/active/mohe-grill-rulings-2026-09-04-pm.md:25`), minuted as **ADR-0078** — taken against the recommendation, which had argued MIA By-Law R114.3(b) wants per-client authority. It also carries 裁-187 (every attestation ceremony and maker-checker wall abolished, basic RBAC only) | The ruling exists; the build does not. It is the same lane as #28 | **Q-02** (build) · **Q-03** (裁-188 wall removal) | keep |
| 30 | Activity — agent task的component 没有内容, 没有交互? (是backend缺陷?) only cancel? user dk what to do | 活动页那个 agent task 卡是空的, 只能取消, 用户不知道该干嘛 | PARTIAL | The owner's "是backend缺陷?" guess was RIGHT, and one third is fixed. #550 · `apps/web/components/firm/agent-task-detail.tsx` renders the seven read-but-discarded columns and names the two things it cannot honestly give (a receipt link, an attempt counter) rather than faking them. #557 adds `apps/web/components/firm/firm-activity-feed.tsx` | **Four of the five empty receipt shims are still 0103 stubs.** Only `_agent_receipt_src_f_a4` was re-cut (`0174:538`); `f_a2`, `f_a3`, `f_a5`, `f_a8` have no later `create or replace` anywhere in `packages/db/migrations/`. Until they are wired or declared parked, Activity stays thin | unqueued (E-2's remaining two lanes) | keep |
| 31 | Needs you — 那个agent task 没有更多交互and 细节 让我知道发生什么事情? BAD UIUX | 待办里的 agent task 没有细节也没法互动 | FIXED | #550 · `apps/web/components/firm/needs-you-row.tsx` — every row now carries what it is (checked label, no raw key path), why (`:155`), the next act or a deep link, and when (`:183`, `aged_since` as an instant plus a plain age). The detail drawer is `agent-task-detail.tsx` | — | closed | close |
| 32 | Sweep run 是什么?? Bad UIUX | 什么叫 sweep run, 看不懂 | FIXED | #549/#550 · `apps/web/messages/en.json:2805` — a plain definition now leads the panel: one unattended pass over the firm's filings in a time window, recording expected/drafted/skipped/refused, and "a sweep never approves anything: everything it drafts still comes to a person" | The panel also states honestly at `:2811` that acknowledging a finalized run is not reachable (no surface hands a person a run id) | unqueued (the ack path) | close |
| 33 | journal clarification 的agent text is JSON, cant have better uiux shown?? | 日记账澄清那段 agent 文字是 JSON, 能不能好看点 | FIXED | #548 · `apps/web/components/journals/interruptions-panel.tsx:47` now reads `question.question` (what every live writer actually writes) with the old `question.text` kept only as a fallback, plus `context` and `framing` rendered at `:113`. The raw dump survives only as the fail-closed arm | — | closed | close |
| 34 | 没有一个UIUX table for journal entry? (go see DataTable shadcn) | 日记账分录没有一个表格界面 | FIXED | #548 · `apps/web/components/journals/journal-entries-table.tsx` (346 lines) — sort, three filters and page slicing over hydrated rows, built on `apps/web/components/common/data-table-card.tsx` | Built WITHOUT TanStack deliberately (no new dependency). If the owner wants shadcn's own data-table, that is a re-decision, and a human-facing journal number would need a migration | unqueued | close |
| 35 | Firm Home: Go mobbin mcp and design a dahboard for it and with access to other pages of the product | 事务所首页要照 Mobbin 做成一个能通往各页的仪表板 | FIXED | #557 · `apps/web/app/(firm)/page.tsx:1` mounts `FirmHomeBoard`; the board reads `caller_context`, `list_review_queue`, `clients`, `agent_tasks_visible` and the firm timeline, and every tile links into the surface owning its verb — `firm-home/` holds the scoreboard, oldest-waiting list, timeline section and the Clara-working tile. The old body was a header and a stale "never built" note, now deleted rather than softened | — | closed | close |
| 36 | Agent不能autopost 吗???? | agent 不能自动过账吗 | PARTIAL | **It can, and it does.** `packages/runtime/workflows/autoDraft.v10.ts:144` settles a run as `"posted"` on the post branch, and `:90` declares `posted` in the outcome union — layer-1 autonomy posts unattended today, serving in v75 | **What is off is the CLOCK, not the capability.** 裁-165 ships beta with the G1 cadence wake sources DISABLED (`docs/plan/active/mohe-grill-rulings-2026-09-04.md:180`), so nothing schedules an unattended pass — the six `held_outbox` rows are that posture being loud, not a fault. **Restated:** "should beta turn on the automatic clock (autonomy layer 2)?" | **C-05** / G1 PR-2 | owner decides |

## (a) Counts per disposition

| disposition | count |
|---|---|
| FIXED | 19 |
| PARTIAL | 13 |
| OPEN-VALID | 1 |
| WRONG | 1 |
| DRIFT | 1 |
| OWNER-ACT | 1 |
| STALE | 0 |
| OBSOLETE | 0 |
| **total** | **36** |

FIXED: 1, 3, 11, 12, 13, 14, 16, 17, 18, 20, 21, 22, 23, 25, 31, 32, 33, 34, 35.
PARTIAL: 4, 5, 6, 7, 8, 10, 19, 24, 26, 27, 28, 30, 36.

## (b) Items I was NOT sure about

- **#2 (card component and 其他东西)** — I could not identify which card on which screen. Five separate lanes rewrote card surfaces this session, so any disposition I picked would be a guess about the owner's referent. Marked DRIFT and restated rather than closed.
- **#5 (emil animation skills)** — I verified the motion TOKENS exist and are wired with a reduced-motion guard, and that no motion library ships. I did NOT measure per-surface motion quality in a browser, so "does it follow the animation skill" is answered only at the token layer.
- **#19 (socrates way)** — the interview asks a fixed frozen script (`interview.v2.questions.ts`), which is not adaptive. Whether that fails the owner's "socratic" bar is a taste call I did not make; I dispositioned only the measurable half (captures are not projected into the client record).
- **#21 (fullscreen vs zoom)** — the build chose an opt-in escalation link and refuses auto-redirect, with a reason in the source. That is a design ruling the owner has not confirmed, so "FIXED" here means "the ask was built one way", not "the owner agreed".
- **#26 vs #10** — I read "可下载的 export 文件" as files the chat produces, matching the Reports tab's export notice. If the owner meant downloading the transcript itself, the disposition changes and nothing on main addresses it.
- **#24 (archive)** — PROGRESS's known-issues section states that `apps/web` "already ships readers for" `chat_sessions.archived_at`. I grepped `apps/web/lib` and `apps/web/components` and found the column named only in comments; `apps/web/lib/firm-admin/chat-sharing.ts:47` selects seven named columns and not that one. I am reporting my measured read and flagging the disagreement rather than resolving it.

## (c) 三件最需要老板拍板的事 (大白话)

1. **客户 AI 同意的界面 (第 28、29 项) — 这条挡住真的 beta 客户。** 你自己读代码读对了: 最后一步「發放同意」网页上没有按钮, 到今天还是没有。数据库那道读门 (`client_egress_state`) 上个月 5 号已经上线了, 差的只是网页。你已经用 裁-186 定了方向 —— 事务所在签 DPA 那一步一次签完, 底下所有客户自动算同意。**现在要决定的是: 这个界面是不是排在所有队列的第一个 (Q-02)。** 不做的话, 一个真的 beta 事务所自己开不了 AI, 只能等我们后台帮他开。

2. **GI clock 到底是什么 (第 9 项) —— 没有你的定义, 这条永远建不了。** 我在整个前端里搜过, GI clock 在这个仓库里对应不到任何东西。三种可能, 成本差很远: (甲) 财年结束倒数条 —— 最便宜, 后端什么都不用加, 一两天; (乙) 法定申报日历 (SST、CP204、Form C 的到期日) —— 要种子数据、一道读门, 还要一个算到期日的引擎, 那个引擎 `0139` 当初是故意没做的; (丙) SST 门槛时钟 —— 中等, 但现在连门槛表都还没开放给前端读 (F-01)。**你说是哪一种, 我就能开工。**

3. **聊天里的生成式界面还差多少, 值不值得现在补 (第 10、27 项)。** 现在 26 种卡片里有 24 种会画, 但其中 **9 种只显示一串 id**, 用户看不到内容; 另外还有 5 件 agent 会做的事在聊天里完全没有工具也没有卡 (送文件进来、开始开户访谈、加固定资产、经常性调整、开始结账)。补齐大概两个半单位的工作量 (Q-10 加 Q-11)。**要现在补, 还是先把缺的产品面 (税务、GI clock) 建起来?** 两边抢的是同一批人手。
<!-- end verbatim: R1-owner-flaws.md -->
