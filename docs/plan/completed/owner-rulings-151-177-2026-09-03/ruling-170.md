# 裁-170 — OD-20: BELCORT is NOT SST-registered (owner's statement on the record, 2026-09-03); Stripe Tax stays OFF for the whole beta as a consequence of that fact; the real-money switch ceremony re-asks the status before any tax line

**Ruled 2026-09-03 ≈20:4x MYT (shell clock; owner, AskUserQuestion), verbatim:** 「BELCORT 目前未註冊 SST」.

- The Wave-G checklist rule (`docs/ops/wave-g-setup-checklist.md:181-183`: Stripe Tax only once
  BELCORT's own SST registration status says so; no tax line before registration) now rests on a
  stated fact, not an omission.
- Beta: sandbox, MYR 0 (裁-126/148) — no tax amount exists to compute; Stripe Tax OFF; recorded in
  the FS-11 as-run and the launch-sitting record.
- The real-money switch ceremony (live mode + KYB, 裁-125/126; after the pricing sitting 裁-58 and
  the tier tranche 裁-144) carries a line: "re-confirm BELCORT's SST registration status; if
  registered by then, enable Stripe Tax and verify the service-tax rate + invoice format before the
  first priced checkout."

**Record.** Ledger `-09-03` + digest row; the checklist line gains the dated fact (T-I).
