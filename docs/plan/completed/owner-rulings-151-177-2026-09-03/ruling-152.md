# 裁-152 — OD-3: the owner MINTS the pepper and the auth-wall service token ONCE at FS-10 S8; the same bytes go to clara-runtime at FS-11 step 12, where the hash-equality proof runs

**Ruled 2026-09-03 ≈19:5x MYT (owner, in chat): 「照建議」.**

- `CLARA_RATE_WALL_PEPPER` and `CLARA_AUTH_WALL_SERVICE_TOKEN` are generated by the owner locally
  (random, ≥32 bytes each, e.g. `openssl rand -base64 32`), kept in the owner's password manager,
  set on `clara-web` env-to-env at FS-10 S8. Never pasted in chat; the lead never sees them.
- FS-11 step 12 sets the SAME BYTES on `clara-runtime`; the hash comparison (checklist `:132-133`)
  runs THERE — the first moment it has two operands.
- FS-10 records: both names present in `wrangler secret list --name clara-web` + a dated as-run
  line naming them as the minting values FS-11 reuses verbatim.
- The alternative (defer both to FS-11; checkout refuses fail-closed between the ceremonies) was
  offered and not taken.

**Record.** Ledger `-09-03` + digest row at the final truing; FS-10 prep D2 and FS-11 prep P-17/R-5
carry the outcome before those ceremonies open.
