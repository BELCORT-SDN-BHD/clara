# 裁-173 — OD-23: the close housekeeping — the two untracked repo-root PNGs are DELETED (done at the ruling); the locked worktree shells and the WSL vhdx compaction are the OWNER's elevated-shell acts at the pause window, with the exact commands supplied by the lead; the teardown census is a WALK, never a list

**Ruled 2026-09-03 ≈20:5x MYT (shell clock; owner, AskUserQuestion): 「照建議」.**

- `fs2-01-login.png` and `fs2-02-login-focus-email.png` (untracked, FS-2-era login screenshots,
  superseded by the walk's own receipts; the repo is PUBLIC, 裁-135) — removed from the working
  tree at the ruling; `git status` no longer lists them.
- At the pause window (after the launch sitting + the final truing), the owner runs in an
  elevated PowerShell: the removal of the locked worktree shells (enumerated by a walk of
  `.claude/worktrees/` + `git worktree list --porcelain`, reparse points unlinked FIRST per the
  09-02 lesson) and `Optimize-VHD` (or `diskpart compact vdisk`) on the WSL distro's `.vhdx` — the
  lead writes the exact commands into `housekeeping-worktrees.md` and hands them over in chat; the
  owner runs them; the census after is a walk.
- The lead's own share: every removable worktree (merged lanes), `packages/runtime/node_modules/.cache`
  + `.nitro`, rigs by exact name, the conductor + monitors torn down — all by a walk, receipted in
  the final truing.

**Record.** Ledger `-09-03` + digest row; `housekeeping-worktrees.md` gets the owner's command block.
