# 裁-172 — OD-22: at the FINAL truing, a replacement subject for the DR STRICT `4.9` cross-schema parity probe is named from the post-reset estate (a durable run the walk itself produced); if none qualifies, `4.9` is recorded UNPROVEN IN THE FIELD with a Known-issues row — never a silent skip

**Ruled 2026-09-03 ≈20:5x MYT (shell clock; owner, AskUserQuestion): 「照建議」.**

- Candidates after FS-11: the `clientOnboarding_v4` run from Product-walk item 1, a `chatTurn_v17`
  run from item 5, a render job from item 6 — any durable run with BOTH a `workflow.workflow_runs`
  row and its clara-side projection (`clara.agent_tasks` / the run's clara carrier).
- The final truing writes the subject's ids (never the pinned ids) into `docs/ops/DR-full-drill.md`'s
  `4.9` line and `packages/db/scripts/dr-verify-checks.mjs`'s expectation if it hard-codes the
  canary (`:399,415`) — if the script hard-codes, that is a code change → a Backlog row naming the
  file, not a hand edit on launch night.
- "N/A without a subject" was offered with its cost and not taken.

**Record.** Ledger `-09-03` + digest row; the DR drill doc line + the Known-issues row at the final
truing.
