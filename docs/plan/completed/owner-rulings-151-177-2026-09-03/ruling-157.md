# 裁-157 — OD-8: NO maintenance page during FS-11's reset; the as-run records the window (start/end timestamps) and the expected errors

**Ruled 2026-09-03 ≈20:2x MYT (owner, AskUserQuestion): 「照建議」.**

- With 裁-156 (no soak), FS-11 opens as soon as FS-10's as-run is written — the same sitting if the
  owner has the hours.
- During FS-11 (runtime stopped; `DROP SCHEMA clara CASCADE`; fresh apply `0001`…`0164`, 159 files,
  no delta apply; seed; the nine evaluators; secrets), every route on `app.clarabook.com` errors.
  No holding page is configured on Cloudflare; the as-run records the window's start and end and
  states the errors were expected. Ground: the first invited firm signs up AFTER FS-11; a holding
  page is an unordered, unproven surface added at the worst hour.
- The "simple maintenance page" alternative was offered with its cost and not taken.

**T-G is now ANSWERED:** the FS-10↔FS-11 posture = 裁-156 (switch + delete, gated by S21, no
soak) + 裁-157 (no maintenance page, a recorded window). Both go into the as-runs and the Wave-G
checklist at the final truing.

**Record.** Ledger `-09-03` + digest row at the final truing.
