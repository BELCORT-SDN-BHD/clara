# 裁-165 — OD-15b: beta live ships the FIRST autonomy layer only (the automatic draft belt + chat-driven agent acts, human-disposed); the SECOND layer — the G1 cadence wake sources (`bank_agent`, `close_prep`) — stays OFF; G1 PR-2 is a Backlog row, built before 上市

**Ruled 2026-09-03 ≈21:1x MYT (owner, AskUserQuestion): 「照建議」.**

**Ground (measured):** `PROGRESS.md:52` — the G1 engine (`0133`, #349) and both wake bodies
(`bankAgent_v1` + `closePrep_v1`, #437) are MERGED; **NOT deployed, both switches OFF — the 裁-40
flip is inert until G1 PR-2 builds the two PRODUCERS** (`bank_agent_run_due` + the `bank.agent_due`
event/taxonomy row + a `leader.mjs` cadence gate; the `close_prep` task producer) **and the eight
deferred DB items** (task-bound bank credential, in-tx running check, CAS settle,
`all_writes_refused`, DB-side fault latch, `call_kind` values, `login_pool` truing) + the three
Codex-r6 LOWs; `PROGRESS.md:410` — `bank_agent_due_claims` has no retention path, owed before F-A3
enables the source. The v71 deploy record confirms both `wake_engine_sources` disabled by design.

**What beta has (layer 1, LIVE, walked at Product-walk items 3–5):** statement upload → the
matcher + auto-draft belt on the leader's cadence → drafts in the Journals queue → the human POSTS
or REFUSES; chat as the execution surface (`chatTurn_v17`): coding, close-prep, reports — the agent
proposes, the human disposes (law 71).

**What beta lacks (layer 2):** unattended per-client cadence wakes. **Backlog row (owner · next
step · ruling):** "G1 PR-2 — the two producers + the eight deferred DB items + the retention path
for `bank_agent_due_claims`; a DB+runtime train under the full ladder with a D1 window and a
ceremony; then the 裁-40 flip through the G1 operator door; before 上市" — 裁-165.

**Walk consequence:** the Product-walk's agentic section (裁-164) records layer 2 as "OFF by
ruling 裁-165", never as a failure.

**Record.** Ledger `-09-03` + digest row at the final truing; PROGRESS Backlog row at the final
truing (ONE-AUTHOR).
