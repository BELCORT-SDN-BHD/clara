# 裁-164 — OD-15: the walk's denominator = the eleven enumerated milestones + the owner's product walk (FS-11 step 16), the honest count recorded; AND the product walk MUST exercise the agentic paths — auto-post, bank reconciliation on a bank-statement upload, chat as the execution surface for accounting — to prove the backend against the product vision

**Ruled 2026-09-03 ≈21:0x MYT (owner, AskUserQuestion), verbatim:** 「照建議， and agentic 能auto 的東西like autopost and bank reconcialition due uplaod bankstatembt and using chat for all accoubting execution 也是要test ，確保其我們的backend 沒有大問題in product vision/objectives and agentic ambitious」.

**Part 1 (照建議).** "Sixteen steps" is re-cut wherever it appears to the enumerated list
(`docs/plan/active/frontend-sprint-handoff-2026-08-31.md:287-291` + 裁-83's milestones) + FS-11
step 16's product walk; the as-run records the enumerated count; nothing is invented to reach
sixteen (rubric IT-2: an exact assertion per acceptance cell).

**Part 2 (the owner's addition — the AGENTIC walk).** The product walk (FS-11 step 16, the
Wave-G checklist's "Product walk" section) is extended with an explicit agentic section, each item
with its expected observation and its honest "dark for beta" status where a surface is absent:
- (a) **Bank statement upload → intake → reconciliation** — upload a real-shaped statement for a
  seeded client; observe the document pipeline's parts and the reconciliation proposals reaching the
  chat / workbench; the human disposes on a card or in the workbench (the DB owns the numbers).
- (b) **Chat as the accounting execution surface** — drive at least: an onboarding "Do", a coding /
  journal proposal, a close-prep step (the F-A4 close-prep lane, rung A8), a report render, through
  `chatTurn_v17` on the deployed runtime, each proposal disposed by the human.
- (c) **Auto-post / automatic wake** — the G1 wake sources (`bank_agent`, `close_prep`) ship DISABLED
  and are an OPERATOR flip (BELCORT `is_operator`, FS-11 step 14). Whether the walk flips them ON to
  observe one automatic wake, and whether beta ships with them ON, is **OD-15b** (asked next, after
  grounding what the flip does).
- (d) Witness activation and the FA / adjustment authorities have NO web surface at this tip (dark
  in the UI; the doors exist in the DB) — recorded as such in the walk, not walked through SQL.
The walk's verdict on the agentic section is a go/no-go input at the launch sitting: a backend
defect found here is a launch blocker if it breaks (a) or (b); (c)/(d) gaps are Backlog rows.

**Record.** Ledger `-09-03` + digest row; the checklist's Product-walk section gains the agentic
items (T-I) at the final truing; FS-11 prep §12 item 10 re-cut.
