# 裁-169 — OD-19: 裁-102's wall on `/signup`'s Supabase-sent code = the two platform rate limits, ACCEPTED in writing by their numbers — both numbers READ BACK by Management API at the walk (never from memory or a screenshot), written into the as-run and the Wave-G checklist; the owner accepts the numbers as read

**Ruled 2026-09-03 ≈20:4x MYT (shell clock; owner, AskUserQuestion): 「照建議」.**

- Number 1: the Resend plan's daily/monthly cap (read from Resend's account API/dashboard at the
  walk, quoted with the plan name).
- Number 2: the Supabase Auth email rate limit as raised by the owner on 2026-09-03 (the value was
  never stated anywhere) — read by the Supabase Management API (`/v1/projects/{ref}/config/auth`,
  the rate-limit fields) at the walk; the default mailer's 2/hour does NOT apply once custom SMTP
  is on.
- Security-pass line 6 (as re-cut by 裁-146) is ticked against those two read values, not against
  a number that does not exist (the false-measurement class).
- Tripwire stated to the owner: if the read-back value is disproportionate (hundreds per hour),
  the walk stops and asks whether to lower it before accepting.
- The "build our own server-side wall" alternative was offered with its cost and not taken;
  裁-102 closes as SUBSTITUTED by 裁-169, not repealed.

**Record.** Ledger `-09-03` + digest row; the checklist's signup-gate section gains the two
read-back lines (T-I) at the final truing.
