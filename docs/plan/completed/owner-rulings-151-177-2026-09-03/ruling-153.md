# 裁-153 — OD-4: the four security-pass cutover lines are EXPLICITLY deferred at FS-10, each to its FS-11 folded step; the 0161→0163 truing rides

**Ruled 2026-09-03 ≈20:0x MYT (owner, AskUserQuestion, after one 大白话 re-explanation): 「照建議」.**

- `docs/plan/active/security-pass-2026-09-02.md:541-593` lines 3 · 4 · 5 · 7 are NOT ticked at
  FS-10; the as-run writes beside each "deferred to FS-11 step N": line 3 → **step 12** (the C-5
  secrets, 裁-152); line 4 → **step 11** (the two NOLOGIN→LOGIN flips); line 5 → **step 6**
  (`acl-baseline.sql` on the live project — `ACL baseline verify: OK`, the eleven-role roster
  `usage_public = f`, `clara_auth_wall` holding no `public` USAGE); line 7's walk half → **step 13**
  (one sandbox MYR 0 checkout, 裁-148) — its DPA-read half is already MET on the tree.
- The other seven lines are ticked at FS-10 as written.
- Ground stated to the owner: nobody signs up between FS-10 and FS-11 (the first invited firm is
  after FS-11), so "before the origin takes applicants" is discharged at FS-11's close.
- **Truing T-B rides:** items 4 and 5 cite `0161` (Q-D6); the auth-wall role pair is minted by
  `0163_checkout_gate_c3_folded_door.sql`; same defect at `packages/runtime/lib/checkout-pools.mjs:45`
  (a code comment — docs-only PR carries the doc lines; the comment rides the next code PR touching
  that file, or the fix-chat-proxy PR if the reviewer accepts a one-line comment rider — decide at
  arming, never a separate code PR for a comment).
- The alternative (re-order FS-11 before FS-10) was offered with its cost and not taken.

**Record.** Ledger `-09-03` + digest row at the final truing; FS-10 prep D3 carries the folded
step numbers (its own text has the pre-fold numbering — corrected in the as-run).
