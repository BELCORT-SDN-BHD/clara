# 裁-168 — OD-18: the two `clarabook-frontend` recut PRs (裁-64②'s `--input` token value; R3 §9's focus-ring founder amendment) are DEFERRED — a dated `PROGRESS.md` Backlog row naming the OWNER as the actor

**Ruled 2026-09-03 ≈20:41 MYT (shell clock; owner, AskUserQuestion): 「照建議」.**

- Both PRs are the owner's own, in the design-authority repo, outside every lane's write
  boundary; they change no shipped behaviour here (`apps/web/app/globals.css:304` already carries the
  lawful `--input` value via #515).
- Backlog row (owner · next step · ruling): "open the two recut PRs in `clarabook-frontend` —
  裁-64② (`--input`), R3 §9 (focus ring) — on a date the owner picks; until then the design law
  drifts from the shipped app and any future port re-imports the drift" — 裁-168.
- 裁-167's note rides the same row: if the design repo later implements token contract §5.2,
  `apps/web` follows.

**Record.** Ledger `-09-03` + digest row + the Backlog row at the final truing (ONE-AUTHOR).
