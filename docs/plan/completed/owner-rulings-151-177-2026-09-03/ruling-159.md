# 裁-159 — OD-10: BELCORT is re-minted after the reset through the product's own self-serve door (route a); `is_operator` follows at FS-11 step 14

**Ruled 2026-09-03 ≈20:3x MYT (owner, AskUserQuestion): 「照建議」.**

- FS-11 step 13: the owner walks `https://app.clarabook.com/signup` as an applicant — account,
  the six-digit code (this IS the Mail certification, 裁-146 point 3, when done at a non-team
  address), the DPA signature, ONE sandbox MYR 0 checkout (裁-148) — which mints BELCORT.
- FS-11 step 14: `is_operator` via `docs/ops/g1-operator-firm-ceremony.md` (refuses on zero
  BELCORT rows, hence AFTER step 13).
- Route (b) (hand-made `clara.users` row + admission token + `create_firm` + a manual Supabase
  auth-user step) was offered with its cost and not taken.
- Rider: the walk address must be never-before-registered (OD-12 decides the purge).

**Record.** Ledger `-09-03` + digest row at the final truing; FS-11 prep D-1 carries it.
