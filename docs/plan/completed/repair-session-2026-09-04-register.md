# Repair session 2026-09-04 — the condensed register

The lead's condensed register of the #541 beta handover's items as triaged on 2026-09-05 — id ·
status · size · anchor · cause · fix · needs · open questions — grouped by area, filed here
byte-verbatim from the session scratchpad (historical label: register-condensed.md). Dispositions and the queue
order the session actually ran live in `PROGRESS.md`, which governs on any divergence from what
is stated below. Filed under `completed/` as a closed record of what was triaged; the live queue
and its order are `PROGRESS.md`'s Backlog, which governs.

<!-- begin verbatim: register-condensed.md · md5 5274659dc2d7de6e9223d2015acff263 -->
## A-entry-auth-checkout
- **H-35 / CB-AE2E-006 · confirmed · M** — check-email card promises "the next screen" but renders no /auth/confirm link; resend copy promises a send that always refuses. Anchor: `apps/web/components/entry/signup-account-form.tsx:253`. Cause: The check-email fork renders only a /login link; resend defaults to an unavailable stub and no /api/auth-wall/resend route exists anywhere in the runtime. Fix: Add a primary Link to /auth/confirm in the check-email fork with a new message key; retune the two resend promises; separately build POST /api/auth-wall/resend reusing claim/settle_confirmation_attempt and widen showResend. Needs: existing claim/settle_confirmation_attempt; a new resend route for part (iii). OQ: Does a repeat signUp actually re-send the code? Should the confirm link render on the duplicate-account arm?
- **CB-AE2E-007 · backend_gap · L** — DPA signed state cannot hydrate; no read door reports whether the caller already signed the current version. Anchor: `packages/db/migrations/0163_checkout_gate_c3_folded_door.sql:328`. Cause: get_current_dpa_document returns the document with no caller predicate, and dpa_signatures is force-RLS with no application-role grant, so signed state is unreadable. Fix: New migration adding clara.get_own_dpa_signature() SECURITY DEFINER, jwt_sub-scoped, joining on the composite (version, body_sha256); add dpa-signature-reads plus a server wrapper; seed SignupDpaForm's render from an alreadySigned prop. Needs: NEW clara.get_own_dpa_signature(), granted to clara_authenticated. OQ: When a signature is against a superseded version, does the person re-sign the new text?
- **#541 stage 7 · confirmed · S** — /pending's pending and checkout_open cards both say "nothing more for you to do" beside their own next-step control. Anchor: `apps/web/messages/en.json:1577`. Cause: FS-4 C-6 added the controls without retuning the descriptions that predated them; the paid state already carries the honest wording. Fix: Rewrite Pending.pending.description and Pending.checkout_open.description plus the checkout_open banner to match paid's shape, then add a pending-a11y cell asserting no state that renders a control says "nothing more to do". Needs: none. OQ: none.
- **#541 stage 2 · confirmed · S** — Password policy is stated nowhere before input on signup or invite-accept, and both minLength courtesies sit below the reset face's twelve. Anchor: `apps/web/components/entry/signup-account-form.tsx:310`. Cause: Three password surfaces, only the reset face states the rule; the README obligation to keep both minLength sites aligned with the server minimum was never honoured. Fix: Mint lib/auth/password-policy.ts with PASSWORD_MIN_LENGTH=12 and one shared Auth.passwordPolicy key; repoint all three inputs and render the hint between Label and Input; add a census cell; update the README. Needs: none. OQ: signup-keyboard.test.tsx pins minLength 8; has the hosted project actually been set to 12 plus HIBP?
- **CB-AE2E-003 / H-38 · confirmed · M** — Stripe key-class gate absent, a live-class secret accepted silently, and the Checkout Session carries no customer_email. Anchor: `apps/web/lib/checkout/stripe-session.ts:212`. Cause: The only secret validation is a non-empty test, no key-class check exists estate-wide, and ServerSession carries subject only so no address reaches checkoutSessionForm. Fix: Refuse at request time when the key class disagrees with a declared deployment mode, plus a startup assertion; widen ServerSession with the verified email claim from the same getClaims result and set customer_email only when present. Needs: no migration; a new required deployment-mode env var. OQ: Which env var declares Stripe mode? Adding customer_email 400s a same-key retry inside the idempotency window.
- **CB-AE2E-031 · not_reproduced · S** — Brand casing is filenames, hostnames and a ruled glyph transform, not inconsistent user-facing copy. Anchor: `apps/web/components/entry/brand-lockup.tsx:134`. Cause: All 51 lowercase hits are filenames, hosts or repo names; the visual lowercase is text-transform under 裁-137 with the DOM text still ClaraBook. Fix: No defect. Optional follow-up: convert the twelve prose strings hardcoding the literal brand to ICU interpolation of Brand.productName, replacing the enumerated roster with a no-literal-outside-productName rule. Needs: none. OQ: Which surface did the tester see? The census covers apps/web only, not the hosted mail templates.
- **CB-AE2E-001 / C-06 · backend_gap · XL** — Beta Terms have no acceptance evidence; dpa_documents has no kind discriminator so a Terms signature has nowhere to live. Anchor: `packages/db/migrations/0158_checkout_gate_c1_dpa.sql:120`. Cause: dpa_documents has no kind column and its currency index permits one current row table-wide; sign_dpa and both checkout consumers assume a single document. Fix: One migration adding kind to documents and signatures, re-keying currency and signature uniqueness per kind, widening get_current_dpa_document, minting a kind-carrying sign_dpa, adding kind='dpa' to open_checkout_intent and claim_paid_firm, seeding the Terms row. Needs: ALTER both tables, CoR four doors, seed one terms row. OQ: Does checkout require both kinds signed? Seed a Terms body still carrying 27 unresolved lawyer markers?

## B-onboarding-interview
- **H-26 / H-27 / CB-AE2E-008 / owner: onboarding cards · confirmed · L** — Structured capture answers render as "[object Object]" on the checklist and as raw JSON in the interview thread. Anchor: `apps/web/components/clara/OnboardingItemRow.tsx:107`. Cause: Nothing in apps/web knows a plan-item answer's shape; the row String()s a jsonb object and the thread JSON.stringifies it, while the runtime's prose echo is dropped for client scope. Fix: New lib/onboarding/answer-format.ts with a per-item_key table for the nine object shapes and an ordered key:value fallback that is never JSON.stringify; wire it at both row sites and resolution-history; fold markers.activity for client scope. Needs: none. OQ: The thread fix must land with the formatter or seeded and activity texts duplicate; badge an unverified registration?
- **CB-AE2E-023 / owner: onboarding card 都还在 · confirmed · M** — After commit the whole checklist plus live Commit and Cancel triggers keep rendering, with no completed summary or receipt. Anchor: `apps/web/components/clara/OnboardingChecklistCard.tsx:216`. Cause: Visibility keys on plan existence and never on plan or client state, so the door row renders unconditionally and both triggers open dialogs the DB can only refuse. Fix: Add a third settled Loaded variant when plan.state is not open, rendering a receipt from columns already read (committed_at, committed_by, commit_attestation, review_maker) with the item list collapsed; keep ApplyStandardChartControl reachable. Needs: none. OQ: Does the settled card stay mounted forever, and do committed plan items stay amendable?
- **H-50 · confirmed · M** — Client workspace header keeps reading "Status Onboarding" after commit until a hard reload. Anchor: `apps/web/lib/firm/use-async-read.ts:106`. Cause: useAsyncRead reads once with deliberately empty deps, and the commit fires from the rail, a sibling React subtree with no shared provider, event or router path. Fix: Add a CLIENT_RECORD_CHANGED window CustomEvent modelled on lib/command/bus.ts, emitted from the card's commit and cancel success paths and subscribed by ClientWorkspaceOverview to call reload; the same subscription fixes client-register-list. Needs: none. OQ: Cancel archives the client from the same card, so the Home tab is stale in the other direction too.
- **H-28 · confirmed · S** — Checklist N/N counter is frozen at the mount snapshot while the interview run writes items. Anchor: `apps/web/components/clara/OnboardingChecklistCard.tsx:163`. Cause: The interview half polls every three seconds; the checklist reads through useHydratedPart whose mount effect never re-fires, and onPlanChanged fires only at terminal. Fix: Widen InterviewRunCard's terminal-sync effect into a progress-sync effect keyed on the run's monotonic pendingPark.parkIndex, calling onPlanChanged when it advances; filter interview_run out of the counted items so the header stops reading 1/1. Needs: none. OQ: none.
- **H-29 · backend_gap · M** — The CoA apply row reads "undecided" while the plan is open because the DB read only sees committed plans. Anchor: `packages/db/migrations/0156_coa_apply_template.sql:1082`. Cause: coa_chart_state's dec CTE filters p2.state='committed', so an open plan's answered decision returns no row and the CASE falls through to undecided. The reported field-name cause is wrong. Fix: CREATE OR REPLACE coa_chart_state dropping the hard committed filter and ordering committed first; add seed_decision_plan_state to the returned jsonb so the card can say "decided in the interview, not yet committed". Needs: CoR clara.coa_chart_state, stable sql, no signature change. OQ: Does 裁-23's "after the client is created" mean after begin or after commit? A chart applied pre-commit then cancelled?
- **H-30 · confirmed · S** — The apply-chart dialog can exceed the viewport with no scroll container, putting Confirm out of reach. Anchor: `apps/web/components/ui/dialog.tsx:69`. Cause: DialogContent caps width twice and height never; the centred fixed popup overflows symmetrically and no ancestor scrolls, so header and footer are both unreachable at once. Fix: Add max-h-[calc(100dvh-2rem)] and grid-rows-[auto_minmax(0,1fr)_auto] to the popup, export a DialogBody wrapper carrying min-h-0 overflow-y-auto and adopt it in OnboardingDoorDialog; keep the close button outside the scrolling region. Needs: none. OQ: Should the cap be a shared --dialog-max-h token, given compose-dialog and correction-wizard want the same fix?
- **H-52 · backend_gap · L** — The interview asks for an SST number after sst_regime is not_registered, and a skip is accepted with no confirmation echo. Anchor: `packages/runtime/workflows/interview.v2.questions.ts:84`. Cause: The sst_no segment declares no appliesTo so it always asks; askAndConfirmSegmentV2 returns skipped before validation and isSkip matches the empty string. Every body is frozen. Fix: Ship interview.v4.questions.ts mapping V3 with an appliesTo on sst_no, an interview.v3.core.ts whose successor parks on a skip confirmation, and clientOnboarding.v5.ts as a byte copy with two import swaps plus a registry repoint. Needs: no DB verb; new frozen _vN closures and a registry repoint. OQ: Should a confirmed skip write a declined plan item? Should the empty string stop counting as a skip?
- **owner: fullscreen in chat onboarding / zoom in · design_task · M** — Auto-escalate a client in onboarding status to the full-screen Clara thread, or zoom the rail. Anchor: `apps/web/components/clara/ClaraRail.tsx:62`. Cause: No route or component anywhere branches on client.status; the rail is a fixed w-80 with only a binary open state, and the thread id is resolved client-side. Fix: Do not auto-redirect. Render an opt-in escalation card on the Home tab when status is onboarding, emitting the existing focusRail event or resolving a thread id for a real Link; add a width variant driven by a claraThreadStore flag. Needs: none. OQ: Fullscreen or a wider rail? If fullscreen, does it replace the workspace for the whole onboarding?
- **H-51 / CB-AE2E-024 · confirmed · M** — /clients has no Add client control; the only entry points are the palette's Do row and the firm-altitude rail card. Anchor: `apps/web/components/firm/client-register-list.tsx:83`. Cause: The register page renders a header and a read-only table only, so the one page a person visits to add a client is the one surface with no affordance. Fix: Add one primary Add client control above the table reusing the palette path, permittedDoActions plus runDoAction for beginClientOnboarding, inheriting the 裁-141 pre-filter and the drift guard, then reload the register read on success. Needs: none. OQ: Should the register's control open the same dialog as the rail, or hand off so Clara narrates from the first moment?
- **H-21 / CB-AE2E-009 / CB-AE2E-030 · backend_gap · XL** — commit_client_onboarding projects no interview capture into the client record; identifiers, FY end, facts and bank accounts are all dropped. Anchor: `packages/db/migrations/0017_wave_b.sql:2824`. Cause: The live body only activates the client, stamps the plan, snapshots a revision, audits and appends two events; every capture stays inside onboarding_plan_items.answer. Fix: Add clara.project_onboarding_captures(client, plan, op_key) called by commit, mapping ssm and tin through add_client_identifier and entity_type and msic through record_client_fact with basis_kind interview_carryover; leave banks a visible TODO. Needs: new projection door; fact catalog rows plus matching validator arms. OQ: The interview captures an FY month while the door needs month plus day. Which captures become facts versus registry columns?
- **H-18 / 裁-182 / owner: egress · needs_owner_ruling · L** — The client egress grant and activate doors have no web surface, so a beta firm cannot enable AI processing for a client. Anchor: `packages/db/migrations/0123_f_a7_gamma_egress.sql:326`. Cause: No apps/web call site exists for any egress door, and the consent tables are force-RLS with a single clara_fn_owner policy, so no read can report live state. Fix: Price the read verb first, since clara.client_egress_state is the common prerequisite of every option; then build the owner-floored client consent panel over the already-granted doors, before deciding firm-level versus per-client. Needs: NEW stable clara.client_egress_state(p_client), granted to clara_authenticated. OQ: Firm-level consent versus MIA By-Law R114.3(b)'s per-client obligation. Do no_consent consumers read the firm relation?

## C-chat-panel-genui
- **C1 / owner-chatpanel-thread-mgmt · design_task · L** — Thread management: no new, switch, clear or archive anywhere; NEW exists end-to-end with no UI and ARCHIVE is trigger-refused. Anchor: `apps/web/lib/clara/useActiveThread.ts:15`. Cause: The rail header holds only escalate and collapse; selectOwnSession discards the fetched list except the newest own thread, and the update trigger raises CLR08 on delete or any non-visibility change. Fix: Add a thread menu calling the already-live createSession and listing sessions, with an explicit per-altitude selection on claraThreadStore that resolve consults; separately a migration adding archived_at and an author-only archive door. Never build clear. Needs: chat_sessions.archived_at plus clara.archive_chat_session for the archive half. OQ: Does 裁-117's one-thread-per-altitude beta shape want a switcher now? Is archive author-only and one-way?
- **H-24 · confirmed · S** — The Ask Clara composer has no key handler at all; Enter inserts a newline and submit is mouse-only. Anchor: `apps/web/components/clara/ClaraThreadView.tsx:405`. Cause: The composer textarea binds only onChange, and a textarea inside a form does not submit on Enter, so the form is reachable only by clicking the Send button. Fix: Add onKeyDown copying InterviewRunCard's in-repo precedent, guarded on the same disabled predicate the Send button uses and on nativeEvent.isComposing for the zh and ms IMEs; extract one submitDraft the form's onSubmit also calls. Needs: none. OQ: none.
- **H-32 / owner-journal-clarification-json · confirmed · S** — The Journals Clarifications panel reads question.text while every live writer writes question.question, so it always dumps raw JSON. Anchor: `apps/web/components/journals/interruptions-panel.tsx:24`. Cause: questionText reads a field no writer produces; openInterruptionStep writes type, question, context and framing in every chatTurn version, so the fail-closed dump is always taken. Fix: Read question.question, context and framing transcribed from chatTurn.v10.impl.ts:328 with the text arm kept as a compatibility fallback, keep the pre dump as the fail-closed arm, and label expires_at with a date-and-time formatter. Needs: none. OQ: Should the panel also render framing, the firm-visibility warning the chat card shows, on every row?
- **H-33 · confirmed · S** — Two live "Your answer" controls on the journals route, the Clarifications tab's AnswerRow and the rail's ClarifyCard. Anchor: `apps/web/components/journals/interruptions-panel.tsx:114`. Cause: Two distinct message keys carry the identical string and both surfaces render on the same route, so getByLabel resolves two nodes and Playwright strict mode fails. Fix: Re-cut the journals answerLabel to name its location, keep the rail's label as-is, and re-scope chat-parity-walk's selector to the rail locator; do not suppress either surface, since both legitimately answer one door from two altitudes. Needs: none. OQ: Which surface owns the control if the owner prefers suppression? Journals also offers promote_clarify_to_question.
- **H-16 / owner-chat-export · confirmed · L** — The Reports tab tells the user to ask Clara for an export, but chatTurn_v17's 32 tools contain no export verb. Anchor: `apps/web/components/reports/SandboxExportsPanel.tsx:57`. Cause: The notice's second half is false: the whole v10 through v17 builder chain names no export, sandbox or download tool, and no runtime caller of complete_sandbox_export exists. Fix: Re-cut requestNotice in en, ms and zh to say the mint and request verbs are agent-lane-only and no surface can call them today. A v18 export tool and the F-A5b PDF render worker are separate, larger lanes. Needs: the wake verbs are already granted; the render worker is the real gap. OQ: Ship the copy fix alone for beta, or add a v18 tool that can only ever report "queued"?
- **C6 / owner-no-generative-chat-uiux · design_task · XL** — Generative UI covers 13 of 26 reader kinds; 14 live tools render as a bare badge and tool_result renders nothing. Anchor: `apps/web/components/parts/PartRenderer.tsx:246`. Cause: Only thirteen kinds have a promotion arm; tool_result is a status resolver returning null and tool_call renders the tool name with no resolved status, so payloads never reach the reader. Fix: Ship the two needing no wire bump first, a hydrated je_review card and rendering BankActCard's part.result and BankPackCard's part.pack, then a tool_call status chip composed from its sibling result or error. Trial balance needs a v18 kind. Needs: trial_balance, doc_review and close kinds need a new frozen chatTurn closure. OQ: Which of the five first? Does je_review carry approve doors? Does trial_balance need a books_version stamp?
- **C-42 · already_fixed · S** — No provisional reply text streams into the thread; TurnProgress is the shipped answer and streaming is ruled post-beta. Anchor: `apps/web/components/clara/TurnProgress.tsx:13`. Cause: The store accumulates provisionalChunks and discards them on a terminal message; the settled-only thread plus the DB-driven TurnProgress is 裁-132's deliberate shipped answer. Fix: No build. If taken up later, a render arm between the pending user bubble and the clarify group, visually provisional and replaced rather than merged by the DB row, respecting the log's existing polite live region. Needs: none. OQ: none.
- **H-17 / owner-autodraft-autopost · confirmed · L** — autoDraft_v9 does run unattended and can post; it self-refused because a kind-blind alias unique's 23505 maps to CLR23. Anchor: `packages/runtime/workflows/autoDraft.v9.errors.ts:153`. Cause: Any 23505 whose constraint name contains "counterpart" or "alias" becomes "the counterparty could not be resolved"; uq_counterparty_aliases_live_name is kind-blind while counterparties.kind carries vendor and customer. Fix: Ship autoDraft_v10 splitting the map by exact constraint name so an alias collision is retryable and never opens an unanswerable question; separately a migration making the alias unique kind-scoped. Record layer-2 wakes as OFF by 裁-165, never as broken. Needs: kind-scoped alias unique migration; autoDraft_v10 closure and registry repoint. OQ: Add a kind column or re-key through counterparties.kind? Do existing rows violate the new unique?

## D-documents
- **D1 / H-22 · backend_gap · L** — "Needs your confirmation" persists after Confirm and file; the classification open question stays open after set_document_kind. Anchor: `packages/db/migrations/0123_f_a7_gamma_egress.sql:1949`. Cause: The named frontend seam re-reads correctly. The live set_document_kind body never touches open_questions, and confirm disposes one candidate while every re-extraction mints new ones. Fix: Migration re-cutting set_document_kind to resolve origin='classification' questions in the same transaction, disposing sibling candidates in confirm_attribution_candidate, and suppressing candidate minting for an already-filed document; plus a one-line onFiled fix in documents-workbench. Needs: CoR set_document_kind, confirm_attribution_candidate and record_attribution_attempt. OQ: Dismissed or confirmed for the unchosen sibling? Suppress on a live filing, a live resolution, or both?
- **D2 · design_task · L** — Page-overlay evidence viewer: render the page with region highlights keyed to extracted facts. Anchor: `apps/web/components/documents/document-evidence.tsx:24`. Cause: The geometry exists and get_document_extract already returns locator per region, but the detail panel's own read omits locator and no PDF or image renderer ships anywhere in apps/web. Fix: New document-page-overlay.tsx driven by getDocumentExtract's regions, pdfjs-dist to canvas with its worker under public/ for PDFs and a plain img for raster, one absolutely-positioned svg polygon per page_polygon locator, skipping malformed geometry rather than guessing. Needs: no DB change; get_document_extract already returns locator. OQ: Page dimensions are never persisted so image scale has no source. Does pdfjs fit the Workers bundle?
- **D3 · design_task · M** — Extraction text renders as a raw envelope blob; structure it into a facts table plus a collapsible raw section. Anchor: `apps/web/components/documents/document-extract-panel.tsx:70`. Cause: The panel prints up to twenty thousand characters of raw JSON envelope in a pre, and every OCR line and table cell renders as a truncated two-column dl row. Fix: Split DocumentExtractContent into three tiers with no new dependency: a facts table over dotted or monetary field_paths, layout text grouped per page inside a details, and the envelope pretty-printed inside a details with a parse fallback for truncation. Needs: none. OQ: Is the facts tier keyed on the invoice prefix or on monetary_cents? Should the raw envelope be visible in the product at all?
- **C-07 / 裁-175 / D4 · confirmed · M** — An uploaded XML executes script in the app's own origin via the blob new-tab open, and no CSP is set anywhere. Anchor: `apps/web/lib/documents/open-in-new-tab.ts:78`. Cause: The viewer reuses the runtime's intake allowlist which admits application/xml; a blob URL inherits the creating page's origin and the new tab carries the session. Fix: Add a narrower VIEWABLE_IN_NEW_TAB set in bytes.ts enforced in open-in-new-tab before navigation, revoking the blob and closing the tab on a typed not_viewable result; separately set Content-Security-Policy in next.config and the proxy, report-only first. Needs: none. OQ: Should the gate also sniff magic bytes? Does Next 16 on Workers need unsafe-inline or a nonce?
- **H-53 · backend_gap · L** — Consent-evidence filings sit in the coding lane awaiting coding. Anchor: `packages/db/migrations/0011_daily_loop.sql:3983`. Cause: list_uncoded_filings selects document_kind but never filters on it, and list_review_queue's filing_rows carries the identical predicate, so any filed document with no entry reads as uncoded. Fix: One migration defining clara._coding_lane_excluded_kind(text) once, re-cutting list_uncoded_filings's WHERE and splicing the same conjunct into list_review_queue's filing_rows; leave _coding_lane_core's classifier honest and change only the population readers. Needs: new _coding_lane_excluded_kind helper; CoR and splice the two population readers. OQ: Which kinds are excluded, and does a NULL kind get excluded too? Is this all of H-23's discrepancy?
- **CB-AE2E-022 (documents/journals half) · confirmed · M** — Internal DB verb names printed to the user in five dialog descriptions. Anchor: `apps/web/messages/en.json:2276`. Cause: Five DialogDescription strings name the function the act calls; no test or e2e pins them, and the house already has a verbatim CLR code chip for genuine diagnostics. Fix: Rewrite the five strings to say what the act does and what it needs, then add an optional diagnostic prop rendered through one shared collapsed technical-detail component so each call site passes its verb name below the sentence. Needs: none. OQ: Keep the verb name behind a disclosure or remove it entirely? Sweep the other 26 keys in one PR or six?

## E-firm-home-activity-needs-you
- **E-1 / owner: Firm Home — go mobbin and design a dashboard · design_task · L** — Firm home is a header and nothing else; every candidate widget's backing read enumerated, nine exist and three do not. Anchor: `apps/web/app/(firm)/page.tsx:17`. Cause: The page performs zero reads and does not even consume the scope the layout already positively read, so it cannot name the firm, the caller's role, or one number. Fix: A two-column grid, the review-queue counts and oldest-N triage on the left reusing NeedsYouCounts verbatim, a clara.domain_events timeline on the right; every tile links to the surface owning the verb, with honest not-built notes where no read exists. Needs: domain_events and audit_log exist, are granted and are unused; no firm-wide close rollup exists. OQ: Shared provider or a second list_review_queue call? domain_events has no rank floor and exposes raw payload.
- **E-2 / CB-AE2E-018 / owner: Activity — agent task 没有内容 · confirmed · L** — Activity is empty because five of nine receipt shims are typed-empty stubs; the task row drops seven of eleven read columns. Anchor: `packages/db/migrations/0103_f_a7_pi_additive.sql:311`. Cause: The f_a2, f_a3, f_a4, f_a5 and f_a8 arms are still 0103's select-null-where-false stubs, and the kind label map cites a register that has been superseded twice. Fix: Three lanes: wire the five stub shims or declare them unwired; add onboarding_agent and binding_agent to the kind map and pin it against agent_receipt_surfaces; rebuild Activity as a domain_events timeline plus the receipts feed plus a task detail drawer. Needs: CoR five _agent_receipt_src_* views; the domain_events and audit_log reads. OQ: Which stubs are genuinely owed versus parked? domain_events has no rank floor while receipts are bookkeeper-plus.
- **E-3 / CB-AE2E-026 / owner: Needs you — Sweep run 是什么 · confirmed · M** — Needs-you rows carry no client, no age, no why and no deadline, and "Sweep runs" renders with no explanation. Anchor: `apps/web/components/firm/needs-you-row.tsx:112`. Cause: aged_since, lane, rule_backed, auto and tier are read and never rendered, client_id only builds an href, and period renders raw under one label that means three different dates. Fix: Give every row what, why, next and when from columns the RPC already ships, merging client names from loadClientRegister, per-kind period labels through a checked map, and lead the sweep panel with one plain definition sentence moved below the queue. Needs: no new DB read. OQ: Client name merge or id-only? Does the owner accept a derived one-line reason, or only raw flags as chips?
- **E-4 / H-25 · confirmed · S** — The agentTasks loading message key is absent from messages/en.json, so the Activity console MISSING_MESSAGE is real. Anchor: `apps/web/components/firm/agent-tasks-panel.tsx:74`. Cause: The panel's scoped namespace block has no loading key and the first-load branch fires on every mount; pnpm lint carries no message-key coverage check at all. Fix: Add a loading key inside the agentTasks block rather than repointing the call site at the parent namespace, then close the class with a scripts/check-message-keys.mjs wired into pnpm lint that walks every static t() literal against the catalog. Needs: none. OQ: none.
- **E-5 / H-23 · confirmed · M** — "Uncoded filing times four" versus the uncoded_documents gate's zero: two genuinely different populations behind one word. Anchor: `packages/db/migrations/0056_wave_e_close_model.sql:1391`. Cause: The gate scopes to financial_date inside the fiscal year, joins on document_id and excludes reversal mirrors; the queue has no date predicate, joins on filing_id and accepts a mirror. Fix: Do not unify the predicates, since the catalog is append-only and the digest binds signed attestations. Qualify the queue label, state its scope, add a note beside the gate row pointing at undated_documents, and group the client home by row_kind. Needs: no DB change; _close_gate_uncoded is sha-pinned and must not move. OQ: Is a per-client reconciliation explainer wanted? Did the walk's client actually hold undated documents?
- **E-6 / CB-AE2E-022 (firm half) · confirmed · M** — 54 user-visible strings carry a DB verb, a migration number or "altitude", and seven carry session-internal ruling and lane ids. Anchor: `apps/web/components/firm/needs-you-gaps.tsx:137`. Cause: A four-pattern census over every catalog string; the verb-name class is a deliberate house pattern with a written rationale, while the ruling-id class has none. Fix: Class A, rewrite the seven strings carrying ruling, lane, track, PR and PROGRESS tokens and add a lint cell banning the pattern. Class B, demote verb names below the plain sentence and drop migration numbers. Class C, rewrite the one altitude string. Needs: none. OQ: Does user-visible copy name DB doors and migration numbers on purpose? Do the four raw-token fallbacks stay?
- **E-7 / CB-AE2E-014 / CB-AE2E-033 / issue stages 36-37 · needs_owner_ruling · L** — A bookkeeper sees Change-threshold and the role and remove menu because the build renders every control and lets the DB refuse. Anchor: `apps/web/lib/firm/navigation.ts:5`. Cause: Nav floors key on each destination's primary read and never on the write that happens to live there; three file headers state the doctrine that the DB's wall is the only wall. Fix: If flipped, widen NavigationScope to the full caller context, derive one capability object naming each mirrored DB floor and its migration line, split read floor from write floor so controls render disabled with the required rank named, and keep every gate fail-open. Needs: caller_context is already read; no DB change. OQ: Hide a control the caller cannot use, or show it and teach the ceiling? Is disabled-with-reason acceptable?
- **E-8 / CB-AE2E-025 · confirmed · S** — Two "Members" headings stack on the members page, and every date on it is a raw UTC slice. Anchor: `apps/web/components/admin/members-panel.tsx:113`. Cause: A local day() slices the ISO string at T across four timestamptz columns, so a 00:00 to 08:00 MYT instant renders the previous day; the section h2 repeats the page h1's name. Fix: Delete day() and use businessDate from lib/business-date.ts at all four call sites, keeping the null guard; rename the roster heading to "Everyone with access" so it pairs with "Pending invites", and trim the page description's echo. Needs: none. OQ: none.

## F-client-workspace-home
- **owner-client-workspace-home · design_task · L** — Client workspace Home tab renders two facts and a bare text list; every client-state read the app already owns is unused. Anchor: `apps/web/components/firm/client-workspace-overview.tsx:70`. Cause: The whole tab is 96 lines, a two-row dl and queue rows as bare li text with no link, badge or affordance; counts and sweep from the same envelope are never destructured. Fix: Rebuild as a seven-section situation board over reads that already exist: identity and facts, onboarding progress, needs-you through NeedsYouRow with counts chips, close state, document and coding backlog, bank accounts, last activity, each with its own DataState. Needs: no migration; every read already exists. OQ: Which sections above the fold? Should Home show an AR/AP aging summary at two more RPCs per load?
- **H-34 · confirmed · S** — Counterparty hygiene pairs a kind-scoped read with kind-blind empty copy, claiming no counterparties while customers exist. Anchor: `apps/web/components/registers/counterparty-hygiene-panel.tsx:59`. Cause: The panel's kind state initialises to vendor and the only read filters on it, while the empty string makes a claim about the client; the parent aging register defaults to receivables. Fix: Fetch both kinds with Promise.all over the same function, keep vendor and customer in data, render the active kind and put each kind's row count on its own toggle; split the empty string per kind so the sentence names what was read. Needs: none. OQ: none.
- **CB-AE2E-029 · confirmed · S** — fa_register_tie asserts "the register ties to the GL" on an empty universe: v_tie initialises true and the walk never runs. Anchor: `packages/db/migrations/0041_wave_d_a_fa_register.sql:4260`. Cause: v_tie is declared true and only ever set false inside the loop, so a client with no enrolled profile and no register row yields zero iterations and a positive assertion from an empty comparison. Fix: Make the banner three-valued off accounts.length with a notEvaluated message and drop the duplicate EmptyState; fold in the four explained columns the DB already returns so a red tie is actionable rather than an unattributed difference. Needs: no migration; the UI fix is honest without one. OQ: Should the DB itself return a third state or an evaluated flag rather than the UI inferring it?
- **CB-AE2E-032 / owner-tax-uiux · confirmed · M** — Tax tab is three static not-built notes whose copy leaks lane ids, migration numbers, ruling numbers and raw verb signatures. Anchor: `apps/web/components/tax/TaxWorkbenchPage.tsx:36`. Cause: The tab issues zero reads and each card's body is internal build-log prose naming lane ids, three migration numbers, an owner ruling id and two SQL signatures. Fix: Rewrite the three strings in professional register with zero project vocabulary, keeping the never-files promise as product behaviour; then give the tab one live section, this client's SST compliance watch through list_review_queue, and wire the live set_turnover_classification verb. Needs: set_turnover_classification is live, bookkeeper-plus, with no UI. OQ: Keep three cards while only one has data? A repo-wide sweep for the same raw-verb leak is warranted.
- **owner-GI-clock · needs_owner_ruling · M** — No GI clock exists anywhere in apps/web; the one real DB clock is runtime-only and the deadline table is empty and ungranted. Anchor: `packages/db/migrations/0139_statutory_deadlines.sql:123`. Cause: GI resolves to nothing in the repo. statutory_deadlines is empty, ungranted and has no due-date oracle, and close_prep_due is clara_runtime-only with no human read. Fix: Get the ruling first: a statutory filing calendar, the fiscal-year-end clock, or the SST liability clock. The year-end strip is cheap and needs no backend; the statutory calendar needs seeds, a read verb and the oracle that 0139 deliberately did not ship. Needs: seeds, a grant or definer read, and a due-date oracle for statutory_deadlines. OQ: What is GI? If the statutory calendar, who seeds it and does the owner-only posture relax?
- **CB-AE2E-027 / CB-AE2E-028 · confirmed · M** — Raw user UUIDs rendered to professionals in six places, while a firm-scoped display-name view exists and one panel uses it. Anchor: `apps/web/components/close/ClosePrepHoldPanel.tsx:56`. Cause: firm_members_visible publishes display_name, is granted to clara_authenticated and does not filter removed memberships, but loadFirmMembers has exactly one caller in the whole app. Fix: Add lib/members/use-member-names.ts resolving a user id to display_name once per firm scope, falling back to a shortened raw id and never a guessed name; apply it at all six sites and replace the export-recipient uuid text input with a member picker. Needs: clara.firm_members_visible, already granted. OQ: Cache the roster at the firm-scope provider or one read per panel? A viewer would see an empty picker.

## G-journals
- **owner: 没有一个UIUX table for journal entry · design_task · L** — No journal-entries table exists: both journals lists are card stacks with no sort, filter or pagination. Anchor: `apps/web/components/journals/posted-panel.tsx:66`. Cause: Both panels map entries to Cards and the only Table is a draft's lines; Posted rides created_at.desc with no control, drafts ride the queue's order, and @tanstack/react-table is not a dependency. Fix: Do not add TanStack. Build journal-entries-table.tsx on DataTableCard with useMemo sort, three client-side filters and page slicing over hydrated rows; surface entriesTruncated as a warning and render an honest source cell, never a fabricated reference. Needs: a real journal reference number would need a migration. OQ: Does the owner want a human-facing journal number? Should the drafts queue become this table or stay a disclosure list?
- **CB-AE2E-021 / issue stage 31 · confirmed · M** — The Posted tab labels its rows Approved, and one draft offers two competing approve buttons whose difference is never stated. Anchor: `apps/web/components/journals/drafts-queue-panel.tsx:333`. Cause: The DB status domain has no posted value so tab and badge use two words for one state; approve_routine_entry only refuses high-stakes then delegates, and the attestation is discarded below the threshold. Fix: Collapse to one primary Approve routing by row.high_stakes to the guarded or the attesting door, make the attestation field conditional on high_stakes, rename the approved badge to Posted, and add a state-machine legend from the real catalog. Needs: both doors stay; no DB change. OQ: Posted everywhere or Approved everywhere? Should the routine door survive as a batch affordance elsewhere?
- **CB-AE2E-013 / issue stage 38 · needs_owner_ruling · L** — A bookkeeper can draft then approve their own journal below the firm's threshold, and no surface discloses the policy. Anchor: `packages/db/migrations/0037_wave_c_a_subledger.sql:1992`. Cause: The whole maker-checker apparatus sits inside if is_high_stakes, so below the threshold no same-actor comparison runs at all, and ENTRY_SELECT never reads last_human_editor. Fix: Path A now, read last_human_editor and render "you are approving your own work" plus the threshold in force, showing maker against checker on the receipt. Path B if ruled, a tenth _approve_entry_core body by dynamic splice scoped to manual origin. Needs: Path B replaces a live audited writer under a D1 quiesce window. OQ: Different-person approval for all manual journals, or the threshold plus disclosure? What happens in a solo firm?
- **CB-AE2E-022 (journals half) · confirmed · S** — The manual-entry dialog shows the DB verb names for the two-call compose ceremony to the user. Anchor: `apps/web/messages/en.json:690`. Cause: The compose dialog's description key names the real ceremony; the copy is true but written for the engineer who built it, and it sits in the dialog's most-read line under the title. Fix: Replace the string with what the act does and costs, recorded by hand against this client and left as a draft until approved, and sweep the four sibling door-verb descriptions in the same PR, keeping the verb names in the source comments where they belong. Needs: none. OQ: Should the guard be a blanket ban on DB identifiers in the catalog plus an explicit allow-list constant?
- **C-42 · already_fixed · S** — A parked clarify surviving a page reload was already fixed by P6-5; both surfaces re-read it from the DB. Anchor: `apps/web/lib/clara/turnRun.ts:150`. Cause: readThreadRunSnapshot re-derives the non-terminal task and its pending interruption on every mount, and the journals tab was never in-memory; commit 1697714b closed it. Fix: Nothing to build. Two residuals if wanted: a bounded cancellable re-read while the turn is non-terminal and the stream is detached, and a pointer from a journals clarification row back to the thread it belongs to. Needs: none. OQ: Is a park that appears only after the stream detaches mid-session reachable? Measure before building a poll.

## H-bank-close-reports-registers
- **H-06 · confirmed · S** — enter_bank_statement can never succeed from the UI: the header omits the institution_code and account_number pair the normalizer requires. Anchor: `apps/web/lib/bank/doors.ts:76`. Cause: BankStatementHeaderInput has seven keys and cannot carry the pair, so _stmt_header_norm raises header_unreadable on every hand-keyed statement before any other validation runs. Fix: Add institution_code and account_number to the header type, fill them from the selected account row in statements-section, make statement_date required, and re-cut the doors test whose deepEqual currently canonises the defective seven-key header. Needs: no DB change. OQ: Send the printed account number or the normalized form? Either works; printed is the more honest receipt payload.
- **H-09 · backend_gap · M** — No door records a payer identifier on an existing counterparty, only at creation, while the settle gate reads registration and tin. Anchor: `packages/db/migrations/0121_f_a3_pr1b_agent_limb.sql:6116`. Cause: Five update sites touch counterparties and none writes registration_no, registration_normalized or tin; the only producer is create_counterparty's insert, so a coding-lane-born party can never acquire them. Fix: New migration adding clara.set_counterparty_identifiers with an admin floor, op_key reservation over all args, byte-identical normalization, a named refusal on the partial unique, and the 0062 name-only guard left to raise on its own; add a dialog beside RenameCounterpartyDialog. Needs: NEW clara.set_counterparty_identifiers door, granted. OQ: Bookkeeper or admin floor? Should the door also accept clearing a value to NULL?
- **CB-AE2E-034 · confirmed · S** — Every bank empty state is the same six-word string across eight distinct absences. Anchor: `apps/web/components/bank/read-state.tsx:80`. Cause: ReadState takes no empty-copy prop and binds to one common namespace, and the reconciliation tab stacks two identical sentences inside a single card. Fix: Add an optional emptyCopy prop defaulting to today's string, give each of the eight call sites its own key under its section namespace, and collapse the second picker's empty state when the first is empty since it is a derived truth. Needs: none. OQ: none.
- **H-11 · confirmed · S** — After Abandon, and after Reopen, the close tab shows no door at all because canBegin tests for an absent run. Anchor: `apps/web/components/close/CloseDoors.tsx:97`. Cause: get_close_plan returns the latest run in any state, so an abandoned run reads present; all three branches then render null and the year is stranded in the UI while the DB would admit a restart. Fix: One line allowing canBegin when the run is absent, abandoned or finalized and the fiscal year is open or reopened, mirroring _begin_close_core's own predicate rather than inventing a client-side rule; label the trigger Restart close on an abandoned run. Needs: no DB change. OQ: none.
- **CB-AE2E-016 / H-54 / H-56 · confirmed · M** — Close copy has no Restart, the Begin dialog never says it freezes the period, and Finalize renders above raw untranslated gate tokens. Anchor: `apps/web/components/close/ClosePlanPanel.tsx:112`. Cause: Begin sets the year to closing which the period wall refuses writes into, while the description names only an RPC; Finalize renders above the gate list and GateCheckRow prints the raw DB state token. Fix: Add restart copy, rewrite the Begin description to state the write freeze plainly, add a pre-flight summary counting unknown gates and unattested drawer-two items without disabling Finalize, and add a gate-state label map with a raw-value fallback. Needs: no DB change; finalize already refuses unknown gates. OQ: Should the pre-flight banner name gates by check_key or by the title the DB already returns?
- **CB-AE2E-004 · confirmed · L** — All fifteen DoorDialog wrappers close on any settled act: act swallows the refusal and the human's typed input is destroyed. Anchor: `apps/web/lib/parts/single-fire-guard.ts:30`. Cause: runOnce returns true whenever the handler ran, and useHydratedPart's act catches every failure then resolves, so onConfirm never rejects and setOpen(false) always fires. Fix: Widen runOnce to return ran plus value, have the hook's act return a boolean, close only on an explicit true, and stop the six opening call sites discarding the boolean they already have; the other thirteen wrappers inherit the fix. Needs: none. OQ: Should a refusal the human cannot fix in-dialog close anyway? One behaviour is reviewable, two are not.
- **CB-AE2E-020 · confirmed · M** — The opening Ties banner is a false green: it tests one of the four conditions approve_opening_seed enforces. Anchor: `apps/web/components/registers/opening-dryrun-strip.tsx:44`. Cause: _assert_opening_tie raises on zero TB targets, an unmapped target, any nonzero delta and a nonzero OBE; the strip renders only the last and mislabels it as the tie itself. Fix: Render four named gates in the assert's own order over already-fetched payload, targets present, all mapped, all tie, OBE nil, threading loadOpeningTbTargets from the workbench and renaming the fourth to the DB's own obe_not_nil word. Needs: no DB change; optionally a target_count field on get_opening_dryrun. OQ: Should gate one be added to the dryrun payload rather than threaded from the caller?
- **H-16 · confirmed · S** — The Reports sandbox note tells the human to ask Clara to mint a view or request an export; the live chatTurn has no such tool. Anchor: `apps/web/messages/en.json:1324`. Cause: The first half is true, the verbs are wake-only, but the live v17 tool set contains no sandbox verb, so the note routes the human to an affordance that does not exist anywhere. Fix: Rewrite the notice to say sandbox exports are minted by the unattended lane, not by a control here and not by the chat rail, and that finished exports appear below with a download offer; a chat tool is a separate v18 lane. Needs: none. OQ: Is a chat-lane sandbox request wanted for beta? If yes this becomes a v18 lane, not a copy fix.
- **CB-AE2E-017 / H-15 · backend_gap · L** — Zero report templates: the doors exist and are granted but nothing published one, and the house-style prerequisite is structurally unrenderable. Anchor: `packages/db/migrations/0069_wave_e_epsilon_reporting_security.sql:64`. Cause: publish_house_style_version requires every manifest value to be a flat sha256 string while the renderer reads fonts as a nested array, so a manifest satisfying one refuses the other; no seed or web surface publishes anything. Fix: Migration re-cutting publish_house_style_version to accept the renderer's nested fonts array while keeping the flat-string rule for other keys, with a distinct refusal token; then a data act publishing style, template, chart template and spec through the real audited doors. Needs: CoR publish_house_style_version; a publish data act. OQ: Management or statutory template first? Does the fix belong in the validator or the renderer?

## I-runtime-workflows
- **H-02 / H-03 / H-05 · confirmed · L** — statementFacts_v2 has no period derivation, no institution name-to-code normalisation, and a persist failure strands the task running. Anchor: `packages/runtime/workflows/statementFacts.v2.behavior.mjs:339`. Cause: The prompt contradicts itself on period_start and invites a printed bank name the DB can never match to a roster code, and the persist step alone has no terminal-settle wrapper. Fix: Ship statementFacts_v3, wrapping the persist in withStatementTerminalSettle first, resolving the institution against the DB roster through a new definer rather than a hand-kept list, and adding a period_basis field with exactly one explicit lawful derivation. Needs: NEW clara._stmt_institution_code or a bank_institutions grant; a new frozen v3 closure. OQ: Is the month derivation the owner's call? Which home for the resolver? Should the DB derive the bounds?
- **H-17 / CB-AE2E-012 / H-19 · confirmed · L** — autoDraft's 23505 mask is substring-matched and over-broad, the kind-blind alias unique is the hole, and set_sales_lane_activation has no identity wall. Anchor: `packages/runtime/workflows/autoDraft.v9.errors.ts:150`. Cause: Three different uniques all contain "counterpart" or "alias", so registration, name and alias collisions reach the human as one untokened sentence; the report's kind-passing sketch is already done. Fix: autoDraft_v10 replacing the substring test with an exact constraint-name map carrying a reason token per arm and an unknown name falling through; separately kind-scope the alias unique; for H-19 mint an owner-floor wrapper, never grant the existing signature. Needs: a kind column plus a re-keyed alias unique; a new owner-floor sales-lane wrapper. OQ: Should a birth race retry rather than refuse? Kind column or three-site refusals? Is the flip wanted in the UI?
- **H-07 / H-08 / CB-AE2E-015 · confirmed · L** — The freeform read's oracle-safe refusal renders CLR03's session-authority sentence, and the close tables sit outside the enumerated 35. Anchor: `packages/runtime/workflows/chatTurn.v15.freeform.ts:130`. Cause: The oracle message borrows readToolRefusalMessage's 42501 arm, so every refusal reason collapses into a lie about the caller's session; only close_runs is on the granted relation list. Fix: chatTurn_v18 defining the oracle string locally, honest about the class without naming the refused table and keeping the single-string collapse; plus a migration granting and policying the descriptive close tables and moving 0131's own tail counts with them. Needs: grants plus per-relation freeform policies; a new frozen v18 closure. OQ: Which close tables should be readable? Do they carry client_id? Confirm the wording before it freezes.
- **H-04 · design_task · M** — The doc_classify prompt is definition-only with zero few-shots and no bank-statement disambiguation, and the engine id never moves. Anchor: `packages/runtime/lib/classify-llm.mjs:69`. Cause: bank_statement's whole definition is two lines and the calibration paragraph pushes a noisy OCR toward "other"; classify-llm.mjs is off the freeze surface so a prompt change is a plain edit. Fix: Build measure-classify-recall.mjs first on the measure-invoice-id-capture shape with fixtures, live and replay modes printing the model id and a prompt sha; then sharpen bank_statement with a CRITICAL line and synthesised few-shots, and bump the engine id to v2. Needs: no migration; the engine id is a code constant. OQ: Where does the labelled manifest live? Is the id bump safe against existing rows? What recall floor counts as done?
- **H-48 / C-04 · confirmed · M** — assertProductionPoolConfig checks env presence only and readiness probes one pool; the relay's makePool has no error listener. Anchor: `packages/runtime/lib/relay.mjs:146`. Cause: The boot assert is a truthiness loop over three env names and checkReadiness makes one round trip through the runtime pool; makePool is the only pg.Pool of nine with no error listener attached. Fix: Attach the listener with a counter and last-error stamp, surface it as a readiness warning that never flips ready false, and add a concurrent bounded per-lane select-1 probe that skips unconfigured lazy lanes and warns rather than failing for every lane except runtime. Needs: no migration; select 1 per lane is the whole DB surface. OQ: Does the leader stay log-and-reconnect against 裁-149's wrong premise? Should any lane fail readiness? Probe cadence?
- **H-43 · confirmed · S** — The runtime image ships no pooler CA, so verify-full with sslrootcert cannot resolve; the CA is in the repo and never copied. Anchor: `packages/runtime/Dockerfile:50`. Cause: The runner stage copies five things and none is the CA; the public trust store cannot validate Supabase's self-signed pooler chain, so verify-full either throws at readFileSync or is refused outright. Fix: Copy the CA into the runner stage, port validateCa's structural and fingerprint checks into a boot assert beside assertProductionPoolConfig, and add a Dockerfile drift cell; then re-set every lane DSN, image first and secrets second or every lane throws. Needs: no DB change; image content plus a DSN secrets ceremony. OQ: Which in-image path, since it bakes into every secret? Refuse a no-verify DSN? One fingerprint constant or two?
- **H-01 / CB-AE2E-005 · already_fixed · S** — reconcile_autopost_rules is gone from runtime source but not from the serving image; v71 predates the fix. Anchor: `packages/runtime/lib/reconciler.mjs:503`. Cause: Commit d9520061 landed four hours after v71 was built, and v72 through v74 are secrets imports over the same image, so the live machine still polls a dropped verb every two seconds. Fix: No code. One deploy from main producing v75, with the rollback preflight on workflow exports first and a positive read of a clean log interval after, since an absence claim needs a measured window and not a source grep. Needs: no migration, no data act; a runtime deploy only. OQ: None on the code; scheduling the deploy is the owner's under 裁-150.
- **CB-AE2E-035 · backend_gap · M** — No build-info surface anywhere: neither web nor runtime exposes a git SHA, an image digest or the migration frontier. Anchor: `packages/runtime/src/index.ts:89`. Cause: /health returns a pid, /ready returns liveness and /workflows names exports; schema_migrations grants select only to clara_fn_owner, so the runtime cannot read the frontier at all. Fix: Add clara.build_frontier() SECURITY DEFINER granted to clara_runtime only, a runtime build-info route returning a baked build sha that is null when unset plus the image ref, workflow names and the frontier, and a web route baking its own sha. Needs: NEW clara.build_frontier() definer over schema_migrations. OQ: Public or session-gated? Should it feed 裁-147's operator screen? Report the checksum too?

## J-database
- **H-12 / H-55 · confirmed · L** — _close_gate_uncoded counts non-codeable kinds as uncoded, and _close_gate_bank_items passes vacuously when a client has zero statements. Anchor: `packages/db/migrations/0056_wave_e_close_model.sql:1391`. Cause: The uncoded gate has no document_kind predicate while ingest stamps every statement's financial_date inside the year, and the bank gate derives its account population from the statements themselves. Fix: One migration re-cutting both bodies, adding a codeability predicate derived from a new document_kind_codeability table rather than an inline literal, and re-deriving the bank gate's population from enrolled bank_accounts; add an accounts_basis key so the change shows in the digest. Needs: new clara.document_kind_codeability table; CoR two close gate bodies. OQ: Which kinds are codeable is an owner ruling. One migration for both, or two?
- **CB-AE2E-004 · confirmed · M** — The opening approvals' serializable assert depends on a proconfig pin that exists only as a manual ceremony. Anchor: `packages/db/deploy/wave-b-0017-ceremony.sql:14`. Cause: The pin is not in the migrate path, the rig sets isolation at session level so it never consults it, dr-verify diffs source against target, and readiness never probes it. Fix: Add an absolute target-only cell in dr-verify-checks asserting both regprocedures carry the proconfig and failing rather than skipping; add the same assertion to checkReadiness as a warning; optionally promote the alter function into a forward migration. Needs: the alter function could ride a forward migration. OQ: Has the ceremony actually been run against live? Should the proconfig move into the migrate path?
- **H-47 · confirmed · M** — 0154's absolute fourteen-role census, and an unconditional nologin flip in four migrations that cuts live login shells. Anchor: `packages/db/migrations/0154_binding_proposal_pr_1.sql:3788`. Cause: Roles are cluster-global while migrations are database-scoped, so a second from-scratch migrate on the live cluster meets sixteen to eighteen roles and re-flips four login shells to NOLOGIN. Fix: Leave 0154, since the checksum guard makes editing it a ceremony, and add a position-0153 count cell beside the derived roster; add a fail-closed migrate preflight refusing a fresh database on a cluster whose clara shells already canlogin, plus two runbook walls. Needs: no migration; a migrate.mjs preflight and runbook edits. OQ: Derive 0154's pin at the cost of a checksum-repair ceremony? Any legitimate reason to migrate a second database onto live?
- **C-17 · already_fixed · S** — client_identifiers uniqueness is already built and merged as 0155 with a refusing duplicate pre-flight; the live deploy is owed. Anchor: `packages/db/migrations/0155_client_identifiers_unique.sql:402`. Cause: 0007 deliberately left the index non-unique; 0155 adds the plain unique, a refusing pre-flight, a measured prosrc writer census and typed refusals ahead of the raw 23505. Fix: No build. Re-measure the live duplicate census, take the owner's route from the refusal's own hint, where the Wave-G factory reset is the accounting-safe answer for a resettable fixture, then apply inside a D1 write-quiesce window. Needs: a ruled live deploy; two live audited writer bodies are replaced. OQ: Route (a) reset or route (b) an owner-authorised dedupe ceremony? What is the current live duplicate count?
- **H-22 / H-53 · confirmed · M** — set_document_kind never closes the classification question it answers, and no coding-lane read filters by document_kind. Anchor: `packages/db/migrations/0123_f_a7_gamma_egress.sql:1949`. Cause: The live body stamps the kind, retires tasks, mints an extraction and appends an event but touches open_questions nowhere, so _coding_lane_core pins the filing at needs_you forever. Fix: Forward migration resolving origin='classification' questions inside set_document_kind with the same event shape resolve_open_question appends, scoped narrowly to open classification rows; then add the shared codeable-kind predicate to list_uncoded_filings, admitting NULL as still-work. Needs: CoR set_document_kind and list_uncoded_filings; the shared kind vocabulary. OQ: Resolve or dismiss, given the verb writes the text? Does a bank statement belong in the coding lane at all?
- **CB-AE2E-013 · needs_owner_ruling · M** — Approval segregation as built: the mechanism, precisely, for the owner to rule on. Anchor: `packages/db/migrations/0037_wave_c_a_subledger.sql:1992`. Cause: Nineteen lines inside if is_high_stakes are the entire gate; maker is last_human_editor, eligible checker is a runtime count, and the same three-arm shape is replicated five times with different tokens. Fix: No fix proposed. A ruling decides whether maker should be the creator, whether non-high-stakes entries carry segregation, whether the five implementations consolidate, and whether the checker floor is right; any change replaces 0037's live core under a quiesce window. Needs: any change is a forward migration on the posting path. OQ: All four decisions are the owner's. Is the absence below the threshold intentional law or an unstated gap?
- **H-49 · confirmed · S** — dr-verify's canary section anchors on two hard-coded id prefixes, so a factory reset makes STRICT a false FAIL and a new canary invisible. Anchor: `packages/db/scripts/dr-verify-checks.mjs:398`. Cause: No seed, table or migration plants those ids; the check measures a spelling rather than the property it exists to prove, so it fails in both directions after a fixture reset. Fix: Add clara.dr_canary_subjects as a small registry seeded with the current subjects, iterate it in checkCanary keeping the parity assertions verbatim, and separate three outcomes: empty registry, registered but absent, and absent from both. Needs: NEW clara.dr_canary_subjects registry table plus a seed. OQ: Is the current canary still present on live? Who may insert a registry row without making the subject forgeable?
- **CB-AE2E-018 · design_task · L** — A member-readable firm timeline: three partial surfaces exist today and none of them is it. Anchor: `packages/db/migrations/0005_event_spine.sql:79`. Cause: audit_log has no client_id and raw args, domain_events has no rank floor and raw payload, and agent_receipts_visible is agent-only with five of its seven shims still stubs. Fix: One masked clara.firm_timeline_visible over domain_events on the agent_receipts_visible precedent, bookkeeper-plus floor with the payload dropped entirely rather than key-masked, plus a keyset read on seq; separately repoint the dormant shims starting with f_a4 over agent_act_receipts. Needs: NEW clara.firm_timeline_visible view plus grant; CoR the dormant shims. OQ: Bookkeeper-plus or all members? Human acts, agent acts or interleaved? Can the DB honestly report a result?

## K-shell-responsive-a11y-nav
- **CB-AE2E-019 / issue stage 46 · confirmed · L** — The authenticated firm shell has zero width breakpoints; three fixed columns crush the workbench at narrow widths and 200% zoom. Anchor: `apps/web/app/(firm)/layout.tsx:76`. Cause: 544 pixels of fixed chrome plus 64 of PageShell padding before any content, and no responsive variant exists in the layout, either nav, the rail or globals.css. Fix: Three seams: the firm rail becomes a sheet-backed drawer behind lg, the client nav collapses to an overflow menu, and the Clara rail becomes a fixed overlay below the breakpoint; rescope the parity-holes docked-rail proof to the wide arm rather than relaxing it. Needs: no DB change; vendoring the shadcn sheet primitive. OQ: lg or md? Does railOpen stay true on a narrow first load? Confirm the overlay does not read as a change to 裁-117.
- **CB-AE2E-036 · confirmed · M** — Browser e2e is not a CI job at all: no Playwright leg exists and the meta-gate cannot require one. Anchor: `.github/workflows/ci.yml:637`. Cause: The workflow defines nine jobs and the string playwright appears nowhere; the e2e README records this as a policy position under 裁-86, not an oversight. Fix: Add one web-e2e-smoke job on the build job's shape, installing chromium and invoking the harness through the package script with an explicit smoke spec list; then add it to the meta-gate's needs and its code_gated call, keeping retries at zero. Needs: no DB change; a CI job plus a meta-gate wiring. OQ: 裁-86 makes the walk acceptance-only, so this needs a ruling. Wall-clock budget and which specs are in the set.
- **H-31 · confirmed · S** — No favicon or app icon ships: the root favicon path 404s and metadata declares no icons. Anchor: `apps/web/app/layout.tsx:39`. Cause: generateMetadata returns title and description only, no file-convention icon exists anywhere under apps/web, and the public directory holds a single brand folder. Fix: Derive the icon from the already-shipping Ledger Fold mark so tab and wordmark cannot drift, dropping icon.png and apple-icon.png at the App Router convention plus a favicon.ico for the root probe; do not also hand-write an icons key. Needs: none. OQ: Ledger Fold or the Clara mascot? The crop is a design call the owner should see at 16 pixels.
- **C-43 · confirmed · L** — The command palette's Go section is a static manifest, never rank-shaped, and cannot reach a client by name from firm altitude. Anchor: `apps/web/lib/command/routes.ts:73`. Cause: FIRM_ROUTES carries no floor field and the Go path filters by text only, while client routes render only once the URL is already under a client; the palette mounts outside the firm scope provider. Fix: Both fixes ride loadDoEnv's existing caller context: derive the Go firm rows from the navigation registries and filter with hasNavigationAccess, failing closed and distinguishing a failed read from an empty list; add a clients group over loadClientRegister. Needs: no new DB read; loadClientRegister already exists. OQ: Making Go asynchronous changes the palette's feel. The client-by-name half has no order. Cap, paginate, or query per keystroke?
- **owner: 确定我们有利用shadcn · design_task · S** — shadcn census: thirteen primitives vendored on Base UI at base-nova, and fifteen of the sixteen this program needs are absent. Anchor: `apps/web/components.json:3`. Cause: The CLI works locally with a receipt in the dropdown-menu header; only table is present of the sixteen, and data-table is a docs pattern needing a new TanStack dependency rather than a CLI add. Fix: Add in order: sheet for the firm drawer, tabs for the client nav plus a free roving-focus upgrade, then tooltip, popover, scroll-area and breadcrumb; each add strips dark variants, uses semantic tokens, carries a provenance header and adds no dependency. Needs: none; sonner and TanStack would each add one. OQ: skeleton contradicts the recorded prose-over-skeleton ruling; sonner and TanStack add dependencies; sidebar versus a hand-built drawer.

## Similar flaws noted by the explorers
- Dead emailRedirectTo option whose comment describes a journey the build no longer has · `apps/web/components/entry/signup-account-form.tsx:180` · delete the inert option or annotate it, so the next lane does not rebuild the deleted link.
- The handover assigns H-38 to a runtime lane, but the Session create is a web lane · `docs/plan/active/beta-handover-2026-09-04.md:207` · correct the owner field; stripeRoutes is webhook-only and contains no Stripe Session call.
- The copy-promises-a-control class repeats three times in the entry package alone · `apps/web/messages/en.json:1525` · one a11y cell per face asserting rendered prose and rendered controls agree, cheaper than three string fixes that rot.
- SignupDpaForm mints its op_key once for the component's lifetime · `apps/web/components/entry/signup-dpa-form.tsx:87` · re-mint when the document prop's version changes, once hydration lands, as signup-firm-form already does on edit.
- confirmation-resend.ts is the one Lane-B seam with no design-specified door to diff against · `apps/web/lib/registration/confirmation-resend.ts:54` · get an owner line on whether a resend spends the same rate-wall budget as a wrong guess.
- Every DialogContent call site passes width only, so the missing scroll container is product-wide · `apps/web/components/journals/compose-dialog.tsx:95` · the shared DialogBody fix covers the journal composer and correction wizard too.
- The commit block reasons mirror four refusal arms but not the checker arms, so a solo firm's attestation is effectively mandatory while presented as optional · `apps/web/components/clara/OnboardingChecklistCard.tsx:148` · say so in the copy.
- hasFinalizedOpeningSeed throws by design, withdrawing the whole card on an unreadable registry · `apps/web/lib/onboarding/api.ts:101` · any post-commit rework must keep that read or the summary claims less than it knows.
- Two mount-only readers of clients.status exist and neither re-reads · `apps/web/components/firm/client-register-list.tsx:58` · the same invalidation event fixes the register's stale row on back-navigation.
- agent_interruptions.question is untyped jsonb with no shape CHECK, which is how the reader-writer split survived · `packages/db/migrations/0006_runtime_core.sql:198` · add a shape CHECK or a contract pin to transcribe from.
- useActiveThreadId silently creates a chat session on every altitude it has never seen · `apps/web/lib/clara/useActiveThread.ts:80` · one un-archivable row per client visited per user; compounds with the missing archive door.
- listSessions already returns firm-shared threads that selectOwnSession silently drops · `packages/runtime/src/chatRoutes.ts:167` · the wire already carries the read half of a thread switcher, so no backend work is owed there.
- The v17 report tools are grant-live but inert: their only inputs are UUIDs no surface displays · `packages/runtime/workflows/chatTurn.v17.tools.ts:159` · a control the human cannot operate; needs a ruling alongside the export item.
- chatTurn_v17 has no close tool at all, though beta copy implies chat can run a close · `packages/runtime/workflows/chatTurn.v16.prompt.ts:48` · either add close tools in a v18 with an allowlist change, or say so honestly in the prompt.
- FormattedDate is a calendar-date renderer applied to at least one timestamptz deadline · `apps/web/components/journals/formatted-date.tsx:17` · sweep its other call sites for the same truncation misuse.
- The correction wizard's onDone reloads detail and filed but never the candidates cell · `apps/web/components/documents/document-detail.tsx:89` · same one-line omission as the upload panel's onFiled, different trigger.
- A failed reload leaves stale data standing under a red banner · `apps/web/lib/parts/hooks.ts:191` · correct per the sticky-refusal design, but "the list did not change" is a symptom of a failed read, not only a failed write.
- The web byte allowlist is a hand-mirrored copy of the runtime intake list with no drift guard on either side · `apps/web/lib/documents/bytes.ts:22` · pin all the lists against each other in one test.
- Two near-identical region renderers differ only in whether confidence shows · `apps/web/components/documents/document-extract-panel.tsx:98` · fold them into one component in whichever of the two document items lands first.
- The candidate loader silently drops a candidate whose document could not be read · `apps/web/lib/documents/loaders.ts:80` · honest by design, but the on-screen count can be lower than the DB's, which matters to the two-census discrepancy.
- Stale closed-world rosters are a repeating class and the activity feed's is live-wrong today · `apps/web/components/firm/firm-activity-feed.tsx:54` · one gate that reads each register out of the migrations rather than a hand roster.
- The needs-you openTab map has seven entries for nine row_kinds · `apps/web/lib/firm/needs-you-links.ts` · it degrades correctly through hasOwningTab; noted so nobody "fixes" it into a cast.
- The firm activity read is a flat limit of one hundred with no pagination · `apps/web/lib/firm/reads.ts:61` · honest today because the feed is near-empty, a real gap the moment the shims land.
- The activity feed keys React rows on receipt_id alone, though the address is the kind and id pair · `apps/web/components/firm/firm-activity-feed.tsx:46` · two union members can legitimately collide on a bigint PK.
- More raw DB verb names sit in shipping dialog copy outside the four copy items, in close and reports · `apps/web/messages/en.json` (ClientClose.closePrep.hold.description) · one repo-wide sweep rather than five separate PRs.
- retire_counterparty_alias is execute-granted but unreachable because no read can discover an alias id · `apps/web/lib/registers/counterparty.ts:94` · a backend read gap; fold it into the counterparty-identifiers migration lane.
- The review queue's compliance and lint envelope objects are typed unknown and dropped in one module while a second module reads them · `apps/web/lib/firm/needs-you.ts:234` · that envelope is the only human-reachable SST state.
- Two surfaces in this program have no test-manifest entry at all · `apps/web/test/manifest.txt` · client-workspace-overview and fa-register-tie each need a new manifest row, not just a new cell.
- entriesTruncated is computed and typed but rendered nowhere, while its sibling linesTruncated is surfaced · `apps/web/lib/journals/api.ts:248` · a client past a thousand entries silently shows an incomplete Posted list.
- The Posted list orders by created_at while rendering posting_date · `apps/web/lib/journals/api.ts:83` · a backdated entry sorts to the top under a date the reader will take as the sort key.
- The review queue's keyset cursor exists and is hard-wired to null · `apps/web/lib/journals/api.ts:220` · any real drafts pagination should ride that cursor, not a client-side slice.
- _opening_seed_deltas never references its p_include_drafts parameter · `packages/db/migrations/0017_wave_b.sql:3631` · a dead knob that reads as live; honour it or drop it, which is a new function rather than a replace.
- The statements form defaults to the first account before the human picks one · `apps/web/components/bank/statements-section.tsx:55` · becomes load-bearing once the header fix lands; render the resolved account name or force an explicit choice.
- Bank read-state error copy is also one-size-fits-all across eight reads with four different authorities · `apps/web/components/bank/read-state.tsx:56` · an errCopy override is the same edit as the emptyCopy prop.
- The human lane compares digits-only account numbers while add_bank_account preserves hyphens · `packages/db/migrations/0038_wave_c_b_bank.sql:1580` · flagged so the header fix is not later "corrected" to send the normalized form.
- The stale local runtime bundle still contains the retired autopost body, so the rule's built-bundle grep returns a false positive · `packages/runtime/.output/server/index.mjs` · rebuild before any bundle grep, or note it in the rule.
- The 23505 mask's own test cells use three fictional constraint names · `packages/runtime/tests/s6-closure-logic.test.mjs:72` · they prove the substring rule, not the behaviour on the constraints Postgres actually raises; pin the real names.
- statement-grammar.mjs is off the freeze surface only because its single importer is non-frozen · `packages/runtime/workflows/statementFacts.v1.engine.mjs:43` · a v3 importing it would pull it onto the freeze surface for the first time.
- The header normalizer admits null totals while the witness lane makes them mandatory · `packages/db/migrations/0098_f_a1_statements.sql:441` · a statement printing no totals block fails one lane and passes the other.
- _close_gate_undated carries the same missing document_kind predicate as the uncoded gate · `packages/db/migrations/0104_f_a4_pr_1a_measurement_layer.sql:364` · it must ride the same migration or the fix is half-done.
- domain_events has no rank floor while audit_log is bookkeeper-plus and receipts are bookkeeper-plus · `packages/db/migrations/0005_event_spine.sql:380` · three overlapping histories with three different floors; needs one explicit decision.
- The DR readiness table says ten roles and twenty-four memberships while the bootstrap mints eighteen or nineteen · `docs/ops/DR.md:213` · stale by eight or nine roles.
- The Clara rail has an enter transition and no exit, recorded deliberately · `apps/web/components/clara/ClaraRail.tsx:51` · an overlay that vanishes instantly reads as a bug in a way a docked panel does not.
- SectionTabs declares tablist and tab roles while every tab stays an individual tab stop · `apps/web/components/common/section-tabs.tsx:20` · an ARIA pattern half-implemented; vendoring the shadcn tabs primitive closes it for free.
- The client workspace header renders the client name as a paragraph, not a heading · `apps/web/app/(firm)/clients/[clientId]/layout.tsx:42` · at 200% zoom there is no heading to navigate by once the nav wraps.
- The skip link targets the firm content column only, by recorded decision · `apps/web/components/common/skip-link.tsx:16` · re-check that disposition once the full-screen routes become the primary narrow surface.
- PageShell's padding is unconditional at 64 pixels horizontal · `apps/web/components/common/page-shell.tsx:23` · about 20% of a 320px viewport; any reflow fix that leaves it alone still fails the reflow criterion.
- Command palette Go rows carry a planned-status badge path with zero planned rows in the manifest · `apps/web/lib/command/routes.ts:48` · a live but unreachable branch, worth knowing before that render path is rewritten.

## Counts
| Verdict | Count |
|---|---|
| confirmed | 50 |
| design_task | 11 |
| backend_gap | 10 |
| needs_owner_ruling | 5 |
| already_fixed | 4 |
| not_reproduced | 1 |
| **total** | **81** |

| Size | Count |
|---|---|
| S | 25 |
| M | 28 |
| L | 25 |
| XL | 3 |
| **total** | **81** |
<!-- end verbatim: register-condensed.md -->
