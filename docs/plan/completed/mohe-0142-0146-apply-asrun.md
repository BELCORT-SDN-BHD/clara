# The combined `0142`–`0146` apply — as run (2026-08-29, ONE D1 window)

**Live 136/`0141` → 141/`0146`.** Five files in one `migrate.mjs` run, each in its own transaction, in number order, from merged `main` `b2b0eca5` — `0142` F-A7b PR-a (windowless by inventory, folded in) · `0143` 裁-22 proposal bases DB-resolved (two `clara_wake_filing` doors replaced) · `0144` hardening A (`sign_vendor_identity_binding` replaced; eight `ALTER VIEW … security_barrier`) · `0145` P4 tranche-2 (five membership/firm writers replaced) · `0146` 裁-17 (a STABLE reader spliced). **Runtime stopped 03:17:01Z → `/ready` 200 at 03:17:52Z — the write-quiesce lasted ~51 s; the apply itself 03:17:14Z → 03:17:4xZ.** Sleeper `2862312b7993d8` created 02:29Z, destroyed at close. Scripts: the `ceremony-0141` set adapted (`01-rig.sh` · `02-backup.sh` · `03-pre-live.sh` · `04-window.sh` · `05-post.sh` · `live.sh` · `tripwire.sql` · `inflight.sql` · `quiesce.sql` · `post-reads.sql`); every live connection through `scripts/ops/dsn-pipe.mjs` (`sslmode=verify-full`, pinned CA, env-to-env, never printed).

## The five refuse-conditions, each read POSITIVELY before the window

1. **Pre-window sweep ALL-GREEN on merged main** — `gh workflow run ci.yml` on `b2b0eca5` (run 33228990585): lint · typecheck/build · db-estate · db-live-gates · render-drill · the closed-wave drills (incl. hardening A's new recut-guard step) · the four D-b frontier legs — `completed success`. (An earlier sweep on `9289fcf5`, dispatched because #410 changed the drill action, also green.)
2. **Backup banked** — the scheduled `clara-backup` machine `d895470c6024e8` started at 02:05:25Z; its own run `run-2026-08-29T02-05-41-795Z` logged `migration-head: 136 migration(s)`, `bundle: age encrypt (1 recipient)`, `r2: rclone copyto … db-snapshots/2026/2026-08-29T02-05-41-795Z`, **`clara-backup: DONE — bundle 23172797 bytes`** at 02:07:21Z, plaintext staging purged, machine auto-stopped. *Instrument note:* `02-backup.sh`'s log poll compared an ANSI-coloured first field against its start timestamp and kept an EMPTY tail (exit 1 with the backup DONE) — the DONE line was read directly from the machine log with this run's id; the script now strips ANSI first.
3. **Rig-vs-live tripwire IDENTICAL** — a throwaway `postgres:17` at the LIVE frontier (0001–0141, 136 files, 117 s), `tripwire.sql` captured the prosrc sha256 + ACL + owner of every body the five files replace or read (21 functions), the eleven masked views' reloptions/ACL (three barriered pre-window), the absence of every name the files create (4 relations, 7 functions), the allowlist count (87), the two partial-unique invariants: **45/45 rows byte-identical** as SORTED sets; the one REPORTED row, `OP_RECEIPTS_D1` (0143's changed Door-1 fingerprint), read **0** on live — no pre-migration reservation to strand.
4. **In-flight ZERO, with positive controls** — `agent_tasks` 0 in flight of 232 · `document_processing_tasks` 0 open of 435 · `document_intakes` 0 in flight of 159 · `wake_engine_sources` bank_agent/close_prep both `enabled=false` · pending OWNER-role invites 0 (0145's accept-time re-check has nothing to retro-refuse) · operator firms 0 (0145's doors inert until the `0133` act) · **live `approve_opening_seed` / `approve_opening_correction` `proconfig` carry `default_transaction_isolation=serializable`** — the manual wave-b Part-A pin IS on live (owner-batch item 11 closes: no action; T2's operator hint is a belt only). Quiesce inside the window: 11 idle `clara_runtime_login` sessions reaped, `NON_IDLE_AFTER|0`.
5. **TLS both polarities** — every read and the apply went through the bridge's `verify-full` + pinned CA (a positive by construction: the bridge refuses any other mode); `/ready` positive-controlled at 200 before the window.

## The apply and the post-reads (live)

`migrate: 5 new migration(s) applied · 141 total` — each file's own prestate and tail notices fired on live exactly as on the rehearsal rig: `fa7b pr-a prestate: clean … tail: OK (D1 EMPTY, confirmed)` · `proposal_basis_resolved prestate: clean — frontier 0142, both live bodies pinned by exact prosrc sha256 … tail: OK` · `hrd-a prestate: OK — the same-shape family derives to EXACTLY the expected 11 members, two-sided … tail: OK — EVERY member now carries security_barrier` · `p4t2 prestate: OK — 7 new names clear … tail: OK` · `ninth-rowkind (裁-17) OK`. The applied file shas (`0142` 52f72efd… · `0143` f4c687cd… · `0144` b7528a9d… · `0145` 988776c6… · `0146` 561ede4d…) are the bytes of merged main `b2b0eca5`.

Post-reads on live, sorted, diffed against the rehearsal rig's post-reads (a fresh 0001–0141 rig + the same five files applied 02:26Z): **identical apart from the two `FROZEN|…` count rows** (`workflow` 23 · `graphile_worker` 20 — the frozen schemas exist only on live; constraint 15, reported not asserted). Positive reads, verbatim class: `FRONTIER|0146…|141` · the five `APPLIED` rows · `BARRIERED_COUNT|13` (eleven + 0145's two new views) · `OLD_OVERLOAD_GONE|true` (the 8-arg `wake_propose_identifier_promotion`) · both wake doors granted to `clara_fn_owner,clara_wake_filing` only · `_resolve_proposal_basis` ungranted · `KIND_CHECK|true` · `WALL|true|reason=true|ruled_words=true` (the signer≠proposer wall NULL-safe, typed, in the owner's words) · `CEILING|…|true` ×3 (the role ceiling BEFORE `_reserve_op` in `set_member_role`/`invite_member`/`add_member`) · `ALIAS_BASE_ACL` = `clara_fn_owner` + `clara_freeform_ro` only (the Wave-A fn-fronted invariant kept; the human read is the barriered view) · `TABLE|firm_registration_requests|rls=true|forced=true|acl=(null)` · `TABLE|onboarding_agent_receipts|…` likewise · `NINTH|true|open_batch_join=true` · `ALLOWLIST|88` (87 + 0142's filing row) · `OPERATOR_FIRMS|0`. Every replaced body's post sha on live equals the rehearsal rig's (`sign_vendor_identity_binding` 5285581e… · `list_review_queue` d3a8ba44… — **note:** the reviews measured `dd2dee4f…` for `list_review_queue`; that value predates the merge-prep renumber, whose citation truings changed comment bytes inside the spliced arm — rehearsal and live agree byte-for-byte, and the migration's comment-stripped roster + BEHAVIOURAL postcheck passed on live).

## The close

Runtime `48ee715b763048` restarted 03:17:42Z, `/ready` 200 after 10 s; sleeper destroyed; the rig container removed. `05-post.sh` (a fresh sleeper, the same post-reads, windowless) is the standing re-proof recipe.

## What is now LIVE and what it changes for the frontend

- The agent can PROPOSE client onboarding (`0142`) and both proposal doors persist DB-RESOLVED citations (`0143`) — PR-e's `/needs-you` proposal card renders real regions.
- Six cross-tenant read channels are CLOSED (`0144`: the barrier over the whole masked-view family); the vendor-binding Sign door refuses a self-signer with the owner's words.
- Self-serve firm registration + the operator approval queue exist but are INERT until BELCORT is flagged `is_operator` (the `0133` ops act); the role ceiling binds every membership entrance; `counterparty_aliases_visible` is T8's alias read.
- The firm needs-you inbox emits the ninth row_kind `seeding_proposal` for open batches only.

## Owed next

Hardening B (裁-16, `0147`) — its own D1 window (`create_firm` + `invite_member`), after its review clears (Codex leg: a legacy-receipt scrub owed). The P4 UI tranche after the pricing sitting. The 0133 operator-flag act for BELCORT when the owner is ready.
