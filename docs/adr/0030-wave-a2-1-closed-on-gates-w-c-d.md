### ADR-030 — Wave A2.1 CLOSED on Gates W/C/D (owner ruling WA21-R13); the 0016 ceremony executed; Gates S/P split to a follow-on eval deferred to real documents (2026-07-23)
**Decision:** Wave A2.1 is **CLOSED** (owner ruling **WA21-R13**). The owner-gated **0016 deploy ceremony executed clean**: fresh full-profile backup (R2 bundle + dead-man's-switch green) → write-quiesce (v23 stopped; the v24 image pre-built to shrink the window) → **0016 applied to live in one atomic transaction (16/16, post-verify 13/13** — the B4b repair declined exactly the one customer/AR `vendor_account` rule with its audit row; B4c/B4d zero targets as probed; books untouched at 32/33; agent role zero-EXECUTE re-asserted) → **runtime v24** (chatTurn_v6/autoDraft_v2 + the three new consumers; all 8 loops acquired; `/ready` clean) → the WA21-R11 six-doc re-classification via `classify_document`'s no-task path (verdicts `other@0.90–0.97` — honest: the docs are aggregate JV memos, not payroll registers; the live `facts_gate` re-fired all six to terminal `skipped_kind`, zero egress) → the rclone-1.74.4 backup image proven live (supervised run: **zero 501s**, after PR #61 fixed the `ARG RCLONE_VERSION` collision with rclone's own env-config namespace). **The §9 eval then closed W/C/D on live books:** **Gate W** — the owner's turnover classification (500-000 `included`, effective 2025-04-01) → evaluator: **`overdue`, earliest crossing 2025-06-01** (cumulative RM 663,056.51 > RM 500,000.00 at June-end, independently cross-checked), application due 2025-07-31, **confirmed included RM 1,310,276.40 to the sen** (mixed → 0); the queue row at `needs_you`/overdue with integer-only counts + fresh evaluator; **the agent surfaced the watch UNPROMPTED during an unrelated coding turn** with basis label + verification status + the supplied `application_due` + "not a legal determination" (the §9-A2 assertion passing in production); audited ack ×2 with rationale, the +10pp re-arm ladder armed (RM 1,360,276.40), overlay-never-erases held visibly. **Gate C** — a fresh hardcopy-scan bank statement through real intake → the classify lane → `clara-classify-llm:v1` verdict **`bank_statement@0.99`** → kind set, `invoice_facts` terminal `skipped_kind` at attempts=0 (zero Azure spend). **Gate D** — the ComplianceWatchCard's full surface verified live (tier banner, three basis-labeled figures, the s.13(1)/(3) countdown, the always-on qualification). **WA21-R13:** Gates **S/P are follow-on eval items** — S structurally cannot run at close (zero credit sightings; the structured floor needs ≥3 post-0016 human-approved sales entries; no MyInvois XML exists), P has no unused SST-stated bill; both close on the next real document cycle, never on synthetic documents.
**Why (recorded lessons):** (a) **The envelope refusing a day-one sales rule is the control working, not a blocker** — the side-aware sighting pool starts empty by design, so autopost authority must be re-earned with post-0016 human approvals; recording S as pass-by-refusal-and-defer beats fabricating evidence on real books. (b) **The layered gates held against the agent's own run grant**: the permission classifier stopped the agent at exactly the two live mutations that mattered most (the migrate, remote exec on the runtime) and the owner-`!` handoff protocol absorbed them without friction — defense-in-depth behaved as designed. (c) **Live execution keeps catching what review cannot**: the OpenAI org quota outage (would have silently broken chat; caught by the ceremony's first model call), the bank-export PDF's encryption tripping the intake quarantine (a Slice-5 control verified live), the boot-race duplicate-watch insert (the partial-unique index arbitrated, the belt's receipt pass self-healed in-cycle — exactly-once proven under real concurrency), and the `ARG RCLONE_VERSION` build failure (rclone treats every `RCLONE_*` env var as config — invisible to static review). (d) Deferred flags recorded in PART 2: no DB terminal-fail writer for the classify lane (runtime caps at 3 attempts; a `fail_classify` writer is a 0017 candidate); the card does not echo `acknowledged_at` (the queue envelope lacks the field); the queue section-order ruling remains open. Ref: PRs #57–#61, the ceremony + eval evidence in the session record, memory `project-clara-rebuild-state`, ADR-027/-028/-029.
