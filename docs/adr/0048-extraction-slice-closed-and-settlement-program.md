### ADR-048 — THE EXTRACTION SLICE CLOSED + interview_v2 + the pre-Wave-C settlement program (2026-07-28)
**Decision:** The extraction slice (contract v1.0, ADR-047) is **CLOSED as built**: migrations 0022+0023 and runtime v30→v32 live via two §0-guard-proven quiesced ceremonies; XG1(amended)/XG2(amended)/XG4/XG5 CLOSED on live receipts, XG3 OPEN→the runway (receipts: `docs/plan/research/extraction-slice/slice-close-2026-07-28.md`). The OCR tier's `corroborated` is now **explicit two-reader agreement** — per-field typed-collapsed evidence for net AND tax plus the corrected component identity; vendor confidence is GONE (ADR-047 Q1); the honest live result is **zero corroborating documents today** (the sole pre-K1 flip was typed-only — the exact false-corroboration path the adversarial ladder refused; 5 Codex rounds total on X5, 3 each on X2/X6, every finding reproduced before fixing). `interview_v2` shipped in the same image (PR #120, 2 Codex rounds): five registration families incl. LLP/PLT, the owner-ruled escape hatch (`format_verified:false`, never blocking), the two-axis entity-aware framework/basis config with exactly two statutory hard blocks, v1 byte-identical with in-flight runs preserved, manifest 80/80 deploy-locked (#121). **The owner's 2026-07-28 program rulings are recorded as law**: synthetic/sample evidence SANCTIONED pre-GO-LIVE (Phase-5 methodology pulled forward; such closures are LABELED synthetic in the ledger, never claimed as real); the C6 owner/legal items GRANTED for the test posture; deferrals only per the owner's three criteria (pre-GO-LIVE-only · future-wave · full-E2E), each named.
**Why:** The slice's purpose was an extraction tier Wave C can stand on, and the closing posture is the proof: posting authority now derives from structural agreement a database can verify, not a self-reported model score — and it honestly grants that authority to nothing until a document earns it. The wrong-money constructions the ladders killed (the transposed net/tax swap, the negative-discount forge, the double-counted component, the unbounded rounding) are each a committed refusal cell now. The settlement program (runway on the real corpus · Gate S/L/K-doc synthetic · 0020 activation · the backlog) runs next so Wave C starts debt-free.
