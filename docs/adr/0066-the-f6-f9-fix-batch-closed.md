### ADR-066 — the F6–F9 fix batch is BUILT, DEPLOYED and ACCEPTED: Wave E's first strike closes on real books, and a six-round-reviewed reader fails its field test (2026-08-09)

**Decision:** **The F6–F9 batch — E-R1's first strike into the Wave E build — is ACCEPTED and CLOSED**, with F6 accepted **PARTIALLY WITNESSED** and said so rather than rounded up. Evidence of record, as-run and evidence-grade: **`docs/plan/completed/wave-e-f6f9-acceptance.md`** — cite it, don't restate it; it carries the receipts, the counters, the ceremony log and §6's honest boundary. The findings' origin is ADR-064 §3; the boundary ruling is E-R1 (ADR-065); the reader's field story is `docs/plan/completed/extraction-slice-x7-field-record.md`; the **review records of record are the Law-1 record comments on PRs #216/#217/#218/#219/#220**. **Live at the close: 53 migrations (frontier `0054`) · runtime v60.**

**(1) WHAT SHIPPED — the rulings; the build narrative is the acceptance record §§1–2.** **F6 → `0051`:** §1 admits a lane whose newest task is terminally `failed` (a POSITIVE read of the task row; a failed first attempt leaves ZERO extraction rows, so any extraction-row read would have been absence-as-evidence), §2 mints a recovery attempt when the user re-uploads the same bytes against a terminally-failed ingest lane, and ADR-062's registered extraction-recovery door is discharged in the same item per E-R1's ONE-item ruling. **F7 → `0052`+#220:** the product's first deterministic customer-identity reader. **F8 → `0053`:** a five-conjunct re-admit arm, each conjunct reduction-proven — **a reversal is NOT a withdrawal**. **F9 → `0054`:** regions cited by a server-assigned `region_idx`; the provenance wall stays **byte-untouched**. New closures `autoDraft_v7`/`chatTurn_v10`.

**(2) THE LADDER** — ADR-061 uniform intensity, native + Codex on every unit of all five PRs; **the round counts live in those PRs' Law-1 comments.** Durable: **six stated justifications dissolved under measurement, two of them the native reviewer's own**; rejected predicates stay **executable in CI**; the normalization law — **admission narrows, comparison merges, never unify**.

**(3) THE CEREMONY, THEN THE FIELD FAILURE** — run log: acceptance record §3. **F7 — six rounds reviewed — FAILED on the two real documents it was written for**, not by refusing wrongly but by never GENERATING the company as a candidate. #220's ruling: vendor **proximity** gives way to vendor **IDENTITY**, both terms **refuse-only** (review law 3 — a name is a projection and may never admit).

**(4) ACCEPTANCE — counters and receipts: acceptance record §4; these are the decisions.** Verify-before-approve caught the field failure with **zero harm**. **The audit instrument itself was corrected mid-acceptance to exact equality on the outcome token: a `like '%re_admitted%'` audit yields 5 pre-ceremony false positives — *spelling is not identity*, applied to the measuring instrument.** The name-only birth is F7's receipt; the counterparty was born **ONCE**, `256d6100`, **NAME-ONLY** (registration NULL, tin NULL) — RS now stands at **11 customers / 0 registrations** and the F3 enrichment trap holds; entry `f6da5aff` is approved with the TB tying to the sen. **F6 is PARTIALLY witnessed and is accepted as such:** the reextraction branch ran **4× live** (new version rows; terminal rows never mutated), the **`failed_retry` branch is UNWITNESSED** (drill C1 needs a purpose-built sandbox upload — open), and the §2 live read **corrected the register**: **Gate P's waiting population is SEVEN documents, not four**, every one with a newest `ocr` task `failed`/`bad_type` and `document_kind` NULL, so **the door refuses by design on both halves** and the honest remedy is an owner **re-export** or the **Wave-F 401/403 auth-code split**. **F6 therefore does not unblock Gate P** — E-R1's expectation is corrected by measurement, and Gate P stays operating runway. The 2506 leg stays held at §7-A's `0046` watermark (owner's backfill call).

**(5) OWNER ACCEPTANCES (recorded).** X7's **residual (5)** — suffixed relational phrases that mention a company without being the addressee — was accepted 2026-08-09 (*"可以, 我点头"*) on the two-lane-verified post-R6 record, and **RE-CONFIRMED the same day on the WIDENED envelope**: the anchor sweep adds a second way in on label-less pages, so the residual is wider than the round-6 record stated, and the owner re-nodded knowing that. Harm ceiling: a wrong DRAFT behind maker/checker — no unattended-post path reaches the field. **FINCARE stays a human coding decision** and was never in F7's scope: attribution anchors on the typed `CustomerName` region, and relaxing that to "far from the vendor" would be absence-as-evidence.

**THE STANDING LESSON — minted by A1, carried on PR #220: *a wall that never refused anything is not a wall that held — it is a wall that was never asked.*** (Measured basis: acceptance record §7 + the field record's header.) The corollary, now standing practice: a review round must ask what the code has never been ASKED, not only whether what it was asked was answered correctly — and a zero-count refusal head is a question to open, never a wall to bank. This batch banks neither of its own: `in_vendor_block`/`is_vendor_name` counted **zero** live and are recorded unproven.

**Why:** the four §7-A findings went first because real documents had already broken them, and two were holding real rows out of real books. The batch's real product is epistemic: the first time in this rebuild that a fully-reviewed, fully-green judgement component **met the field and lost**, caught by the acceptance procedure — not the tests, the reviewers or the author — at zero cost. That is the argument for acceptance staying evidential rather than argumentative, and for ADR-061's uniform ladder staying a floor rather than a ceiling.

**Supersessions.** Nothing is superseded. **ONE prior statement is CORRECTED by measurement: E-R1's "F6 unblocks Gate P's four waiting manual bills"** — the waiting population is SEVEN, and the door refuses by design on both halves; the remedy is an owner re-export or the Wave-F auth-code split. Two further items are **DISPOSITIONS, narrower than the findings that asked for them**, and both live in PART 2: **ADR-064 §3's F8** asked for either a reworded message or a reset door and got **both, deliberately bounded** — one re-admission per withdrawal, `p_origin='one_click'` only, so a no-op retry spends the door and the estate sweep can never spend it at all · **ADR-062's extraction-recovery-door registration is DISCHARGED**, built as `0051` (`0053` reuses `0034_autodraft_retry_door.sql`'s supersede mechanics verbatim on the F8 side). E-R1's first-strike ruling itself, ADR-064's findings, and every prior ADR stand unchanged.
