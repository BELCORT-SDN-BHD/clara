# ADR digest — the 2026-09 sprint-ruling rows (§15, continued)

*This file is the continuation of the digest's §15 · Sprint-ruling amendments (裁-140): the
per-ruling ROWS themselves. It was opened on **2026-09-03** because `README.md` reached its
500-line ceiling — the same convention the ruling ledgers already use when one file fills
(`mohe-grill-rulings-2026-09-02-pm.md` continues `-09-02`, and `-09-03` continues that one).*

*Three rules travel with the split. **(1)** The LAW — how a sprint ruling enters the ADR system —
stays in `README.md` §15 and governs; the rows here execute it. **(2)** Each ruling's TEXT lives in
its ledger under `docs/plan/active/`, which governs on any divergence with a row here. **(3)** The
block below was moved BYTE-FOR-BYTE (md5 `e860d0fc8186a233722af7385ff2dae0` on both sides of the
move, 16,777 bytes), which is why it opens by restating item 86's own law sentence: the line moved
whole rather than being re-typed. Every row minted after the split is written HERE, never in the
digest.*

**COUPLING MARKER — read before editing either file.** Item 86's opening sentence below is an EMBEDDED COPY of §15's law text in `README.md`: **if that law text changes, this copy changes with it, in the same commit.** It exists only because the line moved whole (that is what makes the md5 above provable), and `README.md` §15 remains the authority on any divergence. The rows themselves have no such coupling — they live only here.

## The rows (裁-111 · 114 · 125 · 129 · 131 · 133 · 135 · 139 · 141 · 143 · 144 · 145 · 146 · 147 · 148 · 149 · 150)

86. **A sprint ruling enters the ADR system as a DIGEST ROW plus an "amended by" line on the ADR it amends — never as a new ADR** *(裁-140, owner, 2026-09-02 ~23:40; permanent)*. One row here, one dated line in [`README-log.md`](README-log.md), each stating its TIME BOX and pointing at the ledger entry; a new ADR is minted only when a ruling contradicts an ADR's text outright AND permanently. Rationale: the sprint's rulings are mostly time-boxed, so writing each as a permanent ADR would manufacture the next stale record; a consolidating ADR was offered and declined. **The rows — 裁-114/125/129/131 live in `docs/plan/active/mohe-grill-rulings-2026-09-02.md` and 裁-133/135/139/140/141/143/144/145 in its continuation `docs/plan/active/mohe-grill-rulings-2026-09-02-pm.md`, and **裁-146** in the NINTH ledger `docs/plan/active/mohe-grill-rulings-2026-09-03.md` (each file split at the previous one's 500-line ceiling); 裁-111's own file is named on its row:** **裁-111** *(2026-09-01; UNTIL BETA LIVE, resumes unless the owner rules otherwise)* — law 28's cross-family Codex adversarial leg is SUSPENDED, not repealed; the one fresh-context opus review is the complete gate meanwhile (ledger `docs/plan/active/mohe-grill-rulings-2026-09-01-pm.md`; amends ADR-0077 §14). **裁-114** *(2026-09-02 morning; permanent — amends PRD §6, the row this section owed since 裁-140 and did not carry until 2026-09-03)* — the owner corrected BOTH of §6's texts that the drift scan found contradicting ruled-and-shipped reality. **(1) The split-trust corollary** (PRD §6 + ARCHITECTURE §1) read "service credentials live only in the agent service", false since P4-4, and is **re-stated as the wall that actually holds: no service credential ever reaches a browser; no `NEXT_PUBLIC_`-prefixed variable ever carries one; `apps/web`'s server-only Route Handlers are a second, browser-isolated holder alongside the agent service** — the alternative, demanding the code move, was declined because it would reject the ruled checkout design. **(2) Invariant 16** was titled "client data egress is governed" but described only observability-trace export, and is **split into 16(a) document/OCR egress** (typed consent + separate owner activation, re-checked at dispatch — the `0020_typed_consent.sql` subsystem, ADR-0040/0041, law 58, which the beta DPA itself cites) **and 16(b) trace export** (the existing ADR-011 text), ARCHITECTURE gaining the matching subsection. Both are truings of already-ruled decisions; the DECISION is that the law documents say so, and both are EXECUTED — §6's "Split-trust corollary" paragraph and ARCHITECTURE §1's runtime line each carry the re-stated text and cite 裁-114 by number (measured on this commit's tree). **Amended by 裁-142 (2026-09-03):** the premise's EXAMPLE named the wrong secret — `STRIPE_WEBHOOK_SECRET` is runtime/Fly env, not `apps/web`'s (裁-126 routes the `whsec_` value to Fly secrets, and 裁-93 had already ruled the webhook to `packages/runtime` before 裁-114 was written); `apps/web`'s real FS-4 credential is `STRIPE_SECRET_KEY`. **The RULING stands** — the wall it re-states is unaffected by which credential illustrates it. The two law documents inherited the same wrong example and were corrected by the owner's own ruling, **#526 MERGED `5d70b8dd` (2026-09-03)**, not by the lead's errata: PRD §6's corollary now carries "example corrected by 裁-142" and names `STRIPE_SECRET_KEY`, and ARCHITECTURE §1 marks `STRIPE_WEBHOOK_SECRET` as RUNTIME env. Full text and the errata verbatim: the ledger's `## 裁-114` entry. **裁-125** *(2026-09-02; UNTIL OFFICIAL LAUNCH)* — every user-facing legal text ships as an agent template, lawyer-refined at launch, and is NEVER darkened or cut. **裁-129** *(2026-09-02; permanent, figure subject to the lawyer's pass)* — the beta terms are a SEPARATE document kind from the DPA (never one combined signature), an RM 5,000 liability floor, the courts of Kuala Lumpur. **裁-131** *(2026-09-02; permanent, value SET only once C-5's attempt wall is live)* — Email OTP expiry is 60 minutes for BOTH arms and `checkout-gate-design.md` C4 is amended. **裁-133** *(2026-09-02; UNTIL BETA LIVE)* — no Codex lane of any kind, builds included; native lanes only (amends ADR-0077 §14 / law 28). **裁-135** *(2026-09-02; permanent until the owner reverses it at official launch)* — the repo is PUBLIC and CI is GitHub-hosted, the owner OVERRIDING the 裁-134 recommendation; amends law 38 and supersedes 裁-134's slot cap (see §10 and the 2026-09-02 minute). **裁-139** *(2026-09-02; permanent)* — a firm member is REFUSED at `POST /checkout` before Stripe is called, under checkout design §5's "no path may strand a paying customer without a firm". **The four PERMANENT review laws of 2026-09-01, one row for the set** *(permanent — they bind every PR, not just the sprint; ledger `docs/plan/active/mohe-grill-rulings-2026-09-01-pm.md`, which stays the source of truth and is NOT compressed into these lines)*: **裁-107(a/b)** a bidirectional seam↔door parameter diff lives in the PR BODY — a dropped door PARAMETER is a defect, a dropped RETURN field a recorded widening · **裁-108** an UNNUMBERED migration merges silently and NEVER applies, so the number claim is what ARMS its tests (rename → repo-wide citation true-up → prove nothing mechanical keys on the FILENAME → a fresh-rig re-verify that it applies IN SEQUENCE) · **裁-112(a/b/c)** the OVERCLAIM class — construction-path scoping for absence claims, a model is never its own oracle, and a cell proving a gate discriminates EXECUTES the gate rather than a copy of its predicate · **裁-113** a dynamic-SQL waiver is CHECKED (the literal-at-EXECUTE rewrite), never trusted. **裁-141** *(2026-09-03 ~00:10; permanent — AMENDS 裁-37)* — the ⌘K "Do" palette pre-filters on the caller's DB-computed role rank plus each door's floor TRANSCRIBED into the web, re-read on every open, **with a required DRIFT GUARD** (a cell that reads every transcribed floor against the live door's and reds when they diverge — a projection of a door is law 27(3)'s class, so it must be pinned to its source); the door stays the only authority, `clara.wake_fn_allowlist` stays invisible to application roles, and the declined alternative was a new read door over it (literal on 裁-37's first half, a breach of its second). **裁-143** *(2026-09-03 ~15:05; permanent — amends PRD §2 and §4, ARCHITECTURE §3.3; the product-law counterpart of ADR-0075's operator-firm rewrite of `AGENTS.md` constraint 13)* — the **OPERATOR tier** (BELCORT, `clara.firms.is_operator`) is written into the product law as a **SEPARATE TIER BESIDE the four permission levels, never a fifth level above the firm owner**, and as operational-support tooling rather than a product surface, with a **HARD CAP**: two read queues — registration applications and Stripe problem events — plus the one estate-wide wake-source control that reads no firm's data, and **never any figure of another firm's books**. Both queues are pre-firm admission-plane objects and no operator door reads a book table, so **PRD §6 is UNTOUCHED by design** — the cap carries a cross-reference to invariant 2's tenancy wall instead of new §6 text. The flag is `not null default false` with `uq_firms_one_operator` admitting at most one operator firm ever (`0133_g1_wake_engine.sql`), is carried by NO firm today (裁-43), and is flipped only by the raw owner-run one-shot ceremony `docs/ops/g1-operator-firm-ceremony.md` — never an app screen or API — at FS-11's Wave-G reset (裁-121③). The tier landed as a NAMED §4 subsection beside item 1, not as a numbered `§4.1`: the PRD carries no such heading and the ruling's shorthand was not made into a new anchor. **裁-144** *(2026-09-03 ≈15:18, shell clock; permanent — amends PRD §8)* — billing's **TIER tranche is a BACKLOG item, completed before the official launch 上市 and NOT before beta live**, and PRD §8's billing cell is re-cut IN PLACE by date, in the style §8's tax row already uses: **BETA (built — FS-4 + C-2/C-5)** = Stripe Checkout + webhooks + the fail-closed admission gate + `clara.billing_plans` (`0163_checkout_gate_c3_folded_door.sql`, `amounts_ruled=false` → RM0/trial); **BEFORE 上市 (backlog)** = paid seats, Active-Client slots, the shared firm-wide AI allowance in RM and its overage, and invoicing — the tier tranche of `docs/plan/active/billing-design.md` §5 (the rest of PR-1 + all of PR-2) plus an AI usage ledger. Measured 2026-09-03: **zero** files under `packages/db/migrations` carry `billing_usage_rates`, `firm_subscriptions`, `client_lifecycle_events`, `evaluate_firm_billing_v1`, `invoice_lines`, `get_firm_invoice` or `issue_invoice`, so §8's promise that "`issue_invoice` refuses" named a function that **does not exist** — that code census over `packages/db`, `packages/runtime` and `apps` is the whole scope of the claim; the name survives only in docs prose — and is re-worded to **"no invoice is issued in beta"** (a guarantee may not be carried by a name with no thing behind it — law 27(3)'s class). The 裁-71④ scope note and every earlier citation (裁-42, 裁-50–56, 裁-57, 裁-58, 裁-68) are KEPT, and the ringgit amounts still defer to the pricing sitting (裁-58). Beta live launches on the built half — signup → the emailed confirmation code → the DPA signature → Checkout at RM0 → a born firm, plus invites and /pending. **裁-145** *(2026-09-03 ≈15:27, shell clock; permanent — annotates PRD §9 item 3)* — the tier-3 **signup-gate composition** that 裁-57/58/68 fixed as "DPA e-sign + rate wall + email-bound token + Stripe checkout success" now names **FIVE** items, and one of the original four is retired. The fifth is the **Beta terms of service** (`docs/ops/legal/clara-beta-terms.md`) — a SEPARATE document kind from the DPA, never one combined signature (裁-129), shipped as an agent template refined with a lawyer at official launch (裁-125). The **email-bound token is RETIRED** by 裁-89's one-transaction fold of claim → create-firm → close-registration ("no admission token survives", ARCHITECTURE §1a) and was **never built** — measured 2026-09-03, zero hits for `email_bound` / `email-bound` / `bound_token` across `apps/` and `packages/`. So four of the five are live: DPA e-sign · Beta terms · rate wall · Stripe checkout success. The note is added in the "原地加日期" style beside the original text, which is kept for provenance; 裁-58's open ringgit-amounts question is untouched, so §9 item 3 stays an open question. **Answered in the same breath as a repo FACT, not a ruling** (the owner's `btw`): step 1's confirmation mail is sent by **Supabase's own auth mailer**, triggered by **`supabase.auth.signUp`** (`apps/web/components/entry/signup-account-form.tsx`:167) — the confirmation card's RESEND control is not the sender and sends nothing today, its seam returning `{kind:"unavailable"}` pending a walled route handler — and **Resend the vendor carries INVITATIONS only** (`RESEND_API_KEY` appears solely on the invite path); whether that Supabase project has custom SMTP configured is a dashboard fact and is not asserted.
87. **Mail: Supabase Auth's SMTP is pointed at Resend, and Wave-G's "Mail" line is a LAUNCH GATE** *(裁-146, owner, 2026-09-03 ≈15:51 MYT by the shell clock; permanent as an arrangement, and the gate binds until the Wave-G walk certifies it — ledger `docs/plan/active/mohe-grill-rulings-2026-09-03.md`, the NINTH ledger and the first entry in it, which governs on any divergence; recorded per 裁-140 as a row plus a dated line, no new ADR)*. **Who sends what:** signup confirmation and password reset are sent by **Supabase Auth's own mailer over CUSTOM SMTP pointed at Resend** (host smtp.resend.com, username the literal string `resend`, password a Resend API key the owner enters at Authentication → SMTP Settings or through the Management API, PATCH /v1/projects/{ref}/config/auth, with his personal access token at FS-11 — never in the repo, never printed); **invitations** are sent by the **Resend API from the server-only invite route** (`apps/web/app/api/invite/route.ts` → `apps/web/lib/members/invite-mail.ts`); the **sender** is a no-reply mailbox on **mail.clarabook.com** — the one Verified entry in the owner's Resend dashboard read 2026-09-03, and the domain `INVITE_MAIL_FROM` must carry, the repo itself pinning none (it ships blank). **Why custom SMTP is not optional:** Supabase's DEFAULT mailer refuses every address outside the project's organisation team (*Email address not authorized*), sends at 2 messages/hour, carries no SLA and is documented as "not meant for production" (the official auth-smtp guide) — and the failure is invisible from the app, because `supabase.auth.signUp` (`apps/web/components/entry/signup-account-form.tsx:167`, the call that actually triggers the confirmation mail) resolves normally and the UI paints "check your email". **The cap** is the Resend plan's, never Supabase's 2/hour — with Supabase's own auth rate limit in front of it, which starts at 30 messages/hour when custom SMTP is saved and is raised on the Rate Limits page in the same owner act. **Templates and expiry:** the *Confirm signup* template stays the six-digit `{{ .Token }}` with nothing to click (裁-92) because the confirmation card reads a numeric one-time code, while the *Reset password* template keeps a LINK because that arm spends a `?code=` through `exchangeCodeForSession`; the single Email OTP expiry is 60 minutes for both arms (裁-131). **The gate:** the Wave-G "Mail" line certifies ONLY after a real signup confirmation is sent to and received at a **NON-team** address through the custom SMTP — a settings screenshot does not certify it, and neither does a message delivered to a team address. Executed in `docs/ARCHITECTURE.md` §1a (the standing "Mail — who sends what" paragraph), `docs/ops/wave-g-setup-checklist.md` (the owner act before the walk + the certification line) and `docs/plan/active/security-pass-2026-09-02.md` item 6 (re-cut so "the Supabase project's own email rate limits" names the two real numbers rather than the default mailer's; **裁-102 stays OPEN** — the server-side wall on the send path is unbuilt either way). **CORROBORATES, not corrects, one point of fact in 裁-145's `btw` half**: that ruling first named `supabase.auth.resend` as the confirmation path, and **#535's own review fold has since corrected it in all three of its carriers** (measured on that PR's head `303f8586` — its ledger entry, its digest row and its log minute each now name `supabase.auth.signUp`), so in either merge order there is nothing left here to correct and this row records the measurement rather than a change. The string is live nowhere — `requestConfirmationResend` (`apps/web/lib/registration/confirmation-resend.ts:52`) returns `{kind:"unavailable"}` unconditionally; scoped census, `git grep` over tracked files: **exactly three under `apps/`** (two modules under `apps/web/lib/registration` plus `apps/web/README.md:426`), and every other occurrence is document prose — the ADR digests and the ruling ledgers — none of it a send path; the repo-wide number is deliberately unstated, because it moves each time a ruling writes about the defect. 裁-145's ruling and its conclusion (Supabase's mailer sends it, Resend the vendor carries invitations) were never in question. **The owner's acts that afternoon, recorded at their true weight.** **MEASURED:** custom SMTP was configured ≈16:08 MYT 2026-09-03 (Enable custom SMTP ON, host smtp.resend.com, sender no-reply@mail.clarabook.com, sender name Clara; port/username/password below the fold and not read back), and at **≈16:55** a Supabase *Invite user* mail sent through it arrived at a private address OUTSIDE the project's Supabase team — the **Invite-user template arm, not the signup-code arm**, fired from the dashboard rather than the app's courier path and carrying a LINK where the confirmation arm carries a CODE. So the transport and sender identity are PROVEN and the *Email address not authorized* wall is retired as a measurement, while **the signup-confirmation arm is NOT certified and the gate above stays open**. **REPORTED, not measured (裁-112), words only with no read-back, ≈17:00:** the test user deleted, the Rate Limits raise applied — **the raised value was not stated, so no number is recorded anywhere** — and the *Confirm signup* template confirmed to carry `{{ .Token }}`; all three are read back at the Wave-G walk, and the checklist's two proof-bearing boxes stay OPEN until then.
88. **The C-2 operator problem-event SCREEN is post-beta; a manual check line holds its place from now** *(裁-147, owner, 2026-09-03 ≈17:12 MYT by the shell clock — 「照建议」; the screen is post-beta, the checklist line binds from today until it exists; ledger `docs/plan/active/mohe-grill-rulings-2026-09-03.md`, which governs)*. FS-4 C-2 shipped two operator doors — `clara.list_stripe_event_problems(boolean)` and `clara.resolve_stripe_event_problem(uuid, text, text)`, both walled to an OWNER of the operator firm by the same predicate `approve_firm_registration` uses (`0160_checkout_gate_c2_stripe_events.sql`) — and `docs/plan/active/checkout-gate-design-part2.md` promises under M4 that "the problems must be watched by someone, so they have a surface". There is no surface: a census over `apps` and `packages/runtime` returns **zero** callers. Ruled: **the screen is built AFTER beta live** (a PROGRESS Backlog row; owner = the first post-beta UI lane), and **NOW** `docs/ops/wave-g-setup-checklist.md` gains ONE manual line — at the Wave-G walk AND again at cutover the operator runs the list door (or a plain select on `clara.stripe_event_problems`) and the result must be EMPTY of unresolved rows before the cutover proceeds; a non-empty result is cleared through `resolve_stripe_event_problem` with its reason, which re-arms the next sweep to retry the event. The check is cheap **only if that line exists**, which is why the ruling puts it in the checklist rather than leaving it to a runbook nobody opens — the design's own words. Executed in the checklist's Billing (Stripe) section and in the design's M4 paragraph, whose "where it is watched" sentence now names the checklist line as the interim surface. PRD §2's operator cap (裁-143) already names this queue as one of the tier's two read queues and is untouched.
89. **The non-zero-price checkout walk moves to the REAL-MONEY SWITCH; Wave-G walks the seeded beta price in the sandbox** *(裁-148, owner, 2026-09-03 ≈17:16 MYT by the shell clock — 「B7 照建议」; permanent as a sequencing rule until the real-money switch runs; ledger `docs/plan/active/mohe-grill-rulings-2026-09-03.md`)*. `docs/ops/wave-g-setup-checklist.md` required the Wave-G walk to exercise checkout at a **non-zero** test price — the 裁-58 dissent's mitigation, written before 裁-126 fixed the whole beta in the Stripe SANDBOX at **MYR 0** (裁-58's `amounts_ruled=false`). The two cannot both hold: the checkout door reads the CURRENT plan only, so a priced plan can be seeded but not walked without temporarily making it current — an ops act on the admission plane, at the walk, for a path beta never takes. Ruled: **walk checkout ONCE at the seeded beta price (sandbox, MYR 0)**, and the non-zero-price proof — charge, webhook, invoice/receipt — belongs to the **real-money switch ceremony**, with Stripe live mode and KYB beside it (裁-125/126). **No temporary current-plan switch at Wave-G.** The 裁-58 dissent is not withdrawn; it is re-homed to the ceremony that can actually discharge it. Executed in the checklist's Billing (Stripe) section. **The pre-上市 roadmap, aligned with the owner in the same exchange (≈17:14) and recorded here because three rulings now point at it:** beta live on the built half (agent-template legal texts + RM 0) → the pricing sitting (裁-58) → the billing TIER tranche (裁-144) → the lawyer's pass over the DPA, the beta terms and the consent text (裁-125) → the real-money switch + KYB + the non-zero checkout walk = 上市. It lives as an ordered Backlog block in `PROGRESS.md`, which is where the owner reads it.
90. **The runtime's background-client error CONTRACT: the general pool logs and recycles, the leader crashes and fails over** *(裁-149, owner, 2026-09-03 ≈17:22 MYT by the shell clock — 「照建议」 = option C, after 「解释清楚这个, 我不是很明白」 and a plain-language A/B/C; permanent as a contract, built after beta live; ledger `docs/plan/active/mohe-grill-rulings-2026-09-03.md`)*. `packages/runtime/lib/relay.mjs` attaches no `'error'` listener to the pool `makePool()` returns, nor to the leader's dedicated `makeClient()` session; pg's contract turns any idle-client error — a backend terminated by a failover, a pooler restart, a maintenance kill — into an `uncaughtException`, so the runtime process dies, Fly restarts it and durable runs resume by design. The same absence is what made the CI relay-taxonomy class re-throw a teardown FATAL (cured test-side by #534 `e7577af6`; the PRODUCT half is this row). Options heard: **A** keep crash-as-contract (zero code) · **B** log-and-recycle everywhere · **C** the hybrid. Ruled **C**: (1) the GENERAL pool gets a listener that logs at error level, COUNTS, and raises a health flag on `/ready` — never a silent swallow, because the relay pool is the runner's real connection pool and a background error there is an availability signal; (2) the LEADER's dedicated session stays **crash-loud**, because losing it releases the session-level advisory lock and a standby takes over — the designed failover, which a listener would convert into a silent stall; (3) the chosen behaviour is written as a CONTRACT in `docs/ARCHITECTURE.md` ("what a background client error does to the process", per pool), since `packages/runtime/lib/relay.mjs`'s failover semantics depend on it; (4) timing is **after beta live** — a product PR of roughly half a lane-unit (the listener, the health flag, cells including a mutant proving the leader path still crashes) riding a v7x deploy. Today's fail-loud behaviour is safe and stands until then. Every other `new Pool` in the runtime is censused in that PR.
91. **After the beta-live e2e this session CLOSES; the repo is the handover and there are NO next lanes** *(裁-150, owner, 2026-09-03 ≈18:02 MYT by the shell clock; permanent as the sprint's exit condition; ledger `docs/plan/active/mohe-grill-rulings-2026-09-03.md`)*. The owner's alignment, in his words: 「beta e2e 后所有东西都被harness 记录好了right? like backlog and knownissue..etc , beta live launch e2e 后我就要close 这个session 了, 接下来的东西就是我自己看backlog/knownissue 再决定怎么完善这产品. no next lanes 了ok?」 Confirmed. So: **(1)** after the Wave-G walk and the launch sitting, this session ends and the repo IS the handover — hard constraint 8, the repo is the system of record and `PROGRESS.md` is the state authority; every open item lives there as a Backlog row or a Known-issues row carrying its **owner · next step · ruling number**, and the machine-local memory holds lessons and preferences only. **(2)** No lane is dispatched after launch on an agent's initiative: the owner reads Backlog and Known issues and decides what to perfect; the next session starts on his ask. **(3)** `PROGRESS.md`'s Backlog is therefore written as an ordered PICK-LIST — the pre-上市 roadmap (裁-148) first, then the post-beta product PRs already ruled (裁-147 the operator screen, 裁-149 the pool error contract), then the day's technical rows. **(4)** Two truings remain in the session: the afternoon's (this one) and the FINAL clock-out truing after the launch sitting, which inherits this shape and adds the launch facts, the housekeeping and a memory refresh.
