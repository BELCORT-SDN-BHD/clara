### ADR-027 — Wave A2 DEPLOYED + the §9 eval CLOSED; chatTurn_v4/_v5 shipped as §9 live finds; the live eval ratified as the acceptance gate that review cannot replace (2026-07-22)
**Decision:** Wave A2 is **LIVE and its §9 eval is CLOSED** (PRs #43–#46; live Supabase at **15 migrations**, Fly `clara-runtime` **v23**). The owner-`!`-gated ceremony ran: full-profile backup → `0015` applied (tail asserts passed; canary untouched) → runtime deployed → CoA re-onboarded (`300-000 TRADE DEBTORS` created through the audited writer, writes=1) → **Gate A EXACT** (6 approved sales invoices; `300-000` debits = `500-000` credits = **RM 1,973,332.91**; customer identity = ONE counterparty with the D&Dream→Dare-To-Dream rename executed through `rename_counterparty`, the April name a `former_name` alias) → **Gate B EXACT** (6 JVs chat-coded as NULL-kind `journal_entry` drafts; salaries 405,000 · EPF-er 52,200 · SOCSO-er 2,187.15 · EIS-er 249.90 · share capital 1,000 — all to the sen). **The eval surfaced three REAL as-built gaps, each fixed live through the lawful path:** (1) `onboard-rpr.mjs` still validated `account_class` against `payable`-only while 0015 + the ratified CSV said `receivable` — fail-closed abort, two-line fix (#44). (2) The frozen `chatTurn_v3` draft tool hardcoded `coding_kind='supplier_bill'` and its prompt taught supplier-bills-only, so the agent structurally could not draft the AR side 0015 already enforced → **`chatTurn_v4`** (coding_kind passthrough `supplier_bill|sales_invoice|sales_credit_note`, kind-neutral `counterparty`, direction-first + rename-not-duplicate + professional-vigilance prompt; #45). (3) **No chat version could send a NULL coding_kind** — the generic voucher lane the DB has always accepted was unreachable, falsifying §9 Gate B's "zero new build" premise → **`chatTurn_v5`** (`journal_entry`→NULL at the writer, counterparty optional; #46). Both bumps followed Appendix A exactly (prior closures byte-untouched, freeze manifest 29→41 append-only, registry repoints, two-axis reviews clean — the v4 review also caught an unmapped CLR30 and codified clarify-on-ambiguous-direction). Operational corrections made live: the intake CORS allowlist secret was stale since the Cloudflare-Pages move (set to `https://app.clarabook.com`); the firm `daily_token_limit` was temp-raised 1M→3M for the eval day and **restored to default** after — eval days may lawfully do this, restore is part of ceremony close.
**Why (the recorded lessons):** (a) **Six rounds of adversarial code review missed all three gaps — the live eval caught them in hours.** Review verifies what code does; only execution reveals what a system cannot do. The §9-style live replay eval is hereby ratified as the standing acceptance gate for every wave — a wave is not DONE at merged-and-reviewed, it is done when its eval gates close on live books. (b) **The professional-vigilance flag did NOT fire** (the >RM500k-commission-no-SST note on RPRINV-2508/01, asserted by §9): the v4/v5 prompt carries the instruction and the model still missed it — model discipline is again shown to be the wrong home for a firm-critical signal; A2.1 carries a **structural** revenue-accumulation visibility check (SST research fact-sheet on file: taxable via Group G estate-agency or the 1-Mar-2024 Group I real-estate brokerage expansion; RM500k rolling threshold; 8%; RPR crossed ~June-2025, exemption analysis = owner/professional judgment). (c) One-draft-per-turn held structurally when the owner asked for a bulk coding ("code the rest") — the turn produced exactly one card and the shortfall was visible in the gate probe, not silently absorbed. Eval finding ledger for A2.1 beyond the above: settled `je_review` cards hydrate an empty "unknown" shell after reload (render the terminal state); AP-era vocabulary on customer flows ("NEW VENDOR" chip, `vendor_unresolved` sweep reason); a doc-type classifier to gate the facts engines (invoice_facts ran on JVs) which would also populate `document_kind`. Ref: PRs #43–#46, memory `project-clara-rebuild-state`, the §9 probes in the session scratchpad.
