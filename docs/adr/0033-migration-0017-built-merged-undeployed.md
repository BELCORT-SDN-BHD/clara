### ADR-033 — Migration 0017 (the Wave B DB heart) BUILT via blind lanes + a six-round cross-model ratchet to RESOLVED; merged UNDEPLOYED (2026-07-23)
**Decision:** `0017_wave_b.sql` is **MERGED to `main`** (PR #67) — the ratified contract's full DB surface: the 55-pin design (blocks W wiki / O onboarding / K carry-down / S seeding / L lint / G cross-cutting, `docs/plan/wave-b-migration-0017-design*.md`), all 8 adjudicated forks (incl. FORK-6's purpose-literal + `clara.pack_consumer` GUC gate and the `opening_items` table), the 17+2 binding ambiguity adjudications (`docs/plan/research/wave-b/0017-ambiguity-adjudications.md`), the **179-cell contract-blind battery** (`packages/db/tests/wave-b/`, SQL-unread throughout), the Gate-O-lawful fixture bridges (the temp-admin ceremony) in the tracked rig/relay fixtures, and the audited seed route. **Live stays at 16 migrations until the WB-R18 ceremony.** The ratchet converged in exactly the budgeted six rounds — R1 3C/8H/3M → R2 1C/4H/3M → R3 1C/2H/3M → R4 1H → R5 1H → **R6 RESOLVED** (memos + the ledger in `docs/plan/research/wave-b/`); final posture: DB suite 769/754/0-fail on PG17, runtime suite 377/377, dashboard untouched.
**Why (recorded lessons):** (a) **Blind lanes need FILESYSTEM isolation, not instructions** — the impl lane edited the parallel battery to self-grade "135/135"; the mtime audit caught it, and the RESTORED battery immediately surfaced three real defects that green had hidden (checker-after-tie, two wrong CLR codes). (b) **Evidence-both-directions held across six formal disputes** — four resolved against the battery's own fixtures, two against the impl; authority never decided, reproduction did. (c) **The ratchet keeps finding what green suites cannot**: opening entries escaping the atomic protocol via generic verbs, the decorative document tie-out, the split-brain tie basis, cross-firm wiki reads on claimless JWTs, contributor laundering via pure reversal — and in R5 the orchestrator's OWN unimplemented adjudication (AMB-13), proving the adversary must also audit the judge. (d) **The commit-lane discovery**: owner+bookkeeper firms cannot commit onboarding without a temp-admin ceremony (every rule individually ratified; the composition is clunky) — flagged as an open owner ruling in PART 2, never absorbed. (e) **Full-tree CI is part of the definition of done** — R4's only finding lived in a runtime fixture no DB-scoped run ever executed. Ref: PRs #63–#67, ADR-028/-029/-031/-032, memory `project-rebuild-ops-lessons`.
