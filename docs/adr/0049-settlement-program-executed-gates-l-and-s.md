### ADR-049 — THE SETTLEMENT PROGRAM EXECUTED: Gates L+S closed (labelled synthetic), XG3 closed live, migrations 0024–0026 → runtime v33, the vendor-binding design RATIFIED (2026-07-28)
**Decision:** The pre-Wave-C settlement program (ADR-048) is substantially executed; live pin: **26 migrations · runtime v33**. **(1) Gate L CLOSED** as a labelled synthetic (PR #127): the mechanism is the 0017 lint belt's typed `contradiction` detector — the only surface in the codebase that turns "two sources disagree" into a durable human-routed finding — given its first-ever coverage (12 rig cells, mutation-tested in both directions; the agreeing control IS the real Bee corroboration one sen apart). Pinned constraint, deliberately not "fixed": the value comparison is type-strict, so every future citation producer owes ONE canonical `detail.value` encoding per `subject_key`. **(2) Migrations 0024+0025 DEPLOYED (runtime v33)** through a five-round cross-model ladder: `fail_classify` + an 8-arg no-default `classify_document` whose task-bound settle requires a **claim-secret capability** (digest-only storage; the preimage returns exactly once at fresh claim — a second runtime session presenting everything the table exposes is structurally refused; the readable-run-token binding was REFUSED as advisory, not structural), with the pre-0024 op-key hash shape replaying byte-identically; and receipts **auto-routed into the facts lane** (where Malaysian SST actually lives) with both sibling TOCTOU document locks. **(3) Migration 0026 DEPLOYED** (three-round ladder): the Gate-S-measured structural defect — 0007's `unique (document_id, engine_id, version_n)` without a lane column made the XML facts lane unreachable for EVERY document, swallowed silently by `on conflict do nothing` — closed by lane/engine_kind-widened keys, CLR35 loud impossible-states (`persist_invoice_facts` previously had NO conflict handling at all), kind-scoped version mints, and the **filed-bootstrap door** (live filing + zero facts-lane tasks + zero non-terminal tasks of any lane — the missed-enqueue recovery shape, predicate corrected against the live vehicle after the first draft refused its own target). **(4) XG3 CLOSED live**: the runway's five pre-registered EZSEC re-extractions all corroborated (the §12.2 prediction held exactly) and three fresh ingests corroborated first-pass — live corroborated population **0→9**, including the first STRUCTURED-branch corroboration. **(5) Gate S CLOSED** (labelled synthetic, Rome `39008536` only): XML→`local_facts`→structured Tier-A corroboration→a signed structured sales rule→the **first `execute_rule_post` in the product's history, unattended** (census-verified: one call ever; the first PRODUCTION autopost stays a distinct open claim); `cn_not_autopostable` drilled by name; the `not_corroborated` drill RE-HOMED to the OCR runway where its cause lives. **(6) The first-production-autopost fork RULED (owner)**: vendor identity may resolve from **human-approval-derived KB bindings** (option A); widening X6's page evidence (B) and hand-draft eligibility (C) REJECTED — hand-drafts-never-autopost is now intended design. The **vendor-binding design is RATIFIED v4.1** (`docs/plan/completed/autopost-vendor-binding-design.md` + `-part2.md`, four adversarial rounds): F3 ruled corroboration-not-option-B (amendment D: *the rejected direction of authority flow is what made option B option B*), registered-only scope, 12-month expiry, advisory-never-blocking with divergence visible-only, one total lock order with position-0 op-receipt reservation, build = **0027 + 0028** (0028 recuts `execute_rule_post` AND `_approve_entry_core` via `receipt_preheld`) gated by a `schema_migrations` interlock + a standing CI assertion; largest open residual C.1 (legitimate c/o issuer attribution — no structural bound; the divergence surface watches). Dwell gate: the first binding needs a fourth EZSEC bill posted ≥ 08/09/2025 (IV-00846/847 qualify; the window orders by `approved_at`). New defects found by the program are ledgered, never folded: the pre-existing `file_document`/`confirm_attribution_candidate` 40P01 deadlock, the CLR23 remedy-text-vs-lane-capability gap, and the chat lane's inability to bind an existing counterparty.
**Why:** The program's purpose was Wave C starting debt-free, and the autonomy gap ADR-046 named now has its first evidence: an unattended post exists — labelled synthetic, inside a sandbox firm, with every refusing control along the way reading as a named skip (`counterparty_unresolved` ×3, `no_live_rule` ×2, `cn_not_autopostable`). Measurement beat assertion all day — the lane-blind unique keys, the readable run token, the guard-after-reserve replay, the bootstrap predicate refusing its own target vehicle — each caught by probing the live instrument before code shipped, which is the review ladder and the probe-first discipline doing exactly what they cost.
