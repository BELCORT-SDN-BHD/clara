### ADR-050 — THE FIRST PRODUCTION AUTOPOST: the binding founded, six firings, RM350 posted unattended (2026-07-29)
**Decision:** **The first production autopost is DONE**: entry `f65eba11`, rule `90a07e89`, ROME SECRETARY, RM350.00 (EZSEC IV-00780), 2026-07-29 08:20:47 UTC — **38 seconds from a filed PDF to a posted, audited, rule-checked journal entry with no human touching the coding**, every step receipted (intake → rule resolution → filing → FIRST-PASS two-reader corroboration → `autoDraft.v5` drafting the 2-leg SST-zero shape → `execute_rule_post` with the draft- AND post-phase binding resolutions 162ms apart, `rule_post_runs` 1→2, books moving exactly 35000 on both sides). The road there, all deployed live (**33 migrations · runtime v37**): **0028/0029** the binding machinery + executor (the 5-round ladder of ADR-049's design, plus the build's own catches — the interlock grant gap, the 42883-dead Slot-A resolver, a fifth lock-order participant); **0030** F1 becomes the windowed LCP (the live founding measured the letterhead fragment suffix-truncating per scan — `features_unstable` was the wall working; F1 adopted F2's own LCP+starts_with discipline); **0031** the lane unification + the near-duplicate invoice_id/sha256 discriminator (the stale op-key admission cache root-caused and §C-cleared from production; "same fee again" stops flagging, "same bill twice" still does); **0033** the model-id fix (`openai/gpt-5-mini` — a gateway-style id fed to the bare-id provider by an unset-GUC fallback since 0011; one-click autodraft had NEVER run in production) + **autoDraft v4** honest stream diagnostics (the real API error had been swallowed behind `NoOutputGeneratedError`, then behind WDK's FatalError boundary, then behind its retry prefix — three layers, each proven); **0034** the admission retry door (terminal-failed supersedes as `re_admitted`; the ladder's second pass caught an unbounded token-ledger double-refund and its third caught the fix's own subtransaction rollback hole); **autoDraft v5** the SST-zero precedent (**owner-precedent-ruled**: the owner's four approved EZSEC entries are all 2-leg; a stated-ZERO tax takes the 2-leg shape — the model's `coding_incomplete` refusal on IV-00743 was CORRECT behavior against a rule the client's own books contradicted; the rule moved, not the wall). **The binding itself**: `d871c50c`, proposed and owner-signed 2026-07-29 — F1 `ez 易计 ezaccount` (LCP), F2 `ezsec-iv-00`, registration 202301030264 pinned, three evidence entries with dual extraction pins, content-hashed, expires 2027-07-29. **Standing laws minted en route:** migration numbers are claimed at MERGE time (the deploy-onto-existing frontier check is the gate — learned twice); condition 5 admits `birth` OR `registration_conflict`-with-candidate-equality (the two-level `_coding_lane_core` catch); F2 is a consistency gate, never a selection key; the DB-password rotation after the printenv exposure was **DECLINED by the owner** (recorded; the no-live-env-dump discipline stands regardless). **Six firings; every refusal en route was a real control or a real defect — none were worked around** — which is precisely why the receipt is trustworthy.
**Why:** ADR-046 named the autonomy gap as the one honest delta from the North Star: the supervised loop was proven to the sen while the autonomous half had zero production evidence. This closes it on a real client's real books: posting authority derived entirely from structural evidence and the owner's own signatures, exercised unattended, and re-verified at post time before a sen moved. The remaining register (the chatTurn diagnostic twin + tax-rule propagation, the reconciler double-dispatch cosmetics, the nonzero-tax DB belt, the sweep-vs-human budget contention design question, the drafting-lane trio) is ledgered with named destinations — Wave C starts on a machine that has already done the thing it was built to do.
