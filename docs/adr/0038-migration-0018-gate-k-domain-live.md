### ADR-038 — Migration 0018 BUILT, RATIFIED, and DEPLOYED same-day; the Gate-K domain is live (2026-07-24)
**Decision:** The WB-R24(i) micro-migration ran the FULL money-path discipline in one sitting and is LIVE. Build: as-built dossier → design contract v1.0 (`docs/plan/completed/wave-b-migration-0018-design.md`, LAW, incl. the §10 reconcile-adjudication ledger) → Codex design debate (caught the K5 replacement-reversal STRANDING hole — verified 0017:3959 direct-join vs :3613 reversal-counting basis — the binding-escape leak, and the auto-deploy split) → mutually-blind lanes → rig reconcile (9 divergences adjudicated [AMB-0018-1..9]) → fix round → codex ratchet R1 (4 findings, fixed [R1-0018-1..4]) → ratchet R2 (1 LOW, fixed [R2-0018-1]) → PG17.9 full verify (18/18 apply; battery 826/0 fail) → native fresh-context review MERGEABLE_WITH_NITS → **PR #77 merged undeployed; PR #78 (the dashboard rider) parked DRAFT per the §8 split.** Ships: seed-bound keyed resolutions (bound mint `record_opening_keyed_resolution`, confidence pinned 1.0, auto-supersede last-wins under seed FOR UPDATE, UNIQUE live-binding index, capability confinement BOTH directions), `seed_fixed_asset(+p_resolution)` (keyed FA unblocked; locked-filing tied derivation; legacy op-hash byte-compatible), dual-lane K5/K6 purity guards from one classifier (`correction_draft_present` / `non_correction_draft_present`), five typed commit-refusal CLR10 reasons, DB-authored `entry_count` on both approval receipts. **The ceremony (owner-`!`/agent split, one continuous sitting):** backup-first (fresh encrypted snapshot `2026-07-24T05-16-29-435Z`, exit 0) → preflight reads (17 migrations · 0 keyed items · 2 known parked runs · 33 unbound resolutions) → quiesce → **atomic apply (`applied 0018_gate_k_domain · 18 total` — the in-txn tail incl. the empirical zero-keyed-items precondition passed against production)** → `notify pgrst` → post-verify ALL GREEN (catalog: single 5-arg `seed_fixed_asset`, mint grants authenticated-only, UNIQUE index, `entry_count` both verbs; PostgREST cache-fresh proven by a clara-schema 42501 anon refusal probe) → un-quiesce (`/ready` 200, zero warnings, all consumers lag 0; `held_outbox` 34 CLASSIFIED — all `condition='notification'`, the by-design wake-authority ledger accumulating since 07-22, not a ceremony effect) → PR #78 ready+merged → **the rider verified IN THE DEPLOYED BUNDLE** (the bound-mint rpc name present in the live chunks). **LIVE POSTURE / the Gate-K version pin: Supabase 18 migrations · Fly `clara-runtime:wave-b-v25` (unchanged — zero workflow-body changes, zero freeze impact) · dashboard rider deployed.** Gate K may now run document-primary OR attributed-keyed INCLUDING fixed assets.
**Why (recorded lessons):** (a) The owner-`!`/agent split absorbed the permission classifier gracefully — every live mutation and credential-touching step ran under the owner's authority; the agent kept verification, probes, and records. (b) A PostgREST rpc probe MUST send `Content-Profile: clara` (wire.ts:42) — without it PGRST202 reports a `public.`-schema miss that mimics a stale cache. (c) Classify before concluding: the `held_outbox` 4→34 jump was the notification wake ledger doing its designed job, not a regression. (d) Same-day contract→deploy is viable when every phase gate is mechanical and adversarial: the debate and the blind reconcile caught the structural defects BEFORE any reviewer saw a diff. Ref: ADR-037 (WB-R24), PRs #77/#78, the contract §8 as-run.
