### ADR-046 — WAVE B CLOSED ON INTENT; the deferral register; the extraction slice is the sole pre-Wave-C engineering (2026-07-27)
**Decision:** **Wave B is CLOSED on its intent** — the knowledge + onboarding machinery is proven on real documents across two entity shapes — under the contract's own deferral law (*"Gate deferral needs an owner ruling; fabricated documents never"*). The honest ledger: **CLOSED on real evidence** — O ××2, K(keyed) ××2 (corroborated by the client's own YA2025 `BALANCE B/F`), B-12 (checklist reading; supersedes WB-R29's date half), W2 claims (1)+(2)-structural, **R2 claims (1)+(2)** (6 signed `vendor_account` rules; 0 sightings bred from prior GL; the DB refused 6/12 name-only ticks `CLR23` against registered counterparties — correctly; Clara's **first 12 `recurring_pattern` knowledge pages** published, deterministic), **F** (ADR-045). **DEFERRED with cause, destination named:** S (no MyInvois XML exists in any corpus) · L (the only real candidate pair **agrees to the sen**; manufacturing a conflict is fabrication) · K-document-tied (`uq_opening_seed_registry_once` — both real clients' slots are spent, RPR is greenfield, the demo clients hold zero documents; a producer built now has no client to run on) → **all Phase 5, whose methodology is synthetic-only by design**. **DEFERRED to the operating runway (real future business, not engineering):** R2 claim (3), W2's journey claims, and the **first production autopost** — the mechanism is fully built and never operated (propose/sign/execute = 0 calls each); 5 sighting pairs meet the ≥3 floor but none can fire (payroll JVs bind no facts lane; the AR-control pair lacks the sales-side evidence class), so the runway is ~3 approved small-ticket bills from one recurring vendor (e.g. KOK LIONG at RM1,190–2,600, comfortably under ADR-044's ceiling). **The owner's proposal to seed autopost rules from prior-GL/management-account data was REFUSED under WB-R2** (*"no autopost rules from seeding, ever"*) — posting authority derives from verified in-system approvals, not another system's claims; the compliant equivalent (seeded `vendor_account` hints → correct drafts from day one → 3 approvals unlock the proposal) is already live. **The sole engineering between here and Wave C is the extraction slice** (`docs/plan/extraction-slice-contract.md`, DRAFT v0.1 pending the owner's grilling): X1 migration 0022 (re-extract verb + `set_firm_high_stakes_threshold` + the sum-of-stated-components sales tie) → X2 the deterministic totals reader → X4 an explicit `anchor_missing` guard → **X5 corroboration-by-agreement (the two-reader rule) LAST and ALONE** — because it is the one step that opens posting authority, and the naive one-shot build was **adversarially refused on three grounds** (closes nothing without re-extraction; switches off a live barrier as a side effect; wakes four dormant sales ties that fail on any service-charge document) — record: `docs/plan/research/wave-b/gate-p-build-refused-2026-07-27.md`.
**Why:** A wave is DONE at its live gates on real documents (WB-R17) — and equally, a gate whose evidence class does not exist in the world cannot be closed honestly, only deferred with its cause written down. Every deferral above names both. The week's deeper finding stands recorded: Wave A/B's **supervised** loop is proven to the sen; the **autonomous** half has zero production evidence (autodraft 0/55, Tier-A 0/29 on vendor confidence alone, 165 wake credentials expired unconsumed), Phase 5 §6 gates on measuring exactly that, and Wave C (bank rec — the most extraction-hungry documents in the product) would otherwise build on the broken tier. Direction audit: `docs/plan/research/wave-b/vision-alignment-audit-2026-07-27.md` — on the North-Star path; the one honest delta is autonomy evidence, and the slice is its first stepping stone.
