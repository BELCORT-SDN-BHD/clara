# PROJECTLOG — Clara rebuild

> Append-only decision log — the durable "why it is this way." Supersede a decision with a NEW entry; don't rewrite old ones. Build narrative / commit hashes / status do NOT belong here — those live in git, the plan, and memory.
> Artifacts-of-record (cite, don't duplicate): `docs/prd/PRD.md`, `docs/architecture/ARCHITECTURE.md` (+ Appendix A), `docs/design/DIRECTION.md`, `docs/plan/REBUILD-PLAN.md`, `docs/audit/` (Gate-1 evidence + rulings), `docs/00-GATE-2-README.md`, `docs/phase2-research/`.

---

> **START HERE.** Clara is a greenfield **AI-native Agentic Accounting OS** for Malaysian accounting firms, rebuilt from the frozen prior BELCORT build. This repo (`github.com/BELCORT-SDN-BHD/clara`, `main` PR-only) is the active workspace; the old repo `initial acc software skillmd` + Supabase `belcort-shared` are FROZEN read-only audit evidence (untouched until Phase-5 decommission). The DB owns every number; the agent orchestrates. **This log is DECISIONS ONLY** — append-only ADRs + rationale; supersede, never rewrite. Current phase/slice **status lives in memory (`project-clara-rebuild-state`) + `docs/plan/REBUILD-PLAN.md`**; tasks live in the handoff; build narrative lives in git. Product law → `docs/prd/PRD.md`; target architecture → `docs/architecture/ARCHITECTURE.md`.

---

## PART 1 — DECISIONS (append-only)

> **ADR-001 .. ADR-021** live in [`PROJECTLOG-ARCHIVE-ADR-001-021.md`](./PROJECTLOG-ARCHIVE-ADR-001-021.md);
> **ADR-022 .. ADR-043** (Phase-4 Waves A/A2/A2.1/B) in
> [`PROJECTLOG-ARCHIVE-ADR-022-043.md`](./PROJECTLOG-ARCHIVE-ADR-022-043.md);
> **ADR-044 .. ADR-054** (the Wave-B close through the WAVE C CLOSE) in
> [`PROJECTLOG-ARCHIVE-ADR-044-054.md`](./PROJECTLOG-ARCHIVE-ADR-044-054.md). Split 2026-07-26,
> 2026-07-27 and 2026-08-02 as this append-only log grows by design — the fix is a split, never
> a rewrite or a prune. Nothing was edited; cite archived ADRs by number exactly as before.
> This file holds **ADR-055 onward** plus PART 2 (open items). ADR-055..059 are the Wave D
> arc, now COMPLETE across all four D-b slices; ADR-060 is the cross-cutting pre-beta data
> doctrine. Split the next time this section outgrows a read — never rewrite, never prune.


### ADR-055 — WAVE D OPENED: the contract ratified (WD-R1..R15), split D-a → D-b, staff advances ruled B-lite on a cross-model research record (2026-08-01)
**Decision:** Wave D (assets + adjustments) is **OPEN under a ratified contract** — `docs/plan/wave-d-contract.md` is the mechanism of record; this entry minutes the rulings, the contract carries their rationale and the substrate census. Grounded first (8-lane read-only fan-out + orchestrator live probes: `clara.fixed_assets` = full schema, ZERO rows, zero read RPCs, zero day-two writers — the carry-down's own CLR31 refusal *"non-straight-line depreciation is deferred to Wave D"* named this wave in code a month ago). **The fifteen rulings (owner, 2026-08-01):** WD-R1 acquisition **soft-births** the register row at approve (intrinsic per ARCHITECTURE §3.5; missing particulars = a visible pending state, chased, never blocking) · WD-R2 **no DB capitalisation threshold** (the account choice IS the decision) · WD-R3 methods widen to **straight_line + reducing_balance + none** (real reducing-balance evidence confirmed — it must fire in acceptance) · WD-R4 **per-client cadence, default monthly** · WD-R5 depreciation authority = **admin+-signed per-client authority; first run drafts (the ramp); then auto-post with receipts; high-stakes always to a distinct checker** (WCA-R7) · WD-R6 "close-gated" = a **named deferral to Wave E** (schedule-only + the advisory "not run through M" visibility — the WC-R3 precedent against half-built close models) · WD-R7 disposal **full AND partial** (owner widened the full-only recommendation; partial stated as a COST PORTION, sen-exact accum-dep pro-rating, the supersede-lineage split) · WD-R8 recurring/reversing = **admin+-signed templates, first-occurrence ramp, auto-reversal under the same signed authority, edits re-ramp** · WD-R9 **admin+ floor for both** new authority families · WD-R10 staff advances = **the B-lite register** (append-only `staff_advances` + explicit `staff_advance_applications`, DB-derived outstanding/age, a managed-account same-transaction belt, an as-of Σregister=GL tie-out, NO silent FIFO; `open_items` untouched; no employee counterparty, ever) — ruled on a commissioned cross-model research record (`docs/plan/research/wave-d/staff-advance-research-2026-08-01.md`: ERPNext is the B-shaped precedent; SAP B1's employee-as-vendor is the WC-R10-forbidden shape; convention-only cannot answer *"who owes what since when"* without the agent inventing a FIFO — which the DB-owns-every-number law forbids) · WD-R11 **closing stock defers to Wave E with cause** (a close-time mechanic whose trigger does not exist until E) · WD-R12 **CA metadata trio** (`ca_class`/`is_commercial_vehicle`/`is_new`) added **inert until Wave F** · WD-R13 **all four Wave-C residuals ride**: the AF-1 guard as a **HARD refuse** (no override — the remedy is the deposit/advance route) + the `reverse_entry` MYT splice in **D-a/0041**; the AF-2 composite resolve-and-book verb + the `bank_rule_suggested` producer in **D-b/0042** · WD-R14 acceptance = sandbox labelled-synthetic first, then **BOTH real registers — ROME PROPERTIES and ROME SECRETARY** — seeded through the carry-down's first real firing, ≥1 real reducing-balance asset fired · WD-R15 the wave **splits D-a (FA register, 0041) → D-b (adjustments + advances, 0042)**, each through the full house arc.
**Why:** The wave's two genuinely new machine lanes — a time-triggered poster (nothing in the product posts on a calendar today) and the product's first non-trade register mechanics — are exactly the kind of authority decisions that must be ruled before code exists, and the grilling surfaced that "close-gated" was unbuildable as written (Wave E owns periods), that the plan's own Wave D row silently included a close-shaped item (closing stock) whose trigger cannot exist yet, and that the staff-advance depth choice was a genuine research question, not a preference — the owner commissioned the research mid-grill and ruled on its record rather than on instinct. One authority doctrine (sign once at admin+, ramp the first firing, receipt everything, high-stakes always human) now covers both new posters, which keeps Wave D's autonomy surface reviewable as a single shape. The ride-along ruling closes the AF register born hours earlier at the Wave C close: every acceptance finding now has a named build destination inside the very next wave.

### ADR-056 — WAVE D-a CLOSED: 0041 live through a four-round ladder + two merge-gate sittings; the sandbox acceptance complete; the WD-R14 real half closed on the honest truth — both real registers hold ZERO fixed assets (2026-08-02)
**Decision:** **D-a (the FA register slice) is BUILT, DEPLOYED and ACCEPTED; the WD-R14 real half closes on a named deviation the owner ruled same-day.** The build (PR #177, squash `5a0be9b`, migration **0041** claimed at merge — 58 files, +17,789) ran as a four-lane dispatch under an orchestrator-pinned interface contract; the contract-blind x41 battery caught **three class-(a) migration defects on first contact** (the disposal per-asset precondition folded away "by construction" · the reversal dependency refusal unfollowable · the RB Σ-segments law unreachable — the design's OWN RM14,000 worked figure charged RM2,000), the C-b lesson a third time. **The as-built ladder ran FOUR rounds** (3 → 3.5 → 4 → 4.6; three native opus lenses + Codex gpt-5.6-sol per round, ending in two Codex merge-gate sittings, second verdict MERGE-WITH-NOTES, notes folded) — the full record + every adjudication lives in `wave-d-a-fa-design-part2.md` and is never re-litigated. Headline folds: ONE lineage-accumulated read (bakes carry only carried-baseline content) · the PERIOD-NET companion read (unwinds joined to their original's period; the §1.3 is_live law verbatim) for the RB basis + disposal relief · ONE due oracle (due ⇔ compute — the zero-sen freeze and the stranded-months classes dead) · entry-shape reversal dispatch + the revision-chain closure (reverse-not-delete stays reachable) · immutable enrolment intervals (`retired_at`, version-forward, the belt at `approved_at` closed both ends) · ONE reservation predicate (active profiles ∪ codes baked on register rows) feeding topology, disposal hardening AND an undeferred bank-side table belt · the K6 `superseded_at` backfill + eleventh splice (the acceptance instrument had read 2× on the exact carry-down shape) · the lineage-wide stub money-clock (a revision + life-end disposal could depreciate beyond cost) · the eager `:approve` reservation STANDS (TAIL 7's 0037 deadlock law). Final: DB **1535/1510/0-fail ×2** · upgrade drill · runtime 986/986 · dashboard 488/488 · all gates; the ONE CI red en route was the MYT splice WORKING (CI ran at 07:1x MYT inside the exact window; the UTC assert it broke was the day-early bug itself). **The ceremony (2026-08-02, owner GO):** dashboard auto-deployed from main (/assets live) → runtime **v53** first (dormant on 0040 by exact-signature feature-detect) → quiesce (beats stale 118–120s) → 0041 applied (40 total; the thirteen tail censuses in-txn on production) → fifteen postverify probes green (grants exact: run verb machine-only, manual human-only, due probe both lanes; both belts live; RLS forced; register empty) → restart → `/ready` 200 · Supavisor **38/60**. **Sandbox acceptance (ROME PUBLIC ADVISORY, labelled synthetic) — every drill green through the production verbs:** soft-birth + queue chase + placeholder · completion · authority signed with the **Dec-31 fallback SURFACED** · the ramp (first run DRAFTED at exactly RM100.00; ledger row + receipt minted AT approve; July then **POSTED autonomously**; August honestly `period_not_ended`) · late-completion catch-up (a second June receipt, lawful by design) · RB at the exact sen floor (13,333) · a mid-year 20%→10% **prospective revision** (supersede-forward, history intact) · partial disposal sen-exact + the **partial REVERSAL** (original restored, successors unwound, the stub flipped dead beside its born-dead unwind row) · a high-stakes disposal DRAFTED, the register untouched through the window, executed only at the attested approve · **the AF-1 wall fired by name** (`allocation_to_unborn_item`, the deposit remedy verbatim) + the deposit route green (RM500 on account) · `fa_register_tie` TRUE with diffs EXACTLY 0 at TWO as-ofs. **The WD-R14 real half — the named deviation (owner-ruled 2026-08-02):** the owner's raw YA2025 documents for BOTH real registers were measured (RPR: TB/BS/GL/P&L to 08-12-2025 — a P&L-only book, premises and even the water purifier RENTED; RS: BS/GL/P&L 05-05-2025→31-03-2026 — the BS prints Cost/AccDeprn/NBV columns EMPTY; the "RM20,000" supplier invoice is a mis-named RM2,000 SSM service fee) — **neither company has ever owned a fixed asset; there are no FA schedules because there is nothing to schedule.** Under the never-fabricate law nothing was seeded; both real registers stay honestly empty; **the carry-down's first real firing + the ≥1 real reducing-balance asset DEFER, named, to the first asset-owning client** (the sandbox half already fired every mechanism incl. RB, under the ADR-048 sanction WD-R7 itself invokes). Recorded en route: **RS's real FYE is 31 MARCH** — the first genuine non-December FY for `set_client_fy_end` when RS ever holds assets.
**Why:** The wave's own doctrine did the work: the contract-blind battery and the loop-until-dry ladder each caught real wrong-money defects no earlier stage saw (four consecutive rounds found something; the ladder closed only when a pass came back dry), and the acceptance's real half proved the OTHER half of the honesty law — the register's first real duty was to stay empty rather than invent assets to satisfy a ratified expectation. The contract §6 premise ("the owner holds the FA schedules for both") was measured against the primary documents and found wrong, and the correction went to the owner as a ruling, not a silent re-scope. Ladder residuals ride the D-b register: the second-outstanding-disposal-draft guard · the writer-side 64-edge lineage cap (65 audited revisions can brick a register's tie with no unwind verb) · `fixed_assets.cost_cents` NOT NULL · the split-month allocation ruling (unruled; x41.s2 lane-notes it) · the WD-R14 acceptance pre-flight law for any future real enrolment (zero approved non-opening GL movement on a cost account before enrolling it; no parked depreciation/disposal drafts — an abandoned draft is a client-wide sequencing stop).

### ADR-057 — WAVE D-b DESIGN CLOSED at v8: sixteen rulings (WDB-G1..G16), an eight-round design ladder (~170 findings, all adjudicated), the split-month law pinned on a commissioned research record (2026-08-02)
**Decision:** **The D-b design (adjustments + staff advances, migration `0042` — claimed at the BUILD PR's merge) is RATIFIED and ladder-closed** — the design of record is `docs/plan/wave-d-b-design.md` v8 + `wave-d-b-design-abi.md` (the builder ABI: every signature/envelope, seven new-table DDL blocks, the single-owner op-key matrix with literal pre-lock-knowable hashes, the refusal-token table, event payloads, the two hot-loop partial indexes) + the ladder record in `-part2.md`/`-part3.md` (PR #180). **The grill (owner, one fork at a time):** WDB-G1/G2 auto-reversal is hook-born at the occurrence's approval — ONE act births the approved pair, routed through `_approve_entry_core(receipt_preheld)` so CLR05 holds and the approve-path/hook-caller censuses stay the pinned FOUR; one-way relational linkage (`auto_reversal_of` only — reusing `reversal_of`/`reversed_by` would starve the ramp and dead-end corrections) · G3 cadences monthly+annual · G4 catch-up occurrences ALL draft (autonomy is forward-only from the MYT sign date) · G5 the advance belt is ASYMMETRIC (approved debits soft-birth the register row, WD-R1's posture; bare approved credits refuse — no silent FIFO exists to auto-apply) · G6 enrolment admin+ · G7 free account coding, enrolment is the truth · G8 NO real staff-advance case exists — acceptance closes on the named deferral (the ADR-056 honest-empty precedent) · G9 AF-2 high-stakes parks a declared `pending_resolution` the bookkeeper+ flip executes (authority attaches at declaration) · G10 the disposal second-draft guard + the PER-ASSET freeze scope ratified (the ceremony note's "client-wide" struck as imprecise) · G11 the 64-edge cap closes WRITER-side on THREE minting paths (revise · partial-split · the 0017 K6 replacement) · G12 `cost_cents` NOT NULL + the 0017 validator's NULL-propagating OR-chain fixed at BOTH sites, cost-only · G13 seven design positions · **G14 the split-month law PINNED to the ACTUAL as-built arithmetic** (day-1 revision → the successor owns the month; day-2+ → the PREDECESSOR owns it; no month ever split; no day-level pro-rating) — ruled on an owner-commissioned two-lane research record (`research/wave-d/split-month-research-2026-08-02.md`: Codex + live-web; IAS 8/MPERS operate at period-of-change grain, no mainstream ERP day-splits a revision month, the 2026 AI-native cohort is documented-silent) **which also caught that the grounding census had the as-built law BACKWARDS** — verified against the code and the x41 tests before the ruling · G15 the related-party clause is ATTESTATION, not structure (no DB fact exists to check) · G16 AF-2's settlement-belt recuts are the WD-R13-authorized delivery (the contract's "posters never touch the belts" binds the two posters, which touch neither immutability nor the belts). **The ladder (eight rounds, eighteen lanes — native opus/sonnet + Codex gpt-5.6-sol, all xhigh):** rounds 1–6 each found genuine money defects — the mirror birth violated line-immutability as first written · the force-approved mirror bypassed maker-checker · `_reserve_op` RAISES on same-transaction re-reservation (the composition law rebuilt on preheld-aware cores: settle + both allocates factored, S4.Z pins move) · the parked-declaration admission is SEVEN evidence-channeled sites, not one predicate · post-flip unmatch REOPENS the exception via an immutable `resolution_exception_id` (supersedes x40.z-A1's stale-survives posture) · corrections are HOOK-BORN ONLY at the uncorrected remainder · the outstanding equation is effective-dated signed effects (originals persist at historical as-ofs; a void is dated, never an exclusion) · the temporal application cap (fits outstanding at its OWN effective date and every later boundary) · enrol-clean-only (a synthetic opening row would fabricate the per-disbursement identity WD-R10 forbids) · **the UNIFIED ramp clock** (a completed pair receipt AND a plain solo `reverse_entry` both reset the template's autonomy — without it the sweep would silently re-post a human-corrected occurrence). Rounds 7–8 found zero mechanism defects (the closing signal); closure adjudicated in part3.
**Why:** The wave's riskiest surface — the product's second time-triggered poster compounding with auto-reversal, a new register belt, and a composite that must thread the settlement belts — earned the longest design ladder yet, and every round through six paid for itself in wrong-money defects that no earlier stage saw. The two structural insights of record: **route every machine approval through the existing core** (the censuses, CLR05, and the attestation law all hold for free — a fifth approval constructor was the wrong shape three different ways), and **make the receipt the authorization channel** where a one-argument hook cannot see caller identity. The split-month commission is the probe-before-arguing lesson at ruling grade: the census misread the as-built law, the cross-model research read the code and the tests, and the owner ruled on the corrected fact — the ruling's arithmetic, not its prose. The build inherits a contract-blind-testable spec: named refusal tokens, literal hashes, published equations — with the callee-owned literals explicitly assigned to the build's harvest step so live law is cited, never duplicated into prose that rots.

### ADR-058 — WAVE D-b CLOSED as a FOUR-SLICE SPLIT: the round-11 condition fired; 0042+0043+0044 built, gated, merged and LIVE (43 migrations, frontier 0044); the sandbox acceptance ran as one thread with all four structural invariants witnessed; the real half closed on the measured strike-off deviation; D-b2 (0045) HELD with a named inheritance (2026-08-05)
**Decision:** **The D-b shipping slices are BUILT, DEPLOYED and ACCEPTED; the wave closes with `0045` deliberately parked.** The as-built record is `docs/plan/wave-d-b-asbuilt.md` + `-part2.md` (the design of record `wave-d-b-design.md`/`-abi.md` is now DESIGN-TIME only, bannered as such — it describes a monolith that never shipped, and it is four rounds stale). **THE SPLIT IS THE EXECUTED RULING, not a preference:** the owner's 2026-08-04 grant resolution ratified whole-ship under a hard condition — *"if round 11 finds ANOTHER money mechanism, the unit is proven non-convergent and the SPLIT becomes the executed ruling"* — and round 11 found two (the silent re-code double, reachable with no lineage involved; P1's period-blind prohibition, whose only escape destroys a true lineage fact), quadruply confirmed by Codex. The eleven-round as-built ladder (127 findings; **for eight consecutive rounds the worst defect of the round lived inside the previous round's repair**) had already localised every surviving mechanism to ONE family — recurring adjustments — while advances, AF-2/bank and the FA residuals went dry and stayed dry across multiple rounds and every lens. **The executed shape: `0042` D-b0 (shared class authorities + the D-a residual recuts onto live 0041 bodies, sha `f701d3d1`) → `0043` D-b1 (staff advances, `1a1e6ba8`) → `0044` D-b3 (the AF-2 composite + the `bank_rule_suggested` producer, `c1ce10c2`) → `0045` D-b2 (recurring adjustments, `f66f4631`) HELD.** Each slice was generated from canonical sections by deterministic generators (regenerable byte-for-byte; the repo migration is GENERATED, never hand-edited), and the partition was proven complete TWO ways rather than argued: **twin-rig equivalence — the four-slice chain end-state vs the whole unit (sha `6770ea84`, 21,163 lines) at ZERO semantic difference across 14 exhaustive axes** (599 function signatures + ACLs, 1,514 columns, 399 indexes, 1,054 constraints, 277 triggers, 283 policies, 127 RLS postures, 1,084 grants, registries, seeds, sequences), with **exactly three comment-only body diffs**, each adjudicated and each proven executable- and string-literal-identical; and **a 418-cell test partition** (75 files → D-b0 146 · D-b1 66 · D-b3 70 · D-b2 134 · superseded 2, every cell in exactly one slice), where the four-slice battery scores 415/0/3 **in exactly the same cells as the whole unit** (per-file sweep sha identical). **The five-reviewer confirming round over the INTERMEDIATE FRONTIERS AS PRODUCTION STATES** (three slice lenses + a cross lens + Codex; 34 findings) earned the split's cost by finding and killing two money mechanisms the monolith could not have had: **(1) the phantom staff advance at frontier `0044`** — the producer granted while its six-axis approval guard lives in held `0045`, so an enrolment inside the draft window births a register row for money nobody received (Codex CX1 ≡ native CF-B3-1, probed; the earlier "courtesy" adjudication OVERTURNED) → **root fix: the producer's `clara_authenticated` grant is WITHHELD at `0044` and lands as one guarded block with `0045`** (which also kills the revise-a-suggested-draft, rollback-asymmetry and most of the test-assignment findings); **(2) the disposed-asset walk gate at frontier `0042`-only** — S5.15's reservation release live while S5.19's gate is D-b1's, putting an UNCLEARABLE permanent difference into `fa_register_tie` → S5.19 pulled forward into D-b0. Fix waves W-A/W-B/W-C landed them, V1 verified the integration 11/11 exact on the finals, and a light re-confirm (fresh native lens + Codex) came back **MONEY-FREE with all five originals closed by probe**, leaving eleven findings entirely in the unshipped PR-time wiring, which W-E closed with a `RENUMBER.md` dry run and a 21/21 CI scenario matrix. **The PR chain — #182 (`ace9326`) → #183 (`da1beb2`) → #184 (`dda9655`) — each through local full verification, real CI green in both contexts, and a Codex merge gate.** The gate's two teeth: on **#182** the first binding-gate exemption was **FAIL-OPEN** (five probed evasion shapes all passed) → collapsed to the **STRICT CENSUS GRAMMAR** with the five shapes as permanent negative selftests, and the exactly-one invariant became an **ENFORCED ALLOWLIST** (a second exemption reds the gate and is a review event); on **#184** the **show-B-settle-A** money-UI blocker — `ExceptionBookingFields` cleared the displayed items on a counterparty/kind switch but preserved `allocations`, and **the DB derives the counterparty FROM those item IDs**, so the UI could show party B while silently settling party A (fixed at the UI + walled at the model + a switching regression test; #183's gate was clean, two notes). **The ceremony (2026-08-05, owner GO):** a pre-flight hazard caught first — the migrate runner resolved to the WAVE CHECKOUT carrying the uncommitted monolith, so the ceremony ran a **main-pinned runner** against a checkout verified `≡ origin/main` — then quiesce → **`0042→0043→0044` applied CLEAN ON THE FIRST ATTEMPT** (43 total; every in-txn tail census passed on production) → **postverify 12/12 exact** (frontier `0044`; authenticated 189 / runtime 69; **`producer_authed` FALSE — the money fix, live**; composite TRUE; the four new tables RLS-forced at 0 rows; adjustment tables absent; `ea1955` seed 3; the advance belt trigger live; 563 functions = twin 599 − D-b2's 36) → `NOTIFY pgrst` → `/ready` 200, checks 2/2, **Supavisor 32/60**; **runtime v53 UNTOUCHED** (the slices ship no runtime change). **Sandbox acceptance (ROME PUBLIC ADVISORY, every artifact SYNTHETIC-labelled) ran as ONE THREAD through the production verbs:** synthetic statement PDF → the live intake transport → attribution **ABSTAINED** honestly → **the live agent saw the unassigned document and REFUSED to file it, naming the human's door** → the human two-step → `enter_bank_statement` CHAINED to October's closing (the `duplicate_period` wall firing honestly twice on the preserved C-c months) → `except_bank_line` → **`resolve_and_book_bank_line` in ONE ACT** (resolution minted + entry approved + match live + exception resolved, line = entry to the sen) → **`_adv_on_approve` BIRTHED the advance** → particulars completed → the EA-1955 policy notes speaking → **`staff_advance_tie` TRUE at difference EXACTLY 0** → `reverse_entry` walled by name → **`accept_bank_rule_suggestion` 42501 ON PRODUCTION**; six walls fired by name en route and **all four structural invariants were witnessed live** (attribution fail-closed · provenance sha-binding · wake authority · write authorization at the role). **The real half closes as a NAMED, MEASURED deviation (the ADR-056 precedent, strictly stronger):** both real clients are **in STRIKE-OFF** — RPR's GL is a cessation cut to 08-12-2025 with the strike-off invoice, the bank at zero and a director's waiver; RS carries a 6/3/2026 strike-off fee with both banks at zero by 19/3/2026; both end at a final net loss of exactly RM1,000.00 = share capital — so **no going-concern real client exists on which a live recurring template can run**, and RPR's nine closed-reconciled months (ADR-054) may not be disturbed. Evidence: `recovered/real-half-acceptance-evidence-2026-08-04.md`, which also CITES the real pattern the mechanism automates (RPR's month-end salary-accrual JV series, three instances identical to the sen — the sandbox template was modelled structurally on it). Nothing was booked on real books; the first LIVE real template defers, named, to the first going-concern client. **`0045` (D-b2) is HELD, built and twin-proven, and ships no round-11 fix BY DESIGN; its hold-ladder inherits, named:** the round-11 fix list (period-membership wall · P1's period-blind prohibition · the period-overlap advisory · single-live-successor · the `truncated` dangling-pointer flag · the declare/render surface chain · `pair_already_active` ordering · the parked-pair completer) · **CX6** the E8 comment-token hardening (test call-absence on comment-STRIPPED SQL) · **the declared-party cross-check question** raised by the #184 gate — should `resolve_and_book_bank_line` refuse stale-ID payloads structurally, given that `settle_from_bank_line` takes an explicit `p_counterparty` while the composite DERIVES the party from the items named · the D-b3 lane inheritance (`x40.am` · the `StatementDetail` chip thread · the `reconApi` producer wrapper · the cohort roster, which lands with the slice that makes it whole · the eight producer cells across four files, one of them a measured FALSE GREEN) · the producer's own guarded grant with E16's seven arm-(3) cells and E7's `revise_entry` refusal · and **CX10 stands ADJUDICATED AGAINST** (the S5.10 key-split is refused; E7's probed containment is the record). **The archive branch `build/wave-d-b-0042` must NEVER be merged** — it is evidence only, and three of its files are known-stale and must not be restored over what shipped (the dead `runPeriod` in the `x42-r7-fa-stamp` copies · `ExceptionBookingFields` still carrying MG1/MG2 · the `x42-af2-rebook` trailing whitespace); reconciliation runs toward `main`. **Standing law added:** migration numbers still claim at MERGE, now with a **`RENUMBER.md` procedure** (templates, an exhaustive stable-name inventory — never prefix-only — the version as a shared assembler input with a verified output path, and a mandatory dry run) · **the binding-gate exemption allowlist is EXACTLY ONE, printed as an audit line, and any addition is a review event** · a test for a granted verb belongs no later than the grant slice · a fork's shipping set is the transitive closure of its prologue, and **the dashboard splits with the DB** (import-closure rosters verified by shadow typecheck) · **ceremonies run a main-pinned migrate runner, never the wave checkout** · `NOTIFY pgrst, 'reload schema'` after any ceremony that adds RPCs · **a probe that can error under `|| true` reports blank as success — force the locale, never trust the ambient** (four assemblers reported "0 non-ASCII" for months; the real counts are 0/0/1/1) · a slice never rewrites a prior slice's installed comment · an OPTIONAL cross-section patch is a PROPOSAL, not a patch.
**Why:** The wave's closing lesson is that **a convergence rule written before the evidence exists is worth more than any judgement made after it.** The whole-ship ruling was correct on the evidence available at round 10 and was overturned by its own stated condition one round later, without argument, re-grilling, or a re-litigation of anything the owner had ruled. What followed vindicated the split on grounds nobody had listed: the intermediate frontiers are production states, and reviewing them as such found a live money mechanism (a phantom advance born of a grant that outran its guard) that no review of the monolith could have found, because in the monolith the grant and the guard ship in the same second. The corollary is now law — a test for a granted verb belongs no later than the grant slice, and a slice's surface roster is an import closure, not a keyword list. The three completeness instruments carried the risk the split created: twin-rig equivalence proved the partition lost nothing (and the three comment-only residues were kept, not erased, because a live body should carry the audit trail of how it reached its state), the 418-cell conservation proved the tests lost nothing, and the merge gate proved the reviews lost nothing — one fail-open security gate, one clean pass, one money-UI blocker that only a fresh reader of the payload could see. The acceptance was deliberately run as ONE THREAD rather than a drill checklist, because the wave's claim is that these mechanisms compose: the same statement produced the abstention, the agent's refusal, the human's resolution, the composite's one-act booking, the advance's birth, the tie at zero and the producer's 42501 — four structural invariants witnessed in a single story. And the real half closed the way D-a's did, on measurement rather than expectation: the books said strike-off, so the honest answer was a named deviation citing the real accrual pattern, not a fabricated template on a company that is winding up.

### ADR-059 — D-b2 CLOSED: 0045 LIVE (44 migrations) through a five-wave hold-ladder, three two-lens rounds ending CLEAN, a native-substitute merge gate, a timeout-taught ceremony, and a production acceptance whose headline is auto_reversal_of 0→live; WAVE D-b IS COMPLETE ACROSS ITS FOUR SLICES (2026-08-06)
**Decision:** **D-b2 (recurring + reversing adjustments, migration `0045`) is BUILT, GATED, MERGED (`c60abb8`, PR #188), DEPLOYED and ACCEPTED — the wave ADR-058 closed with a held slice is now whole in production.** **The hold-ladder (the ADR-058 inheritance, executed):** five fix waves through the deterministic generators — **W-R13** (the re-confirm list: the `lineage_root_id` constraint trigger, DEFERRABLE-never-skipped, CLR38 `adjustment_template_lineage_root_false` · calendar validation inside `_wdb_iso_day`, named exceptions → NULL = fail-closed · the sentinel-preserving lexer + ONE shared argument-blind token-aware call matcher across the four deploy instruments · count conventions NAMED IN THE FILE and **asserted by the assembler every run**) · **W-R14** (probe 10 + tail 1(b) onto the shared matcher · ALL thirteen retired-regexp call-judgement sites moved · the quoted-identifier token class — the uniform-blindness silent double was measured to EXECUTION before fixing · sentinels → control bytes with a fail-loud guard · the Unicode dollar-tag charset, measured against `lc_ctype=C` first because `[[:alpha:]]` would silently not have worked) · **W-R14.1** (tail 3(8)'s mixed roster split: token questions stay raw AND SAY SO; call questions join the matcher; the erasure arm proven non-vacuous on all three writers) · **W-R15** (the U& spelling of BOTH token classes with a measured ADJACENCY law; the inherited draft's own fix was found INCOMPLETE — the **UESCAPE clause** re-codes a plain-looking identifier, measured to execution as a grep-surviving double — closed by a whole-body detector that is over-broad ON PURPOSE, the documented owner-reversible trade; the dollar-tag LEFT TOKEN BOUNDARY (`x$tag$` is ONE identifier in Postgres) + loud refusal on unterminated regions; the tail 3(2) content anchor made comment-tolerant ON the shared text after the build lane proved the orchestrator's own smaller fix would have broken offset commensurability) · **W-R15.1** (comments-only tidy, proven comments-only by a token-level stripper with self-tests: 250,076 code bytes byte-identical). **The rounds:** r13-round (Codex + fresh native probe lens) and r14-round (Codex + fresh native lens; Codex's four static claims → measurement CONFIRMED two, CUT one from three sites to one, and one was OVERRULED on the lens's data + the splice-anchor precedent) each mechanism-bearing, all findings armour-side, the money core clean in EVERY round; **round three — Lens B refutation-first (re-derived the catalog census itself after finding the harness's `"char"` cast bug) + Lens A live-probe (reproduced the silent double TWICE on the baseline to prove the delta load-bearing; a 1,827-fact whole-database diff = 0 lines; the alignment invariant proven on 1,216 subjects) — BOTH: MECHANISM-BEARING: NO. The close rule fired.** The marker census instrument is `grep -c "[RNN FIX" <migration>` = **25/25/21/23/9** (two lanes independently failed to reproduce it by grepping other strings — the instrument is now named here so it cannot be mis-measured again). **The terminal armour law, now in the file itself: FAIL-CLOSED-ON-UNKNOWN** — the deploy instruments name their verified grammar subset, and any construct outside it refuses loudly with a diagnosis naming the construct or is a documented invisible-by-design entry; silent misjudgement is no longer a reachable outcome class. **The merge gate (DEVIATION, owner-ruled 2026-08-05):** Codex hit an account usage lock (until 2026-08-08) — the owner chose a **native substitute gate** (fresh opus lane, standards + spec axes) over waiting or buying credits. The gate verified on its own four rigs: roster exact both ways, every do-not-restore exclusion held by content probe, three money spot-checks confirmed on rigs, hygiene re-derived (the gitleaks findings live ONLY in the local NEVER-MERGE archive commit — not an ancestor, on no remote). Verdict HOLD on exactly one item, **MG188-1**: main's D-b1/D-b3 blocks promise their ungranted internals' "disappearance half arrives with the cohort at D-b2" — the gate derived the TRUE rosters empirically (0043: **17** ungranted, the prose names 13; 0044: **14**, the prose names 7 — MG188-2 records the incomplete prose) and prescribed the discharge; the constructor **re-derived both rosters independently, matched the gate's arrays diff-empty**, applied (cohort 49→80), and the gate confirmed **MERGE-CLEAN** on its own fresh rig. The PR itself: 66 files +25,445/−172, the manifest executed as law with adjudicated re-stages (the final build sha `a65af636`, the nine r11-dash dashboard files, a three-way merge of `adjustmentApi.test.ts` whose diff vs source is exactly the five `gitleaks:allow` lines), the **empirical ADJUSTMENTS_0045_COHORT** (a 0044-rig vs 0045-rig `pg_proc` diff: 40 net-new = 12 granted + 28 ungranted, verified cell-for-cell against live ACLs), floor 168/0/4 exactly, dashboard 656/656, runtime 1015/1015. **The ceremony (2026-08-06 00:0x MYT, the standing grant):** two apply attempts FAILED on the live cluster's ~2-minute default `statement_timeout` (the four whole-schema lex passes need ~211s on shared hardware) and **rolled back transactionally clean both times**; attempt two proved `ALTER ROLE` cannot reach a Supavisor pooled backend (`DISCARD ALL` restores login-time GUCs; pooled backends never re-login) — **the recipe, now law: a ceremony-local session-level `set statement_timeout` inside the migration connection (port 5432 = session mode), reverted after; role/db-level settings are invisible through the pool.** Attempt three applied in 211s; postverify exact (ledger 44/0045 · grants 201/71 · the producer grant flipped to authenticated-ONLY — the ADR-058 money fix completing · three adjustment tables born empty RLS-forced · the lineage trigger live) · NOTIFY pgrst · machine 2/2 · /ready 200. **THE ADJUSTMENTS BELT IS ARMED** (runtime v54 unchanged — the slices' design). **The acceptance (2026-08-06 00:15–01:10 MYT, sandbox, every artifact SYNTHETIC, the RPA session owner-minted):** the full runbook witnessed on production — WDB-G4's forced draft (audit: `ramp_earned=false, catch_up=true, high_stakes=true`, RM130,496.30) · the attested approve birthing **the auto-reversal mirror + the run receipt IN THE APPROVING STATEMENT** (mirror dated period_end+1, APPROVED, nine lines debit↔credit swapped, Σ=13,049,630 both sides — `auto_reversal_of` 0→LIVE) · June re-run `ramp_earned=true` with NO false mirror-shape refusal · fourteen-plus walls by name incl. the full pair cycle (park → `pair_already_active` → `proposal_not_revisable` → **`pair_draft_locked` firing FIRST**, the S5.10a order) · **the r11/r12 advisory SPEAKING on production** (propose B: `replaced_period_overlap` naming the retired predecessor, its one standing May charge, and the wall promise; the sign re-ask warning AGAIN) · **the wall keeping the promise** (run B May → `period_shape_already_met` with the four-remedy lineage-aware payload — identical shapes belong to the shape wall PER E38; carried on the oracle's `blocked[]`) · **the single-successor law live** (`template_replaces_already_succeeded` naming predecessor AND successor) · the producer verb NOT 42501 with the six-axis guard refusing by token (`wrong_period`) · **THE MONEY ASSERTION: every account nets to ZERO** across approved recurring-adjustment lines · `staff_advance_tie` still TRUE at difference 0. **Deviations, named:** an UN-SCRIPTED wall fired correctly (retire refuses `occurrence_draft_outstanding`) so June was WITHDRAWN rather than left parked; 4.12 fired the shape wall not the membership token (E38's ratified partition of the two walls; the membership token stays rig-proven at x42.r11a); B (windowed from May) was retired per **the wall's own remedy sentence** and **B2 is LIVE from 2026-07-01** — the oracle advertises July due with `blocked:[]`, so **the belt's next daily sweep drafts July autonomously (the first autonomous belt draft in the product's history, dated ~2026-08-06/07), and the first autonomous POST comes when a period ends after the sign date with the ramp earned (≥2026-10-01 for September)**. No real books touched — the real half stands as ADR-058's measured strike-off deviation.
**Why:** The ladder's closing lesson generalises ADR-058's: **the way out of an armour arms-race is not a better spelling list but a grammar boundary with a loud edge** — after three rounds in which every fix wave's new machinery became the next round's finding surface, the round that finally came back clean from two independent lenses was the one after the instruments stopped trying to out-enumerate Postgres and started refusing what they could not certify. Three times this slice, the worker's measurement overruled the orchestrator's prescription (the v_c offset commensurability, the opaque-vs-refuse direction for escaped U&, the live `U&'…'` body from 0028 that would have made the draft's own prose undeployable) — and each overruling was accepted because it arrived with the probe, which is the division of labour working exactly as the doctrine intends. The gate deviation proved the same shape at review grade: a substitute lane earned the gate's authority not by title but by re-deriving every roster it was asked to trust, and the one finding it held the merge for was a promise two merged slices had already made in writing. And the acceptance closed the loop the PRD draws: the machinery that five wave-records argued about now speaks its warnings in sentences on production, refuses doubles at the moment money would move, and leaves behind a live template whose first unattended act is scheduled by design rather than hoped for.

### ADR-060 — THE PRE-BETA DATA DOCTRINE: every firm's data is partner-authorized test data until beta; the agent holds full reset/reseed authority; the product's security mechanisms and the engineering gates are explicitly NOT relaxed (2026-08-06)
**Decision:** Owner-ruled (grilled 2026-08-06, four rulings): **(1)** The product has NO real clients today — every firm/client in the live DB (the ROME PROPERTIES / ROME SECRETARY corpora included) is **partner-authorized material for testing and review**. All "real data untouchable" prose across the harness (the honest-empty registers of ADR-056/058, RPR's nine reconciled months, the "never disturb" lines) converts to **resettable test state**: the agent may delete, reverse, reseed and re-run any client/accounting data on the live DB in service of wave validation, recording each reset in the session record. The earlier ADRs are not rewritten (append-only law); this entry supersedes their operational force pre-beta. **(2)** The authority is DATA-scoped only. The product's security MECHANISMS — forced RLS, the four structural invariants, maker/checker, audited-function-only writes, the wake allowlists — are **the product under test and are never weakened, bypassed or special-cased for testing convenience**; a test that needs a wall opened is a test of the wrong thing. **(3)** The engineering gates stay: `main` PR-only, green CI, the review ladders, cross-model gates where ruled — wave-validation quality is the point of this doctrine, not its casualty. Secrets discipline is unconditional. **(4)** The three raw corpora on the owner's desktop (BEE CREATIVE YA2024 · Rome Properties YA2025 · RS YA2025) are the sanctioned test material; where they cannot cover a mechanism, labelled-synthetic data remains the fallback (the ADR-048 sanction). **Expiry: this doctrine dies at beta/go-live** — the moment any genuinely un-authorized client's data enters, every suspended protection resumes at full force, and that resumption is itself a named gate item.
**Why:** The rebuild's validation was inheriting debt from a fiction — protections written for a live product were shielding partner-authorized test data from the very tests the partners authorized, and the "honest empty" deviations (correct under the fiction) were leaving whole mechanisms (real FA carry-down, payroll-shaped advances, real sales drafting) unproven against the only real-world documents available. Naming the doctrine, its exact boundary (data yes; mechanisms no; process no; secrets never) and its expiry converts an ad-hoc permission into an auditable law a future session cannot over- or under-read.

## PART 2 — OPEN ITEMS

*Open decisions + triggered obligations/gates only. Operational tasks (resume the 48h park, rotate a leaked token, and the like) live in the handoff + memory; phase/slice status lives in memory + `docs/plan/`; build narrative lives in git.*

*(Housekept 2026-07-29, again 2026-08-01 at the Wave C close, again 2026-08-05 at the Wave D-b close, and again **2026-08-06 in the post-Wave-D harness refresh** — completed items pruned to their ADRs; this register is maintained, unlike PART 1.)*

**Open now (top of the queue):**
- **TWO OPEN PRs, both JUDGEMENT-LOGIC changes, both awaiting an independent review pass before merge (the 2026-08-06 law).** Until they land, **`main` still ships the one-sided 409 defect.**
  - **#193** — *a surfaced interview refusal must outlive the poller*: the error-generation guard (today `useInterviewRun.ts:52` fires `setError(null)` on every `POLL_MS = 3000` tick, so a refusal is wiped ~3 s after it is shown) + the optimistic-bubble rollback + **all eight** stale `0042` comment refs in `reconciler-adjustments.mjs` (MG188-3's remaining half, above).
  - **#195** — *the 409 recovery reads fail-closed*: replaces `!stillOpenAtOurPark` with POSITIVE evidence of advance (a strictly-greater numeric park index, or a terminal in an explicit delivered set); everything else throws. Carries the AST-guard hardening and its named cost (below).
- **THREE LAWS MINTED 2026-08-06** — recorded in `CLAUDE.md`'s working protocol, which is what a fresh session actually reads; kept here as the decision trail only. (1) a judgement-logic PR gets an independent review pass before merge; (2) **absence is not evidence, and a derived state is not evidence** — only what a read actually SAW counts, everything else falls to the fail-closed branch; (3) **spelling is not identity** — a guard that reads a NAME reads a projection, so prove an identifier IS its import.
- **OPEN OWNER DECISION — tier review intensity by risk (raised 2026-08-06, UNRULED).** Proposal: the full ladder on money-touching paths; ONE two-lens round on instrument / UI / doc changes. Exhibit A is **#195** — six cross-model rounds on a non-money dashboard function; the lane's own retrospective reads rounds 1–3 as correct spend and rounds 4–6 as over-spend. Until the owner rules, the floor is law (1) above.
- **ADR-060's §1 SCOPE, NARROWED HERE (operative; the ADR itself is append-only and stands):** §1's sweep of *"all 'real data untouchable' prose"* reaches **CLIENT AND ACCOUNTING DATA IN THE LIVE CLARA PROJECT ONLY**. It does NOT reach — and these stay fully protected — **the frozen prior build** (`initial acc software skillmd` + the `belcort-shared` Supabase, read-only audit evidence until Phase-5 decommission) and **the Slice-0 spike's `workflow` / `graphile_worker` / `spike` schemas and its live parked run**. Those are the only referents of the harness's "never disturb" line and neither is client data; a literal reading of §1 that resets them would be an over-read of a law written to prevent over-reads.
- **ADR-060 (the pre-beta data doctrine) IS IN FORCE** — every firm's data is partner-authorized test state, agent holds reset/reseed authority, mechanisms/process/secrets explicitly not relaxed; EXPIRES AT BETA (the resumption is a named gate item).
- **WAVE D — COMPLETE. D-a CLOSED (ADR-056) · D-b CLOSED ACROSS ALL FOUR SLICES (ADR-058 + ADR-059, 2026-08-06): `0042`+`0043`+`0044`+`0045` LIVE.** Mechanisms of record: `wave-d-contract.md` (WD-R1..R15) · the as-built truth `wave-d-b-asbuilt.md` + `-part2.md` · the D-b2 hold-ladder record (`~/.clara-tools/d-b-build-backup/recovered/split-build-record.md`, ADR-059) — **never re-grill WD/WDB rulings, any ladder's adjudications, or the hold-ladder's settled residuals** (the probe C/A design residuals · the raw-subject sentinel guard · the S5.8-b2 splice anchor · trade (f), owner-reversible to a real UESCAPE decoder). **Live: 44 migrations (frontier `0045`) · runtime v54, THE ADJUSTMENTS BELT ARMED · producer verb granted (authenticated-only) · dashboard from main incl. /rules adjustments panel.** **Two dated witnesses stand:** the belt's **first autonomous DRAFT** (sandbox B2's July, ~2026-08-06/07) and the **first autonomous POST** (≥2026-10-01, September's period under the earned ramp). MPERS presentation wording stays a Wave-E verification item. Archive branch `build/wave-d-b-0042`: **NEVER MERGE** (evidence only; do-not-restore list asbuilt-part2 §13). **Follow-ups from the close (ADR-059):** ~~MG188-2 rig-meta prose 13→17 / 7→14~~ **DISCHARGED by #191** (verified mechanically: prose names diff-empty against both arrays) · MG188-3 `reconciler-adjustments.mjs` "0042" → **HALF discharged by #191** (`:103` fixed; **EIGHT** comment refs still name the wrong migration — re-counted on main 2026-08-06 at `:2/:4/:6/:14/:79/:130/:143/:148`, three more than the five this register first listed — so the file is internally inconsistent. **The remaining eight are #193's subject**; comment-only, and never touch the `'0042 '` error-message prefix, which is the wave-wide convention) · any FIFTH whole-schema lex pass in a future migration needs a deploy-time measurement first (the fourth cost ~+11-14s; live apply = 211s vs the ~2min cluster default) · Codex account locked until 2026-08-08 (the merge-gate deviation was owner-ruled; restore cross-model gating when it returns).
- **THE WAVE-D REAL-HALF DEFERRALS NOW HAVE A NAMED CANDIDATE — BEE CREATIVE SOLUTION (`9e957c0f`, a BELCORT client, verified live 2026-08-06: 5 journal entries, 4 approved).** ADR-056 deferred the FA carry-down's first real firing + the ≥1 real reducing-balance asset "to the first asset-owning client"; ADR-058/059 deferred the first LIVE real recurring template "to the first going-concern client". Both deferrals were written when BELCORT's only real clients were the ROME pair, **both in strike-off**. BEE is a sole prop and a going concern, so it is the first client that could discharge them — **but nothing is discharged yet, and the deferrals stand as written until a real firing is actually witnessed.** Its two NARROW gates are already scoped (Gate P on its SST invoices; the WD-R14 real FA carry-down over the scanned desktop asset). *A sole proprietor is NOT an employee — his account is EQUITY, never a staff advance and never a counterparty (WC-R10).*
- **WAVE C — CLOSED (ADR-054, 2026-08-01).** C0 (0036) · C-a (ADR-052; 0037) · C-b (ADR-053; 0038+0039) · **C-c ACCEPTED (ADR-054; 0040)** — thirteen complete receipts at exact zero (sandbox 4 + one deliberate drill void; RPR 9), the learn loop live with `origin='rule'` matches, WCC-R9 executed born-bills-only. *(Wave D followed — see the bullet above.)* **C-c acceptance findings, open as follow-ups:** (F-1) an allocation whose effective_date predates its target item's `item_date` breaks the interim-as-of Σbuckets=control tie silently — candidate guard on `allocate_payment`/`allocate_receipt` (refuse or explicit override); two documented historical scars stand at RPR as-of 2025-08-31 (+10,000¢×1000) and 2025-09-30 (the corrected first spread; heal at as-of ≥ 2026-08-01) · (F-2) non-P&L match splits need the two-entry one-group idiom — folded into the composite resolve-and-book verb; **CLOSED BY ADR-058** (the composite is live at `0044` and fired in the sandbox acceptance) · (F-3) the completing-recon GUC one-receipt width CONFIRMED live (a 3-recon transaction refuses at the deferred belt) — fine as-is, the UI is one-per-request; documented. **The RM5,000-bill question is CLOSED (owner confirmation, 2026-08-01):** the booked Jul–Dec ROME PUBLIC bills stand at RM5,000 ("accounting fee and payroll services" — payroll started Jul); the on-disk RM500 invoice series covers only the accounting-fee component. **C-a residuals still open:** §5.3 pool segregation (the WCA-R8 pin is its live evidence) · the Section-I wedge remedy (`cancel_agent_task` → re-admit) · the real-PG dead-letter battery (declined as not-cheap) · MSIC backfill for the three real clients (sanctioned lane).
- **Gate P** (the WA21-R13 survivor; Gate S closed synthetic per ADR-049): closes on the next **real SST-stated supplier bill** via the chat 3-leg split (TB tie to the sen) — **operating runway, not engineering**. 0036 §A widened its territory: the capitalised/mixed-purchase tax-allocation question is now a NAMED Gate-P design item (the belt deliberately does not claim those shapes). Gate D residuals ride along.
- **R2 backup cadence — RULED (WB-R26, ADR-037):** monthly LIGHT human-assisted drill + quarterly STRICT per DR.md. **First monthly sitting still to schedule (owner + agent).** Wiring DONE 2026-07-22 (evidence `docs/ops/DR.md` §9).
- *(Pruned to ADR-051: the #51/#52/#53 register + the sales mis-route — all landed in 0036.)*

**Open decisions / obligations (owner/legal):**
- **The ROME PUBLIC payment allocation — RULED (WCC-R9) and EXECUTED (ADR-054, 2026-08-01):** FIFO oldest-first, executed **born-bills-only** (each payment reaches only bills in existence at its date — the reading under which the ruling's own figures are exact): 8 bills / RM26,500 settled · the Dec-02 bill honestly open · the **RM30,000 excess live as a related-party ADVANCE in `350-003`** (non-`payable`-class per WC-R10 — never in AP aging) · the fourth payment (−RM13,000 2025-10-09) booked as further advance under the owner's session grant. Reversible by construction (`unallocate_group`/`apply_open_items`) if mislaid rental/service bills later surface.
- **The onboarding commit-lane shape — RULED (WB-R22, ADR-037):** the audited temp-admin ceremony stands (Gate O closed on it); target = a scoped review-attestation capability (future migration).
- **C6 checklist (ADR-011):** the DPA execution, firm-facing disclosure text, and PDPA cross-border check are OWNER/legal work items before any vendor trace export; engineering keeps the vendor-trace flag OFF until all three are evidenced. (WA2-R2 declared client consent material handled for the product's own cross-border processors; the C6 vendor-*trace-export* items remain open.)
- **Billing / scale guardrails, MyInvois depth (API pull + issuance), tax-comp v1-vs-v1.1 slip, multi-currency, opening-balance onboarding** — deferred product questions (PRD §9; multi-currency and opening-balance land with their waves).
- ~~Undecided: structured (MyInvois) sales autopost on Wave C's tail~~ — **RULED (WC-R12, ADR-051): not a Wave C item; superseded by fact (zero XML documents exist outside the sandbox).** Corrected en route: the OCR-sales nine-control envelope **is already BUILT** (0016 §3.3 + `_ocr_sales_floor` + post-time re-derivation) — the blocker is the missing unattended sales drafter + the floor not accruing from manual drafts (contract §7-A), not the envelope.
- **Remaining product waves** (REBUILD-PLAN; A/A2/A2.1/B, the pre-Wave-C program, and **C — CLOSED, ADR-051..054**): **D** assets + adjustments — **COMPLETE** (D-a CLOSED ADR-056/`0041`; D-b CLOSED across all four slices, ADR-058 `0042`+`0043`+`0044` and ADR-059 `0045`) · **E** periods + statements (now also owns: closing stock per WD-R11 · the segment-aware FA tie-out · the close gate for depreciation per WD-R6 · MPERS presentation wording) · **F** tax (SST engine + payroll calendar + tax-comp; CA computation over D's inert metadata; the payroll staff-master mapping over D's advance register) · **G** the OS surface. The §7-B PRD §4 amendment LANDED with ADR-054 (the five no-home behaviours named with waves: staff advances → D · staff allowances → F · self-billed e-Invoice detection → F · WHT-as-mechanic → F · foreign currency → its own post-G wave per WC-R5). The **§7-A runtime bundle stays PARKED as a unit — destination the unattended sales drafter build**.

**Triggered gates (met / standing):**
- **Full-profile fresh-project DR drill (ADR-020): ✅ CLOSED 2026-07-20** — executed against a real fresh Supabase project, 177/0 STRICT (AP gate exact; canary parked both sides; storage byte-identical); found + fixed five defects three review stages missed; CI-gated by a cross-cluster round-trip. **Re-run quarterly.** The off-site R2 wiring closed 2026-07-22 (evidence DR.md §9); still owner-tracked: PITR (deferred).

**Deferred hardening (triggered follow-ups; note, don't build now):** *(landed since the last sweep, pruned to their records: the 0017 queue-rank alignment · the `fail_classify` terminal writer → 0024 · the Actions SHA-pin.)*
- **ComplianceWatchCard `acknowledged_at` echo:** the queue envelope doesn't carry it, so the card can't show "acknowledged ✓ at …" after an ack (the DB trail is complete; UI-only).
- **Supavisor session headroom watch:** the 0045 ceremony (2026-08-06) restored the runtime pool to its 12-session baseline post-restart (32/60 at the 0044 ceremony; 38/60 at 0041; 31/60 at 2026-07-29). The two `clara_runtime` grants landed with `0045`; the belt's 5th due-check adds no session. **Re-measured 2026-08-06 in the harness refresh: 32/60 total, 11 of them `clara_runtime` — steady at baseline with the belt armed.** Re-measure at the next wave's consumer additions.
- **Local disposable Supabase stack** (`supabase start`) as the intended local test target — needs Docker (unavailable). Interim = a throwaway remote schema / the throwaway-PG17.6 rig + the destructive-target guards (`packages/db/lib/guard.mjs`).
- **Server-side branch protection** on `main` — owner-only plan upgrade; the git-base freeze-lint + CI are the interim gate.
- **C-b acceptance-night findings (2026-07-31):** item (1) the 0017 authoritative-extraction trigger's kind-blind supersede — readers fixed kind-honestly (PRs #154 et al.); **the trigger's own kind-scoped supersede stays an OPEN ADJUDICATION** (needs an `authoritative_extraction_id` consumer census before a migration candidate). *(Items 2–5 — the leader-cancel `settle_chat_turn` misuse, the `other`→`consent_evidence` door, the CSV single-column preamble, `request_reextraction`'s statement scope — ALL LANDED in the C-c build, migration 0040 + the runtime ride-alongs.)*
- *(Pruned to ADR-054/PR #169: re-kind task hygiene and the statement-refusal sink `{mode:0o600}` — both LANDED in the C-c build.)*
- **C-c build residuals (WD-R13): FULLY PAID.** The D-a half (ADR-056/0041): the `reverse_entry` MYT mirror-date splice · the sst-watch DB-clock · the `BankReconReceiptCard` void-history render. The D-b half (ADR-058/0044 + **ADR-059/0045**): the composite resolve-and-book verb LIVE and granted · the `bank_rule_suggested` producer verb's `clara_authenticated` grant **LANDED with `0045`** (NOT-42501 + the six-axis guard refusing by token, both proven on production at the D-b2 acceptance) — **the AF-2/producer register is CLOSED.**
- **Interview v3 residuals (PR #186) — the #191 (`ba05a27`) half is DISCHARGED; the rest is live in #193/#195 above, plus THREE residuals that no PR owns yet:** the AST-hardening of the ordering guard and the two stale `interview-e2e.mjs` comments are DONE. The dashboard's 409 re-read-and-retry shipped but its **disambiguation is ONE-SIDED** — a native review proved `!stillOpenAtOurPark` reads an absent/older/empty/cancelled/shape-drifted park as *delivered*, which is the very "assume delivered" class #191 existed to kill, and `/state`'s park is a best-effort 800 ms bounded stream replay that genuinely produces those shapes on the long client interview (**#195**). The 3-second error wipe rides with it, and the optimistic answer bubble is never rolled back on a throw — **its id is keyed on park+phase, so a retyped DIFFERENT answer at the same park never appears at all** (**#193**).
  - **UNOWNED — the concurrent-submitter receipt gap (a RUNTIME CONTRACT change, not a dashboard fix).** Even after #195, *"a higher park index ⇒ my answer landed"* is an **inference, not a receipt**: **any bookkeeper+ of the firm can win a CLIENT-scope hook.** Verified on main — `interviewRoutes.ts:337` gates the client branch on `isBookkeeperPlus(p.role)` + the plan binding only, whereas the FIRM branch at `:318` binds ONE principal (`firmOwnerMatches(markers.owner, sub)`). So when two bookkeepers answer the same park, the index advances for both and each concludes it was theirs. **The real fix is a server-authored per-(run, park, submission) receipt** the caller can match against.
  - **UNOWNED — no cell pins the caller half.** There is no `useInterviewRun` test (the only test in `apps/dashboard/app/onboarding/` is `thread.test.ts`), so *"deliverValue returns false for a dropped receipt"* is prose-only.
  - **UNOWNED — `interview-e2e.mjs:246` on main:** the de-pin names `interview.v2.core.ts` **inside a clause that says no version is named here**. True today, stale at the next core bump — a dated tripwire, not a defect.
  - **The AST guard's NAMED COST (a deliberate fail-closed trade, #195).** After the hardening these four shapes are all RED **with a diagnosis**: `try { arm; await announce } finally {}` · a second hook armed elsewhere in the file · `const p = streamPromptStep(...); await p;` · a ternary arm. Refusing a shape it cannot certify is the intended posture — do not "fix" a red by loosening the guard; re-shape the body or extend the grammar deliberately.
- ~~**Gitleaks PR-scope follow-up**~~ — **DISCHARGED by PR #190 (`9859f64`), and it landed WIDER than this register asked.** As shipped: the `pull_request` context scans only the PR's own commits (`--log-opts "--diff-merges=first-parent origin/main..HEAD"`) while push/manual keep full history (`--diff-merges=first-parent --all`), so an evidence/archive branch can no longer red every PR. The addition nobody had registered: **`--diff-merges=first-parent` now applies in BOTH contexts**, closing a measured blind spot — a secret introduced only in a merge-commit conflict resolution appears on neither parent's own line and was invisible to gitleaks in either context before this. `--diff-merges=cc` was measured and rejected (its multi-parent line prefixes never match gitleaks' diff parser, so it reports "no leaks found" on a verbatim secret). **Standing interim law survives the discharge:** scan any branch with CI's own pinned config BEFORE pushing it.
- **The unreverted-admin-grant lint watch** (Gate-O era) — rides along; no trigger yet.
- **Always-run role/membership reconciliation (Slice-2 HIGH 6/7):** `clara_*` role attributes + cross-lane memberships live at the cluster level; poisoning them needs SUPERUSER (outside the threat model, un-re-normalizable on managed Supabase) — a fail-closed runner-level reconciliation is deferred.
- **Supabase non-superuser deploy-role CI (Slice-2 HIGH 8/9):** CI applies migrations as superuser `postgres`; `0002` is non-superuser-safe, but a CI job running the full set under a Supabase-shaped non-superuser role is the follow-up.
- **Opaque/HMAC pack tokens (Slice-3 C12, declined):** the books-version token is a plain number; the DB gate + commit-time recheck kill the accidental-stale class; a stronger structural binding is optional.
- **`activate_taxonomy_version(v)` operator fn (Slice-3 C8)** + **predicate-dimension taxonomy schema (Slice-3 C16):** ship when a second taxonomy version / workflow-period-materiality routing state first exists.
- **Slice-4 residuals (ADR-017):** per-part-type field schemas → the fail-closed card catalog; audited owner compliance export + a visibility-aware trace-debug surface; per-firm chat-visibility toggle + un-share; S4-V2 engine-hook-lifetime≥14d (the **canary `daba7f2e` watch, due 2026-08-02 — NEVER answer it**); job-level engine liveness; firm-local-time budgets; billing-grade metering.
- **Slice-6/Wave-A residuals still open (ADR-019/023):** task-per-ingest coding; a dedicated proactive notification-inbox surface (the current one-queue is the interim); agent-visible attribution candidates. *(Most of the old Slice-6 deferral list — standing rules, SST split, the vendor→account rules table, batch approve, the per-client egress registry, sales invoices + MyInvois — LANDED in Wave A/A2. The AB-3 `engine_kind` pre-MyInvois pin is PRESERVED as-built.)*

**LAW clarifications (narrowing, not changing, an invariant):**
- "The DB owns every number; the agent never computes a figure" at Slice 2 means an agent-proposed draft becomes authoritative ONLY after exact-revision human approval (maker/checker + revision token). Deterministic derivation of legs from persisted OCR-extracted source facts is the Slice-5 document pipeline (Codex MEDIUM 18).
- The wake-secret txn-local property is a **runtime pool contract (Slice 4)**, not DB-enforceable — the DB trusts the `clara.wake_secret` GUC within a request (Codex MEDIUM 14).
