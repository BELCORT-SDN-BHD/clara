# PROJECTLOG ARCHIVE — ADR-060 .. ADR-064 (the pre-E clearing + the §7-A arc)

> Split from `docs/PROJECTLOG.md` on 2026-08-08 at the §7-A close housekeeping — the
> append-only log grows by design and the fix is a split, never a rewrite or a prune.
> Nothing here was edited; cite these ADRs by number exactly as before. The arc (the
> pre-beta data doctrine → the uniform-review ruling → the pre-E clearing day → the
> §7-A grill → the §7-A two-halves close) is COMPLETE; the live log (`PROJECTLOG.md`)
> holds **ADR-065 onward**.

### ADR-060 — THE PRE-BETA DATA DOCTRINE: every firm's data is partner-authorized test data until beta; the agent holds full reset/reseed authority; the product's security mechanisms and the engineering gates are explicitly NOT relaxed (2026-08-06)
**Decision:** Owner-ruled (grilled 2026-08-06, four rulings): **(1)** The product has NO real clients today — every firm/client in the live DB (the ROME PROPERTIES / ROME SECRETARY corpora included) is **partner-authorized material for testing and review**. All "real data untouchable" prose across the harness (the honest-empty registers of ADR-056/058, RPR's nine reconciled months, the "never disturb" lines) converts to **resettable test state**: the agent may delete, reverse, reseed and re-run any client/accounting data on the live DB in service of wave validation, recording each reset in the session record. The earlier ADRs are not rewritten (append-only law); this entry supersedes their operational force pre-beta. **(2)** The authority is DATA-scoped only. The product's security MECHANISMS — forced RLS, the four structural invariants, maker/checker, audited-function-only writes, the wake allowlists — are **the product under test and are never weakened, bypassed or special-cased for testing convenience**; a test that needs a wall opened is a test of the wrong thing. **(3)** The engineering gates stay: `main` PR-only, green CI, the review ladders, cross-model gates where ruled — wave-validation quality is the point of this doctrine, not its casualty. Secrets discipline is unconditional. **(4)** The three raw corpora on the owner's desktop (BEE CREATIVE YA2024 · Rome Properties YA2025 · RS YA2025) are the sanctioned test material; where they cannot cover a mechanism, labelled-synthetic data remains the fallback (the ADR-048 sanction). **Expiry: this doctrine dies at beta/go-live** — the moment any genuinely un-authorized client's data enters, every suspended protection resumes at full force, and that resumption is itself a named gate item.
**Why:** The rebuild's validation was inheriting debt from a fiction — protections written for a live product were shielding partner-authorized test data from the very tests the partners authorized, and the "honest empty" deviations (correct under the fiction) were leaving whole mechanisms (real FA carry-down, payroll-shaped advances, real sales drafting) unproven against the only real-world documents available. Naming the doctrine, its exact boundary (data yes; mechanisms no; process no; secrets never) and its expiry converts an ad-hoc permission into an auditable law a future session cannot over- or under-read.

### ADR-061 — REVIEW INTENSITY STAYS UNIFORM: the risk-tiering proposal is DECLINED; every substantive change keeps the full ladder (2026-08-06)
**Decision:** Owner-ruled with the #195 exhibit in full view (seven cross-model review rounds plus an independent native review on a dashboard client function whose worst failure is one retyped onboarding answer): review intensity is NOT tiered by blast radius — the "full ladder on money paths, one two-lens round on instrument/UI/doc" proposal is declined, and the full ladder stays the standing posture for every substantive change. The 2026-08-06 law-1 floor (an independent review pass on any judgement-logic PR before merge) is unchanged and remains the minimum, not the ceiling. §7-A and every later wave budget review accordingly — assurance was chosen over cost as a standing posture, not per-case. Same-day exhibits, all run under this posture: #196 (docs refresh, lane-verified item-by-item; merged `5e483f9`) · #195 (independent from-scratch review MERGE, convergent with Codex rounds 1–7 on the guard's two blind spots; merged `cbc024b`) · #193 (review HOLD → two blockers fixed by a separate lane → the original reviewer re-reviewed CLEAR; merged `ca6e07f`). `main` no longer ships the one-sided 409 defect.

### ADR-062 — THE PRE-E CLEARING DAY: real-document executions on BEE, three structural gaps registered from walls working correctly, Gate P re-scoped to operating runway, the §7-A skeleton ratified at v2 (2026-08-06)
**Decision:** Owner-directed clearing of all pre-Wave-E debt in one session under ADR-060's data authority, every mechanism at full force. **(1) WD-R14's acquisition half EXECUTED on real documents:** BEE's ENOTEX desktop (IV-2502-00074, RM6,236.00, 21/2/2025, C.O.D.) capitalized via the manual attested lane — document-bound entry `547fbb9c` with four verbatim region citations, approved; **the first real client asset in the product's history born through acquisition-from-coding** (`dfd0fc52`, profile 200-C01/210-C01/900-D02); particulars complete (straight-line 60 months, start 2025-02-01, residual 0 — the prior accountant's recovered 20%-monthly policy); **the first real depreciation authority signed LIVE** (`31faabac`, monthly). `run_depreciation_period` correctly 42501s the human lane — the Feb–Dec 2025 catch-up belongs to the belt's daily tick (same tick as the B2 witness; the DB's sen-exact accumulated figure is recorded when it fires; prior accountant printed 1,143.30 with ~3-sen rounding slack). The carry-down and reducing-balance halves stay honestly deferred: BEE held zero assets at its 1/1/2025 opening and its one asset is straight-line by prior policy. **(2) Gate P: method re-ruled, then measured structurally unclosable, then re-scoped.** The ruled statement-is-truth method met measured reality: zero OpenAI debits in all twelve YA2025 HLB months (the account carries no card rail at all — proprietor-paid personally, matching the entity law), and three of the eight invoices print no MYR at all. The re-ruled method (base = the invoice's own printed SST × 12.5 — the 8% ratio, zero FX invented; Cr 160-DRW) then met the **MYR-only ledger wall (WC-R5, by design)**: the chat coding lane refuses USD documents, correctly. **Owner-ruled disposition: books kept honest via the manual attested lane** — invoice 0008 booked (`863e8349`: 900-S03 82.63 + 900-SST 6.61 / 160-DRW 89.24, four citations incl. the printed RM6.61; TB ties at 21,632,524¢ both sides); the remaining four MYR-stated invoices follow the same method once the extraction-recovery door exists — **and Gate P itself is RE-SCOPED to operating runway**: it closes on the first native-MYR SST-stated supplier bill. **Sibling ruling, same session: Gate S's real-MyInvois-XML leg is likewise RE-SCOPED to operating runway** (owner-ruled, no extra synthetic drill — zero XML exists in any corpus, the synthetic leg closed at ADR-049; it closes on the first genuine e-invoice document any client receives or issues). **(3) Three structural gaps registered, each found by a wall working as designed:** MSIC backfill is impossible for active clients (`commit_client_onboarding`'s CLR10 guard, 0017:2751/2777; no path re-opens an active client; no lighter door — folded into Wave E's client-facts item with entity_type surfacing, the owner's codes RPR 68109 · RS 82110 · BEE 74101 parked); **a terminally-failed document ingest has no recovery door** (`request_reextraction` CLR16 requires a classified kind, sha-adoption is a no-op on a failed ingest — surfaced by the 2026-08-06 Azure DI credential outage that failed seven fresh ingests; a narrow recovery verb is a registered design item); **foreign-currency documents are structurally refused** (the MYR-only ledger, correct until the FX wave — and the corpus measurement that OpenAI×8 + Midjourney-monthly are routine Malaysian-SME reality formally raises the **FX-lite prioritization question** for E/F planning: purchase-side foreign bills over effective-dated official BNM rate tables, the my-tax policy-table pattern, DB-computed conversion citing the rate row). **(4) §7-A skeleton RATIFIED AT v2** (`docs/plan/wave-7a-design-skeleton.md`; provenance v1 → native read clean → Codex `gpt-5.6-sol` NOT-READY with the deployment-order hazard, half-1's four admission blockers, the DB-authoritative tri-state routing contract, the full 0023 corroboration predicate, `p_workflow_run_id text` → v2 fold, five-slice build order, `_coding_lane_core` recut sized in; records durable at `~/.clara-tools/g7a-grounding/`). The build session grills §6 — including Q4b (should the sales floor require `coding_kind='sales_invoice'` — the generic-JE provenance hole) and the tax-silent draft-only bypass. **(5) The deploy-lock miss closed** (PR #198 `b2250c9`: all 26 live-but-unlocked frozen entries stamped via the tool's own `--lock-deployed`; the cutover e2e's dated point-in-time tripwire corrected; **the ceremony recipe now ends with `--lock-deployed` + commit, every ceremony**). **(6) Interview hardening landed** (PR #199: bubble rollback incl. deliverValue, guard file-wide announces + closed-set call-argument reachability, structural fetch bounds in runtimeFetch, draft kept on refusal — the full law-1 loop: independent HOLD on a MEASURED false green, fix, original-reviewer re-check with fresh probes, CLEAR). **(7) The first local backup→restore→verify drill PASS** (STRICT 330 probes: 136/136 row parity, security envelope byte-identical, 145/145 entries balanced, ledger checksum-exact at 44/frontier-0045; all 109 raw FAILs root-caused — 101 proven a session-timezone hashing artifact by UTC re-hash; three tooling follow-ups registered). **(8) Grants recorded:** the self-mint is standing (owner, re-confirmed; custody-safe by design; account/password acts stay the owner's); ADR-061's uniform ladder governed every item above.

### ADR-063 — §7-A GRILLED AND RATIFIED: nine owner rulings open the unattended-sales-drafter build; the settlement-corroboration door registered (2026-08-06)
**Decision:** Owner-grilled (session `fd311e06`, Chinese-language interview rounds; mechanism of record `docs/plan/wave-7a-contract.md`, 7A-R1..R12 — never re-grill). The load-bearing rulings: **(1) rollout = ONE continuous quiesce ceremony WITH a kill-switch** (activation flag ships OFF, flips only after v6 is verified live, and survives as an emergency de-activation switch — supersedes the skeleton's two-ceremony lean); **(2) the DB-authoritative TRI-STATE direction contract** (`sales|purchase|unresolved` bound at admission, revalidated in the draft writer; model `coding_kind` is a proposal, never routing authority; `unresolved` never drafts); **(3) tax-silent sales draft-only** (`tier_a_fails` leaves the sales lane's DRAFT admission blockers only; every posting path keeps the full gate — all 22 real RS invoices are tax-silent, and without drafts they starve every future automation of approval fuel); **(4) floor purity** (`_ocr_sales_floor` counts only `coding_kind='sales_invoice'`, atop the binding `corroborated>=6` ROOT fix — the generic-JE provenance hole closed); **(5) stampede control** = per-firm cursor/cap + an explicit recorded backfill operation, no date cutoff; **(6) RS's sales side APPROVED** — revenue + trade-debtors accounts created, 12 customers born through approvals, Half 1 stays real-document on RS's genuine YA2025 books; **(7) menu** = `sales_invoice` + schema-only `sales_credit_note` (never-autopostable stands), NO free-form `journal_entry` in the unattended lane; **(8) classifier untouched; (9) the nine OCR controls restated** in the contract §4, superseding the wave-a2.1 `:17` control-7 summary error; **(10) Wave E is SEQUENTIAL** (the parallel-draft offer is withdrawn by the owner). **Registered on the roadmap (not this wave): the settlement-corroboration door** — the bank-settlement receipt as an independent second reader, the designed path by which tax-silent documents eventually earn corroboration and autopost eligibility; it records the owner's product vision that **autopost is to encompass every accounting behavior class, each class earning its own envelope**. Standing refusals re-confirmed unchanged during the grill: autopost-from-seeding (WB-R2), hand-created rules past the floor, hand-drafts as autopost fuel.
**Why:** The skeleton deliberately left §6 to the owner because every item is gate policy, not mechanism. The grill also surfaced (and the contract records) the teaching arc behind the rulings — draft-authority vs posting-authority, CN risk asymmetry, the shape-wall doctrine for free-form entries — so a future session inherits the reasoning, not just the verdicts.

### ADR-064 — §7-A CLOSED: the two-halves acceptance — the OCR-sales envelope posts unattended in the sandbox and drafts 21 real invoices on RS's books; the nine-controls battery is complete; four findings minted (2026-08-07)
**Decision:** **§7-A — the unattended sales drafter — is ACCEPTED and CLOSED** on the two-halves acceptance run 2026-08-07, executed under ADR-060's data authority with every mechanism at full force and ADR-061's uniform ladder on every merge. Evidence of record, verbatim and evidence-grade: `docs/plan/wave-7a-acceptance-h2.md` (Half 2) and `docs/plan/wave-7a-acceptance-h1.md` (Half 1) — as-run records, NOT design docs; the mechanisms stay `wave-7a-contract.md` (7A-R1..R12, ADR-063) and `wave-7a-design-skeleton.md` v2. **Live at the close: 46 migrations (frontier `0047`) · runtime v57.**

**(1) HALF 2 — the sandbox (ROME PUBLIC ADVISORY `39008536` / "Fictional Test Services" `9ab680ea`), labelled-synthetic under ADR-048.** An `ocr_sales` autopost rule was **PROPOSED → PREVIEWED → SIGNED → POSTED UNATTENDED** — the envelope's first exercise in the product's history. **Five unattended posts across two rule ids** (`a3f93bfe` ×3 · its successor `12c495dd` ×2); the chain is 109 ms end-to-end with no human in it (`draft_entry` by the agent actor → `approve_entry` by the rule's signer, `checked_via_rule_id` stamped, `op_key rulepost:…`). The signing-time preview verb and the floor it previews returned **identical numbers in the same minute, in BOTH directions** — `floor_met` true at 7/7/7/85 and honestly FALSE at 5/5/5/85 after two floor entries were reversed. **The nine-controls receipt battery is COMPLETE:** eight named post-time skip tokens SEEN verbatim in `clara.rule_post_skips` / `sweep_run_items` (`evidence_class_mismatch` ×2 · `over_cap` ×2 · `not_corroborated` · `anchor_missing` · `counterparty_unresolved` · `window_exhausted` · `not_a_draft` ×2 · **`floor_lost`** receipt `aa87e30e`, 12:09:05.522554Z — **wild-caught**: the runtime's own sweep drafted the trigger 15 s before the lane's `request_autodraft` landed, so the last outstanding cell was closed by genuine unattended traffic, not by a forced fixture), plus admission-side `customer_ambiguous` and `refused_budget` on BOTH its gates (token and concurrency, distinguishable by refusal-token shape). **Four tokens are documented NOT-REACHABLE from any lane the product exposes today, each for a measured reason, none for want of trying:** `polarity_unverified` / `direction_unproven` (admission BINDS direction and `set_document_kind` writes a positive human classify verdict) · `customer_unresolved` (needs the rule's own counterparty retired — destroys the floor, no un-retire verb) · **`buyer_mismatch`, ruled a defence-in-depth PROPERTY rather than a gap, on three independent measured reasons** — the unattended lane cannot diverge (`execute_rule_post` selects the rule BY the draft's own resolved counterparty, so a mis-billed invoice returns `no_live_rule` before control (b2)); **the chat lane refused twice, on principle, to author a knowingly false accounting record** — including after an explicit "labelled-synthetic adversarial fixture" justification, which it rejected by name ("a labelled synthetic/adversarial test designation does not permit a deliberately inaccurate accounting draft"); and no third door stamps `coding_kind` (7A-R12 holds). The lane did not coerce past those refusals and that is recorded as the right call. **The F1 blocker found and fixed inside the campaign:** every unattended post stranded its own autodraft task and jammed the leader loop in a ~2 s retry cycle (52 cycle-errors in one window; measured collateral — document tasks starved 19 minutes) because the settle guard read *"is this entry still a draft?"* as proof of *"did this task produce this entry"* — **Law 2's exact shape at the settlement layer**. Fixed by `0047_settle_guard_identity` + runtime **v57** (identity, not status; plus per-task isolation in the reconciler so one un-settleable task can never abort a sweep); regression evidence is the two post-fix posts (`c72dbc16`, `6413f8f5`) settling `completed` at attempt 1 with **zero strands and zero cycle-errors** while the 100 ms race is demonstrably unchanged. **Sandbox close: floor restored `floor_met` TRUE at 6/6/6/85; TB 33,133,816 = 33,133,816 sen, difference 0.** The restoration proved the **anti-circular exclusion** from the function's own source: `_ocr_sales_floor_pop` requires `checked_via_rule_id is null`, so **an entry a rule posted itself can never count as evidence for its own authority** — the floor settled at 6, not the recipe's anticipated 7, and the mechanism (not an inference) says why.

**(2) HALF 1 — ROME SECRETARY `e054b797` (BELCORT), 22 REAL sales invoices, draft-only.** **21 drafted unattended** (RM24,815.00 in draft, zero posted), each direction `sales`, corroborated FALSE (RS is sub-threshold; every invoice is tax-silent) — **7A-R3's `tier_a_fails` draft-only bypass working exactly as ruled**: tax-silent sales invoices reach a human queue and can never autopost. **1 of 22 (FINCARE) correctly HELD at `needs_review`** with `customer_name_missing`, recurring stably every ~5-minute tick and never drafting — the wall proving itself on a real document whose extraction captured no buyer name at all. 7A-R5's explicit backfill door was exercised for the one pre-watermark filing (batch size 1, self-closed 1/1). **19 approved under the owner-granted mode-A batch: TB 915,000/915,000 → 3,056,500/3,056,500 sen, reconciling to the sen (+2,141,500 = the 19 rows' exact sum); 10 customers born NAME-ONLY**, correct names, per the F3 ruling — zero buyer registrations exist anywhere in the 22 (`document_regions` read directly). **Steady-state finding, corrected against the preflight's prediction:** the draft-only terminal receipt is `counterparty_unresolved`, not `no_live_rule` — the skip fires one gate earlier because customers are born only at approval, so `v_counterparty` stays NULL for the whole draft-only phase; after the births, redrafts do return `no_live_rule`, and the single hand-door row returns `ineligible_no_coding_kind` (7A-R12's hand-drafts-are-never-autopost-eligible, holding by shape rather than by rule-absence). **The three contract fixtures, adjudicated:** #15 **PASS** (the planted FINCARE body line was ignored; `INF ADVISORY SDN BHD` extracted cleanly) · #16 **PASS** after an owner-authorized re-export — content ("LUMINOUS EVENTS SDN BHD") beat filename ("LUMINOUS SDN BHD"), exactly as the fixture was built to test; its original document's extraction failure was **transient, not content-deterministic**, and minted F6 · #12 **became the F7 finding** — the filename divergence (RESTAURANT vs RESTAURANTS) the fixture existed to probe **was never in the documents**: both PDFs' boxed bill-to blocks are identical, and both extractions captured the Attn-line person instead. **Live wall exhibits during the approval batch:** the CLR23 anti-stale guard fired **×9** (every superseded draft refused), the CLR26 open-question gate **×3** (each resolved only on a recorded basis, never waved through), and **the provenance evidence wall caught a model UUID mis-transcription EVERY time it occurred** — three refusals on the same entry, all correct.

**(3) FOUR FINDINGS MINTED — all post-close fix candidates, tasks #31–34, to be taken as ONE Wave-E batch.** **F6 (#31) — a terminally-failed FIRST extraction has no recovery path**, now with a real-document exhibit and **four doors confirmed closed**: `request_reextraction` refuses CLR16 (it only redoes a *completed* extraction) · content-addressed re-ingest **adopts** the same `document_id` and spawns no new attempt · terminal `document_processing_tasks` rows are IMMUTABLE (`_tf_processing_task_update`, no legal transition exits `failed`) · the only working path is an out-of-product re-export (new bytes, same content) — a user workaround, not a product door. This widens ADR-062's extraction-recovery-door registration from a credential-outage inference to a measured product gap. **F7 (#32) — the Attn-line person name is captured as `customer_name` over the boxed bill-to company** (X2 anchoring reads the wrong block); two RS rows are HELD pending the fix rather than approved into a wrong counterparty. **F8 (#33) — CLR23's remedy instruction is unfollowable in the unattended lane:** `clara.autodraft_attempts` is a durable filing-keyed registry (CLR08 on DELETE) and `admit_autodraft_task`'s first check short-circuits `already_done` **permanently**; `withdraw_draft` never touches it and no reset/retry/refile verb exists — so the refusal's own advice points nowhere and the real remedies are the chat and hand doors. **F9 (#34) — model-transcribed region UUIDs are fragile:** one hex group wrong (`…-4c6d-…` for the true `…-4fce-…`) refused CLR21 `evidence_invalid` on three independent attempts while every other cited region matched verbatim; the wall behaved perfectly (fail-closed, every time) — the fix shape is **citing regions by index rather than by transcribed UUID**.

**(4) OPS RECORD (ADR-060 ledger).** Three temporary limit raises, each restored and **verified in both directions**: BELCORT `daily_token_limit` 1,000,000 → 3,000,000 → NULL · BELCORT `sales_admission_daily_cap` 15 → 40 → NULL · sandbox `daily_token_limit` 1,000,000 → 2,000,000 → NULL. The owner's **2026-08-07 acceleration directive** is recorded: the pre-beta data authority reads TOTAL over partner-authorized state, while **mechanisms and process gates are unchanged and the validation posture stays ADR-061 uniform** (owner re-confirmed the same day). The standing **agent-side session-mint grant** was re-exercised for the sandbox RPA account; the password-bearing BELCORT owner mint stayed the owner's act throughout, dangerous-skip mode notwithstanding. Isolation held on every leg: zero cross-firm writes, the sandbox's protected B2 witness `d023b48c` read at every checkpoint and **still `draft`, never approved**.

**Why:** §7-A is the first wave where the product both **produces** and **consumes** an entry with no human in the loop, so the acceptance had to be evidential rather than argumentative: a claim table where every cell is a quoted receipt, a negative battery where each control is either SEEN firing or documented not-reachable with a measured reason, and a real-document half that proves the draft-only floor on books that actually matter. That posture paid for itself three times over — it caught F1 (a ship-blocking leader-loop wedge that no unit test could have produced, because the two lanes had never raced before), it converted `buyer_mismatch` from an apparent coverage gap into a stated property, and it turned three fixtures into three findings about how the product actually reads real Malaysian invoices. The floor's anti-circular exclusion and the tax-silent draft-only bypass are both now witnessed rather than asserted, which is what makes the next autonomy wave arguable at all.
